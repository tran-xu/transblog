<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>一只萨摩耶的博客</title>
  
  <subtitle>程序员技术栈</subtitle>
  <link href="https://trans-xu.github.io/transblog/atom.xml" rel="self"/>
  
  <link href="https://trans-xu.github.io/transblog/"/>
  <updated>2025-08-27T03:38:12.581Z</updated>
  <id>https://trans-xu.github.io/transblog/</id>
  
  <author>
    <name>trans-xu</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>设计模式</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</id>
    <published>2025-08-27T03:50:08.926Z</published>
    <updated>2025-08-27T03:38:12.581Z</updated>
    
    <content type="html"><![CDATA[<h1 id="设计模式-以C-为例"><a href="#设计模式-以C-为例" class="headerlink" title="设计模式(以C++为例)"></a>设计模式(以C++为例)</h1><h2 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h2><p>工厂模式：简单工厂模式、工厂方法模式、抽象工厂模式</p><h3 id="1-简单工厂模式"><a href="#1-简单工厂模式" class="headerlink" title="1.  简单工厂模式"></a>1.  简单工厂模式</h3><p>主要特点是需要在工厂类中做判断，从而创造相应的产品，当增加新产品时，需要修改工厂类。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">enum</span></span><br><span class="line">&#123;</span><br><span class="line">    T80 = <span class="number">1</span>,</span><br><span class="line">    T99</span><br><span class="line">&#125;TankType;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">message</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank80</span>:<span class="keyword">public</span> Tank</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Tank80&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank99</span>:<span class="keyword">public</span> Tank</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Tank99&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TankFactory</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">Tank* <span class="title">createTank</span><span class="params">(TankType type)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">switch</span>(type)</span><br><span class="line">        &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Tank80</span>();</span><br><span class="line">        <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Tank99</span>();</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="2-工厂方法模式"><a href="#2-工厂方法模式" class="headerlink" title="2. 工厂方法模式"></a>2. 工厂方法模式</h3><p>是指定义一个创建对象的接口，让子类决定实例化哪一个类，Factory Method使一个类的实例化延迟到其子类。</p><p>主要解决：接口选择的问题。</p><p>何时使用：我们明确地计划不同条件下创建不同实例时。</p><p>如何解决：让其子类实现工厂接口，返回的也是一个抽象的产品。</p><p>关键代码：创建过程在其子类执行。</p><p>缺点：每增加一种产品，就需要增加一个对象工厂。相比简单工厂模式，工厂方法模式需要定义更多的类。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Tank</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">message</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank80</span>:<span class="keyword">public</span> Tank</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Tank80&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank99</span>:<span class="keyword">public</span> Tank</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Tank99&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TankFactory</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Tank* <span class="title">createTank</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank80Factory</span>:<span class="keyword">public</span> TankFactory</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">Tank* <span class="title">createTank</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Tank80</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank99Factory</span>:<span class="keyword">public</span> TankFactory</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">Tank* <span class="title">createTank</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Tank99</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="3-抽象工厂模式"><a href="#3-抽象工厂模式" class="headerlink" title="3. 抽象工厂模式"></a>3. 抽象工厂模式</h3><p>提供一个创建一系列相关或相互依赖的对象接口，而无需指定它们的具体类。</p><p>主要解决：接口选择的问题。</p><p>何时使用：系统的产品有多于一个的产品族，而系统只消费其中某一族的产品。</p><p>如何解决：在一个产品族里面，定义多个产品。</p><p>关键代码：在一个工厂里聚合多个同类产品。</p><p>缺点：产品族扩展非常困难，要增加一个系列的某一产品，既要在抽象的 Creator 里加代码，又要在具体的里面加代码。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Tank</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">message</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank80</span>:<span class="keyword">public</span> Tank</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Tank80&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank99</span>:<span class="keyword">public</span> Tank</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Tank99&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Plain</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">message</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Plain80</span>: <span class="keyword">public</span> Plain</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Plain80&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Plain99</span>: <span class="keyword">public</span> Plain</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">message</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Plain99&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Factory</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Tank* <span class="title">createTank</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Plain* <span class="title">createPlain</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Factory80</span>:<span class="keyword">public</span> Factory</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">Tank* <span class="title">createTank</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Tank80</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">Plain* <span class="title">createPlain</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Plain80</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Factory99</span>:<span class="keyword">public</span> Factory</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">Tank* <span class="title">createTank</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Tank99</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">Plain* <span class="title">createPlain</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Plain99</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h2><p>策略模式是指定义一系列的算法，把它们一个个封装起来，并且使它们可以互相替换。使得算法可以独立于使用它的客户而变化，也就是说这些算法所完成的功能是一样的，对外接口是一样的，只是各自现实上存在差异。</p><p>主要解决：在有多种算法相似的情况下，使用 if…else 所带来的复杂和难以维护。</p><p>何时使用：一个系统有许多许多类，而区分它们的只是他们直接的行为。</p><p>如何解决：将这些算法封装成一个一个的类，任意地替换。</p><p>关键代码：实现同一个接口。</p><p>缺点： 1、策略类会增多。 2、所有策略类都需要对外暴露。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//传统策略模式实现</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Hurt</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">redBuff</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">AdcHurt</span>:<span class="keyword">public</span> Hurt</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">redBuff</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Adc hurt&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ApcHurt</span>:<span class="keyword">public</span> Hurt</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">redBuff</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Apc hurt&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//方法1：传入一个指针参数</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Soldier</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Soldier</span>(Hurt* hurt):<span class="built_in">m_hurt</span>(hurt)</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    ~<span class="built_in">Soldier</span>()</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">beInjured</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_hurt-&gt;<span class="built_in">redBuff</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Hurt* m_hurt;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//方法2：传入一个参数标签</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">enum</span></span><br><span class="line">&#123;</span><br><span class="line">    adc,</span><br><span class="line">    apc</span><br><span class="line">&#125;HurtType;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Master</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Master</span>(HurtType type)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">switch</span>(type)</span><br><span class="line">        &#123;</span><br><span class="line">        <span class="keyword">case</span> adc:</span><br><span class="line">            m_hurt = <span class="keyword">new</span> AdcHurt;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> apc:</span><br><span class="line">            m_hurt = <span class="keyword">new</span> ApcHurt;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            m_hurt = <span class="literal">NULL</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ~<span class="built_in">Master</span>()</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">beInjured</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(m_hurt != <span class="literal">NULL</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            m_hurt-&gt;<span class="built_in">redBuff</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;Not hurt&quot;</span> &lt;&lt; endl;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Hurt* m_hurt;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//方法3：使用模板类</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tank</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">beInjured</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_hurt.<span class="built_in">redBuff</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T m_hurt;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//END</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//使用函数指针实现策略模式</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">adcHurt</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;adc hurt:&quot;</span> &lt;&lt; num &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">apcHurt</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;apc hurt:&quot;</span> &lt;&lt; num &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//普通函数指针</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Aid</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*HurtFun)</span><span class="params">(<span class="type">int</span>)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">Aid</span>(HurtFun fun):<span class="built_in">m_fun</span>(fun)</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">beInjured</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">m_fun</span>(num);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    HurtFun m_fun;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//使用std::function , 头文件：#include&lt;functional&gt;</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Bowman</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">typedef</span> std::function&lt;<span class="type">void</span>(<span class="type">int</span>)&gt; HurtFunc;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">Bowman</span>(HurtFunc fun):<span class="built_in">m_fun</span>(fun)</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">beInjured</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">m_fun</span>(num);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    HurtFunc m_fun;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//END</span></span><br></pre></td></tr></table></figure><h2 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h2><p>适配器模式：将一个类的接口转换成客户希望的另一个接口，使得原本由于接口不兼容而不能一起工作的哪些类可以一起工作。</p><p>主要解决：主要解决在软件系统中，常常要将一些”现存的对象”放到新的环境中，而新环境要求的接口是现对象不能满足的。</p><p>何时使用： 1、系统需要使用现有的类，而此类的接口不符合系统的需要。 2、想要建立一个可以重复使用的类，用于与一些彼此之间没有太大关联的一些类，包括一些可能在将来引进的类一起工作，这些源类不一定有一致的接口。 3、通过接口转换，将一个类插入另一个类系中。（比如老虎和飞禽，现在多了一个飞虎，在不增加实体的需求下，增加一个适配器，在里面包容一个虎对象，实现飞的接口。）</p><p>如何解决：继承或依赖（推荐）。</p><p>关键代码：适配器继承或依赖已有的对象，实现想要的目标接口。</p><p>缺点：1、过多地使用适配器，会让系统非常零乱，不易整体进行把握。比如，明明看到调用的是 A 接口，其实内部被适配成了 B 接口的实现，一个系统如果太多出现这种情况，无异于一场灾难。因此如果不是很有必要，可以不使用适配器，而是直接对系统进行重构。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//使用复合，对象模式</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Deque</span>  <span class="comment">//双端队列，被适配类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push_back</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque push_back:&quot;</span> &lt;&lt; x &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push_front</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque push_front:&quot;</span> &lt;&lt; x &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop_back</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque pop_back&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop_front</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque pop_front&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Sequence</span>  <span class="comment">//顺序类，目标类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">push</span><span class="params">(<span class="type">int</span> x)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">pop</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Stack</span>:<span class="keyword">public</span> Sequence   <span class="comment">//栈, 适配类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_deque.<span class="built_in">push_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_deque.<span class="built_in">pop_back</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Deque m_deque;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Queue</span>:<span class="keyword">public</span> Sequence  <span class="comment">//队列，适配类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_deque.<span class="built_in">push_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_deque.<span class="built_in">pop_front</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Deque m_deque;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//EN</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//使用继承,类模式</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Deque</span>  <span class="comment">//双端队列，被适配类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push_back</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque push_back:&quot;</span> &lt;&lt; x &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push_front</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque push_front:&quot;</span> &lt;&lt; x &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop_back</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque pop_back&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop_front</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Deque pop_front&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Sequence</span>  <span class="comment">//顺序类，目标类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">push</span><span class="params">(<span class="type">int</span> x)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">pop</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Stack</span>:<span class="keyword">public</span> Sequence, <span class="keyword">private</span> Deque   <span class="comment">//栈, 适配类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">push_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">pop_back</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Queue</span>:<span class="keyword">public</span> Sequence, <span class="keyword">private</span> Deque  <span class="comment">//队列，适配类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">push_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">pop</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">pop_front</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//END</span></span><br></pre></td></tr></table></figure><h2 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h2><p>单例模式：保证一个类仅有一个实例，并提供一个访问它的全局访问点。</p><p>主要解决：一个全局使用的类频繁地创建与销毁。</p><p>何时使用：想控制实例数目，节省系统资源的时候。</p><p>如何解决：判断系统是否已存在单例，如果有则返回，没有则创建。</p><p>关键代码：构造函数是私有的。</p><p>单例大约有两种实现方法：懒汉与饿汉。</p><p>懒汉：故名思义，不到万不得已就不会去实例化类，也就是说在第一次用到类实例的时候才会去实例化，所以上边的经典方法被归为懒汉实现；</p><p>饿汉：饿了肯定要饥不择食。所以在单例类定义的时候就进行实例化。</p><p>特点与选择：由于要进行线程同步，所以在访问量比较大，或者可能访问的线程比较多时，采用饿汉实现，可以实现更好的性能。这是以空间换时间。</p><p>在访问量较小时，采用懒汉实现。这是以时间换空间。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//懒汉式，线程不安全</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Singleton</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">Singleton</span>();</span><br><span class="line">    ~<span class="built_in">Singleton</span>()&#123;&#125;;</span><br><span class="line">    <span class="built_in">Singleton</span>(<span class="type">const</span> Singleton&amp; obj) = <span class="keyword">delete</span>;</span><br><span class="line">    Singleton&amp; <span class="keyword">operator</span>=(<span class="type">const</span> Singleton&amp; obj) = <span class="keyword">delete</span>;</span><br><span class="line">    <span class="type">static</span> Singleton* m_pSingleton;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> Singleton* <span class="title">getInstance</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Singleton * Singleton::m_pSingleton = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line"><span class="function">Singleton*  <span class="title">Singleton::getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(m_pSingleton==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        m_pSingleton = <span class="keyword">new</span> Singleton;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> m_pSingleton;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//懒汉式，线程安全：加锁</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Singleton</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">Singleton</span>();</span><br><span class="line">    ~<span class="built_in">Singleton</span>()&#123;&#125;;</span><br><span class="line">    <span class="built_in">Singleton</span>(<span class="type">const</span> Singleton&amp; obj) = <span class="keyword">delete</span>;</span><br><span class="line">    Singleton&amp; <span class="keyword">operator</span>=(<span class="type">const</span> Singleton&amp; obj) = <span class="keyword">delete</span>;</span><br><span class="line">    <span class="type">static</span> Singleton* m_pSingleton;</span><br><span class="line">    <span class="type">static</span> std::mutex M_mutex;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> Singleton* <span class="title">getInstance</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line">Singleton * Singleton::m_pSingleton = <span class="literal">NULL</span>;</span><br><span class="line">std::mutex Singleton::M_mutex;</span><br><span class="line"></span><br><span class="line"><span class="function">Singleton*  <span class="title">Singleton::getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//  这里使用了两个 if判断语句的技术称为双检锁；好处是，只有判断指针为空的时候才加锁，</span></span><br><span class="line">    <span class="comment">//  避免每次调用 GetInstance的方法都加锁，锁的开销毕竟还是有点大的。</span></span><br><span class="line">    <span class="keyword">if</span>(m_pSingleton==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="function">std::unique_lock&lt;std::mutex&gt;  <span class="title">lock</span><span class="params">(M_mutex)</span></span>;</span><br><span class="line">        <span class="keyword">if</span> (m_pSingleton == <span class="literal">NULL</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            m_pSingleton = <span class="keyword">new</span> Singleton;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> m_pSingleton;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//饿汉式，线程安全</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Singleton</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">Singleton</span>();</span><br><span class="line">    ~<span class="built_in">Singleton</span>()&#123;&#125;;</span><br><span class="line">    <span class="built_in">Singleton</span>(<span class="type">const</span> Singleton&amp; obj) = <span class="keyword">delete</span>;</span><br><span class="line">    Singleton&amp; <span class="keyword">operator</span>=(<span class="type">const</span> Singleton&amp; obj) = <span class="keyword">delete</span>;</span><br><span class="line">    <span class="type">static</span> Singleton* m_pSingleton;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> Singleton* <span class="title">getInstance</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line">Singleton * Singleton::m_pSingleton = <span class="keyword">new</span> Singleton;</span><br><span class="line"></span><br><span class="line"><span class="function">Singleton*  <span class="title">Singleton::getInstance</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> m_pSingleton;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h2><p>原型模式：用原型实例指定创建对象的种类，并且通过拷贝这些原型创建新的对象。</p><p>主要解决：在运行期建立和删除对象。</p><p>何时使用：1).当我们的对象类型不是开始就能确定的，而这个类型是在运行期确定的话，那么我们通过这个类型的对象克隆出一个新的对象比较容易一些；2).有的时候，我们需要一个对象在某个状态下的副本，此时，我们使用原型模式是最好的选择；例如：一个对象，经过一段处理之后，其内部的状态发生了变化；这个时候，我们需要一个这个状态的副本，如果直接new一个新的对象的话,但是它的状态是不对的，此时，可以使用原型模式，将原来的对象拷贝一个出来，这个对象就和之前的对象是完全一致的了；3).当我们处理一些比较简单的对象时，并且对象之间的区别很小，可能就几个属性不同而已，那么就可以使用原型模式来完成，省去了创建对象时的麻烦了；4).有的时候，创建对象时，构造函数的参数很多，而自己又不完全的知道每个参数的意义，就可以使用原型模式来创建一个新的对象，不必去理会创建的过程。</p><p>适当的时候考虑一下原型模式，能减少对应的工作量，减少程序的复杂度，提高效率。</p><p>如何解决：利用已有的一个原型对象，快速地生成和原型对象一样的实例。</p><p>关键代码：拷贝，return new className(*this);</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Clone</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Clone</span>()</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Clone</span>()</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Clone* <span class="title">clone</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">show</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Sheep</span>:<span class="keyword">public</span> Clone</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Sheep</span>(<span class="type">int</span> id, string name):<span class="built_in">Clone</span>(),<span class="built_in">m_id</span>(id),<span class="built_in">m_name</span>(name)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Sheep() id add:&quot;</span> &lt;&lt; &amp;m_id &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Sheep() name add:&quot;</span> &lt;&lt; &amp;m_name &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    ~<span class="built_in">Sheep</span>()</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">Sheep</span>(<span class="type">const</span> Sheep&amp; obj)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;m_id = obj.m_id;</span><br><span class="line">        <span class="keyword">this</span>-&gt;m_name = obj.m_name;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Sheep(const Sheep&amp; obj) id add:&quot;</span> &lt;&lt; &amp;m_id &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Sheep(const Sheep&amp; obj) name add:&quot;</span> &lt;&lt; &amp;m_name &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function">Clone* <span class="title">clone</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Sheep</span>(*<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;id  :&quot;</span> &lt;&lt; m_id &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;name:&quot;</span> &lt;&lt; m_name.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> m_id;</span><br><span class="line">    string m_name;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Clone* s1 = <span class="keyword">new</span> <span class="built_in">Sheep</span>(<span class="number">1</span>, <span class="string">&quot;abs&quot;</span>);</span><br><span class="line">    s1-&gt;<span class="built_in">show</span>();</span><br><span class="line">    Clone* s2 = s1-&gt;<span class="built_in">clone</span>();</span><br><span class="line">    s2-&gt;<span class="built_in">show</span>();</span><br><span class="line">    <span class="keyword">delete</span> s1;</span><br><span class="line">    <span class="keyword">delete</span> s2;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="模板模式"><a href="#模板模式" class="headerlink" title="模板模式"></a>模板模式</h2><p>模板模式：定义一个操作中的算法的骨架，而将一些步骤延迟到子类中。模板方法使得子类可以不改变一个算法的结构即可重定义该算法的某些特定步骤。</p><p>主要解决：多个子类有相同的方法，并且逻辑相同，细节有差异。</p><p>如何解决：对重要，复杂的算法，将核心算法设计为模板方法，周边细节由子类实现，重构时，经常使用的方法，将相同的代码抽象到父类，通过钩子函数约束行为。</p><p>关键代码：在抽象类实现通用接口，细节变化在子类实现。</p><p>缺点：每一个不同的实现都需要一个子类来实现，导致类的个数增加，使得系统更加庞大。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Computer</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">product</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">installCpu</span>();</span><br><span class="line">        <span class="built_in">installRam</span>();</span><br><span class="line">        <span class="built_in">installGraphicsCard</span>();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span>    <span class="type">void</span> <span class="title">installCpu</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">installRam</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">installGraphicsCard</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ComputerA</span>:<span class="keyword">public</span> Computer</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">installCpu</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ComputerA install Inter Core i5&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">installRam</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ComputerA install 2G Ram&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">installGraphicsCard</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ComputerA install Gtx940 GraphicsCard&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ComputerB</span>:<span class="keyword">public</span> Computer</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">installCpu</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ComputerB install Inter Core i7&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">installRam</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ComputerB install 4G Ram&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">installGraphicsCard</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ComputerB install Gtx960 GraphicsCard&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="建造者模式"><a href="#建造者模式" class="headerlink" title="建造者模式"></a>建造者模式</h2><p>建造者模式：将复杂对象的构建和其表示分离，使得同样的构建过程可以创建不同的表示。</p><p>主要解决：一个复杂对象的创建工作，由各个部分的子对象用一定的算法构成；由于需求变化，这个复杂对象的各个部分经常面临变化，但将它们组合在一起的算法却相对稳定。</p><p>如何解决：将变与不变分开</p><p>关键代码：建造者：创建和提供实例，Director：管理建造出来的实例的依赖关系。</p><p>缺点：1、产品必须有共同点，范围有限制。 2、如内部变化复杂，会有很多的建造类。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">enum</span></span><br><span class="line">&#123;</span><br><span class="line">    type1,</span><br><span class="line">    type2</span><br><span class="line">&#125;ProductType;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Product</span>   <span class="comment">//产品</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setNum</span><span class="params">(<span class="type">int</span> num)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setColor</span><span class="params">(string color)</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setType</span><span class="params">(ProductType type)</span></span>;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showProduct</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> m_num;</span><br><span class="line">    string m_color;</span><br><span class="line">    ProductType m_type;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Product::setNum</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    m_num = num;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Product::setColor</span><span class="params">(string color)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    m_color = color;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Product::setType</span><span class="params">(ProductType type)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    m_type = type;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Product::showProduct</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Product: &quot;</span> &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;       num  : &quot;</span> &lt;&lt; m_num &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;       color: &quot;</span> &lt;&lt; m_color.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;       type : &quot;</span> &lt;&lt; m_type &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//建造者父类，定义接口</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Builder</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Builder</span>()&#123;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Builder</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">buildNum</span><span class="params">(<span class="type">int</span> num)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">buildColor</span><span class="params">(string color)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">buildType</span><span class="params">(ProductType type)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">createProduct</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Product* <span class="title">getProduct</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">show</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//建造者A</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BuilderA</span>:<span class="keyword">public</span> Builder</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">BuilderA</span>()&#123;&#125;</span><br><span class="line">     ~<span class="built_in">BuilderA</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">buildNum</span><span class="params">(<span class="type">int</span> num)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">buildColor</span><span class="params">(string color)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">buildType</span><span class="params">(ProductType type)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">createProduct</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function">Product* <span class="title">getProduct</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Product* m_product;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderA::buildNum</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderA build Num: &quot;</span> &lt;&lt; num &lt;&lt; endl;</span><br><span class="line">    m_product-&gt;<span class="built_in">setNum</span>(num);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderA::buildColor</span><span class="params">(string color)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderA build color: &quot;</span> &lt;&lt; color.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    m_product-&gt;<span class="built_in">setColor</span>(color);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderA::buildType</span><span class="params">(ProductType type)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderA build type: &quot;</span> &lt;&lt; type &lt;&lt; endl;</span><br><span class="line">    m_product-&gt;<span class="built_in">setType</span>(type);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderA::createProduct</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderA CreateProduct: &quot;</span> &lt;&lt; endl;</span><br><span class="line">    m_product = <span class="keyword">new</span> <span class="built_in">Product</span>();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function">Product* <span class="title">BuilderA::getProduct</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> m_product;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderA::show</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    m_product-&gt;<span class="built_in">showProduct</span>();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//建造者B</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BuilderB</span>:<span class="keyword">public</span> Builder</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">BuilderB</span>()&#123;&#125;</span><br><span class="line">     ~<span class="built_in">BuilderB</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">buildNum</span><span class="params">(<span class="type">int</span> num)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">buildColor</span><span class="params">(string color)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">buildType</span><span class="params">(ProductType type)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">createProduct</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function">Product* <span class="title">getProduct</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Product* m_product;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderB::buildNum</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderB build Num: &quot;</span> &lt;&lt; num &lt;&lt; endl;</span><br><span class="line">    m_product-&gt;<span class="built_in">setNum</span>(num);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderB::buildColor</span><span class="params">(string color)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderB build color: &quot;</span> &lt;&lt; color.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    m_product-&gt;<span class="built_in">setColor</span>(color);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderB::buildType</span><span class="params">(ProductType type)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderB build type: &quot;</span> &lt;&lt; type &lt;&lt; endl;</span><br><span class="line">    m_product-&gt;<span class="built_in">setType</span>(type);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderB::createProduct</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;BuilderB CreateProduct: &quot;</span> &lt;&lt; endl;</span><br><span class="line">    m_product = <span class="keyword">new</span> <span class="built_in">Product</span>();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function">Product* <span class="title">BuilderB::getProduct</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> m_product;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BuilderB::show</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    m_product-&gt;<span class="built_in">showProduct</span>();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//管理类，负责安排构造的具体过程</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Director</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Director</span>(Builder* builder):<span class="built_in">m_builder</span>(builder)</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">construct</span><span class="params">(<span class="type">int</span> num, string color, ProductType type)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_builder-&gt;<span class="built_in">createProduct</span>();</span><br><span class="line">        m_builder-&gt;<span class="built_in">buildNum</span>(num);</span><br><span class="line">        m_builder-&gt;<span class="built_in">buildColor</span>(color);</span><br><span class="line">        m_builder-&gt;<span class="built_in">buildType</span>(type);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Builder* m_builder;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="外观模式"><a href="#外观模式" class="headerlink" title="外观模式"></a>外观模式</h2><p>外观模式：为子系统中的一组接口定义一个一致的界面，外观模式提供了一个高层接口，这个接口使得这一子系统更加容易被使用；对于复杂的系统，系统为客户提供一个简单的接口，把复杂的实现过程封装起来，客户不需要了解系统内部的细节。</p><p>主要解决：客户不需要了解系统内部复杂的细节，只需要一个接口；系统入口。</p><p>如何解决：客户不直接与系统耦合，而是通过外观类与系统耦合。</p><p>关键代码：客户与系统之间加一个外观层，外观层处理系统的调用关系、依赖关系等。</p><p>缺点：需要修改时不易继承、不易修改。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Cpu</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">productCpu</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Product Cpu&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Ram</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">productRam</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Product Ram&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Graphics</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">productGraphics</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Product Graphics&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Computer</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">productComputer</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        Cpu cpu;</span><br><span class="line">        cpu.<span class="built_in">productCpu</span>();</span><br><span class="line">        Ram ram;</span><br><span class="line">        ram.<span class="built_in">productRam</span>();</span><br><span class="line">        Graphics graphics;</span><br><span class="line">        graphics.<span class="built_in">productGraphics</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//客户直接调用computer生产函数，无需关心具体部件的生产过程。也可直接单独生产部件</span></span><br><span class="line">    Computer computer;   </span><br><span class="line">    computer.<span class="built_in">productComputer</span>();</span><br><span class="line"> </span><br><span class="line">    Cpu cpu;</span><br><span class="line">    cpu.<span class="built_in">productCpu</span>();</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="组合模式"><a href="#组合模式" class="headerlink" title="组合模式"></a>组合模式</h2><p>组合模式：将对象组合成树形结构以表示“部分-整体”的层次结构，组合模式使得用户对单个对象和组合对象的使用具有一致性。</p><p>主要解决：它在我们树型结构的问题中，模糊了简单元素和复杂元素的概念，客户程序可以像处理简单元素一样处理复杂元素，从而使得客户程序与复杂元素的内部结构解耦。</p><p>如何解决：树枝和树叶实现统一接口，树枝内部组合该接口。</p><p>关键代码：树枝内部组合该接口，并且含有内部属性list，里面放Component。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Company</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Company</span>(string name):<span class="built_in">m_name</span>(name)</span><br><span class="line">    &#123;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Company</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">add</span><span class="params">(Company* company)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">remove</span><span class="params">(string name)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">display</span><span class="params">(<span class="type">int</span> depth)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function">string <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> m_name;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    string m_name;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//具体的公司</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteCompany</span>:<span class="keyword">public</span> Company   <span class="comment">//树枝</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">ConcreteCompany</span>(string name):<span class="built_in">Company</span>(name)</span><br><span class="line">    &#123;&#125;</span><br><span class="line">    ~<span class="built_in">ConcreteCompany</span>()</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;~ConcreteCompany()&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(Company* company)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(string name)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">(<span class="type">int</span> depth)</span> <span class="keyword">override</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    list&lt;shared_ptr&lt;Company&gt;&gt; m_listCompany;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ConcreteCompany::add</span><span class="params">(Company* company)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">shared_ptr&lt;Company&gt; <span class="title">temp</span><span class="params">(company)</span></span>;</span><br><span class="line">    m_listCompany.<span class="built_in">push_back</span>(temp);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ConcreteCompany::remove</span><span class="params">(string name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    list&lt;shared_ptr&lt;Company&gt;&gt;::iterator iter = m_listCompany.<span class="built_in">begin</span>();</span><br><span class="line">    <span class="keyword">for</span>(; iter != m_listCompany.<span class="built_in">end</span>(); iter++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">shared_ptr&lt;Company&gt; <span class="title">temp</span><span class="params">(*iter)</span></span>;</span><br><span class="line">        string strName = temp.<span class="built_in">get</span>()-&gt;<span class="built_in">getName</span>();</span><br><span class="line">        <span class="keyword">if</span>(name == strName)</span><br><span class="line">        &#123;</span><br><span class="line">            m_listCompany.<span class="built_in">erase</span>(iter);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ConcreteCompany::display</span><span class="params">(<span class="type">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; depth; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;-&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; m_name.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    list&lt;shared_ptr&lt;Company&gt;&gt;::iterator iter = m_listCompany.<span class="built_in">begin</span>();</span><br><span class="line">    <span class="keyword">for</span>(; iter != m_listCompany.<span class="built_in">end</span>(); iter++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">shared_ptr&lt;Company&gt; <span class="title">temp</span><span class="params">(*iter)</span></span>;</span><br><span class="line">        temp.<span class="built_in">get</span>()-&gt;<span class="built_in">display</span>(depth + <span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//公司下的部门</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FinanceDept</span>:<span class="keyword">public</span> Company    <span class="comment">//树叶</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">FinanceDept</span>(string name):<span class="built_in">Company</span>(name)</span><br><span class="line">    &#123;&#125;</span><br><span class="line">    ~<span class="built_in">FinanceDept</span>()</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;~FinanceDept()&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(Company* company)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(string name)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">(<span class="type">int</span> depth)</span> <span class="keyword">override</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FinanceDept::add</span><span class="params">(Company* company)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;FinanceDept add failed&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FinanceDept::remove</span><span class="params">(string name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;FinanceDept remove failed&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FinanceDept::display</span><span class="params">(<span class="type">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; depth; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;-&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; m_name.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//公司下的部门</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HRDept</span>:<span class="keyword">public</span> Company  <span class="comment">//树叶</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">HRDept</span>(string name):<span class="built_in">Company</span>(name)</span><br><span class="line">    &#123;&#125;</span><br><span class="line">    ~<span class="built_in">HRDept</span>()</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;~HRDept()&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(Company* company)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(string name)</span> <span class="keyword">override</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">(<span class="type">int</span> depth)</span> <span class="keyword">override</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">HRDept::add</span><span class="params">(Company* company)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;HRDept add failed&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">HRDept::remove</span><span class="params">(string name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;HRDept remove failed&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">HRDept::display</span><span class="params">(<span class="type">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; depth; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;-&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; m_name.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Company* root = <span class="keyword">new</span> <span class="built_in">ConcreteCompany</span>(<span class="string">&quot;zong&quot;</span>);</span><br><span class="line">    Company* f1 = <span class="keyword">new</span> <span class="built_in">FinanceDept</span>(<span class="string">&quot;F1&quot;</span>);</span><br><span class="line">    Company* h1 = <span class="keyword">new</span> <span class="built_in">HRDept</span>(<span class="string">&quot;H1&quot;</span>);</span><br><span class="line">    root-&gt;<span class="built_in">add</span>(f1);</span><br><span class="line">    root-&gt;<span class="built_in">add</span>(h1);</span><br><span class="line">    Company* c1 = <span class="keyword">new</span> <span class="built_in">ConcreteCompany</span>(<span class="string">&quot;fen1&quot;</span>);</span><br><span class="line">    Company* f2 = <span class="keyword">new</span> <span class="built_in">FinanceDept</span>(<span class="string">&quot;F2&quot;</span>);</span><br><span class="line">    Company* h2 = <span class="keyword">new</span> <span class="built_in">HRDept</span>(<span class="string">&quot;H2&quot;</span>);</span><br><span class="line">    c1-&gt;<span class="built_in">add</span>(f2);</span><br><span class="line">    c1-&gt;<span class="built_in">add</span>(h2);</span><br><span class="line">    root-&gt;<span class="built_in">add</span>(c1);</span><br><span class="line">    root-&gt;<span class="built_in">display</span>(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">delete</span> root;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h2><p>代理模式：为其它对象提供一种代理以控制对这个对象的访问。</p><p>主要解决：在直接访问对象时带来的问题，比如：要访问的对象在远程服务器上。在面向对象系统中，有些对象由于某些原因，直接访问会给使用者或系统带来很多麻烦，可以在访问此对象时加上一个对此对象的访问层。</p><p>如何解决：增加中间代理层。</p><p>关键代码：实现与被代理类组合。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Gril</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Gril</span>(string name = <span class="string">&quot;gril&quot;</span>):<span class="built_in">m_string</span>(name)&#123;&#125;</span><br><span class="line">    <span class="function">string <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> m_string;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string m_string;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Profession</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Profession</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">profess</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">YoungMan</span>:<span class="keyword">public</span> Profession</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">YoungMan</span>(Gril gril):<span class="built_in">m_gril</span>(gril)&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">profess</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Young man love &quot;</span> &lt;&lt; m_gril.<span class="built_in">getName</span>().<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Gril m_gril;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ManProxy</span>:<span class="keyword">public</span> Profession</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">ManProxy</span>(Gril gril):<span class="built_in">m_man</span>(<span class="keyword">new</span> <span class="built_in">YoungMan</span>(gril))&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">profess</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;I am Proxy&quot;</span> &lt;&lt; endl;</span><br><span class="line">        m_man-&gt;<span class="built_in">profess</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    YoungMan* m_man;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Gril <span class="title">gril</span><span class="params">(<span class="string">&quot;hei&quot;</span>)</span></span>; <span class="comment">//代理</span></span><br><span class="line">    Profession* proxy = <span class="keyword">new</span> <span class="built_in">ManProxy</span>(gril);</span><br><span class="line">    proxy-&gt;<span class="built_in">profess</span>();</span><br><span class="line">    <span class="keyword">delete</span> proxy;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="享元模式"><a href="#享元模式" class="headerlink" title="享元模式"></a>享元模式</h2><p>享元模式：运用共享技术有效地支持大量细粒度的对象。</p><p>主要解决：在有大量对象时，把其中共同的部分抽象出来，如果有相同的业务请求，直接返回内存中已有的对象，避免重新创建。</p><p>如何解决：用唯一标识码判断，如果内存中有，则返回这个唯一标识码所标识的对象。</p><p>关键代码：将内部状态作为标识，进行共享。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//以Money的类别作为内部标识，面值作为外部状态。</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MoneyCategory</span>   <span class="comment">//类别，内在标识，作为标识码</span></span><br><span class="line">&#123;</span><br><span class="line">    Coin,</span><br><span class="line">    bankNote</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">FaceValue</span>      <span class="comment">//面值，外部标识，需要存储的对象</span></span><br><span class="line">&#123;</span><br><span class="line">    ValueOne = <span class="number">1</span>,</span><br><span class="line">    ValueTwo</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Money</span>      <span class="comment">//抽象父类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Money</span>(MoneyCategory cate):<span class="built_in">m_mCate</span>(cate)&#123;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Money</span>()&#123; cout &lt;&lt; <span class="string">&quot;~Money() &quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">save</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    MoneyCategory m_mCate;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MoneyCoin</span>:<span class="keyword">public</span> Money    <span class="comment">//具体子类1</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MoneyCoin</span>(MoneyCategory cate):<span class="built_in">Money</span>(cate)&#123;&#125;</span><br><span class="line">    ~<span class="built_in">MoneyCoin</span>()&#123; cout &lt;&lt; <span class="string">&quot;~MoneyCoin()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">save</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Save Coin&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MoneyNote</span>:<span class="keyword">public</span> Money   <span class="comment">//具体子类2</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MoneyNote</span>(MoneyCategory cate):<span class="built_in">Money</span>(cate)&#123;&#125;</span><br><span class="line">    ~<span class="built_in">MoneyNote</span>()&#123; cout &lt;&lt; <span class="string">&quot;~MoneyNote()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">save</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Save BankNote&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Bank</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Bank</span>():<span class="built_in">m_coin</span>(<span class="literal">nullptr</span>),<span class="built_in">m_note</span>(<span class="literal">nullptr</span>),<span class="built_in">m_count</span>(<span class="number">0</span>)&#123;&#125;</span><br><span class="line">    ~<span class="built_in">Bank</span>()</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(m_coin != <span class="literal">nullptr</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">delete</span> m_coin;</span><br><span class="line">            m_coin = <span class="literal">nullptr</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(m_note != <span class="literal">nullptr</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">delete</span> m_note;</span><br><span class="line">            m_note = <span class="literal">nullptr</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">saveMoney</span><span class="params">(MoneyCategory cate, FaceValue value)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">switch</span>(cate)    <span class="comment">//以类别作为标识码</span></span><br><span class="line">        &#123;</span><br><span class="line">        <span class="keyword">case</span> Coin:</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(m_coin == <span class="literal">nullptr</span>)  <span class="comment">//内存中存在标识码所标识的对象，则直接调用，不再创建</span></span><br><span class="line">            &#123;</span><br><span class="line">                m_coin = <span class="keyword">new</span> <span class="built_in">MoneyCoin</span>(Coin);</span><br><span class="line">            &#125;</span><br><span class="line">            m_coin-&gt;<span class="built_in">save</span>();</span><br><span class="line">            m_vector.<span class="built_in">push_back</span>(value);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">case</span> bankNote:</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(m_note == <span class="literal">nullptr</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                m_note = <span class="keyword">new</span> <span class="built_in">MoneyNote</span>(bankNote);</span><br><span class="line">            &#125;</span><br><span class="line">            m_note-&gt;<span class="built_in">save</span>();</span><br><span class="line">            m_vector.<span class="built_in">push_back</span>(value);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">sumSave</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> iter = m_vector.<span class="built_in">begin</span>();</span><br><span class="line">        <span class="keyword">for</span>(; iter != m_vector.<span class="built_in">end</span>(); iter++)</span><br><span class="line">        &#123;</span><br><span class="line">            m_count += *iter;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> m_count;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    vector&lt;FaceValue&gt; m_vector;</span><br><span class="line">    Money* m_coin;</span><br><span class="line">    Money* m_note;</span><br><span class="line">    <span class="type">int</span> m_count;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Bank b1;</span><br><span class="line">    b<span class="number">1.</span><span class="built_in">saveMoney</span>(Coin, ValueOne);</span><br><span class="line">    b<span class="number">1.</span><span class="built_in">saveMoney</span>(Coin, ValueTwo);</span><br><span class="line">    b<span class="number">1.</span><span class="built_in">saveMoney</span>(Coin, ValueTwo);</span><br><span class="line">    b<span class="number">1.</span><span class="built_in">saveMoney</span>(bankNote, ValueOne);</span><br><span class="line">    b<span class="number">1.</span><span class="built_in">saveMoney</span>(bankNote, ValueTwo);</span><br><span class="line">    cout &lt;&lt; b<span class="number">1.</span><span class="built_in">sumSave</span>() &lt;&lt; endl;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="桥接模式"><a href="#桥接模式" class="headerlink" title="桥接模式"></a>桥接模式</h2><p>桥接模式：将抽象部分与实现部分分离，使它们都可以独立变换。</p><p>主要解决：在有很多中可能会变化的情况下，用继承会造成类爆炸问题，不易扩展。</p><p>如何解决：把不同的分类分离出来，使它们独立变化，减少它们之间的耦合。</p><p>关键代码：将现实独立出来，抽象类依赖现实类。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//将各种App、各种手机全部独立分开，使其自由组合桥接</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">App</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">App</span>()&#123; cout &lt;&lt; <span class="string">&quot;~App()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">run</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GameApp</span>:<span class="keyword">public</span> App</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">run</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;GameApp Running&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TranslateApp</span>:<span class="keyword">public</span> App</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">run</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;TranslateApp Running&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MobilePhone</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">MobilePhone</span>()&#123; cout &lt;&lt; <span class="string">&quot;~MobilePhone()&quot;</span> &lt;&lt; endl;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">appRun</span><span class="params">(App* app)</span> </span>= <span class="number">0</span>;  <span class="comment">//实现App与手机的桥接</span></span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">XiaoMi</span>:<span class="keyword">public</span> MobilePhone</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">appRun</span><span class="params">(App* app)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;XiaoMi: &quot;</span>;</span><br><span class="line">        app-&gt;<span class="built_in">run</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HuaWei</span>:<span class="keyword">public</span> MobilePhone</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">appRun</span><span class="params">(App* app)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;HuaWei: &quot;</span>;</span><br><span class="line">        app-&gt;<span class="built_in">run</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    App* gameApp = <span class="keyword">new</span> GameApp;</span><br><span class="line">    App* translateApp = <span class="keyword">new</span> TranslateApp;</span><br><span class="line">    MobilePhone* mi = <span class="keyword">new</span> XiaoMi;</span><br><span class="line">    MobilePhone* hua = <span class="keyword">new</span> HuaWei;</span><br><span class="line">    mi-&gt;<span class="built_in">appRun</span>(gameApp);</span><br><span class="line">    mi-&gt;<span class="built_in">appRun</span>(translateApp);</span><br><span class="line">    hua-&gt;<span class="built_in">appRun</span>(gameApp);</span><br><span class="line">    hua-&gt;<span class="built_in">appRun</span>(translateApp);</span><br><span class="line">    <span class="keyword">delete</span> hua;</span><br><span class="line">    <span class="keyword">delete</span> mi;</span><br><span class="line">    <span class="keyword">delete</span> gameApp;</span><br><span class="line">    <span class="keyword">delete</span> translateApp;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="装饰模式"><a href="#装饰模式" class="headerlink" title="装饰模式"></a>装饰模式</h2><p>装饰模式：动态地给一个对象添加一些额外的功能，就新增加功能来说，装饰器模式比生产子类更加灵活。</p><p>主要解决：通常我们为了扩展一个类经常使用继承的方式，由于继承为类引入静态特征，并且随着扩展功能的增多，子类会很膨胀。</p><p>如何解决：将具体的功能划分，同时继承装饰者类。</p><p>关键代码：装饰类复合和继承组件类，具体的扩展类重写父类的方法。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Dumplings</span>    <span class="comment">//抽象类   饺子</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Dumplings</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">showDressing</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MeatDumplings</span>:<span class="keyword">public</span> Dumplings    <span class="comment">//现实类  肉馅饺子</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">     ~<span class="built_in">MeatDumplings</span>()&#123; cout &lt;&lt; <span class="string">&quot;~MeatDumplings()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Add Meat&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DecoratorDumpling</span>:<span class="keyword">public</span> Dumplings    <span class="comment">//装饰类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">DecoratorDumpling</span>(Dumplings* d):<span class="built_in">m_dumpling</span>(d)&#123;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">DecoratorDumpling</span>()&#123; cout &lt;&lt; <span class="string">&quot;~DecoratorDumpling()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_dumpling-&gt;<span class="built_in">showDressing</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Dumplings* m_dumpling;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SaltDecorator</span>:<span class="keyword">public</span> DecoratorDumpling   <span class="comment">// 装饰类  加盐</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">SaltDecorator</span>(Dumplings* d):<span class="built_in">DecoratorDumpling</span>(d)&#123;&#125;</span><br><span class="line">    ~<span class="built_in">SaltDecorator</span>()&#123; cout &lt;&lt; <span class="string">&quot;~SaltDecorator()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        DecoratorDumpling::<span class="built_in">showDressing</span>();   <span class="comment">//注意点</span></span><br><span class="line">        <span class="built_in">addDressing</span>();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">addDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Add Salt&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">OilDecorator</span>:<span class="keyword">public</span> DecoratorDumpling   <span class="comment">//装饰类  加油</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">OilDecorator</span>(Dumplings* d):<span class="built_in">DecoratorDumpling</span>(d)&#123;&#125;</span><br><span class="line">    ~<span class="built_in">OilDecorator</span>()&#123; cout &lt;&lt; <span class="string">&quot;~OilDecorator()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        DecoratorDumpling::<span class="built_in">showDressing</span>(); <span class="comment">//注意点</span></span><br><span class="line">        <span class="built_in">addDressing</span>();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">addDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Add Oil&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CabbageDecorator</span>:<span class="keyword">public</span> DecoratorDumpling  <span class="comment">//装饰类   加蔬菜</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">CabbageDecorator</span>(Dumplings* d):<span class="built_in">DecoratorDumpling</span>(d)&#123;&#125;</span><br><span class="line">    ~<span class="built_in">CabbageDecorator</span>()&#123; cout &lt;&lt; <span class="string">&quot;~CabbageDecorator()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        DecoratorDumpling::<span class="built_in">showDressing</span>(); <span class="comment">//注意点</span></span><br><span class="line">        <span class="built_in">addDressing</span>();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">addDressing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Add Cabbage&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Dumplings* d = <span class="keyword">new</span> MeatDumplings;       <span class="comment">//原始的肉饺子</span></span><br><span class="line">    Dumplings* d1 = <span class="keyword">new</span> <span class="built_in">SaltDecorator</span>(d);   <span class="comment">//加盐后的饺子</span></span><br><span class="line">    Dumplings* d2 = <span class="keyword">new</span> <span class="built_in">OilDecorator</span>(d1);   <span class="comment">//加油后的饺子</span></span><br><span class="line">    Dumplings* d3 = <span class="keyword">new</span> <span class="built_in">CabbageDecorator</span>(d2);  <span class="comment">//加蔬菜后的饺子</span></span><br><span class="line">    d3-&gt;<span class="built_in">showDressing</span>();</span><br><span class="line">    <span class="keyword">delete</span> d;</span><br><span class="line">    <span class="keyword">delete</span> d1;</span><br><span class="line">    <span class="keyword">delete</span> d2;</span><br><span class="line">    <span class="keyword">delete</span> d3;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="备忘录模式"><a href="#备忘录模式" class="headerlink" title="备忘录模式"></a>备忘录模式</h2><p>备忘录模式：在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态。这样以后就可以将该对象恢复到原来保存的状态。</p><p>如何解决：通过一个备忘录类专门存储对象状态。</p><p>关键代码：备忘录类、客户类、备忘录管理类；客户类不与备忘录类耦合，而是与备忘录管理类耦合。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span>  <span class="comment">//需要保存的信息</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> grade;</span><br><span class="line">    string arm;</span><br><span class="line">    string corps;</span><br><span class="line">&#125;GameValue;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Memento</span>   <span class="comment">//备忘录类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Memento</span>()&#123;&#125;</span><br><span class="line">    <span class="built_in">Memento</span>(GameValue value):<span class="built_in">m_gameValue</span>(value)&#123;&#125;</span><br><span class="line">    <span class="function">GameValue <span class="title">getValue</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> m_gameValue;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    GameValue m_gameValue;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Game</span>     <span class="comment">//客户类 游戏</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Game</span>(GameValue value):<span class="built_in">m_gameValue</span>(value)</span><br><span class="line">    &#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">addGrade</span><span class="params">()</span>  <span class="comment">//等级增加</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_gameValue.grade++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">replaceArm</span><span class="params">(string arm)</span>  <span class="comment">//更换武器</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_gameValue.arm = arm;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">replaceCorps</span><span class="params">(string corps)</span>  <span class="comment">//更换工会</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_gameValue.corps = corps;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">Memento <span class="title">saveValue</span><span class="params">()</span>    <span class="comment">//保存当前信息</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="function">Memento <span class="title">memento</span><span class="params">(m_gameValue)</span></span>;</span><br><span class="line">        <span class="keyword">return</span> memento;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">load</span><span class="params">(Memento memento)</span> <span class="comment">//载入信息</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_gameValue = memento.<span class="built_in">getValue</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">showValue</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Grade: &quot;</span> &lt;&lt; m_gameValue.grade &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Arm  : &quot;</span> &lt;&lt; m_gameValue.arm.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Corps: &quot;</span> &lt;&lt; m_gameValue.corps.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    GameValue m_gameValue;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Caretake</span> <span class="comment">//备忘录管理类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">save</span><span class="params">(Memento memento)</span>  <span class="comment">//保存信息</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_memento = memento;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">Memento <span class="title">load</span><span class="params">()</span>            <span class="comment">//读已保存的信息</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> m_memento;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Memento m_memento;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    GameValue v1 = &#123;<span class="number">0</span>, <span class="string">&quot;Ak&quot;</span>, <span class="string">&quot;3K&quot;</span>&#125;;</span><br><span class="line">    <span class="function">Game <span class="title">game</span><span class="params">(v1)</span></span>;    <span class="comment">//初始值</span></span><br><span class="line">    game.<span class="built_in">addGrade</span>();</span><br><span class="line">    game.<span class="built_in">showValue</span>();</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;----------&quot;</span> &lt;&lt; endl;</span><br><span class="line">    Caretake care;</span><br><span class="line">    care.<span class="built_in">save</span>(game.<span class="built_in">saveValue</span>());  <span class="comment">//保存当前值</span></span><br><span class="line">    game.<span class="built_in">addGrade</span>();          <span class="comment">//修改当前值</span></span><br><span class="line">    game.<span class="built_in">replaceArm</span>(<span class="string">&quot;M16&quot;</span>);</span><br><span class="line">    game.<span class="built_in">replaceCorps</span>(<span class="string">&quot;123&quot;</span>);</span><br><span class="line">    game.<span class="built_in">showValue</span>();</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;----------&quot;</span> &lt;&lt; endl;</span><br><span class="line">    game.<span class="built_in">load</span>(care.<span class="built_in">load</span>());   <span class="comment">//恢复初始值</span></span><br><span class="line">    game.<span class="built_in">showValue</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="中介者模式"><a href="#中介者模式" class="headerlink" title="中介者模式"></a>中介者模式</h2><p>中介者模式：用一个中介对象来封装一系列的对象交互，中介者使各对象不需要显示地相互引用，从而使其耦合松散，而且可以独立地改变它们之前的交互。</p><p>主要解决：对象与对象之前存在大量的关联关系，这样势必会造成系统变得复杂，若一个对象改变，我们常常需要跟踪与之关联的对象，并做出相应的处理。</p><p>如何解决：将网状结构分离为星型结构。</p><p>关键代码：将相关对象的通信封装到一个类中单独处理。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Mediator</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span>   <span class="comment">//抽象同事类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Person</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">setMediator</span><span class="params">(Mediator* mediator)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_mediator = mediator;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">sendMessage</span><span class="params">(<span class="type">const</span> string&amp; message)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">getMessage</span><span class="params">(<span class="type">const</span> string&amp; message)</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Mediator* m_mediator;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Mediator</span>    <span class="comment">//抽象中介类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Mediator</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">setBuyer</span><span class="params">(Person* buyer)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">setSeller</span><span class="params">(Person* seller)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">send</span><span class="params">(<span class="type">const</span> string&amp; message, Person* person)</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Buyer</span>:<span class="keyword">public</span> Person   <span class="comment">//买家类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">sendMessage</span><span class="params">(<span class="type">const</span> string&amp; message)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_mediator-&gt;<span class="built_in">send</span>(message, <span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">getMessage</span><span class="params">(<span class="type">const</span> string&amp; message)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Buyer Get: &quot;</span> &lt;&lt; message.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Seller</span>:<span class="keyword">public</span> Person  <span class="comment">//卖家类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">sendMessage</span><span class="params">(<span class="type">const</span> string&amp; message)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_mediator-&gt;<span class="built_in">send</span>(message, <span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">getMessage</span><span class="params">(<span class="type">const</span> string&amp; message)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Seller Get: &quot;</span> &lt;&lt; message.<span class="built_in">data</span>() &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HouseMediator</span>:<span class="keyword">public</span> Mediator  <span class="comment">//具体的中介类</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">HouseMediator</span>():<span class="built_in">m_buyer</span>(<span class="literal">nullptr</span>),<span class="built_in">m_seller</span>(<span class="literal">nullptr</span>)&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setBuyer</span><span class="params">(Person* buyer)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_buyer = buyer;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setSeller</span><span class="params">(Person *seller)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        m_seller = seller;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">send</span><span class="params">(<span class="type">const</span> string&amp; message, Person* person)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(person == m_buyer)</span><br><span class="line">        &#123;</span><br><span class="line">            m_seller-&gt;<span class="built_in">getMessage</span>(message);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(person == m_seller)</span><br><span class="line">        &#123;</span><br><span class="line">            m_buyer-&gt;<span class="built_in">getMessage</span>(message);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Person* m_buyer;</span><br><span class="line">    Person* m_seller;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Person* buyer = <span class="keyword">new</span> Buyer;</span><br><span class="line">    Person* seller = <span class="keyword">new</span> Seller;</span><br><span class="line">    Mediator* houseMediator = <span class="keyword">new</span> HouseMediator;</span><br><span class="line">    buyer-&gt;<span class="built_in">setMediator</span>(houseMediator);</span><br><span class="line">    seller-&gt;<span class="built_in">setMediator</span>(houseMediator);</span><br><span class="line">    houseMediator-&gt;<span class="built_in">setBuyer</span>(buyer);</span><br><span class="line">    houseMediator-&gt;<span class="built_in">setSeller</span>(seller);</span><br><span class="line">    buyer-&gt;<span class="built_in">sendMessage</span>(<span class="string">&quot;1.5?&quot;</span>);</span><br><span class="line">    seller-&gt;<span class="built_in">sendMessage</span>(<span class="string">&quot;2!!!&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="职责链模式"><a href="#职责链模式" class="headerlink" title="职责链模式"></a>职责链模式</h2><p>职责链模式：使多个对象都有机会处理请求，从而避免请求的发送者和接收者之前的耦合关系，将这些对象连成一条链，并沿着这条链传递请求，直到有一个对象处理它为止。</p><p>主要解决：职责链上的处理者负责处理请求，客户只需要将请求发送到职责链上即可，无需关心请求的处理细节和请求的传递，所有职责链将请求的发送者和请求的处理者解耦了。</p><p>如何解决：职责链链扣类都现实统一的接口。</p><p>关键代码：Handler内指明其上级，handleRequest()里判断是否合适，不合适则传递给上级。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">RequestLevel</span></span><br><span class="line">&#123;</span><br><span class="line">    One = <span class="number">1</span>,</span><br><span class="line">    Two,</span><br><span class="line">    Three</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Leader</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Leader</span>(Leader* leader):<span class="built_in">m_leader</span>(leader)&#123;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Leader</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">handleRequest</span><span class="params">(RequestLevel level)</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Leader* m_leader;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Monitor</span>:<span class="keyword">public</span> Leader   <span class="comment">//链扣1</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Monitor</span>(Leader* leader):<span class="built_in">Leader</span>(leader)&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">handleRequest</span><span class="params">(RequestLevel level)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(level &lt; Two)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;Mointor handle request : &quot;</span> &lt;&lt; level &lt;&lt; endl;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            m_leader-&gt;<span class="built_in">handleRequest</span>(level);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Captain</span>:<span class="keyword">public</span> Leader    <span class="comment">//链扣2</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Captain</span>(Leader* leader):<span class="built_in">Leader</span>(leader)&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">handleRequest</span><span class="params">(RequestLevel level)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(level &lt; Three)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;Captain handle request : &quot;</span> &lt;&lt; level &lt;&lt; endl;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            m_leader-&gt;<span class="built_in">handleRequest</span>(level);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">General</span>:<span class="keyword">public</span> Leader   <span class="comment">//链扣3</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">General</span>(Leader* leader):<span class="built_in">Leader</span>(leader)&#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">handleRequest</span><span class="params">(RequestLevel level)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;General handle request : &quot;</span> &lt;&lt; level &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Leader* general = <span class="keyword">new</span> <span class="built_in">General</span>(<span class="literal">nullptr</span>);</span><br><span class="line">    Leader* captain = <span class="keyword">new</span> <span class="built_in">Captain</span>(general);</span><br><span class="line">    Leader* monitor = <span class="keyword">new</span> <span class="built_in">Monitor</span>(captain);</span><br><span class="line">    monitor-&gt;<span class="built_in">handleRequest</span>(Three);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h2><p>观察者模式：定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都要得到通知并自动更新。</p><p>主要解决：一个对象更新，其它对象也要更新。</p><p>如何解决：目标类通知函数通知所有观察者自动更新。</p><p>关键代码：在目标类中增加一个ArrayList来存放观察者们。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//数据模型为目标类，视图为观察者类。当数据模型发生改变时，通知视图类更新</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">View</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DataModel</span>   <span class="comment">//目标抽象类   数据模型</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">DataModel</span>()&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">add</span><span class="params">(View* view)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">remove</span><span class="params">(View* view)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">notify</span><span class="params">()</span> </span>= <span class="number">0</span>;   <span class="comment">//通知函数</span></span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">View</span>      <span class="comment">//观察者抽象类   视图</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">View</span>()&#123; cout &lt;&lt; <span class="string">&quot;~View()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">update</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">IntModel</span>:<span class="keyword">public</span> DataModel   <span class="comment">//具体的目标类， 整数模型</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ~<span class="built_in">IntModel</span>()</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(View* view)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> iter = std::<span class="built_in">find</span>(m_list.<span class="built_in">begin</span>(), m_list.<span class="built_in">end</span>(), view); <span class="comment">//判断是否重复添加</span></span><br><span class="line">        <span class="keyword">if</span>(iter == m_list.<span class="built_in">end</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            m_list.<span class="built_in">push_back</span>(view);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(View* view)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> iter = m_list.<span class="built_in">begin</span>();</span><br><span class="line">        <span class="keyword">for</span>(;iter != m_list.<span class="built_in">end</span>(); iter++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(*iter == view)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">delete</span> *iter;        <span class="comment">//释放内存</span></span><br><span class="line">                m_list.<span class="built_in">erase</span>(iter);  <span class="comment">//删除元素</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">notify</span><span class="params">()</span>  <span class="comment">//通知观察者更新</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> iter = m_list.<span class="built_in">begin</span>();</span><br><span class="line">        <span class="keyword">for</span>(; iter != m_list.<span class="built_in">end</span>(); iter++)</span><br><span class="line">        &#123;</span><br><span class="line">            (*iter)-&gt;<span class="built_in">update</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">clear</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(!m_list.<span class="built_in">empty</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">auto</span> iter = m_list.<span class="built_in">begin</span>();</span><br><span class="line">            <span class="keyword">for</span>(;iter != m_list.<span class="built_in">end</span>(); iter++)  <span class="comment">//释放内存</span></span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">delete</span> *iter;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    list&lt;View*&gt; m_list;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TreeView</span>:<span class="keyword">public</span> View  <span class="comment">//具体的观察者类   视图</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">TreeView</span>(string name):<span class="built_in">m_name</span>(name),<span class="built_in">View</span>()&#123;&#125;</span><br><span class="line">    ~<span class="built_in">TreeView</span>()&#123; cout &lt;&lt; <span class="string">&quot;~TreeView()&quot;</span> &lt;&lt; endl; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">update</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; m_name.<span class="built_in">data</span>() &lt;&lt; <span class="string">&quot; : Update&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string m_name;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    View* v1 = <span class="keyword">new</span> <span class="built_in">TreeView</span>(<span class="string">&quot;view1&quot;</span>);</span><br><span class="line">    View* v2 = <span class="keyword">new</span> <span class="built_in">TreeView</span>(<span class="string">&quot;view2&quot;</span>);</span><br><span class="line">    View* v3 = <span class="keyword">new</span> <span class="built_in">TreeView</span>(<span class="string">&quot;view3&quot;</span>);</span><br><span class="line">    View* v4 = <span class="keyword">new</span> <span class="built_in">TreeView</span>(<span class="string">&quot;view4&quot;</span>);</span><br><span class="line">    DataModel* model = <span class="keyword">new</span> IntModel;</span><br><span class="line">    model-&gt;<span class="built_in">add</span>(v1);</span><br><span class="line">    model-&gt;<span class="built_in">add</span>(v2);</span><br><span class="line">    model-&gt;<span class="built_in">add</span>(v3);</span><br><span class="line">    model-&gt;<span class="built_in">add</span>(v2);</span><br><span class="line">    model-&gt;<span class="built_in">add</span>(v4);</span><br><span class="line">    model-&gt;<span class="built_in">notify</span>(); <span class="comment">//更新所有观察者对象(View指针指向的TreeView对象)</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;----------&quot;</span> &lt;&lt; endl;</span><br><span class="line">    model-&gt;<span class="built_in">remove</span>(v2);</span><br><span class="line">    model-&gt;<span class="built_in">notify</span>(); <span class="comment">//更新所有观察者对象(View指针指向的TreeView对象)</span></span><br><span class="line">    <span class="keyword">delete</span> model;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;设计模式-以C-为例&quot;&gt;&lt;a href=&quot;#设计模式-以C-为例&quot; class=&quot;headerlink&quot; title=&quot;设计模式(以C++为例)&quot;&gt;&lt;/a&gt;设计模式(以C++为例)&lt;/h1&gt;&lt;h2 id=&quot;工厂模式&quot;&gt;&lt;a href=&quot;#工厂模式&quot; class=&quot;</summary>
      
    
    
    
    <category term="设计" scheme="https://trans-xu.github.io/transblog/categories/%E8%AE%BE%E8%AE%A1/"/>
    
    
  </entry>
  
  <entry>
    <title>计算机网络</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/</id>
    <published>2025-08-27T03:50:08.923Z</published>
    <updated>2025-08-27T03:37:29.448Z</updated>
    
    <content type="html"><![CDATA[<h1 id="OSI七层模型-TCP-IP五层网络模型"><a href="#OSI七层模型-TCP-IP五层网络模型" class="headerlink" title="OSI七层模型&amp;TCP&#x2F;IP五层网络模型"></a>OSI七层模型&amp;TCP&#x2F;IP五层网络模型</h1><p><img data-src="/transblog/images/4214940125131.png"></p><ul><li><strong>传输层，给应⽤数据前⾯增加了 TCP 头；⽹络层，给 TCP 数据包前⾯增加了 IP 头；⽹络接⼝层，给 IP 数据包前后分别增加了帧头和帧尾；</strong></li><li><strong>网络接口层的传输单位是帧（frame），IP 层的传输单位是包（packet），TCP 层的传输单位是段（segment），HTTP 的传输单位则是消息或报文（message）。</strong></li></ul><p><img data-src="/transblog/images/399031810246845.png"></p><h1 id="输入网址到网页显示，期间发生了什么？"><a href="#输入网址到网页显示，期间发生了什么？" class="headerlink" title="输入网址到网页显示，期间发生了什么？"></a>输入网址到网页显示，期间发生了什么？</h1><p><img data-src="/transblog/images/520723210259680.png"></p><h2 id="HTTP"><a href="#HTTP" class="headerlink" title="HTTP"></a>HTTP</h2><p><strong>解析URL</strong><br>当没有路径名时，就代表访问根目录下事先设置的默认文件，也就是 &#x2F;index.html 或者 &#x2F;default.html</p><p><img data-src="/transblog/images/16953510256235.png"></p><p><strong>生成HTTP请求</strong></p><p><img data-src="/transblog/images/398233710251989.png"></p><h2 id="DNS"><a href="#DNS" class="headerlink" title="DNS"></a><span id="target7">DNS</span></h2><p>在这一流程中，<strong>需要查询服务器域名对应的 IP 地址</strong>，因为委托操作系统发送消息时，必须提供通信对象的 IP 地址。在域名中，越靠右的位置表示其层级越高。</p><ul><li>根 DNS 服务器（.）</li><li>顶级域 DNS 服务器（.com）</li><li>权威 DNS 服务器（server.com）</li></ul><p><img data-src="/transblog/images/186484410269869.png"></p><p>DNS 域名解析，只指路不带路。浏览器会先看自身有没有对这个域名的缓存，如果有，就直接返回，如果没有，就去问操作系统，操作系统也会去看自己的缓存，如果有，就直接返回，如果没有，再去 hosts 文件看，也没有，才会去问「本地 DNS 服务器」。</p><p>流程示例：<br>客户端-&gt;缓存-&gt;本地DNS服务器-&gt;根域服务器(.)-&gt;顶级域名服务器(.com)-&gt;权威DNS服务器(server.com)</p><p>通过 DNS 获取到 IP 后，就可以把 HTTP 的传输工作交给操作系统中的协议栈。</p><p><img data-src="/transblog/images/589825710267473.png"></p><h2 id="TCP和UDP"><a href="#TCP和UDP" class="headerlink" title="TCP和UDP"></a>TCP和UDP</h2><p>如果 HTTP 请求消息比较长，超过了<strong>MSS(TCP 最大报文段长度)</strong> ，这时 TCP 就需要把 HTTP 的数据拆解成一块块的数据发送，而不是一次性发送所有数据。这样即使中途有一个分块丢失或损坏了，只需要重新发送这一个分块，而不用重新发送整个数据包。在 TCP 协议中，我们把每个分块称为一个 TCP 段（TCP Segment）</p><p><img data-src="/transblog/images/411031010258978.png"></p><p>至此，网络报文如图：</p><p><img data-src="/transblog/images/483730811241294.png"></p><h2 id="IP"><a href="#IP" class="headerlink" title="IP"></a>IP</h2><p><span id="target5"></span><br>网络层负责将数据从一个设备传输到另一个设备，该如何找到对方呢？因此，网络层需要有区分设备的编号。一般用 IP 地址给设备进行编号，对于 IPv4 协议， IP 地址共 32 位，分成了四段（比如，192.168.100.1），每段是 8 位。只有一个单纯的 IP 地址虽然做到了区分设备，但是寻址起来就特别麻烦，全世界那么多台设备，难道一个一个去匹配？这显然不科学。</p><p>因此，需要将 IP 地址分成两种意义：</p><ul><li>一个是网络号，负责标识该 IP 地址是属于哪个「子网」的；</li><li>一个是主机号，负责标识同一「子网」下的不同主机；</li></ul><p>那么在寻址的过程中，先匹配到相同的网络号（表示要找到同一个子网），才会去找对应的主机。</p><p>因为 HTTP 是经过 TCP 传输的，所以在 IP 包头的协议号，要填写为 06（十六进制），表示协议为 TCP</p><p><img data-src="/transblog/images/240855510268045.png"></p><p>IP 协议会将传输层的报文作为数据部分，再加上 IP 包头组装成 IP 报文，如果 IP 报文大小超过 <strong>MTU(以太网中一般为 1500 字节)</strong> 就会再次进行分片，得到一个即将发送到网络的 IP 报文。</p><p><img data-src="/transblog/images/471094610250257.png"></p><p>至此，网络报文如图：</p><p><img data-src="/transblog/images/146291011252832.png"></p><h2 id="MAC"><a href="#MAC" class="headerlink" title="MAC"></a>MAC</h2><p>生成了 IP 头部之后，接下来要交给网络接口层（Link Layer）在 IP 头部的前面加上 MAC 头部，并封装成数据帧（Data frame）发送到网络上。</p><p><img data-src="/transblog/images/442861411243551.png"></p><p>电脑上的以太网接口，Wi-Fi接口，以太网交换机、路由器上的千兆，万兆以太网口，还有网线，它们都是以太网的组成部分。所以说，网络接口层主要为网络层提供「链路级别」传输的服务，负责在以太网、WiFi 这样的底层网络上发送原始数据包，工作在网卡这个层次，使用 MAC 地址来标识网络上的设备。</p><p>IP 头部中的接收方 IP 地址表示网络包的目的地，通过这个地址我们就可以判断要将包发到哪里，但在以太网的世界中，这个思路是行不通的。以太网在判断网络包目的地时和 IP 的方式不同，因此必须采用相匹配的方式才能在以太网中将包发往目的地，而 MAC 头部就是干这个用的，所以，在以太网进行通讯要用到 MAC 地址。MAC 头部是以太网使用的头部，它包含了接收方和发送方的 MAC 地址等信息，<strong>可以通过 ARP 协议获取对方的 MAC 地址。</strong><span id="target6"></span></p><p><img data-src="/transblog/images/557811511262706.png"></p><p>ARP 协议会在以太网中以广播的形式，对以太网所有的设备喊出：“这个 IP 地址是谁的？请把你的 MAC 地址告诉我”。然后就会有人回答：“这个 IP 地址是我的，我的 MAC 地址是 XXXX”。如果对方和自己处于同一个子网中，那么通过上面的操作就可以得到对方的 MAC 地址。ARP 是借助 ARP 请求与 ARP 响应两种类型的包确定 MAC 地址的。然后，我们将这个 MAC 地址写入 MAC 头部，MAC 头部就完成了。</p><p>操作系统会把本次查询结果放到一块叫做 ARP 缓存的内存空间留着以后用，不过缓存的时间就几分钟。在发包时先查询 ARP 缓存，如果其中已经保存了对方的 MAC 地址，就不需要发送 ARP 查询，直接使用 ARP 缓存中的地址。而当 ARP 缓存中不存在对方 MAC 地址时，则发送 ARP 广播查询。</p><p>一般在 TCP&#x2F;IP 通信里，MAC 包头的协议类型只使用：0800 ： IP 协议，0806 ： ARP 协议</p><p>至此，网络报文如图：</p><p><img data-src="/transblog/images/189722011242358.png"></p><h2 id="网卡"><a href="#网卡" class="headerlink" title="网卡"></a>网卡</h2><p>网卡驱动获取网络包之后，会将其复制到网卡内的缓存区中，接着会在其开头加上报头和起始帧分界符，<strong>在末尾加上用于检测错误的帧校验序列</strong>。</p><p><img data-src="/transblog/images/291732611240804.png"></p><h2 id="交换机"><a href="#交换机" class="headerlink" title="交换机"></a>交换机</h2><p>交换机的设计是将网络包原样转发到目的地。交换机工作在 MAC 层，也称为二层网络设备。<strong>交换机的端口不具有 MAC 地址</strong>。交换机的端口不核对接收方 MAC 地址，而是直接接收所有的包并存放到缓冲区中。</p><p>交换机的 MAC 地址表主要包含两个信息：<br>一个是设备的 MAC 地址，另一个是该设备连接在交换机的哪个端口上。</p><p><img data-src="/transblog/images/352223211270622.png"></p><p>交换机根据 MAC 地址表查找 MAC 地址，然后将信号发送到相应的端口，如果交换机无法判断应该把包转发到哪个端口，只能将包转发到除了源端口之外的所有端口上，无论该设备连接在哪个端口上都能收到这个包。这样做不会产生什么问题，因为以太网的设计本来就是将包发送到整个网络的，然后只有相应的接收者才接收包，而其他设备则会忽略这个包。</p><h2 id="路由器"><a href="#路由器" class="headerlink" title="路由器"></a>路由器</h2><p>路由器是基于 IP 设计的，俗称三层网络设备，<strong>路由器的各个端口都具有 MAC 地址和 IP 地址</strong>；<br>而交换机是基于以太网设计的，俗称二层网络设备，<strong>交换机的端口不具有 MAC 地址</strong>。</p><p>路由器的端口具有 MAC 地址，因此它就能够成为以太网的发送方和接收方；同时还具有 IP 地址，从这个意义上来说，它和计算机的网卡是一样的。当转发包时，首先路由器端口会接收发给自己的以太网包，然后路由表查询转发目标，再由相应的端口作为发送方将以太网包发送出去。</p><p><strong>完成包接收操作之后，路由器就会去掉包开头的 MAC 头部</strong>。MAC 头部的作用就是将包送达路由器，其中的接收方 MAC 地址就是路由器端口的 MAC 地址。因此，当包到达路由器之后，MAC 头部的任务就完成了，于是 MAC 头部就会被丢弃。接下来，路由器会根据 MAC 头部后方的 IP 头部中的内容进行包的转发操作。</p><p><img data-src="/transblog/images/72064011249556.png"></p><p>根据路由表的网关列判断对方的地址。<br>如果网关是一个 IP 地址，则这个IP 地址就是我们要转发到的目标地址，还未抵达终点，还需继续需要路由器转发。<br>如果网关为空，则 IP 头部中的接收方 IP 地址就是要转发到的目标地址，也是就终于找到 IP 包头里的目标地址了，说明已抵达终点。</p><p>在网络包传输的过程中，<strong>源 IP 和目标 IP 始终是不会变的</strong>，一直变化的是 MAC 地址，<strong>因为需要 MAC 地址在以太网内进行两个设备之间的包传输</strong>。</p><h2 id="服务器与客户端"><a href="#服务器与客户端" class="headerlink" title="服务器与客户端"></a>服务器与客户端</h2><p><img data-src="/transblog/images/322135509240558.png"></p><h1 id="远程定位—-IP"><a href="#远程定位—-IP" class="headerlink" title="远程定位—-IP"></a>远程定位—-IP</h1><h2 id="IP地址分类"><a href="#IP地址分类" class="headerlink" title="IP地址分类"></a>IP地址分类</h2><p><img data-src="/transblog/images/159985015260239.png"></p><p>A、B、C 分类对应的地址范围、最大主机个数：</p><p><img data-src="/transblog/images/416630416255292.png"></p><p>如何计算最大主机个数，就是要看主机号的位数，C类为 2^8 - 2 &#x3D; 254，</p><p><img data-src="/transblog/images/495735315245968.png"></p><p>减去两个：主机号全为 1 指定某个网络下的所有主机，用于广播，主机号全为 0 指定某个网络</p><p><strong>缺点：</strong></p><ol><li><p>同一网络下没有地址层次，比如一个公司里用了 B 类地址，但是可能需要根据生产环境、测试环境、开发环境来划分地址层次，而这种 IP 分类是没有地址层次划分的功能，所以这就缺少地址的灵活性。</p></li><li><p>不能很好的与现实网络匹配。C 类地址能包含的最大主机数量实在太少了，只有 254 个，估计一个网吧都不够用。而 B 类地址能包含的最大主机数量又太多了，6 万多台机器放在一个网络下面，一般的企业基本达不到这个规模，闲着的地址就是浪费。</p></li></ol><h2 id="无分类地址–CIDR"><a href="#无分类地址–CIDR" class="headerlink" title="无分类地址–CIDR"></a>无分类地址–CIDR</h2><p>这种方式不再有分类地址的概念，32 比特的 IP 地址被划分为两部分，前面是网络号，后面是主机号。<a href="#target5">点击此处跳转到IP地址划分参考</a></p><p>表示形式 a.b.c.d&#x2F;x，其中 &#x2F;x 表示前 x 位属于网络号， x 的范围是 0 ~ 32，这就使得 IP 地址更加具有灵活性。比如 10.100.122.2&#x2F;24，这种地址表示形式就是 CIDR，**&#x2F;24 表示前 24 位是网络号，剩余的 8 位是主机号。**</p><p><img data-src="/transblog/images/497681016242390.png"></p><h2 id="子网掩码–VLSM"><a href="#子网掩码–VLSM" class="headerlink" title="子网掩码–VLSM"></a>子网掩码–VLSM</h2><p>子网掩码，掩码的意思就是掩盖掉主机号，剩余的就是网络号。<br>配合子网掩码才能算出 IP 地址 的网络号和主机号。10.100.122.0&#x2F;24，后面的&#x2F;24表示就是 255.255.255.0 子网掩码，255.255.255.0 二进制是「11111111-11111111-11111111-00000000」，为了简化子网掩码的表示，用&#x2F;24代替255.255.255.0。将 10.100.122.2 和 255.255.255.0 进行按位与运算，就可以得到网络号</p><p><img data-src="/transblog/images/326012516266188.png"></p><p>子网掩码还有一个作用，那就是划分子网。<strong>子网划分实际上是将主机地址分为两个部分：子网网络地址和子网主机地址</strong></p><p><img data-src="/transblog/images/93032516260433.png"></p><p><strong>网络地址 192.168.1.0，使用子网掩码 255.255.255.192 对其进行子网划分。要划分4个子网，且每个子网可容纳55台主机：</strong></p><ol><li>计算子网号：2^n−2≥55，得n &#x3D; 6 （此时可容纳62台主机，题目只需55台）,从 8 位主机号中借用 2 位作为子网号。</li></ol><p><img data-src="/transblog/images/421843316266820.png"></p><ol start="2"><li><p>由于子网网络地址被划分成 2 位，那么子网地址就有 4 个，分别是 00、01、10、11，具体划分如下图：</p></li><li><p><img data-src="/transblog/images/319113416257556.png"></p></li><li><p>最终结果如图</p></li></ol><p><img data-src="/transblog/images/45433616250415.png"></p><h2 id="IPV6"><a href="#IPV6" class="headerlink" title="IPV6"></a>IPV6</h2><p>IPv4 的地址是 32 位的，大约可以提供 42 亿个地址，但是早在 2011 年 IPv4 地址就已经被分配完了。<br>IPv6 的地址是 128 位的，IPv6 可以保证地球上的每粒沙子都能被分配到一个 IP 地址。</p><p><img data-src="/transblog/images/134550616252059.png"></p><p>IPv6 相比 IPv4 的首部改进：</p><ul><li><strong>取消了首部校验和字段</strong>。 因为在数据链路层和传输层都会校验，因此 IPv6 直接取消了 IP 的校验。</li><li><strong>取消了分片&#x2F;重新组装相关字段</strong>。 分片与重组是耗时的过程，IPv6 不允许在中间路由器进行分片与重组，这种操作只能在源与目标主机，这将大大提高了路由器转发的速度。</li><li><strong>取消选项字段</strong>。 选项字段不再是标准 IP 首部的一部分了，但它并没有消失，而是可能出现在 IPv6 首部中的「下一个首部」指出的位置上。删除该选项字段使的 IPv6 的首部成为固定长度的 40 字节。</li></ul><h2 id="DNS-1"><a href="#DNS-1" class="headerlink" title="DNS"></a>DNS</h2><p><a href="#target7">点击此处跳转到DNS</a></p><h2 id="ARP"><a href="#ARP" class="headerlink" title="ARP"></a>ARP</h2><p>ARP ⽤于根据 IP 地址查询相应的以太⽹ MAC 地址。在传输⼀个 IP 数据报的时候，确定了源 IP 地址和⽬标 IP 地址后，就会通过主机「路由表」确定 IP 数据包下⼀跳。然⽽，⽹络层的下⼀层是数据链路层，所以我们还要知道「下⼀跳」的 MAC 地址，所以可以通过 ARP 协议，求得下⼀跳的 MAC 地址。<br><a href="#target6">点击此处跳转到ARP协议</a></p><h2 id="DHCP"><a href="#DHCP" class="headerlink" title="DHCP"></a>DHCP</h2><p>路由器常用</p><p><img data-src="/transblog/images/500414010271844.png"></p><p>一旦客户端收到 DHCP ACK 后，交互便完成了，并且客户端能够在租用期内使用 DHCP 服务器分配的 IP 地址。可以发现，DHCP 交互中，全程都是使用 UDP 广播通信。</p><p>如果租约的 DHCP IP 地址快期后，客户端会向服务器发送 DHCP 请求报文：</p><ul><li>服务器如果同意继续租用，则用 DHCP ACK 报文进行应答，客户端就会延长租期。</li><li>服务器如果不同意继续租用，则用 DHCP NACK 报文，客户端就要停止使用租约的 IP 地址。</li></ul><h2 id="NAT"><a href="#NAT" class="headerlink" title="NAT"></a>NAT</h2><p>简单的来说 NAT 就是同个公司、家庭、教室内的主机对外部通信时，把私有 IP 地址转换成公有 IP 地址。<br>普通的 NAT 转换没什么意义。由于绝大多数的网络应用都是使用传输层协议 TCP 或 UDP 来传输数据的。因此，可以把 IP 地址 + 端口号一起进行转换（网络地址与端口转换 NAPT）。</p><p><img data-src="/transblog/images/212873810264333.png"></p><p>图中有两个客户端 192.168.1.10 和 192.168.1.11 同时与服务器 183.232.231.172 进行通信，并且这两个客户端的本地端口都是 1025。此时，两个私有 IP 地址都转换 IP 地址为公有地址 120.229.175.121，但是以不同的端口号作为区分。</p><h2 id="ICMP"><a href="#ICMP" class="headerlink" title="ICMP"></a>ICMP</h2><p>互联⽹控制报⽂协议。ICMP 主要的功能包括：<strong>确认 IP 包是否成功送达⽬标地址、报告发送过程中 IP 包被废弃的原因和改善⽹络设置等。</strong></p><p>ICMP ⼤致可以分为两⼤类：</p><ol><li>⼀类是⽤于诊断的查询消息，也就是「查询报⽂类型」</li><li>另⼀类是通知出错原因的错误消息，也就是「差错报⽂类型」</li></ol><p><img data-src="/transblog/images/5537494615604.png"></p><p>在 IP 通信中如果某个 IP 包因为某种原因未能达到目标地址，那么这个具体的原因将由 ICMP 负责通知。</p><p><img data-src="/transblog/images/153575516269224.png"></p><h2 id="ping的工作原理"><a href="#ping的工作原理" class="headerlink" title="ping的工作原理"></a>ping的工作原理</h2><p>ping 命令执⾏的时候，源主机⾸先会构建⼀个 ICMP<br>ICMP 数据包内包含多个字段，最重要的是两个：</p><ul><li>第⼀个是类型，对于回送请求消息⽽⾔该字段为 8 ；</li><li>另外⼀个是序号，主要⽤于区分连续 ping 的时候发出的多个数据包。<br>每发出⼀个请求数据包，序号会⾃动加 1 。为了能够计算往返时间 RTT ，它会在报⽂的数据部分插⼊发送时间。<br><img data-src="/transblog/images/1489291806710.png"><br><img data-src="/transblog/images/3501300595802.png"></li></ul><p><img data-src="/transblog/images/1454719219187.png"></p><h1 id="可靠传输—-TCP和UDP"><a href="#可靠传输—-TCP和UDP" class="headerlink" title="可靠传输—-TCP和UDP"></a>可靠传输—-TCP和UDP</h1><p><strong>UDP与TCP比较</strong></p><table><thead><tr><th align="center">特点</th><th align="center">TCP</th><th align="center">UDP</th></tr></thead><tbody><tr><td align="center">连接</td><td align="center">面向连接</td><td align="center">不需要连接，即刻传输</td></tr><tr><td align="center">服务对象</td><td align="center">一对一</td><td align="center">一对一、一对多、多对多</td></tr><tr><td align="center">可靠性</td><td align="center">可靠交付数据，数据可以无差错、不丢失、不重复、按序到达</td><td align="center">尽最大努力交付数据，不保证可靠交付数据</td></tr><tr><td align="center">拥塞控制、流量控制</td><td align="center">有</td><td align="center">没有</td></tr><tr><td align="center">首部开销</td><td align="center">没有使用「选项」字段时是 20 个字节，较长</td><td align="center">8 个字节，较小</td></tr><tr><td align="center">传输方式</td><td align="center">流式传输,保证顺序和可靠</td><td align="center">一个包一个包的发送，有边界，可能丢包和乱序</td></tr><tr><td align="center">分片不同</td><td align="center">大于MSS大小，在传输层进行分片，丢失了一个分片，只需要传输丢失的这个分片</td><td align="center">大于 MTU 大小在IP层进行分片</td></tr></tbody></table><p><strong>TCP与UDP服务端的不同之处:</strong></p><ol><li><p>Socket类型：<br>TCP使用SOCK_STREAM类型，而UDP使用SOCK_DGRAM类型。</p></li><li><p>连接方式：<br>TCP是面向连接的，需要通过connect、listen和accept函数建立连接。<br>UDP是无连接的，不需要建立连接，直接使用sendto和recvfrom函数发送和接收数据。</p></li><li><p>数据传输：<br>TCP提供可靠的、有序的数据传输，保证数据完整性。<br>UDP提供不可靠的、无序的数据传输，可能丢包，但速度更快。</p></li><li><p>并发处理：<br>TCP服务端通常需要为每个客户端连接创建一个线程或使用非阻塞I&#x2F;O来处理并发。<br>UDP服务端通常在主循环中使用recvfrom和sendto处理所有客户端的数据，不需要为每个客户端创建线程（UDP服务端示例代码中没有使用线程，因为UDP是无连接的，通常不需要为每个客户端创建线程。如果需要处理大量并发UDP客户端，可以考虑使用线程池或非阻塞I&#x2F;O）。</p></li><li><p>错误处理：<br>TCP在建立连接时会进行错误处理，如连接失败会返回错误。<br>UDP在发送和接收数据时可能会遇到错误，如目标不可达等，需要在发送和接收时处理。</p></li><li><p>资源消耗：<br>TCP由于需要维护连接状态，资源消耗相对较高。<br>UDP不需要维护连接状态，资源消耗相对较低。</p></li><li><p>适用场景：<br>TCP适用于需要可靠传输的场景，如文件传输、网页浏览等。<br>UDP适用于对实时性要求高的场景，如视频会议、在线游戏等。</p></li></ol><h2 id="bind作用"><a href="#bind作用" class="headerlink" title="bind作用"></a>bind作用</h2><p>bind()函数用于将socket()函数<strong>返回的文件描述符绑定addr 指定的地址</strong>，传统上此操作称为“为套接字分配名称”</p><p><strong>那么客户端可以调用bind吗</strong>？<br>服务器和客户端都可以bind。但是 一般情况下客户端是不用调用bind函数的，一切都交给内核。</p><ul><li><p>UDP的客户端和服务端以及TCP的服务端可以通过调用bind函数来配置本地信息。使用bind函数时，通过将my_addr.sin_port置为0，函数会自动为你选择一个未占用的端口来使用。</p></li><li><p>TCP的客户端一般无须调用bind()，它调用Connect函数在socket数据结构中保存本地和远端信息。因为这种情况下只需知道目的机器的IP地址，而客户通过哪个端口与服务器建立连接并不需要关心，socket执行体为你的程序自动选择一个未被占用的端口，并通知你的程序数据什么时候打开端口。（当然也有特殊情况，linux系统中rlogin命令应当调用bind函数绑定一个未用的保留端口号，还有当客户端需要用指定的网络设备接口和端口号进行通信等等）</p></li></ul><p>客户端与服务端调用bind区别：</p><ul><li>客户端进程bind端口：<br>由进程选择一个端口去连服务器，（如果默认情况下，调用bind函数时，内核指定的端口是同一个，那么调用多个调用了bind的client程序，会出现端口被占用的错误）注意这里的端口是客户端的端口。如果不分配就表示交给内核去选择一个可用端口。</li><li>客户端进程bind IP地址：<br>相当于为发送出去的IP数据报分配了源IP地址，但交给进程分配IP地址的时候（就是这样写明了bind IP地址的时候）这个IP地址必须是主机的一个接口，不能分配一个不存在的IP。如果不分配就表示由内核根据所用的输出接口来选择源IP地址。</li><li>服务端进程bind端口:<br>基本是必须要做的事情，比如一个服务器启动时（比如freebsd），它会一个一个的捆绑众所周知的端口来提供服务，同样，如果bind了一个端口就表示我这个服务器会在这个端口提供一些“特殊服务”。</li><li>服务端进程bind IP地址：<br>目的是限制了服务端进程创建的socket只接受那些目的地为此IP地址的客户链接，一般一个服务器程序里都有<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">## UDP</span><br><span class="line">### UDP头部</span><br><span class="line">![](/images/1340921605603.png)</span><br><span class="line">1. ⽬标和源端⼝：主要是告诉 UDP 协议应该把报⽂发给哪个进程。</span><br><span class="line">2. 包⻓度：该字段保存了 UDP ⾸部的⻓度跟数据的⻓度之和。</span><br><span class="line">3. 校验和：校验和是为了提供可靠的 UDP 首部和数据而设计，防止收到在网络传输中受损的 UDP 包。</span><br><span class="line"></span><br><span class="line">### UDP特点</span><br><span class="line">1. **面向无连接**</span><br><span class="line">首先 UDP 是不需要和 TCP一样在发送数据前进行三次握手建立连接的，想发数据就可以开始发送了。并且也只是数据报文的搬运工，不会对数据报文进行任何拆分和拼接操作。</span><br><span class="line">在发送端，应用层将数据传递给传输层的 UDP 协议，UDP 只会给数据增加一个 UDP 头标识下是 UDP 协议，然后就传递给网络层了</span><br><span class="line">在接收端，网络层将数据传递给传输层，UDP 只去除 IP 报文头就传递给应用层，不会任何拼接操作</span><br><span class="line">2. **有单播，多播，广播的功能**</span><br><span class="line">UDP 不止支持一对一的传输方式，同样支持一对多，多对多，多对一的方式，也就是说 UDP 提供了单播，多播，广播的功能。</span><br><span class="line">3. **UDP是面向报文的**</span><br><span class="line">发送方的UDP对应用程序交下来的报文，在添加首部后就向下交付IP层。UDP对应用层交下来的报文，既不合并，也不拆分，而是保留这些报文的边界。因此，应用程序必须选择合适大小的报文</span><br><span class="line">4. **不可靠性**</span><br><span class="line">首先不可靠性体现在无连接上，通信都不需要建立连接，想发就发，这样的情况肯定不可靠。并且收到什么数据就传递什么数据，并且也不会备份数据，发送数据也不会关心对方是否已经正确接收到数据了。</span><br><span class="line">再者网络环境时好时坏，但是 UDP 因为没有拥塞控制，一直会以恒定的速度发送数据。即使网络条件不好，也不会对发送速率进行调整。这样实现的弊端就是在网络条件不好的情况下可能会导致丢包，但是优点也很明显，在某些实时性要求高的场景（比如电话会议）就需要使用 UDP 而不是 TCP。</span><br><span class="line">5. **头部开销小，传输数据报文时是很高效的**</span><br><span class="line">因此 UDP 的头部开销小，只有八字节，相比 TCP 的至少二十字节要少得多，在传输数据报文时是很高效的</span><br><span class="line"></span><br><span class="line">### UDP网络编程</span><br><span class="line">![](/images/249403104840499.png)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#### UDP服务器代码</span><br><span class="line">```C++</span><br><span class="line">#include &lt;iostream&gt;</span><br><span class="line">#include &lt;cstring&gt;</span><br><span class="line">#include &lt;unistd.h&gt;</span><br><span class="line">#include &lt;arpa/inet.h&gt;</span><br><span class="line"> </span><br><span class="line">#define PORT 8080</span><br><span class="line">#define BUFFER_SIZE 1024</span><br><span class="line"> </span><br><span class="line">int main() &#123;</span><br><span class="line">    int sockfd;</span><br><span class="line">    struct sockaddr_in server_addr, client_addr;</span><br><span class="line">    char buffer[BUFFER_SIZE];</span><br><span class="line">    const char* message = &quot;Hello from server&quot;;</span><br><span class="line"> </span><br><span class="line">    // 创建套接字</span><br><span class="line">    if ((sockfd = socket(AF_INET, SOCK_DGRAM, 0)) &lt; 0) &#123;</span><br><span class="line">        perror(&quot;socket creation failed&quot;);</span><br><span class="line">        exit(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    memset(&amp;server_addr, 0, sizeof(server_addr));</span><br><span class="line">    memset(&amp;client_addr, 0, sizeof(client_addr));</span><br><span class="line"> </span><br><span class="line">    // 填充服务器信息</span><br><span class="line">    server_addr.sin_family = AF_INET;</span><br><span class="line">    server_addr.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line">    server_addr.sin_port = htons(PORT);</span><br><span class="line"> </span><br><span class="line">    // 绑定套接字</span><br><span class="line">    if (bind(sockfd, (const struct sockaddr *)&amp;server_addr, sizeof(server_addr)) &lt; 0) &#123;</span><br><span class="line">        perror(&quot;bind failed&quot;);</span><br><span class="line">        close(sockfd);</span><br><span class="line">        exit(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    socklen_t len = sizeof(client_addr);</span><br><span class="line">    int n = recvfrom(sockfd, (char *)buffer, BUFFER_SIZE, MSG_WAITALL, (struct sockaddr *)&amp;client_addr, &amp;len);</span><br><span class="line">    buffer[n] = &#x27;\0&#x27;;</span><br><span class="line">    std::cout &lt;&lt; &quot;Message received: &quot; &lt;&lt; buffer &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    sendto(sockfd, message, strlen(message), MSG_CONFIRM, (const struct sockaddr *) &amp;client_addr, len);</span><br><span class="line">    std::cout &lt;&lt; &quot;Hello message sent&quot; &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    close(sockfd);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="UDP客户端代码"><a href="#UDP客户端代码" class="headerlink" title="UDP客户端代码"></a>UDP客户端代码</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8080</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFER_SIZE 1024</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> sockfd;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> serv_addr;</span><br><span class="line">    <span class="type">char</span> buffer[BUFFER_SIZE];</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* message = <span class="string">&quot;Hello from client&quot;</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 创建套接字</span></span><br><span class="line">    <span class="keyword">if</span> ((sockfd = <span class="built_in">socket</span>(AF_INET, SOCK_DGRAM, <span class="number">0</span>)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;socket creation failed&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="built_in">sizeof</span>(serv_addr));</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 填充服务器信息</span></span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_port = <span class="built_in">htons</span>(PORT);</span><br><span class="line">    serv_addr.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">sendto</span>(sockfd, message, <span class="built_in">strlen</span>(message), MSG_CONFIRM, (<span class="type">const</span> <span class="keyword">struct</span> sockaddr *) &amp;serv_addr, <span class="built_in">sizeof</span>(serv_addr));</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Hello message sent&quot;</span> &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="type">socklen_t</span> len = <span class="built_in">sizeof</span>(serv_addr);</span><br><span class="line">    <span class="type">int</span> n = <span class="built_in">recvfrom</span>(sockfd, (<span class="type">char</span> *)buffer, BUFFER_SIZE, MSG_WAITALL, (<span class="keyword">struct</span> sockaddr *) &amp;serv_addr, &amp;len);</span><br><span class="line">    buffer[n] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Message received: &quot;</span> &lt;&lt; buffer &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">close</span>(sockfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="TCP"><a href="#TCP" class="headerlink" title="TCP"></a>TCP</h2><h3 id="TCP头部"><a href="#TCP头部" class="headerlink" title="TCP头部"></a>TCP头部</h3><p><img data-src="/transblog/images/2092909816511.png"></p><ol><li>序列号：在建⽴连接时由计算机⽣成的随机数作为其初始值，通过 SYN 包传给接收端主机，每发送⼀次数据，就「累加」⼀次该「数据字节数」的⼤⼩。⽤来解决⽹络包乱序问题。</li><li>确认应答号：指下⼀次「期望」收到的数据的序列号，发送端收到这个确认应答以后可以认为在这个序号以前的数据都已经被正常接收。⽤来解决丢包的问题。</li><li>头部长度:标识该TCP头部有多少个32bit字(4字节)。4位最大能表示15，所以TCP头部最长是60字节。</li><li>窗口大小：是TCP流量控制的一个手段。它告诉对方本段的TCP接受缓冲区的情况，控制对方的发送的速度。</li><li>校验和：由发送端填充，接收端对TCP报文端执行CRC算法以校验TCP报文段在传输过程是否损坏。(数据和头部全部校验的。)</li><li>紧急指针：发送端向接受端发送紧急数据使用的。</li><li>控制位：</li></ol><ul><li>URG：表示紧急指针是否有效</li><li>ACK：该位为 1 时，「确认应答」的字段变为有效，TCP 规定除了最初建⽴连接时的 SYN 包之外该位必须设置为 1 。</li><li>RST：该位为 1 时，表示 TCP 连接中出现异常必须强制断开连接</li><li>PSH：提示接收端应用程序应该立即从TCP接受缓冲区读走数据，为之后的接受的数据腾出位置。</li><li>SYN：该位为 1 时，表示希望建⽴连接，并在其「序列号」的字段进⾏序列号初始值的设定。</li><li>FIN：该位为 1 时，表示今后不会再有数据发送，希望断开连接。当通信结束希望断开连接时，通信双⽅的主机之间就可以相互交换 FIN 位为 1 的 TCP 段。</li></ul><h3 id="TCP特点"><a href="#TCP特点" class="headerlink" title="TCP特点"></a>TCP特点</h3><ol><li><strong>面向连接</strong><br>面向连接，是指发送数据之前必须在两端建立连接。建立连接的方法是“三次握手”，这样能建立可靠的连接。建立连接，是为数据的可靠传输打下了基础。</li><li><strong>仅支持单播传输</strong><br>每条TCP传输连接只能有两个端点，只能进行点对点的数据传输，不支持多播和广播传输方式。</li><li><strong>面向字节流</strong><br>TCP不像UDP一样那样一个个报文独立地传输，而是在不保留报文边界的情况下以字节流方式进行传输。<br>消息是「没有边界」的，所以⽆论我们消息有多⼤都可以进⾏传输。并且消息是「有序的」，当「前⼀个」消息没有收到的时候，即使它先收到了后⾯的字节，那么也不能扔给应⽤层去处理，同时对「重复」的报⽂会⾃动丢弃。</li><li><strong>可靠传输</strong><br>对于可靠传输，判断丢包，误码靠的是TCP的段编号以及确认号。TCP为了保证报文传输的可靠，就给每个包一个序号，同时序号也保证了传送到接收端实体的包的按序接收。然后接收端实体对已成功收到的字节发回一个相应的确认(ACK)；如果发送端实体在合理的往返时延(RTT)内未收到确认，那么对应的数据（假设丢失了）将会被重传。</li><li><strong>提供拥塞控制</strong><br>当网络出现拥塞的时候，TCP能够减小向网络注入数据的速率和数量，缓解拥塞</li><li><strong>TCP提供全双工通信</strong><br>TCP允许通信双方的应用程序在任何时候都能发送数据，因为TCP连接的两端都设有缓存，用来临时存放双向通信的数据。当然，TCP可以立即发送一个数据段，也可以缓存一段时间以便一次发送更多的数据段（最大的数据段大小取决于MSS）</li></ol><h3 id="TCP四元组"><a href="#TCP四元组" class="headerlink" title="TCP四元组"></a>TCP四元组</h3><p><strong>TCP 四元组可以唯一的确定一个连接</strong>，四元组包括如下：<br>源地址、源端口、目的地址、目的端口</p><p>源地址和目的地址的字段（32 位）是在 IP 头部中，作用是通过 IP 协议发送报文给对方主机。<br>源端口和目的端口的字段（16 位）是在 TCP 头部中，作用是告诉 TCP 协议应该把报文发给哪个进程。</p><h3 id="TCP三次握手"><a href="#TCP三次握手" class="headerlink" title="TCP三次握手"></a>TCP三次握手</h3><p><img data-src="/transblog/images/21412016243506.png"></p><ol><li>服务端主动监听某个端口，处于 LISTEN 状态</li><li>客户端会随机初始化序号（client_isn），将此序号置于 TCP 首部的「序号」字段中，同时把 SYN 标志位置为 1，表示 SYN 报文。接着把第一个 SYN 报文发送给服务端，表示向服务端发起连接，该报文不包含应用层数据，之后客户端处于 SYN-SENT 状态。</li></ol><p><img data-src="/transblog/images/232160215268656.png"></p><ol start="3"><li>服务端收到客户端的 SYN 报文后，首先服务端也随机初始化自己的序号（server_isn），将此序号填入 TCP 首部的「序号」字段中，其次把 TCP 首部的「确认应答号」字段填入 client_isn + 1, 接着把 SYN 和 ACK 标志位置为 1。最后把该报文发给客户端，该报文也不包含应用层数据，之后服务端处于 SYN-RCVD 状态。</li></ol><p><img data-src="/transblog/images/391953015263792.png"></p><ol start="4"><li>客户端收到服务端报文后，还要向服务端回应最后一个应答报文，首先该应答报文 TCP 首部 ACK 标志位置为 1 ，其次「确认应答号」字段填入 server_isn + 1 ，最后把报文发送给服务端，这次报文可以携带客户到服务端的数据，之后客户端处于 ESTABLISHED 状态。</li></ol><p><img data-src="/transblog/images/354023115257338.png"></p><ol start="5"><li>服务端收到客户端的应答报文后，也进入 ESTABLISHED 状态。</li></ol><p>从上面的过程可以发现第三次握手是可以携带数据的，前两次握手是不可以携带数据的。</p><h4 id="为什么不是两次握手或四次握手？"><a href="#为什么不是两次握手或四次握手？" class="headerlink" title="为什么不是两次握手或四次握手？"></a>为什么不是两次握手或四次握手？</h4><h5 id="防止旧的重复连接初始化造成混乱-主要原因）"><a href="#防止旧的重复连接初始化造成混乱-主要原因）" class="headerlink" title="防止旧的重复连接初始化造成混乱(主要原因）"></a>防止旧的重复连接初始化造成混乱(主要原因）</h5><p>若客户端向服务端发送的连接请求丢失，客户端等待应答超时后就会再次发送连接请求，此时，<strong>上一个连接请求就是失效的</strong>。<br>如果是历史连接（序列号过期或超时），则第三次握⼿发送的报⽂是 RST 报⽂，以此中⽌历史连接；</p><p><img data-src="/transblog/images/504923715250472.png"></p><p>如果采用两次握手建立 TCP 连接的场景下，服务端在向客户端发送数据前，并没有阻止掉历史连接，导致服务端建立了一个历史连接，又白白发送了数据，妥妥地浪费了服务端的资源。因此，最好就是在服务端发送数据前，也就是建立连接之前，要阻止掉历史连接:</p><h5 id="同步双⽅的初始序列号"><a href="#同步双⽅的初始序列号" class="headerlink" title="同步双⽅的初始序列号"></a>同步双⽅的初始序列号</h5><p>当客户端发送携带「初始序列号」的 SYN 报⽂的时候，需要服务端回⼀个 ACK 应答报⽂，表示客户端的 SYN 报⽂已被服务端成功接收，那当服务端发送「初始序列号」给客户端的时候，依然也要得到客户端的应答回应，这样⼀来⼀回，才能确保双⽅的初始序列号能被可靠的同步。</p><p>序列号是可靠传输的一个关键因素，它的作用：</p><ul><li>接收方可以去除重复的数据；</li><li>接收方可以根据数据包的序列号按序接收；</li><li>可以标识发送出去的数据包中， 哪些是已经被对方收到的（通过 ACK 报文中的序列号知道）；</li></ul><p><img data-src="/transblog/images/323262616252453.png"></p><p>四次握手其实也能够可靠的同步双方的初始化序号，但由于第二步和第三步可以优化成一步，所以就成了「三次握手」。而两次握手只保证了一方的初始序列号能被对方成功接收，没办法保证双方的初始序列号都能被确认接收。</p><h5 id="避免资源浪费"><a href="#避免资源浪费" class="headerlink" title="避免资源浪费"></a>避免资源浪费</h5><p>假设采用“两次握手”，服务端重复接受无用的连接请求 SYN 报文。服务端不清楚客户端是否收到了自己发送的建立连接的 ACK 确认报文，所以每收到一个 SYN 就只能先主动建立一个连接，而造成重复分配资源</p><h4 id="第一次握手丢失"><a href="#第一次握手丢失" class="headerlink" title="第一次握手丢失"></a>第一次握手丢失</h4><p>如果客户端迟迟收不到服务端的 SYN-ACK 报文（第二次握手），就会触发「超时重传」机制，重传 SYN 报文，而且重传的 SYN 报文的序列号都是一样的。</p><p>在 Linux 里，客户端的 SYN 报文最大重传次数由 tcp_syn_retries内核参数控制，这个参数是可以自定义的，默认值一般是 5。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash"><span class="built_in">cat</span> /proc/sys/net/ipv4/tcp_syn_retries</span></span><br><span class="line">5</span><br></pre></td></tr></table></figure><p>通常，第一次超时重传是在 1 秒后，第二次超时重传是在 2 秒，第三次超时重传是在 4 秒后，第四次超时重传是在 8 秒后，第五次是在超时重传 16 秒后。没错，每次超时的时间是上一次的 2 倍。</p><h4 id="第二次握手丢失"><a href="#第二次握手丢失" class="headerlink" title="第二次握手丢失"></a>第二次握手丢失</h4><p>第二次握手的 SYN-ACK 报文其实有两个目的 ：</p><ul><li>第二次握手里的 ACK， 是对第一次握手的确认报文；</li><li>第二次握手里的 SYN，是服务端发起建立 TCP 连接的报文；</li></ul><p>如果客户端迟迟没有收到第二次握手，那么客户端就觉得可能自己的 SYN 报文（第一次握手）丢失了，于是客户端就会触发超时重传机制，重传 SYN 报文。</p><p>同时，服务端就收不到第三次握手，于是服务端这边会触发超时重传机制，重传 SYN-ACK 报文。在 Linux 下，SYN-ACK 报文的最大重传次数由 tcp_synack_retries内核参数决定，默认值是 5。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash"><span class="built_in">cat</span> /proc/sys/net/ipv4/tcp_synack_retries</span></span><br><span class="line">5</span><br></pre></td></tr></table></figure><p><strong>因此，当第二次握手丢失了，客户端和服务端都会重传</strong></p><h4 id="第三次握手丢失"><a href="#第三次握手丢失" class="headerlink" title="第三次握手丢失"></a>第三次握手丢失</h4><p>第三次握手的 ACK 是对第二次握手的 SYN 的确认报文，所以当第三次握手丢失了，如果服务端那一方迟迟收不到这个确认报文，就会触发超时重传机制，重传 SYN-ACK 报文，直到收到第三次握手，或者达到最大重传次数。<br><strong>注意，ACK 报文是不会有重传的，当 ACK 丢失了，就由对方重传对应的报文。</strong></p><h4 id="TCP-半连接和全连接队列"><a href="#TCP-半连接和全连接队列" class="headerlink" title="TCP 半连接和全连接队列"></a>TCP 半连接和全连接队列</h4><p>在 TCP 三次握手的时候，Linux 内核会维护两个队列，分别是：</p><ul><li>半连接队列，也称 SYN 队列；</li><li>全连接队列，也称 Accept 队列；</li></ul><p>其中全连接队列<code>icsk_accept_queue</code>是一个链表，而半连接队列由于需要快速的查找，所以使用的是一个哈希表<code>syn_table</code>。</p><p>服务端收到客户端发起的 SYN 请求后，内核会把该连接存储到半连接队列，并向客户端响应 SYN+ACK，接着客户端会返回 ACK，服务端收到第三次握⼿的 ACK 后，内核会把连接从半连接队列移除，然后创建新的完全的连接，并将其添加到 accept 队列，等待进程调⽤ accept 函数时把连接取出来。</p><p><img data-src="/transblog/images/137551216255655.png"></p><h5 id="全连接队列满了"><a href="#全连接队列满了" class="headerlink" title="全连接队列满了"></a>全连接队列满了</h5><p>查看全连接队列长度：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">ss -lnt</span></span><br></pre></td></tr></table></figure><p>如果想知道客户端连接不上服务端，是不是服务端 TCP 全连接队列满的原因，那么可以把 tcp_abort_on_overflow 设置为 1，此时在客户端异常中看到很多 connection reset by peer 的错误，那么就可以证明是全连接队列满。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash"><span class="built_in">cat</span> /proc/sys/net/ipv4/tcp_abort_on-overflow</span></span><br><span class="line">0    #默认值为0</span><br></pre></td></tr></table></figure><ul><li>0 ：如果全连接队列满了，那么 server 扔掉 client 发过来的 ack ；</li><li>1 ：如果全连接队列满了，server 发送一个 reset 包（RST）给 client，表示废掉这个握手过程和这个连接；</li></ul><p><strong>但是通常情况下，应当把 tcp_abort_on_overflow 设置为 0，因为这样更有利于应对突发流量</strong>。如下面这种情况：</p><ol><li>Client 端向 Server 端发送 SYN 发起握手</li><li>Server 端收到 Client 端 SYN 后，向 Client 端回复 SYN+ACK，socket 连接存储到半连接队列(SYN Queue)</li><li>Client 端收到 Server 端 SYN+ACK 后，向 Server 端回复 ACK，Client 端进入 ESTABLISHED状态(注意：此时仅仅是 Client 端认为 tcp 连接建立成功)</li><li>由于 Client 端认为 TCP 连接已经建立完成，所以向 Server 端发送数据 [PSH，ACK]，但是一直未收到 Server 端的确认 ACK，所以一直在 RETRY</li><li>Server 端一直在 RETRY 发送 SYN+ACK</li></ol><p>上述情况是由于 Server 端 socket 连接进入了半连接队列，在收到 Client 端 ACK 后，本应将 socket 连接存储到全连接队列，但是全连接队列已满，所以 Server 端 DROP 了该 ACK 请求，所以 Server 端一直在 RETRY 发送 SYN+ACK（TCP重试），socket 连接仍旧在半连接队列中，等待 Client 端回复 ACK。等到全连接队列有空位时，socket 连接进入了全连接队列，Server 端不再RETRY 发送 SYN+ACK，使用Client 端的ACK成功建立连接，Client 端也不会再RETRY</p><p>当 TCP 全连接队列满导致服务器丢掉了 ACK，与此同时，客户端的连接状态却是 ESTABLISHED，进程就在建立好的连接上发送请求。只要服务器没有为请求回复 ACK，请求就会被多次重发。如果服务器上的进程只是短暂的繁忙造成 accept 队列满，那么当 TCP 全连接队列有空位时，<strong>再次接收到的请求报文由于含有 ACK，仍然会触发服务器端成功建立连接</strong>。</p><h5 id="半连接队列满了"><a href="#半连接队列满了" class="headerlink" title="半连接队列满了"></a>半连接队列满了</h5><p>TCP 半连接队列长度的长度没有像全连接队列那样可以用 ss 命令查看。但是我们可以抓住 TCP 半连接的特点，就是服务端处于 SYN_RECV 状态的 TCP 连接，就是 TCP 半连接队列：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">netstat -natp | grep SYN_RECV | <span class="built_in">wc</span> -l</span></span><br></pre></td></tr></table></figure><ul><li><strong>如果半连接队列满了，并且没有开启 tcp_syncookies，则会丢弃</strong>；</li><li><strong>若全连接队列也满了，且没有重传 SYN+ACK 包的连接请求多于 1 个，则会丢弃</strong>；</li><li><strong>如果没有开启 tcp_syncookies，并且 tcp_max_syn_backlog 减去 当前半连接队列长度小于 (tcp_max_syn_backlog &gt;&gt; 2)，则会丢弃</strong>；</li></ul><p>模拟 TCP 半连接溢出场景不难，实际上就是对服务端一直发送 TCP SYN 包，但是不回第三次握手 ACK，这样就会使得服务端有大量的处于 SYN_RECV 状态的 TCP 连接。<strong>这其实也就是所谓的 SYN 洪泛、SYN 攻击、DDos 攻击</strong>。</p><h5 id="避免-SYN-攻击"><a href="#避免-SYN-攻击" class="headerlink" title="避免 SYN 攻击"></a>避免 SYN 攻击</h5><h6 id="无效连接监视释放"><a href="#无效连接监视释放" class="headerlink" title="无效连接监视释放"></a>无效连接监视释放</h6><p>这种方法不停监视系统的半开连接和不活动连接，当达到一定阈值时拆除这些连接，从而释放系统资源。这种方法对于所有的连接一视同仁，而且由于SYN Flood造成的半开连接数量很大，正常连接请求也被淹没在其中被这种方式误释放掉，因此这种方法属于入门级的SYN Flood方法。</p><h6 id="调大netdev-max-backlog"><a href="#调大netdev-max-backlog" class="headerlink" title="调大netdev_max_backlog"></a>调大netdev_max_backlog</h6><p>当网卡接收数据包的速度大于内核处理的速度时，会有一个队列保存这些数据包。控制该队列的最大值如下参数，默认值是 1000，我们要适当调大该参数的值，比如设置为 10000：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">net.core.netdev_max_backlog = 10000</span><br></pre></td></tr></table></figure><h6 id="增大半连接队列"><a href="#增大半连接队列" class="headerlink" title="增大半连接队列"></a>增大半连接队列</h6><p>要同时增大下面这三个参数：</p><ol><li>增大 net.ipv4.tcp_max_syn_backlog</li><li>增大 listen() 函数中的 backlog</li><li>增大 net.core.somaxconn</li></ol><h6 id="开启-tcp-syncookies"><a href="#开启-tcp-syncookies" class="headerlink" title="开启 tcp_syncookies"></a><span id="target4">开启 tcp_syncookies</span></h6><p>开启 tcp_syncookies，延缓任务控制块（TCB）分配方法<br>开启 syncookies 功能就可以在不使用 SYN 半连接队列的情况下成功建立连接，相当于绕过了 SYN 半连接来建立连接。0 值，表示关闭该功能；1 值，表示仅当 SYN 半连接队列放不下时，再启用它；2 值，表示无条件开启功能；<br><img data-src="/transblog/images/385275809245949.png"><br>当 「 SYN 队列」满之后，后续服务端收到 SYN 包，不会丢弃，而是根据算法（通信双方的IP地址端口、时间戳、MSS等信息），<strong>实时计算出一个 cookie 值；将 cookie 值放到第二次握手报文的「序列号」（seq）里</strong>，然后服务端回第二次握手给客户端；服务端接收到客户端的应答报文时，服务端会检查这个 ACK 包的合法性。如果合法，将该连接对象放入到「 Accept 队列」。最后应用程序通过调用 accpet() 接口，从「 Accept 队列」取出的连接。</p><ul><li><strong>cookies方案为什么不直接取代半连接队列</strong>？</li><li><ul><li>因为cookies方案服务端并不会保存连接信息，所以如果传输过程中数据包丢了，也不会重发第二次握手的信息。</li></ul></li><li><ul><li>编码解码cookies，都是比较耗CPU的，利用这一点，如果此时攻击者构造大量的第三次握手包（ACK包），进行ACK攻击(通过构造大量ACK包去消耗服务端资源的攻击)，同时带上各种瞎编的cookies信息，服务端收到ACK包后以为是正经cookies，憨憨地跑去解码（耗CPU），最后发现不是正经数据包后才丢弃。</li></ul></li></ul><h6 id="减少-SYN-ACK-重传次数"><a href="#减少-SYN-ACK-重传次数" class="headerlink" title="减少 SYN+ACK 重传次数"></a>减少 SYN+ACK 重传次数</h6><p>当服务端受到 SYN 攻击时，就会有大量处于 SYN_REVC 状态的 TCP 连接，处于这个状态的 TCP 会重传 SYN+ACK ，当重传超过次数达到上限后，就会断开连接。<br>可以减少 SYN-ACK 的重传次数，以加快处于 SYN_REVC 状态的 TCP 连接断开。</p><h6 id="使用SYN-Proxy防火墙"><a href="#使用SYN-Proxy防火墙" class="headerlink" title="使用SYN Proxy防火墙"></a>使用SYN Proxy防火墙</h6><p>因此很多防火墙中都提供一种SYN代理的功能，其主要原理是对试图穿越的SYN请求进行验证后才放行。</p><h5 id="全-半连接队列长度"><a href="#全-半连接队列长度" class="headerlink" title="全&#x2F;半连接队列长度"></a>全&#x2F;半连接队列长度</h5><ol><li>全连接队列的长度<br>其最大长度是 listen 时传入的 backlog 和 net.core.somaxconn 之间较小的那个值。<br>somaxconn默认值是 128，Linux 内核参数：&#x2F;proc&#x2F;sys&#x2F;net&#x2F;core&#x2F;somaxconn<br>backlog 是 listen(int sockfd, int backlog) 函数中的 backlog 大小，Nginx 默认值是 511，可以通过修改配置文件设置其长度；</li><li>半连接队列的长度<br>需要一并考虑 backlog，somaxconn 和 tcp_max_syn_backlog 这三个参数：<br>当 tcp_max_syn_backlog &gt; min(somaxconn, backlog) 时， 半连接队列最大值 max_qlen_log &#x3D; min(somaxconn, backlog) * 2;<br>当 tcp_max_syn_backlog &lt; min(somaxconn, backlog) 时， 半连接队列最大值 max_qlen_log &#x3D; tcp_max_syn_backlog * 2;</li></ol><h4 id="TCP快速连接"><a href="#TCP快速连接" class="headerlink" title="TCP快速连接"></a>TCP快速连接</h4><p>客户端在向服务端发起 HTTP GET 请求时，一个完整的交互过程，需要 2.5 个 RTT 的时延。<strong>由于第三次握手是可以携带数据的</strong>，这时如果在第三次握手发起 HTTP GET 请求，需要 2 个 RTT 的时延。</p><p><img data-src="/transblog/images/8723711258187.png"></p><p>在 Linux 3.7 内核版本中，提供了 TCP Fast Open 功能，内核参数：net.ipv4.tcp_fastopen，0-关闭；1-作为客户端使用 Fast Open 功能；2-作为服务端使用 Fast Open 功能；3-无论作为客户端还是服务器，都可以使用 Fast Open 功能这个功能可以减少 TCP 连接建立的时延。</p><p><img data-src="/transblog/images/591953711245178.png"></p><p>在第一次建立连接的时候，服务端在第二次握手产生一个 Cookie （已加密）并通过 SYN、ACK 包一起发给客户端，在下次请求的时候，<strong>客户端在 SYN 包带上 Cookie 发给服务端，就提前可以跳过三次握手的过程</strong>，因为 Cookie 中维护了一些信息，服务端可以从 Cookie 获取 TCP 相关的信息，这时发起的 HTTP GET 请求就只需要 1 个 RTT 的时延；</p><h4 id="已建立连接的TCP收到SYN"><a href="#已建立连接的TCP收到SYN" class="headerlink" title="已建立连接的TCP收到SYN"></a>已建立连接的TCP收到SYN</h4><p>已建立连接的TCP，收到SYN会发生什么？如果客户端掉线了，服务器不知道，客户端再上线的时候发起了SYN握手，服务器如何应对？</p><ol><li>客户端的 SYN 报文里的端口号与历史连接不相同<br>此时服务端会认为是新的连接要建立，于是就会通过三次握手来建立新的连接。那旧连接里处于 Established 状态的服务端会有两种情况：</li></ol><ul><li><ul><li>如果服务端发送了数据包给客户端，由于客户端的连接已经被关闭了，此时客户的内核就会回 RST 报文，服务端收到后就会释放连接。</li></ul></li><li><ul><li>如果服务端一直没有发送数据包给客户端，在超过一段时间后，TCP 保活机制就会启动，检测到客户端没有存活后，接着服务端就会释放掉该连接。</li></ul></li></ul><ol start="2"><li>客户端的 SYN 报文里的端口号与历史连接相同<br><strong>处于 Established 状态的服务端，如果收到了客户端的 SYN 报文（注意此时的 SYN 报文其实是乱序的，因为 SYN 报文的初始化序列号其实是一个随机数），会回复一个携带了正确序列号和确认号的 ACK 报文，这个 ACK 被称之为 Challenge ACK。接着，客户端收到这个 Challenge ACK，发现确认号（ack num）并不是自己期望收到的，于是就会回 RST 报文，服务端收到后，就会释放掉该连接</strong>。<br>RFC 793 文档里的第 34 页里，有说到这个例子。</li></ol><p><img data-src="/transblog/images/58691818266061.png"></p><h3 id="TCP网络编程"><a href="#TCP网络编程" class="headerlink" title="TCP网络编程"></a>TCP网络编程</h3><p><img data-src="/transblog/images/8306125857679.png"></p><h4 id="TCP服务器代码"><a href="#TCP服务器代码" class="headerlink" title="TCP服务器代码"></a>TCP服务器代码</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8080</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFER_SIZE 1024</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> server_fd, new_socket;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> address;</span><br><span class="line">    <span class="type">int</span> opt = <span class="number">1</span>;</span><br><span class="line">    <span class="type">int</span> addrlen = <span class="built_in">sizeof</span>(address);</span><br><span class="line">    <span class="type">char</span> buffer[BUFFER_SIZE] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* message = <span class="string">&quot;Hello from server&quot;</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 创建套接字</span></span><br><span class="line">    <span class="keyword">if</span> ((server_fd = <span class="built_in">socket</span>(AF_INET, SOCK_STREAM, <span class="number">0</span>)) == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;socket failed&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 绑定套接字</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">setsockopt</span>(server_fd, SOL_SOCKET, SO_REUSEADDR | SO_REUSEPORT, &amp;opt, <span class="built_in">sizeof</span>(opt))) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;setsockopt&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    address.sin_family = AF_INET;</span><br><span class="line">    address.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line">    address.sin_port = <span class="built_in">htons</span>(PORT);</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">bind</span>(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;address, <span class="built_in">sizeof</span>(address)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;bind failed&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 监听连接</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">listen</span>(server_fd, <span class="number">3</span>) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;listen&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 接受连接</span></span><br><span class="line">    <span class="keyword">if</span> ((new_socket = <span class="built_in">accept</span>(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;address, (<span class="type">socklen_t</span>*)&amp;addrlen)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;accept&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">read</span>(new_socket, buffer, BUFFER_SIZE);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Message received: &quot;</span> &lt;&lt; buffer &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">send</span>(new_socket, message, <span class="built_in">strlen</span>(message), <span class="number">0</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Hello message sent&quot;</span> &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">close</span>(new_socket);</span><br><span class="line">    <span class="built_in">close</span>(server_fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="TCP客户端代码"><a href="#TCP客户端代码" class="headerlink" title="TCP客户端代码"></a>TCP客户端代码</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8080</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFER_SIZE 1024</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> sock = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> serv_addr;</span><br><span class="line">    <span class="type">char</span> buffer[BUFFER_SIZE] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* message = <span class="string">&quot;Hello from client&quot;</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 创建套接字</span></span><br><span class="line">    <span class="keyword">if</span> ((sock = <span class="built_in">socket</span>(AF_INET, SOCK_STREAM, <span class="number">0</span>)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        std::cerr &lt;&lt; <span class="string">&quot;Socket creation error&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_port = <span class="built_in">htons</span>(PORT);</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 将服务器地址转为二进制形式</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">inet_pton</span>(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;serv_addr.sin_addr) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">        std::cerr &lt;&lt; <span class="string">&quot;Invalid address/ Address not supported&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 连接到服务器</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">connect</span>(sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="built_in">sizeof</span>(serv_addr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        std::cerr &lt;&lt; <span class="string">&quot;Connection Failed&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">send</span>(sock, message, <span class="built_in">strlen</span>(message), <span class="number">0</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Hello message sent&quot;</span> &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">read</span>(sock, buffer, BUFFER_SIZE);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Message received: &quot;</span> &lt;&lt; buffer &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">close</span>(sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="listen作用"><a href="#listen作用" class="headerlink" title="listen作用"></a>listen作用</h4><p><a href="https://blog.csdn.net/m0_46995061/article/details/144983907">点击此处查看CSDN：网络服务程序为什么在 bind 之后还需要 listen 一下，是否多此一举？</a></p><p>listen 最主要的工作就是<strong>申请和初始化接收队列，包括全连接队列和半连接队列</strong>。</p><p>全&#x2F;半两个队列是三次握手中很重要的两个数据结构，有了它们服务器才能正常响应来自客户端的三次握手。所以服务器端都需要 listen 一下才行。</p><h5 id="没有listen，能建立TCP连接吗？"><a href="#没有listen，能建立TCP连接吗？" class="headerlink" title="没有listen，能建立TCP连接吗？"></a>没有listen，能建立TCP连接吗？</h5><p>是可以的</p><ol><li><strong>客户端是可以自己连自己的形成连接（TCP自连接）</strong></li><li><strong>两个客户端同时向对方发出connect请求建立连接（TCP同时打开）</strong></li></ol><p>这两个情况都有个共同点，就是没有服务端参与，也就是没有listen，就能建立连接。</p><h5 id="TCP自连接"><a href="#TCP自连接" class="headerlink" title="TCP自连接"></a><strong>TCP自连接</strong></h5><ul><li>内核还有个全局 hash 表，可以用于存放 socket 连接的信息。</li><li><strong>在 TCP 自连接的情况中，客户端在 connect 方法时，最后会将自己的连接信息放入到这个全局 hash 表中</strong>。</li><li>然后将信息发出，消息在经过回环地址重新回到 TCP 传输层的时候，就会根据 IP + 端口信息，再一次从这个全局 hash 中取出信息。于是握手包一来一回，最后成功建立连接。</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> LOCAL_IP_ADDR(0x7F000001) <span class="comment">// IP 127.0.0.1</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LOCAL_TCP_PORT(34567) <span class="comment">// 端口</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> local, peer;</span><br><span class="line"><span class="type">int</span> ret;</span><br><span class="line"><span class="type">char</span> buf[<span class="number">128</span>];</span><br><span class="line"><span class="type">int</span> sock = <span class="built_in">socket</span>(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">memset</span>(&amp;local, <span class="number">0</span>, <span class="built_in">sizeof</span>(local));</span><br><span class="line"><span class="built_in">memset</span>(&amp;peer, <span class="number">0</span>, <span class="built_in">sizeof</span>(peer));</span><br><span class="line"></span><br><span class="line">local.sin_family = AF_INET;</span><br><span class="line">local.sin_port = <span class="built_in">htons</span>(LOCAL_TCP_PORT);</span><br><span class="line">local.sin_addr.s_addr = <span class="built_in">htonl</span>(LOCAL_IP_ADDR);</span><br><span class="line"></span><br><span class="line">peer = local;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> flag = <span class="number">1</span>;</span><br><span class="line">    ret = <span class="built_in">setsockopt</span>(sock, SOL_SOCKET, SO_REUSEADDR, &amp;flag, <span class="built_in">sizeof</span>(flag));</span><br><span class="line">    <span class="keyword">if</span> (ret == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Fail to setsocket SO_REUSEADDR: %s\n&quot;</span>, <span class="built_in">strerror</span>(errno));</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">ret = <span class="built_in">bind</span>(sock, (<span class="type">const</span> <span class="keyword">struct</span> sockaddr *)&amp;local, <span class="built_in">sizeof</span>(local));</span><br><span class="line"><span class="keyword">if</span> (ret) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Fail to bind: %s\n&quot;</span>, <span class="built_in">strerror</span>(errno));</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ret = <span class="built_in">connect</span>(sock, (<span class="type">const</span> <span class="keyword">struct</span> sockaddr *)&amp;peer, <span class="built_in">sizeof</span>(peer));</span><br><span class="line"><span class="keyword">if</span> (ret) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Fail to connect myself: %s\n&quot;</span>, <span class="built_in">strerror</span>(errno));</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Connect to myself successfully\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//发送数据</span></span><br><span class="line"><span class="built_in">strcpy</span>(buf, <span class="string">&quot;Hello, myself~&quot;</span>);</span><br><span class="line"><span class="built_in">send</span>(sock, buf, <span class="built_in">strlen</span>(buf), <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">memset</span>(buf, <span class="number">0</span>, <span class="built_in">sizeof</span>(buf));</span><br><span class="line"></span><br><span class="line"><span class="comment">//接收数据</span></span><br><span class="line"><span class="built_in">recv</span>(sock, buf, <span class="built_in">sizeof</span>(buf), <span class="number">0</span>);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Recv the msg: %s\n&quot;</span>, buf);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">sleep</span>(<span class="number">1000</span>);</span><br><span class="line"><span class="built_in">close</span>(sock);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以看到 TCP socket 成功的“连接”了自己，并发送和接收了数据包，netstat 的输出更证明了 TCP 的两端地址和端口是完全相同的。<br><img data-src="/transblog/images/245001518260552.png"></p><h5 id="TCP同时打开"><a href="#TCP同时打开" class="headerlink" title="TCP同时打开"></a><strong>TCP同时打开</strong></h5><p>可以参考内核源码，主要原理可以总结为：<strong>对于处于syn_sent状态的套接字来说，如果收到了syn报文，则会正常回复syn+ack，完成TCP三次握手。即如果一个syn包命中了一个已经发送过syn包的连接，则执行“同时打开”握手流程</strong>。<br><img data-src="/transblog/images/222232710265935.png" alt="两个客户端同时发送connect请求"></p><h4 id="accept作用"><a href="#accept作用" class="headerlink" title="accept作用"></a>accept作用</h4><p>accept 操作的作用是<strong>从已完成三次握手的连接请求队列中取出一个连接，并为这个连接创建一个新的socket描述符</strong>。这个新描述符专用于与该客户端之间的数据传输，而原来的监听socket继续用于接收新的连接请求。</p><p>accept 的行为可以根据服务器 socket 的设置（阻塞或非阻塞模式）有所不同：</p><ul><li>在阻塞模式下，如果没有待接受的连接，accept 会阻塞等待；</li><li>而在非阻塞模式下，如果没有连接可接受，accept 会立即返回一个错误，这为服务器提供了灵活的控制机制。</li></ul><h5 id="没有accept，能建立TCP连接吗？"><a href="#没有accept，能建立TCP连接吗？" class="headerlink" title="没有accept，能建立TCP连接吗？"></a>没有accept，能建立TCP连接吗？</h5><p>可以，建立连接的过程中根本不需要accept()参与， 执行accept()只是为了从全连接队列里取出一条连接。</p><p><img data-src="/transblog/images/389671918249238.png"></p><h4 id="connect作用"><a href="#connect作用" class="headerlink" title="connect作用"></a>connect作用</h4><p>对于客户端的 connect() 函数，该函数的功能为客户端主动连接服务器，建立连接是通过三次握手，而<strong>这个连接的过程是由内核完成</strong>，不是这个函数完成的，<strong>这个函数的作用仅仅是通知 Linux 内核</strong>，让 Linux 内核自动完成 TCP 三次握手连接，最后把连接的结果返回给这个函数的返回值（成功连接为0， 失败为-1）。</p><p>通常的情况，客户端的 connect() 函数默认会一直阻塞，直到三次握手成功或超时失败才返回（正常的情况，这个过程很快完成）。</p><h4 id="已建立连接，客户端出现故障"><a href="#已建立连接，客户端出现故障" class="headerlink" title="已建立连接，客户端出现故障"></a>已建立连接，客户端出现故障</h4><p>如果服务端一直不会发送数据给客户端，那么永远无法感知到客户端宕机，也就是服务端的 TCP 连接将一直处于 ESTABLISH 状态，占用着系统资源。</p><p><strong>为了避免这种情况，TCP使用保活机制</strong>：<br>定义一个时间段，在这个时间段内，如果没有任何连接相关的活动，TCP 保活机制会开始作用。每隔一个时间间隔，发送一个探测报文，该探测报文包含的数据非常少，如果连续几个探测报文都没有得到响应，则认为当前的 TCP 连接已经死亡，系统内核将错误信息通知给上层应用程序。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">net.ipv<span class="number">4.</span>tcp_keepalive_time=<span class="number">7200</span> <span class="comment">//表示保活时间是 7200 秒（2小时），也就 2 小时内如果没有任何连接相关的活动，则会启动保活机制</span></span><br><span class="line">net.ipv<span class="number">4.</span>tcp_keepalive_intvl=<span class="number">75</span>  <span class="comment">//表示每次检测间隔 75 秒</span></span><br><span class="line">net.ipv<span class="number">4.</span>tcp_keepalive_probes=<span class="number">9</span>  <span class="comment">//表示检测 9 次无响应，认为对方是不可达的，从而中断本次的连接。</span></span><br></pre></td></tr></table></figure><p>在 Linux 系统中，最少需要经过 2 小时 11 分 15 秒才可以发现一个「死亡」连接。我们可以自己在应用层实现一个心跳机制<br>tcp_keepalive_time + （tcp_keepalive_intvl * tcp_keepalive_probes）</p><p>如果开启了 TCP 保活，需要考虑以下几种情况：</p><ol><li>对端程序是正常工作的。当 TCP 保活的探测报文发送给对端, 对端会正常响应，这样 TCP 保活时间会被重置，等待下一个 TCP 保活时间的到来。</li><li>对端主机宕机并重启。当 TCP 保活的探测报文发送给对端后，对端是可以响应的，但由于没有该连接的有效信息，会产生一个 RST 报文，这样很快就会发现 TCP 连接已经被重置。</li><li>是对端主机宕机（<em>注意不是进程崩溃，进程崩溃后操作系统在回收进程资源的时候，会发送 FIN 报文，而主机宕机则是无法感知的，所以需要 TCP 保活机制来探测对方是不是发生了主机宕机</em>），或对端由于其他原因导致报文不可达。当 TCP 保活的探测报文发送给对端后，没有响应，连续几次，达到保活探测次数后，TCP 会报告该 TCP 连接已经死亡。</li></ol><h4 id="已建立连接，服务端出现故障"><a href="#已建立连接，服务端出现故障" class="headerlink" title="已建立连接，服务端出现故障"></a>已建立连接，服务端出现故障</h4><p> 如果已经建立了连接，但是服务端突然出现故障了怎么办？<br>在 kill 掉进程后，服务端会发送 FIN 报文，与客户端进行四次挥手</p><h4 id="建立连接时，初始化序列号都不一样"><a href="#建立连接时，初始化序列号都不一样" class="headerlink" title="建立连接时，初始化序列号都不一样"></a>建立连接时，初始化序列号都不一样</h4><ol><li><p>为了防止历史报文被下一个相同四元组的连接接收（主要方面）；<br>如果每次建立连接，客户端和服务端的初始化序列号都是一样的话，很容易出现历史报文被下一个相同四元组的连接接收的问题。</p><p> <strong>已经有了TIME_WAIT 状态且持续 2 MSL 时长，历史报文不是早就消散了吗?</strong></p><blockquote><p>思考下面这种情况：</p></blockquote><p> 客户端和服务端建立一个 TCP 连接，在客户端发送数据包被网络阻塞了，然后超时重传了这个数据包，而此时服务端设备断电重启了，之前与客户端建立的连接就消失了，于是在收到客户端的数据包的时候就会发送 RST 报文。紧接着，客户端又与服务端建立了与上一个连接相同四元组的连接；在新连接建立完成后，上一个连接中被网络阻塞的数据包正好抵达了服务端，刚好该数据包的序列号正好是在服务端的接收窗口内，所以该数据包会被服务端正常接收，就会造成数据错乱。</p></li></ol><p>如果每次建立连接客户端和服务端的初始化序列号都「不一样」，<strong>就有大概率因为历史报文的序列号「不在」对方接收窗口</strong>，从而很大程度上避免了历史报文，如果每次的初始化序列号一样，在对方接收窗口的概率就会变大。而且TCP产生的随机数是会基于时钟计时器递增的，基本不可能会随机成一样的初始化序列号。</p><hr><p><strong>但是也不是完全避免：</strong></p><blockquote><ul><li>序列号(SEQ)，是 TCP 一个头部字段，标识了 TCP 发送端到 TCP 接收端的数据流的一个字节，因为 TCP 是面向字节流的可靠协议，为了保证消息的顺序性和可靠性，TCP 为每个传输方向上的每个字节都赋予了一个编号，以便于传输成功后确认、丢失后重传以及在接收端保证不会乱序。序列号是一个 32 位的无符号数，<strong>因此在到达 4G 之后再循环回到 0</strong>。</li><li>初始序列号(ISN)，在 TCP 建立连接的时候，客户端和服务端都会各自生成一个初始序列号，它是基于时钟生成的一个随机数，来保证每个连接都拥有不同的初始序列号。初始化序列号可被视为一个 32 位的计数器，该计数器的数值每 4 微秒加 1，<strong>循环一次需要 4.55 小时</strong>。</li></ul></blockquote><p><strong>序列号和初始化序列号并不是无限递增的，会发生回绕为初始值的情况</strong>，这意味着无法根据序列号来判断新老数据。为了解决这个问题，就需要有 TCP 时间戳。tcp_timestamps 参数是默认开启的，开启了 tcp_timestamps 参数，TCP 头部就会使用时间戳选项，它有两个好处，<strong>一个是便于精确计算 RTT ，另一个是能防止序列号回绕（PAWS）</strong>。<br>在开启 tcp_timestamps 选项情况下，一台机器发的所有 TCP 包都会带上发送时的时间戳，PAWS 要求连接双方维护最近一次收到的数据包的时间戳（Recent TSval），每收到一个新数据包都会读取数据包中的时间戳值跟 Recent TSval 值做比较，<strong>如果发现收到的数据包中时间戳不是递增的，则表示该数据包是过期的，就会直接丢弃这个数据包</strong>。</p><ol start="2"><li>为了安全性，防止黑客伪造的相同序列号的 TCP 报文被对方接收；</li></ol><h3 id="TCP四次挥手"><a href="#TCP四次挥手" class="headerlink" title="TCP四次挥手"></a>TCP四次挥手</h3><p><img data-src="/transblog/images/513040811240805.png"></p><ul><li><p>服务端在收到 FIN 报文的时候，TCP 协议栈会为 FIN 包插入一个文件结束符 EOF 到接收缓冲区中，服务端应用程序可以通过 read 调用来感知这个 FIN 包，<strong>这个 EOF 会被放在已排队等候的其他已接收的数据之后</strong>，所以必须要得继续 read 接收缓冲区已接收的数据；</p></li><li><p>当服务端在 read 数据的时候，最后自然就会读到 EOF，接着 <strong>read() 就会返回 0，这时服务端应用程序如果有数据要发送的话，就发完数据后才调用关闭连接的函数，这时服务端就会发一个 FIN 包</strong>，这个 FIN 报文代表服务端不会再发送数据了，之后处于 LAST_ACK 状态；</p></li><li><p><strong>是否要发送第三次挥手的控制权不在内核，而是在被动关闭方（上图的服务端）的应用程序</strong>，因为应用程序可能还有数据要发送，由应用程序决定什么时候调用关闭连接的函数，当调用了关闭连接的函数，内核就会发送 FIN 报文了</p></li></ul><h4 id="为什么挥手需要四次？"><a href="#为什么挥手需要四次？" class="headerlink" title="为什么挥手需要四次？"></a>为什么挥手需要四次？</h4><ul><li><strong>保证数据完整性</strong></li></ul><p>TCP连接可能涉及双向数据传输，四次挥手确保双方都能完成数据的发送和接收。服务端收到客户端的 FIN 报文时，先回一个 ACK 应答报文，而服务端可能还有数据需要处理和发送，等服务端不再发送数据时，才发送 FIN 报文给客户端来表示同意现在关闭连接。</p><ul><li><strong>可靠的连接关闭</strong><br>四次挥手提供了双向关闭连接的机会，确保双方都能正确关闭连接并释放资源，提高连接的可靠性，防止意外中断。关闭连接时，客户端向服务端发送 FIN 时，<strong>仅仅表示客户端不再发送数据了但是还能接收数据</strong>。</li></ul><h5 id="变成三次挥手"><a href="#变成三次挥手" class="headerlink" title="变成三次挥手"></a>变成三次挥手</h5><p>TCP可以变成三次挥手吗？能不能把第二次的 ACK 报文， 放到第三次 FIN 报文一起发送？<br><strong>在特定情况下，四次挥手是可以变成三次挥手的</strong></p><p><img data-src="/transblog/images/236384910244415.png"></p><p><strong>「没有数据要发送」并且「开启了 TCP 延迟确认机制」</strong>，那么第二和第三次挥手就会合并传输，这样就出现了三次挥手。</p><h6 id="TCP延迟确认"><a href="#TCP延迟确认" class="headerlink" title="TCP延迟确认"></a>TCP延迟确认</h6><ul><li>当有响应数据要发送时，ACK 会随着响应数据一起立刻发送给对方</li><li>当没有响应数据要发送时，ACK 将会延迟一段时间，以等待是否有响应数据可以一起发送</li><li>如果在延迟等待发送 ACK 期间，对方的第二个数据报文又到达了，这时就会立刻发送 ACK</li></ul><p><img data-src="/transblog/images/333440611240666.png"></p><p>最大延迟确认时间是 200 ms，最短延迟确认时间是 40 ms如果要关闭 TCP 延迟确认机制，可以在 Socket 设置里启用 TCP_QUICKACK。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1 表示开启 TCP_QUICKACK，即关闭 TCP 延迟确认机制</span></span><br><span class="line"><span class="type">int</span> value = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">setsockopt</span>(socketfd, IPPROTO_TCP, TCP_QUICKACK, (<span class="type">char</span>*)&amp; value, <span class="built_in">sizeof</span>(<span class="type">int</span>));</span><br></pre></td></tr></table></figure><h4 id="第一次挥手丢失了，会发生什么？"><a href="#第一次挥手丢失了，会发生什么？" class="headerlink" title="第一次挥手丢失了，会发生什么？"></a>第一次挥手丢失了，会发生什么？</h4><p>如果第一次挥手丢失了，那么客户端迟迟收不到被动方的 ACK 的话，也就会触发超时重传机制，重传 FIN 报文，重发次数由 tcp_orphan_retries 参数控制。</p><ul><li><font color="BlueGreen">当客户端超时重传已达到最大重传次数，于是再等待一段时间（时间为上一次超时时间的 2 倍），如果还是没能收到服务端的第二次挥手（ACK 报文），那么客户端就会断开连接。</font></li></ul><h4 id="第二次挥手丢失了，会发生什么？"><a href="#第二次挥手丢失了，会发生什么？" class="headerlink" title="第二次挥手丢失了，会发生什么？"></a>第二次挥手丢失了，会发生什么？</h4><p><strong>ACK 报文是不会重传的</strong>，所以如果服务端的第二次挥手丢失了，客户端就会触发超时重传机制，重传 FIN 报文，直到收到服务端的第二次挥手，或者达到最大的重传次数。</p><ul><li><font color="BlueGreen">当客户端超时重传已达到最大重传次数，于是再等待一段时间（时间为上一次超时时间的 2 倍），如果还是没能收到服务端的第二次挥手（ACK 报文），那么客户端就会断开连接。</font></li></ul><h4 id="第三次挥手丢失了，会发生什么？"><a href="#第三次挥手丢失了，会发生什么？" class="headerlink" title="第三次挥手丢失了，会发生什么？"></a>第三次挥手丢失了，会发生什么？</h4><p>内核是没有权利替代进程关闭连接，必须由进程主动调用 close 函数来触发服务端发送 FIN 报文。如果迟迟收不到这个 ACK，服务端就会重发 FIN 报文，重发次数仍然由 tcp_orphan_retries 参数控制，这与客户端重发 FIN 报文的重传次数控制方式是一样的。</p><ul><li><font color="YellowGreen">服务端重传第三次挥手报文的次数达到了重传最大次数，于是再等待一段时间（时间为上一次超时时间的 2 倍），如果还是没能收到客户端的第四次挥手（ACK报文），那么服务端就会断开连接。</font></li><li>客户端因为是通过 close 函数关闭连接的，处于 FIN_WAIT_2 状态是有时长限制的，如果 tcp_fin_timeout （默认值是 60 秒）时间内还是没能收到服务端的第三次挥手（FIN 报文），那么客户端就会断开连接。</li></ul><h4 id="第四次挥手丢失了，会发生什么？"><a href="#第四次挥手丢失了，会发生什么？" class="headerlink" title="第四次挥手丢失了，会发生什么？"></a>第四次挥手丢失了，会发生什么？</h4><p><strong>ACK 报文是不会重传的</strong>，如果第四次挥手的 ACK 报文没有到达服务端，服务端就会重发 FIN 报文，重发次数仍然由前面介绍过的 tcp_orphan_retries 参数控制。</p><ul><li><font color="YellowGreen">服务端重传第三次挥手报文的次数达到了重传最大次数，于是再等待一段时间（时间为上一次超时时间的 2 倍），如果还是没能收到客户端的第四次挥手（ACK报文），那么服务端就会断开连接。</font></li><li>客户端在收到第三次挥手后，就会进入 TIME_WAIT 状态，开启时长为 2MSL 的定时器，如果途中再次收到第三次挥手（FIN 报文）后，就会重置定时器，<strong>当等待 2MSL 时长后，客户端就会断开连接</strong>。</li></ul><h4 id="四次挥手收到乱序的-FIN"><a href="#四次挥手收到乱序的-FIN" class="headerlink" title="四次挥手收到乱序的 FIN"></a>四次挥手收到乱序的 FIN</h4><p>在 FIN_WAIT_2 状态时，如果收到乱序的 FIN 报文，<strong>那么就被会加入到「乱序队列」，这个队列的数据结构是红黑树</strong>，并不会进入到 TIME_WAIT 状态。</p><p>等再次收到前面被网络延迟的数据包时，会判断乱序队列有没有数据，然后会检测乱序队列中是否有可用的数据，如果能在乱序队列中找到与当前报文的序列号保持的顺序的报文，就会看该报文是否有 FIN 标志，如果发现有 FIN 标志，这时才会进入 TIME_WAIT 状态。</p><p><img data-src="/transblog/images/395313709268159.png"></p><p><a href="https://elixir.bootlin.com/linux/v3.19.8/source/net/ipv4/tcp_input.c#L4214">tcp_input.c&#x2F;tcp_data_queue_ofo()</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">tcp_v4_rcv</span><span class="params">(<span class="keyword">struct</span> sk_buff *skb)</span></span>&#123;</span><br><span class="line">...</span><br><span class="line">    <span class="comment">//根据四元组查找相应连接的socket结构</span></span><br><span class="line">sk = __inet_lookup_skb(&amp;tcp_hashinfo, skb, th-&gt;source, th-&gt;dest);</span><br><span class="line"><span class="keyword">if</span> (!sk)</span><br><span class="line"><span class="keyword">goto</span> no_tcp_socket;</span><br><span class="line">...</span><br><span class="line">    <span class="comment">//判断socket是否被user占用，如果没有占用，调用tcp_v4_do_rcv()</span></span><br><span class="line"><span class="keyword">if</span> (!<span class="built_in">sock_owned_by_user</span>(sk)) &#123;</span><br><span class="line"><span class="keyword">if</span> (!<span class="built_in">tcp_prequeue</span>(sk, skb))</span><br><span class="line">ret = <span class="built_in">tcp_v4_do_rcv</span>(sk, skb);</span><br><span class="line">&#125; </span><br><span class="line"><span class="comment">//为了避免并发操作socket，将数据包放入backlog队列，放入失败或已满，丢弃数据包</span></span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">unlikely</span>(<span class="built_in">sk_add_backlog</span>(sk, skb,</span><br><span class="line">   sk-&gt;sk_rcvbuf + sk-&gt;sk_sndbuf))) &#123;</span><br><span class="line"><span class="built_in">bh_unlock_sock</span>(sk);</span><br><span class="line"><span class="built_in">NET_INC_STATS_BH</span>(net, LINUX_MIB_TCPBACKLOGDROP);</span><br><span class="line"><span class="keyword">goto</span> discard_and_relse;</span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">tcp_v4_do_rcv</span><span class="params">(<span class="keyword">struct</span> sock *sk, <span class="keyword">struct</span> sk_buff *skb)</span></span>&#123;</span><br><span class="line">...</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">tcp_rcv_state_process</span>(sk, skb, <span class="built_in">tcp_hdr</span>(skb), skb-&gt;len)) &#123;</span><br><span class="line">rsk = sk;</span><br><span class="line"><span class="keyword">goto</span> reset;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>shutdown 只关闭了写方向，所以会继续往下调用 tcp_data_queue 函数（因为 case TCP_FIN_WAIT2 代码块里并没有 break 语句，所以会走到该函数）。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">tcp_rcv_state_process</span><span class="params">(<span class="keyword">struct</span> sock *sk, <span class="keyword">struct</span> sk_buff *skb,</span></span></span><br><span class="line"><span class="params"><span class="function">  <span class="type">const</span> <span class="keyword">struct</span> tcphdr *th, <span class="type">unsigned</span> <span class="type">int</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">case</span> TCP_FIN_WAIT2:</span><br><span class="line"><span class="comment">/* RFC 793 says to queue data in these states,</span></span><br><span class="line"><span class="comment"> * RFC 1122 says we MUST send a reset.</span></span><br><span class="line"><span class="comment"> * BSD 4.4 also does reset.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (sk-&gt;sk_shutdown &amp; RCV_SHUTDOWN) &#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">TCP_SKB_CB</span>(skb)-&gt;end_seq != <span class="built_in">TCP_SKB_CB</span>(skb)-&gt;seq &amp;&amp;</span><br><span class="line">    <span class="built_in">after</span>(<span class="built_in">TCP_SKB_CB</span>(skb)-&gt;end_seq - th-&gt;fin, tp-&gt;rcv_nxt)) &#123;</span><br><span class="line"><span class="built_in">NET_INC_STATS_BH</span>(<span class="built_in">sock_net</span>(sk), LINUX_MIB_TCPABORTONDATA);</span><br><span class="line"><span class="built_in">tcp_reset</span>(sk);</span><br><span class="line"><span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* Fall through */</span></span><br><span class="line"><span class="keyword">case</span> TCP_ESTABLISHED:</span><br><span class="line"><span class="built_in">tcp_data_queue</span>(sk, skb);</span><br><span class="line">queued = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* tcp_data could move socket to TIME-WAIT */</span></span><br><span class="line"><span class="keyword">if</span> (sk-&gt;sk_state != TCP_CLOSE) &#123;</span><br><span class="line"><span class="built_in">tcp_data_snd_check</span>(sk);</span><br><span class="line"><span class="built_in">tcp_ack_snd_check</span>(sk);</span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">tcp_data_queue</span><span class="params">(<span class="keyword">struct</span> sock *sk, <span class="keyword">struct</span> sk_buff *skb)</span></span>&#123;</span><br><span class="line">...</span><br><span class="line"><span class="comment">/*  Queue data for delivery to the user.</span></span><br><span class="line"><span class="comment"> *  Packets in sequence go to the receive queue.</span></span><br><span class="line"><span class="comment"> *  Out of sequence packets to the out_of_order_queue.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">//如果报文的序列号是有序的</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">TCP_SKB_CB</span>(skb)-&gt;seq == tp-&gt;rcv_nxt) &#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">tcp_receive_window</span>(tp) == <span class="number">0</span>)</span><br><span class="line"><span class="keyword">goto</span> out_of_window;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Ok. In sequence. In window. */</span></span><br><span class="line"><span class="keyword">if</span> (tp-&gt;ucopy.task == current &amp;&amp;</span><br><span class="line">    tp-&gt;copied_seq == tp-&gt;rcv_nxt &amp;&amp; tp-&gt;ucopy.len &amp;&amp;</span><br><span class="line">    <span class="built_in">sock_owned_by_user</span>(sk) &amp;&amp; !tp-&gt;urg_data) &#123;</span><br><span class="line"><span class="type">int</span> chunk = <span class="built_in">min_t</span>(<span class="type">unsigned</span> <span class="type">int</span>, skb-&gt;len,</span><br><span class="line">  tp-&gt;ucopy.len);</span><br><span class="line"></span><br><span class="line">__set_current_state(TASK_RUNNING);</span><br><span class="line">...</span><br><span class="line">        <span class="comment">//如果有fin标识，会调用tcp_fin()</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">TCP_SKB_CB</span>(skb)-&gt;tcp_flags &amp; TCPHDR_FIN)</span><br><span class="line"><span class="built_in">tcp_fin</span>(sk);</span><br><span class="line">        <span class="comment">//检查乱序队列有没有数据</span></span><br><span class="line"><span class="keyword">if</span> (!<span class="built_in">skb_queue_empty</span>(&amp;tp-&gt;out_of_order_queue)) &#123;</span><br><span class="line">    <span class="comment">//检查乱序队列中是否有数据包可用，即是否在乱序队列中找到与当前数据包保持序列号连续的数据包</span></span><br><span class="line"><span class="built_in">tcp_ofo_queue</span>(sk);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* RFC2581. 4.2. SHOULD send immediate ACK, when</span></span><br><span class="line"><span class="comment"> * gap in queue is filled.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">skb_queue_empty</span>(&amp;tp-&gt;out_of_order_queue))</span><br><span class="line"><span class="built_in">inet_csk</span>(sk)-&gt;icsk_ack.pingpong = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line"><span class="comment">/* If window is closed, drop tail of packet. But after</span></span><br><span class="line"><span class="comment"> * remembering D-SACK for its head made in previous line.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (!<span class="built_in">tcp_receive_window</span>(tp))</span><br><span class="line"><span class="keyword">goto</span> out_of_window;</span><br><span class="line"><span class="keyword">goto</span> queue_and_out;</span><br><span class="line">&#125;</span><br><span class="line">    <span class="comment">//如果是乱序的，通过此函数加入乱序队列</span></span><br><span class="line"><span class="built_in">tcp_data_queue_ofo</span>(sk, skb);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="TIME-WAIT"><a href="#TIME-WAIT" class="headerlink" title="TIME_WAIT"></a>TIME_WAIT</h4><p>MSL(最大分段生存期)，指明TCP报文在Internet上最长生存时间，每个具体的TCP实现都必须选择一个确定的MSL值</p><p>因为 TCP 报文基于是 IP 协议的，而 IP 头中有一个 TTL 字段，是 IP 数据报可以经过的最大路由数，每经过一个处理他的路由器此值就减 1，当此值为 0 则数据报将被丢弃，同时发送 ICMP 报文通知源主机。<br><strong>MSL 应该要大于等于 TTL 消耗为 0 的时间，以确保报文已被自然消亡</strong>。TTL 的值一般是 64，Linux 将 MSL 设置为 30 秒，意味着<strong>Linux 认为数据报文经过 64 个路由器的时间不会超过 30 秒，如果超过了，就认为报文已经消失在网络中了</strong>。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TCP_TIMEWAIT_LEN (60*HZ) <span class="comment">/* how long to wait to destroy TIME-WAIT </span></span></span><br><span class="line"><span class="comment"><span class="meta">                                    state, about 60 seconds  */</span></span></span><br></pre></td></tr></table></figure><h5 id="TIME-WAIT的作用"><a href="#TIME-WAIT的作用" class="headerlink" title="TIME_WAIT的作用"></a>TIME_WAIT的作用</h5><ol><li><p><strong>保证「被动关闭连接」的一方，能被正确的关闭</strong><br>如果第四次挥手丢失，就会触发超时重发 Fin 报⽂，另⼀⽅接收到 FIN 后，会触发 ACK 给被动关闭⽅， ⼀来⼀去正好 2 个 MSL。<br>2MSL 的时间是从客户端接收到 FIN 后发送 ACK 开始计时的。如果在 TIME-WAIT 时间内，因为客户端的 ACK 没有传输到服务端，客户端⼜接收到了服务端重发的 FIN 报⽂，那么 2MSL 时间将重新计时。<strong>可以看到 2MSL时长，这其实是相当于至少允许报文丢失一次</strong>。比如，若 ACK 在一个 MSL 内丢失，这样被动方重发的 FIN 会在第 2 个 MSL 内到达，TIME_WAIT 状态的连接可以应对。<br><img data-src="/transblog/images/597362314252895.png"></p></li><li><p><strong>防止新连接收到旧链接的TCP报文</strong><br>如果新、旧连接的TCP四元组完全一致，则内核协议栈无法区分这两条连接。<strong>2MSL 的时间足以保证两个方向上的数据都被丢弃，使得原来连接的数据包在网络中都自然消失，再出现的数据包一定是新连接上产生的</strong>。<br>例如：服务端在关闭连接之前发送的 SEQ &#x3D; 301 报文，被网络延迟了。接着，服务端以相同的四元组重新打开了新连接，前面被延迟的 SEQ &#x3D; 301 这时抵达了客户端，而且该数据报文的序列号刚好在客户端接收窗口内，因此客户端会正常接收这个数据报文，但是这个数据报文是上一个连接残留下来的，这样就产生数据错乱等严重的问题。</p></li></ol><p><strong>没有TIME_WAIT可以吗</strong>？<br>没有数据要发送的话也是可以的，假设客户端没有 TIME_WAIT 状态，而是在发完最后一次回 ACK 报文就直接进入 CLOSE 状态，如果该 ACK 报文丢失了，服务端则重传的 FIN 报文，而这时客户端已经进入到关闭状态了，在收到服务端重传的 FIN 报文后，就会回 RST 报文。</p><h5 id="解决TIME-WAIT过多"><a href="#解决TIME-WAIT过多" class="headerlink" title="解决TIME_WAIT过多"></a>解决TIME_WAIT过多</h5><p>如果客户端 TIME_WAIT 状态的连接过多，当端口资源被耗尽，就无法与这个服务器再建立连接了。</p><h6 id="net-ipv4-tcp-tw-reuse"><a href="#net-ipv4-tcp-tw-reuse" class="headerlink" title="net.ipv4.tcp_tw_reuse"></a>net.ipv4.tcp_tw_reuse</h6><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">net.ipv4.tcp_tw_reuse = 1</span><br></pre></td></tr></table></figure><p>开启了这个内核参数后，客户端调用 connect 函数时：</p><ul><li>如果没有开启内核参数，那么内核就会选择下一个端口，然后继续判断，直到找到一个没有被相同四元组的连接使用的端口， 如果端口资源耗尽还是没找到，那么 connect 函数就会返回错误。</li><li>如果开启了内核参数，就会判断该四元组的连接状态是否处于 TIME_WAIT 状态，如果连接处于 TIME_WAIT 状态并且该状态持续的时间超过了 1 秒，那么就会重用该连接，这时 connect 就会返回成功。</li></ul><p>提醒一下，<strong>是客户端（连接发起方） 在调用 connect() 函数时才起作用</strong>，所以在服务端开启这个参数是没有效果的。</p><p><strong>为什么 tcp_tw_reuse 默认是关闭的</strong>？<br>问题一：因为快速复用 TIME_WAIT 状态的端口，导致新连接可能<strong>被回绕序列号的 RST 报文断开</strong>(未开启tcp_timestamps)。</p><ul><li>被网络延迟 RST 报文抵达了客户端，而且 RST 报文的序列号在客户端的接收窗口内，RST 报文会被客户端接受了，于是客户端的连接就断开了。</li><li>而如果不跳过 TIME_WAIT 状态，而是停留 2MSL 时长，那么这个 RST 报文就不会出现下一个新的连接。</li></ul><p>问题二：如果快速复用后，第四次挥手的 ACK 报文丢失了，服务端会触发重传第三次挥手报文，处于 syn_sent 状态的客户端收到服务端重传第三次挥手报文，则会回 RST 给服务端。</p><p>同时，快速复用后客户端会发送SYN报文，处于 last_ack 状态的服务端收到了 SYN 报文后，会回复Challenge ACK 。处于 syn_sent 状态的客户端收到 Challenge ACK 后，发现不是自己期望收到的确认号，于是就会回复 RST 报文，服务端收到后，就会断开连接。</p><p><img data-src="/transblog/images/347242915273272.png"></p><h6 id="net-ipv4-tcp-tw-recycle"><a href="#net-ipv4-tcp-tw-recycle" class="headerlink" title="net.ipv4.tcp_tw_recycle"></a>net.ipv4.tcp_tw_recycle</h6><p>如果开启该选项的话，允许处于 TIME_WAIT 状态的连接被快速回收</p><p>要使得上面两个选项生效，有一个前提条件，就是要打开 TCP 时间戳，即 net.ipv4.tcp_timestamps&#x3D;1（默认即为 1)）。</p><h6 id="net-ipv4-tcp-max-tw-buckets"><a href="#net-ipv4-tcp-max-tw-buckets" class="headerlink" title="net.ipv4.tcp_max_tw_buckets"></a>net.ipv4.tcp_max_tw_buckets</h6><p>这个值默认为 18000，当系统中处于 TIME_WAIT 的连接一旦超过这个值时，系统就会将后面的 TIME_WAIT 连接状态重置，这个方法比较暴力。</p><h6 id="程序中使用-SO-LINGER"><a href="#程序中使用-SO-LINGER" class="headerlink" title="程序中使用 SO_LINGER"></a>程序中使用 SO_LINGER</h6><p>设置 socket 选项，来设置调用 close 关闭连接行为。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">linger</span> so_linger;</span><br><span class="line">so_linger.l_onoff = <span class="number">1</span>;</span><br><span class="line">so_linger.l_linger = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">setsockopt</span>(s, SOL_SOCKET, SO_LINGER, &amp;so_linger,<span class="built_in">sizeof</span>(so_linger));</span><br></pre></td></tr></table></figure><p>如果l_onoff为非 0， 且l_linger值为 0，那么调用close后，会立该发送一个RST标志给对端，该 TCP 连接将跳过四次挥手，也就跳过了TIME_WAIT状态，直接关闭。</p><p>《UNIX网络编程》：<strong>TIME_WAIT 是我们的朋友，它是有助于我们的，不要试图避免这个状态，而是应该弄清楚它。如果服务端要避免过多的 TIME_WAIT 状态的连接，就永远不要主动断开连接，让客户端去断开，由分布在各处的客户端去承受 TIME_WAIT。</strong></p><h5 id="服务器大量-TIME-WAIT-状态"><a href="#服务器大量-TIME-WAIT-状态" class="headerlink" title="服务器大量 TIME_WAIT 状态"></a>服务器大量 TIME_WAIT 状态</h5><p>什么场景下服务端会主动断开连接呢？</p><ol><li>HTTP 没有使用长连接（Keep-Alive）：<figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Connection</span><span class="punctuation">: </span>Keep-Alive</span><br></pre></td></tr></table></figure></li></ol><ul><li><p><strong>客户端禁用了 HTTP Keep-Alive，服务端开启 HTTP Keep-Alive，谁是主动关闭方？</strong><br>当客户端禁用了 HTTP Keep-Alive，这时候 HTTP 请求的 header 就会有 Connection:close 信息，这时服务端在发完 HTTP 响应后，就会主动关闭连接。为什么要这么设计呢？HTTP 是请求-响应模型，发起方一直是客户端，HTTP Keep-Alive 的初衷是为客户端后续的请求重用连接，如果我们在某次 HTTP 请求-响应模型中，请求的 header 定义了 connection：close 信息，那不再重用这个连接的时机就只有在服务端了，所以我们在 HTTP 请求-响应这个周期的「末端」关闭连接是合理的。</p></li><li><p><strong>客户端开启了 HTTP Keep-Alive，服务端禁用了 HTTP Keep-Alive，谁是主动关闭方？</strong><br>当客户端开启了 HTTP Keep-Alive，而服务端禁用了 HTTP Keep-Alive，这时服务端在发完 HTTP 响应后，服务端也会主动关闭连接。为什么要这么设计呢？在服务端主动关闭连接的情况下，只要调用一次 close() 就可以释放连接，剩下的工作由内核 TCP 栈直接进行了处理，整个过程只有一次 syscall；<strong>如果是要求客户端关闭，则服务端在写完最后一个 response 之后需要把这个 socket 放入 readable 队列，调用 select &#x2F; epoll 去等待事件；然后调用一次 read() 才能知道连接已经被关闭，这其中是两次 syscall</strong>，多一次用户态程序被激活执行，而且 socket 保持时间也会更长。</p></li></ul><p>所以，根据大多数 Web 服务的实现，不管哪一方禁用了 HTTP Keep-Alive，都会导致服务端在处理完一个 HTTP 请求后，就主动关闭连接，此时服务端上就会出现大量的 TIME_WAIT 状态的连接。都是由服务端主动关闭连接，那么此时服务端上就会出现 TIME_WAIT 状态的连接。</p><ol start="2"><li><p>HTTP 长连接超时<br>为了避免资源浪费的情况，web 服务软件一般都会提供一个参数，用来指定 HTTP 长连接的超时时间，如 nginx 提供的 keepalive_timeout 参数。<br>假设设置了 HTTP 长连接的超时时间是 60 秒，nginx 就会启动一个「定时器」，如果客户端在完后一个 HTTP 请求后，在 60 秒内都没有再发起新的请求，定时器的时间一到，nginx 就会触发回调函数来关闭该连接，那么此时服务端上就会出现 TIME_WAIT 状态的连接。</p></li><li><p>HTTP 长连接的请求数量达到上限<br>Web 服务端通常会有个参数，来定义一条 HTTP 长连接上最大能处理的请求数量，当超过最大限制时，就会主动关闭连接。<br>比如 nginx 的 keepalive_requests 这个参数，这个参数是指一个 HTTP 长连接建立之后，nginx 就会为这个连接设置一个计数器，记录这个 HTTP 长连接上已经接收并处理的客户端请求的数量。如果达到这个参数设置的最大值时，则 nginx 会主动关闭这个长连接，那么此时服务端上就会出现 TIME_WAIT 状态的连接。<br>对于一些 QPS 比较高的场景，比如超过 10000 QPS，甚至达到 30000 , 50000 甚至更高，如果 keepalive_requests 参数值是 100，这时候<strong>nginx 就会很频繁地关闭连接，那么此时服务端上就会出大量的 TIME_WAIT 状态</strong>。</p></li></ol><h5 id="服务器大量-CLOSE-WAIT-状态"><a href="#服务器大量-CLOSE-WAIT-状态" class="headerlink" title="服务器大量 CLOSE_WAIT 状态"></a>服务器大量 CLOSE_WAIT 状态</h5><p>当服务端出现大量 CLOSE_WAIT 状态的连接的时候，说明服务端的程序没有调用 close 函数关闭连接。需要排查代码</p><p>一个普通的 TCP 服务端的流程：</p><ol><li>创建服务端 socket，bind 绑定端口、listen 监听端口</li><li>将服务端 socket 注册到 epoll</li><li>epoll_wait 等待连接到来，连接到来时，调用 accpet 获取已连接的 socket</li><li>将已连接的 socket 注册到 epoll</li><li>epoll_wait 等待事件发生</li><li>对方连接关闭时，我方调用 close</li></ol><ul><li>第一个原因：第 2 步没有做，没有将服务端 socket 注册到 epoll，这样有新连接到来时，服务端没办法感知这个事件，也就无法获取到已连接的 socket，那服务端自然就没机会对 socket 调用 close 函数了。不过这种原因发生的概率比较小，这种属于明显的代码逻辑 bug，在前期 read view 阶段就能发现的了。</li><li>第二个原因： 第 3 步没有做，有新连接到来时没有调用 accpet 获取该连接的 socket，导致当有大量的客户端主动断开了连接，而服务端没机会对这些 socket 调用 close 函数，从而导致服务端出现大量 CLOSE_WAIT 状态的连接。发生这种情况可能是因为服务端在执行 accpet 函数之前，代码卡在某一个逻辑或者提前抛出了异常。</li><li>第三个原因：第 4 步没有做，通过 accpet 获取已连接的 socket 后，没有将其注册到 epoll，导致后续收到 FIN 报文的时候，服务端没办法感知这个事件，那服务端就没机会调用 close 函数了。发生这种情况可能是因为服务端在将已连接的 socket 注册到 epoll 之前，代码卡在某一个逻辑或者提前抛出了异常。</li><li>第四个原因：第 6 步没有做，当发现客户端关闭连接后，服务端没有执行 close 函数，可能是因为代码漏处理，或者是在执行 close 函数之前，代码卡在某一个逻辑，比如发生死锁等等。</li></ul><h5 id="TIME-WAIT-收到-SYN"><a href="#TIME-WAIT-收到-SYN" class="headerlink" title="TIME_WAIT 收到 SYN"></a>TIME_WAIT 收到 SYN</h5><p>在TIME_WAIT状态的 TCP 连接，收到 SYN 后会发生什么？<br>在 TCP 正常挥手过程中，处于 TIME_WAIT 状态的连接，收到相同四元组的 SYN 后会发生什么？</p><p>针对这个问题，<strong>关键是要看 SYN 的「序列号和时间戳」是否合法</strong><br>合法 SYN：客户端的 SYN 的「序列号」比服务端「期望下一个收到的序列号」要大，并且 SYN 的「时间戳」比服务端「最后收到的报文的时间戳」要大。<br>非法 SYN：客户端的 SYN 的「序列号」比服务端「期望下一个收到的序列号」要小，或者 SYN 的「时间戳」比服务端「最后收到的报文的时间戳」要小。</p><p>如果处于 TIME_WAIT 状态的连接收到「合法的 SYN 」后，<strong>就会重用此四元组连接，跳过 2MSL 而转变为 SYN_RECV 状态</strong>，接着就能进行建立连接过程。<br>如果处于 TIME_WAIT 状态的连接收到「非法的 SYN 」后，<strong>就会再回复一个第四次挥手的 ACK 报文，服务端收到后，发现并不是自己期望收到确认号，就回 RST 报文给客户端</strong>。</p><h5 id="TIME-WAIT-收到-RST"><a href="#TIME-WAIT-收到-RST" class="headerlink" title="TIME_WAIT 收到 RST"></a>TIME_WAIT 收到 RST</h5><p>那么处于 TIME_WAIT 状态的连接，收到 RST 会断开连接吗？</p><p>net.ipv4.tcp_rfc1337 这个内核参数（默认情况是为 0）：<br>如果这个参数设置为 0， 收到 RST 报文会提前结束 TIME_WAIT 状态，释放连接。<br>如果这个参数设置为 1， 就会丢掉 RST 报文。</p><h3 id="TCP的关闭"><a href="#TCP的关闭" class="headerlink" title="TCP的关闭"></a>TCP的关闭</h3><h4 id="close函数–粗暴关闭"><a href="#close函数–粗暴关闭" class="headerlink" title="close函数–粗暴关闭"></a>close函数–粗暴关闭</h4><p><strong>如果有一个进程调用了 close 关闭只是让 socket 引用计数减1，并不会导致 socket 不可用，其他进程还是可以正常读写该 socket，直到引用计数变为 0，就会对socket进行彻底释放</strong>。</p><p>客户端：<br>如果客户端是用 close 函数来关闭连接，那么在 TCP 四次挥手过程中，如果收到了服务端发送的数据，客户端的内核会回 RST 报文给服务端，然后内核会释放连接，这时就不会经历完成的 TCP 四次挥手，所以调用 close 是粗暴的关闭</p><ul><li>在数据接收方向，系统内核会将socket设置为不可读，任何读操作都会返回异常；</li><li>在数据发送方向，系统内核尝试将发送缓冲区的数据发送给对端，并最后向对端发送一个FIN报文，接下来如果再对socket进行写操作会返回异常。</li></ul><p>服务端：<br>当服务端收到 RST 后，内核就会释放连接，当服务端应用程序再次发起读操作或者写操作时，就会触发异常，就能感知到连接已经被释放了：</p><ul><li>如果是读操作，则会返回报错，也就是我们常见的Connection reset by peer。</li><li>如果是写操作，那么程序会产生 SIGPIPE 信号，应用层代码可以捕获并处理信号，如果不处理，则默认情况下进程会终止，异常退出。</li></ul><p><img data-src="/transblog/images/185051015257934.png"></p><h4 id="shutdown函数–优雅关闭"><a href="#shutdown函数–优雅关闭" class="headerlink" title="shutdown函数–优雅关闭"></a>shutdown函数–优雅关闭</h4><p>可以指定 socket 只关闭发送方向而不关闭读取方向，也就是 socket 不再有发送数据的能力，但是还是具有接收数据的能力。<br>shutdown 函数因为可以指定只关闭发送方向而不关闭读取方向，所以即使在 TCP 四次挥手过程中，如果收到了服务端发送的数据，客户端也是可以正常读取到该数据的，<strong>然后就会经历完整的 TCP 四次挥手，所以调用 shutdown 是优雅的关闭</strong>。</p><p><img data-src="/transblog/images/11861215259229.png"></p><h4 id="区别"><a href="#区别" class="headerlink" title="区别"></a>区别</h4><ul><li>**close用来关闭套接字(引用为0时)**，将套接字描述符（或句柄）从内存清除，之后再也不能使用该套接字。应用程序关闭套接字后，与该套接字相关的连接和缓存也失去了意义，TCP协议会自动触发关闭连接的操作。</li><li><strong>shutdown用来关闭连接，而不是套接字</strong>，不管调用多少次 shutdown，套接字依然存在，直到调用close将套接字从内存清除。(即调用shutdown后，仍然需要调用close关闭socket)</li></ul><h4 id="如果被动方一直不发第三次挥手，会怎么样"><a href="#如果被动方一直不发第三次挥手，会怎么样" class="headerlink" title="如果被动方一直不发第三次挥手，会怎么样"></a>如果被动方一直不发第三次挥手，会怎么样</h4><p>如果由于代码错误或者其他一些原因，被动方就是不执行第三次挥手。<br>这时候，主动方会根据自身第一次挥手的时候用的是 close() 还是 shutdown(fd, SHUT_WR) ，有不同的行为表现。</p><ul><li>如果是 shutdown(fd, SHUT_WR) ，说明主动方其实只关闭了写，但还可以读，此时会一直处于 FIN-WAIT-2， 死等被动方的第三次挥手。</li><li>如果是 close()， 说明主动方读写都关闭了，这时候会处于 FIN-WAIT-2一段时间，这个时间由 net.ipv4.tcp_fin_timeout 控制，一般是 60s，这个值正好跟2MSL一样 。超过这段时间之后，状态不会变成 <code>TIME-WAIT</code>，而是直接变成<code>CLOSED</code>。<figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash"><span class="built_in">cat</span> /proc/sys/net/ipv4/tcp_fin_timeout</span></span><br><span class="line">60</span><br></pre></td></tr></table></figure></li></ul><h3 id="TCP保活机制"><a href="#TCP保活机制" class="headerlink" title="TCP保活机制"></a>TCP保活机制</h3><p>TCP保活(KeepAlive)功能工作过程中，开启该功能的一端会发现对方处于以下四种状态之一：</p><ol><li><p>对方主机仍在工作，并且可以到达。此时请求端将保活计时器重置。如果在计时器超时之前应用程序通过该连接传输数据，计时器再次被设定为保活时间值。</p></li><li><p>对方主机已经崩溃，包括已经关闭或者正在重新启动。这时对方的TCP将不会响应。请求端不会接收到响应报文，并在经过保活时间间隔指定的时间后超时。超时前，请求端会持续发送探测报文，一共发送保活探测数指定次数的探测报文，如果请求端没有收到任何探测报文的响应，那么它将认为对方主机已经关闭，连接也将被断开。</p></li><li><p>客户主机崩溃并且已重启。在这种情况下，请求端会收到一个对其保活探测报文的响应，但这个响应是一个重置报文段RST，请求端将会断开连接。</p></li><li><p>对方主机仍在工作，但是由于某些原因不能到达请求端（例如网络无法传输，而且可能使用ICMP通知也可能不通知对方这一事实）。这种情况与状态2相同，因为TCP不能区分状态2与状态4，结果是都没有收到探测报文的响应。</p></li></ol><p>其存在以下两点主要弊端：</p><ol><li>在出现短暂的网络错误的时候，保活机制会使一个好的连接断开；</li><li>保活机制会占用不必要的带宽；</li></ol><h4 id="应用层自己实现的心跳包"><a href="#应用层自己实现的心跳包" class="headerlink" title="应用层自己实现的心跳包"></a>应用层自己实现的心跳包</h4><p>由应用程序自己发送心跳包来检测连接是否正常，大致的方法是：服务器在一个 Timer事件中定时 向客户端发送一个短小精悍的数据包，然后启动一个低级别的线程，在该线程中不断检测客户端的回应， 如果在一定时间内没有收到客户端的回应，即认为客户端已经掉线；同样，如果客户端在一定时间内没 有收到服务器的心跳包，则认为连接不可用。</p><h4 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h4><ol><li><p>TCP自带的KeepAlive使用简单，发送的数据包相比应用层心跳检测包更小，仅提供检测连接功能</p></li><li><p>应用层心跳包不依赖于传输层协议，无论传输层协议是TCP还是UDP都可以用</p></li><li><p>应用层心跳包可以定制，可以应对更复杂的情况或传输一些额外信息</p></li><li><p>KeepAlive仅代表连接保持着，而心跳包往往还代表客户端可正常工作</p></li></ol><h3 id="TCP常见问题"><a href="#TCP常见问题" class="headerlink" title="TCP常见问题"></a>TCP常见问题</h3><h4 id="如何理解是-TCP-面向字节流协议"><a href="#如何理解是-TCP-面向字节流协议" class="headerlink" title="如何理解是 TCP 面向字节流协议"></a>如何理解是 TCP 面向字节流协议</h4><p>当消息通过 UDP 协议传输时，<strong>操作系统不会对消息进行拆分</strong>，在组装好 UDP 头部后就交给网络层来处理，所以发出去的 UDP 报文中的数据部分就是完整的用户消息，也就是每个 UDP 报文就是一个用户消息的边界，这样接收方在接收到 UDP 报文后，读一个 UDP 报文就能读取到完整的用户消息。<br>操作系统在收到 UDP 报文后，会将其插入到队列里，队列里的每一个元素就是一个 UDP 报文，这样当用户调用 recvfrom() 系统调用读数据的时候，就会从队列里取出一个数据，然后从内核里拷贝给用户缓冲区。</p><p><img data-src="/transblog/images/263713616268767.png"></p><p>当用户消息通过 TCP 协议传输时，消息可能会被操作系统分组成多个的 TCP 报文，也就是一个完整的用户消息被拆分成多个 TCP 报文进行传输。例如：”Hello World”、”Hello” + “ World”、”He”+”llo World”等各种情况，所以，我们不能认为一个用户消息对应一个 TCP 报文，正因为这样，所以 TCP 是面向字节流的协议。</p><h4 id="TCP粘包和拆包"><a href="#TCP粘包和拆包" class="headerlink" title="TCP粘包和拆包"></a>TCP粘包和拆包</h4><p>如果客户端连续不断的向服务端发送数据包时，服务端接收的数据会出现两个数据包粘在一起的情况，这时接收方不知道消息的边界的话，是无法读出有效的消息。</p><h5 id="TCP发生粘包和拆包原因"><a href="#TCP发生粘包和拆包原因" class="headerlink" title="TCP发生粘包和拆包原因"></a>TCP发生粘包和拆包原因</h5><ol><li>要发送的数据大于TCP发送缓冲区剩余空间大小，将会发生拆包。</li><li>待发送数据大于MSS（最大报文长度），TCP在传输前将进行拆包。</li><li>要发送的数据小于TCP发送缓冲区的大小，TCP将多次写入缓冲区的数据一次发送出去，将会发生粘包。</li><li>接收数据端的应用层没有及时读取接收缓冲区中的数据，将发生粘包</li></ol><h5 id="粘包的解决办法"><a href="#粘包的解决办法" class="headerlink" title="粘包的解决办法"></a>粘包的解决办法</h5><p>解决问题的关键在于<strong>如何给每个数据包添加边界信息</strong>，常用的方法有如下几个：</p><p>一般有三种方式分包的方式：</p><ol><li>固定长度的消息<br>规定一个消息的长度是 64 个字节，当接收方接满 64 个字节，就认为这个内容是一个完整且有效的消息。但是这种方式灵活性不高，实际中很少用。</li><li>特殊字符作为边界<br>HTTP 是一个非常好的例子。</li></ol><p><img data-src="/transblog/images/409224516247382.png"></p><ol start="3"><li><p>自定义消息结构。<br>首先 4 个字节大小的变量来表示数据长度，真正的数据则在后面。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> &#123; </span><br><span class="line">    <span class="type">u_int32_t</span> message_length; </span><br><span class="line">    <span class="type">char</span> message_data[]; </span><br><span class="line">&#125; message;</span><br></pre></td></tr></table></figure></li><li><p>发送端给每个数据包添加包首部，首部中应该至少包含数据包的长度，这样接收端在接收到数据后，通过读取包首部的长度字段，便知道每一个数据包的实际长度了。</p></li></ol><h5 id="UDP协议是否会发生粘包问题"><a href="#UDP协议是否会发生粘包问题" class="headerlink" title="UDP协议是否会发生粘包问题"></a>UDP协议是否会发生粘包问题</h5><p>不会。UDP是基于报文发送的，在UDP首部采用了16bit来指示UDP数据报文的长度，因此在应用层能很好的将不同的数据报文区分开，从而避免粘包和拆包的问题。</p><h4 id="有一个-IP-的服务端监听了一个端口，它的-TCP-的最大连接数是多少？"><a href="#有一个-IP-的服务端监听了一个端口，它的-TCP-的最大连接数是多少？" class="headerlink" title="有一个 IP 的服务端监听了一个端口，它的 TCP 的最大连接数是多少？"></a>有一个 IP 的服务端监听了一个端口，它的 TCP 的最大连接数是多少？</h4><p>客户端 IP 和端口是可变的，最大TCP连接数 &#x3D; 客户端的 IP 数 * 客户端的端口数</p><p>对 IPv4，客户端的 IP 数最多为 2 的 32 次方，客户端的端口数最多为 2 的 16 次方，所以，服务端单机最大 TCP 连接数，约为 2 的 48 次方。</p><p>服务端最大并发 TCP 连接数远不能达到理论上限，会受以下因素影响：</p><ul><li>文件描述符限制，每个 TCP 连接都是一个文件，如果文件描述符被占满了，会发生 Too many open files。Linux 对可打开的文件描述符的数量分别作了三个方面的限制：<br>系统级：当前系统可打开的最大数量，通过 cat &#x2F;proc&#x2F;sys&#x2F;fs&#x2F;file-max 查看；<br>用户级：指定用户可打开的最大数量，通过 cat &#x2F;etc&#x2F;security&#x2F;limits.conf 查看；<br>进程级：单个进程可打开的最大数量，通过 cat &#x2F;proc&#x2F;sys&#x2F;fs&#x2F;nr_open 查看；</li><li>内存限制，每个 TCP 连接都要占用一定内存，操作系统的内存是有限的，如果内存资源被占满后，会发生 OOM。</li></ul><h4 id="TCP-和-UDP-可以同时绑定相同的端口吗？"><a href="#TCP-和-UDP-可以同时绑定相同的端口吗？" class="headerlink" title="TCP 和 UDP 可以同时绑定相同的端口吗？"></a>TCP 和 UDP 可以同时绑定相同的端口吗？</h4><p>可以，<strong>在操作系统的协议栈中，TCP和UDP是两个不同的模块</strong>，当主机收到数据包后，可以在 IP 包头的「协议号」字段知道该数据包是 TCP&#x2F;UDP，所以可以根据这个信息确定送给哪个模块（TCP&#x2F;UDP）处理，送给 TCP&#x2F;UDP 模块的报文根据「端口号」确定送给哪个应用程序处理。</p><p><img data-src="/transblog/images/406574211246216.png"></p><h4 id="多个-TCP-服务进程可以绑定同一个端口吗？"><a href="#多个-TCP-服务进程可以绑定同一个端口吗？" class="headerlink" title="多个 TCP 服务进程可以绑定同一个端口吗？"></a>多个 TCP 服务进程可以绑定同一个端口吗？</h4><p><strong>如果两个 TCP 服务进程同时绑定的 IP 地址和端口都相同，那么执行 bind() 时候就会出错，错误是“Address already in use”。</strong></p><p>注意，如果 TCP 服务进程 A 绑定的地址是 0.0.0.0 和端口 8888，而如果 TCP 服务进程 B 绑定的地址是 192.168.1.100 地址（或者其他地址）和端口 8888，那么执行 bind() 时候也会出错。这是因为 0.0.0.0 地址比较特殊，代表任意地址，意味着绑定了 0.0.0.0 地址，相当于把主机上的所有 IP 地址都绑定了。</p><h4 id="重启-TCP-服务进程时，为什么会有“Address-in-use”的报错信息？"><a href="#重启-TCP-服务进程时，为什么会有“Address-in-use”的报错信息？" class="headerlink" title="重启 TCP 服务进程时，为什么会有“Address in use”的报错信息？"></a>重启 TCP 服务进程时，为什么会有“Address in use”的报错信息？</h4><p>TCP四次挥手后，会在 TIME_WAIT 这个状态里停留一段时间，这个时间大约为 2MSL。</p><p>当 TCP 服务进程重启时，服务端会出现 TIME_WAIT 状态的连接，TIME_WAIT 状态的连接使用的 IP+PORT 仍然被认为是一个有效的 IP+PORT 组合，相同机器上不能够在该 IP+PORT 组合上进行绑定，那么执行 bind() 函数的时候，就会返回了 Address already in use 的错误。</p><p>如何解决：<br>对 socket 设置 SO_REUSEADDR 属性，可以解决这个问题。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> on = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">setsockopt</span>(listenfd, SOL_SOCKET, SO_REUSEADDR, &amp;on, <span class="built_in">sizeof</span>(on));</span><br></pre></td></tr></table></figure><p>如果当前启动进程绑定的 IP+PORT 与处于TIME_WAIT 状态的连接占用的 IP+PORT 存在冲突，但是新启动的进程使用了 SO_REUSEADDR 选项，那么该进程就可以绑定成功。</p><h4 id="客户端的端口可以重复使用吗？"><a href="#客户端的端口可以重复使用吗？" class="headerlink" title="客户端的端口可以重复使用吗？"></a>客户端的端口可以重复使用吗？</h4><p>TCP 连接是由四元组（源IP地址，源端口，目的IP地址，目的端口）唯一确认的，那么<strong>只要四元组中其中一个元素发生了变化，那么就表示不同的 TCP 连接的</strong>。所以如果客户端已使用端口 64992 与服务端 A 建立了连接，那么客户端要与服务端 B 建立连接，还是可以使用端口 64992 的，因为内核是通过四元祖信息来定位一个 TCP 连接的，并不会因为客户端的端口号相同，而导致连接冲突的问题。</p><h4 id="既然-IP-层会分片，为什么-TCP-层还需要-MSS-呢？"><a href="#既然-IP-层会分片，为什么-TCP-层还需要-MSS-呢？" class="headerlink" title="既然 IP 层会分片，为什么 TCP 层还需要 MSS 呢？"></a>既然 IP 层会分片，为什么 TCP 层还需要 MSS 呢？</h4><p><img data-src="/transblog/images/444213610272904.png"></p><p>如果一个 IP 分片丢失，整个 IP 报文的所有分片都得重传。<strong>因为 IP 层本身没有超时重传机制，它由传输层的 TCP 来负责超时和重传。</strong><br>为了达到最佳的传输效能 TCP 协议在建立连接的时候通常要协商双方的 MSS 值，<strong>当 TCP 层发现数据超过 MSS 时，则就先会进行分片，当然由它形成的 IP 包的长度也就不会大于 MTU ，自然也就不用 IP 分片了。</strong><br>经过 TCP 层分片后，如果一个 TCP 分片丢失后，进行重发时也是以 MSS 为单位，而不用重传所有的分片，大大增加了重传的效率。</p><p><strong>UDP层的分片</strong></p><blockquote><ol><li>由于UDP是不需要保证可靠性的，那么它就不会保存发送的数据包，TCP之所以保存发送的数据包是因为要进行重传。所以UDP本身是没有像TCP一样的发送缓冲区的。这就导致了对UDP进行write系统调用的时候，实际上应用层的数据是直接传输到IP层，由于IP层本身也不会有缓冲区，数据就会直接写到链路层的输出队列中。</li><li>在这种情况下，IP层会不会对来自UDP的数据进行分片呢？这个取决于UDP数据报的大小。如果UDP数据报的大小大于链路层的MTU，那么IP层就会直接进行分片，然后在发送到链路层的输出队列中，反之，则不会进行分片，直接加上IP头部发送到链路层的输出队列中。</li></ol></blockquote><p><img data-src="/transblog/images/2871612129098.png"></p><h4 id="TCP-连接，一端断电和进程崩溃有什么区别？"><a href="#TCP-连接，一端断电和进程崩溃有什么区别？" class="headerlink" title="TCP 连接，一端断电和进程崩溃有什么区别？"></a>TCP 连接，一端断电和进程崩溃有什么区别？</h4><p>在没有开启 TCP keepalive，且双方一直没有数据交互的情况下，如果客户端的「主机崩溃」了，服务端的 TCP 连接将会一直处于 ESTABLISHED 连接状态，直到服务端重启进程。</p><p>进程崩溃，在 kill 掉进程后，服务端会发送 FIN 报文，与客户端进行四次挥手。</p><p>有数据传输的场景：</p><ol><li>客户端主机宕机，又迅速重启<br>只要有一方重启完成后，收到之前 TCP 连接的报文，都会回复 RST 报文，以断开连接。</li><li>客户端主机宕机，一直没有重启<br>这种情况，服务端超时重传报文的次数达到一定阈值后，内核就会判定出该 TCP 有问题，然后通过 Socket 接口告诉应用程序该 TCP 连接出问题了，于是服务端的 TCP 连接就会断开。</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cat /proc/sys/net/ipv4/tcp_retries2</span><br><span class="line">15</span><br></pre></td></tr></table></figure><p>内核会根据 tcp_retries2 设置的值，计算出一个 timeout，如果重传间隔超过这个 timeout，则认为超过了阈值，就会停止重传，然后就会断开 TCP 连接。</p><h4 id="拔掉网线后，-原本的-TCP-连接还存在吗？"><a href="#拔掉网线后，-原本的-TCP-连接还存在吗？" class="headerlink" title="拔掉网线后， 原本的 TCP 连接还存在吗？"></a>拔掉网线后， 原本的 TCP 连接还存在吗？</h4><p>存在，TCP在内核中以struct socket结构体存在，当拔掉网线的时候，操作系统并不会变更该结构体的任何内容，所以 TCP 连接的状态也不会发生改变。所以拔网线的动作不会影响TCP的状态<br>后续：</p><ol><li><p>拔掉网线后，有数据传输，触发超时重传机制<br>如果在服务端重传报文的过程中，客户端刚好把网线插回去了，无事发生。<br>如果在服务端重传报文的过程中，客户端一直没有将网线插回去，服务端超时重传报文的次数达到一定阈值后，内核就会判定出该 TCP 有问题，然后通过 Socket 接口告诉应用程序该 TCP 连接出问题了，于是服务端的 TCP 连接就会断开。而等客户端插回网线后，如果客户端向服务端发送了数据，由于服务端已经没有与客户端相同四元祖的 TCP 连接了，因此服务端内核就会回复 RST 报文，客户端收到后就会释放该 TCP 连接。</p></li><li><p>拔掉网线后，没有数据传输<br>如果没有开启 TCP keepalive 机制，在客户端拔掉网线后，那么客户端和服务端的 TCP 连接将会一直保持存在。<br>如果开启了 TCP keepalive 机制，对端主机宕机，或对端由于其他原因导致报文不可达。当 TCP 保活的探测报文发送给对端后，没有响应连续几次，达到保活探测次数后，TCP 会报告该连接已经死亡。</p></li></ol><h3 id="TCP可靠传输"><a href="#TCP可靠传输" class="headerlink" title="TCP可靠传输"></a>TCP可靠传输</h3><h4 id="超时重传"><a href="#超时重传" class="headerlink" title="超时重传"></a>超时重传</h4><p><img data-src="/transblog/images/289163016260231.png"></p><p>当超时时间 RTO 较大时，重发就慢，丢了老半天才重发，没有效率，性能差；<br>当超时时间 RTO 较小时，会导致可能并没有丢就重发，于是重发的就快，会增加网络拥塞，导致更多的超时，更多的超时导致更多的重发。<br>精确的测量超时时间 RTO 的值是非常重要的，这可让重传机制更高效。超时重传时间 RTO 的值应该略大于报文往返 RTT 的值。</p><p> Linux 是如何计算 RTO 的呢？估计往返时间，通常需要采样以下两个：</p><ol><li>需要 TCP 通过采样 RTT 的时间，然后进行加权平均，算出一个平滑 RTT 的值，而且这个值还是要不断变化的，因为网络状况不断地变化。</li><li>除了采样 RTT，还要采样 RTT 的波动范围，这样就避免如果 RTT 有一个大的波动的话，很难被发现的情况。</li></ol><p>RFC6289 建议使用以下的公式计算 RTO：</p><p><img data-src="/transblog/images/589313816260408.png"></p><p>其中 SRTT 是计算平滑的RTT ，DevRTR 是计算平滑的RTT 与 最新 RTT 的差距。<br>在 Linux 下，α &#x3D; 0.125，β &#x3D; 0.25， μ &#x3D; 1，∂ &#x3D; 4。别问怎么来的，问就是大量实验中调出来的。<br>每当遇到一次超时重传的时候，都会将下一次超时时间间隔设为先前值的两倍。两次超时，就说明网络环境差，不宜频繁反复发送。</p><h4 id="快速重传"><a href="#快速重传" class="headerlink" title="快速重传"></a><span id="target1">快速重传</span></h4><p>快速重传机制，它不以时间为驱动，而是以数据驱动重传。</p><p><img data-src="/transblog/images/53474316245960.png"></p><ul><li>第一份 Seq1 先送到了，于是就 Ack 回 2；</li><li>结果 Seq2 因为某些原因没收到，Seq3 到达了，于是还是 Ack 回 2；</li><li>后面的 Seq4 和 Seq5 都到了，但还是 Ack 回 2，因为 Seq2 还是没有收到；</li><li>发送端收到了三个 Ack &#x3D; 2 的确认，知道了 Seq2 还没有收到，就会在定时器过期之前，重传丢失的 Seq2。</li><li>最后，收到了 Seq2，此时因为 Seq3，Seq4，Seq5 都收到了，于是 Ack 回 6 。</li></ul><p>此时会引出一个问题：只丢Seq2和Seq2、Seq3都丢失了，都是回复 ACK2 给发送方，但是发送方并不清楚这连续的 ACK2 是接收方收到哪个报文而回复的， 那是选择重传 Seq2 一个报文，还是重传 Seq2 之后已发送的所有报文呢，那就要用到下面这种机制：</p><h4 id="SACK-选择性确认"><a href="#SACK-选择性确认" class="headerlink" title="SACK(选择性确认)"></a>SACK(选择性确认)</h4><p>需要在 TCP 头部「选项」字段里加一个 SACK 的东西，<strong>它可以将已收到的数据的信息发送给「发送方」</strong>，这样发送方就可以知道哪些数据收到了，哪些数据没收到，知道了这些信息，就可以<strong>只重传丢失的数据</strong>。<br>发送方收到了三次同样的 ACK 确认报文，于是就会触发快速重发机制，通过 SACK 信息发现只有 200~299 这段数据丢失，则重发时，就只选择了这个 TCP 段进行重复。</p><p><img data-src="/transblog/images/95104917262239.png"></p><p>SACK可以还用来标识ACK报文丢失的情况：</p><ul><li>「接收方」发给「发送方」的两个 ACK 确认应答都丢失了，所以发送方超时后，重传第一个数据包（3000 ~ 3499）</li><li>于是「接收方」发现数据是重复收到的，于是回了一个 SACK &#x3D; 3000<del>3500，告诉「发送方」 3000</del>3500 的数据早已被接收了，因为 ACK 都到了 4000 了，已经意味着 4000 之前的所有数据都已收到，所以这个 SACK 就代表着 D-SACK。</li><li>这样「发送方」就知道了，数据没有丢，是「接收方」的 ACK 确认报文丢了。</li></ul><p><img data-src="/transblog/images/154375917255284.png"></p><p>还可以用来标识发送方的数据包被网络延迟了:</p><ul><li>数据包（1000~1499） 被网络延迟了，导致「发送方」没有收到 Ack 1500 的确认报文。</li><li>而后面报文到达的三个相同的 ACK 确认报文，就触发了快速重传机制，但是在重传后，被延迟的数据包（1000~1499）又到了「接收方」；</li><li>所以「接收方」回了一个 SACK&#x3D;1000~1500，因为 ACK 已经到了 3000，所以这个 SACK 是 D-SACK，表示收到了重复的包。</li><li>这样发送方就知道快速重传触发的原因不是发出去的包丢了，也不是因为回应的 ACK 包丢了，而是因为网络延迟了。</li></ul><p><img data-src="/transblog/images/511880018252051.png"></p><p>D-SACK 有这么几个好处：</p><ol><li>可以让「发送方」知道，是发出去的包丢了，还是接收方回应的 ACK 包丢了;</li><li>可以知道是不是「发送方」的数据包被网络延迟了;</li><li>可以知道网络中是不是把「发送方」的数据包给复制了;</li></ol><p>在 Linux 下可以通过 net.ipv4.tcp_dsack 参数开启&#x2F;关闭这个功能（Linux 2.4 后默认打开）。</p><h4 id="校验和"><a href="#校验和" class="headerlink" title="校验和"></a>校验和</h4><p>发送方：在发送数据之前计算检验和，并进行校验和的填充。<br>接收方：收到数据后，对数据以同样的方式进行计算，求出校验和，与发送方的进行比对。<br>注意：如果接收方比对校验和与发送方不一致，那么数据一定传输有误。但是如果接收方比对校验和与发送方一致，数据不一定传输成功。</p><p><img data-src="/transblog/images/1864845172004.png"></p><h4 id="停止等待"><a href="#停止等待" class="headerlink" title="停止等待"></a>停止等待</h4><p>停止等待”就是每发送完一个分组就停止发送，等待对方的确认。在收到确认后再发送下一个分组<br>序列号的作用不仅仅是应答的作用，有了序列号能够将接收到的数据根据序列号排序，并且去掉重复序列号的数据。这也是TCP传输可靠性的保证之一。</p><p><img data-src="/transblog/images/2651946189884.png"></p><h4 id="确认迟到"><a href="#确认迟到" class="headerlink" title="确认迟到"></a>确认迟到</h4><p><img data-src="/transblog/images/5192010194990.png"></p><ul><li>A收到重复的确认后，直接丢弃。</li><li>B收到重复的M1后，也直接丢弃重复的M1</li></ul><h4 id="流量控制"><a href="#流量控制" class="headerlink" title="流量控制"></a>流量控制</h4><p>窗口大小就是指无需等待确认应答，而可以继续发送数据的最大值。窗口的实现实际上是操作系统开辟的一个缓存空间，发送方主机在等到确认应答返回之前，必须在缓冲区中保留已发送的数据。如果按期收到确认应答，此时数据就可以从缓存区清除。</p><p>这个字段是接收端告诉发送端自己还有多少缓冲区可以接收数据。于是发送端就可以根据这个接收端的处理能力来发送数据，而不会导致接收端处理不过来。<strong>通常窗口的大小是由接收方的窗口大小来决定的</strong>。</p><h5 id="累计确认"><a href="#累计确认" class="headerlink" title="累计确认"></a>累计确认</h5><p>假设窗口大小为 3 个 TCP 段，那么发送方就可以「连续发送」 3 个 TCP 段，并且中途若有 ACK 丢失，可以通过「下一个确认应答进行确认」。如下图：</p><p><img data-src="/transblog/images/301740718242382.png"></p><h5 id="滑动窗口协议"><a href="#滑动窗口协议" class="headerlink" title="滑动窗口协议"></a>滑动窗口协议</h5><p><strong>发送方：#2 和 #3就是发送方的滑动窗口</strong><br>“#1” 是已发送并收到 ACK确认的数据<br>“#2” 是已发送但未收到 ACK确认的数据<br>“#3” 是未发送但总大小在接收方处理范围内（接收方还有空间）<br>“#4” 是未发送但总大小超过接收方处理范围（接收方没有空间）<br><strong>滑动窗口在被连续确认后才进行滑动</strong>，当收到之前发送的数据 32<del>36 字节的 ACK 确认应答后，如果发送窗口的大小没有变化，则滑动窗口往右边移动5个字节，接下来 52</del>56 字节又变成了可用窗口，那么后续也就可以发送这5个字节的数据了。</p><p><img data-src="/transblog/images/450811110266181.png"></p><ul><li>SND.WND：表示发送窗口的大小（大小是由接收方指定的）；</li><li>SND.UNA：是一个绝对指针，它指向的是已发送但未收到确认的第一个字节的序列号，也就是 #2 的第一个字节。</li><li>SND.NXT：也是一个绝对指针，它指向未发送但可发送范围的第一个字节的序列号，也就是 #3 的第一个字节。<br>那么可用窗口大小的计算就可以是：<strong>可用窗口大小 &#x3D; SND.WND -（SND.NXT - SND.UNA）</strong></li></ul><p><strong>接收方：#2就是接收窗口</strong><br>#1 + #2 是已成功接收并确认的数据（等待应用进程读取）；<br>#3 是未收到数据但可以接收的数据；<br>#4 未收到数据并不可以接收的数据；</p><p><img data-src="/transblog/images/576241410266813.png"></p><ul><li>RCV.WND：表示接收窗口的大小，它会通告给发送方。</li><li>RCV.NXT：是一个指针，它指向期望从发送方发送来的下一个数据字节的序列号，也就是 #3 的第一个字节。</li></ul><p><img data-src="/transblog/images/341123510257549.png"></p><p><strong>思考丢包的情形？</strong><br><strong>为了防止丢包的情况产生，TCP 规定是不允许同时减少缓存又收缩窗口的，而是采用先收缩窗口，过段时间再减少缓存，这样就可以避免了丢包情况。</strong></p><h6 id="TCP的”死锁”"><a href="#TCP的”死锁”" class="headerlink" title="TCP的”死锁”"></a>TCP的”死锁”</h6><p>当发生窗口关闭时，接收方处理完数据后，会向发送方通告一个窗口非 0 的 ACK 报文，如果这个通告窗口的 ACK 报文在网络中丢失了，这会导致发送方一直等待接收方的非 0 窗口通知，接收方也一直等待发送方的数据，如不采取措施，这种相互等待的过程，会造成了死锁的现象。</p><p>只要 TCP 连接一方收到对方的零窗口通知，就启动持续计时器。<strong>如果持续计时器超时，就会发送窗口探测报文</strong>，而对方在确认这个探测报文时，给出自己现在的接收窗口大小。</p><h5 id="糊涂窗口综合症"><a href="#糊涂窗口综合症" class="headerlink" title="糊涂窗口综合症"></a>糊涂窗口综合症</h5><p>如果发送端为产生数据很慢的应用程序服务，例如一次产生一个字节。这个应用程序一次将一个字节的数据写入发送端的TCP的缓存。如果发送端的TCP没有特定的指令，它就产生只包括一个字节数据的报文段。结果有很多41字节的IP数据报就在互连网中传来传去。(大车拉少人)</p><p>就要同时解决两个问题就可以了：</p><ol><li>让接收方不通告小窗口给发送方</li><li>让发送方避免发送小数据</li></ol><p>接收方策略如下:<br>当「窗口大小」小于 MSS 与 1&#x2F;2 缓存大小中的最小值时，就会向发送方通告窗口为 0，也就阻止了发送方再发数据过来。</p><p>发送方策略如下:<br>防止发送端的TCP逐个字节地发送数据。必须强迫发送端的TCP收集数据，然后用一个更大的数据块来发送。发送端的TCP要等待多长时间呢？如果它等待过长，它就会使整个的过程产生较长的时延。如果它的等待时间不够长，它就可能发送较小的报文段。</p><p>使用 Nagle 算法，该算法的思路是延时处理，只有满足下面两个条件中的任意一个条件，才可以发送数据：</p><p>条件一：要等到窗口大小 &gt;&#x3D; MSS 并且 数据大小 &gt;&#x3D; MSS；<br>条件二：收到之前发送数据的 ack 回包；</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> 有数据要发送 &#123;</span><br><span class="line">    <span class="keyword">if</span> 可用窗口大小 &gt;= MSS <span class="keyword">and</span> 可发送的数据 &gt;= MSS &#123;</span><br><span class="line">    立刻发送MSS大小的数据</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> 有未确认的数据 &#123;</span><br><span class="line">            将数据放入缓存等待接收ACK</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            立刻发送数据</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意，如果接收方不能满足「不通告小窗口给发送方」，那么即使开了 Nagle 算法，也无法避免糊涂窗口综合症，因为如果对端 ACK 回复很快的话（达到 Nagle 算法的条件二），Nagle 算法就不会拼接太多的数据包，这种情况下依然会有小数据包的传输，网络总体的利用率依然很低。所以，<strong>接收方得满足「不通告小窗口给发送方」+ 发送方开启 Nagle 算法，才能避免糊涂窗口综合症</strong>。</p><h4 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h4><h5 id="慢开始"><a href="#慢开始" class="headerlink" title="慢开始"></a>慢开始</h5><p>拥塞窗口cwnd，发送窗口swnd<br>慢开始算法的思路就是，不要一开始就发送大量的数据，先探测一下网络的拥塞程度，也就是说由小到大逐渐增加拥塞窗口的大小。拥塞窗口的增长是指数级别的。慢启动的机制只是说明在开始的时候发送的少，发送的慢，但是增长的速度是非常快的。</p><p><img data-src="/transblog/images/520560911250408.png"></p><p>慢启动门限ssthresh状态变量。<br>当 cwnd &lt; ssthresh 时，使用慢启动算法。<br>当 cwnd &gt;&#x3D; ssthresh 时，就会使用「拥塞避免算法」。</p><h5 id="拥塞避免"><a href="#拥塞避免" class="headerlink" title="拥塞避免"></a>拥塞避免</h5><p>为了控制拥塞窗口的增长，不能使拥塞窗口单纯的加倍，设置一个<strong>拥塞窗口的阈值</strong>，当拥塞窗口大小超过阈值时，不能再按照指数来增长，而是线性的增长。<br>在慢启动开始的时候，慢启动的阈值等于窗口的最大值，一旦造成网络拥塞，发生超时重传时，慢启动的阈值会为原来的一半（这里的原来指的是发生网络拥塞时拥塞窗口的大小），同时拥塞窗口重置为 1</p><p><img data-src="/transblog/images/578581711264325.png"></p><h5 id="快恢复"><a href="#快恢复" class="headerlink" title="快恢复"></a>快恢复</h5><p>如果网络出现拥塞的话就不会收到三个重复的ACK确认（触发快速重传），所以发送方现在认为网络可能没有出现拥塞。</p><p>所以此时不执行慢开始算法，而是将cwnd设置为ssthresh减半后的值，然后执行拥塞避免算法（这种行为叫做快恢复），使cwnd缓慢增大。</p><p><img data-src="/transblog/images/208812611270847.png"></p><h3 id="TCP的缺陷"><a href="#TCP的缺陷" class="headerlink" title="TCP的缺陷"></a>TCP的缺陷</h3><ol><li><p>升级困难<br>存在与操作系统内核的协议栈，升级新的TCP需要升级内核，很麻烦</p></li><li><p>建立连接的延迟<br>现在大多数网站都是使用 HTTPS 的，这意味着在 TCP 三次握手之后，还需要经过 TLS 四次握手后，才能进行 HTTP 数据的传输，这在一定程序上增加了数据传输的延迟。</p></li><li><p>存在队头阻塞问题<br>TCP 是字节流协议，TCP 层必须保证收到的字节数据是完整且有序的，如果序列号较低的 TCP 段在网络传输中丢失了，即使序列号较高的 TCP 段已经被接收了，应用层也无法从内核中读取到这部分数据。</p></li><li><p>网络迁移需要重新建立 TCP 连接<br>当移动设备的网络从 4G 切换到 WIFI 时，意味着 IP 地址变化了，那么就必须要断开连接，然后重新建立 TCP 连接</p></li></ol><h4 id="TCP一定可靠吗？"><a href="#TCP一定可靠吗？" class="headerlink" title="TCP一定可靠吗？"></a>TCP一定可靠吗？</h4><h5 id="数据包的发送流程"><a href="#数据包的发送流程" class="headerlink" title="数据包的发送流程"></a>数据包的发送流程</h5><ol><li><p>为了发送数据包，两端首先会通过三次握手，建立TCP连接。</p></li><li><p>一个数据包，从聊天框里发出，消息会从聊天软件所在的<strong>用户空间拷贝到内核空间的发送缓冲区</strong>（send buffer），数据包就这样顺着<strong>传输层、网络层，进入到数据链路层，在这里数据包会经过流控（qdisc），再通过RingBuffer发到物理层的网卡</strong>。数据就这样顺着网卡发到了纷繁复杂的网络世界里。这里头数据会经过n多个路由器和交换机之间的跳转，<strong>最后到达目的机器的网卡处</strong>。</p></li><li><p>此时目的机器的网卡会<strong>通知DMA将数据包信息放到RingBuffer中</strong>，再触发一个硬中断给CPU，<strong>CPU触发软中断让ksoftirqd去RingBuffer收包</strong>，于是一个数据包就这样顺着物理层，数据链路层，网络层，传输层，<strong>最后从内核空间拷贝到用户空间里的聊天软件里</strong>。</p></li></ol><h5 id="何时丢包"><a href="#何时丢包" class="headerlink" title="何时丢包"></a>何时丢包</h5><h6 id="建立连接时丢包"><a href="#建立连接时丢包" class="headerlink" title="建立连接时丢包"></a>建立连接时丢包</h6><p>半连接队列和全连接队列已满，那新来的包就会被丢弃。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">全连接队列溢出次数</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">netstat -s | grep overflowed</span></span><br><span class="line">    4343 times the listen queue of a socket overflowed</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">半连接队列溢出次数</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">netstat -s | grep -i <span class="string">&quot;SYNs to LISTEN sockets dropped&quot;</span></span></span><br><span class="line">    109 times the listen queue of a socket overflowed </span><br></pre></td></tr></table></figure><h6 id="流量控制丢包"><a href="#流量控制丢包" class="headerlink" title="流量控制丢包"></a>流量控制丢包</h6><p>如果所有数据不加控制一股脑冲入到网卡，网卡会吃不消，让数据按一定的规则排个队依次处理，也就是所谓的qdisc(Queueing Disciplines，排队规则)，这也是我们常说的流量控制机制。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">ifconfig eth0</span></span><br><span class="line">eth0: flags=4163&lt;UP,BROADCAST,RUNNING,MULTICAST&gt;  mtu 1500</span><br><span class="line">        inet 172.21.66.69  netmask 255.255.240.0  broadcast 172.21.79.255</span><br><span class="line">        inet6 fe80::216:3eff:fe25:269f  prefixlen 64  scopeid 0x20&lt;link&gt;</span><br><span class="line">        ether 00:16:3e:25:26:9f  txqueuelen 1000  (Ethernet)</span><br><span class="line">        RX packets 6962682  bytes 1119047079 (1.0 GiB)</span><br><span class="line">        RX errors 0  dropped 0  overruns 0  frame 0</span><br><span class="line">        TX packets 9688919  bytes 2072511384 (1.9 GiB)</span><br><span class="line">        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0</span><br></pre></td></tr></table></figure><p>txqueuelen后面的数字1000，其实就是流控队列的长度。当发送数据过快，流控队列长度txqueuelen又不够大时，就容易出现丢包现象。查看TX下的dropped字段，当它大于0时，则有可能是发生了流控丢包。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">ifconfig eth0 txqueuelen 1500 //流控队列长度从1000提升为1500.</span></span><br></pre></td></tr></table></figure><h6 id="网卡丢包"><a href="#网卡丢包" class="headerlink" title="网卡丢包"></a>网卡丢包</h6><ol><li><p>网线质量差，接触不良等</p></li><li><p>RingBuffer过小导致丢包<br>上面提到，在接收数据时，会将数据暂存到RingBuffer接收缓冲区中，然后等着内核触发软中断慢慢收走。如果这个缓冲区过小，而这时候发送的数据又过快，就有可能发生溢出，此时也会产生丢包。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">ifconfig</span></span><br><span class="line">eth0:  RX errors 0  dropped 0  overruns 0  frame 0</span><br><span class="line">//查看上面的overruns指标，它记录了由于RingBuffer长度不足导致的溢出次数。</span><br></pre></td></tr></table></figure></li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">ethtool -g eth0</span></span><br><span class="line">Ring parameters for eth0:</span><br><span class="line">Pre-set maximums:</span><br><span class="line">RX:        4096</span><br><span class="line">RX Mini:    0</span><br><span class="line">RX Jumbo:    0</span><br><span class="line">TX:        4096</span><br><span class="line">Current hardware settings:</span><br><span class="line">RX:        1024</span><br><span class="line">RX Mini:    0</span><br><span class="line">RX Jumbo:    0</span><br><span class="line">TX:        1024</span><br><span class="line"></span><br><span class="line">//RingBuffer最大支持4096的长度，但现在实际只用了1024。想要修改这个长度可以执行ethtool -G eth1 rx 4096 tx 4096将发送和接收RingBuffer的长度都改为4096。</span><br></pre></td></tr></table></figure><ol start="3"><li>网卡性能不足</li></ol><h6 id="接收缓冲区丢包"><a href="#接收缓冲区丢包" class="headerlink" title="接收缓冲区丢包"></a>接收缓冲区丢包</h6><p>使用TCP socket进行网络编程的时候，内核都会分配一个发送缓冲区和一个接收缓冲区。不管是接收缓冲区还是发送缓冲区，都能看到三个数值，分别对应缓冲区的最小值，默认值和最大值 （min、default、max）。缓冲区会在min和max之间动态调整。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看接收缓冲区</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">sysctl net.ipv4.tcp_rmem</span></span><br><span class="line">net.ipv4.tcp_rmem = 4096    87380   6291456</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看发送缓冲区</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">sysctl net.ipv4.tcp_wmem</span></span><br><span class="line">net.ipv4.tcp_wmem = 4096    16384   4194304</span><br><span class="line"></span><br></pre></td></tr></table></figure><ul><li>当发送缓冲区满了，如果是阻塞调用，那就会等，等到缓冲区有空位可以发数据。如果是非阻塞调用，就会立刻返回一个 EAGAIN 错误信息，意思是 Try again。让应用程序下次再重试。这种情况下一般不会发生丢包。</li><li>当接受缓冲区满了，它的TCP接收窗口会变为0，也就是所谓的零窗口，并且会通过数据包里的win&#x3D;0，告诉发送端。一般这种情况下，发送端就该停止发消息了，但如果这时候确实还有数据发来，就会发生丢包。</li></ul><h6 id="两端之间的网络丢包"><a href="#两端之间的网络丢包" class="headerlink" title="两端之间的网络丢包"></a>两端之间的网络丢包</h6><p>路由器和交换机还有光缆啥的</p><h1 id="灵活应用—-HTTP和HTTPS"><a href="#灵活应用—-HTTP和HTTPS" class="headerlink" title="灵活应用—-HTTP和HTTPS"></a>灵活应用—-HTTP和HTTPS</h1><p>到目前为止，HTTP 常见到版本有 HTTP&#x2F;1.1，HTTP&#x2F;2.0，HTTP&#x2F;3.0，不同版本的 HTTP 特性是不一样的。</p><p>HTTP 是超⽂本传输协议，也就是HyperText Transfer Protocol。HTTP 是⼀个在计算机世界⾥专⻔在「两点」之间「传输」⽂字、图⽚、⾳频、视频等「超⽂本」数据的「约定和规范」。</p><h2 id="HTTP请求报文"><a href="#HTTP请求报文" class="headerlink" title="HTTP请求报文"></a>HTTP请求报文</h2><p><strong>HTTP 协议通过设置回车符、换行符作为 HTTP header 的边界，通过 Content-Length 字段作为 HTTP body 的边界，这两个方式都是为了解决“粘包”的问题。</strong></p><p><img data-src="/transblog/images/562873316267020.png"></p><p>Get请求例子，使用Charles抓取的request：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">GET /562f25980001b1b106000338.jpg HTTP/1.1</span><br><span class="line">Host    img.mukewang.com</span><br><span class="line">User-Agent    Mozilla/5.0 (Windows NT 10.0; WOW64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/51.0.2704.106 Safari/537.36</span><br><span class="line">Accept    image/webp,image/*,*/*;q=0.8</span><br><span class="line">Referer    http://www.imooc.com/</span><br><span class="line">Accept-Encoding    gzip, deflate, sdch</span><br><span class="line">Accept-Language    zh-CN,zh;q=0.8</span><br></pre></td></tr></table></figure><h3 id="请求行"><a href="#请求行" class="headerlink" title="请求行"></a>请求行</h3><p>用来说明请求方法，要访问的资源以及所使用的HTTP版本。GET说明请求方法为GET，jpg为要访问的资源，该行的最后一部分说明使用的是HTTP1.1版本。</p><h4 id="请求方法"><a href="#请求方法" class="headerlink" title="请求方法"></a>请求方法</h4><h5 id="1-GET"><a href="#1-GET" class="headerlink" title="1. GET"></a>1. GET</h5><p>传递参数长度受限制，因为传递的参数是直接表示在地址栏中，而特定浏览器和服务器对url的长度是有限制的。因此，GET不适合用来传递私密数据，也不适合拿来传递大量数据。一般的HTTP请求大多都是GET。</p><h5 id="2-POST"><a href="#2-POST" class="headerlink" title="2. POST"></a>2. POST</h5><p>POST把传递的数据封装在HTTP请求数据中，以名称&#x2F;值的形式出现，可以传输大量数据，对数据量没有限制，也不会显示在URL中。表单的提交用的是POST。</p><h5 id="GET和POST的区别："><a href="#GET和POST的区别：" class="headerlink" title="GET和POST的区别："></a>GET和POST的区别：</h5><ul><li><p>GET 的语义是从服务器获取指定的资源⽅法，GET 请求的参数位置一般是写在 URL 中，URL 规定只能支持 ASCII，所以 GET 请求的参数只允许 ASCII 字符 ，而且浏览器会对 URL 的长度有限制（HTTP协议本身对 URL长度并没有做任何规定）。</p></li><li><p>GET是安全且幂等的，因为它是「只读」操作，⽆论操作多少次，服务器上的数据都是安全的，且每次的结果都是相同的。<strong>可以对 GET 请求的数据做缓存，这个缓存可以做到浏览器本身上（彻底避免浏览器发请求），也可以做到代理上（如nginx），而且在浏览器中 GET 请求可以保存为书签。</strong></p></li><li><p>POST 的语义是根据请求负荷（报文body）对指定的资源做出处理，POST 请求携带数据的位置一般是写在报文 body 中，body 中的数据可以是任意格式的数据，只要客户端与服务端协商好即可，而且浏览器不会对 body 大小做限制。</p></li><li><p>POST 因为是「新增或提交数据」的操作，会修改服务器上的资源，所以是不安全的，且多次提交数据就会创建多个资源，所以不是幂等的。<strong>所以，浏览器一般不会缓存 POST 请求，也不能把 POST 请求保存为书签。</strong></p></li></ul><p><strong>3. HEAD</strong><br>HEAD跟GET相似，不过服务端接收到HEAD请求时只返回响应头，不发送响应内容。所以，如果只需要查看某个页面的状态时，用HEAD更高效，因为省去了传输页面内容的时间。<br><strong>4. DELETE</strong><br>删除某一个资源。<br><strong>5. OPTIONS</strong><br>用于获取当前URL所支持的方法。若请求成功，会在HTTP头中包含一个名为“Allow”的头，值是所支持的方法，如“GET, POST”。<br><strong>6. PUT</strong><br>把一个资源存放在指定的位置上。本质上来讲， PUT和POST极为相似，都是向服务器发送数据，但它们之间有一个重要区别，PUT通常指定了资源的存放位置，而POST则没有，POST的数据存放位置由服务器自己决定。<br><strong>7. TRACE</strong><br>回显服务器收到的请求，主要用于测试或诊断。<br><strong>8. CONNECT</strong><br>CONNECT方法是HTTP&#x2F;1.1协议预留的，能够将连接改为管道方式的代理服务器。通常用于SSL加密服务器的链接与非加密的HTTP代理服务器的通信。</p><h3 id="请求头部"><a href="#请求头部" class="headerlink" title="请求头部"></a>请求头部</h3><p>请求头部由关键字&#x2F;值对组成，每行一对<br>User-Agent : 产生请求的浏览器类型<br>Accept : 客户端希望接受的数据类型，比如 Accept：text&#x2F;xml（application&#x2F;json）表示希望接受到的是xml（json）类型<br>Content-Type：发送端发送的实体数据的数据类型。比如，Content-Type：text&#x2F;html（application&#x2F;json）表示发送的是html类型。<br>Host : 请求的主机名，允许多个域名同处一个IP地址，即虚拟主机<br>Referer：表示当前请求是从哪个资源发起的；或者是请求的上一步的地址。<br>Referer是常用于网站的访问统计，比如我在很多地方都做了广告链接到我网站的主页，这时候我就可以通过Referer来查看哪些地方跳转过来的人多，就说广告的效果好。</p><h3 id="空行"><a href="#空行" class="headerlink" title="空行"></a>空行</h3><p>请求头部后面的空行是必须的，即使第四部分的请求数据为空，也必须有空行。</p><h3 id="请求数据"><a href="#请求数据" class="headerlink" title="请求数据"></a>请求数据</h3><p>请求数据也叫主体，可以添加任意的其他数据。</p><h2 id="HTTP响应报文"><a href="#HTTP响应报文" class="headerlink" title="HTTP响应报文"></a>HTTP响应报文</h2><p><img data-src="/transblog/images/288873416259689.png"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">HTTP/1.1 200 OK</span><br><span class="line">Date: Fri, 22 May 2009 06:07:21 GMT</span><br><span class="line">Content-Type: text/html; charset=UTF-8</span><br><span class="line"></span><br><span class="line">&lt;html&gt;</span><br><span class="line">      &lt;head&gt;&lt;/head&gt;</span><br><span class="line">      &lt;body&gt;</span><br><span class="line">            &lt;!--body goes here--&gt;</span><br><span class="line">      &lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h3 id="状态行"><a href="#状态行" class="headerlink" title="状态行"></a>状态行</h3><p>由HTTP协议版本号， 状态码， 状态消息 三部分组成。（HTTP&#x2F;1.1）表明HTTP版本为1.1版本，状态码为200，状态消息为（ok）</p><h4 id="状态码"><a href="#状态码" class="headerlink" title="状态码"></a>状态码</h4><p><img data-src="/transblog/images/3665347114429.png"></p><h5 id="1xx-类状态码"><a href="#1xx-类状态码" class="headerlink" title="1xx 类状态码"></a>1xx 类状态码</h5><p>属于提示信息，是协议处理中的一种中间状态，实际用到的比较少。</p><h5 id="2xx-类状态码"><a href="#2xx-类状态码" class="headerlink" title="2xx 类状态码"></a>2xx 类状态码</h5><p>表示服务器成功处理了客户端的请求，也是我们最愿意看到的状态。</p><ul><li>「200 OK」是最常见的成功状态码，表示一切正常。如果是非 HEAD 请求，服务器返回的响应头都会有 body 数据。</li><li>「204 No Content」也是常见的成功状态码，与 200 OK 基本相同，但响应头没有 body 数据。</li><li>「206 Partial Content」是应用于 HTTP 分块下载或断点续传，表示响应返回的 body 数据并不是资源的全部，而是其中的一部分，也是服务器处理成功的状态。</li></ul><h5 id="3xx-类状态码"><a href="#3xx-类状态码" class="headerlink" title="3xx 类状态码"></a>3xx 类状态码</h5><p>表示客户端请求的资源发生了变动，需要客户端用新的 URL 重新发送请求获取资源，也就是重定向。</p><ul><li><p>「301 Moved Permanently」表示永久重定向，说明请求的资源已经不存在了，需改用新的 URL 再次访问。</p></li><li><p>「302 Found」表示临时重定向，说明请求的资源还在，但暂时需要用另一个 URL 来访问。</p><blockquote><p>301 和 302 都会在响应头里使用字段 Location，指明后续要跳转的 URL，浏览器会自动重定向新的 URL。</p></blockquote></li><li><p>「304 Not Modified」不具有跳转的含义，表示资源未修改，重定向已存在的缓冲文件，也称缓存重定向，也就是告诉客户端可以继续使用缓存资源，用于缓存控制。</p></li></ul><h5 id="4xx-类状态码"><a href="#4xx-类状态码" class="headerlink" title="4xx 类状态码"></a>4xx 类状态码</h5><p>表示客户端发送的报文有误，服务器无法处理，也就是错误码的含义。</p><ul><li>「400 Bad Request」表示客户端请求的报文有错误，但只是个笼统的错误。</li><li>「403 Forbidden」表示服务器禁止访问资源，并不是客户端的请求出错。</li><li>「404 Not Found」表示请求的资源在服务器上不存在或未找到，所以无法提供给客户端。</li></ul><h5 id="5xx-类状态码"><a href="#5xx-类状态码" class="headerlink" title="5xx 类状态码"></a>5xx 类状态码</h5><p>表示客户端请求报文正确，但是服务器处理时内部发生了错误，属于服务器端的错误码。</p><ul><li>「500 Internal Server Error」与 400 类型，是个笼统通用的错误码，服务器发生了什么错误，我们并不知道。</li><li>「501 Not Implemented」表示客户端请求的功能还不支持，类似“即将开业，敬请期待”的意思。</li><li>「502 Bad Gateway」通常是服务器作为网关或代理时返回的错误码，表示服务器自身工作正常，访问后端服务器发生了错误。</li><li>「503 Service Unavailable」表示服务器当前很忙，暂时无法响应客户端，类似“网络服务正忙，请稍后重试”的意思。</li></ul><h3 id="响应头"><a href="#响应头" class="headerlink" title="响应头"></a>响应头</h3><p>用来说明客户端要使用的一些附加信息<br>Date:生成响应的日期和时间；Content-Type:指定了MIME类型的HTML(text&#x2F;html),编码类型是UTF-8</p><h3 id="空行-1"><a href="#空行-1" class="headerlink" title="空行"></a>空行</h3><p>第三部分：空行，消息报头后面的空行是必须的</p><h3 id="响应体"><a href="#响应体" class="headerlink" title="响应体"></a>响应体</h3><p>服务器返回给客户端的文本信息。空行后面的html部分为响应正文。</p><h2 id="HTTP缓存技术"><a href="#HTTP缓存技术" class="headerlink" title="HTTP缓存技术"></a>HTTP缓存技术</h2><h3 id="强制缓存"><a href="#强制缓存" class="headerlink" title="强制缓存"></a>强制缓存</h3><p>强缓存指的是只要浏览器判断缓存没有过期，则直接使用浏览器的本地缓存，决定是否使用缓存的主动性在于浏览器这边。</p><p><img data-src="/transblog/images/404601918267482.png"></p><ul><li>Cache-Control， 是一个相对时间；</li><li>Expires，是一个绝对时间；</li></ul><p>如果 HTTP 响应头部同时有 Cache-Control 和 Expires 字段的话，<strong>Cache-Control 的优先级高于 Expires</strong> 。Cache-control 选项更多一些，设置更加精细，所以建议使用 Cache-Control 来实现强缓存。具体的实现流程如下：</p><ul><li>当浏览器第一次请求访问服务器资源时，服务器会在返回这个资源的同时，在 Response 头部加上 Cache-Control，Cache-Control 中设置了过期时间大小；</li><li>浏览器再次请求访问服务器中的该资源时，会先<strong>通过请求资源的时间与 Cache-Control 中设置的过期时间大小，来计算出该资源是否过期，如果没有，则使用该缓存，否则重新请求服务器；</strong></li><li>服务器再次收到请求后，会再次更新 Response 头部的 Cache-Control。</li></ul><h3 id="协商缓存"><a href="#协商缓存" class="headerlink" title="协商缓存"></a>协商缓存</h3><p>某些请求的响应码是 304，这个是告诉浏览器可以使用本地缓存的资源，通常这种通过服务端告知客户端是否可以使用缓存的方式被称为协商缓存。</p><p><img data-src="/transblog/images/198132218264984.png"></p><p><strong>协商缓存就是与服务端协商之后，通过协商结果来判断是否使用本地缓存</strong>，协商缓存可以基于两种头部来实现。</p><p>第一种：</p><ul><li>响应头部中的 Last-Modified：标示这个响应资源的最后修改时间；</li><li>请求头部中的 If-Modified-Since：当资源过期了，发现响应头中具有 Last-Modified 声明，则再次发起请求的时候带上 Last-Modified 的时间，服务器收到请求后发现有 If-Modified-Since 则与被请求资源的最后修改时间进行对比（Last-Modified），如果最后修改时间较新（大），说明资源又被改过，则返回最新资源，HTTP 200 OK；如果最后修改时间较旧（小），说明资源无新修改，响应 HTTP 304 走缓存。</li></ul><p>第二种：</p><ul><li>响应头部中 Etag：唯一标识响应资源；</li><li>请求头部中的 If-None-Match：当资源过期时，浏览器发现响应头里有 Etag，则再次向服务器发起请求时，会将请求头 If-None-Match 值设置为 Etag 的值。服务器收到请求后进行比对，如果资源没有变化返回 304，如果资源变化了返回 200。</li></ul><p>第一种实现方式是基于时间实现的，第二种实现方式是基于一个唯一标识实现的，相对来说后者可以更加准确地判断文件内容是否被修改，避免由于时间篡改导致的不可靠问题。</p><h2 id="HTTP-1-1"><a href="#HTTP-1-1" class="headerlink" title="HTTP&#x2F;1.1"></a>HTTP&#x2F;1.1</h2><h3 id="HTTP-1-1优点"><a href="#HTTP-1-1优点" class="headerlink" title="HTTP&#x2F;1.1优点"></a>HTTP&#x2F;1.1优点</h3><ol><li>简单<br>HTTP 基本的报⽂格式就是 header + body ，头部信息也是 key-value 简单⽂本的形式，易于理解</li><li>灵活和易于扩展<br>HTTP协议⾥的各类请求⽅法、URI&#x2F;URL、状态码、头字段等每个组成要求都没有被固定死，都允许开发⼈员⾃定义和扩充。同时 HTTP 由于是⼯作在应⽤层（ OSI 第七层），则它下层可以随意变化。</li><li>应⽤⼴泛和跨平台</li></ol><h3 id="HTTP-1-1缺点"><a href="#HTTP-1-1缺点" class="headerlink" title="HTTP&#x2F;1.1缺点"></a>HTTP&#x2F;1.1缺点</h3><ol><li>⽆状态</li></ol><ul><li>好处：因为服务器不会去记忆 HTTP 的状态，所以不需要额外的资源来记录状态信息，这能减轻服务器的负担，能够把更多的 CPU 和内存⽤来对外提供服务。</li><li>坏处：既然服务器没有记忆能⼒，它在完成有关联性的操作时会⾮常麻烦。</li></ul><ol start="2"><li>不安全</li></ol><ul><li>通信使用明文（不加密），内容可能会被窃听</li><li>不验证通信方的身份，因此有可能遭遇伪装。</li><li>无法证明报文的完整性，所以有可能已遭篡改。</li></ul><h3 id="HTTP-1-1特点"><a href="#HTTP-1-1特点" class="headerlink" title="HTTP&#x2F;1.1特点"></a>HTTP&#x2F;1.1特点</h3><ol><li>⻓连接</li></ol><ul><li>早期 HTTP&#x2F;1.0 性能上的⼀个很⼤的问题，那就是每发起⼀个请求，都要新建⼀次 TCP 连接（三次握⼿），⽽且是串⾏请求，做了⽆谓的 TCP 连接建⽴和断开，增加了通信开销。</li><li>为了解决上述 TCP 连接问题，HTTP&#x2F;1.1 提出了⻓连接的通信⽅式，也叫持久连接。这种⽅式的好处在于减少了TCP 连接的复建⽴和断开所造成的额外开销，减轻了服务器端的负载。<strong>持久连接的特点是，只要任意⼀端没有明确提出断开连接，则保持 TCP 连接状态。</strong></li></ul><ol start="2"><li>管道⽹络传输</li></ol><ul><li>HTTP&#x2F;1.1 采⽤了⻓连接的⽅式，这使得管道⽹络传输成为了可能。即可在同⼀个 TCP 连接⾥⾯，客户端可以发起多个请求，只要第⼀个请求发出去了，不必等其回来，就可以发第⼆个请求出去，<strong>可以减少整体的响应时间。</strong></li><li>管道机制则是允许浏览器同时发出 A 请求和 B 请求。但是服务器必须按照接收请求的顺序发送对这些管道化请求的响应。</li></ul><ol start="3"><li>队头阻塞</li></ol><ul><li>但是服务器还是按照顺序，先回应 A 请求，完成后再回应 B 请求。要是前⾯的回应特别慢，后⾯就会有许多请求排队等着。这称为「队头堵塞」。</li><li>「请求 - 应答」的模式加剧了 HTTP 的性能问题。因为当顺序发送的请求序列中的⼀个请求因为某种原因被阻塞时，在后⾯排队的所有请求也⼀同被阻塞了，会招致客户端⼀直请求不到数据，这也就是「队头阻塞」。<strong>HTTP&#x2F;1.1 管道解决了请求的队头阻塞，但是没有解决响应的队头阻塞</strong></li></ul><h3 id="HTTP-1-1-相⽐-HTTP-1-0-性能上的改进："><a href="#HTTP-1-1-相⽐-HTTP-1-0-性能上的改进：" class="headerlink" title="HTTP&#x2F;1.1 相⽐ HTTP&#x2F;1.0 性能上的改进："></a>HTTP&#x2F;1.1 相⽐ HTTP&#x2F;1.0 性能上的改进：</h3><ol><li>使⽤ TCP ⻓连接的⽅式改善了 HTTP&#x2F;1.0 短连接造成的性能开销。</li><li>⽀持管道⽹络传输，只要第⼀个请求发出去了，不必等其回来，就可以发第⼆个请求出去，可以减少整体的响应时间。</li></ol><h3 id="HTTP-1-1性能瓶颈"><a href="#HTTP-1-1性能瓶颈" class="headerlink" title="HTTP&#x2F;1.1性能瓶颈"></a>HTTP&#x2F;1.1性能瓶颈</h3><ol><li>请求 &#x2F; 响应头部（Header）未经压缩就发送，⾸部信息越多延迟越⼤。只能压缩 Body 的部分；</li><li>发送冗⻓的⾸部。每次互相发送相同的⾸部造成的浪费较多；</li><li>服务器是按请求的顺序响应的，如果服务器响应慢，会招致客户端⼀直请求不到数据，也就是队头阻塞；</li><li>没有请求优先级控制；</li><li>请求只能从客户端开始，服务器只能被动响应。</li></ol><h2 id="HTTP-2"><a href="#HTTP-2" class="headerlink" title="HTTP&#x2F;2"></a>HTTP&#x2F;2</h2><p>HTTP&#x2F;2 协议是基于 HTTPS 的，所以 HTTP&#x2F;2 的安全性也是有保障的。</p><p><img data-src="/transblog/images/469225416256244.png"></p><h3 id="HTTP-2做了哪些优化"><a href="#HTTP-2做了哪些优化" class="headerlink" title="HTTP&#x2F;2做了哪些优化"></a>HTTP&#x2F;2做了哪些优化</h3><ol><li>头部压缩<br>HTTP&#x2F;2 会压缩头（Header）如果你同时发出多个请求，他们的头是⼀样的或是相似的，那么，<strong>协议会帮你消除重复的部分</strong>。这就是所谓的 HPACK 算法：在客户端和服务器同时维护⼀张头信息表，所有字段都会存⼊这个表，⽣成⼀个索引号，以后就不发送同样字段了，只发送索引号，这样就提⾼速度了。<br>为高频出现在头部的字符串和字段建立了一张静态表：</li></ol><p><img data-src="/transblog/images/10224215259237.png"></p><ol start="2"><li><p>⼆进制格式<br>HTTP&#x2F;2 不再像 HTTP&#x2F;1.1 ⾥的纯⽂本形式的报⽂，⽽是全⾯采⽤了⼆进制格式，头信息和数据体都是⼆进制，并且统称为帧：头信息帧和数据帧。<strong>增加了传输效率</strong></p></li><li><p>并发传输<br>HTTP&#x2F;2引出了 Stream 概念，多个 Stream 复用在一条 TCP 连接。</p></li></ol><p><img data-src="/transblog/images/538245916251998.png"></p><p>针对不同的 HTTP 请求用独一无二的 Stream ID 来区分，接收端可以通过 Stream ID 有序组装成 HTTP 消息，不同 Stream 的帧是可以乱序发送的，因此可以并发不同的 Stream ，也就是 HTTP&#x2F;2 可以并行交错地发送请求和响应。</p><ol start="4"><li>服务器主动推送</li></ol><ul><li>HTTP&#x2F;2 还在⼀定程度上改善了传统的「请求 - 应答」⼯作模式，服务不再是被动地响应，也可以主动向客户端发送消息。举例来说，在浏览器刚请求 HTML 的时候，就提前把可能会⽤到的 JS、CSS ⽂件等静态资源主动发给客户端，减少延时的等待，也就是服务器推送。</li><li>客户端和服务器双方都可以建立 Stream， Stream ID 也是有区别的，客户端建立的 Stream 必须是奇数号，而服务器建立的 Stream 必须是偶数号。</li></ul><h3 id="HTTP-2存在的问题"><a href="#HTTP-2存在的问题" class="headerlink" title="HTTP&#x2F;2存在的问题"></a>HTTP&#x2F;2存在的问题</h3><p>TCP的队头阻塞：HTTP&#x2F;2 是基于 TCP 协议来传输数据的，TCP 是字节流协议，TCP 层必须保证收到的字节数据是完整且连续的，这样内核才会将缓冲区里的数据返回给 HTTP 应用，那么当「前 1 个字节数据」没有到达时，<strong>后收到的字节数据只能存放在内核缓冲区里，只有等到这 1 个字节数据到达时，HTTP&#x2F;2 应用层才能从内核中拿到数据</strong>，这就是 HTTP&#x2F;2 队头阻塞问题。</p><p><img data-src="/transblog/images/466970617269878.png"></p><p>所以，一旦发生了丢包现象，就会触发 TCP 的重传机制，这样在一个 TCP 连接中的所有的 HTTP 请求都必须等待这个丢了的包被重传回来。</p><h2 id="HTTP-3"><a href="#HTTP-3" class="headerlink" title="HTTP&#x2F;3"></a>HTTP&#x2F;3</h2><h3 id="HTTP-3做了哪些优化"><a href="#HTTP-3做了哪些优化" class="headerlink" title="HTTP&#x2F;3做了哪些优化"></a>HTTP&#x2F;3做了哪些优化</h3><p><strong>HTTP&#x2F;3 把 HTTP 下层的 TCP 协议改成了 UDP,基于 UDP 的 QUIC 协议 可以实现类似 TCP 的可靠性传输。</strong></p><ul><li>QUIC 有⾃⼰的⼀套机制可以保证传输的可靠性的。当某个流发⽣丢包时，<mark>只会阻塞这个流，其他流不会受到影响。</mark></li><li>TLS3 升级成了最新的 1.3 版本，头部压缩算法也升级成了 QPack 。</li><li>HTTPS 要建⽴⼀个连接，要花费 6 次交互，先是建⽴三次握⼿，然后是 TLS&#x2F;1.3 的三次握⼿。QUIC 直接把以往的 TCP 和 TLS&#x2F;1.3 的 6 次交互合并成了 3 次，减少了交互次数。</li></ul><p><img data-src="/transblog/images/3227683816513.png"></p><p>QUIC 是一个在 UDP 之上的伪 TCP + TLS + HTTP&#x2F;2 的多路复用的协议。</p><h2 id="HTTPS"><a href="#HTTPS" class="headerlink" title="HTTPS"></a>HTTPS</h2><p>HTTPS 在 TCP 三次握手之后，还需进行 SSL&#x2F;TLS 的握手过程，才可进入加密报文传输。两者的默认端口不一样，HTTP 默认端口号是 80，HTTPS 默认端口号是 443。HTTPS 协议需要向 CA（证书权威机构）申请数字证书，来保证服务器的身份是可信的。很好的解决了<strong>窃听⻛险，篡改⻛险，冒充⻛险</strong>。</p><ol><li>混合加密的⽅式实现信息的机密性，解决了窃听的⻛险。</li><li>摘要算法的⽅式来实现完整性，它能够为数据⽣成独⼀⽆⼆的「指纹」，指纹⽤于校验数据的完整性，解决了篡改的⻛险。</li><li>将服务器公钥放⼊到数字证书中，解决了冒充的⻛险。</li></ol><h3 id="HTTPS的特点"><a href="#HTTPS的特点" class="headerlink" title="HTTPS的特点"></a>HTTPS的特点</h3><h4 id="摘要算法"><a href="#摘要算法" class="headerlink" title="摘要算法"></a>摘要算法</h4><p>客户端在发送明⽂之前会通过摘要算法（哈希函数）算出内容的哈希值，这个哈希值是唯一的，且无法通过哈希值推导出内容。发送的时候把「哈希值 + 明⽂」⼀同加密成密⽂后，发送给服务器，服务器解密后，⽤相同的摘要算法算出发送过来的明⽂，通过⽐较客户端携带的「哈希值」和当前算出的「哈希值」做⽐较，若「哈希值」相同，说明数据是完整的。</p><p><img data-src="/transblog/images/25951248990.png"></p><p><strong>通过哈希算法可以确保内容不会被篡改，但是并不能保证「内容 + 哈希值」不会被中间人替换，因为这里缺少对客户端收到的消息是否来源于服务端的证明</strong>。为了避免这种情况，用非对称加密算法来解决</p><h4 id="混合加密"><a href="#混合加密" class="headerlink" title="混合加密"></a>混合加密</h4><p>HTTPS 采⽤的是对称加密和⾮对称加密结合的「混合加密」⽅式：</p><ul><li>在通信建⽴前采⽤⾮对称加密的⽅式交换「会话秘钥」，后续就不再使⽤⾮对称加密。</li><li>在通信过程中全部使⽤对称加密的「会话秘钥」的⽅式加密明⽂数据。</li></ul><p><strong>采⽤「混合加密」方式的原因：</strong></p><ul><li>对称加密只使⽤⼀个密钥，运算速度快，密钥必须保密，⽆法做到安全的密钥交换。</li><li>⾮对称加密使⽤两个密钥：公钥和私钥，公钥可以任意分发⽽私钥保密，解决了密钥交换问题但速度慢。</li></ul><p>流程的不同，意味着目的也不相同：<br>公钥加密，私钥解密。这个目的是<strong>为了保证内容传输的安全</strong>，因为被公钥加密的内容，其他人是无法解密的，只有持有私钥的人，才能解密出实际的内容；<br>私钥加密，公钥解密。这个目的是<strong>为了保证消息不会被冒充</strong>，因为私钥是不可泄露的，如果公钥能正常解密出私钥加密的内容，就能证明这个消息是来源于持有私钥身份的人发送的。</p><p>但是一般不会用非对称加密来加密实际的传输内容，因为非对称加密的计算比较耗费性能的。而是对内容的哈希值加密。</p><p><img data-src="/transblog/images/204515509246226.png"></p><h4 id="数字证书"><a href="#数字证书" class="headerlink" title="数字证书"></a>数字证书</h4><p>需要借助第三⽅权威机构 CA （数字证书认证机构），将服务器公钥放在数字证书（由数字证书认证机构颁发）中，只要证书是可信的，公钥就是可信的。一个数字证书通常包含了：</p><ol><li>公钥；</li><li>持有者信息；</li><li>证书认证机构（CA）的信息；</li><li>CA 对这份文件的数字签名及使用的算法；</li><li>证书有效期；</li><li>还有一些其他额外信息；</li></ol><h3 id="SSL-TLS-协议基本流程"><a href="#SSL-TLS-协议基本流程" class="headerlink" title="SSL&#x2F;TLS 协议基本流程"></a>SSL&#x2F;TLS 协议基本流程</h3><p>TLS 的「握手阶段」涉及四次通信，一般位于TCP和HTTP之间，在三次握手之后</p><p><img data-src="/transblog/images/256104010263802.png"></p><ol><li>首先，由客户端向服务器发起加密通信请求，也就是 ClientHello 请求。涉及以下信息：<blockquote><p><strong>①TLS 协议版本</strong><br><strong>②随机数(Client Random)，后面用于生成「会话秘钥」条件之一</strong><br><strong>③支持的密码套件列表，如 RSA 加密算法</strong></p></blockquote></li></ol><p><img data-src="/transblog/images/426134610257348.png"></p><ol start="2"><li>服务器收到客户端请求后，向客户端发出响应，也就是 SeverHello + Server Certificate + Server Hello Done。回应的内容：<blockquote><p><strong>①确认 TLS 协议版本，如果浏览器不支持，则关闭加密通信</strong><br><strong>②随机数(Server Random)，也是后面用于生产「会话秘钥」条件之一</strong><br><strong>③确认的密码套件列表，如 RSA 加密算法。</strong><br><strong>④服务器的数字证书</strong></p></blockquote></li></ol><p><img data-src="/transblog/images/69494710250482.png" alt="Server Hello"></p><p><img data-src="/transblog/images/574544810241012.png" alt="Server Certificate"></p><p><img data-src="/transblog/images/84844910243516.png" alt="Server Hello Done"></p><ol start="3"><li>客户端收到服务器的回应之后，发送响应：Client Key Exchange + Change Cipher Spec + Encrypted Handshake Message（Finishd）<br>首先通过浏览器或者操作系统中的 CA 公钥，确认服务器的数字证书的真实性。如果证书没有问题，<strong>客户端会从数字证书中取出服务器的公钥</strong>，然后使用它加密报文，向服务器发送如下信息：<blockquote><p><strong>①一个随机数(pre-master key)。该随机数会被服务器公钥加密</strong><br><strong>②加密通信算法改变通知，表示随后的信息都将用「会话秘钥」加密通信</strong><br><strong>③客户端握手结束通知，表示客户端的握手阶段已经结束。这一项同时把之前所有内容的发生的数据做个摘要，用来供服务端校验</strong></p></blockquote></li></ol><p><img data-src="/transblog/images/186055410252463.png" alt="Client Key Exchange"></p><p>上面第一项的随机数是整个握手阶段的第三个随机数，会发给服务端，所以这个随机数客户端和服务端都是一样的。服务器和客户端有了这三个随机数（Client Random、Server Random、pre-master key），接着就用双方协商的加密算法，各自生成本次通信的「会话秘钥」。</p><p>生成完「会话密钥」后，然后客户端发一个「Change Cipher Spec」，告诉服务端开始使用加密方式发送消息。</p><p><img data-src="/transblog/images/75415610245348.png" alt="Change Cipher Spec"></p><p>然后，客户端再发一个「Encrypted Handshake Message（Finishd）」消息，把之前所有发送的数据做个摘要，再用会话密钥（master secret）加密一下，让服务器做个验证，验证加密通信「是否可用」和「之前握手信息是否有被中途篡改过」。</p><p><img data-src="/transblog/images/402781011245957.png" alt="Encrypted Handshake Message"></p><ol start="4"><li>服务器收到客户端的第三个随机数（pre-master key）之后，通过协商的加密算法，计算出本次通信的「会话秘钥」。然后，向客户端发送最后的信息：<blockquote><p><strong>①加密通信算法改变通知，表示随后的信息都将用「会话秘钥」加密通信</strong><br><strong>②服务器握手结束通知，表示服务器的握手阶段已经结束。这一项同时把之前所有内容的发生的数据做个摘要，用来供客户端校验</strong></p></blockquote></li></ol><p>至此，整个 TLS 的握手阶段全部结束。接下来，客户端与服务器进入加密通信，就完全是使用普通的 HTTP 协议，只不过用「会话秘钥」加密内容。</p><h3 id="HTTPS一定安全？"><a href="#HTTPS一定安全？" class="headerlink" title="HTTPS一定安全？"></a>HTTPS一定安全？</h3><p>HTTPS本身是安全的</p><p><img data-src="/transblog/images/96472810268666.png"></p><p>中间人服务器与客户端在 TLS 握手过程中，<strong>实际上发送了自己伪造的证书给浏览器，而这个伪造的证书是能被浏览器（客户端）识别出是非法的</strong>，于是就会提醒用户该证书存在问题。如果你<strong>无视风险，继续访问</strong>，那就是不安全的</p><h3 id="密匙交换算法"><a href="#密匙交换算法" class="headerlink" title="密匙交换算法"></a>密匙交换算法</h3><p>使⽤⾮对称加密的⽅式来保护对称加密密钥的协商，这个⼯作就是密钥交换算法负责的。</p><h4 id="RSA-算法"><a href="#RSA-算法" class="headerlink" title="RSA 算法"></a>RSA 算法</h4><ul><li>传统的 TLS 握⼿基本都是使⽤ RSA 算法来实现密钥交换的，在将 TLS 证书部署服务端时，证书⽂件中包含⼀对公私钥，其中公钥会在 TLS 握⼿阶段传递给客户端，私钥则⼀直留在服务端，⼀定要确保私钥不能被窃取。</li><li>在 RSA 密钥协商算法中，客户端会⽣成随机密钥，并使⽤服务端的公钥加密后再传给服务端。根据⾮对称加密算法，公钥加密的消息仅能通过私钥解密，这样服务端解密后，双⽅就得到了相同的密钥，再⽤它加密应⽤消息。</li></ul><p><strong>缺陷：</strong><br><strong>不⽀持前向保密</strong>。因为客户端传递随机数（⽤于⽣成对称加密密钥的条件之⼀）给服务端时使⽤的是公钥加密的，服务端收到到后，会⽤私钥解密得到随机数。所以⼀旦服务端的私钥泄漏了，过去被第三⽅截获的所有 TLS 通讯密⽂都会被破解。</p><h4 id="ECDHE-算法"><a href="#ECDHE-算法" class="headerlink" title="ECDHE 算法"></a>ECDHE 算法</h4><p>ECDHE 密钥协商算法是 DH 算法演进过来的，先从 DH 算法说起。</p><h5 id="DH-算法"><a href="#DH-算法" class="headerlink" title="DH 算法"></a>DH 算法</h5><p>该算法的核心数学思想是离散对数。离散对数是在对数运算的基础上加了「模运算」，也就说取余数，也可以用 mod 表示。</p><p><img data-src="/transblog/images/293411411272912.png"></p><p>底数 a 和模数 p 是离散对数的公共参数，也就说是公开的，b 是真数，i 是对数。<strong>知道了对数，就可以用上面的公式计算出真数。但反过来，知道真数却很难推算出对数。特别是当模数 p 是一个很大的质数，即使知道底数 a 和真数 b ，在现有的计算机的计算水平是几乎无法算出离散对数的，这就是 DH 算法的数学基础。</strong></p><p>现假设⼩红和⼩明约定使⽤ DH 算法来交换密钥，那么基于离散对数，需要先确定模数和底数作为算法的参数，这两个参数是公开的，⽤ P 和 G 来代称。然后⼩红和⼩明各⾃⽣成⼀个随机整数作为私钥，⼩红的私钥⽤ a 代称，⼩明的私钥⽤ b 代称。</p><p>现在⼩红和⼩明双⽅都有了 P 和 G 以及各⾃的私钥，于是就可以计算出公钥：<br>⼩红的公钥记作 A，A &#x3D; G ^ a ( mod P )；<br>⼩明的公钥记作 B，B &#x3D; G ^ b ( mod P )；</p><ul><li>A 和 B 也是公开的，因为根据离散对数的原理，从真数（A 和 B）反向计算对数 a 和 b 是⾮常困难的</li><li>双⽅交换各⾃ DH 公钥后，⼩红⼿上共有 5 个数：P、G、a、A、B，⼩明⼿上也同样共有 5 个数：P、G、b、B、A。</li><li>⼩红执⾏运算： B ^ a ( mod P )，其结果为 K，<strong>因为离散对数的幂运算有交换律</strong>，所以⼩明执⾏运算： A ^ b (mod P )，得到的结果也是 K。</li><li>这个 K 就是⼩红和⼩明之间⽤的对称加密密钥，可以作为会话密钥使⽤。</li></ul><p><img data-src="/transblog/images/3347520485606.png"></p><p>可以看到，整个密钥协商过程中，⼩红和⼩明公开了 4 个信息：P、G、A、B，其中 P、G 是算法的参数，A 和 B是公钥，⽽ a、b 是双⽅各⾃保管的私钥，⿊客⽆法获取这 2 个私钥，因此⿊客只能从公开的 P、G、A、B ⼊⼿，计算出离散对数（私钥）。如果 P 是⼀个⼤数，在现有的计算机的计算能⼒是很难破解出 私钥 a、b的，破解不出私钥，也就⽆法计算出会话密钥，因此 DH 密钥交换是安全的。</p><p><strong>缺陷：</strong></p><ul><li><strong>不具备前向安全性</strong>。DH 算法里有一方的私钥是静态的，也就说每次密钥协商的时候有一方的私钥都是一样的，一般是服务器方固定，即小红的私钥 a 不变，客户端的私钥则是随机生成的。于是，DH 交换密钥时就只有客户端（小明）的公钥是变化，而服务端（小红）公钥是不变的，</li><li>那么随着时间延长，因为密钥协商的过程有些数据是公开的，黑客就会截获海量的密钥协商过程的数据，暴力破解出服务器的私钥，然后就可以计算出会话密钥了，于是之前截获的加密数据会被破解</li></ul><h5 id="DHE算法"><a href="#DHE算法" class="headerlink" title="DHE算法"></a>DHE算法</h5><p>让双方的私钥在每次密钥交换通信时，都是随机生成的、临时的，这个方式也就是 DHE 算法，E 全称是 ephemeral（临时性的）。</p><p><strong>缺陷：</strong><br>计算性能不佳，因为需要做大量的乘法</p><h5 id="ECDHE算法"><a href="#ECDHE算法" class="headerlink" title="ECDHE算法"></a>ECDHE算法</h5><p>ECDHE 算法是在 DHE 算法的基础上利用了 ECC 椭圆曲线特性，可以用更少的计算量计算出公钥，以及最终的会话密钥。</p><h6 id="椭圆曲线"><a href="#椭圆曲线" class="headerlink" title="椭圆曲线"></a>椭圆曲线</h6><p>什么是椭圆曲线？椭圆曲线有两个特点：</p><ul><li>如果你在上方随便画一个点，那么下方也一定有一个对称的点，上下方距离水平线X轴是相同的。</li><li>随便在图形上画两个点，让这两个点连成线然后延长会经过第三个点，当然除了垂直线以外。</li></ul><ol><li>如果有A，B两个点，延长以后会经过第三个点，而这第三个点以X轴为中心是会有一个点与其对称，把这个对称的点称为C点。A和B得出C，把运算过程称为 “点运算”，A点B得到C，这个 “点运算”其实就是椭园曲线上的加法运算。</li><li>现在把A和C进行连线，同样经过了第三个点，第三个点也有一个对称的点，这里称为D点，也就是A点C得到D，</li><li>再把A和D连线，也经过了第三个点，第三个点也有一个对称的点，这里称为E点，也就是A点D得到E。</li></ol><p><img data-src="/transblog/images/352361615252903.png"></p><p>问题：已知起点是A，终点是E，请问起点A经过多少次点运算得到E？很难知道经过了多少次，这就很符合我们前面说的公钥加密的特点：正向简单，逆向困难</p><p><img data-src="/transblog/images/99411015240674.png"></p><h6 id="ECDHE密钥交换过程"><a href="#ECDHE密钥交换过程" class="headerlink" title="ECDHE密钥交换过程"></a>ECDHE密钥交换过程</h6><p>Alice和Bob使用 ECDHE 密钥交换算法的过程：</p><p><img data-src="/transblog/images/491822515257942.png"></p><ol><li>双方事先确定好使用哪种椭圆曲线，和曲线上的基点 G，这两个参数都是公开的；</li><li>Alice随机生成一个随机数作为私钥a，并与基点G相乘得到公钥A（A &#x3D; a * G，就是G这个点进行点运算，次数是a，也就是G点G点G点…一共a次），此时Alice的公私钥为 A 和 a，把大A和G发送给Bob</li><li>Bob收到后，也生成了一个私钥b，然后生成椭圆曲线上的一个新点，公钥B（B &#x3D; b * G，就是G点进行小b次运算得到的，也就是G点G点G点…一共b次），此时Bob的公私钥为 B 和 b，把生成的大B发送给Alice，别人知道 B 和 G 也很难得到 b</li><li>最后Alice计算点（x1，y1） &#x3D; B * a，Bob计算点（x2，y2）&#x3D; A * b，<strong>由于椭圆曲线上是可以满足乘法交换和结合律</strong>，所以 B * a &#x3D; G * a * b &#x3D; A * b（假设a&#x3D;3，b&#x3D;2） ，因此双方的 x 坐标是一样的，所以它是共享密钥，也就是会话密钥。</li></ol><h1 id="应用层"><a href="#应用层" class="headerlink" title="应用层"></a>应用层</h1><h2 id="请求转发和请求重定向的异同"><a href="#请求转发和请求重定向的异同" class="headerlink" title="请求转发和请求重定向的异同"></a>请求转发和请求重定向的异同</h2><ol><li><p>跳转机制：<br>请求转发（Forward）：是服务器内部的行为，当服务器收到客户端的请求后，会将请求转发给目标地址，并将目标地址的响应结果返回给客户端。在整个过程中，客户端对服务器内部的转发过程毫不知情，只感知到一次请求和响应。<br>请求重定向（Redirect）：是客户端的行为，服务器在接收到客户端的请求后，返回一个临时响应头，其中包含客户端需要再次发送请求的URL地址。客户端在收到这个地址后，会向新的地址发送请求，从而实现页面的跳转。这个过程对客户端来说是两次独立的请求和响应。</p></li><li><p>数据共享：<br>请求转发：由于是服务器内部操作，整个交互过程中使用的是同一个Request对象和一个Response对象，因此请求和返回的数据是共享的。<br>请求重定向：客户端发送两次完全不同的请求，因此两次请求中的数据是不同的。</p></li><li><p>URL地址的改变：<br>请求转发：浏览器上的地址不会改变，因为服务器内部的处理对客户端来说是透明的。<br>请求重定向：浏览器上的地址会改变，因为客户端会看到一个不同的URL地址被加载。</p></li><li><p>效率：<br>请求转发：因为只涉及一次请求和响应，所以效率更高。<br>请求重定向：涉及两次请求和响应，所以效率相对较低。</p></li><li><p>数据传递：<br>请求转发：可以使用Request对象在多个页面间传递参数。<br>请求重定向：通常不使用Request对象传递参数，但可以通过URL参数或会话(Session)等方式传递。</p></li><li><p>表单重复提交：<br>请求转发：可能造成表单的重复提交，因为表单提交后的行为发生在服务器内部，无法直接感知到表单提交的结果。<br>请求重定向：不会造成表单的重复提交，因为表单提交后会触发浏览器的跳转，从而避免了重复提交的问题。</p></li><li><p>目标服务器：<br>请求转发：只能在服务器内部转发，不能跳转到其他服务器。<br>请求重定向：可以跳转到其他服务器进行转发。</p></li></ol><h2 id="Cookie、Session、Token"><a href="#Cookie、Session、Token" class="headerlink" title="Cookie、Session、Token"></a>Cookie、Session、Token</h2><h3 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h3><p>在客户端第⼀次请求后，服务器会下发⼀个装有客户信息的「⼩贴纸」，后续客户端请求服务器的时候，带上「⼩贴纸」，服务器就能认得了，Cookie具有不可跨域名性</p><h3 id="Session"><a href="#Session" class="headerlink" title="Session"></a>Session</h3><ul><li><strong>Session机制是通过检查服务器上的“客户明细表”来确认客户身份。Session相当于程序在服务器上建立的一份客户档案，客户来访的时候只需要查询客户档案表。</strong></li><li>Session机制决定了当前客户只会获取到自己的Session，而不会获取到别人的Session。各客户的Session也彼此独立，互不可见。</li><li><strong>Session对象是在客户端第一次请求服务器的时候创建的</strong>。Session也是一种key-value的属性对，通过getAttribute(Stringkey)和setAttribute(String key，Objectvalue)方法读写客户状态信息。Servlet里通过request.getSession()方法获取该客户的Session，</li><li>Session生成后，只要用户继续访问，服务器就会更新Session的最后访问时间，并维护该Session。为防止内存溢出，服务器会把长时间内没有活跃的Session从内存删除。这个时间就是Session的超时时间。如果超过了超时时间没访问过服务器，Session就自动失效了。</li></ul><h3 id="区别-1"><a href="#区别-1" class="headerlink" title="区别"></a>区别</h3><ol><li><strong>cookie数据存放在客户的浏览器上，session数据放在服务器上。Cookie通过在客户端记录信息确定用户身份，Session通过在服务器端记录信息确定用户身份。</strong></li><li>cookie不是很安全，别人可以分析存放在本地的COOKIE并进行COOKIE欺骗,如果主要考虑到安全应当使用session</li><li>session会在一定时间内保存在服务器上。当访问增多，会比较占用你服务器的性能，如果主要考虑到减轻服务器性能方面，应当使用COOKIE</li><li>将登陆信息等重要信息存放为SESSION;其他信息如果需要保留，可以放在COOKIE中</li><li>两者最大的区别在于生存周期，一个是IE启动到IE关闭.(浏览器页面一关 ,session就消失了)，一个是预先设置的生存周期，或永久的保存于本地的文件。(cookie)</li></ol><h3 id="痛点"><a href="#痛点" class="headerlink" title="痛点"></a>痛点</h3><p>实际在生产上，为了保障高可用，一般服务器至少需要两台机器，客户端请求后，由负载均衡器（如 Nginx）来决定到底打到哪台机器</p><p>假设登录请求打到了 A 机器，A 机器生成了 session 并在 cookie 里添加 sessionId 返回给了浏览器，那么问题来了：下次添加购物车时如果请求打到了 B 或者 C，由于 session 是在 A 机器生成的，此时的 B,C 是找不到 session 的，那么就会发生无法添加购物车的错误，就得重新登录了，此时请问该怎么办。主要有以下三种方式：</p><ol><li>session 复制<br>A 生成 session 后复制到 B, C，这样每台机器都有一份 session，无论添加购物车的请求打到哪台机器，由于 session 都能找到，故不会有问题。<br>缺点：同一样的一份 session 保存了多份，数据冗余；如果节点少还好，但如果节点多的话，可能需要部署成千上万台机器，这样节点增多复制造成的性能消耗也会很大。</li><li>session 粘连<br>这种方式是让每个客户端请求只打到固定的一台机器上，比如浏览器登录请求打到 A 机器后，后续所有的添加购物车请求也都打到 A 机器上，Nginx 的 sticky 模块可以支持，支持按 ip 或 cookie 粘连等等，如按 ip 粘连方式如下<figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">upstream tomcats &#123;</span><br><span class="line">　　ip_hash;</span><br><span class="line">　　server 10.1.1.107:88;</span><br><span class="line">　　server 10.1.1.132:80;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>缺点：机器挂了没有其它办法</li><li>session 共享<br>许多公司普遍采用的方案，将 session 保存在 redis，memcached 等中间件中，请求到来时，各个机器去这些中间件取一下 session 即可。</li></ol><p><img data-src="/transblog/images/272494516240770.png"></p><p>缺点：就是每个请求都要去 redis 取一下 session，多了一次内部连接，消耗了一点性能，另外为了保证 redis 的高可用，必须做集群。搞个校验机制我还得搭个 redis 集群？Token应运而生</p><h3 id="Token令牌"><a href="#Token令牌" class="headerlink" title="Token令牌"></a>Token令牌</h3><p>Token是服务端生成的一串字符串，当作客户端进行请求的一个令牌，当第一次登录后，服务器生成一个Token并将此Token返回给客户端，以后客户端只需带上这个Token前来请求数据即可，无需再次带上用户名和密码</p><p>无状态、可扩展：Token是 服务经过计算发给客户端的，<strong>服务不保存，每次客户端来请求</strong>，经过<strong>解密</strong>等计算来验证是否是自己下发的</p><p><img data-src="/transblog/images/454174716259196.png"></p><p>前面说过，Token不需要保存，无论请求哪台机器，根据第一次下发的令牌进行实时的计算，就可以解决session的痛点</p><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;OSI七层模型-TCP-IP五层网络模型&quot;&gt;&lt;a href=&quot;#OSI七层模型-TCP-IP五层网络模型&quot; class=&quot;headerlink&quot; title=&quot;OSI七层模型&amp;amp;TCP&amp;#x2F;IP五层网络模型&quot;&gt;&lt;/a&gt;OSI七层模型&amp;amp;TCP&amp;#</summary>
      
    
    
    
    <category term="四件套" scheme="https://trans-xu.github.io/transblog/categories/%E5%9B%9B%E4%BB%B6%E5%A5%97/"/>
    
    
  </entry>
  
  <entry>
    <title>拼手气红包只抢了0.01</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E7%BA%A2%E5%8C%85%E7%AE%97%E6%B3%95%E4%B8%8E%E9%9A%8F%E6%9C%BA%E6%95%B0/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E7%BA%A2%E5%8C%85%E7%AE%97%E6%B3%95%E4%B8%8E%E9%9A%8F%E6%9C%BA%E6%95%B0/</id>
    <published>2025-08-27T03:50:08.921Z</published>
    <updated>2025-08-27T03:36:56.580Z</updated>
    
    <content type="html"><![CDATA[<h1 id="抢到红包0-01元，怎么搞的？"><a href="#抢到红包0-01元，怎么搞的？" class="headerlink" title="抢到红包0.01元，怎么搞的？"></a>抢到红包0.01元，怎么搞的？</h1><p><strong>抢红包时你为什么只抢到0.01，拼手气红包的随机是如何运作的？</strong><br>最初的随机算法中，领取越早获得大额红包几率越高，为了避免抢红包变成一个拼手速的游戏，后来的随机算法也对随机范围区间进行了一定调整。</p><h2 id="普通随机"><a href="#普通随机" class="headerlink" title="普通随机"></a>普通随机</h2><p>剩余值随机红包算法 ，一般都是使用剩余值在计算一把。用余下的值为最大区间进行随机，但可能不均匀，有些人一把随到99，下面很多人都没得随机了。</p><p>优点：逻辑简单，<br>缺点：随机区间步步减少，可以明显看出随机值的递减特性，做抢红包体验很差，对后面玩家极不公平，且容易被抓到规律，造成舆论不满。</p><h2 id="二倍均值算法"><a href="#二倍均值算法" class="headerlink" title="二倍均值算法"></a>二倍均值算法</h2><p>该算法的核心思想：<br>在每次剩余的总金额和剩余人数的基础上，执行一个操作：将剩余总金额除以剩余人数，然后乘以2，得到一个边界值E。接着，在从0到E的随机区间内生成一个随机金额R，此时总金额更新为M-R，剩余人数更新为N-1。这个过程重复进行，直到最终剩余人数N-1为0，即所有随机数已经产生完毕。</p><p><img data-src="/transblog/images/435884609258196.png"></p><ol><li>剩余红包金额为M，剩余人数为N，每次抢到的金额 &#x3D; 随机区间 （0， M &#x2F; N X 2），</li><li>假设有10个人，红包总额100元。</li><li>100&#x2F;10X2 &#x3D; 20, 所以第一个人的随机范围是（0，20 )，平均可以抢到10元。</li><li>假设第一个人随机到10元，那么剩余金额是100-10 &#x3D; 90 元。</li><li>90&#x2F;9X2 &#x3D; 20, 所以第二个人的随机范围同样是（0，20 )，平均可以抢到10元。</li><li>假设第二个人随机到10元，那么剩余金额是90-10 &#x3D; 80 元。</li><li>80&#x2F;8X2 &#x3D; 20, 所以第三个人的随机范围同样是（0，20 )，平均可以抢到10元。</li><li>以此类推，每一次随机范围的均值是相等的。</li></ol><p>此外，二倍均值算法还基于一个公平的概率策略：即每个人抢到J &#x2F; N的金额的概率是相同的。例如，如果有一个100元的红包要发给10个人，那么最公平的策略是使每个人抢到10元的概率相同。通过计算随机数区间上界U &#x3D; J &#x2F; N * 2，并在(0,U)的区间内生成第一个随机数金额M，然后继续生成第二个、第三个等随机金额，直到最后一个红包金额被生成。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> List&lt;Double&gt; <span class="title function_">doubleMeanMethod</span><span class="params">(<span class="type">double</span> money,<span class="type">int</span> number)</span>&#123;</span><br><span class="line">List&lt;Double&gt; result = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;Double&gt;();</span><br><span class="line"><span class="keyword">if</span>(money&lt;<span class="number">0</span>&amp;&amp;number&lt;<span class="number">1</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line"><span class="type">double</span> amount,sum=<span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> remainingNumber=number;</span><br><span class="line"><span class="type">int</span> i=<span class="number">1</span>;</span><br><span class="line"><span class="keyword">while</span>(remainingNumber&gt;<span class="number">1</span>)&#123;</span><br><span class="line">amount= nextDouble(<span class="number">0.01</span>,<span class="number">2</span>*(money/remainingNumber));</span><br><span class="line">sum+=amount;</span><br><span class="line">System.out.println(<span class="string">&quot;第&quot;</span>+i+<span class="string">&quot;个人领取的红包金额为：&quot;</span>+format(amount));</span><br><span class="line">money -= amount;</span><br><span class="line">remainingNumber--;</span><br><span class="line">result.add(amount);</span><br><span class="line">i++;</span><br><span class="line">&#125;</span><br><span class="line">result.add(money);</span><br><span class="line">System.out.println(<span class="string">&quot;第&quot;</span>+i+<span class="string">&quot;个人领取的红包金额为：&quot;</span>+format(money));</span><br><span class="line">sum+=money;</span><br><span class="line">System.out.println(<span class="string">&quot;验证发出的红包总金额为：&quot;</span>+format(sum));</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>优点：</p><ul><li>它能够保证每次生成的随机金额R的随机性和平等性，同时由于每次总金额M的更新采用递减方式（即M&#x3D;M-R），可以确保最终产生的所有随机金额之和等于初始的总金额。</li><li>这种算法通过在每次迭代中更新总金额和剩余人数，确保了每个人获得金额的平均值相等，避免了因抢红包的顺序不同而导致的分配不均问题。</li></ul><h2 id="线段分割算法"><a href="#线段分割算法" class="headerlink" title="线段分割算法"></a>线段分割算法</h2><p>我们将红包总额度比喻为一条同等长度的线段，如果有10个人领红包，我们就对其切割9次，线段就会分为时段，这时每人拿一段就达到了要求，非常的巧妙。</p><p>计算过程：我们用数组或者List去模拟一条线段，第一个数是0，最后一个数是红包总额，有n个人抢红包我们就生成n-1个不同的随机数插入到0和红包总额之间，然后对数组或list中的数字大小进行排序，每个人拿到的金额就是后一个数减去前一个数之差。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line">function lineSegmentRedbag($Moneys, $userNums, $isEveryHave = 1, $baseMoney = 1)</span><br><span class="line">&#123;</span><br><span class="line">     if ($Moneys &lt;= 0 || $userNums &lt;= 0) &#123;</span><br><span class="line">        return [&#x27;code&#x27; =&gt; -3, &#x27;msg&#x27; =&gt; &#x27;红包金额或拆红包总人数不合法&#x27;];</span><br><span class="line">    &#125;</span><br><span class="line">    if ($isEveryHave &amp;&amp; $userNums &gt; $Moneys) &#123;</span><br><span class="line">        return [&#x27;code&#x27; =&gt; -4, &#x27;msg&#x27; =&gt; &#x27;红包数量不足&#x27;];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    $cutPoints = []; //切割点数组</span><br><span class="line">    $pointNums = $userNums - 1; //存放的</span><br><span class="line">    $userMoney = []; //每一个用户该分得的钱</span><br><span class="line">    //正式线段分割，完全随机 </span><br><span class="line">    // $j = 0;</span><br><span class="line">    // 当 用户数 和 总金额差距不大时，这种写法效率极差</span><br><span class="line">    while ($pointNums &gt; 0) &#123;</span><br><span class="line">        if ($isEveryHave == 1) &#123;</span><br><span class="line">            $randVal = mt_rand(1, $Moneys - 1); //每个人都有，mt_rand包含区间边界的，即包含最大值 和 最小值 ，1和2都会出现</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            $randVal = mt_rand(0, $Moneys); //所有用户，全区间随机，保证了公平，所有人概率一致 0~10。如果$Moneys设置-1，导致最后一位必定不为0</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        if (in_array($randVal, $cutPoints)) &#123;  //这边会产生随机碰撞，500个随机需要2500次左右才能覆盖。</span><br><span class="line">            // $j++;</span><br><span class="line">            continue;</span><br><span class="line">        &#125;</span><br><span class="line">        $cutPoints[] = $randVal;</span><br><span class="line">        $pointNums--;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // echo &quot;无效循环次数：&quot; . $j . &quot;&lt;br&gt;&quot;;</span><br><span class="line">    // echo &quot;最终切割点数组数量：&quot; . count($cutPoints) . &quot;&lt;br&gt;&quot;;</span><br><span class="line">    // var_dump($cutPoints);</span><br><span class="line">    // return;</span><br><span class="line"></span><br><span class="line">    //根据cutPoint计算每个人所得 同时考虑：就一个人</span><br><span class="line">    $lastVal = 0;</span><br><span class="line">    if (count($cutPoints) &gt; 0) &#123;</span><br><span class="line">        sort($cutPoints);</span><br><span class="line">        foreach ($cutPoints as $RandPoint) &#123;</span><br><span class="line">            $userMoney[] = $RandPoint - $lastVal;</span><br><span class="line">            $lastVal = $RandPoint;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    $lastDiff = $Moneys - $lastVal;</span><br><span class="line">    $userMoney[] =  $lastDiff;</span><br><span class="line"></span><br><span class="line">    // echo &quot;总数：&quot; . count($userMoney) . &quot;&lt;br&gt;&quot;;</span><br><span class="line">    // echo &quot;总值：&quot; . array_sum($userMoney) . &quot;&lt;br&gt;&quot;;</span><br><span class="line">    return [&#x27;code&#x27; =&gt; 0, &#x27;msg&#x27; =&gt; &quot;success&quot;, &#x27;redbag&#x27; =&gt; $userMoney];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>但是有个致命缺陷，随机值碰撞，分割数量越接近总金额，碰撞概率越大 ，所以最好用户数量与总金额差的越大越好</strong>，利用array_rand一次拿出多个随机值时，随机且去重,且随机区间包括首尾。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line">function lineSegmentOptimize($Moneys, $userNums, $isEveryHave = 1) //$baseMoney = 1默认为1</span><br><span class="line">&#123;</span><br><span class="line">    if ($Moneys &lt;= 0 || $userNums &lt;= 0) &#123;</span><br><span class="line">        return [&#x27;code&#x27; =&gt; -3, &#x27;msg&#x27; =&gt; &#x27;红包金额或拆红包总人数不合法&#x27;];</span><br><span class="line">    &#125;</span><br><span class="line">    if ($isEveryHave &amp;&amp; $userNums &gt; $Moneys) &#123;</span><br><span class="line">        return [&#x27;code&#x27; =&gt; -4, &#x27;msg&#x27; =&gt; &#x27;红包数量不足&#x27;];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    $cutPoints = [];</span><br><span class="line">    $userMoney = [];</span><br><span class="line"></span><br><span class="line">    if ($isEveryHave) &#123;</span><br><span class="line">        $MoneysArr = array_fill(1, $Moneys - 1, 0); //转成数组时,去掉头尾得-1，如果10，则下标是1-9</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        $MoneysArr = array_fill(0, $Moneys + 1, 0); //转成数组,为了保留头尾得+1，如果10，则下标是0-10，array_rand区间包含首尾</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if ($userNums == 1) &#123;</span><br><span class="line">        $userMoney[] = $Moneys;</span><br><span class="line">        return [&#x27;code&#x27; =&gt; 0, &#x27;msg&#x27; =&gt; &quot;success&quot;, &#x27;redbag&#x27; =&gt; $userMoney];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    $cutPoints = array_rand($MoneysArr, $userNums - 1); //多随机、且去重、且区间包含首尾，array_rand第二个值不可为0</span><br><span class="line">    sort($cutPoints);</span><br><span class="line">    $lastVal = 0;</span><br><span class="line">    foreach ($cutPoints as  $randPoint) &#123;</span><br><span class="line">        $diff = $randPoint - $lastVal;</span><br><span class="line">        $userMoney[] =  $diff;</span><br><span class="line">        $lastVal = $randPoint;</span><br><span class="line">    &#125;</span><br><span class="line">    $lastDiff = $Moneys - $lastVal;</span><br><span class="line">    $userMoney[] =  $lastDiff;</span><br><span class="line"></span><br><span class="line">    // echo &quot;总数：&quot; . count($userMoney) . &quot;&lt;br&gt;&quot;;</span><br><span class="line">    // var_dump($userMoney);</span><br><span class="line">    // echo &quot;总值：&quot; . array_sum($userMoney) . &quot;&lt;br&gt;&quot;;</span><br><span class="line">    return [&#x27;code&#x27; =&gt; 0, &#x27;msg&#x27; =&gt; &quot;success&quot;, &#x27;redbag&#x27; =&gt; $userMoney];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="总结对比"><a href="#总结对比" class="headerlink" title="总结对比"></a>总结对比</h2><p><img data-src="/transblog/images/408022611264994.png"></p><h1 id="计算机到底是真随机还是假随机？"><a href="#计算机到底是真随机还是假随机？" class="headerlink" title="计算机到底是真随机还是假随机？"></a>计算机到底是真随机还是假随机？</h1><p><a href="https://www.random.org/">随机数网站</a></p><h2 id="真随机数"><a href="#真随机数" class="headerlink" title="真随机数"></a>真随机数</h2><p>真正的随机数是使用物理现象产生的：比如掷钱币、骰子、转轮、使用电子元件的噪音、核裂变等等，这样的随机数发生器叫做物理性随机数发生器，它们的缺点是技术要求比较高。</p><p><img data-src="/transblog/images/142184411246235.png" alt="真随机数图像"></p><h2 id="伪随机数"><a href="#伪随机数" class="headerlink" title="伪随机数"></a>伪随机数</h2><p>真正意义上的随机数（或者随机事件）在某次产生过程中是按照实验过程中表现的分布概率随机产生的，其结果是不可预测的，是不可见的。而计算机中的随机函数是按照一定算法模拟产生的，其结果是确定的，是可见的。我们可以这样认为这个可预见的结果其出现的概率是100%。所以用计算机随机函数所产生的“随机数”并不随机，是伪随机数。</p><p><strong>伪随机数其实是有规律的。只不过这个规律周期比较长，但还是可以预测的</strong>。主要原因就是伪随机数是计算机使用算法模拟出来的，这个过程并不涉及到物理过程，所以自然不可能具有真随机数的特性。</p><p><img data-src="/transblog/images/426334411268675.png" alt="伪随机数图像"></p><h2 id="使用程序产生随机数"><a href="#使用程序产生随机数" class="headerlink" title="使用程序产生随机数"></a>使用程序产生随机数</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> random = <span class="built_in">rand</span>();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,random);</span><br><span class="line">    random = <span class="built_in">rand</span>();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,random);</span><br><span class="line">    random = <span class="built_in">rand</span>();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>,random);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行后貌似是一串随机数。可是，当我们多次执行时，发现它的数值却还是和之前一样，所以需要设置种子。</p><h3 id="使用time作为种子"><a href="#使用time作为种子" class="headerlink" title="使用time作为种子"></a>使用time作为种子</h3><p>最理想的就是时间了。时间每分每秒都在变动，正好符合我们的要求。<br><strong>srand函数必须放在循环或者循环调用的外面，否则还是会得出重复的数字。</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">#include &lt;stdio.h&gt;</span><br><span class="line">#include &lt;time.h&gt;</span><br><span class="line">#include &lt;stdlib.h&gt;</span><br><span class="line"> </span><br><span class="line">int main ()</span><br><span class="line">&#123;</span><br><span class="line">srand ((unsigned)time(NULL));</span><br><span class="line">int random = rand();</span><br><span class="line">printf(&quot;%d\n&quot;,random);</span><br><span class="line">random = rand();</span><br><span class="line">printf(&quot;%d\n&quot;,random);</span><br><span class="line">random = rand();</span><br><span class="line">printf(&quot;%d\n&quot;,random);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我想让产生的随机数在1-100范围内，用int random &#x3D; rand（）%100，这样行吗？答案是不行！！这种只适用范围较小的随机数</p><p>结果是在范围内的。但是你如果短时间内连续执行，会发现它是有规律可循的，会随着时间的推移慢慢上涨，到100后再回到0，再重新上涨，</p><h3 id="使用n-rand-RAND-MAX-1-0-作为种子"><a href="#使用n-rand-RAND-MAX-1-0-作为种子" class="headerlink" title="使用n*rand()&#x2F;(RAND_MAX+1.0)作为种子"></a>使用n*rand()&#x2F;(RAND_MAX+1.0)作为种子</h3><p>一种通用的做法是</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">（<span class="type">int</span>）(n*<span class="built_in">rand</span>()/(RAND_MAX<span class="number">+1.0</span>))</span><br></pre></td></tr></table></figure><p>这样产生随机数的周期会大大缩短</p><h3 id="使用chrono作为种子"><a href="#使用chrono作为种子" class="headerlink" title="使用chrono作为种子"></a>使用chrono作为种子</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">std::chrono::system_clock::<span class="built_in">now</span>().<span class="built_in">time_since_epoch</span>().<span class="built_in">count</span>()</span><br><span class="line">std::chrono::steady_clock::<span class="built_in">now</span>().<span class="built_in">time_since_epoch</span>().<span class="built_in">count</span>() </span><br></pre></td></tr></table></figure><p>这两个作为种子是更好的替代，它们都会返回精度很高的时间（单位为 long long），精度可以达到纳秒，即使连续重复调用多次也会得到不一样的返回值，它们具体的含义和差别可以在网上找到，对于算法竞赛来说用哪个都无所谓，使用时需要头文件&lt;chrono&gt;</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span>  </span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span>  </span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;random&gt;</span>  </span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// obtain a seed from the system clock:  </span></span><br><span class="line">    <span class="type">unsigned</span> seed = std::chrono::system_clock::<span class="built_in">now</span>().<span class="built_in">time_since_epoch</span>().<span class="built_in">count</span>();</span><br><span class="line"> </span><br><span class="line">    <span class="function">std::mt19937 <span class="title">generator</span><span class="params">(seed)</span></span>;  <span class="comment">// mt19937 is a standard mersenne_twister_engine  </span></span><br><span class="line">    <span class="keyword">auto</span> number = <span class="built_in">generator</span>();<span class="comment">//生成一个随机数</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Random value: &quot;</span> &lt;&lt; number &lt;&lt; std::endl;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="random-device-Linux下真随机"><a href="#random-device-Linux下真随机" class="headerlink" title="random_device(Linux下真随机)"></a>random_device(Linux下真随机)</h3><p>std::random_device 是一种随机数生成器类型，如果在 Linux 下调用会通过系统提供的一个系统噪音收集器来获取真随机数，在 Windows 下就会利用很多数据来计算熵，可以近似认为是真随机的<br>std::random_device 在头文件 &lt;random&gt; 中，调用时其会返回一个 unsigned int，值域就是 unsigned int 的值域范围<br><strong>由于是真随机数，效率问题是很明显的（在 Linux 下如果系统没有足够的噪声会造成阻塞，等待系统收集到足够的噪声后才会继续运行），我们通常只用其生成一个随机数作为种子</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;random&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">signed</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">random_device rand;</span><br><span class="line">   cout &lt;&lt; <span class="built_in">rand</span>() &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="mt19937"><a href="#mt19937" class="headerlink" title="mt19937"></a>mt19937</h3><p>这个奇怪的名字来源于这个生成随机数的算法 Mersenne twister（梅森旋转算法），至于其中的19937则是代表它生成的随机数的周期可以到达2^19937-1的惊人数量级，相比起 rand() 确实可以避免很多问题，而且 mt19937 几乎是最快的伪随机数生成器，效率非常高。有趣的是与很多人的直觉不同，这个算法其实是由两位日本人发明的，其中使用到了梅森质数，所以这个算法才有了这个名字</p><ul><li>std::mt19937 是一个随机数生成器类型，在头文件 &lt;random&gt; 中，调用时其会返回一个 unsigned int，值域是 unsigned int 的值域范围</li><li>如果需要更大的值域范围可以使用 std::mt19937_64，唯一的区别就是返回值变成了一个 unsigned long long，值域也就是 unsigned long long 的值域</li></ul><p>定义时注意需要传入一个数作为种子（这里以 std::random_device 为例）</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;random&gt;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * std::random_device rd;</span></span><br><span class="line"><span class="comment"> * std::mt19937 Rnd(rd());</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">    <span class="function">std::mt19937 <span class="title">Rnd</span><span class="params">( std::random_device &#123;&#125; () )</span></span>; <span class="comment">/* 这样写等价于上面注释掉的写法，种子为 random_device */</span></span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">Rnd</span>() &lt;&lt; std::endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="default-random-engine"><a href="#default-random-engine" class="headerlink" title="default_random_engine"></a>default_random_engine</h3><p>一个随机化的前置引擎，这个需要提供时间种子，给后面要用到的函数生成一个随机节点（并没有什么卵用，就是让后面的函数随机化更强）</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;random&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">signed</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"><span class="function">default_random_engine <span class="title">rand</span><span class="params">(time(<span class="literal">NULL</span>))</span></span>;</span><br><span class="line">   cout &lt;&lt; <span class="built_in">rand</span>() &lt;&lt; endl;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="uniform-int-distribution"><a href="#uniform-int-distribution" class="headerlink" title="uniform_int_distribution"></a>uniform_int_distribution</h3><p>该函数的作用是生成一个[a,b]范围内的整数，定义的时候传进去相应的参数（数据范围即可）</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">uniform_int_distribution&lt;<span class="type">int</span>&gt; <span class="title">rand1</span><span class="params">(<span class="number">-100</span>, <span class="number">100</span>)</span></span>;</span><br><span class="line">cout &lt;&lt; <span class="built_in">rand1</span>(rand) &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br></pre></td></tr></table></figure><h3 id="uniform-real-distribution"><a href="#uniform-real-distribution" class="headerlink" title="uniform_real_distribution"></a>uniform_real_distribution</h3><p>实数域的随机生成器</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">uniform_real_distribution&lt;<span class="type">double</span>&gt; <span class="title">rand2</span><span class="params">(<span class="number">0.0</span>, <span class="number">1.0</span>)</span></span>;</span><br><span class="line">cout &lt;&lt; <span class="built_in">rand2</span>(rand) &lt;&lt; endl;</span><br></pre></td></tr></table></figure><h3 id="normal-distribution"><a href="#normal-distribution" class="headerlink" title="normal_distribution"></a>normal_distribution</h3><p>正态分布，使用方法，第一个参数是μ，第二个是σ</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">normal_distribution&lt;<span class="type">double</span>&gt; <span class="title">N</span><span class="params">(<span class="number">10.0</span>, <span class="number">5.0</span>)</span></span>;</span><br><span class="line"><span class="comment">//为了方便直观的看出数据分布，把每次生成的数据出现次数+1，测试的时候输出了数据分布图像</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">register</span> <span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10000</span>; i++)&#123;</span><br><span class="line">      <span class="type">double</span> num = <span class="built_in">nor</span>(rand);</span><br><span class="line">      <span class="keyword">if</span> ((num &gt;= <span class="number">0.0</span>) &amp;&amp; (num &lt; <span class="number">20.0</span>)) ++p[<span class="built_in">int</span>(num)];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; ++i) &#123;</span><br><span class="line">    cout &lt;&lt; i &lt;&lt; <span class="string">&quot;-&quot;</span> &lt;&lt; (i + <span class="number">1</span>) &lt;&lt; <span class="string">&quot;: &quot;</span>;</span><br><span class="line">    cout &lt;&lt; <span class="built_in">string</span>(p[i] * <span class="number">100</span> / <span class="number">10000</span>, <span class="string">&#x27;*&#x27;</span>) &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><table><thead><tr><th align="center">工具名称</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">std::random_device</td><td align="center">一个真正的随机数生成器，通常用于为其他随机数引擎提供种子。</td></tr><tr><td align="center">std::mt19937</td><td align="center">一个基于Mersenne Twister算法的伪随机数生成器。</td></tr><tr><td align="center">std::default_random_engine</td><td align="center">默认引擎，快速生成随机数</td></tr><tr><td align="center">std::uniform_int_distribution</td><td align="center">用于生成均匀分布的整数随机数。</td></tr><tr><td align="center">std::uniform_real_distribution</td><td align="center">用于生成均匀分布的实数随机数。</td></tr><tr><td align="center">std::normal_distribution</td><td align="center">用于生成正态分布的浮点随机数。</td></tr></tbody></table><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;抢到红包0-01元，怎么搞的？&quot;&gt;&lt;a href=&quot;#抢到红包0-01元，怎么搞的？&quot; class=&quot;headerlink&quot; title=&quot;抢到红包0.01元，怎么搞的？&quot;&gt;&lt;/a&gt;抢到红包0.01元，怎么搞的？&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;抢红包时你为什么只抢</summary>
      
    
    
    
    <category term="奇闻轶事" scheme="https://trans-xu.github.io/transblog/categories/%E5%A5%87%E9%97%BB%E8%BD%B6%E4%BA%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>系统设计</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/</id>
    <published>2025-08-27T03:50:08.918Z</published>
    <updated>2025-08-27T03:38:26.741Z</updated>
    
    <content type="html"><![CDATA[<h1 id="分布式系统"><a href="#分布式系统" class="headerlink" title="分布式系统"></a>分布式系统</h1><h2 id="CAP"><a href="#CAP" class="headerlink" title="CAP"></a>CAP</h2><p>分布式系统不可能同时满足一致性（C：Consistency）、可用性（A：Availability）和分区容忍性（P：Partition Tolerance），最多只能同时满足其中两项。</p><h3 id="一致性"><a href="#一致性" class="headerlink" title="一致性"></a>一致性</h3><p>一致性指的是多个数据副本是否能保持一致的特性，在一致性的条件下，系统在执行数据更新操作之后能够从一致性状态转移到另一个一致性状态。</p><ul><li>最终一致性：这个是弱一致性的一种，不保证在任意时刻同一份数据在所有节点上都是一致的，但是在一段时间之后时间会最终一致。对于我们互联网的应用来说大多数是采用这种策略。</li><li>强一致性：在任意时刻同一份数据在所有节点上都是一致的。对于银行、金融行业来说基本采用这种策略。</li></ul><h3 id="可用性"><a href="#可用性" class="headerlink" title="可用性"></a>可用性</h3><p>可用性指分布式系统在面对各种异常时可以提供正常服务的能力，可以用系统可用时间占总时间的比值来衡量，4 个 9 的可用性表示系统 99.99% 的时间是可用的。</p><p>在可用性条件下，要求系统提供的服务一直处于可用的状态，对于用户的每一个操作请求总是能够在有限的时间内返回结果。</p><h3 id="分区容忍性"><a href="#分区容忍性" class="headerlink" title="分区容忍性"></a>分区容忍性</h3><p>网络分区指分布式系统中的节点被划分为多个区域，每个区域内部可以通信，但是区域之间无法通信。</p><p>在分区容忍性条件下，分布式系统在遇到任何网络分区故障的时候，仍然需要能对外提供一致性和可用性的服务，除非是整个网络环境都发生了故障。</p><h3 id="权衡"><a href="#权衡" class="headerlink" title="权衡"></a>权衡</h3><p>在分布式系统中，分区容忍性必不可少，因为需要总是假设网络是不可靠的。因此，CAP 理论实际上是要在可用性和一致性之间做权衡。</p><p>可用性和一致性往往是冲突的，很难使它们同时满足。在多个节点之间进行数据同步时，</p><p>为了保证一致性（CP），不能访问未同步完成的节点，也就失去了部分可用性；<br>为了保证可用性（AP），允许读取所有节点的数据，但是数据可能不一致。</p><h2 id="BASE"><a href="#BASE" class="headerlink" title="BASE"></a>BASE</h2><p>BASE 是基本可用（Basically Available）、软状态（Soft State）和最终一致性（Eventually Consistent）三个短语的缩写。</p><p>BASE 理论是对 CAP 中一致性和可用性权衡的结果，它的核心思想是：即使无法做到强一致性，但每个应用都可以根据自身业务特点，采用适当的方式来使系统达到最终一致性。</p><h3 id="基本可用"><a href="#基本可用" class="headerlink" title="基本可用"></a>基本可用</h3><p>指分布式系统在出现故障的时候，保证核心可用，允许损失部分可用性。</p><p>例如，电商在做促销时，为了保证购物系统的稳定性，部分消费者可能会被引导到一个降级的页面。</p><h3 id="软状态"><a href="#软状态" class="headerlink" title="软状态"></a>软状态</h3><p>指允许系统中的数据存在中间状态，并认为该中间状态不会影响系统整体可用性，即允许系统不同节点的数据副本之间进行同步的过程存在时延。</p><h3 id="最终一致性"><a href="#最终一致性" class="headerlink" title="最终一致性"></a>最终一致性</h3><p>最终一致性强调的是系统中所有的数据副本，在经过一段时间的同步后，最终能达到一致的状态。</p><p>ACID 要求强一致性，通常运用在传统的数据库系统上。而 BASE 要求最终一致性，通过牺牲强一致性来达到可用性，通常运用在大型分布式系统中。<br>在实际的分布式场景中，不同业务单元和组件对一致性的要求是不同的，因此 ACID 和 BASE 往往会结合在一起使用。</p><h2 id="分布式锁的场景"><a href="#分布式锁的场景" class="headerlink" title="分布式锁的场景"></a>分布式锁的场景</h2><p>在单机场景下，可以使用语言的内置锁来实现进程同步。但是在分布式场景下，需要同步的进程可能位于不同的节点上，那么就需要使用分布式锁。</p><p>阻塞锁通常使用互斥量来实现：<br>互斥量为 0 表示有其它进程在使用锁，此时处于锁定状态；<br>互斥量为 1 表示未锁定状态。<br>1 和 0 可以用一个整型值表示，也可以用某个数据是否存在表示。</p><h3 id="数据库的唯一索引"><a href="#数据库的唯一索引" class="headerlink" title="数据库的唯一索引"></a>数据库的唯一索引</h3><p>获得锁时向表中插入一条记录，释放锁时删除这条记录。唯一索引可以保证该记录只被插入一次，那么就可以用这个记录是否存在来判断是否处于锁定状态。</p><p>存在以下几个问题：</p><ul><li>锁没有失效时间，解锁失败的话其它进程无法再获得该锁；</li><li>只能是非阻塞锁，插入失败直接就报错了，无法重试；</li><li>不可重入，已经获得锁的进程也必须重新获取锁。</li></ul><h3 id="Redis-的-SETNX-指令"><a href="#Redis-的-SETNX-指令" class="headerlink" title="Redis 的 SETNX 指令"></a>Redis 的 SETNX 指令</h3><ul><li><p>使用 SETNX（set if not exist）指令插入一个键值对，如果 Key 已经存在，那么会返回 False，否则插入成功并返回 True。</p></li><li><p>SETNX 指令和数据库的唯一索引类似，保证了只存在一个 Key 的键值对，那么可以用一个 Key 的键值对是否存在来判断是否存于锁定状态。</p></li><li><p>EXPIRE 指令可以为一个键值对设置一个过期时间，从而避免了数据库唯一索引实现方式中释放锁失败的问题。</p></li></ul><h3 id="Zookeeper-的有序节点"><a href="#Zookeeper-的有序节点" class="headerlink" title="Zookeeper 的有序节点"></a>Zookeeper 的有序节点</h3><ol><li>Zookeeper 抽象模型<br>Zookeeper 提供了一种树形结构的命名空间，&#x2F;app1&#x2F;p_1 节点的父节点为 &#x2F;app1。</li></ol><p><img data-src="/transblog/images/2064125869957.png" alt="·"></p><ol start="2"><li>节点类型<br>永久节点：不会因为会话结束或者超时而消失；<br>临时节点：如果会话结束或者超时就会消失；<br>有序节点：会在节点名的后面加一个数字后缀，并且是有序的.</li><li>监听器<br>为一个节点注册监听器，在节点状态发生改变时，会给客户端发送消息。</li><li>分布式锁实现</li></ol><ul><li><p>创建一个锁目录 &#x2F;lock,当一个客户端需要获取锁时，在 &#x2F;lock 下创建临时的且有序的子节点；</p></li><li><p>客户端获取 &#x2F;lock 下的子节点列表，判断自己创建的子节点是否为当前子节点列表中序号最小的子节点，如果是则认为获得锁；否则监听自己的前一个子节点，获得子节点的变更通知后重复此步骤直至获得锁；</p></li><li><p>执行业务代码，完成后，删除对应的子节点。</p></li></ul><ol start="5"><li><p>会话超时<br>如果一个已经获得锁的会话超时了，因为创建的是临时节点，所以该会话对应的临时节点会被删除，其它会话就可以获得锁了。可以看到，这种实现方式不会出现数据库的唯一索引实现方式释放锁失败的问题。</p></li><li><p>羊群效应<br>一个节点未获得锁，只需要监听自己的前一个子节点，这是因为如果监听所有的子节点，那么任意一个子节点状态改变，其它所有子节点都会收到通知（羊群效应，一只羊动起来，其它羊也会一哄而上），而我们只希望它的后一个子节点收到通知。<em>详情参考ZooKeeper–羊群效应</em></p></li></ol><p>【转载】Zookeeper和Redis实现分布式锁的可靠性分析](<a href="https://zhuanlan.zhihu.com/p/517619768">https://zhuanlan.zhihu.com/p/517619768</a>)</p><h2 id="分布式事务"><a href="#分布式事务" class="headerlink" title="分布式事务"></a>分布式事务</h2><p>指事务的操作位于不同的节点上，需要保证事务的 ACID 特性。</p><p>分布式锁和分布式事务区别：</p><ul><li><strong>锁问题的关键在于进程操作的互斥关系</strong>，例如多个进程同时修改账户的余额，如果没有互斥关系则会导致该账户的余额不正确。</li><li><strong>而事务问题的关键则在于事务涉及的一系列操作需要满足 ACID 特性</strong>，例如要满足原子性操作则需要这些操作要么都执行，要么都不执行。</li></ul><h2 id="分布式一致性协议"><a href="#分布式一致性协议" class="headerlink" title="分布式一致性协议"></a>分布式一致性协议</h2><h3 id="二阶段提交协议（2PC）"><a href="#二阶段提交协议（2PC）" class="headerlink" title="二阶段提交协议（2PC）"></a>二阶段提交协议（2PC）</h3><p>两阶段提交，通过引入协调者（Coordinator）来协调参与者的行为，并最终决定这些参与者是否要真正执行事务。</p><ol><li>准备阶段<br>协调者询问参与者事务是否执行成功，参与者发回事务执行结果。询问可以看成一种投票，需要参与者都同意才能执行。</li></ol><p><img data-src="/transblog/images/1800919756710.png"></p><ol start="2"><li>提交阶段<br>如果事务在每个参与者上都执行成功，事务协调者发送通知让参与者提交事务；否则，协调者发送通知让参与者回滚事务。<br>需要注意的是，在准备阶段，参与者执行了事务，但是还未提交。只有在提交阶段接收到协调者发来的通知后，才进行提交或者回滚。</li></ol><p><img data-src="/transblog/images/1185629545802.png"></p><h3 id="Paxos"><a href="#Paxos" class="headerlink" title="Paxos"></a>Paxos</h3><p>用于达成共识性问题，即对多个节点产生的值，该算法能保证只选出唯一一个值。<br>主要有三类节点：<br>提议者（Proposer）：提议一个值；<br>接受者（Acceptor）：对每个提议进行投票；<br>告知者（Learner）：被告知投票的结果，不参与投票过程。</p><p><img data-src="/transblog/images/5367027659123.png"></p><h3 id="ZAB"><a href="#ZAB" class="headerlink" title="ZAB"></a>ZAB</h3><p>Zookeeper 原子广播协议；ZAB 协议是为分布式协调服务 Zookeeper 专门设计的一种支持<strong>崩溃恢复</strong>和<strong>原子广播</strong>协议。</p><p>当 Leader 服务可以正常使用，就进入消息广播模式，当 Leader 不可用时，则进入崩溃恢复模式。基于该协议，Zookeeper 实现了一种 主备模式 的系统架构来保持集群中各个副本之间数据一致性。其中所有客户端写入数据都是写入到 主进程（称为 Leader）中，然后，由 Leader 复制到备份进程（称为 Follower）中。</p><ul><li><p>选举权重：<br>Serverid：服务器ID。比如有三台服务器，编号分别是1,2,3。编号越大在选择算法中的权重越大。<br>Zxid：数据ID。服务器中存放的最大数据ID，值越大说明数据越新，在选举算法中数据越新权重越大。</p></li><li><p>节点角色：<br>Leader：Leader 负责处理客户端请求和状态变更，<br>Follower：Follower 则从 Leader 处同步状态</p></li><li><p>节点状态：<br>LOOKING，竞选状态。<br>FOLLOWING，随从状态，同步leader状态，参与投票。<br>OBSERVING，观察状态，同步leader状态，不参与投票。<br>LEADING，领导者状态。</p></li><li><p>选举过程：</p></li></ul><ol><li>当一个节点启动时，它首先会进入 LOOKING 状态，表示它正在寻找 Leader。</li><li>在 LOOKING 状态下，节点会向其他节点发送消息，请求它们投票支持自己成为 Leader。</li><li>如果收到了超过半数的选票，那么该节点就会成为 Leader，并将状态切换为 LEADING。</li><li>如果一个节点在一定时间内没有收到足够多的投票或者发生了网络分区等情况，它会重新进入 LOOKING 状态，重新发起选举。</li><li>在选举过程中，如果某个节点成为 Leader，那么其他节点会将自己的状态切换为 FOLLOWING，并开始与 Leader 同步数据。</li></ol><p><strong>Paxos算法用于构建一个分布式的一致性状态机系统，ZAB算法用于构建一个高可用的分布式数据主备系统</strong></p><h3 id="Raft"><a href="#Raft" class="headerlink" title="Raft"></a>Raft</h3><h4 id="单个-Candidate-的竞选"><a href="#单个-Candidate-的竞选" class="headerlink" title="单个 Candidate 的竞选"></a>单个 Candidate 的竞选</h4><ul><li><p>有三种节点：Follower、Candidate 和 Leader。Leader 会周期性的发送心跳包给 Follower。每个 Follower 都设置了一个随机的竞选超时时间，一般为 150ms~300ms，如果在这个时间内没有收到 Leader 的心跳包，就会变成 Candidate，进入竞选阶段。</p></li><li><p>一个分布式系统的最初阶段，此时只有 Follower 没有 Leader。Node A 等待一个随机的竞选超时时间之后，没收到 Leader 发来的心跳包，因此进入竞选阶段。</p></li><li><p>此时 Node A 发送投票请求给其它所有节点。</p></li><li><p>其它节点会对请求进行回复，如果超过一半的节点回复了，那么该 Candidate 就会变成 Leader。</p></li><li><p>之后 Leader 会周期性地发送心跳包给 Follower，Follower 接收到心跳包，会重新开始计时。</p></li></ul><h4 id="多个-Candidate-竞选"><a href="#多个-Candidate-竞选" class="headerlink" title="多个 Candidate 竞选"></a>多个 Candidate 竞选</h4><ul><li><p>如果有多个 Follower 成为 Candidate，并且所获得票数相同，那么就需要重新开始投票。</p></li><li><p>由于每个节点设置的随机竞选超时时间不同，因此下一次再次出现多个 Candidate 并获得同样票数的概率很低。</p></li></ul><h4 id="数据同步"><a href="#数据同步" class="headerlink" title="数据同步"></a>数据同步</h4><ul><li><p>来自客户端的修改都会被传入 Leader。注意该修改还未被提交，只是写入日志中。</p></li><li><p>Leader 会把修改复制到所有 Follower。</p></li><li><p>Leader 会等待大多数的 Follower 也进行了修改，然后才将修改提交。</p></li><li><p>此时 Leader 会通知的所有 Follower 让它们也提交修改，此时所有节点的值达成一致。</p></li></ul><h2 id="一致性哈希"><a href="#一致性哈希" class="headerlink" title="一致性哈希"></a>一致性哈希</h2><h3 id="解决了什么问题"><a href="#解决了什么问题" class="headerlink" title="解决了什么问题"></a>解决了什么问题</h3><p>当我们想提高系统的容量，就会<strong>将数据水平切分到不同的节点来存储</strong>，也就是将数据分布到了不同的节点。<strong>比如一个分布式 KV（key-valu） 缓存系统</strong>，某个 key 应该到哪个或者哪些节点上获得，应该是确定的，<strong>不是说任意访问一个节点都可以得到缓存结果的</strong>。</p><p>第一想到的Hash算法，但是该算法有一个很致命的问题，<strong>如果节点数量发生了变化，也就是在对系统做扩容或者缩容时，必须迁移改变了映射关系的数据，否则会出现查询不到数据的问题</strong>。</p><p>扩容前：</p><p><img data-src="/transblog/images/429950210240850.png"></p><p>扩容后：</p><p><img data-src="/transblog/images/89930310259276.png"></p><p><strong>一致性哈希算法就很好地解决了分布式系统在扩容或者缩容时，发生过多的数据迁移的问题。</strong></p><h3 id="Hash原理"><a href="#Hash原理" class="headerlink" title="Hash原理"></a>Hash原理</h3><p>一致哈希算法也用了取模运算，但与哈希算法不同的是，哈希算法是对节点的数量进行取模运算，而一致哈希算法是对 2^32 进行取模运算，是一个固定的值。</p><p>我们可以把一致哈希算法是对 2^32 进行取模运算的结果值组织成一个圆环，就像钟表一样，钟表的圆可以理解成由 60 个点组成的圆，而此处我们把这个圆想象成由 2^32 个点组成的圆，这个圆环被称为哈希环，如下图：</p><p><img data-src="/transblog/images/126134815240848.png"></p><p>一致性哈希要进行两步哈希：</p><p>第一步：对存储节点进行哈希计算，也就是对存储节点做哈希映射，比如根据节点的 IP 地址进行哈希；<br>第二步：当对数据进行存储或访问时，对数据进行哈希映射；<br>所以，一致性哈希是指将「存储节点」和「数据」都映射到一个首尾相连的哈希环上。</p><h3 id="如何找到节点"><a href="#如何找到节点" class="headerlink" title="如何找到节点"></a>如何找到节点</h3><p>对「数据」进行哈希映射得到一个结果要怎么找到存储该数据的节点呢？映射的结果值往顺时针的方向的找到第一个节点，就是存储该数据的节点：</p><p><img data-src="/transblog/images/478910510247143.png"></p><p>例如：对要查询的 key-01 进行哈希计算，确定此 key-01 映射在哈希环的位置，然后从这个位置往顺时针的方向找到第一个节点，就是存储该 key-01 数据的节点。往顺时针的方向找到第一个节点就是节点 A。</p><h3 id="数据迁移"><a href="#数据迁移" class="headerlink" title="数据迁移"></a>数据迁移</h3><p>如果增加一个节点或者减少一个节点会发生大量的数据迁移吗？<br>假设节点数量从 3 增加到了 4，新的节点 D 经过哈希计算后映射到了下图中的位置：key-01、key-03 都不受影响，只有 key-02 需要被迁移节点 D。</p><p><img data-src="/transblog/images/102271010267309.png"></p><p>假设节点数量从 3 减少到了 2，比如将节点 A 移除：key-02 和 key-03 不会受到影响，只有 key-01 需要被迁移节点 B。</p><p><img data-src="/transblog/images/67451110259978.png"></p><h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><p>但是<strong>一致性哈希算法并不保证节点能够在哈希环上分布均匀</strong>，这样就会带来一个问题，会有大量的请求集中在一个节点上。<br>比如，下图中 3 个节点的映射位置都在哈希环的右半边：</p><p><img data-src="/transblog/images/417221210256533.png"></p><p>那么当A节点移除，所有的数据都需要迁移到B节点，显然是有问题的</p><h3 id="如何解决"><a href="#如何解决" class="headerlink" title="如何解决"></a>如何解决</h3><p>使用虚拟节点提高均衡度，<strong>不再将真实节点映射到哈希环上，而是将虚拟节点映射到哈希环上，并将虚拟节点映射到实际节点，所以这里有「两层」映射关系。</strong></p><p><img data-src="/transblog/images/46501610252287.png"></p><p>如果有访问请求寻址到「A-01」这个虚拟节点，接着再通过「A-01」虚拟节点找到真实节点 A，这样请求就能访问到真实节点 A 了。</p><ul><li>虚拟节点数量越多，起到的均衡效果越好：比如 Nginx 的一致性哈希算法，每个权重为 1 的真实节点就含有160 个虚拟节点。</li><li>另外，虚拟节点除了会提高节点的均衡度，还会提高系统的稳定性。当节点变化时，会有不同的节点共同分担系统的变化，因此稳定性更高。比如，当某个节点被移除时，对应该节点的多个虚拟节点均会移除，而这些虚拟节点按顺时针方向的下一个虚拟节点，可能会对应不同的真实节点，即这些不同的真实节点共同分担了节点变化导致的压力。</li><li>而且，有了虚拟节点后，还可以为硬件配置更好的节点增加权重，比如对权重更高的节点增加更多的虚拟机节点即可。</li><li>因此，带虚拟节点的一致性哈希方法不仅适合硬件配置不同的节点的场景，而且适合节点规模会发生变化的场景。</li></ul><h1 id="高可用系统设计"><a href="#高可用系统设计" class="headerlink" title="高可用系统设计"></a>高可用系统设计</h1><h2 id="高可用系统设计要点"><a href="#高可用系统设计要点" class="headerlink" title="高可用系统设计要点"></a>高可用系统设计要点</h2><h3 id="冗余"><a href="#冗余" class="headerlink" title="冗余"></a>冗余</h3><p>单点是系统高可用最大的风险和敌人，应该尽量在系统设计的过程中避免单点。</p><p>保证高可用的主要手段是使用冗余，或者叫“集群化”，当某个服务器故障时就请求其它服务器。</p><ul><li>应用服务器的冗余比较容易实现，只要保证应用服务器不具有状态，那么某个应用服务器故障时，负载均衡器将该应用服务器原先的用户请求转发到另一个应用服务器上，不会对用户有任何影响。</li><li>存储服务器的冗余需要使用主从复制来实现，当主服务器故障时，需要提升从服务器为主服务器，这个过程称为切换。</li></ul><p>有了冗余之后还不够，每次出现故障需要人工介入恢复势必会增加系统的不可服务时间。所以，往往是通过“自动故障转移”来实现系统的高可用。</p><h3 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h3><p>扩展是最常见的提升系统可靠性的方法，系统的扩展可以避免单点故障。一个容易扩展的系统，能够通过扩展来成倍的提升系统能力，轻松应对系统访问量的提升。</p><h4 id="垂直扩展"><a href="#垂直扩展" class="headerlink" title="垂直扩展"></a>垂直扩展</h4><p>比如，当机器内存不够时，我们可以帮机器增加内存,垂直扩展能够提升系统处理能力，但不能解决单点故障问题。我</p><h4 id="水平扩展"><a href="#水平扩展" class="headerlink" title="水平扩展"></a>水平扩展</h4><p>通过增加一个或多个逻辑单元，并使得它们像整体一样的工作。水平扩展，通过冗余部署解决了单点故障，同时又提升了系统处理能力。</p><p>在实际应用中，水平扩展最常见：</p><ol><li>通常我们在部署应用服务器的时候，都会部署多台，然后使用 nginx 来做负载均衡，nginx 使用心跳机制来检测服务器的正常与否，无响应的服务就从集群中剔除。这样的集群中每台服务器的角色是相同的，同时提供一样的服务。</li><li>在数据库的部署中，为了防止单点故障，一般会使用一主多从，通常写操作只发生在主库。不同数据库之间角色不同。当主机宕机时，一台从库可以自动切换为主机提供服务。</li></ol><h3 id="解耦"><a href="#解耦" class="headerlink" title="解耦"></a>解耦</h3><p>在软件工程中，对象之间的耦合度就是对象之间的依赖性。对象之间的耦合越高，维护成本越高，因此对象的设计应使模块之间的耦合度尽量小。在软件架构设计中，模块之间的解耦或者说松耦合有两种，</p><p><mark>1. 使用分布式服务将业务和可复用的服务分离开来，业务使用分布式服务框架调用可复用的服务。</mark><br>新增的产品可以通过调用可复用的服务来实现业务逻辑，对其它产品没有影响。<br><mark>2. 将同步调用转换成异步消息交互。使用消息队列进行解耦，应用之间通过消息传递进行通信.</mark><br>如果我们将同步调用替换成异步消息，机票支付系统发送机票支付成功的消息到消息中间件，出票系统、代金券系统从消息中间件订阅消息。这样一来，出票系统、代金券系统的宕机也就不会对机票支付系统造成任何影响了。</p><p>异步消息解耦，适合那些信息流单向流动（类似发布-订阅这样的），实时性要求不高的系统。常见的开源消息队列框架有：Kafka、RabbitMQ、RocketMQ。</p><h3 id="请求幂等"><a href="#请求幂等" class="headerlink" title="请求幂等"></a>请求幂等</h3><p>幂等性：就是用户对于同一操作发起的一次请求或者多次请求的结果是一致的，不会因为多次点击而产生了副作用<br>用户购买商品后支付，支付扣款成功，但是返回结果的时候网络异常，此时钱已经扣了，用户再次点击按钮，此时会进行第二次扣款，返回结果成功，用户查询余额发现多扣钱了，流水记录也变成了两条。</p><p>一般保证幂等的判断是从数据库查询有没有相同id的记录，但是在分布式系统环境下，可能有主从问题：request1请求过来的时候，查询从库发现没有对应记录，则request1开始操作插入主库record1，但是还没有同步到从库；此时request2查询从库也发现没有相同id的记录，准备插入有相同id的记录record2，这个时候request1成功插入record1，request2开始插入record2,数据库报错.</p><p>解决这个问题有两种方法：1、读写都强制走主库；2、采用分布式锁，考虑性能问题，一般都选2</p><h3 id="分布式锁"><a href="#分布式锁" class="headerlink" title="分布式锁"></a>分布式锁</h3><p>分布式系统一般都有多台机器，常规的多线程锁已经无法解决问题；最简单用redis实现：思路很简单，主要用到的redis函数是setnx()。首先是将某一任务标识名UniqueKey（能唯一识别一个请求的标识）作为键存到redis里，并为其设个过期时间，如果是同样的请求过来，先是通过setnx()看看是否能将UniqueKey插入到redis里，可以的话就返回true，不可以就返回false。</p><p>分布式锁设计原则：</p><ol><li>互斥性，同一时间只有一个线程持有锁</li><li>容错性，即使某一个持有锁的线程，异常退出，其他线程仍可获得锁</li><li>隔离性，线程只能解自己的锁，不能解其他线程的锁</li></ol><h3 id="服务隔离"><a href="#服务隔离" class="headerlink" title="服务隔离"></a>服务隔离</h3><p>是对系统、业务所占有的资源进行隔离，避免一个业务占用整个系统资源，对其他业务造成影响,即发生故障后不会出现滚雪球效应，从而保证只有出问题的服务不可用，其他服务还是可用的。</p><ol><li><strong>线程池隔离：</strong> 不同的业务使用不同的线程池，避免低优先级的任务阻塞高优先级的任务。或者高优先级的任务过多，导致低优先级任务永远不会执行。</li><li><strong>进程隔离：</strong> Linux 中有用于进程资源隔离的 Linux CGroup，通过物理限制的方式为进程间资源控制提供了简单的实现方式，为 Linux Container 技术、虚拟化技术的发展奠定了技术基础。</li><li><strong>模块隔离、应用隔离：</strong> 很多线上故障的发生源于代码修改后，测试不到位导致。按照代码或业务的易变程度来划分模块或应用，把变化较少的划分到一个模块或应用中，变化较多的划分到另一个模块或应用中。减少代码修改影响的范围，也就减少了测试的工作量，减少了故障出现的概率。</li><li><strong>机房隔离：</strong> 主要是为了避免单个机房网络问题或断电。</li><li><strong>读写分离：</strong> 一方面，将对实时性要求不高的读操作，放到 DB 从库上执行，有利于减轻 DB 主库的压力。另一方面，将一些耗时离线业务 sql 放到 DB 从库上执行，能够减少慢 sql 对 DB 主库的影响，保证线上业务的稳定可靠。</li></ol><h3 id="异步调用"><a href="#异步调用" class="headerlink" title="异步调用"></a>异步调用</h3><p>这种方式在服务端平均处理请求时间过长的业务场景下很好用，不需要关心最后的结果，用户请求完成之后就立即返回结果，具体处理可以后续再做。<br>除了可以在程序中实现异步之外，常常还使用消息队列，消息队列可以通过异步处理提高系统性能（削峰、减少响应所需时间）并且可以降低系统耦合性。</p><h3 id="可伸缩性（有-无状态的服务）"><a href="#可伸缩性（有-无状态的服务）" class="headerlink" title="可伸缩性（有&#x2F;无状态的服务）"></a>可伸缩性（有&#x2F;无状态的服务）</h3><p>指不断向集群中添加服务器来缓解不断上升的用户并发访问压力和不断增长的数据存储需求。</p><ul><li><p>伸缩性与性能<br>如果系统存在性能问题，那么单个用户的请求总是很慢的；<br>如果系统存在伸缩性问题，那么单个用户的请求可能会很快，但是在并发数很高的情况下系统会很慢。</p></li><li><p>实现伸缩性<br>应用服务器只要不具有状态，那么就可以很容易地通过负载均衡器向集群中添加新的服务器。<br>关系型数据库的伸缩性通过 Sharding 来实现，将数据按一定的规则分布到不同的节点上，从而解决单台存储服务器的存储空间限制。<br>对于非关系型数据库，它们天生就是为海量数据而诞生，对伸缩性的支持特别好。</p></li></ul><h3 id="一致性（补偿事务、重试）"><a href="#一致性（补偿事务、重试）" class="headerlink" title="一致性（补偿事务、重试）"></a>一致性（补偿事务、重试）</h3><p>强一致性（ACID）和高可用性（BASE）是对立，顾此失彼；因此，为了可用性，我们要讲业务中需要强一致性的动作和不需要强一致性的动作剥离开，对于非强一致性需求的动作，可以做补偿事务；我们应尽量设计更多非强一致性的业务</p><h3 id="模块级自动化测试"><a href="#模块级自动化测试" class="headerlink" title="模块级自动化测试"></a>模块级自动化测试</h3><p>解决上述问题可以使用模块级自动化测试。具体方案是：针对某一模块，收集模块线上的输入、输出、运行时环境等信息，在离线测试环境通过数据mock模块线上场景，回放收集的线上输入，相同的输入比较测试场景与线上收集的输出作为测试结果。<br>模块级自动化测试通过简化复杂系统中的不变因素（mock），将系统的测试边界收拢到改动模块，将复杂系统的整体测试转化为改动模块的单元测试。主要适用于系统业务回归，对系统内部重构场景尤其适用。</p><p>具体如何收集线上数据呢？有两种方法：</p><ol><li>AOP：面向切面编程，动态地织入代码，对原有代码的侵入性较小。</li><li>埋点：很多公司都开发了一下基础组件，可以在这些基础组件中嵌入数据收集的代码。</li></ol><h3 id="灰度发布-回滚"><a href="#灰度发布-回滚" class="headerlink" title="灰度发布 &amp; 回滚"></a>灰度发布 &amp; 回滚</h3><ul><li>发布之前必须制定详细的回滚步骤，回滚是解决发布引起的故障的最快的方法。</li><li>在线上出现故障后，第一个要考虑的就是刚刚有没有代码发布、配置发布，如果有的话就先回滚。</li><li>即让一部分用户继续用产品特性A，一部分用户开始用产品特性B，如果用户对B没有什么反对意见，那么逐步扩大范围，把所有用户都迁移到B上面来。</li><li>灰度发布可以保证整体系统的稳定，在初始灰度的时候就可以发现、调整问题，以保证其影响度。</li></ul><h3 id="超时和重试机制设置"><a href="#超时和重试机制设置" class="headerlink" title="超时和重试机制设置"></a>超时和重试机制设置</h3><p>一旦用户请求超过某个时间的得不到响应，就抛出异常。<br>使用一些 RPC  框架的时候，这些框架都自带的超时重试的配置。如果不进行超时设置可能会导致请求响应速度慢，甚至导致请求堆积进而让系统无法在处理请求。重试的次数一般设为3次，再多次的重试没有好处，反而会加重服务器压力</p><h3 id="应对大流量的能力（熔断、降级、限流）"><a href="#应对大流量的能力（熔断、降级、限流）" class="headerlink" title="应对大流量的能力（熔断、降级、限流）"></a>应对大流量的能力（熔断、降级、限流）</h3><h4 id="熔断（慎用）"><a href="#熔断（慎用）" class="headerlink" title="熔断（慎用）"></a>熔断（慎用）</h4><p>如果系统中，某个目标服务调用慢或者有大量超时，此时，熔断该服务的调用，对于后续调用请求，不在继续调用目标服务，直接返回，快速释放资源。如果目标服务情况好转则恢复调用；熔断主要是应对流量引起的问题，使服务处于关闭、半关闭状态，以保证部分业务成功，或者舍弃次要业务使主业务运行通畅；</p><p>在熔断器中有三种状态：<br>关闭：让请求通过的默认状态。如果请求成功&#x2F;失败但低于阈值，则状态保持不变。<br>打开：当熔断器打开的时候，所有的请求都会被标记为失败；这是故障快速失败机制，而不需要等待超时时间完成。<br>半开：定期的尝试发起请求来确认系统是否恢复。如果恢复了，熔断器将转为关闭状态或者保持打开</p><p>下面这张图，就是熔断器的基本原理，包含三个状态：</p><ol><li>服务正常运行时的 Closed 状态，当服务调用失败量达到阈值时，熔断器进入 Open 状态</li><li>在 Open 状态，服务调用不会真正去请求外部资源，会快速失败。</li><li>当进入 Open 状态一段时间后，进入 Half-Open状态，需要去尝试调用几次服务，检查故障的服务是否恢复。如果成功则熔断器关闭，如果失败，则再次进入 Open 状态。</li></ol><p><img data-src="/transblog/images/712816119290.png"></p><h4 id="限流"><a href="#限流" class="headerlink" title="限流"></a>限流</h4><p>熔断的一种，半开状态，只允许少部分的请求，其他的都拒绝，如果设计得当，被拒绝的请求，客户端会通过重试、补偿操作来完成；</p><p>限流策略：</p><ol><li>计数器算法：<br>设置一个计数器统计单位时间内某个请求的访问量，在进入下一个单位时间内把计数器清零，对于单位时间内超过计数器的访问，可以放入等待队列、直接拒接访问等策略</li><li>漏斗算法：<br>一个固定容量的漏桶，按照常量固定速率流出水滴；可以以任意速率流入水滴到漏桶；如果流入水滴超出了桶的容量，则流入的水滴溢出了，而漏桶容量是不变的。</li><li>令牌桶算法：<br>令牌将按照固定的速率被放入令牌桶中。比如每秒放10个。每次请求调用需要先获取令牌，只有拿到令牌，才有机会继续执行，否则选择等待可用的令牌、或者直接拒绝。当令牌桶满时，新添加的令牌被丢弃或拒绝</li><li>滑动窗口计数法<br>计数法是限流算法里最容易理解的一种，该方法统计最近一段时间的请求量，如果超过一定的阈值，就开始限流。在 TCP 网络协议中，也用到了滑动窗口来限制数据传输速率。<br>滑动窗口计数有两个关键的因素：窗口时长、滚动时间间隔。滚动时间间隔一般等于上图中的一个桶 bucket，窗口时长除以滚动时间间隔，就是一个窗口所包含的 bucket 数目。</li><li>动态限流<br>一般情况下的限流，都需要我们手动设定限流阈值，不仅繁琐，而且容易因系统的发布升级而过时。为此，我们考虑根据系统负载来动态决定是否限流，动态计算限流阈值。可以参考的系统负载参数有：Load、CPU、接口响应时间等。</li></ol><p>漏桶算法与令牌桶算法的区别在于:<br><strong>漏桶算法能够强行限制数据的传输速率，令牌桶算法能够在限制数据的平均传输速率的同时还允许某种程度的突发传输</strong>。需要注意的是，在某些情况下，漏桶算法不能够有效地使用网络资源，因为漏桶的漏出速率是固定的，所以即使网络中没有发生拥塞，漏桶算法也不能使某一个单独的数据流达到端口速率。因此，漏桶算法对于存在突发特性的流量来说缺乏效率。而令牌桶算法则能够满足这些具有突发特性的流量。通常，漏桶算法与令牌桶算法结合起来为网络流量提供更高效的控制。</p><h4 id="降级"><a href="#降级" class="headerlink" title="降级"></a>降级</h4><ul><li><p>业务降级，是指牺牲非核心的业务功能，保证核心功能的稳定运行。</p></li><li><p>要实现优雅的业务降级，需要将功能实现拆分到相对独立的不同代码单元，分优先级进行隔离。在后台通过开关控制，降级部分非主流程的业务功能，减轻系统依赖和性能损耗，从而提升集群的整体吞吐率。</p></li><li><p>业务降级通常需要通过开关工作，开关一般做成配置放在专门的配置系统，配置的修改最好能够实时生效。开源的配置系统有阿里的diamond、携程的Apollo、百度的disconf。</p></li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><table><thead><tr><th align="center">技术</th><th align="center">解决问题</th></tr></thead><tbody><tr><td align="center">扩展</td><td align="center">通过冗余部署，避免单点故障</td></tr><tr><td align="center">隔离</td><td align="center">避免业务之间的相互影响，机房隔离避免单点故障</td></tr><tr><td align="center">解耦</td><td align="center">减少依赖，减少相互间的影响</td></tr><tr><td align="center">限流</td><td align="center">遇到突发流量时，保证系统稳定</td></tr><tr><td align="center">降级</td><td align="center">牺牲非核心业务，保证核心业务的高可用</td></tr><tr><td align="center">熔断</td><td align="center">减少不稳定的外部依赖对核心服务的影响</td></tr><tr><td align="center">自动化测试</td><td align="center">通过完善的测试，减少发布引起的故障</td></tr><tr><td align="center">灰度发布</td><td align="center">灰度发布是速度与安全性作为妥协，能够有效减少发布故障</td></tr></tbody></table><h2 id="分层高可用架构实践"><a href="#分层高可用架构实践" class="headerlink" title="分层高可用架构实践"></a>分层高可用架构实践</h2><h3 id="常见的互联网分层架构"><a href="#常见的互联网分层架构" class="headerlink" title="常见的互联网分层架构"></a>常见的互联网分层架构</h3><p><img data-src="/transblog/images/3557521175650.png"></p><p>常见互联网分布式架构如上，分为：</p><p>（1）客户端层：典型调用方是浏览器browser或者手机应用APP</p><p>（2）反向代理层：系统入口，反向代理</p><p>（3）站点应用层：实现核心应用逻辑，返回html或者json</p><p>（4）服务层：如果实现了服务化，就有这一层</p><p>（5）数据-缓存层：缓存加速访问存储</p><p>（6）数据-数据库层：数据库固化数据存储</p><p>整个系统的高可用，又是通过每一层的冗余+自动故障转移来综合实现的。</p><h3 id="【客户端层-反向代理层】的高可用"><a href="#【客户端层-反向代理层】的高可用" class="headerlink" title="【客户端层-&gt;反向代理层】的高可用"></a>【客户端层-&gt;反向代理层】的高可用</h3><p><img data-src="/transblog/images/5586822176259.png"></p><p>【客户端层】到【反向代理层】的高可用，是通过反向代理层的冗余来实现的。以nginx为例：有两台nginx，一台对线上提供服务，另一台冗余以保证高可用，常见的实践是keepalived存活探测，相同virtual IP提供服务。</p><p><img data-src="/transblog/images/3570223203214.png"></p><p>自动故障转移：当nginx挂了的时候，keepalived能够探测到，会自动的进行故障转移，将流量自动迁移到shadow-nginx，由于使用的是相同的virtual IP，这个切换过程对调用方是透明的。</p><h3 id="【反向代理层-站点层】的高可用"><a href="#【反向代理层-站点层】的高可用" class="headerlink" title="【反向代理层-&gt;站点层】的高可用"></a>【反向代理层-&gt;站点层】的高可用</h3><p><img data-src="/transblog/images/414224185427.png"></p><p>【反向代理层】到【站点层】的高可用，是通过站点层的冗余来实现的。假设反向代理层是nginx，nginx.conf里能够配置多个web后端，并且nginx能够探测到多个后端的存活性。</p><p><img data-src="/transblog/images/4036524174725.png"></p><p>自动故障转移：当web-server挂了的时候，nginx能够探测到，会自动的进行故障转移，将流量自动迁移到其他的web-server，整个过程由nginx自动完成，对调用方是透明的。</p><h3 id="【站点层-服务层】的高可用"><a href="#【站点层-服务层】的高可用" class="headerlink" title="【站点层-&gt;服务层】的高可用"></a>【站点层-&gt;服务层】的高可用</h3><p><img data-src="/transblog/images/1439825170976.png"></p><p>【站点层】到【服务层】的高可用，是通过服务层的冗余来实现的。“服务连接池”会建立与下游服务多个连接，每次请求会“随机”选取连接来访问下游服务。</p><p><img data-src="/transblog/images/3445325171115.png"></p><p>自动故障转移：当service挂了的时候，service-connection-pool能够探测到，会自动的进行故障转移，将流量自动迁移到其他的service，整个过程由连接池自动完成，对调用方是透明的（所以说RPC-client中的服务连接池是很重要的基础组件）。</p><h3 id="【服务层-缓存层】的高可用"><a href="#【服务层-缓存层】的高可用" class="headerlink" title="【服务层&gt;缓存层】的高可用"></a>【服务层&gt;缓存层】的高可用</h3><p><img data-src="/transblog/images/279926183205.png"></p><p>【服务层】到【缓存层】的高可用，是通过缓存数据的冗余来实现的。<br>缓存层的数据冗余又有几种方式：第一种是利用客户端的封装，service对cache进行双读或者双写。</p><p><img data-src="/transblog/images/2601226188244.png"></p><p>缓存层也可以通过支持主从同步的缓存集群来解决缓存层的高可用问题。<br>以redis为例，redis天然支持主从同步，redis官方也有sentinel哨兵机制，来做redis的存活性检测。</p><p><img data-src="/transblog/images/994127189539.png"></p><p>自动故障转移：当redis主挂了的时候，sentinel能够探测到，会通知调用方访问新的redis，整个过程由sentinel和redis集群配合完成，对调用方是透明的。</p><p>说完缓存的高可用，这里要多说一句，业务对缓存并不一定有“高可用”要求，更多的对缓存的使用场景，是用来“加速数据访问”：把一部分数据放到缓存里，如果缓存挂了或者缓存没有命中，是可以去后端的数据库中再取数据的。</p><p>这类允许“cache miss”的业务场景，缓存架构的建议是：</p><p><img data-src="/transblog/images/3868627190541.png"></p><p>将kv缓存封装成服务集群，上游设置一个代理（代理可以用集群冗余的方式保证高可用），代理的后端根据缓存访问的key水平切分成若干个实例，每个实例的访问并不做高可用。</p><p><img data-src="/transblog/images/5835127190718.png"></p><p>缓存实例挂了屏蔽：当有水平切分的实例挂掉时，代理层直接返回cache miss，此时缓存挂掉对调用方也是透明的。key水平切分实例减少，不建议做re-hash，这样容易引发缓存数据的不一致。</p><h3 id="【服务层-数据库层】的高可用"><a href="#【服务层-数据库层】的高可用" class="headerlink" title="【服务层&gt;数据库层】的高可用"></a>【服务层&gt;数据库层】的高可用</h3><p>大部分互联网技术，数据库层都用了“主从同步，读写分离”架构，所以数据库层的高可用，又分为“读库高可用”与“写库高可用”两类。</p><h4 id="【服务层-数据库层“读”】的高可用"><a href="#【服务层-数据库层“读”】的高可用" class="headerlink" title="【服务层&gt;数据库层“读”】的高可用"></a>【服务层&gt;数据库层“读”】的高可用</h4><p><img data-src="/transblog/images/4102128176270.png"></p><p>【服务层】到【数据库读】的高可用，是通过读库的冗余来实现的。<br>既然冗余了读库，一般来说就至少有2个从库，“数据库连接池”会建立与读库多个连接，每次请求会路由到这些读库。</p><p><img data-src="/transblog/images/1059729192549.png"></p><p>自动故障转移：当读库挂了的时候，db-connection-pool能够探测到，会自动的进行故障转移，将流量自动迁移到其他的读库，整个过程由连接池自动完成，对调用方是透明的（所以说DAO中的数据库连接池是很重要的基础组件）。</p><h4 id="【服务层-数据库层“写”】的高可用"><a href="#【服务层-数据库层“写”】的高可用" class="headerlink" title="【服务层&gt;数据库层“写”】的高可用"></a>【服务层&gt;数据库层“写”】的高可用</h4><p><img data-src="/transblog/images/4051929185594.png"></p><p>【服务层】到【数据库写】的高可用，是通过写库的冗余来实现的。<br>以mysql为例，可以设置两个mysql双主同步，一台对线上提供服务，另一台冗余以保证高可用，常见的实践是keepalived存活探测，相同virtual IP提供服务。</p><p><img data-src="/transblog/images/1152330182361.png"></p><p>自动故障转移：当写库挂了的时候，keepalived能够探测到，会自动的进行故障转移，将流量自动迁移到shadow-db-master，由于使用的是相同的virtual IP，这个切换过程对调用方是透明的。</p><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><p>高可用HA（High Availability）是分布式系统架构设计中必须考虑的因素之一，它通常是指，通过设计减少系统不能提供服务的时间。<br>方法论上，高可用是通过冗余+自动故障转移来实现的。<br>整个互联网分层系统架构的高可用，又是通过每一层的冗余+自动故障转移来综合实现的，具体的：</p><ol><li><p>【客户端层】到【反向代理层】的高可用，是通过反向代理层的冗余实现的，常见实践是keepalived + virtual IP自动故障转移</p></li><li><p>【反向代理层】到【站点层】的高可用，是通过站点层的冗余实现的，常见实践是nginx与web-server之间的存活性探测与自动故障转移</p></li><li><p>【站点层】到【服务层】的高可用，是通过服务层的冗余实现的，常见实践是通过service-connection-pool来保证自动故障转移</p></li><li><p>【服务层】到【缓存层】的高可用，是通过缓存数据的冗余实现的，常见实践是缓存客户端双读双写，或者利用缓存集群的主从数据同步与sentinel保活与自动故障转移；更多的业务场景，对缓存没有高可用要求，可以使用缓存服务化来对调用方屏蔽底层复杂性</p></li><li><p>【服务层】到【数据库“读”】的高可用，是通过读库的冗余实现的，常见实践是通过db-connection-pool来保证自动故障转移</p></li><li><p>【服务层】到【数据库“写”】的高可用，是通过写库的冗余实现的，常见实践是keepalived + virtual IP自动故障转移</p></li></ol><h3 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h3><p>总结一下高可用的设计原理：</p><ol><li>要做到数据不丢，就必需要持久化</li><li>要做到服务高可用，就必需要有备用（复本），无论是应用结点还是数据结点</li><li>要做到复制，就会有数据一致性的问题。</li></ol><h4 id="如何实现高可用"><a href="#如何实现高可用" class="headerlink" title="如何实现高可用"></a>如何实现高可用</h4><h5 id="入口层"><a href="#入口层" class="headerlink" title="入口层"></a>入口层</h5><p>入口层，通常指Nginx和Apache等层面的东西，负责应用（不管是Web应用还是移动应用）的服务入口。我们通常会将服务定位在一个IP，如果这个IP对应的服务器当机了，那么用户的访问肯定会中断。此时，可以用keepalived来实现入口层的高可用。例如，机器A 的IP是 1.2.3.4，机器 B 的 IP 是 1.2.3.5, 那么再申请一个 IP 1.2.3.6（称为⼼跳IP）, 平时绑定在机器A上,如果A当机，IP会自动绑定在机器B上；如果B当机，IP会自动绑定在机器A上。对于这种形式，我们将DNS绑定到心跳IP上，即可实现入口层的高可用。</p><p>这里要注意，keepalived在使用上会有一些限制。但这个方案有一点小问题。<br>第一，它的切换可能会有一到两秒的中断，也就是说，如果不是要求到非常严格的毫秒级就不会有问题。<br>第二，对入口的机器会有些浪费，因为买了两台机器的入口，可能就只有一台机器用上。对一些长连接的应用可能会导致服务中断，这时候就需要客户端做配合做一些重新创建连接的工作。简单来说，对于比较普通的业务来说，这个方案就能解决一部分问题。</p><p>两台机器必须在同一个网段，不是在同一个网段，没有办法实现互相抢IP。<br>内网服务也可以做心跳，但需要注意的是，以前为了安全我们会把内网服务绑定在内网IP上，避免出现安全问题。但为了使用keepalived，必须监听在所有IP上（如果监听在心跳IP上，那么机器没有持有该IP时，服务无法启动），简单的方案是启用 iptables, 避免内网服务被外网访问。<br>服务器利用率下降，这时可以考虑做混合部署来改善这一点。<br>比较常见的一个错误是，如果有两台机器，两个公网IP，DNS上把域名同时定位到两个IP，就觉得已经做了高可用了。这完全不是高可用，因为如果一台机器当机,那么就有一半左右的用户无法访问。</p><p>除了keepalive，lvs也能用来解决入口层的高可用问题。不过，与keepalived相比，lvs会更复杂一些，门槛也会高一些。</p><h5 id="业务层"><a href="#业务层" class="headerlink" title="业务层"></a>业务层</h5><p>业务层通常是由PHP、Java、Python、Go等写的逻辑代码构成的，需要依赖于后台数据库及一些缓存层面的东西。如何实现业务层的高可用呢？最核心的就是，业务层不要有状态,将状态分散到缓存层和数据库。目前大家通常喜欢将以下几种数据放入业务层。</p><p>第一个是session，即用户登录相关的数据，但好的做法是将session放在数据库里，或者一个比较稳定的缓存系统中。</p><p>第二个是缓存，在访问数据库时，如果一个查询很慢，就希望将这些结果暂时放到进程里，下次再做查询时就不用再访问数据库了。这种做法带来的问题是，当业务层服务器不只一台时，数据很难做到一致，从缓存拿到的数据就可能是错误的。。</p><p>一个简单的原则就是业务层不要有状态。在业务层没有状态时，一台业务层服务器当掉了之后，Nginx&#x2F;Apache会自动将所有的请求打到另外一台业务层的服务器上。由于没有状态，两台服务器没有任何差异，所以用户完全感受不到。如果把session放在业务层里面的话，那么面临的问题是，这个用户以前是登录在一台机器上的，这个进程死掉后，用户就会被登出了。</p><p>友情提醒：有一段时间比较流行cookie session，就是将session中的数据加密之后放在客户的cookie里，然后下发到客户端，这样也能做到与服务端完全无状态。但这里面有很多坑，如果能绕过这些坑就可以这样使用。第一个坑是怎么保证加密的密钥不泄露，一旦泄露就意味着攻击者可以伪造任何人的身份。第二个坑是重放攻击，如何避免别人通过保存 cookie 去不停地尝试的验证码，当然也还有其他一些攻击手段。如果没有好办法解决这两方面的问题，那么cookie session尽量慎用。最好是将session放在一个性能比较好的数据库中。如果数据库性能不行，那么将session放在缓存中也比放在cookie里要好一点。</p><h5 id="缓存层"><a href="#缓存层" class="headerlink" title="缓存层"></a>缓存层</h5><p>非常简单的架构里是没有缓存这个概念的。但在访问量上来之后，MySQL之类的数据库扛不住了，比如在SATA盘里跑MySQL，QPS到达200、300甚至500时，MySQL的性能会大幅下降，这时就可以考虑用缓存层来挡住绝大部分服务请求，提升系统整体的容量。</p><p>缓存层做高可用一个简单的方法就是，将缓存层分得细一点儿。比如说，缓存层就一台机器的话，那么这台机器当了以后，所有应用层的压力就会往数据库里压，数据库扛不住的话，整个网站（或应用）就会随之当掉。而如果缓存层分在四台机器上的话，每台只有四分之一，这台机器当掉了以后，也只有总访问量的四分之一会压在数据库上面，数据库能扛住的话，网站就能很稳定地等到缓存层重新起来。在实践中，四分之一显然是不够的，我们会将它分得更细，以保证单台缓存当机后数据库还能撑得住即可。在中小规模下，缓存层和业务层可以混合部署,这样可以节省机器。</p><h5 id="数据库层"><a href="#数据库层" class="headerlink" title="数据库层"></a>数据库层</h5><p>在数据库层面实现高可用，通常是在软件层面来做。例如，MySQL有主从模式(Master-Slave)，还有主主模式(Master-Master)都能满足需求。MongoDB也有ReplicaSet的概念,基本都能满足大家的需求。</p><p>总之，要想实现高可用，需要做到这几点：入口层做心跳，业务层服务器无状态，缓存层减小粒度，数据库做一个主从模式。对于这种模式来讲，我们做的高可用不需要太多服务器，这些东西都可以同时部署在两台服务器上。这时，两台服务器就能满足早期的高可用需求了。任何一台服务器当机用户完全无感知。</p><h4 id="如何实现可伸缩"><a href="#如何实现可伸缩" class="headerlink" title="如何实现可伸缩"></a>如何实现可伸缩</h4><h5 id="入口层-1"><a href="#入口层-1" class="headerlink" title="入口层"></a>入口层</h5><p>在入口层实现伸缩性，可以通过直接水平扩机器，然后DNS加IP来实现。但需要注意，尽管一个域名解析到几十个IP没有问题,但是很多浏览器客户端只会使用前几个IP,部分域名供应商对此有优化(如每次返回的IP顺序随机),但这个优化效果不稳定。</p><p>推荐的做法是使用少量的Nginx机器作为入口,业务服务器隐藏在内网(HTTP类型的业务这种方式居多)。另外,也可以把所有IP下发到客户端，然后在客户端做一些调度(特别是非HTTP型的业务,如游戏、直播)。</p><h5 id="业务层-1"><a href="#业务层-1" class="headerlink" title="业务层"></a>业务层</h5><p>业务层的伸缩性如何实现?与做高可用时的解决方案一样,要实现业务层的伸缩性，保证无状态是很好的手段。此外，加机器继续水平部署即可。</p><h5 id="缓存层-1"><a href="#缓存层-1" class="headerlink" title="缓存层"></a>缓存层</h5><p>比较麻烦的是缓存层的伸缩性，最简单粗暴的方式是什么呢？趁着半夜量比较低的时候，把整个缓存层全部下线，然后上线新的缓存层。新的缓存层启动起来之后，再等这些缓存慢慢预热。当然这里一个要求，你的数据库能抗住低估期的请求量。如果扛不住呢？取决于缓存类型，下面我们先可以将缓存的类型区分一下。</p><p>强一致性缓存：无法接受从缓存拿到错误的数据 (比如用户余额，或者会被下游继续缓存这种情形)<br>弱一致性缓存：能接受在一段时间内从缓存拿到错误的数据 (比如微博的转发数)。<br>不变型缓存：缓存key对应的value不会变更 (比如从SHA1推出来的密码, 或者其他复杂公式的计算结果)。<br>那什么缓存类型伸缩性比较好呢？弱一致性和不变型缓存的扩容很方便,用一致性Hash即可；强一致性情况稍微复杂一些，稍后再讲。使用一致性Hash，而不用简单Hash的原因是缓存的失效率。如果缓存从9台扩容到10台,简单Hash 情况下90%的缓存会马上失效,而如果使用一致性Hash情况,只有10%的缓存会失效。</p><p>那么，强一致性缓存会有什么问题？第一个问题是，缓存客户端的配置更新时间会有微小的差异,在这个时间窗内有可能会拿到过期的数据。第二个问题是，如果扩容之后再裁撤节点,会拿到脏数据。比如 a 这个key之前在机器1，扩容后在机器2，数据更新了，但裁撤节点后key回到机器1，这时候就会拿到脏数据。</p><p>要解决问题2比较简单，要么保持永不减少节点,要么节点调整间隔大于数据的有效时间。问题1可以用如下的步骤来解决：</p><p>两套hash配置都更新到客户端，但仍然使用旧配置；<br>逐个客户端改为只有两套hash结果一致的情况下会使用缓存，其余情况从数据库读，但写入缓存；<br>逐个客户端通知使用新配置。<br>Memcache 设计得比较早，导致在伸缩性高可用方面的考虑得不太周到。Redis 在这方面有不少改进，特别是 @ngaut 团队基于 redis 开发了 codis 这个软件，一次性地解决了缓存层的绝大部分问题。推荐大家考察一下。</p><h5 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h5><p>在数据库层面实现伸缩，方法很多，文档也很多，此处不做过多赘述。大致方法为：水平拆分、垂直拆分和定期滚动。</p><p>总之，我们可以在入口层、业务层面、缓存层和数据库层四个层面，使用刚才介绍的方法和技术实现系统高可用和可伸缩性。具体为：在入口层用心跳来做到高可用，用平行部署来伸缩；在业务层做到服务无状态；在缓存层，可以减小一些粒度，以方便实现高可用，使用一致性Hash将有助于实现缓存层的伸缩性；数据库层的主从模式能解决高可用问题，拆分和滚动能解决可伸缩问题。</p><h1 id="容灾设计"><a href="#容灾设计" class="headerlink" title="容灾设计"></a>容灾设计</h1><h2 id="一：逻辑层容灾"><a href="#一：逻辑层容灾" class="headerlink" title="一：逻辑层容灾"></a>一：逻辑层容灾</h2><p>逻辑层服务一般都设计从无状态服务，客户端当前请求和下次请求在逻辑层没有任何的关联，因此客户端可以在在多次请求中分别到不同的机器上，而返回的结果和一直在同一台机器上一样。由于这种特性的存在使得逻辑层可以通过多级备份来实现容灾。<br><strong>备份可以有 ：主备(1+1), 一主多备（1+n），多主一备（n+1）， 无备（1+0），互相备份（n）</strong></p><p>切换的策略可以为：</p><ol><li>冷切：即主完全承担所有业务，当主不可服务时再启用备，该方式由于备机一直处于不服务状态，会出现当要切换到备的时候，备也不可服务，可信度低。</li><li>热切：主和备一起分担所有请求，主备只承担部分业务请求（总和为100%）。这样可以解决之前的冷切遇到的信任度低的问题。同时业务由主备共同分担，节约成本。但是在主完全不可服务时，备有过载的风险。这里可以没有主备之分，在我们的实践中常用多台机器组成的集群来实现互为备份。</li><li>双在线：主备各跑100%的任务。这个可以同时解决上面的遇到的问题。但是成本高（带宽、电力、机房维护成本等）</li></ol><h2 id="二：数据层容灾"><a href="#二：数据层容灾" class="headerlink" title="二：数据层容灾"></a>二：数据层容灾</h2><p>相对于逻辑层是无状态的服务，数据层服务需要存储和用户相关的数据，用户的多次请求之间的数据是有关联的。因此如何在多机容灾备份的情况下保证数据一致性成为一个关键问题。</p><h3 id="数据一致性的问题："><a href="#数据一致性的问题：" class="headerlink" title="数据一致性的问题："></a>数据一致性的问题：</h3><p>中心化备份策略：<br>在传统的数据库、数据服务器设计中往往采用中心化的的模型。每台机器中存储全量的数据，其中有只有一台可写的主机，有一台或多台机器可读的备机去同步主机的数据。当主机不可服务时，其中一台备机升级为主机。</p><p>数据增量同步：只对有更新的数据进行同步， 当主机有数据更新时， 会先在本地生成Binlog及同步相关的信息，主机与备机之间通过一定的协议将数据同步到备机。<br>数据全量同步： 每次都全量同步主机的数据。一般会一段时间内做一次，是增量同步的一个补充。可以一块一数据计算MD5值进行比较，只同步MD5值不相等的数 据快。</p><p>问题：只有一台可写容易出现单点问题（虽然可以切换，但是还是会在一定时间内不可服务）、由于网络问题可能出现多主。</p><p>去中心化备份策略：<br>相对于中心化只有一个主可写，其他备机都只是同步主机的数据，去中心化没有主备之分， 所有的机器都可读可写，这样写的性能会有很大的提高，但是要做到数据的一致性比较复杂。目前主要的设计方案有： RNW协议、2pc、3pc协议、paxos协议等。</p><h2 id="三：容灾判定"><a href="#三：容灾判定" class="headerlink" title="三：容灾判定"></a>三：容灾判定</h2><ul><li><p>中心主动探测：该方法是中心主动和其他子系统或服务定时发送消息（可用私有协议或ping消息等），根据回包来判定服务是否可用。</p></li><li><p>等待服务上报：该方法是子系统或者服务，又或者是专门agent主动的将服务的运行状态上报给中心系统，中心系统通过上报消息来判定服务是否运行正常。</p></li><li><p>请求者判定：即服务的使用方将根据请求的服务状态（延迟、是否成功等信息）来判定服务是否正常，请求者在本地记录这些信息，只将请求发送到那些正常的服务。这种方式相对于中央判定可以防止中心和其他子服务之间由于网络问题而导致误判。</p></li></ul><h2 id="四：异地部署"><a href="#四：异地部署" class="headerlink" title="四：异地部署"></a>四：异地部署</h2><p>某些重要的服务为了在局部的网络不可用、自然灾害等问题时也能保证服务的可用性，需要将服务部署在不同的城市、机房。这样一方面可以保证服务的可靠性，也可以让不同地区的用户就近访，提高服务质量。</p><h2 id="五、负载均衡"><a href="#五、负载均衡" class="headerlink" title="五、负载均衡"></a>五、负载均衡</h2><ul><li><p>接入负载：通过代理服务将服务分发到不同的机器上，这个可以通过单独部署负载均衡服务器、LVS、DNS、http重定向、nginx方向代理、NAT等实现。</p></li><li><p>号段负载：通过请求自身的特殊性将不同的请求分发到固定的服务上进行处理。如通过一致性hash将hash值相关的请求路由到固定的服务，也可以将固定号段的qq号路由到固定的服务。如何分配不够离散化，可能会导致某些热点请求都集中在同一台服务，从而导致负载不均衡。</p></li><li><p>用户自助负载：这种类似于游戏服务器中用户选择不同的服来分配不同的机器，达到将不同的请求分配到不同的机器的目的。</p></li></ul><h2 id="六：过载保护"><a href="#六：过载保护" class="headerlink" title="六：过载保护"></a>六：过载保护</h2><p>当负载达到系统处理能力的上限时，系统的处理能力将随着负载的增加急剧下降，俗称滚雪球。在系统设计的时候做好过载保护是一项很重要的工作。过载保护的方法有:</p><ol><li><p>轻重分离、隔离部署：将系统、业务、功能隔离部署（可以分不同的set），确保系统过载状态不会扩散到其他业务系统，对其他业务系统造成影响，使得影响最小化。</p></li><li><p>频率控制：控制单位时间内的请求量（可以是总的请求量也可以是单个用户的请求量），这样可以保证系统在过载的时候不会被压垮，保证部分用户的请求可以被处理。</p></li><li><p>设置请求的有效时间： 用户的请求都期望在一定时间范围内返回结果。如果没有返回则会当做超时出错处理。如果服务端一直在处理这样的请求，那么其实是在无用功，对用户没有如何意义。因此我们需要对每个请求做超时限制，在一定的时间内没有处理完则丢弃该请求，理想的状态是从收到请求开始计时，这样可以防止数据包在队列中已经超时。</p></li><li><p>有损服务：服务做好有损处理，当系统过载时，可以将某些非关键性的服务下掉，从而保证关键业务可以正常服务。如果是底层非关键性的服务过载，则可以不去请求该服务的数据而直接反回给前端。同时前端也可以做相应的有损策略，防止后台服务过载时给用户不好的体验。</p></li></ol><h2 id="七：常见的互联网事故及解决策略"><a href="#七：常见的互联网事故及解决策略" class="headerlink" title="七：常见的互联网事故及解决策略"></a>七：常见的互联网事故及解决策略</h2><table><thead><tr><th align="center">事故</th><th align="center">解决策略</th></tr></thead><tbody><tr><td align="center">服务器硬件故障死机：</td><td align="center">集群部署、多机备份、自动检测并切换</td></tr><tr><td align="center">网络丢包、光钎断：</td><td align="center">异地部署、自动检测故障并切换</td></tr><tr><td align="center">服务器雪崩：</td><td align="center">负载均衡、过载保护、容量告警</td></tr><tr><td align="center">外部依赖故障：</td><td align="center">柔性逻辑、降级服务、限制重试</td></tr><tr><td align="center">DNS故障 ：</td><td align="center">自搭建类DNS服务、使用IP列表替代DNS</td></tr><tr><td align="center">程序CORE：</td><td align="center">自动拉起、实时告警</td></tr><tr><td align="center">操作失望：</td><td align="center">灰度、保护逻辑、人员备份确认</td></tr></tbody></table><h1 id="负载均衡"><a href="#负载均衡" class="headerlink" title="负载均衡"></a>负载均衡</h1><p>负载均衡，意思是将负载（工作任务，访问请求）进行平衡、分摊到多个操作单元（服务器，组件）上进行执行。是解决高性能，单点故障（高可用），扩展性（水平伸缩）的终极解决方案。</p><p><img data-src="/transblog/images/3061934156547.png"></p><p>应用集群：将同一应用部署到多台机器上，组成处理集群，接收负载均衡设备分发的请求，进行处理，并返回相应数据。<br>负载均衡设备：将用户访问的请求，根据负载均衡算法，分发到集群中的一台处理服务器。（一种把网络请求分散到一个服务器集群中的可用服务器上去的设备）</p><p>负载均衡的作用（解决的问题）：</p><ol><li>解决并发压力，提高应用处理性能（增加吞吐量，加强网络处理能力）；</li><li>提供故障转移，实现高可用；</li><li>通过添加或减少服务器数量，提供网站伸缩性（扩展性）；</li><li>安全防护；（负载均衡设备上做一些过滤，黑白名单等处理）</li></ol><h2 id="负载均衡实现"><a href="#负载均衡实现" class="headerlink" title="负载均衡实现"></a>负载均衡实现</h2><h3 id="http重定向协议实现负载均衡"><a href="#http重定向协议实现负载均衡" class="headerlink" title="http重定向协议实现负载均衡"></a>http重定向协议实现负载均衡</h3><p>原理：根据用户的http请求计算出一个真实的web服务器地址，并将该web服务器地址写入http重定向响应中返回给浏览器，由浏览器重新进行访问。</p><p><img data-src="/transblog/images/272621174104.png"></p><p>优点：比较简单<br>缺点：<br>浏览器需要零次请求服务器才能完成一次访问，性能较差。<br>http重定向服务器自身的处理能力可能成为瓶颈。<br>使用http302响应重定向，有可能使搜索引擎判断为SEO作弊，降低搜索排名。</p><h3 id="【协议层】DNS负载均衡"><a href="#【协议层】DNS负载均衡" class="headerlink" title="【协议层】DNS负载均衡"></a>【协议层】DNS负载均衡</h3><p>原理：在DNS服务器上配置多个域名对应IP的记录。例如一个域名<a href="http://www.baidu.com对应一组web服务器ip地址,域名解析时经过dns服务器的算法将一个域名请求分配到合适的真实服务器上./">www.baidu.com对应一组web服务器IP地址，域名解析时经过DNS服务器的算法将一个域名请求分配到合适的真实服务器上。</a></p><p><img data-src="/transblog/images/5181921160784.png"></p><p>优点：<br>使用简单：负载均衡工作，交给DNS服务器处理，省掉了负载均衡服务器维护的麻烦<br>提高性能：可以支持基于地址的域名解析，解析成距离用户最近的服务器地址，可以加快访问速度，改善性能；<br>缺点：<br>可用性差：DNS解析是多级解析，新增&#x2F;修改DNS后，解析时间较长；解析过程中，用户访问网站将失败；http重定向服务器自身的处理能力可能成为瓶颈。<br>扩展性低：DNS负载均衡的控制权在域名商那里，无法对其做更多的改善和扩展；</p><p>将DNS作为第一级负载均衡，A记录对应着内部负载均衡的IP地址，通过内部负载均衡将请求分发到真实的Web服务器上。一般用于互联网公司，复杂的业务系统不合适使用。</p><p><img data-src="/transblog/images/4278339152301.png"></p><h3 id="【网络层】IP负载均衡"><a href="#【网络层】IP负载均衡" class="headerlink" title="【网络层】IP负载均衡"></a>【网络层】IP负载均衡</h3><p>在网络层通过修改请求目标地址进行负载均衡。<br>用户请求数据包，到达负载均衡服务器后，负载均衡服务器在操作系统内核进程获取网络数据包，根据负载均衡算法得到一台真实服务器地址，然后将请求目的地址修改为，获得的真实ip地址，不需要经过用户进程处理。<br>真实服务器处理完成后，响应数据包回到负载均衡服务器，负载均衡服务器，再将数据包源地址修改为自身的ip地址，发送给用户浏览器。如下图：</p><p><img data-src="/transblog/images/4541627153818.png"></p><p>IP负载均衡，真实物理服务器返回给负载均衡服务器，存在两种方式：</p><ol><li>负载均衡服务器在修改目的的IP地址的同时修改源地址，将数据源地址设为自身IP，即源地址转换(SNAT),这样Web服务器的响应会再回到负载均衡服务器。</li><li>将负载均衡服务器同时作为物理服务器集群的网关服务器，这样所有响应数据都会到达负载均衡服务器。</li></ol><p>优点：在内核进程完成数据分发，比在应用层分发性能更好；<br>缺点：所有请求响应都需要经过负载均衡服务器，集群最大吞吐量受限于负载均衡服务器网卡带宽；</p><h3 id="【协议层】反向代理负载均衡"><a href="#【协议层】反向代理负载均衡" class="headerlink" title="【协议层】反向代理负载均衡"></a>【协议层】反向代理负载均衡</h3><p>原理：在部署位置上，<mark>反向代理服务器处于Web服务器前面</mark>（这样才可能缓存Web响应，加速访问），反向代理服务器同时提供负载均衡的功能，管理一组Web服务器，将请求根据负载均衡算法转发到不同的Web服务器上。Web服务器处理完成的响应也需要通过反向代理服务器返回给用户。由于Web服务器不直接对外提供访问，因此Web服务器不需要使用外部 IP地址，而反向代理服务器则需要配置双网卡和内部外部两套IP地址。</p><p><img data-src="/transblog/images/5875522151314.png"></p><p>例如：浏览器访问请求的地址是反向代理服务器的地址114.100.80.10，反向代理服务器收到请求后，根据负载均衡算法计算得到一台真实物理服务器的地址10.0.0.3，并将请求转发给服务器。10.0.0.3处理完请求后将响应返回给反向代理服务器，反向代理服务器再将该响应返回给用户。<br>优点：部署简单，处于http协议层面。<br>缺点：使用了反向代理服务器后，web 服务器地址不能直接暴露在外，因此web服务器不需要使用外部IP地址，而反向代理服务作为沟通桥梁就需要配置双网卡、外部内部两套IP地址。</p><h3 id="【链路层】链路层负载均衡"><a href="#【链路层】链路层负载均衡" class="headerlink" title="【链路层】链路层负载均衡"></a>【链路层】链路层负载均衡</h3><p>在通信协议的数据链路层修改mac地址，进行负载均衡。</p><p><img data-src="/transblog/images/3177729162765.png"></p><p>数据分发时，不修改ip地址，指修改目标mac地址，配置真实物理服务器集群所有机器虚拟ip和负载均衡服务器ip地址一致，达到不修改数据包的源地址和目标地址，进行数据分发的目的。<br>实际处理服务器ip和数据请求目的ip一致，不需要经过负载均衡服务器进行地址转换，可将响应数据包直接返回给用户浏览器，避免负载均衡服务器网卡带宽成为瓶颈。也称为直接路由模式（DR模式），DR模式是目前使用最广泛的一种负载均衡方式。<br>优点：性能好；<br>缺点：配置复杂；</p><h3 id="基于NAT的负载均衡技术"><a href="#基于NAT的负载均衡技术" class="headerlink" title="基于NAT的负载均衡技术"></a>基于NAT的负载均衡技术</h3><p><strong>如Linux VirtualServer，简称LVS</strong><br>该技术通过一个地址转换网关<mark>将每个外部连接均匀转换为不同的内部服务器地址</mark>，因此外部网络中的计算机就各自与自己转换得到的地址上的服务器进行通信，从而达到负载均衡的目的。其中网络地址转换网关位于外部地址和内部地址之间，不仅可以实现当外部客户机访问转换网关的某一外部地址时可以转发到某一映射的内部的地址上，还可使内部地址的计算机能访问外部网络。 </p><h3 id="混合型负载均衡"><a href="#混合型负载均衡" class="headerlink" title="混合型负载均衡"></a>混合型负载均衡</h3><p>由于多个服务器群内硬件设备、各自的规模、提供的服务等的差异，可以考虑给每个服务器群采用最合适的负载均衡方式，然后又在这多个服务器群间再一次负载均衡或群集起来以一个整体向外界提供服务（即把这多个服务器群当做一个新的服务器群），从而达到最佳的性能。将这种方式称之为混合型负载均衡。</p><p>此种方式有时也用于单台均衡设备的性能不能满足大量连接请求的情况下。是目前大型互联网公司，普遍使用的方式。</p><ol><li>适合有动静分离的场景<br>反向代理服务器（集群）可以起到缓存和动态请求分发的作用，当时静态资源缓存在代理服务器时，则直接返回到浏览器。如果动态页面则请求后面的应用负载均衡（应用集群）。</li></ol><p><img data-src="/transblog/images/4836047165287.png"></p><ol start="2"><li>适合动态请求场景：</li></ol><p><img data-src="/transblog/images/5791948146528.png"></p><h2 id="负载均衡算法"><a href="#负载均衡算法" class="headerlink" title="负载均衡算法"></a>负载均衡算法</h2><h3 id="轮询"><a href="#轮询" class="headerlink" title="轮询"></a>轮询</h3><p>将所有请求，依次分发到每台服务器上，适合服务器硬件同相同的场景。<br>优点：服务器请求数目相同；<br>缺点：服务器压力不一样，不适合服务器配置不同的情况；</p><h3 id="随机"><a href="#随机" class="headerlink" title="随机"></a>随机</h3><p>请求随机分配到各个服务器。<br>优点：使用简单；<br>缺点：不适合机器配置不同的场景；</p><h3 id="最少链接"><a href="#最少链接" class="headerlink" title="最少链接"></a>最少链接</h3><p>将请求分配到连接数最少的服务器（目前处理请求最少的服务器）。<br>优点：根据服务器当前的请求处理情况，动态分配；<br>缺点：算法实现相对复杂，需要监控服务器请求连接数；</p><h3 id="Hash（源地址散列）"><a href="#Hash（源地址散列）" class="headerlink" title="Hash（源地址散列）"></a>Hash（源地址散列）</h3><p>根据IP地址进行Hash计算，得到IP地址。<br>优点：将来自同一IP地址的请求，同一会话期内，转发到相同的服务器；实现会话粘滞。<br>缺点：目标服务器宕机后，会话会丢失；</p><h3 id="加权"><a href="#加权" class="headerlink" title="加权"></a>加权</h3><p>在轮询，随机，最少链接，Hash等算法的基础上，通过加权的方式，进行负载服务器分配。<br>优点：根据权重，调节转发服务器的请求数目；<br>缺点：使用相对复杂；</p><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;分布式系统&quot;&gt;&lt;a href=&quot;#分布式系统&quot; class=&quot;headerlink&quot; title=&quot;分布式系统&quot;&gt;&lt;/a&gt;分布式系统&lt;/h1&gt;&lt;h2 id=&quot;CAP&quot;&gt;&lt;a href=&quot;#CAP&quot; class=&quot;headerlink&quot; title=&quot;CAP&quot;&gt;&lt;/</summary>
      
    
    
    
    <category term="设计" scheme="https://trans-xu.github.io/transblog/categories/%E8%AE%BE%E8%AE%A1/"/>
    
    
  </entry>
  
  <entry>
    <title>数据结构</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/</id>
    <published>2025-08-27T03:50:08.912Z</published>
    <updated>2025-08-27T03:41:00.757Z</updated>
    
    <content type="html"><![CDATA[<h1 id="树"><a href="#树" class="headerlink" title="树"></a>树</h1><h2 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h2><p>每个结点最多 2 棵子树，没有其它限制了。</p><p>‌二叉树的性质‌包括：</p><ul><li>‌节点数的限制‌：在二叉树的第i层上至多有$2^{i-1}$个节点，深度为k的二叉树至多有$2^k-1$个节点。</li><li>‌叶子节点和度为2的节点的关系‌：对于任何一棵二叉树，如果其叶子节点数为$n0$，度为2的节点数为$n2$，则$n0 &#x3D; n2 + 1$。</li></ul><h3 id="前序遍历"><a href="#前序遍历" class="headerlink" title="前序遍历"></a>前序遍历</h3><p>根-&gt;左-&gt;右</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">preorder</span><span class="params">(TreeNode *root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> ;            <span class="comment">// (1)</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">visit</span>(root);            <span class="comment">// (2)</span></span><br><span class="line">    <span class="built_in">preorder</span>(root-&gt;left);   <span class="comment">// (3)</span></span><br><span class="line">    <span class="built_in">preorder</span>(root-&gt;right);  <span class="comment">// (4)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="中序遍历"><a href="#中序遍历" class="headerlink" title="中序遍历"></a>中序遍历</h3><p>左-&gt;根-&gt;右</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">inorder</span><span class="params">(TreeNode *root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> ;            <span class="comment">// (1)</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">inorder</span>(root-&gt;left);    <span class="comment">// (2)</span></span><br><span class="line">    <span class="built_in">visit</span>(root);            <span class="comment">// (3)</span></span><br><span class="line">    <span class="built_in">inorder</span>(root-&gt;right);   <span class="comment">// (4)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="后序遍历"><a href="#后序遍历" class="headerlink" title="后序遍历"></a>后序遍历</h3><p>左-&gt;右-&gt;根</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">postorder</span><span class="params">(TreeNode *root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> ;            <span class="comment">// (1)</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">postorder</span>(root-&gt;left);  <span class="comment">// (2)</span></span><br><span class="line">    <span class="built_in">postorder</span>(root-&gt;right); <span class="comment">// (3)</span></span><br><span class="line">    <span class="built_in">visit</span>(root);            <span class="comment">// (4)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="层序遍历"><a href="#层序遍历" class="headerlink" title="层序遍历"></a>层序遍历</h3><p>如果二叉树为空，则直接返回。否则，依次从树的第一层开始，从上至下逐层遍历。在同一层中，按从左到右的顺序对结点逐个访问。<strong>层序遍历就是一个广度优先搜索</strong>。</p><p>在遍历过程中，将每个节点的值按照遍历的顺序存储到一个队列中，然后依次取出队列中的节点进行访问。这样就可以实现对二叉树的层级遍历。</p><p><img data-src="/transblog/images/467472030003216.gif"></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">levelOrder</span>(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; ans;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    queue&lt;BTNode *&gt; Qu;</span><br><span class="line">    Qu.<span class="built_in">push</span>(root);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (!Qu.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; ret;</span><br><span class="line">        <span class="type">int</span> count = Qu.<span class="built_in">size</span>();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (count--)</span><br><span class="line">        &#123;</span><br><span class="line">            BTNode *tmp = Qu.<span class="built_in">front</span>();</span><br><span class="line">            Qu.<span class="built_in">pop</span>();</span><br><span class="line">            ret.<span class="built_in">push_back</span>(tmp-&gt;val);</span><br><span class="line">            <span class="keyword">if</span> (tmp-&gt;left)</span><br><span class="line">                Qu.<span class="built_in">push</span>(tmp-&gt;left);</span><br><span class="line">            <span class="keyword">if</span> (tmp-&gt;right)</span><br><span class="line">                Qu.<span class="built_in">push</span>(tmp-&gt;right);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ans.<span class="built_in">push_back</span>(ret);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="满二叉树"><a href="#满二叉树" class="headerlink" title="满二叉树"></a>满二叉树</h2><p>在一棵二叉树中，如果所有分支结点都存在左子树和右子树，并且所有叶子都在同一层上，这样的二叉树就称为满二叉树</p><p><img data-src="/transblog/images/244508579344671.png"></p><p>满二叉树的特点有：</p><ul><li>叶子只能出现在最下一层。出现在其他层就不可能达到平衡。</li><li>非叶子结点的度一定是2。</li><li>在同样深度的二叉树中，满二叉树的结点个数最多，叶子树最多。</li></ul><h2 id="完全二叉树"><a href="#完全二叉树" class="headerlink" title="完全二叉树"></a>完全二叉树</h2><p>完全二叉树的特点：</p><ul><li>叶子结点只能出现在最下两层。</li><li>最下层的叶子一定集中在左部连续位置</li><li>倒数二层，若有叶子结点，一定都在右部连续位置。</li><li>如果结点度为1，则该结点只有左孩子，即不存在只有右子树的情况。</li><li>同样结点数的二叉树，完全二叉树的深度最小。</li><li>‌完全二叉树的深度‌：具有$n$个节点的完全二叉树的深度为$log_2{n}+1$</li><li>‌编号规则‌：具有$n$个节点的完全二叉树，按顺序编号，节点$i$的父节点为$i&#x2F;2$，左子节点为$2i$，右子节点为$2i+1$</li></ul><p><img data-src="/transblog/images/597178083951359.png"></p><p>注意：<strong>满二叉树一定是棵完全二叉树，但完全二叉树不一定是满的</strong>。</p><h2 id="二叉查找树"><a href="#二叉查找树" class="headerlink" title="二叉查找树"></a>二叉查找树</h2><p><span id="target1"> </span><br>也叫二叉搜索树、二叉排序树，首先它是二叉树，并且 左子树上所有结点的值 小于 它根结点的值，右子树上所有结点的值 大于 它根结点的值。若对该树进行中序遍历，可得到一个递增的序列。</p><h3 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h3><p>对一棵空二叉排序树进行插入，则直接将结点依次插入即可，遵循二叉排序树各结点值的关系，若插入的结点小于根结点的值，则插入到左子树，否则插入到右子树。</p><h3 id="深度"><a href="#深度" class="headerlink" title="深度"></a>深度</h3><p>对$n$个结点构造二叉排序树，理想情况下，即为一棵满二叉树时的高度，类比于完全二叉树，此时拥有最小深度，即树的高度为$h&#x3D;log_2{(n+1)}$或$log_2{n}+1$<br>而若二叉排序树只是一个单支树（左或右单支树），此时最坏情况，即树的高度为元素的个数，则其最大深度为$n$。</p><p><img data-src="/transblog/images/48845985047899.png"></p><h3 id="查找长度"><a href="#查找长度" class="headerlink" title="查找长度"></a>查找长度</h3><p>对n个结点的二叉排序树中，查找某个元素:</p><ol><li>最坏情况下是当构造的二叉树为单支树时，为n层，此时查找树中最后一个元素或不存在的元素时，需要进行n次比较。</li><li>在等概率的情况下，二叉排序树查找成功的平均查找长度ASL&#x3D;每层层数乘以每层结点个数之和 &#x2F; 二叉排序树的结点总个数。</li></ol><p><img data-src="/transblog/images/38273397980300.png"></p><p>元素总数为10，其中第一层元素个数为1，第二层为2，第三层为4，第四层为3，所以可得查找成功的平均查找长度：<br>$$<br>ASL_{成功}  &#x3D;  \frac{(1×1+2×2+3×4+4×3)}{10}    &#x3D;  \frac{29}{10}<br>$$</p><h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3><ol><li>删除叶子结点<br>若删除的结点是二叉排序树的叶子结点，则可以直接删除，删除后并不影响二叉排序树的性质。</li><li>删除结点存在左孩子或右孩子<br>若删除的结点存在左孩子或右孩子时，则让该结点的子树成为其父结点的子结点。</li><li>删除结点存在左孩子和右孩子<br>若删除结点存在左孩子和右孩子，则令该结点的前驱&#x2F;后继代替其本身（用中序遍历序列中该结点的前驱&#x2F;后继代替），调整后的树仍是二叉排序树。</li></ol><blockquote><p>如下删除66节点，可用70或58其中任意一个代替：</p></blockquote><p><img data-src="/transblog/images/392214361405103.png"> <img data-src="/transblog/images/299376310497972.png">    <img data-src="/transblog/images/49575295089206.png"></p><h2 id="AVL和RB树"><a href="#AVL和RB树" class="headerlink" title="AVL和RB树"></a>AVL和RB树</h2><h3 id="平衡二叉树-AVL"><a href="#平衡二叉树-AVL" class="headerlink" title="平衡二叉树(AVL)"></a>平衡二叉树(AVL)</h3><p>首先它是 “二叉搜索树”，其次，它是平衡的，即是它的每一个结点的左子树的高度和右子树的高度差至多为 1。​将二叉查找树平衡均匀地分布，这样的好处就是可以减少二叉查找树的深度。<br>查找效率：$O(logN)$</p><h4 id="失衡调整"><a href="#失衡调整" class="headerlink" title="失衡调整"></a>失衡调整</h4><p>当不满足平衡条件的话，那就只能去通过旋转来实现平衡。下面有4种不满足平衡条件的情况，如图所示：</p><p><img data-src="/transblog/images/131375166348269.jpeg"></p><ul><li>树2是在左子树右子节点出现失衡情况，属于 LR型</li><li>树3是在右子树右子节点出现失衡情况，属于 RR型</li><li>树4是在左子树左子节点出现失衡情况，属于 LL型</li><li>树5是在右子树左子节点出现失衡情况，属于 RL型</li></ul><h5 id="LL型失衡"><a href="#LL型失衡" class="headerlink" title="LL型失衡"></a>LL型失衡</h5><p><strong>右旋，口诀：左失衡，中为支，高右旋</strong></p><p><strong>右旋就是通过顺时针旋转来实现平衡，就是处于右边的根结点落下变为叶子节点，而中间的节点变为新的根节点，最左边的叶子节点保持不变，最后形成新的结构</strong>。</p><p><img data-src="/transblog/images/436309034293638.gif"></p><p>在结点A的左孩子结点B的左子树插入导致不平衡，A结点的左孩子B结点右旋代替A成为当前最小不平衡二叉树的根节点，A结点的左孩子指向B结点的右孩子，并把B结点的右孩子指向A结点。</p><p><img data-src="/transblog/images/48005183903993.png"></p><p>代码如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">Node* <span class="title">R_Rotate</span><span class="params">(Node* head)</span></span>&#123;</span><br><span class="line">        Node* new_head=head-&gt;lchild;</span><br><span class="line">        head-&gt;lchild=new_head-&gt;rchild;</span><br><span class="line">        new_head-&gt;rchild=head;</span><br><span class="line">        head-&gt;height=<span class="number">1</span>+std::<span class="built_in">max</span>(<span class="built_in">GetHeight</span>(head-&gt;lchild),<span class="built_in">GetHeight</span>(head-&gt;rchild));</span><br><span class="line">        new_head-&gt;height=<span class="number">1</span>+std::<span class="built_in">max</span>(<span class="built_in">GetHeight</span>(new_head-&gt;lchild),<span class="built_in">GetHeight</span>(new_head-&gt;rchild));</span><br><span class="line">        <span class="keyword">return</span> new_head;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h5 id="RR型失衡"><a href="#RR型失衡" class="headerlink" title="RR型失衡"></a>RR型失衡</h5><p><strong>左旋，口诀：右失衡，中为支，高左旋</strong><br><strong>对于RR型失衡，通过逆时针旋转，最左边的根结点落下，变成叶子节点，中间的节点变为新的根节点，最右边的叶子节点保持不变</strong>。</p><p><img data-src="/transblog/images/484298429602040.gif"></p><p>在结点A的右孩子结点B的右子树插入导致不平衡，A结点的右孩子B结点左旋代替A成为当前最小不平衡二叉树的根节点，A结点的右孩子指向B结点的左孩子，并把B结点的左孩子指向A结点。<br><img data-src="/transblog/images/25316809926017.png"></p><p>代码如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">Node* <span class="title">L_Rotate</span><span class="params">(Node* head)</span></span>&#123;</span><br><span class="line">        Node* new_head=head-&gt;rchild;</span><br><span class="line">        head-&gt;rchild=new_head-&gt;lchild;</span><br><span class="line">        new_head-&gt;lchild=head;</span><br><span class="line">        head-&gt;height=<span class="number">1</span>+std::<span class="built_in">max</span>(<span class="built_in">GetHeight</span>(head-&gt;lchild),<span class="built_in">GetHeight</span>(head-&gt;rchild));</span><br><span class="line">        new_head-&gt;height=<span class="number">1</span>+std::<span class="built_in">max</span>(<span class="built_in">GetHeight</span>(new_head-&gt;lchild),<span class="built_in">GetHeight</span>(new_head-&gt;rchild));</span><br><span class="line">        <span class="keyword">return</span> new_head;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h5 id="LR型失衡"><a href="#LR型失衡" class="headerlink" title="LR型失衡"></a>LR型失衡</h5><p><strong>先左旋再右旋，口诀：下二整体左旋，后与LL型调整一样</strong></p><p><strong>对于LR型失衡，先在根节点左子树进行左旋处理，变成LL型失衡，然后再去通过右旋来实现整体平衡</strong></p><p><strong>1. 左旋：</strong><br><img data-src="/transblog/images/244068815229394.gif"></p><p><strong>2. 右旋：</strong><br><img data-src="/transblog/images/590618860780956.gif"></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">Node* <span class="title">LR_Rotate</span><span class="params">(Node* head)</span></span>&#123;</span><br><span class="line">        Node* new_lhead=<span class="built_in">L_Rotate</span>(head-&gt;lchild);</span><br><span class="line">        head-&gt;lchild=new_lhead;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">R_Rotate</span>(head);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h5 id="RL型失衡"><a href="#RL型失衡" class="headerlink" title="RL型失衡"></a>RL型失衡</h5><p><strong>先右旋再左旋，口诀：下二整体右旋，后与RR型调整一样</strong></p><p><strong>对于RL型失衡，在根节点的右子树先进行右旋处理，形成RR型失衡，然后整体进行左旋处理，最后达到平衡状态</strong></p><p><strong>1. 右旋：</strong><br><img data-src="/transblog/images/378125754541736.gif"></p><p><strong>2. 左旋：</strong><br><img data-src="/transblog/images/137778338204621.gif"></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">Node* <span class="title">RL_Rotate</span><span class="params">(Node* head)</span></span>&#123;</span><br><span class="line">        Node* new_rhead=<span class="built_in">R_Rotate</span>(head-&gt;rchild);</span><br><span class="line">        head-&gt;rchild=new_rhead;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">L_Rotate</span>(head);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h4 id="插入操作"><a href="#插入操作" class="headerlink" title="插入操作"></a>插入操作</h4><p>这里的代码通过递归实现，变量bal表示平衡二叉树的平衡因子，一旦完成了插入会从下往上进行return，这样在第一次bal不满足条件时，就是最小不平衡二叉树。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">insert</span><span class="params">(T val)</span></span>&#123;</span><br><span class="line">     root= _insert(root,val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Node* _insert(Node* head,T val)&#123;</span><br><span class="line">        <span class="keyword">if</span>(head==<span class="literal">NULL</span>)&#123;</span><br><span class="line">            head=<span class="keyword">new</span> <span class="built_in">Node</span>(val);</span><br><span class="line">            <span class="keyword">return</span> head;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(val&gt;head-&gt;val)&#123;</span><br><span class="line">            head-&gt;rchild=_insert(head-&gt;rchild,val);</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(val&lt;head-&gt;val)&#123;</span><br><span class="line">            head-&gt;lchild=_insert(head-&gt;lchild,val);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">throw</span> std::<span class="built_in">invalid_argument</span>(<span class="string">&quot;Cannot insert elements with the same value!&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        head-&gt;height=<span class="number">1</span>+std::<span class="built_in">max</span>(<span class="built_in">GetHeight</span>(head-&gt;lchild),<span class="built_in">GetHeight</span>(head-&gt;rchild));</span><br><span class="line">        <span class="type">int</span> bal = <span class="built_in">GetHeight</span>(head-&gt;lchild) -<span class="built_in">GetHeight</span>(head-&gt;rchild);</span><br><span class="line">        <span class="keyword">if</span>(bal&gt;<span class="number">1</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(val&lt;head-&gt;lchild-&gt;val)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">LL_Rotate</span>(head);</span><br><span class="line">            &#125;<span class="keyword">else</span> <span class="keyword">if</span>(val&gt;head-&gt;lchild-&gt;val)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">LR_Rotate</span>(head);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(bal&lt;<span class="number">-1</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(val&gt;head-&gt;rchild-&gt;val)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">RR_Rotate</span>(head);</span><br><span class="line">            &#125;<span class="keyword">else</span> <span class="keyword">if</span>(val&lt;head-&gt;rchild-&gt;val)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">RL_Rotate</span>(head);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> head; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="删除操作"><a href="#删除操作" class="headerlink" title="删除操作"></a>删除操作</h4><p><img data-src="/transblog/images/62092976898535.png"></p><ol><li>蓝色情况：比较简单，直接将其置空，释放，然后返回给父节点即可。</li><li>橙色情况：删除节点有左子树没有右子树。 先保存该结点左子树的地址，delete该结点，然后把地址等于左子树地址， <strong>相当于将原来结点地址覆盖</strong></li><li>黄色情况：删除节点有右子树没有左子树 。与2处理相同，只是将左子树换为右子树。</li><li>绿色情况：既有左子树又有右子树：找到右子树中的最小值，将值赋给当前结点，然后以该最小值为目标继续往右子树寻找并删除结点。</li></ol><p><strong>以上操作完成之后需要判断整体的失衡情况，进行相对的旋转操作</strong>。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">erase</span><span class="params">(T val)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    root = _erase(root, val);</span><br><span class="line">&#125;</span><br><span class="line">Node *_erase(Node *head, T val)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (head == <span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (val &lt; head-&gt;val)</span><br><span class="line">    &#123;</span><br><span class="line">        head-&gt;lchild = _erase(head-&gt;lchild, val);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (val &gt; head-&gt;val)</span><br><span class="line">    &#123;</span><br><span class="line">        head-&gt;rchild = _erase(head-&gt;rchild, val);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (!head-&gt;lchild &amp;&amp; !head-&gt;rchild)</span><br><span class="line">        &#123; <span class="comment">// 无左子树无右子树</span></span><br><span class="line">            <span class="built_in">delete</span> (head);</span><br><span class="line">            head = <span class="literal">nullptr</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (head-&gt;lchild &amp;&amp; !head-&gt;rchild)</span><br><span class="line">        &#123; <span class="comment">// 有左子树无右子树</span></span><br><span class="line">            Node *lc = head-&gt;lchild;</span><br><span class="line">            <span class="built_in">delete</span> (head);</span><br><span class="line">            head = lc;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!head-&gt;lchild &amp;&amp; head-&gt;rchild)</span><br><span class="line">        &#123; <span class="comment">// 无左子树有右子树</span></span><br><span class="line">            Node *rc = head-&gt;rchild;</span><br><span class="line">            <span class="built_in">delete</span> (head);</span><br><span class="line">            head = rc;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123; <span class="comment">// 都有</span></span><br><span class="line">            Node *cur = head-&gt;rchild;</span><br><span class="line">            <span class="keyword">while</span> (cur-&gt;lchild)</span><br><span class="line">            &#123;</span><br><span class="line">                cur = cur-&gt;lchild;</span><br><span class="line">            &#125;</span><br><span class="line">            head-&gt;val = cur-&gt;val;</span><br><span class="line">            head-&gt;rchild = _erase(head-&gt;rchild, cur-&gt;val);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (head == <span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span> head;</span><br><span class="line">    <span class="type">int</span> bal = <span class="built_in">GetHeight</span>(head-&gt;lchild) - <span class="built_in">GetHeight</span>(head-&gt;rchild);</span><br><span class="line">    head-&gt;height = <span class="number">1</span> + std::<span class="built_in">max</span>(<span class="built_in">GetHeight</span>(head-&gt;lchild), <span class="built_in">GetHeight</span>(head-&gt;rchild));</span><br><span class="line">    <span class="keyword">if</span> (bal &gt; <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">GetHeight</span>(head-&gt;lchild-&gt;lchild) &gt;= <span class="built_in">GetHeight</span>(head-&gt;lchild-&gt;rchild))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">RR_Rotate</span>(head);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">LR_Rotate</span>(head);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (bal &lt; <span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">GetHeight</span>(head-&gt;lchild-&gt;lchild) &gt;= <span class="built_in">GetHeight</span>(head-&gt;lchild-&gt;rchild))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">RL_Rotate</span>(head);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">RR_Rotate</span>(head);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="判断操作"><a href="#判断操作" class="headerlink" title="判断操作"></a>判断操作</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//判断一个树是否为平衡二叉树</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">Judge</span><span class="params">(Node* T)</span> </span>&#123;</span><br><span class="line"><span class="keyword">if</span> (!T)&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;NULL\n&quot;</span>);</span><br><span class="line"><span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(T-&gt;left-&gt;data&gt;T-&gt;data||T-&gt;right-&gt;data&lt;T-&gt;data)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">abs</span>(<span class="built_in">GetHeight</span>(T-&gt;left) - <span class="built_in">GetHeight</span>(T-&gt;right)) &gt; <span class="number">1</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line"><span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">Judge</span>(T-&gt;left)&amp;&amp;<span class="built_in">Judge</span>(T-&gt;right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="红黑树-RB"><a href="#红黑树-RB" class="headerlink" title="红黑树(RB)"></a>红黑树(RB)</h3><p>假设全部黑节点有N个</p><ul><li>整棵树的节点数量：$[N, 2N]$之间</li><li>最短路径长度：$O(logN)$</li><li>最长路径长度：$O(2logN)$</li><li>查找效率：$O(2logN)$</li></ul><p>红黑树有以下规则：<br><strong>口决：左根右，根叶红，不红红，黑路同</strong></p><ul><li>规则1：一个节点不是红色就是黑色。</li><li>规则2：红黑树的根节点(root)必须是黑色。</li><li>规则3：如果一个结点是红色的，则它的两个孩子结点必须是黑色的，也就是说任意⼀条路径不会有连续的红色结点。(黑色结点的孩子可能是红也可能是黑，红色结点的孩子只能是黑)</li><li>规则4：对于任意一个结点，从该结点到其所有NULL结点的简单路径上，均包含<strong>相同数量的黑色结点</strong>。</li></ul><p><img data-src="/transblog/images/305607019569861.png"></p><h4 id="红黑树的路径"><a href="#红黑树的路径" class="headerlink" title="红黑树的路径"></a>红黑树的路径</h4><p>《算法导论》等书籍上补充了⼀条每个叶子结点(NIL)都是黑色的规则。它这里所指的叶子结点不是传统的意义上的叶子结点，而是我们说的空结点，有些书籍上也把NIL叫做外部结点。<br><strong>NIL都是黑色既不违反规则3，也不违反规则4</strong>。<br>NIL是为了方便准确的标识出所有路径，《算法导论》在后续讲解实现的细节中也忽略了NIL结点，所以我们知道一下这个概念即可。<br><img data-src="/transblog/images/342567926943166.png"></p><h4 id="红黑树的特性"><a href="#红黑树的特性" class="headerlink" title="红黑树的特性"></a>红黑树的特性</h4><p>红黑树如何确保最长路径不超过最短路径的2倍？</p><ul><li>由规则4可知，从根到NULL结点的每条路径都有相同数量的黑色结点，所以极端场景下，最短路径就是全是黑色结点的路径，假设最短路径长度为bh。</li><li>由规则2和规则3可知，任意一条路径不会有连续的红色结点，所以极端场景下，最长的路径就是一黑一红间隔组成，那么最长路径的长度为2*bh。</li></ul><p>综合红黑树的4点规则而言，理论上的全黑最短路径和一黑一红的最长路径并不是在每棵红黑树都存在的。假设任意一条从根到NULL结点路径的长度为x，那么bh&lt;&#x3D;x&lt;&#x3D;2*bh。</p><h4 id="插入操作-1"><a href="#插入操作-1" class="headerlink" title="插入操作"></a>插入操作</h4><p>红黑树插入一个值的大致过程：</p><ol><li>首先按⼆叉搜索树规则进行插入，插入后我们只需要观察是否符合红黑树的4条规则。</li><li>如果是空树插入，新增结点是黑色结点。如果是非空树插入，<strong>新增结点必须是红色结点，因为非空树插入</strong>，新增黑色结点就破坏了规则4，规则4是难维护的。</li><li>非空树插入后，新增结点必须是红色结点，如果此时父亲结点是黑色的，则没有违反任何规则，插入结束。</li><li>非空树插入后，新增结点必须红色结点，如果父亲结点是红色的，则违反规则3。这种情况是可以维护的，具体维护过程如下分析：</li></ol><h5 id="插入结点的叔叔是红色"><a href="#插入结点的叔叔是红色" class="headerlink" title="插入结点的叔叔是红色"></a>插入结点的叔叔是红色</h5><p>叔父爷变色（红变黑，黑变红），爷爷变插入节点（将爷爷视为插入结点，继续判定是否破坏红黑树的性质）</p><p><img data-src="/transblog/images/173012256743618.png"></p><h5 id="插入结点的叔叔是黑色"><a href="#插入结点的叔叔是黑色" class="headerlink" title="插入结点的叔叔是黑色"></a>插入结点的叔叔是黑色</h5><p>根据（LL、RR、LR、RL）以爷爷作为旋转点进行旋转，然后对旋转点和旋转中心变色</p><h6 id="LL-型"><a href="#LL-型" class="headerlink" title="LL 型"></a>LL 型</h6><p><img data-src="/transblog/images/211181500795484.png"></p><h6 id="RR型"><a href="#RR型" class="headerlink" title="RR型"></a>RR型</h6><p><img data-src="/transblog/images/347913155671739.png"></p><h6 id="LR型"><a href="#LR型" class="headerlink" title="LR型"></a>LR型</h6><p><img data-src="/transblog/images/561352704442024.png"></p><h6 id="RL型"><a href="#RL型" class="headerlink" title="RL型"></a>RL型</h6><p><img data-src="/transblog/images/186070823649922.png"></p><p><a href="https://www.bilibili.com/video/BV1Xm421x7Lg/?spm_id_from=333.788.videopod.sections&vd_source=ca90f6e173ad4e20035c573eba09dd08">点击跳转B站视频，学习更多相关红黑树知识</a></p><h4 id="代码相关"><a href="#代码相关" class="headerlink" title="代码相关"></a>代码相关</h4><h5 id="插入-1"><a href="#插入-1" class="headerlink" title="插入"></a>插入</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">Insert</span><span class="params">(<span class="type">const</span> pair&lt;K, V&gt; &amp;kv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (_root == <span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        _root = <span class="keyword">new</span> <span class="built_in">Node</span>(kv);</span><br><span class="line">        _root-&gt;_col = BLACK;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Node *parent = <span class="literal">nullptr</span>;</span><br><span class="line">    Node *cur = _root;</span><br><span class="line">    <span class="keyword">while</span> (cur)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;_kv.first &lt; kv.first)</span><br><span class="line">        &#123;</span><br><span class="line">            parent = cur;</span><br><span class="line">            cur = cur-&gt;_right;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (cur-&gt;_kv.first &gt; kv.first)</span><br><span class="line">        &#123;</span><br><span class="line">            parent = cur;</span><br><span class="line">            cur = cur-&gt;_left;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cur = <span class="keyword">new</span> <span class="built_in">Node</span>(kv);</span><br><span class="line">    cur-&gt;_col = RED; <span class="comment">// 插入的新结点必须是红色</span></span><br><span class="line">    <span class="keyword">if</span> (parent-&gt;_kv.first &lt; kv.first)</span><br><span class="line">        parent-&gt;_right = cur;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        parent-&gt;_left = cur;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 链接父亲</span></span><br><span class="line">    cur-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 父亲是红色，那么就会出现连续的红色节点，需要处理</span></span><br><span class="line">    <span class="keyword">while</span> (parent &amp;&amp; parent-&gt;_col == RED)</span><br><span class="line">    &#123;</span><br><span class="line">        Node *grandfather = parent-&gt;_parent;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (parent == grandfather-&gt;_left) <span class="comment">// 叔叔(u)在右边</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">//   g</span></span><br><span class="line">            <span class="comment">// p   u</span></span><br><span class="line">            Node *uncle = grandfather-&gt;_right;</span><br><span class="line">            <span class="comment">// 第一种情况：叔叔存在且为红</span></span><br><span class="line">            <span class="keyword">if</span> (uncle &amp;&amp; uncle-&gt;_col == RED)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 变色</span></span><br><span class="line">                parent-&gt;_col = uncle-&gt;_col = BLACK;</span><br><span class="line">                grandfather-&gt;_col = RED;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 继续往上处理</span></span><br><span class="line">                cur = grandfather;</span><br><span class="line">                parent = cur-&gt;_parent;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 第二、三种情况：叔叔不存在或者叔叔存在且为黑</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (cur == parent-&gt;_left) <span class="comment">// 右单旋</span></span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">//     g</span></span><br><span class="line">                    <span class="comment">//   p    (u)</span></span><br><span class="line">                    <span class="comment">// c</span></span><br><span class="line">                    <span class="built_in">RotateR</span>(grandfather);</span><br><span class="line">                    parent-&gt;_col = BLACK; <span class="comment">// 更新颜色</span></span><br><span class="line">                    grandfather-&gt;_col = RED;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> <span class="comment">// 双旋</span></span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">//     g</span></span><br><span class="line">                    <span class="comment">//   p   (u)</span></span><br><span class="line">                    <span class="comment">//     c</span></span><br><span class="line">                    <span class="built_in">RotateL</span>(parent);</span><br><span class="line">                    <span class="built_in">RotateR</span>(grandfather);</span><br><span class="line">                    cur-&gt;_col = BLACK; <span class="comment">// 更新颜色</span></span><br><span class="line">                    grandfather-&gt;_col = RED;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">break</span>; <span class="comment">// 旋转完直接结束，不需要向上再进行判断了</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="comment">// 叔叔(u)在左边，主要逻辑和上面一样，只不过方向变了</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">//   g</span></span><br><span class="line">            <span class="comment">// u   p</span></span><br><span class="line">            Node *uncle = grandfather-&gt;_left;</span><br><span class="line">            <span class="comment">// 叔叔存在且为红 -&gt; 变色即可</span></span><br><span class="line">            <span class="keyword">if</span> (uncle &amp;&amp; uncle-&gt;_col == RED)</span><br><span class="line">            &#123;</span><br><span class="line">                parent-&gt;_col = uncle-&gt;_col = BLACK;</span><br><span class="line">                grandfather-&gt;_col = RED;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 继续往上处理</span></span><br><span class="line">                cur = grandfather;</span><br><span class="line">                parent = cur-&gt;_parent;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="comment">// 叔叔(u)不存在或者存在且为黑</span></span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 情况二、三：叔叔不存在或者存在且为黑 -&gt; 旋转+变色</span></span><br><span class="line">                <span class="comment">//   g</span></span><br><span class="line">                <span class="comment">// (u)   p</span></span><br><span class="line">                <span class="comment">//         c</span></span><br><span class="line">                <span class="keyword">if</span> (cur == parent-&gt;_right) <span class="comment">// 单旋</span></span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="built_in">RotateL</span>(grandfather);</span><br><span class="line">                    parent-&gt;_col = BLACK;</span><br><span class="line">                    grandfather-&gt;_col = RED;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> <span class="comment">// 双旋</span></span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">//     g</span></span><br><span class="line">                    <span class="comment">// (u)   p</span></span><br><span class="line">                    <span class="comment">//     c</span></span><br><span class="line">                    <span class="built_in">RotateR</span>(parent);</span><br><span class="line">                    <span class="built_in">RotateL</span>(grandfather);</span><br><span class="line">                    cur-&gt;_col = BLACK;</span><br><span class="line">                    grandfather-&gt;_col = RED;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>; <span class="comment">// 旋转完直接结束，不需要向上更新</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    _root-&gt;_col = BLACK; <span class="comment">// 特殊处理，插入结束后不管三七二十一直接让根结点为黑(主要是针对情况1)</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h5 id="旋转"><a href="#旋转" class="headerlink" title="旋转"></a>旋转</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 右单旋</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">RotateR</span><span class="params">(Node *parent)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Node *subL = parent-&gt;_left;</span><br><span class="line">    Node *subLR = subL-&gt;_right;</span><br><span class="line"></span><br><span class="line">    parent-&gt;_left = subLR;</span><br><span class="line">    <span class="keyword">if</span> (subLR)</span><br><span class="line">        subLR-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">    Node *pParent = parent-&gt;_parent;</span><br><span class="line"></span><br><span class="line">    subL-&gt;_right = parent;</span><br><span class="line">    parent-&gt;_parent = subL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (parent == _root)</span><br><span class="line">    &#123;</span><br><span class="line">        _root = subL;</span><br><span class="line">        subL-&gt;_parent = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (pParent-&gt;_left == parent)</span><br><span class="line">            pParent-&gt;_left = subL;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            pParent-&gt;_right = subL;</span><br><span class="line"></span><br><span class="line">        subL-&gt;_parent = pParent;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 左单旋</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">RotateL</span><span class="params">(Node *parent)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Node *subR = parent-&gt;_right;</span><br><span class="line">    Node *subRL = subR-&gt;_left;</span><br><span class="line">    parent-&gt;_right = subRL;</span><br><span class="line">    <span class="keyword">if</span> (subRL)</span><br><span class="line">        subRL-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">    Node *parentParent = parent-&gt;_parent;</span><br><span class="line">    subR-&gt;_left = parent;</span><br><span class="line">    parent-&gt;_parent = subR;</span><br><span class="line">    <span class="keyword">if</span> (parentParent == <span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        _root = subR;</span><br><span class="line">        subR-&gt;_parent = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (parent == parentParent-&gt;_left)</span><br><span class="line">            parentParent-&gt;_left = subR;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            parentParent-&gt;_right = subR;</span><br><span class="line">        subR-&gt;_parent = parentParent;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="高度"><a href="#高度" class="headerlink" title="高度"></a>高度</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> _Height(Node* root)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span> (root == <span class="literal">nullptr</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> leftHeight = _Height(root-&gt;_left);</span><br><span class="line"><span class="type">int</span> rightHeight = _Height(root-&gt;_right);</span><br><span class="line"><span class="keyword">return</span> leftHeight &gt; rightHeight ? leftHeight + <span class="number">1</span> : rightHeight + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="结点数量"><a href="#结点数量" class="headerlink" title="结点数量"></a>结点数量</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> _Size(Node* root)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span> (root == <span class="literal">nullptr</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">return</span> _Size(root-&gt;_left) + _Size(root-&gt;_right) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="判断平衡"><a href="#判断平衡" class="headerlink" title="判断平衡"></a>判断平衡</h5><p>这里获取最长路径和最短路径，检查最长路径不超过最短路径的2倍是不可行的，因为就算满足这个条件，红黑树也可能颜色不满足规则，当前暂时没出问题，后续继续插入还是会出问题的。<br>所以我们还是去检查4点规则，满足这4点规则，一定能保证最长路径不超过最短路径的2倍。</p><ol><li>对于规则1，因为每个结点都有颜色，天然实现保证了颜色不是黑色就是红色。</li><li>对于规则2，直接检查根即可。</li><li>对于规则3，前序遍历检查，遇到红色结点查孩子结点的颜色，因为孩子有两个，且不一定存在，所以查起来不太方便，但遇到红色结点，反过来检查父亲的颜色就方便多了。</li><li>对于规则4，前序遍历，遍历过程中用形参记录根到当前结点的blackNum(黑色结点数量)，前序遍历遇到黑色结点就++blackNum，走到空就计算出了⼀条路径的黑色结点数量。再以任意一条路径黑色结点数量作为参考值(refNum)，依次比较即可。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">IsBalance</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (_root == <span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (_root-&gt;_col == RED)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 参考值</span></span><br><span class="line">    <span class="type">int</span> refNum = <span class="number">0</span>;</span><br><span class="line">    Node *cur = _root;</span><br><span class="line">    <span class="keyword">while</span> (cur)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;_col == BLACK)</span><br><span class="line">        &#123;</span><br><span class="line">            ++refNum;</span><br><span class="line">        &#125;</span><br><span class="line">        cur = cur-&gt;_left;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Check</span>(_root, <span class="number">0</span>, refNum);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">Check</span><span class="params">(Node *root, <span class="type">int</span> blackNum, <span class="type">const</span> <span class="type">int</span> refNum)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 前序遍历走到空时，意味着一条路径走完了</span></span><br><span class="line">        <span class="comment">// cout &lt;&lt; blackNum &lt;&lt; endl;</span></span><br><span class="line">        <span class="keyword">if</span> (refNum != blackNum)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;存在黑色结点的数量不相等的路径&quot;</span> &lt;&lt; endl;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 检查孩子不太方便，因为孩子有两个，且不一定存在，反过来检查父亲就方便多了</span></span><br><span class="line">    <span class="keyword">if</span> (root-&gt;_col == RED &amp;&amp; root-&gt;_parent-&gt;_col == RED)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; root-&gt;_kv.first &lt;&lt; <span class="string">&quot;存在连续的红色结点&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (root-&gt;_col == BLACK)</span><br><span class="line">    &#123;</span><br><span class="line">        blackNum++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Check</span>(root-&gt;_left, blackNum, refNum) &amp;&amp; <span class="built_in">Check</span>(root-&gt;_right, blackNum, refNum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="查找结点"><a href="#查找结点" class="headerlink" title="查找结点"></a>查找结点</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//查找结点</span></span><br><span class="line"><span class="function">Node* <span class="title">Find</span><span class="params">(<span class="type">const</span> K&amp; key)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">Node* cur = _root;</span><br><span class="line"><span class="keyword">while</span> (cur)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span> (cur-&gt;_kv.first &lt; key)</span><br><span class="line">cur = cur-&gt;_right;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (cur-&gt;_kv.first &gt; key)</span><br><span class="line">cur = cur-&gt;_left;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line"><span class="keyword">return</span> cur;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="红黑树和AVL树的区别"><a href="#红黑树和AVL树的区别" class="headerlink" title="红黑树和AVL树的区别"></a>红黑树和AVL树的区别</h3><ol><li><p>平衡性的追求：<br>红黑树：追求的是大致平衡，允许左右子树的高度差大于1，但要求从根到叶子的最长路径不超过最短路径的两倍。<br>平衡二叉树（AVL树）：追求的是绝对平衡，要求任意节点的左右子树的高度差的绝对值不超过1。</p></li><li><p>实现复杂度：<br>红黑树：由于追求的是大致平衡，因此其实现相对简单，每次插入最多只需要三次旋转就能达到平衡。<br>平衡二叉树（AVL树）：由于追求绝对平衡，条件比较苛刻，实现起来更为复杂，每次插入新节点后需要旋转的次数不能预知。</p></li><li><p>查找、插入和删除操作的效率：<br>两者在查找、插入和删除操作上的时间复杂度都是O(log n)，其中n是树中元素的数目。但由于红黑树的实现更简单，因此在实际应用中可能具有更高的效率。</p></li><li><p>节点颜色：<br>红黑树：每个节点都带有颜色属性，可以是红色或黑色。颜色信息在维护树的平衡性时起到关键作用。<br>平衡二叉树（AVL树）：节点没有颜色属性，仅通过调整节点的位置（旋转）来维持平衡。</p></li><li><p>应用场景：<br>红黑树：由于其高效的插入、删除和查找操作以及相对简单的实现，被广泛应用于各种数据结构和算法中，如C++ STL中的set和map容器。<br>平衡二叉树（AVL树）：由于其追求绝对平衡的特性，使得它在某些需要严格保证数据平衡性的场景中更为适用。</p></li></ol><table><thead><tr><th>特性</th><th>AVL树</th><th>红黑树</th></tr></thead><tbody><tr><td>平衡性</td><td>高度平衡，严格保证每个节点的左右子树高度差不超过1</td><td>相对AVL树平衡性稍差，但仍然保证树的高度为 O(log n)</td></tr><tr><td>查找</td><td>树高通常比红黑树略低，在查找操作上会稍快</td><td>允许更高的树高，查找操作的性能略逊于 AVL 树</td></tr><tr><td>插入和删除</td><td>需要多次旋转来保持平衡，导致较高的旋转成本</td><td>最多需要进行两次旋转，旋转次数更少，通常更高效</td></tr><tr><td>实现和维护</td><td>插入和删除的实现相对复杂，维护成本较高</td><td>插入和删除的实现和维护相对简单，尤其在删除操作中优势更明显</td></tr></tbody></table><h2 id="B-B-B-树"><a href="#B-B-B-树" class="headerlink" title="B B+ B*树"></a>B B+ B*树</h2><h3 id="B-树"><a href="#B-树" class="headerlink" title="B 树"></a>B 树</h3><p>B树的原英文名是B-tree，所以很多翻译为B-树,就会很多人误以为B-树是一种树、B树是另外一种树。</p><ol><li>所有叶子节点都在同一层级；</li><li>除根结点以外的非叶子结点的儿子数为$[M&#x2F;2, M]$；</li><li>除了根节点和叶子节点外，每个结点存放至少$M&#x2F;2-1$（取上整）和至多$M-1$个关键字；（至少2个关键字）</li><li>根节点必须包含至少2个孩子节点；</li><li>非叶子结点的关键字个数&#x3D;指向儿子的指针个数-1；</li><li>一个节点的所有key值必须是升序排序的；</li></ol><p> 如：（M&#x3D;3）<br><img data-src="/transblog/images/113845696282393.png"></p><h3 id="B-树-1"><a href="#B-树-1" class="headerlink" title="B+ 树"></a>B+ 树</h3><p>B+树是B树的变体，也是一种多路搜索树，其定义基本与B-树相同，除了：</p><ol><li>非叶子结点的子树指针与关键字个数相同；</li><li>非叶子结点的子树指针$P[i]$，指向关键字值属于$[K[i], K[i+1])$的子树（B-树是开区间）；</li><li>为所有叶子结点增加一个链指针；</li><li>所有关键字都在叶子结点出现；</li></ol><p><img data-src="/transblog/images/241896676496006.png"></p><p>B+的搜索与B-树也基本相同，区别是B+树只有达到叶子结点才命中（B-树可以在非叶子结点命中），其性能也等价于在关键字全集做一次二分查找；</p><p><strong>B+的特性</strong>：</p><ul><li>所有关键字都出现在叶子结点的链表中（稠密索引），且链表中的关键字恰好是有序的；</li><li>不可能在非叶子结点命中；</li><li>非叶子结点相当于是叶子结点的索引（稀疏索引），叶子结点相当于是存储（关键字）数据的数据层；</li></ul><p>非关系型数据库索引如mongoDB用的B树，大部分关系型数据库索引使用的是B+树。</p><p><strong>B+树的分裂</strong>：</p><p>当一个结点满时，分配一个新的结点，并将原结点中1&#x2F;2的数据复制到新结点，最后在父结点中增加新结点的指针；B+树的分裂只影响原结点和父结点</p><h3 id="B-树-2"><a href="#B-树-2" class="headerlink" title="B* 树"></a>B* 树</h3><p>是B+树的变体，在B+树的非根和非叶子结点再增加指向兄弟的指针</p><p><img data-src="/transblog/images/346204541233578.png"></p><p>B* 树定义了非叶子结点关键字个数至少为(2&#x2F;3)*M，即块的最低使用率为2&#x2F;3（代替B+树的1&#x2F;2）；</p><p><strong>B* 树的分裂</strong>：</p><p>当一个结点满时，如果它的下一个兄弟结点未满，那么将一部分数据移到兄弟结点中，再在原结点插入关键字，最后修改父结点中兄弟结点的关键字（因为兄弟结点的关键字范围改变了）；如果兄弟也满了，则在原结点与兄弟结点之间增加新结点，并各复制1&#x2F;3的数据到新结点，最后在父结点增加新结点的指针；</p><p>所以，B*树分配新结点的概率比B+树要低，空间使用率更高；</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>B树：多路搜索树，每个结点存储M&#x2F;2到M个关键字，非叶子结点存储指向关键字范围的子结点；所有关键字在整颗树中出现，<strong>且只出现一次</strong>，非叶子结点可以命中；<br>B+树：在B-树基础上，<strong>为叶子结点增加链表指针，所有关键字都在叶子结点中出现</strong>，非叶子结点作为叶子结点的索引；B+树总是到叶子结点才命中；<br>B*树：在B+树基础上，为非叶子结点也增加链表指针，将结点的最低利用率从1&#x2F;2提高到2&#x2F;3；</p><h1 id="跳表"><a href="#跳表" class="headerlink" title="跳表"></a>跳表</h1><p>redis的Zset类型使用了跳表数据结构，<em>更多内容可查看Redis–数据结构–跳表–Zset类型</em></p><p>跳表是在链表基础上改进过来的，实现了一种<strong>多层的有序链表</strong>，这样的好处是能快读定位数据。当数据量很大时，跳表的查找复杂度就是 O(logN)。</p><p><img data-src="/transblog/images/110374418747177.png"></p><h2 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h2><p>跳表查找任意数据的时间复杂度为O(logn)</p><ol><li>从顶层链表的首元素开始，从左往右搜索，直至找到一个大于或等于目标的元素，或者到达当前层链表的尾部</li><li>如果该元素等于目标元素，则表明该元素已被找到</li><li>如果该元素大于目标元素或已到达链表的尾部，则退回到当前层的前一个元素，然后转入下一层进行搜索</li></ol><h2 id="插入-2"><a href="#插入-2" class="headerlink" title="插入"></a>插入</h2><p>跳表插入的时间复杂度为：O(logn)，支持高效的动态插入，插入过程主要涉及三个关键步骤：</p><ol><li><p>确定节点层级<br>首先，我们需要为新插入的节点随机确定其所在的层级<br>同时将这个数据插入到部分索引层中，如何选择索引层，<strong>可以通过一个随机函数来决定这个节点插入到哪几级索引中</strong>，比如随机生成了k，那么就将这个索引加入到，第一级到第k级索引中。</p></li><li><p>寻找插入位置<br>通过之前讨论的搜索方法，我们能够定位到了新节点应当插入的具体位置</p></li><li><p>更新指针关系<br>将新节点在各层的前驱节点（即在该层中小于新节点且最接近新节点的节点）的指针指向新节点。同时，新节点的指针需要指向其在各层的前驱节点原本指向的节点。<br><strong>此操作和单链表的插入操作类似，区别在于跳表需要在多层中的重复进行此操作，而链表只需要进行一次</strong>。</p></li></ol><h2 id="删除-1"><a href="#删除-1" class="headerlink" title="删除"></a>删除</h2><p>跳表的删除操作时间复杂度为：O(logn)，支持动态的删除。</p><ul><li>在跳表中删除某个结点时，如果这个结点在索引中也出现了，我们除了要删除原始链表中的结点，还要删除索引中的。</li><li>在查找要删除的结点的时候，需要拿到删除结点的前驱结点，然后再通过指针操作完成删除（双向链表除外）。</li></ul><h2 id="跳表层数"><a href="#跳表层数" class="headerlink" title="跳表层数"></a>跳表层数</h2><p><strong>跳表的相邻两层的节点数量的比例会影响跳表的查询性能。跳表的相邻两层的节点数量最理想的比例是 2:1，查找复杂度可以降低到 O(logN)</strong></p><p>那怎样才能维持相邻两层的节点数量的比例为 2 : 1 呢？</p><ul><li>如果采用新增节点或者删除节点时，来调整跳表节点以维持比例的方法的话，会带来额外的开销。Redis跳表在创建节点的时候，随机生成每个节点的层数，并没有严格维持相邻两层的节点数量比例为 2 : 1 的情况。</li><li>具体的做法是，跳表在创建节点时候，会生成范围为[0-1]的一个随机数，如果这个随机数小于 0.25（相当于概率 25%），那么层数就增加 1 层，然后继续生成下一个随机数，直到随机数的结果大于 0.25 结束，最终确定该节点的层数。</li><li>这样的做法，相当于每增加一层的概率不超过 25%，层数越高，概率越低，层高限制 7.0 定义为 32，R 5.0 定义为 64， 3.0 定义为 32。</li></ul><p><strong>那么为什么是小于0.25呢</strong>？直接上干货：</p><ul><li>首先，每个节点肯定都有第1层指针（每个节点都在第1层链表里）。</li><li>如果一个节点有第i层(i&gt;&#x3D;1)指针（即节点已经在第1层到第i层链表中），那么它有第(i+1)层指针的概率为p。</li><li>节点最大的层数不允许超过一个最大值，记为MaxLevel（即层高限制）。</li></ul><p>产生越高的节点层数，概率越低,无论如何层数总是满足幂次定律（power law，越大的数出现的概率越小）。定量的分析如下：</p><p>• 节点层数至少为1。而大于1的节点层数，满足一个概率分布。<br>• 节点层数恰好等于1的概率为1-p。<br>• 节点层数大于等于2的概率为p，而节点层数恰好等于2的概率为p(1-p)。<br>……..</p><p>因此，一个节点的平均层数（也即包含的平均指针数目），计算如下：<br>$$<br>1 \times (1 - p) + 2p(1 - p) + 3p^2(1 - p) + 4p^3(1 - p) + … &#x3D; (1 - p) \sum_{k&#x3D;1}^{+\infty} kp^{k-1} &#x3D; (1 - p) \cdot \frac{1}{(1 - p)^2} &#x3D; \frac{1}{1 - p}<br>$$</p><p>当p&#x3D;1&#x2F;2时，每个节点所包含的平均指针数目为2；当p&#x3D;1&#x2F;4时，每个节点所包含的平均指针数目为1.33，你，听懂了吗？</p><h2 id="Redis跳表–Zset"><a href="#Redis跳表–Zset" class="headerlink" title="Redis跳表–Zset"></a>Redis跳表–Zset</h2><h3 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">zskiplist</span> &#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> *header, *tail; <span class="comment">//头尾节点，便于在O(1)时间复杂度内访问跳表的头节点和尾节点；</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> length;                <span class="comment">//长度，便于在O(1)时间复杂度获取跳表节点的数量；</span></span><br><span class="line">    <span class="type">int</span> level;                           <span class="comment">//最大层数，便于在O(1)时间复杂度获取跳表中层高最大的那个节点的层数量；</span></span><br><span class="line">&#125; zskiplist;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> &#123;</span><br><span class="line">    <span class="comment">//Zset 对象的元素值</span></span><br><span class="line">    sds ele;</span><br><span class="line">    <span class="comment">//元素权重值</span></span><br><span class="line">    <span class="type">double</span> score;</span><br><span class="line">    <span class="comment">//后向指针</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> *backward;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">//节点的level数组，保存每层上的前向指针和跨度</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">zskiplistLevel</span> &#123;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> *forward;</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> span;</span><br><span class="line">    &#125; level[];</span><br><span class="line">&#125; zskiplistNode;</span><br></pre></td></tr></table></figure><h3 id="跳表结构"><a href="#跳表结构" class="headerlink" title="跳表结构"></a>跳表结构</h3><p><img data-src="/transblog/images/516273415246960.png"></p><ul><li>每个跳表节点都有一个后向指针（*backward），指向前一个节点，目的是为了方便从跳表的尾节点开始访问节点，这样倒序查找时很方便。</li><li>跳表是一个带有层级关系的链表，而且每一层级可以包含多个节点，每一个节点通过指针连接起来，实现这一特性就是靠跳表节点结构体中的zskiplistLevel 结构体类型的 level 数组。level 数组中的每一个元素代表跳表的一层，也就是由 zskiplistLevel 结构体表示，</li></ul><h3 id="查询过程"><a href="#查询过程" class="headerlink" title="查询过程"></a>查询过程</h3><ul><li>从头节点的最高层开始，在遍历某一层的跳表节点时，会用跳表节点中的 SDS 类型的元素和元素的权重来进行判断</li><li>如果当前节点的权重「小于」要查找的权重时，跳表就会访问该层上的下一个节点。</li><li>如果当前节点的权重「等于」要查找的权重时，并且当前节点的 SDS 类型数据「小于」要查找的数据时，跳表就会访问该层上的下一个节点。</li><li>如果上面两个条件都不满足，或者下一个节点为空时，跳表就会使用目前遍历到的节点的 level 数组里的下一层指针，然后沿着下一层指针继续查找，这就相当于跳到了下一层接着查找。</li></ul><p><img data-src="/transblog/images/167815115256350.png"></p><p>如果要查找「元素：abcd，权重：4」的节点，查找的过程是这样的：</p><ol><li>先从头节点的最高层开始，L2 指向了「元素：abc，权重：3」节点，这个节点的权重比要查找节点的小，所以要访问该层上的下一个节点；</li><li>但是该层的下一个节点是空节点（ leve[2]指向的是空节点），于是就会跳到「元素：abc，权重：3」节点的下一层去找，也就是 leve[1];</li><li>「元素：abc，权重：3」节点的 leve[1] 的下一个指针指向了「元素：abcde，权重：4」的节点，然后将其和要查找的节点比较。虽然「元素：abcde，权重：4」的节点的权重和要查找的权重相同，但是当前节点的 SDS 类型数据「大于」要查找的数据，所以会继续跳到「元素：abc，权重：3」节点的下一层去找，也就是 leve[0]；</li><li>「元素：abc，权重：3」节点的 leve[0] 的下一个指针指向了「元素：abcd，权重：4」的节点，该节点正是要查找的节点，查询结束</li></ol><h1 id="哈希表"><a href="#哈希表" class="headerlink" title="哈希表"></a>哈希表</h1><p>哈希表（Hash Table）：也叫做散列表。是根据关键码值（Key Value）直接进行访问的数据结构。</p><p>哈希表通过「键 key 」和「映射函数 Hash(key) 」计算出对应的「值 value」，把关键码值映射到表中一个位置来访问记录，以加快查找的速度。这个映射函数叫做「哈希函数（散列函数）」，存放记录的数组叫做「哈希表（散列表）」。</p><p><strong>哈希表目的与特性</strong>：<br>数组的最大特点：寻址容易，插入和删除困难；链表的特点正好相反：寻址困难，而插入和删除操作容易。哈希表结合两者的优点，是一个集查找、插入和删除操作于一身的数据结构。</p><h2 id="哈希函数"><a href="#哈希函数" class="headerlink" title="哈希函数"></a>哈希函数</h2><p>哈希函数（Hash Function）：将哈希表中元素的关键键值映射为元素存储位置的函数。哈希函数是哈希表中最重要的部分。</p><p>一般来说，哈希函数会满足以下几个条件：</p><ul><li>哈希函数应该易于计算，并且尽量使计算出来的索引值均匀分布。</li><li>哈希函数计算得到的哈希值是一个固定长度的输出值。</li></ul><p><strong>如果Hash(key1)不等于Hash(key2)，那么 key1、key2 一定不相等</strong>。<br><strong>如果Hash(key1)等于Hash(key2)，那么 key1、key2 可能相等，也可能不相等（会发生哈希碰撞）</strong>。</p><p>一般我们会将各种类型的关键字先转换为整数类型，再通过哈希函数，将其映射到哈希表中。而关于整数类型的关键字，通常用到的哈希函数方法有：直接定址法、除留余数法、平方取中法、基数转换法、数字分析法、折叠法、随机数法、乘积法、点积法等。</p><h3 id="直接定址法"><a href="#直接定址法" class="headerlink" title="直接定址法"></a>直接定址法</h3><p>取关键字本身 &#x2F; 关键字的某个线性函数值 作为哈希地址。</p><p>即：$Hash(key) &#x3D; key$ 或者 $Hash(key) &#x3D; a * key + b$，其中 $a$ 和 $b$ 为常数。</p><p>这种方法计算最简单，且不会产生冲突。适合于关键字分布基本连续的情况，如果关键字分布不连续，空位较多，则会造成存储空间的浪费。</p><ul><li>优点：以关键码key的某个线性函数值为哈希地址，不会产生冲突；</li><li>缺点：要占用连续地址空间，空间效率低。</li></ul><h3 id="除留余数法"><a href="#除留余数法" class="headerlink" title="除留余数法"></a>除留余数法</h3><p>假设哈希表的表长为 $m$，取一个不大于 $m$ 但接近或等于 $m$ 的质数 p，利用取模运算，将关键字转换为哈希地址。即：$Hash(key) &#x3D; key%p$，其中 $p$ 为不大于 $m$ 的质数。</p><p>这也是一种简单且常用的哈希函数方法。其关键点在于 $p$ 的选择。根据经验而言，一般 $p$ 取素数或者 $m$，这样可以尽可能的减少冲突。</p><ul><li>特点：以关键码除以p的余数作为哈希地址。</li><li>技巧：若设计的哈希表长为m，则一般取$p≤m$且为质数 （也可以是不包含小于20质因子的合数）。</li></ul><h3 id="平方取中法"><a href="#平方取中法" class="headerlink" title="平方取中法"></a>平方取中法</h3><p>先通过求关键字平方值的方式扩大相近数之间的差别，然后根据表长度，取关键字平方值的中间几位数为哈希地址。<br>理由：因为中间几位与数据的每一位都相关。</p><blockquote><p>例：2589的平方值为6702921，可以取中间的029为地址。</p></blockquote><p>这种方法因为关键字平方值的中间几位数和原关键字的每一位数都相关，所以产生的哈希地址也比较均匀，有利于减少冲突的发生。</p><h2 id="哈希冲突"><a href="#哈希冲突" class="headerlink" title="哈希冲突"></a>哈希冲突</h2><p>哈希冲突（Hash Collision）：不同的关键字通过同一个哈希函数可能得到同一哈希地址，即 $key1 ≠ key2$，而 $Hash(key1) &#x3D; Hash(key2)$，这种现象称为哈希冲突。</p><p>理想状态下，我们的哈希函数是完美的一对一映射，即一个关键字（key）对应一个值（value），不需要处理冲突。但是一般情况下，不同的关键字 key 可能对应了同一个值 value，这就发生了哈希冲突。</p><p>设计再好的哈希函数也无法完全避免哈希冲突。所以就需要通过一定的方法来解决哈希冲突问题</p><h3 id="开放地址法"><a href="#开放地址法" class="headerlink" title="开放地址法"></a>开放地址法</h3><p>指的是将哈希表中的「空地址」向处理冲突开放。当哈希表未满时，处理冲突时需要尝试另外的单元，直到找到空的单元为止。</p><p><img data-src="/transblog/images/435884609258196.png"></p><blockquote><p>例如，在长度为 11 的哈希表中已经填有关键字分别为 28、49、18 的记录（哈希函数为 $Hash(key) &#x3D; key%11$）。现在将插入关键字为 38 的新纪录。根据哈希函数得到的哈希地址为 5，产生冲突。</p></blockquote><h4 id="线性探测"><a href="#线性探测" class="headerlink" title="线性探测"></a>线性探测</h4><p>按顺序决定值时，如果某数据的值已经存在，则在原来值的基础上往后加一个单位，直至不发生哈希冲突。</p><blockquote><p>得到下一个地址 $H(1) &#x3D; (5 + 1) % 11 &#x3D; 6$，仍然冲突；继续求出 $H(2) &#x3D; (5 + 2) % 11 &#x3D; 7$，仍然冲突；继续求出 $H(3) &#x3D; (5 + 3) % 11 &#x3D; 8$，8 对应的地址为空，处理冲突过程结束，记录填入哈希表中序号为 8 的位置。　</p></blockquote><h4 id="再平方探测"><a href="#再平方探测" class="headerlink" title="再平方探测"></a>再平方探测</h4><p>按顺序决定值时，如果某数据的值已经存在，则在原来值的基础上先加1的平方个单位，若仍然存在则减1的平方个单位。随之是2的平方，3的平方等等。直至不发生哈希冲突。</p><blockquote><p>得到下一个地址 $H(1) &#x3D; (5 + 1 \times 1) % 11 &#x3D; 6$，仍然冲突；继续求出 $H(2) &#x3D; (5 - 1 \times 1) % 11 &#x3D; 4$，4 对应的地址为空，处理冲突过程结束，记录填入哈希表中序号为 4 的位置。</p></blockquote><h4 id="伪随机探测"><a href="#伪随机探测" class="headerlink" title="伪随机探测"></a>伪随机探测</h4><p>按顺序决定值时，如果某数据已经存在，通过随机函数随机生成一个数，在原来值的基础上加上随机数，直至不发生哈希冲突。</p><blockquote><p>假设伪随机数为 9，则得到下一个地址 $H(1) &#x3D; (9 + 5) % 11 &#x3D; 3$，3 对应的地址为空，处理冲突过程结束，记录填入哈希表中序号为 3 的位置。</p></blockquote><h3 id="链式地址法"><a href="#链式地址法" class="headerlink" title="链式地址法"></a>链式地址法</h3><p>HashMap的哈希冲突解决方法<br>对于相同的值，使用链表进行连接。使用数组存储每一个链表。<br><strong>优点</strong>：</p><ul><li>拉链法处理冲突简单，且无堆积现象，即非同义词决不会发生冲突，因此平均查找长度较短；</li><li>由于拉链法中各链表上的结点空间是动态申请的，故它更适合于造表前无法确定表长的情况；</li><li>开放定址法为减少冲突，要求装填因子α较小，故当结点规模较大时会浪费很多空间。而拉链法中可取α≥1，且结点较大时，拉链法中增加的指针域可忽略不计，因此节省空间；</li><li>在用拉链法构造的散列表中，删除结点的操作易于实现。只要简单地删去链表上相应的结点即可。</li></ul><p><strong>缺点</strong>：</p><ul><li>指针占用较大空间时，会造成空间浪费，若空间用于增大散列表规模进而提高开放地址法的效率。</li></ul><h3 id="建立公共溢出区"><a href="#建立公共溢出区" class="headerlink" title="建立公共溢出区"></a>建立公共溢出区</h3><p>建立公共溢出区存储所有哈希冲突的数据。</p><h3 id="再哈希法"><a href="#再哈希法" class="headerlink" title="再哈希法"></a>再哈希法</h3><p>对于冲突的哈希值再次进行哈希处理，直至没有哈希冲突。</p><h1 id="———-请稍等————–"><a href="#———-请稍等————–" class="headerlink" title="———-请稍等————–"></a>———-请稍等————–</h1><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;树&quot;&gt;&lt;a href=&quot;#树&quot; class=&quot;headerlink&quot; title=&quot;树&quot;&gt;&lt;/a&gt;树&lt;/h1&gt;&lt;h2 id=&quot;二叉树&quot;&gt;&lt;a href=&quot;#二叉树&quot; class=&quot;headerlink&quot; title=&quot;二叉树&quot;&gt;&lt;/a&gt;二叉树&lt;/h2&gt;&lt;p&gt;每个结</summary>
      
    
    
    
    <category term="四件套" scheme="https://trans-xu.github.io/transblog/categories/%E5%9B%9B%E4%BB%B6%E5%A5%97/"/>
    
    
  </entry>
  
  <entry>
    <title>操作系统</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/</id>
    <published>2025-08-27T03:50:08.909Z</published>
    <updated>2025-08-27T03:36:35.303Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Linux内核源码地址"><a href="#Linux内核源码地址" class="headerlink" title="Linux内核源码地址"></a>Linux内核源码地址</h1><p><a href="https://elixir.bootlin.com/linux/v3.8.7/source">Linux3.8.7内核源码链接</a></p><h1 id="局部性原理"><a href="#局部性原理" class="headerlink" title="局部性原理"></a>局部性原理</h1><p><strong>程序在执行时呈现出局部性规律，即在一较短的时间内，程序的执行仅局限于某个部分；相应的它所访问的空间也局限于某个区域</strong></p><ul><li>时间局部性：如果一个信息项正在被访问，那么在近期它很可能还会被再次访问。</li><li>空间局部性：在最近的将来将用到的信息很可能与正在使用的信息在空间地址上是临近的。</li><li>顺序局部性：在典型程序中，除转移类指令外，大部分指令是顺序进行的。</li></ul><h1 id="初识操作系统"><a href="#初识操作系统" class="headerlink" title="初识操作系统"></a>初识操作系统</h1><p>现代操作系统，内核⼀般会提供 4 个基本能⼒：</p><ol><li>进程调度：决定哪个进程、线程使⽤ CPU，也就是进程调度的能⼒；</li><li>内存管理：决定内存的分配和回收，也就是内存管理的能⼒；</li><li>设管理备：为进程与硬件设备之间提供通信能⼒，也就是硬件通信能⼒；</li><li>系统调⽤：如果应⽤程序要运⾏更⾼权限运⾏的服务，那么就需要有系统调⽤，它是⽤户程序与操作系统之间的接⼝。</li></ol><h2 id="内核"><a href="#内核" class="headerlink" title="内核"></a>内核</h2><p>对于内核的架构一般有这三种类型：</p><ul><li>宏内核，包含多个模块，一个完整的可执行程序，且拥有最高的权限；内核的所有代码，包括子系统（如内存管理、文件管理、设备驱动程序）都打包到一个文件中。内核中的每一个函数都可以访问到内核中所有其他部分。目前支持模块的动态装卸(裁剪)。宏内核的特征是系统内核的所有模块，比如进程调度、内存管理、文件系统、设备驱动等，都运行在内核态。</li><li>微内核，有一个最小版本的内核，一些模块和服务则由用户态管理；最基本的功能由中央内核（微内核）实现。所有其他的功能都委托给一些独立进程，这些进程通过明确定义的通信接口与中心内核通信</li><li>混合内核，是宏内核和微内核的结合体，内核中抽象出了微内核的概念，也就是内核中会有一个小型的内核，其他模块就在这个基础上搭建，整个内核是个完整的程序；</li></ul><p><strong>Window 的内核设计是混合型内核</strong>，<strong>Linux 的内核是宏内核</strong></p><h3 id="内核态和用户态"><a href="#内核态和用户态" class="headerlink" title="内核态和用户态"></a>内核态和用户态</h3><p>内核态与用户态是操作系统的两种运行级别，intel cpu提供Ring0-Ring3三种级别的运行模式，Ring0级别最高，Ring3最低。<strong>Linux使用了Ring3级别运行用户态，Ring0作为内核态</strong>，没有使用Ring1和Ring2。Ring3状态不能访问Ring0的地址空间，包括代码和数据。</p><p><img data-src="/transblog/images/491498966573150.png"></p><ul><li>当一个进程执行系统调用而陷入内核代码中执行时，我们就称进程处于内核态。此时处理器处于特权级最高的（0级）内核代码中执行，执行的内核代码会使用当前进程的内核栈。<strong>每个进程都有自己的内核栈</strong>。</li><li>当进程在执行用户自己的代码时，则称其处于用户态。即此时处理器在特权级最低的（3级）用户代码中运行。</li><li>当正在执行用户程序而突然被中断程序中断时，此时用户程序也可以象征性地称为处于进程的内核态。因为中断处理程序将使用当前进程的内核栈。这与处于内核态的进程的状态有些类似。</li></ul><p>用户运行一个程序，该程序所创建的进程开始是运行在用户态的，如果要执行文件操作，网络数据发送等操作，必须通过write，send等系统调用。<br>这些系统调用会调用内核中的代码来完成操作，这时，必须切换到Ring0，然后进入3GB-4GB中的内核地址空间去执行这些代码完成操作（<em>Linux进程的4GB地址空间，3G-4G部分大家是共享的，是内核态的地址空间，这里存放在整个内核的代码和所有的内核模块，以及内核所维护的数据</em>），完成后，切换回Ring3，回到用户态。<br>这样，用户态的程序就不能随意操作内核地址空间，具有一定的安全保护作用。通过内存页表操作等机制，保证进程间的地址空间不会互相冲突，一个进程的操作不会修改另一个进程的地址空间中的数据。</p><h3 id="用户态-内核态"><a href="#用户态-内核态" class="headerlink" title="用户态-&gt;内核态"></a>用户态-&gt;内核态</h3><p><strong>用户态到内核态的方式</strong>：</p><ol><li>系统调用<br>用户态进程通过「系统调用」申请使用操作系统提供的服务程序完成工作，系统调用的机制其核心还是使用了操作系统为用户特别开放的一个中断来实现。<br>当程序需要访问硬件资源的时候，比如内存、硬盘等，就需要通过「系统调用」陷入到内核中，才能访问这些特权资源。<br>系统调用可以理解为内核实现的函数，比如应用程序要通过网卡接收数据，会调用 Socket 的 read 函数。</li></ol><p><img data-src="/transblog/images/155561943319901.png"></p><ol start="2"><li>异常<br>当CPU在执行运行在用户态下的程序时，发生了某些事先不可知的异常，这时会触发由当前运行进程切换到处理此异常的内核相关程序中，也就转到了内核态，比如缺页异常。  </li><li>外围设备的中断<br>当外围设备完成用户请求的操作后，会向CPU发出相应的中断信号，这时CPU会暂停执行下一条即将要执行的指令转而去执行与中断信号对应的处理程序，如果先前执行的指令是用户态下的程序，那么这个转换的过程自然也就发生了由用户态到内核态的切换。比如硬盘读写操作完成，系统会切换到硬盘读写的中断处理程序中执行后续操作等。</li></ol><h2 id="Linux内核体系结构"><a href="#Linux内核体系结构" class="headerlink" title="Linux内核体系结构"></a>Linux内核体系结构</h2><p>系统调用接口、进程管理、内存管理、虚拟文件系统、网络堆栈、设备驱动程序、硬件架构的相关代码。</p><h3 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h3><p>SCI 层提供了某些机制执行从用户空间到内核的函数调用。正如前面讨论的一样，这个接口依赖于体系结构，甚至在相同的处理器家族内也是如此。SCI 实际上是一个非常有用的函数调用多路复用和多路分解服务。在 .&#x2F;linux&#x2F;kernel 中可以找到 SCI 的实现，并在 .&#x2F;linux&#x2F;arch 中找到依赖于体系结构的部分。</p><h3 id="进程管理"><a href="#进程管理" class="headerlink" title="进程管理"></a>进程管理</h3><p>进程管理的重点是进程的执行。在内核中，这些进程称为线程，代表了单独的处理器虚拟化（线程代码、数据、堆栈和 CPU 寄存器）。在用户空间，通常使用进程 这个术语，不过 Linux 实现并没有区分这两个概念（进程和线程）。内核通过 SCI 提供了一个应用程序编程接口（API）来创建一个新进程（fork、exec 或 Portable Operating System Interface [POSIX] 函数），停止进程（kill、exit），并在它们之间进行通信和同步（signal 或者 POSIX 机制）。</p><p>进程管理还包括处理活动进程之间共享 CPU 的需求。内核实现了一种新型的调度算法，不管有多少个线程在竞争 CPU，这种算法都可以在固定时间内进行操作。这种算法就称为 O(1) 调度程序，这个名字就表示它调度多个线程所使用的时间和调度一个线程所使用的时间是相同的。O(1) 调度程序也可以支持多处理器（称为对称多处理器或 SMP）。可以在 .&#x2F;linux&#x2F;kernel 中找到进程管理的源代码，在 .&#x2F;linux&#x2F;arch 中可以找到依赖于体系结构的源代码。</p><h3 id="内存管理"><a href="#内存管理" class="headerlink" title="内存管理"></a>内存管理</h3><p>内核所管理的另外一个重要资源是内存。为了提高效率，如果由硬件管理虚拟内存，内存是按照所谓的内存页 方式进行管理的（对于大部分体系结构来说都是 4KB）。Linux 包括了管理可用内存的方式，以及物理和虚拟映射所使用的硬件机制。不过内存管理要管理的可不止 4KB 缓冲区。Linux 提供了对 4KB 缓冲区的抽象，例如 slab 分配器。这种内存管理模式使用 4KB 缓冲区为基数，然后从中分配结构，并跟踪内存页使用情况，比如哪些内存页是满的，哪些页面没有完全使用，哪些页面为空。这样就允许该模式根据系统需要来动态调整内存使用。为了支持多个用户使用内存，有时会出现可用内存被消耗光的情况。由于这个原因，页面可以移出内存并放入磁盘中。这个过程称为交换，因为页面会被从内存交换到硬盘上。内存管理的源代码可以在 .&#x2F;linux&#x2F;mm 中找到。</p><h3 id="虚拟文件系统"><a href="#虚拟文件系统" class="headerlink" title="虚拟文件系统"></a>虚拟文件系统</h3><p>虚拟文件系统（VFS）是 Linux 内核中非常有用的一个方面，因为它为文件系统提供了一个通用的接口抽象。VFS 在 SCI 和内核所支持的文件系统之间提供了一个交换层（请参看图4）。</p><h2 id="Linux文件系统层次结构"><a href="#Linux文件系统层次结构" class="headerlink" title="Linux文件系统层次结构"></a>Linux文件系统层次结构</h2><p>在 VFS 上面，是对诸如 open、close、read 和 write 之类的函数的一个通用 API 抽象。在 VFS 下面是文件系统抽象，它定义了上层函数的实现方式。它们是给定文件系统（超过 50 个）的插件。文件系统的源代码可以在 .&#x2F;linux&#x2F;fs 中找到。文件系统层之下是缓冲区缓存，它为文件系统层提供了一个通用函数集（与具体文件系统无关）。这个缓存层通过将数据保留一段时间（或者随即预先读取数据以便在需要是就可用）优化了对物理设备的访问。缓冲区缓存之下是设备驱动程序，它实现了特定物理设备的接口。</p><h3 id="网络堆栈"><a href="#网络堆栈" class="headerlink" title="网络堆栈"></a>网络堆栈</h3><p>网络堆栈在设计上遵循模拟协议本身的分层体系结构。回想一下，Internet Protocol (IP) 是传输协议（通常称为传输控制协议或 TCP）下面的核心网络层协议。TCP 上面是 socket 层，它是通过 SCI 进行调用的。</p><p>socket 层是网络子系统的标准 API，它为各种网络协议提供了一个用户接口。从原始帧访问到 IP 协议数据单元（PDU），再到 TCP 和 User Datagram Protocol (UDP)，socket 层提供了一种标准化的方法来管理连接，并在各个终点之间移动数据。内核中网络源代码可以在 .&#x2F;linux&#x2F;net 中找到。</p><p>网卡是计算机里的一个硬件，专门负责接收和发送网络包，当网卡接收到一个网络包后，会通过 DMA 技术，将网络包放入到 Ring Buffer，这个是一个环形缓冲区。Linux 内核在 2.6 版本中引入了 NAPI 机制，它是混合「中断和轮询」的方式來接收网络包，它的核心概念就是<mark>不采用中断的方式读取数据</mark>，而是首先<mark>采用中断唤醒数据接收的服务程序</mark>，然后 poll 的方法来轮询数据。</p><p>比如，当有网络包到达时，网卡发起硬件中断，于是会执行网卡硬件中断处理函数，中断处理函数处理完需要「暂时屏蔽中断」，然后唤醒「软中断」来轮询处理数据，直到没有新数据时才恢复中断，这样一次中断处理多个网络包，于是就可以降低网卡中断带来的性能开销。</p><h3 id="设备驱动程序"><a href="#设备驱动程序" class="headerlink" title="设备驱动程序"></a>设备驱动程序</h3><p>Linux 内核中有大量代码都在设备驱动程序中，它们能够运转特定的硬件设备。Linux 源码树提供了一个驱动程序子目录，这个目录又进一步划分为各种支持设备，例如 Bluetooth、I2C、serial 等。设备驱动程序的代码可以在 .&#x2F;linux&#x2F;drivers 中找到。</p><h3 id="依赖体系结构的代码"><a href="#依赖体系结构的代码" class="headerlink" title="依赖体系结构的代码"></a>依赖体系结构的代码</h3><p>尽管 Linux 很大程度上独立于所运行的体系结构，但是有些元素则必须考虑体系结构才能正常操作并实现更高效率。.&#x2F;linux&#x2F;arch 子目录定义了内核源代码中依赖于体系结构的部分，其中包含了各种特定于体系结构的子目录（共同组成了 BSP）。对于一个典型的桌面系统来说，使用的是 x86 目录。每个体系结构子目录都包含了很多其他子目录，每个子目录都关注内核中的一个特定方面，例如引导、内核、内存管理等。这些依赖体系结构的代码可以在 .&#x2F;linux&#x2F;arch 中找到。</p><p>如果 Linux 内核的可移植性和效率还不够好，Linux 还提供了其他一些特性，它们无法划分到上面的分类中。作为一个生产操作系统和开源软件，Linux 是测试新协议及其增强的良好平台。Linux 支持大量网络协议，包括典型的 TCP&#x2F;IP，以及高速网络的扩展（大于 1 Gigabit Ethernet [GbE] 和 10 GbE）。Linux 也可以支持诸如流控制传输协议（SCTP）之类的协议，它提供了很多比 TCP 更高级的特性（是传输层协议的接替者）。</p><p>Linux 还是一个动态内核，支持动态添加或删除软件组件。被称为动态可加载内核模块，它们可以在引导时根据需要（当前特定设备需要这个模块）或在任何时候由用户插入。</p><p>Linux 最新的一个增强是可以用作其他操作系统的操作系统（称为系统管理程序）。最近，对内核进行了修改，称为基于内核的虚拟机（KVM）。这个修改为用户空间启用了一个新的接口，它可以允许其他操作系统在启用了 KVM 的内核之上运行。除了运行 Linux 的其他实例之外， Microsoft Windows也可以进行虚拟化。惟一的限制是底层处理器必须支持新的虚拟化指令。</p><h1 id="CPU"><a href="#CPU" class="headerlink" title="CPU"></a>CPU</h1><h2 id="CPU三级缓存"><a href="#CPU三级缓存" class="headerlink" title="CPU三级缓存"></a>CPU三级缓存</h2><p>cat &#x2F;sys&#x2F;devices&#x2F;system&#x2F;cpu&#x2F;cpu0&#x2F;cache&#x2F;index3&#x2F;size</p><h2 id="CPU性能"><a href="#CPU性能" class="headerlink" title="CPU性能"></a>CPU性能</h2><p><img data-src="/transblog/images/63874010246117.png"></p><h2 id="CPU读取数据方式"><a href="#CPU读取数据方式" class="headerlink" title="CPU读取数据方式"></a>CPU读取数据方式</h2><p><img data-src="/transblog/images/487145910246742.png"></p><h2 id="CPU的缓存写入"><a href="#CPU的缓存写入" class="headerlink" title="CPU的缓存写入"></a>CPU的缓存写入</h2><p>在什么时机才把 Cache 中的数据写回到内存？<br><strong>写直达：</strong><br>把数据同时写入内存和 Cache 中</p><p><img data-src="/transblog/images/519060711266908.png"></p><p><strong>写回</strong><br>写错时获取策略 ：当发生写操作时，新的数据仅仅被写入 Cache Block 里，只有当修改过的 Cache Block「被替换」时才需要写到内存中，减少了数据写回内存的频率</p><p><img data-src="/transblog/images/475621011259577.png"></p><p>内存读取？<br>保持独占机制</p><h2 id="CPU缓存一致性"><a href="#CPU缓存一致性" class="headerlink" title="CPU缓存一致性"></a>CPU缓存一致性</h2><p><img data-src="/transblog/images/227005311256132.png"></p><p>第一点，某个 CPU 核心里的 Cache 数据更新时，必须要传播到其他核心的 Cache，这个称为<strong>写传播</strong>；<br>第二点，某个 CPU 核心里对数据的操作顺序，必须在其他核心看起来顺序是一样的，这个称为<strong>事务的串行化</strong>。</p><p><strong>解决方式</strong><br>写失效：当一个CPU修改了数据，如果其他CPU有该数据，则通知其为无效；<br>写更新：当一个CPU修改了数据，如果其他CPU有该数据，则通知其更新新数据，（写更新会导致大量的更新操作）。</p><h3 id="总线嗅探"><a href="#总线嗅探" class="headerlink" title="总线嗅探"></a>总线嗅探</h3><p><strong>属于写更新</strong><br>当 A 号 CPU 核心修改了 L1 Cache 中 i 变量的值，通过总线把这个事件广播通知给其他所有的核心，然后每个 CPU 核心都会监听总线上的<strong>广播事件</strong>，并检查是否有相同的数据在自己的 L1 Cache 里面，如果 B 号 CPU 核心的 L1 Cache 中有该数据，那么也需要把该数据更新到自己的 L1 Cache</p><h3 id="MESI-协议"><a href="#MESI-协议" class="headerlink" title="MESI 协议"></a>MESI 协议</h3><p><strong>属于写失效，写回</strong></p><ul><li>Modified，已修改<br>缓存行是脏的（dirty），与内存的值不同。如果别的CPU内核要读内存这块数据，该缓存行必须回写到内存，状态变为共享(S).</li><li>Exclusive，独占<br>缓存行只在当前缓存中，但是干净的——缓存数据相同于内存数据。当别的缓存读取它时，状态变为共享；当前写数据时，变为已修改状态。</li><li>Shared，共享<br>缓存行也存在于其它缓存中且是未修改的。缓存行可以在任意时刻抛弃。</li><li>Invalidated，已失效<br>缓存行是无效的</li></ul><p>当块标记为M（已修改）时，其他高速缓存中块的副本必须标记为I（无效）。</p><p><img data-src="/transblog/images/562890211252452.png"></p><ol><li>当一个处理器需要访问某个内存数据时，它首先会检查自己的缓存中是否有该数据的副本。如果缓存中没有该数据的副本，则会发出一个缓存不命中<strong>（miss）</strong>请求，从主内存中获取该数据的副本，并将该数据的副本存储到自己的缓存中。</li><li><strong>当一个处理器发出miss请求时</strong>，如果该数据的副本已经存在于另一个处理器或核心的缓存中（即处于共享状态），则该处理器可以<strong>从另一个处理器的缓存中复制该数据的副本</strong>。这个过程称为缓存到缓存复制（cache-to-cache transfer）。</li><li>如果两个缓存都处于修改状态，那么必须先将其中一个缓存的数据写回到主内存，然后才能进行缓存到缓存复制。</li></ol><h2 id="CPU软中断"><a href="#CPU软中断" class="headerlink" title="CPU软中断"></a>CPU软中断</h2><p>Linux 系统为了解决中断处理程序执行过长和中断丢失的问题，将中断过程分成了两个阶段，分别是「上半部和下半部分」。</p><p>上半部用来快速处理中断，一般会暂时关闭中断请求，主要负责处理跟硬件紧密相关或者时间敏感的事情。<br>下半部用来延迟处理上半部未完成的工作，一般以「内核线程」的方式运行。</p><h1 id="内存管理-1"><a href="#内存管理-1" class="headerlink" title="内存管理"></a>内存管理</h1><h2 id="为什么要有虚拟内存？"><a href="#为什么要有虚拟内存？" class="headerlink" title="为什么要有虚拟内存？"></a>为什么要有虚拟内存？</h2><p>局部性原理<br>单片机的 CPU 是直接操作内存的「物理地址」，要想在内存中同时运行两个程序是不可能的。如果第一个程序在某个位置写入一个新的值，将会擦掉第二个程序存放在相同位置上的所有内容</p><p>操作系统会提供一种机制，将不同进程的虚拟地址和不同内存的物理地址映射起来。</p><h2 id="虚拟内存"><a href="#虚拟内存" class="headerlink" title="虚拟内存"></a>虚拟内存</h2><h3 id="内存分段"><a href="#内存分段" class="headerlink" title="内存分段"></a>内存分段</h3><p>分段机制下的虚拟地址由两部分组成，<strong>段选择因子</strong>和<strong>段内偏移量</strong>。</p><p><img data-src="/transblog/images/5213036139083.png"></p><p><img data-src="/transblog/images/1622019696606.png"></p><h4 id="缺点："><a href="#缺点：" class="headerlink" title="缺点："></a>缺点：</h4><ol><li>存在内存碎⽚的问题<br>如果这个 256MB 不是连续的，被分成了两段 128 MB 内存，这就会导致没有空间再打开⼀个200MB 的程序。</li></ol><p><img data-src="/transblog/images/701232485698.png"></p><p>解决外部内存碎⽚的问题就是内存交换。这个内存交换空间，在 Linux 系统⾥，也就是我们常看到的 Swap 空间，这块空间是从硬盘划分出来的，⽤于内存与硬盘的空间交换。<br>2. 可能内存交换的效率低的问题<br>如果内存交换的时候，交换的是⼀个占内存空间很⼤的程序，这样整个机器都会显得卡顿。</p><h3 id="内存分页"><a href="#内存分页" class="headerlink" title="内存分页"></a>内存分页</h3><p>分页是把整个虚拟和物理内存空间切成一段段固定尺寸的大小。这样一个连续并且尺寸固定的内存空间，叫页。在 Linux 下，每一页的大小为 4KB。</p><p><strong>分页的最大作用就在于：一个程序的一个页面可以存放在任意一个物理页面里，使得进程的物理地址空间可以是非连续的</strong>。</p><p>虚拟地址与物理地址之间通过页表来映射，如下图：</p><p><img data-src="/transblog/images/592690815251886.png"></p><p><img data-src="/transblog/images/7861115269766.png"></p><p><strong>页号</strong>和<strong>页内偏移</strong>。页号作为页表的索引，页表包含物理页每页所在物理内存的基地址，这个基地址与页内偏移的组合就形成了物理内存地址</p><p><img data-src="/transblog/images/4368924759853.png"></p><p>详细转换如下图：</p><p><img data-src="/transblog/images/2931454446495.png"></p><h4 id="缺点：-1"><a href="#缺点：-1" class="headerlink" title="缺点："></a>缺点：</h4><p>内存碎片：<br>当进程访问的虚拟地址在⻚表中查不到时，系统会产⽣⼀个<strong>缺⻚异常</strong>，进⼊系统内核空间分配物理内存、更新进程⻚表，最后再返回⽤户空间，恢复进程的运⾏采⽤了分⻚，那么释放的内存都是以⻚为单位释放的，也就不会产⽣⽆法给进程使⽤的⼩内存。<br>但是，因为内存分页机制分配内存的最小单位是一页，即使程序不足一页大小，我们最少只能分配一个页，所以页内会出现内存浪费，所以针对内存分页机制会有<strong>内部内存碎片</strong>的现象。</p><p>空间上的缺陷：<br>在 32 位的环境下，虚拟地址空间共有 4GB，假设一个页的大小是 4KB（2^12），一个进程的页表需要装下 100 多万个「页表项」（2^20） ，每个「页表项」需要 4 个字节大小来存储，那么整个 4GB 空间的映射就需要有 4MB 的内存来存储页表。</p><p>这 4MB 大小的页表，看起来也不是很大。但是要知道每个进程都是有自己的虚拟地址空间的，也就说都有自己的页表。那么，100 个进程的话，就需要 400MB 的内存来存储页表</p><h3 id="多级分页"><a href="#多级分页" class="headerlink" title="多级分页"></a>多级分页</h3><p>将页表（一级页表）分为 1024 个页表（二级页表），每个表（二级页表）中包含 1024 个「页表项」，形成二级分页。如下图所示：</p><p><img data-src="/transblog/images/40059992973.png"></p><p><strong>映射 4GB 地址空间需要 4KB（一级页表）+ 4MB（二级页表）的内存，更大的空间？（局部性原理的充分应用）</strong><br><strong>局部性原理：</strong><br>每个进程都有 4GB 的虚拟地址空间，对于大多数程序来说，其使用到的空间远未达到 4GB，因为会存在部分对应的页表项都是空的，根本没有分配，<br>对于已分配的页表项，如果存在最近一定时间未访问的页表，在物理内存紧张的情况下，操作系统会将页面换出到硬盘，也就是说不会占用物理内存。</p><p><strong>用时分配：</strong><br>如果使用了二级分页，一级页表就可以覆盖整个 4GB 虚拟地址空间，但<strong>如果某个一级页表的页表项没有被用到，也就不需要创建这个页表项对应的二级页表了，即可以在需要时才创建二级页表。</strong><br>例如：假设只有 20% 的一级页表项被用到了，那么页表占用的内存空间就只有 4KB（一级页表） + 20% * 4MB（二级页表）&#x3D; 0.804MB &lt; 4MB</p><p>总结：<br>页表一定要覆盖全部虚拟地址空间，不分级的页表就需要有 100 多万个页表项来映射，而二级分页则只需要 1024 个页表项（此时一级页表覆盖到了全部虚拟地址空间，二级页表在需要时创建）。</p><h4 id="TLB技术"><a href="#TLB技术" class="headerlink" title="TLB技术"></a>TLB技术</h4><p><strong>局部性原理的充分应用</strong><br>所以，把最常访问的几个页表项存储到访问速度更快的硬件，在CPU 芯片中，加入了一个专门存放程序最常访问的页表项的 Cache，这个 Cache 就是 TLB ，通常称为页表缓存、转址旁路缓存、快表等</p><p><img data-src="/transblog/images/576865409267371.png"></p><p>在CPU 中，封装了内存管理单元芯片，它用来完成地址转换和 TLB 的访问与交互。有了 TLB 后，那么 CPU 在寻址时，会先查 TLB，如果没找到，才会继续查常规的页表。<strong>TLB 的命中率其实是很高的，因为程序最常访问的页就那么几个。</strong></p><h3 id="段页式"><a href="#段页式" class="headerlink" title="段页式"></a>段页式</h3><p><span id="target1"><strong>Linux内核的内存管理方式</strong></span></p><p>Linux 系统中的每个段都是从 0 地址开始的整个 4GB 虚拟空间（32 位环境下），也就是所有的段的起始地址都是一样的。这意味着，Linux 系统中的代码，包括操作系统本身的代码和应用程序代码，所面对的地址空间都是线性地址空间（虚拟地址），这种做法相当于屏蔽了处理器中的逻辑地址概念，<strong>段只被用于访问控制和内存保护</strong></p><p><img data-src="/transblog/images/5586609807317.png"></p><p>在 Linux 操作系统中，虚拟地址空间的内部又被分为<strong>内核空间和用户空间</strong>两部分</p><p>每个进程都各自有独立的虚拟内存，但是每个虚拟内存中的内核地址，其实关联的都是相同的物理内存。这样，进程切换到内核态后，就可以很方便地访问内核空间内存。</p><h3 id="伙伴算法"><a href="#伙伴算法" class="headerlink" title="伙伴算法"></a>伙伴算法</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">unsigned</span> <span class="type">long</span> __init <span class="title">free_all_bootmem</span><span class="params">(<span class="type">void</span>)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">unsigned</span> <span class="type">long</span> __init <span class="title">free_all_bootmem_core</span><span class="params">(<span class="type">bootmem_data_t</span> *bdata)</span></span></span><br></pre></td></tr></table></figure><ul><li>内部碎片是已经被分配出去的（能明确指出属于哪个进程）内存空间大于请求所需的内存空间，不能被利用的空间就是内部碎片。</li><li>外部碎片是指还没分配出去（不属于任何进程），但是由于大小无法分配给申请内存空间的新进程的内存空闲块。</li></ul><p>伙伴系统算法来解决<mark>内存外部碎片</mark>的问题。<mark>swap分区也为一种方法</mark>，把所有的空闲页框分组为 11 块链表，每一块链表分别包含大小为1，2，4，8，16，32，64，128，256，512 和 1024 个连续的页框。<br>假设系统中有 1MB 大小的内存需要动态管理，按照伙伴算法的要求：需要将这1M大小的内存进行划分。这里，我们将这1M的内存分为 64K、64K、128K、256K、和512K 共五个部分，如下图a所示</p><p><img data-src="/transblog/images/270111911240561.png"></p><ol><li>此时，如果有一个程序A想要申请一块45K大小的内存，则系统会将第一块64K的内存块分配给该程序（产生内部碎片为代价），如图b所示；</li><li>然后程序B向系统申请一块68K大小的内存，系统会将128K内存分配给该程序，如图c所示；</li><li>接下来，程序C要申请一块大小为35K的内存。系统将空闲的64K内存分配给该程序，如图d所示；</li><li>之后程序D需要一块大小为90K的内存。当程序提出申请时，系统本该分配给程序D一块128K大小的内存，但此时内存中已经没有空闲的128K内存块了，于是根据伙伴算法的原理，系统会将256K大小的内存块平分，将其中一块分配给程序D，另一块作为空闲内存块保留，等待以后使用，如图e所示；</li><li>紧接着，程序C释放了它申请的64K内存。在内存释放的同时，系统还负责检查与之相邻并且同样大小的内存是否也空闲，由于此时程序A并没有释放它的内存，所以系统只会将程序C的64K内存回收，如图f所示；</li><li>然后程序A也释放掉由它申请的64K内存，系统随机发现与之相邻且大小相同的一段内存块恰好也处于空闲状态。于是，将两者合并成128K内存，如图g所示；</li><li>之后程序B释放掉它的128k，系统也将这块内存与相邻的128K内存合并成256K的空闲内存，如图h所示；</li><li>最后程序D也释放掉它的内存，经过三次合并后，系统得到了一块1024K的完整内存，如图i所示。</li></ol><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>第一，虚拟内存可以使得进程对运行内存超过物理内存大小，<strong>因为程序运行符合局部性原理，CPU 访问内存会有很明显的重复访问的倾向性</strong>，对于那些没有被经常使用到的内存，我们可以把它换出到物理内存之外，比如硬盘上的 swap 区域。<br>第二，由于每个进程都有自己的页表，所以每个进程的虚拟内存空间就是相互独立的。进程也没有办法访问其他进程的页表，所以这些页表是私有的，这就<strong>解决了多进程之间地址冲突的问题。</strong><br>第三，页表里的页表项中除了物理地址之外，还有一些标记属性的比特，比如控制一个页的读写权限，标记该页是否存在等。在内存访问方面，操作系统提供了更好的安全性。</p><h2 id="内存进阶"><a href="#内存进阶" class="headerlink" title="内存进阶"></a>内存进阶</h2><h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>Linux的用户空间和内核空间内存大小</p><p><img data-src="/transblog/images/107475909264873.png"></p><p>用户空间内存从低到高分别是 6 种不同的内存段：代表了不同的含义</p><p><img data-src="/transblog/images/416540910246114.png"></p><ul><li>代码段，包括<strong>二进制可执行代码</strong>；</li><li>数据段，包括<strong>已初始化的静态常量和全局变量</strong>；</li><li>BSS 段，包括<strong>未初始化的静态变量和全局变量</strong>；</li><li>堆段，包括动态分配的内存，<strong>从低地址开始向上增长</strong>；</li><li>文件映射段，包括动态库、共享内存等，<strong>从低地址开始向上增长</strong>（跟硬件和内核版本有关）；</li><li>栈段，包括局部变量和函数调用的上下文等。<strong>从低地址开始向下增长</strong>，栈的大小是固定的，一般是 8 MB。当然系统也提供了参数，以便我们自定义大小；</li></ul><h3 id="缺页中断"><a href="#缺页中断" class="headerlink" title="缺页中断"></a>缺页中断</h3><p>‌缺页中断‌是指在程序执行时，访问的页面不在当前内存页表中，需要从硬盘中调入到内存中的情况。这是虚拟内存管理的一部分。</p><h4 id="缺页中断的发生场景"><a href="#缺页中断的发生场景" class="headerlink" title="缺页中断的发生场景"></a>缺页中断的发生场景</h4><ol><li>‌页面首次访问‌<br>当程序首次访问一个尚未加载到内存中的页面时，会触发缺页中断。这是因为操作系统并不会一次性将整个程序加载到内存，而是按需分页加载。</li><li>‌页面置换‌<br>当内存中的页面不足以存放当前需要访问的页面时，操作系统需要进行页面置换。如果需要访问的页面不在内存中，就会发生缺页中断，触发页面置换算法，将一个不再需要的页面调出内存，将需要访问的页面调入内存。</li><li>‌写时复制‌<br>在某些情况下，多个进程共享同一页面，当其中一个进程尝试写入该页面时，操作系统会将页面复制一份，然后进行写操作。如果写操作导致需要访问一个新的页面，就可能触发缺页中断。</li></ol><h4 id="缺页中断的处理步骤"><a href="#缺页中断的处理步骤" class="headerlink" title="缺页中断的处理步骤"></a>缺页中断的处理步骤</h4><ol><li>‌硬件陷入到内核‌<br>当CPU发出的虚拟地址所对应的页面不在物理内存时，硬件会陷入到内核。</li><li>‌保护通用寄存器‌<br>保护通用寄存器的内容，以便在处理完缺页中断后能够恢复。</li><li>‌操作系统判断所需的虚拟页面号‌<br>操作系统需要判断所需的虚拟页面号是否存在。</li><li>‌地址合法性检查‌<br>操作系统检查地址的合法性。</li><li>‌选择物理页面‌<br>操作系统选择一个物理页面用来存放将要调入的页面。</li><li>‌写盘操作‌<br>如果选择的物理页面包含有未写入磁盘的内容，则首先进行写盘操作。</li><li>‌更新页表‌<br>更新页表以反映新的映射关系。</li><li>‌恢复寄存器‌<br>恢复通用寄存器的内容。</li><li>‌程序继续运行‌<br>处理完缺页中断后，程序可以继续运行。‌</li></ol><h3 id="malloc"><a href="#malloc" class="headerlink" title="malloc"></a>malloc</h3><h4 id="malloc-是如何分配内存的？"><a href="#malloc-是如何分配内存的？" class="headerlink" title="malloc 是如何分配内存的？"></a>malloc 是如何分配内存的？</h4><ul><li>方式一：通过 brk() 系统调用从堆分配内存</li></ul><p><img data-src="/transblog/images/510461310268554.png"></p><ul><li>方式二：通过 mmap() 系统调用在文件映射区域分配内存；也就是从文件映射区“偷”了一块内存</li></ul><p><img data-src="/transblog/images/598501410263690.png"></p><p>malloc() 源码里默认定义了一个阈值：</p><p>如果用户分配的内存小于 128 KB，则通过 brk() 申请内存；<br>如果用户分配的内存大于 128 KB，则通过 mmap() 申请内存；</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep -i &quot;DEFAULT_MMAP_THRESHOLD_MIN&quot; malloc/malloc.c</span><br></pre></td></tr></table></figure><h4 id="malloc-1-会分配多大的虚拟内存？"><a href="#malloc-1-会分配多大的虚拟内存？" class="headerlink" title="malloc(1) 会分配多大的虚拟内存？"></a>malloc(1) 会分配多大的虚拟内存？</h4><p>132K</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;使用cat /proc/%d/maps查看内存分配\n&quot;</span>, <span class="built_in">getpid</span>());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 申请1字节的内存</span></span><br><span class="line">    <span class="type">void</span> *addr = <span class="built_in">malloc</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;此1字节的内存起始地址：%x\n&quot;</span>, addr);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;使用cat /proc/%d/maps查看内存分配\n&quot;</span>, <span class="built_in">getpid</span>());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将程序阻塞，当输入任意字符时才往下执行</span></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放内存</span></span><br><span class="line">    <span class="built_in">free</span>(addr);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;释放了1字节的内存，但heap堆并不会释放\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">getchar</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>malloc 通过 brk() 方式申请的内存，free 释放内存的时候，并不会把内存归还给操作系统，而是缓存在 malloc 的内存池中，待下次使用；<br>malloc 通过 mmap() 方式申请的内存，free 释放内存的时候，会把内存归还给操作系统，内存得到真正的释放。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">//申请1字节的内存</span></span><br><span class="line">  <span class="type">void</span> *addr = <span class="built_in">malloc</span>(<span class="number">128</span>*<span class="number">1024</span>);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;此128KB字节的内存起始地址：%x\n&quot;</span>, addr);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;使用cat /proc/%d/maps查看内存分配\n&quot;</span>,<span class="built_in">getpid</span>());</span><br><span class="line"></span><br><span class="line">  <span class="comment">//将程序阻塞，当输入任意字符时才往下执行</span></span><br><span class="line">  <span class="built_in">getchar</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">//释放内存</span></span><br><span class="line">  <span class="built_in">free</span>(addr);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;释放了128KB字节的内存，内存也归还给了操作系统\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">getchar</span>();</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="为什么不全部使用-mmap-来分配内存？"><a href="#为什么不全部使用-mmap-来分配内存？" class="headerlink" title="为什么不全部使用 mmap 来分配内存？"></a>为什么不全部使用 mmap 来分配内存？</h4><p>申请内存的操作应该避免频繁的系统调用，如果都用 mmap 来分配内存，等于每次都要执行系统调用。<br>另外，因为 mmap 分配的内存每次释放的时候，都会归还给操作系统，于是每次 mmap 分配的虚拟地址都是缺页状态的，然后在第一次访问该虚拟地址的时候，就会触发缺页中断。</p><h4 id="为什么不全部使用-brk-来分配？"><a href="#为什么不全部使用-brk-来分配？" class="headerlink" title="为什么不全部使用 brk 来分配？"></a>为什么不全部使用 brk 来分配？</h4><p>随着系统频繁地 malloc 和 free ，尤其对于小块内存，堆内将产生越来越多不可用的碎片,<strong>导致“内存泄露”</strong></p><h4 id="free-函数只传入一个内存地址，为什么能知道要释放多大的内存？"><a href="#free-函数只传入一个内存地址，为什么能知道要释放多大的内存？" class="headerlink" title="free() 函数只传入一个内存地址，为什么能知道要释放多大的内存？"></a>free() 函数只传入一个内存地址，为什么能知道要释放多大的内存？</h4><p>运行上述脚本代码可发现：打印的地址比实际程序地址多出来 0x10 （16字节）<br>多出来的 16 字节就是保存了该内存块的描述信息，比如有该内存块的大小。</p><p><img data-src="/transblog/images/354790711257236.png"></p><h3 id="内存对齐"><a href="#内存对齐" class="headerlink" title="内存对齐"></a>内存对齐</h3><h4 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h4><ul><li>操作系统在数据读取的时候，其实并不是一字节一字节进行读取的，而是一段一段进行读取</li><li>我们假如是4bytes。假如我们要读取一个int，如果不对齐，这个int占用第1块的后3位和第2块的第1位，需要两次读取，将两次的数据组合起来。这样CPU将做出“多余操作”，严重影响处理速度。</li><li>因此需要进行内存对齐，从而提高CPU处理速率，而这项任务就交给编译器进行相应的地址分配和优化，编译器会根据提供参数或者目标环境进行相应的内存对齐。</li></ul><h4 id="结构体内存对齐的规则"><a href="#结构体内存对齐的规则" class="headerlink" title="结构体内存对齐的规则"></a>结构体内存对齐的规则</h4><p><strong>未指定#pragma pack时</strong>：</p><ol><li>第一个成员起始于0偏移处；其他成员变量对齐到 自身对齐数的整数倍 的地址处，之后占的空间是自身字节大小。</li><li>每个成员按其类型大小进行对齐；</li><li>结构体总长度必须为最大类型参数的整数倍；</li><li>对于数组，可以拆开看做n个数组元素</li></ol><p><strong>指定#pragma pack(n)时</strong>：</p><ol><li>第一个成员起始于0偏移处；其他成员变量对齐到 自身对齐数的整数倍 的地址处，之后占的空间是自身字节大小。</li><li>每个成员按其类型大小和指定对齐参数n中较小的一个进行对齐；</li><li>结构体总长度必须为最大类型参数大小和对齐参数中较小值的整数倍；</li><li>对于数组，可以拆开看做n个数组元素</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">char</span> b;</span><br><span class="line">    <span class="type">short</span> c;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span> &#123;</span><br><span class="line">    <span class="type">char</span> b;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">short</span> c;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>sizeof(A) &#x3D; 8;sizeof(B) &#x3D; 12。</p><table><thead><tr><th align="center"></th><th align="center">a</th><th align="center">b</th><th align="center">c</th></tr></thead><tbody><tr><td align="center">A的内存布局：</td><td align="center">1111</td><td align="center">1*</td><td align="center">11</td></tr><tr><td align="center">B的内存布局：</td><td align="center">1***</td><td align="center">1111</td><td align="center">11**</td></tr></tbody></table><p>其中星号*表示填充的字节。A中，b后面为何要补充一个字节？因为c为short，其起始位置要为2的倍数，就是原则1。c的后面没有补充，因为b和c正好占用4个字节，整个A占用空间为4的倍数，也就是最大成员int类型的倍数，所以不用补充。B中，b是char为1，b后面补充了3个字节，因为a是int为4，根据原则1，起始位置要为4的倍数，所以b后面要补充3个字节。c后面补充两个字节，根据原则3，整个B占用空间要为4的倍数，c后面不补充，整个B的空间为10，不符，所以要补充2个字节。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">double</span> b;</span><br><span class="line">    <span class="type">float</span> c;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span> &#123;</span><br><span class="line">    <span class="type">char</span> e[<span class="number">2</span>];</span><br><span class="line">    <span class="type">int</span> f;</span><br><span class="line">    <span class="type">double</span> g;</span><br><span class="line">    <span class="type">short</span> h;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">A</span> i;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>sizeof(A) &#x3D; 24; 这个比较好理解，int为4，double为8，float为4，总长为8的倍数，补齐，所以整个A为24。<br>sizeof(B) &#x3D; 48; 看看B的内存布局。</p><table><thead><tr><th align="center"></th><th align="center">e</th><th align="center">f</th><th align="center">g</th><th align="center">h</th><th align="center">i</th></tr></thead><tbody><tr><td align="center">B的内存布局</td><td align="center">11**</td><td align="center">1111</td><td align="center">11111111</td><td align="center">11 * * * * * *</td><td align="center">1111****, 11111111, 1111****</td></tr><tr><td align="center">i其实就是A的内存布局。i的起始位置要为24的倍数，所以h后面要补齐。</td><td align="center"></td><td align="center"></td><td align="center"></td><td align="center"></td><td align="center"></td></tr></tbody></table><h3 id="大小端模式"><a href="#大小端模式" class="headerlink" title="大小端模式"></a>大小端模式</h3><ul><li>大端存储是指低字节存储在高地址</li><li>小端存储是指低字节存储在低地址<br>根据联合体来判断该系统是大端还是小端。<mark>因为联合体变量总是从低地址存储</mark><br>判断大小端的代码：<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">union</span> <span class="title class_">test</span> &#123;</span><br><span class="line">        <span class="type">int</span> i;</span><br><span class="line">        <span class="type">char</span> c;</span><br><span class="line">    &#125;;</span><br><span class="line">    test t;</span><br><span class="line">    t.i = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span> (t.c == <span class="number">1</span>) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;big&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;little&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//1在内存中存储为0x000001</span></span><br><span class="line"><span class="comment">//如果第一个字节为0，说明机器是大端存储，低位字节1存储在高位地址</span></span><br><span class="line"><span class="comment">//如果第一个字节为1，说明机器是小端存储，低位字节1存储在低位地址</span></span><br></pre></td></tr></table></figure></li></ul><h2 id="内存回收"><a href="#内存回收" class="headerlink" title="内存回收"></a>内存回收</h2><ol><li>后台内存回收：在物理内存紧张的时候，会唤醒 kswapd 内核线程来回收内存，这个回收内存的过程<strong>异步</strong>的，不会阻塞进程的执行。</li><li>直接内存回收：如果后台异步回收跟不上进程内存申请的速度，就会开始直接回收，这个回收内存的过程是<strong>同步</strong>的，会阻塞进程的执行。</li><li>如果直接内存回收后，空闲的物理内存仍然无法满足此次物理内存的申请，那么内核就会放最后的大招了 ——触发 OOM （Out of Memory）机制。</li></ol><p>OOM Killer 机制：<br>会根据算法选择一个占用物理内存较高的进程，然后将其杀死，以便释放内存资源，如果物理内存依然不足，OOM Killer 会继续杀死占用物理内存较高的进程，直到释放足够的内存位置。</p><h3 id="哪些内存可以被回收？"><a href="#哪些内存可以被回收？" class="headerlink" title="哪些内存可以被回收？"></a>哪些内存可以被回收？</h3><ul><li><p>文件页：<strong>内核缓存的磁盘数据</strong>（Buffer）和<strong>内核缓存的文件数据</strong>（Cache）都叫作文件页。大部分文件页，都可以直接释放内存，以后有需要时，再从磁盘重新读取就可以了。而那些被应用程序修改过，并且暂时还没写入磁盘的数据（也就是脏页），就得先写入磁盘，然后才能进行内存释放。<strong>回收干净页的方式是直接释放内存，回收脏页的方式是先写回磁盘后再释放内存</strong>。</p></li><li><p>匿名页：这部分内存没有实际载体，不像文件缓存有硬盘文件这样一个载体，比如堆、栈数据等。这部分内存很可能还要再次被访问，所以不能直接释放内存，它们回收的方式是通过 Linux 的<strong>Swap 机制</strong>，Swap 会把不常访问的内存先写到磁盘中，然后”释放”这些内存，给其他更需要的进程使用。再次访问这些内存时，重新从磁盘读入内存就可以了。</p></li></ul><p>Linux 提供了一个 &#x2F;proc&#x2F;sys&#x2F;vm&#x2F;swappiness 选项，用来调整文件页和匿名页的回收倾向。数值越大，越积极使用 Swap</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat /proc/sys/vm/swappiness</span><br></pre></td></tr></table></figure><h3 id="基于LRU算法"><a href="#基于LRU算法" class="headerlink" title="基于LRU算法"></a>基于LRU算法</h3><p>维护着 active 和 inactive 两个双向链表，其中：<br>active_list 活跃内存页链表，这里存放的是最近被访问过（活跃）的内存页；<br>inactive_list 不活跃内存页链表，这里存放的是很少被访问（非活跃）的内存页；</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat /proc/meminfo | grep -i active | sort</span><br></pre></td></tr></table></figure><h3 id="如何保护一个进程不被-OOM-杀掉呢？"><a href="#如何保护一个进程不被-OOM-杀掉呢？" class="headerlink" title="如何保护一个进程不被 OOM 杀掉呢？"></a>如何保护一个进程不被 OOM 杀掉呢？</h3><p>Linux 内核里有一个 oom_badness() 函数</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// points 代表打分的结果</span></span><br><span class="line"><span class="comment">// process_pages 代表进程已经使用的物理内存页面数</span></span><br><span class="line"><span class="comment">// oom_score_adj 代表 OOM 校准值</span></span><br><span class="line"><span class="comment">// totalpages 代表系统总的可用页面数</span></span><br><span class="line">points = process_pages + oom_score_adj*totalpages/<span class="number">1000</span></span><br></pre></td></tr></table></figure><p> oom_score_adj。它是可以通过 &#x2F;proc&#x2F;[pid]&#x2F;oom_score_adj 来配置的，设置 -1000 到 1000 之间的任意一个数值，-1000不会被杀死</p><h3 id="swap机制的作用"><a href="#swap机制的作用" class="headerlink" title="swap机制的作用"></a>swap机制的作用</h3><p><img data-src="/transblog/images/107475909264873.png"></p><p>在 32 位&#x2F;64 位操作系统环境下，申请的虚拟内存超过物理内存后会怎么样？</p><p>在 32 位操作系统，因为进程最大只能申请 3 GB 大小的虚拟内存，所以直接申请 8G 内存，会申请失败。<br>在 64 位操作系统，因为进程最大只能申请 128 TB 大小的虚拟内存，即使物理内存只有 4GB，申请 8G 内存也是没问题，因为申请的内存是虚拟内存。<br>程序申请的虚拟内存，如果没有被使用，它是不会占用物理空间的。<strong>当访问这块虚拟内存后，操作系统才会进行物理内存分配。</strong></p><p>如果申请物理内存大小超过了空闲物理内存大小，就要看操作系统有没有开启 Swap 机制：<br>如果没有开启 Swap 机制，程序就会直接 OOM；<br>如果有开启 Swap 机制，程序可以正常运行。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">free -m    #linux</span><br><span class="line">dir /A     #windows</span><br></pre></td></tr></table></figure><h2 id="内存的预读失效和缓存污染"><a href="#内存的预读失效和缓存污染" class="headerlink" title="内存的预读失效和缓存污染"></a>内存的预读失效和缓存污染</h2><p>预读失效：读磁盘多读了一些，但是没有用到 <strong>(局部性原理的充分应用)</strong><br>缓存污染：批量读，把热点数据挤出去</p><p><strong>以上问题十分常见，包括操作系统，redis，mysql等</strong><br>LRU算法是基于最近使用时间，其核心思想是<strong>淘汰最长时间未被使用的数据</strong>，这适用于访问模式具有时间局部性的场景；<br>LFU算法是基于访问频率，其核心思想是<strong>淘汰访问频率最低的数据</strong>，这适用于访问模式具有频率局部性的场景。</p><p>Redis实现 LFU 算法，MySql 和 Linux 操作系统是改进 LRU 算法</p><p><em>有关Redis如何实现LFU，请参照Redis–Redis的内存策略–LRU和LFU</em><br><em>有关MySql如何改进LRU，请参照MySql–内存篇</em></p><h3 id="Linux-的-Page-Cache-和-MySQL-的-Buffer-Pool"><a href="#Linux-的-Page-Cache-和-MySQL-的-Buffer-Pool" class="headerlink" title="Linux 的 Page Cache 和 MySQL 的 Buffer Pool"></a>Linux 的 Page Cache 和 MySQL 的 Buffer Pool</h3><p><img data-src="/transblog/images/321104711250370.png">                                    <img data-src="/transblog/images/439454711240900.png"></p><h3 id="解决预读失效"><a href="#解决预读失效" class="headerlink" title="解决预读失效"></a>解决预读失效</h3><ul><li>Linux 操作系统实现两个了 LRU 链表：活跃 LRU 链表（active_list）和非活跃 LRU 链表（inactive_list）；</li><li>预读页就只需要加入到 inactive list 区域的头部，当页被真正访问的时候，才将页插入 active list 的头部。</li></ul><p><img data-src="/transblog/images/200245111243404.png"></p><ul><li>MySQL 的 Innodb 存储引擎是在一个 LRU 链表上划分来 2 个区域：young 区域 和 old 区域。</li><li>预读的页就只需要加入到 old 区域的头部，当页被真正访问的时候，才将页插入 young 区域的头部。</li><li>两个区域的分界点位置，一般叫做midpoint。在默认配置下，该位置在LRU列表长度的5／8处。由参数<code>innodb_old_blocks_pct</code>控制</li></ul><p><img data-src="/transblog/images/26395211252351.png"></p><h3 id="解决缓存污染"><a href="#解决缓存污染" class="headerlink" title="解决缓存污染"></a>解决缓存污染</h3><p>Linux 操作系统：在内存页<strong>被访问第二次</strong>的时候，才将页从 inactive list 升级到 active list 里。<br>MySQL Innodb：在内存页被访问第二次的时候，并不会马上将该页从 old 区域升级到 young 区域，因为还要<strong>进行停留在 old 区域的时间判断</strong></p><p>如果第二次的访问时间与第一次访问的时间在 1 秒内（默认值），那么该页就不会被从 old 区域升级到 young 区域；<br>如果第二次的访问时间与第一次访问的时间超过 1 秒，那么该页就会从 old 区域升级到 young 区域；</p><h2 id="深入Linux内存"><a href="#深入Linux内存" class="headerlink" title="深入Linux内存"></a>深入Linux内存</h2><p>32位系统，用户态空间划分为3g大小：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/arch/x86/include/<span class="keyword">asm</span>/page_32_types.h        PAGE_OFFSET</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/223035114245236.png"><img data-src="/transblog/images/507125114245845.png"></p><p>进程在内核中的描述符task_struct 结构</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">task_struct</span> &#123;</span><br><span class="line">        <span class="comment">// 进程id</span></span><br><span class="line">    <span class="type">pid_t</span>pid;</span><br><span class="line">        <span class="comment">// 用于标识线程所属的进程 pid</span></span><br><span class="line">    <span class="type">pid_t</span>tgid;</span><br><span class="line">        <span class="comment">// 进程打开的文件信息</span></span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">files_struct</span>*files;</span><br><span class="line">        <span class="comment">// 内存描述符表示进程虚拟地址空间</span></span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">mm_struct</span>*mm;</span><br><span class="line"></span><br><span class="line">        .......... 省略 .......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在进程描述符 task_struct 结构中，有一个专门描述进程虚拟地址空间的内存描述符 mm_struct 结构，这个结构体中包含了进程虚拟内存空间的全部信息。</p><p>当我们调用 fork() 函数创建进程的时候，表示进程地址空间的 mm_struct 结构会随着进程描述符 task_struct 的创建而创建</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="type">long</span> <span class="title">do_fork</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="type">unsigned</span> <span class="type">long</span> stack_start,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="type">unsigned</span> <span class="type">long</span> stack_size,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="type">int</span> __user *parent_tidptr,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="type">int</span> __user *child_tidptr)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="keyword">struct</span> task_struct *<span class="title">copy_process</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="type">unsigned</span> <span class="type">long</span> stack_start,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="type">unsigned</span> <span class="type">long</span> stack_size,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="type">int</span> __user *child_tidptr,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="keyword">struct</span> pid *pid,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="type">int</span> trace)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">int</span> <span class="title">copy_mm</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags, <span class="keyword">struct</span> task_struct *tsk)</span></span></span><br></pre></td></tr></table></figure><p>通过 fork() 函数创建出的子进程，它的虚拟内存空间以及相关页表相当于父进程虚拟内存空间的一份拷贝，直接从父进程中拷贝到子进程中。<br>通过 vfork() 函数创建出的子进程，首先会设置 CLONE_VM 标识，这样来到 copy_mm 函数中就会进入 if (clone_flags &amp; CLONE_VM) 条件中，在这个分支中会将父进程的虚拟内存空间以及相关页表直接赋值给子进程。这样一来父进程和子进程的虚拟内存空间就变成共享的了。也就是说父子进程之间使用的虚拟内存空间是一样的，并不是一份拷贝。<br>vfork() ：进程-&gt;线程，Linux 内核并不区别对待</p><h3 id="内核的内存管理方式"><a href="#内核的内存管理方式" class="headerlink" title="内核的内存管理方式"></a>内核的内存管理方式</h3><p><a href="#target1">点击此处跳转到：Linux内核的内存管理方式（段页式存储）</a><br>Linux作为一个通用的操作系统，需要兼容各种硬件体系，包括不同位数的CPU。对64位的CPU来说，两级页表仍然太少，一个页表会太大，这会占用太多宝贵的物理内存。Linux采用了通用的四级页表。实际采用几级页表则具体受硬件的限制。</p><p><img data-src="/transblog/images/34013333587380.png"></p><p>四种页表分别称为： <strong>页全局目录（pgd_t）、页上级目录（pud_t）、页中间目录（pmd_t）、页表（pte_t）</strong>。<br>对于32位x86系统，两级页表已经足够了。Linux通过使“页上级目录”位和“页中间目录”位全为0，彻底取消了页上级目录和页中间目录字段。不过，页上级目录和页中间目录在指针序列中的位置被保留，以便同样的代码在32位系统和64位系统下都能使用。</p><p><strong>分段可以给每一个进程分配不同的线性地址空间，而分页可以把同一线性地址空间映射到不同的物理空间。</strong></p><p>与分段相比，Linux更喜欢使用分页方式，因为：</p><ol><li>当所有进程使用相同的段寄存器时，内存管理变得更简单，也就是说它们能共享同样的一组线性地址；</li><li>Linux设计目标之一是可以把它移植到绝大多数流行的处理器平台上，然而，RISC体系结构对分段的支持很有限。</li></ol><p>运行在用户态的所有Linux进程都使用一对相同的段来对指令和数据寻址，这一对段就是所谓的用户代码段和用户数据段。</p><p>线性地址段：PGDIR_SHIFT、PUD_SHIFT、PMD_SHIFT、PAGE_SHIFT等。<br>页表处理：pgd_t、pud_t、pmd_t、pte_t。</p><h3 id="内核如何布局进程虚拟内存空间"><a href="#内核如何布局进程虚拟内存空间" class="headerlink" title="内核如何布局进程虚拟内存空间"></a>内核如何布局进程虚拟内存空间</h3><p>在mm_struct中，定义了各个数据域的长度</p><p><img data-src="/transblog/images/431122815272800.png"></p><h3 id="内核如何管理虚拟内存区域"><a href="#内核如何管理虚拟内存区域" class="headerlink" title="内核如何管理虚拟内存区域"></a>内核如何管理虚拟内存区域</h3><p>新的结构体 vm_area_struct，正是这个结构体<strong>描述了</strong>这些虚拟内存区域</p><p><img data-src="/transblog/images/424744115244311.png"></p><p>include&#x2F;linux&#x2F;mm.h：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">vm_flags访问权限</span><br><span class="line">VM_READ可读</span><br><span class="line">VM_WRITE可写</span><br><span class="line">VM_EXEC可执行</span><br><span class="line">VM_SHARD可多进程之间共享</span><br><span class="line">VM_IO可映射至设备 IO 空间</span><br><span class="line">VM_RESERVED内存区域不可被换出</span><br><span class="line">VM_SEQ_READ内存区域可能被顺序访问</span><br><span class="line">VM_RAND_READ内存区域可能被随机访问</span><br></pre></td></tr></table></figure><h3 id="虚拟内存区域在内核中是如何被组织的"><a href="#虚拟内存区域在内核中是如何被组织的" class="headerlink" title="虚拟内存区域在内核中是如何被组织的"></a>虚拟内存区域在内核中是如何被组织的</h3><p><img data-src="/transblog/images/295374815240562.png"></p><p>在进程虚拟内存空间中包含的内存区域 VMA 比较多的情况下，使用红黑树查找特定虚拟内存区域的时间复杂度是 O( logN ) ，可以显著减少查找所需的时间。<br>所以在内核中，同样的内存区域 vm_area_struct 会有两种组织形式，<strong>一种是双向链表用于高效的遍历，另一种就是红黑树用于高效的查找</strong>，每个 VMA 区域都是红黑树中的一个节点，通过 struct vm_area_struct 结构中的 vm_rb 将自己连接到红黑树中。</p><p>总结：mm_struct中的mmap为vm_area_struct类型，指向了每个域的开始和结束，并且同时以双向链表和红黑树存在，而mm_struct定义的start和end，标示了每个域的结束位置</p><h1 id="进程调度"><a href="#进程调度" class="headerlink" title="进程调度"></a>进程调度</h1><h2 id="Linux的进程"><a href="#Linux的进程" class="headerlink" title="Linux的进程"></a>Linux的进程</h2><h3 id="0号进程"><a href="#0号进程" class="headerlink" title="0号进程"></a>0号进程</h3><p>0号进程称为 idle 进程，其 pid 等于0。</p><p>每个进程都有struct task_struct。idle进程对应的PCB是 struct task_struct init_task。<br>idle进程是唯一一个没有通过fork或者kernel_thread产生的进程，因为 init_task 是静态变量（初始化了的全局变量），其他进程的PCB都是fork或者kernel_thread动态申请内存创建的。<br>每个进程都有对应的一个函数，idle进程的函数是 start_kernel()，start_kernel() 最后会调用 cpu_startup_entry() ，其内部是 while(1) {}</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">asmlinkage __visible <span class="type">void</span> __init <span class="title">start_kernel</span><span class="params">(<span class="type">void</span>)</span></span></span><br></pre></td></tr></table></figure><h3 id="1号进程"><a href="#1号进程" class="headerlink" title="1号进程"></a>1号进程</h3><p>1号进程称为 init 进程，其 pid 等于1。</p><p>1号进程是0号进程通过调用 kernel_thread() 创建的，在运行 schedule_preempt_disabled() 内的 schedule() 后，就启动调度器进行进程切换，kernel_init() 也就得以运行。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">kernel_thread</span>(kernel_init, <span class="literal">NULL</span>, CLONE_FS);</span><br></pre></td></tr></table></figure><p>kernel_init() 最后会启动用户态的处于根文件系统存储的 init 进程，从而实现 init 内核态到 init 用户态的转化。<br>init进程完成系统的初始化，是系统中所有其它用户进程的祖先进程。</p><h3 id="2号进程"><a href="#2号进程" class="headerlink" title="2号进程"></a>2号进程</h3><p>2号进程称为 kthreadd 进程，其 pid 等于2。<br>kthreadd进程由idle进程通过kernel_thread创建，并始终运行在内核空间, 负责所有内核线程的创建。</p><p>kthreadd利用for(;;)一直驻留在内存中运行：主要过程如下：<br>检查kthread_create_list为空时，kthreadd让出cpu的执行权<br>kthread_create_list不为空时，利用while循环遍历kthread_create_list链表<br>每取下一个链表节点后调用create_kthread，创建内核线程</p><p><img data-src="/transblog/images/547263518240701.png"></p><p>kernel_thread()函数<strong>是通过调用do_fork()函数创建的线程，而do_fork()则是在应用层fork(), vfork()和clone()函数的系统调用</strong>；此外还需要在其执行函数里调用daemonize()进行资源的释放；该线程挂接在init进程下。<br>kthread_create()函数是通过工作队列workqueue创建的线程，此线程挂在kthreadd线程下。<br>kthread_run()函数本质上是调用了kthread_create()和wake_up_process(), 就是除了挂在工作队列上后，便唤醒进行工作。<br>kthread_create()是比较推崇的创建内核线程的方式。</p><h3 id="写时复制"><a href="#写时复制" class="headerlink" title="写时复制"></a>写时复制</h3><p>在Linux程序中，fork()会产生一个和父进程完全相同的子进程，但子进程在此后多会exec系统调用，出于效率考虑，linux中引入了“写时复制“技术，也就是<strong>只有进程空间的各段的内容要发生变化时，才会将父进程的内容复制一份给子进程</strong><br>该技术是依赖硬件MMU的，没有MMU，就不支持 fork，只支持vfork。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifdef</span> __ARCH_WANT_SYS_FORK</span></span><br><span class="line"><span class="built_in">SYSCALL_DEFINE0</span>(fork)</span><br><span class="line">&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_MMU</span></span><br><span class="line"><span class="keyword">return</span> <span class="built_in">do_fork</span>(SIGCHLD, <span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"><span class="comment">/* can not support in nommu mode */</span></span><br><span class="line"><span class="keyword">return</span> -EINVAL;                    <span class="comment">//返回这个错误码需要被上层捕捉</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifdef</span> __ARCH_WANT_SYS_VFORK</span></span><br><span class="line"><span class="built_in">SYSCALL_DEFINE0</span>(vfork)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">do_fork</span>(CLONE_VFORK | CLONE_VM | SIGCHLD, <span class="number">0</span>,</span><br><span class="line"><span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//写时复制COW体现在这个函数中: copy_mm-&gt;dup_mm-&gt;dup_mmap-&gt;.......-&gt;copy_one_pte</span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">unsigned</span> <span class="type">long</span></span></span><br><span class="line"><span class="function"><span class="title">copy_one_pte</span><span class="params">(<span class="keyword">struct</span> mm_struct *dst_mm, <span class="keyword">struct</span> mm_struct *src_mm,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="type">pte_t</span> *dst_pte, <span class="type">pte_t</span> *src_pte, <span class="keyword">struct</span> vm_area_struct *vma,</span></span></span><br><span class="line"><span class="params"><span class="function"><span class="type">unsigned</span> <span class="type">long</span> addr, <span class="type">int</span> *rss)</span></span></span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/95371510264876.png"></p><ul><li>fork 函数之后，如果想要把子进程换成一个想要执行的进程，这时，就不得不使用 exec()函数了，这也是 fork()的意义所在。</li><li>常见的 fork()调用例子有很多，都是在利用exec系统调用将新产生的子进程完全替换成目标进程。</li></ul><p><strong>exec函数(#include &lt;unistd.h&gt;)：</strong><br>装载一个新的程序（可执行映像）覆盖当前进程内存空间中的映像，从而执行不同的任务。exec系列函数在执行时会直接替换掉当前进程的地址空间。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">execl</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *arg,...<span class="comment">/* (char*)NULL*/</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execlp</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *file, <span class="type">const</span> <span class="type">char</span> *arg,...<span class="comment">/* (char*)NULL*/</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execle</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *arg,...<span class="comment">/* (char*)NULL, char *const envp[]*/</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">char</span> *<span class="type">const</span> argv[])</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execvp</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *file, <span class="type">char</span> *<span class="type">const</span> argv[])</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execvpe</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *file, <span class="type">char</span> *<span class="type">const</span> argv[], <span class="type">char</span> *<span class="type">const</span> envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">execve</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">char</span> *<span class="type">const</span> argv[], <span class="type">char</span> *<span class="type">const</span> envp[])</span></span>;</span><br></pre></td></tr></table></figure><ul><li>在fork之后exec之前两个进程用的是相同的物理空间（内存区），子进程的代码段、数据段、堆栈都是指向父进程的物理空间，也就是说，两者的虚拟空间不同，但其对应的物理空间是同一个。当父子进程中有更改相应段的行为发生时，再为子进程相应的段分配物理空间</li><li>如果是因为exec，由于两者执行的代码不同，子进程的代码段也会分配单独的物理空间。</li><li>如果不是因为exec，内核会给子进程的数据段、堆栈段分配相应的物理空间（至此两者有各自的进程空间，互不影响），而代码段继续共享父进程的物理空间（两者的代码完全相同）。</li></ul><p><strong>写时复制的应用</strong></p><ol><li><p>虚拟内存管理中的写时复制<br>一般把这种被共享访问的页面标记为只读。当一个task试图向内存中写入数据时，内存管理单元（MMU）抛出一个异常，内核处理该异常时为该task分配一份物理内存并复制数据到此内存，重新向MMU发出执行该task的写操作。</p></li><li><p>Linux的文件管理系统使用了写时复制策略。</p></li><li><p>[数据库]服务器也一般采用了写时复制策略，为用户提供一份snapshot。</p></li><li><p>软件应用中的写时复制<br>[C++标准程序库]中的[std::string]类，在C++98&#x2F;C++03标准中是允许写时复制策略。但在[C++11]标准中为了提高并行性取消了这一策略。 GCC从版本5开始，std::string不再采用COW策略。</p></li></ol><h3 id="孤儿进程与僵尸进程"><a href="#孤儿进程与僵尸进程" class="headerlink" title="孤儿进程与僵尸进程"></a>孤儿进程与僵尸进程</h3><p>孤儿进程：一个父进程退出，而它的一个或多个子进程还在运行，那么那些子进程将成为孤儿进程。孤儿进程将被init进程(进程号为1)所收养，并由init进程对它们完成状态收集工作，所以孤儿进程并不会有什么危害。</p><p>僵尸进程：一个进程使用fork创建子进程，如果子进程退出，而父进程并没有调用wait或waitpid获取子进程的状态信息，那么子进程的进程描述符仍然保存在系统中。这种进程称之为僵死进程。</p><ul><li>父进程只管生成新的子进程，对子进程退出之后的事情，则一概不闻不问，系统运行一段时间后，系统中就会存在很多的僵死进程，用ps命令查看的话，就会看到很多状态为Z的进程。</li><li>僵死进程并不是问题的根源，罪魁祸首是产生出大量僵死进程的父进程。答案就是把产生大量僵死进程的那个元凶枪毙掉<mark>（通过kill发送SIGTERM或者SIGKILL信号）</mark></li><li>枪毙了元凶进程之后，它产生的僵死进程就变成了孤儿进程，这些孤儿进程会被init进程接管，init进程会wait()这些孤儿进程，释放它们占用的系统进程表中的资源。</li></ul><h3 id="守护进程"><a href="#守护进程" class="headerlink" title="守护进程"></a>守护进程</h3><p>守护进程（daemon）是后台运行的、系统启动是就存在的、不与任何终端关联的，用于处理一些系统级别任务的特殊进程。</p><h4 id="创建守护进程"><a href="#创建守护进程" class="headerlink" title="创建守护进程"></a>创建守护进程</h4><ol><li>fork()创建子进程，父进程exit()退出；</li></ol><ul><li>由于守护进程是脱离控制终端的，完成这一步后就会在Shell终端里造成程序已经运行完毕的假象。之后的所有工作都在子进程中完成，而用户在Shell终端里则可以执行其他命令，从而在形式上做到了与控制终端的脱离，在后台工作。</li><li>由于父进程先于子进程退出，子进程就变为孤儿进程，并由 init 进程作为其父进程收养。</li></ul><ol start="2"><li>在子进程调用setsid()创建新会话；<br>在调用了 fork() 函数后，子进程全盘拷贝了父进程的会话期、进程组、控制终端等，虽然父进程退出了，但会话期、进程组、控制终端等并没有改变。这还不是真正意义上的独立开来，而 setsid() 函数能够使进程完全独立出来，摆脱其他进程的控制。</li></ol><p>setsid()创建一个新会话，调用进程担任新会话的首进程，其作用有：</p><ul><li>使当前进程脱离原会话的控制</li><li>使当前进程脱离原进程组的控制</li><li>使当前进程脱离原控制终端的控制</li></ul><ol start="3"><li><p>在子进程中调用chdir()改变当前目录为根目录；<br>使用fork创建的子进程继承了父进程的当前工作目录。由于守护进程在后台运行，开始于系统开启，终止于系统关闭，所以要将其目录改为系统的根目录下。进程在执行时，其文件系统不能被卸下。</p></li><li><p>在子进程中调用umask()重设文件权限掩码为0；</p></li></ol><ul><li>文件权限掩码是指屏蔽掉文件权限中的对应位。比如，有个文件权限掩码是050，它就屏蔽了文件组拥有者的可读与可执行权限</li><li>由于使用fork函数新建的子进程继承了父进程的文件权限掩码，这就给该子进程使用文件带来了诸多的麻烦。因此把文件权限掩码重设为0即清除掩码（权限为777），这样可以大大增强该守护进程的灵活性。通常的使用方法为umask(0)。</li></ul><ol start="5"><li>在子进程中close()不需要的文件描述符；</li></ol><ul><li>子进程从父进程那里继承了打开文件描述符。这些被打开的文件可能永远不会被守护进程读写，但它们一样消耗系统资源，而且可能导致所在的文件系统无法卸下。</li><li>在第二步之后，守护进程已经与控制终端失去了联系，终端输入的字符不可能达到守护进程，守护进程中用常规方法输出的字符也不可能在终端显示出来。所以，文件描述符为0、1和2 的3个文件（输入、输出和报错）已经失去了存在的价值，也应被关闭。</li></ul><ol start="6"><li>守护进程退出处理<br>当用户需要外部停止守护进程运行时，往往会使用 kill 命令停止该守护进程。守护进程中需要编码来实现 kill 发出的signal信号处理，达到进程的正常退出。</li></ol><h2 id="进程和线程"><a href="#进程和线程" class="headerlink" title="进程和线程"></a>进程和线程</h2><h3 id="进程："><a href="#进程：" class="headerlink" title="进程："></a>进程：</h3><p><strong>程序段：</strong> 存放程序代码；<br><strong>数据段：</strong> 存放程序运行时使用、产生的运算数据，如全局变量、局部变量、宏定义的常量；<br><strong>PCB（task_struct）：</strong> 存放操作系统对程序进行管理所需的各种信息，如进程描述信息、进程控制和管理信息、资源分配清单、处理机相关信息。</p><h4 id="进程的组成"><a href="#进程的组成" class="headerlink" title="进程的组成"></a>进程的组成</h4><p>详细信息如下图：</p><p><img data-src="/transblog/images/416540910246114.png"></p><ul><li>代码段，包括<strong>二进制可执行代码</strong>；</li><li>数据段，包括<strong>已初始化的静态常量和全局变量</strong>；</li><li>BSS 段，包括<strong>未初始化的静态变量和全局变量</strong>；</li><li>堆段，包括动态分配的内存，<strong>从低地址开始向上增长</strong>；</li><li>文件映射段，包括动态库、共享内存等，<strong>从低地址开始向上增长</strong>（跟硬件和内核版本有关 (opens new window)）；</li><li>栈段，包括局部变量和函数调用的上下文等。<strong>从低地址开始向下增长</strong>，栈的大小是固定的，一般是 8 MB。当然系统也提供了参数，以便我们自定义大小；</li></ul><h4 id="并行和并发"><a href="#并行和并发" class="headerlink" title="并行和并发"></a>并行和并发</h4><p><img data-src="/transblog/images/468791310252792.png"></p><p>虽然单核的 CPU 在某一个瞬间，只能运行一个进程。但在 1 秒钟期间，它可能会运行多个进程，这样就产生并行的错觉，实际上这是并发。</p><h4 id="进程的状态"><a href="#进程的状态" class="headerlink" title="进程的状态"></a>进程的状态</h4><p><img data-src="/transblog/images/114001410257831.png"></p><ul><li>创建状态（new）：进程正在被创建时的状态；</li><li>结束状态（Exit）：进程正在从系统中消失时的状态；</li><li>运行状态（Running）：该时刻进程占用 CPU；</li><li>就绪状态（Ready）：可运行，由于其他进程处于运行状态而暂时停止运行；</li><li>阻塞状态（Blocked）：该进程正在等待某一事件发生（如等待输入&#x2F;输出操作的完成）而暂时停止运行，这时，即使给它CPU控制权，它也无法运行；</li></ul><h3 id="线程："><a href="#线程：" class="headerlink" title="线程："></a>线程：</h3><p><strong>线程是进程当中的一条执行流程</strong>****</p><p><img data-src="/transblog/images/254071810259126.png"></p><ul><li>同⼀个进程内多个线程之间可以共享代码段、数据段、打开的⽂件等资源，</li><li>但每个线程各自都有⼀套独⽴的寄存器和栈，这样可以确保线程的控制流是相对独⽴的</li></ul><h4 id="线程共享的环境"><a href="#线程共享的环境" class="headerlink" title="线程共享的环境"></a>线程共享的环境</h4><p>包括：进程代码段、进程的公有数据(利用这些共享的数据，线程很容易的实现相互之间的通讯)、进程打开的文件描述符、信号的处理器、进程的当前目录和进程用户ID与进程组ID。</p><h4 id="线程的优点"><a href="#线程的优点" class="headerlink" title="线程的优点"></a>线程的优点</h4><ul><li>一个进程中可以同时存在多个线程；</li><li>各个线程之间可以并发执行；</li><li>各个线程之间可以共享地址空间和文件等资源；</li></ul><h4 id="线程的缺点"><a href="#线程的缺点" class="headerlink" title="线程的缺点"></a>线程的缺点</h4><p>当进程中的一个线程崩溃时，会导致其所属进程的所有线程崩溃（这里是针对 C&#x2F;C++ 语言，Java语言中的线程奔溃不会造成进程崩溃）。<br>举个例子，对于游戏的用户设计，则不应该使用多线程的方式，否则一个用户挂了，会影响其他同个进程的线程。</p><h4 id="线程上下文切换"><a href="#线程上下文切换" class="headerlink" title="线程上下文切换"></a>线程上下文切换</h4><p>当两个线程不是属于同一个进程，则切换的过程就跟进程上下文切换一样；<br>当两个线程是属于同一个进程，因为虚拟内存是共享的，所以在切换时，虚拟内存这些资源就保持不动，只需要切换线程的私有数据、寄存器等不共享的数据；</p><h4 id="三种线程的实现⽅式"><a href="#三种线程的实现⽅式" class="headerlink" title="三种线程的实现⽅式"></a>三种线程的实现⽅式</h4><ul><li>⽤户线程（User Thread）：在⽤户空间实现的线程，不是由内核管理的线程，是由⽤户态的线程库来完成线程的管理；</li><li>内核线程（Kernel Thread）：在内核中实现的线程，是由内核管理的线程；</li><li>轻量级进程（LightWeight Process）：在内核中来⽀持⽤户线程；</li></ul><h5 id="用户线程"><a href="#用户线程" class="headerlink" title="用户线程"></a>用户线程</h5><p>用户线程的整个线程管理和调度，操作系统是不直接参与的，⽽是由⽤户级线程库函数来完成线程的管理，包括线程的创建、终⽌、同步和调度等。</p><p>⽤户线程的优点：</p><ul><li>每个进程都需要有它私有的线程控制块（TCB）列表，⽤来跟踪记录它各个线程状态信息（PC、栈指针、寄存器），TCB 由⽤户级线程库函数来维护，可⽤于不⽀持线程技术的操作系统</li><li>⽤户线程的切换也是由线程库函数来完成的，⽆需⽤户态与内核态的切换，所以速度特别快；</li></ul><p>⽤户线程的缺点：</p><ul><li>由于操作系统不参与线程的调度，如果⼀个线程发起了系统调⽤⽽阻塞，那进程所包含的⽤户线程都不能执⾏了。</li><li>当⼀个线程开始运⾏后，除⾮它主动地交出 CPU 的使⽤权，否则它所在的进程当中的其他线程⽆法运⾏，因为⽤户态的线程没法打断当前运⾏中的线程，它没有这个特权，只有操作系统才有，但是⽤户线程不是由操作系统管理的。</li><li>由于时间⽚分配给进程，故与其他进程⽐，在多线程执⾏时，每个线程得到的时间⽚较少，执⾏会⽐较慢；</li></ul><h5 id="内核线程"><a href="#内核线程" class="headerlink" title="内核线程"></a>内核线程</h5><p>内核线程是由操作系统管理的，线程对应的 TCB ⾃然是放在操作系统⾥的，这样线程的创建、终⽌和管理都是由操作系统负责。</p><p>内核线程的优点：</p><ul><li>在⼀个进程当中，如果某个内核线程发起系统调⽤⽽被阻塞，并不会影响其他内核线程的运⾏；</li><li>分配给线程，多线程的进程获得更多的 CPU 运⾏时间；</li></ul><p>内核线程的缺点：</p><ul><li>在⽀持内核线程的操作系统中，由内核来维护进程和线程的上下⽂信息，如 PCB 和TCB；</li><li>线程的创建、终⽌和切换都是通过系统调⽤的⽅式来进⾏，因此对于系统来说，系统开销⽐较⼤；</li></ul><h5 id="轻量级进程"><a href="#轻量级进程" class="headerlink" title="轻量级进程"></a>轻量级进程</h5><ul><li>轻量级进程（LWP）是内核支持的用户线程，一个进程可有一个或多个 LWP，每个 LWP 是跟内核线程一对一映射的，<strong>也就是 LWP 都是由一个内核线程支持，而且 LWP 是由内核管理并像普通进程一样被调度</strong>。</li><li>轻量级进程通常共享相同的地址空间，但具有独立的堆栈和寄存器状态，从而实现了类似进程的隔离和并发执行。</li><li>LWP与普通进程的区别也在于它只有一个最小的执行上下文和调度程序所需的统计信息</li><li>轻量级进程具有局限性。首先，大多数LWP的操作，如建立、析构以及同步，都需要进行系统调用，即在用户态和内核态进行切换。其次，每个LWP都需要有一个内核线程支持，因此LWP要消耗内核资源（内核线程的栈空间），所以一个系统不能支持大量的LWP。</li></ul><p><img data-src="/transblog/images/224831118267320.png"></p><h3 id="线程与进程的⽐较"><a href="#线程与进程的⽐较" class="headerlink" title="线程与进程的⽐较"></a>线程与进程的⽐较</h3><ol><li>进程是资源（包括内存、打开的⽂件等）分配的单位，线程是 CPU 调度的单位；</li><li>进程拥有⼀个完整的资源平台，⽽线程只独享必不可少的资源，如寄存器和栈；</li><li>线程同样具有就绪、阻塞、执⾏三种基本状态，同样具有状态之间的转换关系；</li><li>线程能减少并发执⾏的时间和空间开销；</li></ol><h3 id="线程相⽐进程能减少开销"><a href="#线程相⽐进程能减少开销" class="headerlink" title="线程相⽐进程能减少开销"></a>线程相⽐进程能减少开销</h3><ul><li>线程的创建时间⽐进程快，因为进程在创建的过程中，还需要资源管理信息，⽐如内存管理信息、⽂件管理信息，⽽线程在创建的过程中，不会涉及这些资源管理信息，⽽是共享它们；</li><li>线程的终⽌时间⽐进程快，因为线程释放的资源相⽐进程少很多；</li><li>同⼀个进程内的线程切换⽐进程切换快，因为线程具有相同的地址空间（虚拟内存共享），这意味着同⼀个进程的线程都具有同⼀个⻚表，在切换的时候不需要切换⻚表。⽽进程之间切换的时候要把⻚表给切换掉，⻚表的切换过程开销是⽐较⼤的；</li><li>由于同⼀进程的各线程间共享内存和⽂件资源，那么在线程之间数据传递的时候，就不需要经过内核了，这就使得线程之间的数据交互效率更⾼了；</li></ul><h3 id="多线程和多进程的比较"><a href="#多线程和多进程的比较" class="headerlink" title="多线程和多进程的比较"></a>多线程和多进程的比较</h3><p><img data-src="/transblog/images/2429254083522.png"></p><ol><li>需要频繁创建销毁的优先用线程。<br>实例：web服务器。来一个建立一个线程，断了就销毁线程。要是用进程，创建和销毁的代价是很难承受的。</li><li>需要进行大量计算的优先使用线程。<br>所谓大量计算，当然就是要消耗很多cpu，切换频繁了，这种情况先线程是最合适的。实例：图像处理、算法处理</li><li>强相关的处理用线程，弱相关的处理用进程。<br>一般的server需要完成如下任务：消息收发和消息处理。消息收发和消息处理就是弱相关的任务，而消息处理里面可能又分为消息解码、业务处理，这两个任务相对来说相关性就要强多了。因此消息收发和消息处理可以分进程设计，消息解码和业务处理可以分线程设计。</li><li>可能扩展到多机分布的用进程，多核分布的用线程。</li></ol><h2 id="协程"><a href="#协程" class="headerlink" title="协程"></a>协程</h2><p>协程可以理解为用户态的轻量级的非抢占式的线程。可以在程序的某个点挂起，并在稍后恢复执行</p><p><strong>原理：</strong></p><ul><li>我们知道操作系统在线程等待IO的时候，会阻塞当前线程，切换到其它线程，这样在当前线程等待IO的过程中，其它线程可以继续执行。</li><li>当线程数量非常多的时候，却产生了问题。一是系统线程会占用非常多的内存空间，二是过多的线程切换会占用大量的系统时间。</li><li>协程运行在线程之上，当一个协程执行完成后，可以选择主动让出，让另一个协程运行在当前线程之上。<mark>协程并没有增加线程数量，只是在线程的基础之上通过分时复用的方式运行多个协程</mark>，</li></ul><p><strong>特点：</strong><br>用户态:协程是在用户态实现调度。<br>轻量级:协程不用内核调度，不被被操作系统内核所管理，不需要内核态与用户态之间切换。<br>非抢占:协程是由用户自己实现调度，并且同一时间只能有一个协程在执行，协程自己主动交出CPU的。</p><p><strong>优点：</strong><br>协程切换的时候开销小，用户态且轻量<br>非抢占式，不用加很多锁，减小复杂度，不用很复杂的处理线程同步问题。</p><p><strong>缺点：</strong><br>协程可以在单线程内处理高并发,但是协程不能同时使用单个CPU的多个核心，不能利用多核，只能使用单核。<br>假设协程运行在线程之上，并且协程调用了一个阻塞IO操作，操作系统会让线程进入阻塞状态，<mark>当前的协程和其它绑定在该线程之上的协程都会陷入阻塞而得不到调度</mark>，这往往是不能接受的。因此，<strong>协程只有和异步IO结合起来才能发挥出最大的威力。</strong></p><h2 id="进程的通信"><a href="#进程的通信" class="headerlink" title="进程的通信"></a>进程的通信</h2><h3 id="管道"><a href="#管道" class="headerlink" title="管道"></a>管道</h3><p>所谓的管道，就是内核里面的一串缓存，所谓的管道，就是内核里面的一串缓存</p><h4 id="无名管道"><a href="#无名管道" class="headerlink" title="无名管道"></a>无名管道</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">pipe</span><span class="params">(<span class="type">int</span> fd[<span class="number">2</span>])</span></span>;</span><br></pre></td></tr></table></figure><p>fd[0]为读而打开，fd[1]为写而打开</p><p><img data-src="/transblog/images/312724210260128.png"></p><p>使用 fork 创建子进程，创建的子进程会复制父进程的文件描述符，但是管道只能一端写入，另一端读出，所以这种模式容易造成混乱，需要：<br>父进程关闭读取的 fd[0]，只保留写入的 fd[1]；<br>子进程关闭写入的 fd[1]，只保留读取的 fd[0]；</p><p><img data-src="/transblog/images/206554310260305.png">                   <strong>关闭后变为-&gt;</strong>        <img data-src="/transblog/images/345674310245857.png"></p><p><strong>执行A | B发生了什么</strong><br>A进程和 B 进程都是 shell 创建出来的子进程，A 和 B 之间不存在父子关系，它俩的父进程都是 shell。</p><p><img data-src="/transblog/images/291044710262136.png"></p><h4 id="命名管道"><a href="#命名管道" class="headerlink" title="命名管道"></a>命名管道</h4><p>也被叫做 FIFO ，因为数据是先进先出的传输⽅式。在使⽤命名管道前，先需要通过 mkfifo 命令来创建，并且指定管道名字：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ <span class="built_in">mkfifo</span> myPipe</span><br></pre></td></tr></table></figure><p>myPipe 就是这个管道的名称，基于 Linux ⼀切皆⽂件的理念，所以管道也是以⽂件的⽅式存在，我们可以⽤ ls 看⼀下，这个⽂件的类型是 p，也就是 pipe（管道） 的意思：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ <span class="built_in">ls</span> -l</span><br><span class="line">prw-r--r--. 1 root root 0 Jul 17 02:45 myPipe</span><br></pre></td></tr></table></figure><p>管道传输数据是单向的，如果想相互通信，我们需要创建两个管道才⾏。管道这种通信⽅式效率低，不适合进程间频繁地交换数据。    </p><ul><li>对于匿名管道，<strong>它的通信范围是存在⽗⼦关系的进程</strong>。因为管道没有实体，也就是没有管道⽂件，只能通过 fork 来复制⽗进程 fd ⽂件描述符，来达到通信的⽬的。</li><li>对于命名管道，<strong>它可以在不相关的进程间也能相互通信</strong>。因为命令管道，提前创建了⼀个类型为管道的设备⽂件，在进程⾥只要使⽤这个设备⽂件，就可以相互通信。</li></ul><h3 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">msgget</span>();</span><br></pre></td></tr></table></figure><p><mark>消息队列是保存在内核中的消息链表，</mark>消息队列不适合⽐较⼤数据的传输，<br><mark>消息队列通信过程中，存在⽤户态与内核态之间的数据拷⻉开销</mark>，因为进程写⼊数据到内核中的消息队列时，会发⽣从⽤户态拷⻉数据到内核态的过程，同理另⼀进程读取内核中的消息数据时，会发⽣从内核态拷⻉数据到⽤户态的过程。</p><h3 id="共享内存"><a href="#共享内存" class="headerlink" title="共享内存"></a>共享内存</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">shmget</span>();</span><br></pre></td></tr></table></figure><p>多个进程将同一个文件（内存的匿名段）映射到它们的地址空间<br>共享内存的机制，就是拿出⼀块虚拟地址空间来，映射到相同的物理内存中。</p><p><img data-src="/transblog/images/98108102469.png"></p><h4 id="存储映射I-O（mmap）"><a href="#存储映射I-O（mmap）" class="headerlink" title="存储映射I&#x2F;O（mmap）"></a>存储映射I&#x2F;O（mmap）</h4><p>mmap将一个文件或者其它对象映射进内存上<br>mmap操作提供了一种机制，让用户程序直接访问设备内存，这种机制，相比较在用户空间和内核空间互相拷贝数据，效率更高。在要求高性能的应用中比较常用。mmap映射内存必须是页面大小的整数倍，面向流的设备不能进行mmap，mmap的实现和硬件有关。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="type">void</span> *<span class="title">mmap</span><span class="params">(<span class="type">void</span> *addr,<span class="type">size_t</span> len,<span class="type">int</span> prot,<span class="type">int</span> flags,<span class="type">int</span> fd,<span class="type">off_t</span> off)</span></span>;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/4064114819868.png"></p><ul><li>两个进程中通信<br>两个程序映射同一个文件到自己的地址空间, 进程A先运行, 每隔两秒读取映射区域, 看是否发生变化. 进程B后运行, 它修改映射区域, 然后推出, 此时进程A能够观察到存储映射区的变化。</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span>  </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 100  </span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="type">int</span> fd, nread, i;  </span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">stat</span> sb;  </span><br><span class="line">    <span class="type">char</span> *mapped, buf[BUF_SIZE];  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; BUF_SIZE; i++) &#123;  </span><br><span class="line">        buf[i] = <span class="string">&#x27;#&#x27;</span>;  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    <span class="comment">/* 打开文件 */</span>  </span><br><span class="line">    <span class="keyword">if</span> ((fd = <span class="built_in">open</span>(argv[<span class="number">1</span>], O_RDWR)) &lt; <span class="number">0</span>) &#123;  </span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;open&quot;</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    <span class="comment">/* 获取文件的属性 */</span>  </span><br><span class="line">    <span class="keyword">if</span> ((<span class="built_in">fstat</span>(fd, &amp;sb)) == <span class="number">-1</span>) &#123;  </span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;fstat&quot;</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    <span class="comment">/* 将文件映射至进程的地址空间 */</span>  </span><br><span class="line">    <span class="keyword">if</span> ((mapped = (<span class="type">char</span> *)<span class="built_in">mmap</span>(<span class="literal">NULL</span>, sb.st_size, PROT_READ |   </span><br><span class="line">                    PROT_WRITE, MAP_SHARED, fd, <span class="number">0</span>)) == (<span class="type">void</span> *)<span class="number">-1</span>) &#123;  </span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;mmap&quot;</span>);  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    <span class="comment">/* 文件已在内存, 关闭文件也可以操纵内存 */</span>  </span><br><span class="line">    <span class="built_in">close</span>(fd);  </span><br><span class="line">      </span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 进程A：每隔两秒查看存储映射区是否被修改 */</span>  </span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, mapped);  </span><br><span class="line">        <span class="built_in">sleep</span>(<span class="number">2</span>);  </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 进程B：修改一个字符 */</span>  </span><br><span class="line">    mapped[<span class="number">20</span>] = <span class="string">&#x27;9&#x27;</span>;  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;  </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure><h3 id="信号量"><a href="#信号量" class="headerlink" title="信号量"></a>信号量</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">semget</span>();</span><br></pre></td></tr></table></figure><p>信号量其实是⼀个整型的计数器，主要⽤于实现进程间的互斥与同步，⽽不是⽤于缓存进程间通信的数据。</p><p>信号量表示资源的数量，控制信号量的⽅式有两种原⼦操作：</p><ul><li>⼀个是 P 操作，这个操作会把信号量减去 1，相减后如果信号量 &lt; 0，则表明资源已被占⽤，进程需阻塞等待；相减后如果信号量 &gt;&#x3D; 0，则表明还有资源可使⽤，进程可正常继续执⾏。</li><li>⼀个是 V 操作，这个操作会把信号量加上 1，相加后如果信号量 &lt;&#x3D; 0，则表明当前有阻塞中的进程，于是会将阻塞的进程唤醒运⾏；相加后如果信号量 &gt; 0，则表明当前没有阻塞中的进程；</li></ul><p><img data-src="/transblog/images/595705014240451.png"></p><ul><li><strong>信号初始化为 1 ，就代表着是互斥信号量</strong>，它可以保证共享内存在任何时刻只有⼀个进程在访问，这就很好的保护了共享内存<br>进程 A 在访问共享内存前，先执⾏了 P 操作，由于信号量的初始值为 1，故在进程 A 执 ⾏ P 操作后信号量变为 0，表示共享资源可⽤，于是进程 A 就可以访问共享内存。<br>若此时，进程 B 也想访问共享内存，执⾏了 P 操作，结果信号量变为了 -1，这就意味着临界资源已被占⽤，因此进程 B 被阻塞。<br>直到进程 A 访问完共享内存，才会执⾏ V 操作，使得信号量恢复为 0，接着就会唤醒阻塞中的线程 B，使得进程 B 可以访问共享内存，最后完成共享内存的访问后，执⾏ V 操作，使信号量恢复到初始值 1。</li></ul><p><img data-src="/transblog/images/290185114258877.png"></p><ul><li><strong>信号初始化为 0 ，就代表着是同步信号量</strong>，它可以保证进程 A 应在进程 B 之前执⾏。<br>如果进程 B ⽐进程 A 先执⾏了，那么执⾏到 P 操作时，由于信号量初始值为 0，故信号量会变为 -1，表示进程 A 还没⽣产数据，于是进程 B 就阻塞等待；<br>接着，当进程 A ⽣产完数据后，执⾏了 V 操作，就会使得信号量变为 0，于是就会唤醒阻塞在 P 操作的进程 B；<br>最后，进程 B 被唤醒后，意味着进程 A 已经⽣产了数据，于是进程 B 就可以正常读取数据了。</li></ul><h3 id="信号"><a href="#信号" class="headerlink" title="信号"></a>信号</h3><p>对于异常情况下的⼯作模式，就需要⽤「信号」的⽅式来通知进程。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">$ <span class="built_in">kill</span> -l</span><br><span class="line"> 1) SIGHUP 2) SIGINT 3) SIGQUIT 4) SIGILL 5)SIGTRAP</span><br><span class="line"> 6) SIGABRT 7) SIGBUS 8) SIGFPE 9) SIGKILL 10)SIGUSR1</span><br><span class="line">11) SIGSEGV 12) SIGUSR2 13) SIGPIPE 14) SIGALRM 15)SIGTERM</span><br><span class="line">16) SIGSTKFLT 17) SIGCHLD 18) SIGCONT 19) SIGSTOP 20)SIGTSTP</span><br><span class="line">21) SIGTTIN 22) SIGTTOU 23) SIGURG 24) SIGXCPU 25)SIGXFSZ</span><br><span class="line">26) SIGVTALRM 27) SIGPROF 28) SIGWINCH 29) SIGIO 30)SIGPWR</span><br><span class="line">31) SIGSYS 34) SIGRTMIN 35) SIGRTMIN+1 36) SIGRTMIN+2 37)SIGRTMIN+3</span><br><span class="line">38) SIGRTMIN+4 39) SIGRTMIN+5 40) SIGRTMIN+6 41) SIGRTMIN+7 42)SIGRTMIN+8</span><br><span class="line">43) SIGRTMIN+9 44) SIGRTMIN+10 45) SIGRTMIN+11 46) SIGRTMIN+12 47)SIGRTMIN+13</span><br><span class="line">48) SIGRTMIN+14 49) SIGRTMIN+15 50) SIGRTMAX-14 51) SIGRTMAX-13 52)SIGRTMAX-12</span><br><span class="line">53) SIGRTMAX-11 54) SIGRTMAX-10 55) SIGRTMAX-9 56) SIGRTMAX-8 57)SIGRTMAX-7</span><br><span class="line">58) SIGRTMAX-6 59) SIGRTMAX-5 60) SIGRTMAX-4 61) SIGRTMAX-3 62)SIGRTMAX-2</span><br></pre></td></tr></table></figure><p>Ctrl+C 产⽣ SIGINT（2） （可被忽略）信号，表示终⽌该进程；<br>Ctrl+Z 产⽣ SIGTSTOP（19） 信号，表示挂起该进程，但还未结束；</p><p><strong>唯⼀的异步通信机制</strong>。进程有三种⽅式响应信号 1. 执⾏默认操作、2. 捕捉信号、3. 忽略信号。有两个信号是应⽤进程<mark>⽆法捕捉和忽略</mark>的，即<mark>SIGKILL（9） 和 SEGSTOP（19） </mark>，这是为了⽅便我们能在任何时候结束或停⽌某个进程</p><h3 id="Socket"><a href="#Socket" class="headerlink" title="Socket"></a>Socket</h3><p>TCP网络编程</p><p><img data-src="/transblog/images/4171516095354.png"></p><ul><li>服务端和客户端初始化 socket ，得到⽂件描述符；</li><li>服务端调⽤ bind ，将绑定在 IP 地址和端⼝;</li><li>服务端调⽤ listen ，进⾏监听；</li><li>服务端调⽤ accept ，等待客户端连接；</li><li>客户端调⽤ connect ，向服务器端的地址和端⼝发起连接请求；</li><li>服务端 accept 返回⽤于传输的 socket 的⽂件描述符；</li><li>客户端调⽤ write 写⼊数据；服务端调⽤ read 读取数据；</li><li>客户端断开连接时，会调⽤ close ，那么服务端 read 读取数据的时候，就会读取到了EOF ，待处理完数据后，服务端调⽤ close ，表示连接关闭</li></ul><h2 id="线程的同步与互斥"><a href="#线程的同步与互斥" class="headerlink" title="线程的同步与互斥"></a>线程的同步与互斥</h2><p><strong>如果要访问共享资源（内存，变量等），必须考虑互斥</strong>，保证一个线程在临界区执行时，其他线程应该被阻止进入</p><h3 id="互斥量（互斥锁）"><a href="#互斥量（互斥锁）" class="headerlink" title="互斥量（互斥锁）"></a>互斥量（互斥锁）</h3><p>pthread_mutex_******<br>确保同一时间只有一个线程访问数据</p><h3 id="读写锁（共享互斥锁）"><a href="#读写锁（共享互斥锁）" class="headerlink" title="读写锁（共享互斥锁）"></a>读写锁（共享互斥锁）</h3><p>pthread_rwlock_******<br>一次只有一个线程可以占有写模式的读写锁，多个线程可以同时占有读模式的读写锁</p><ul><li>写加锁状态时，所有试图对这个锁加锁的线程都会被阻塞</li><li>读加锁状态时，所有试图以读模式对它加锁的线程都可以得到访问权，所有试图以写模式对它加锁的线程都会阻塞</li></ul><h3 id="条件变量"><a href="#条件变量" class="headerlink" title="条件变量"></a>条件变量</h3><p>pthread_cond_******</p><ul><li>条件变量与互斥量一起使用时，允许线程以无竞争的方式等待特定的条件发生。</li><li>条件本身是由互斥量保护的。线程在改变条件状态之前必须首先锁住互斥量。其他线程在获得互斥量之前不会察觉到这种改变，因为互斥量必须在锁定以后才能计算条件。</li><li>传递给pthread_cond_wait的<mark>互斥量对条件变量进行保护</mark>。调用者把锁住的互斥量传给函数，函数然后自动把调用线程放到等待条件的线程列表上，对互斥量解锁。</li></ul><h3 id="自旋锁"><a href="#自旋锁" class="headerlink" title="自旋锁"></a>自旋锁</h3><p>pthread_spin_******</p><ul><li>互斥锁加锁失败后，线程会释放 CPU ，给其他线程；</li><li>⾃旋锁加锁失败后，线程会忙等待，直到它拿到锁；</li><li>当加锁失败时，互斥锁⽤「线程切换」来应对，⾃旋锁则⽤「忙等待」来应对</li><li>在单核CPU 上，需要抢占式的调度器（即不断通过时钟中断⼀个线程，运⾏其他线程）。否则，⾃旋锁在单 CPU 上⽆法使⽤，因为⼀个⾃旋的线程永远不会放弃 CPU。</li></ul><h3 id="屏障"><a href="#屏障" class="headerlink" title="屏障"></a>屏障</h3><p>pthread_barrier_******<br>是用户协调多个线程并行工作的同步机制。屏障允许每个线程等待，直到所有的合作线程都达到某一点，然后从该点继续执行。pthread_join函数就是一种屏障，允许一个线程等待，直到另一个线程退出。<br>但是屏障对象的概念更广，它们允许任意数量的线程等待，直到所有的线程完成处理工作，而线程不需要退出。所有线程达到屏障后可以接着工作。</p><h3 id="信号量-1"><a href="#信号量-1" class="headerlink" title="信号量"></a>信号量</h3><p>P、V 操作，见上文信号量</p><h3 id="信号-1"><a href="#信号-1" class="headerlink" title="信号"></a>信号</h3><h2 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h2><h3 id="死锁的四个条件"><a href="#死锁的四个条件" class="headerlink" title="死锁的四个条件"></a>死锁的四个条件</h3><p>互斥条件；互斥条件是指多个线程不能同时使⽤同⼀个资源。<br>持有并等待条件； 一个进程本身占有资源（一种或多种），同时还有资源未得到满足，等待其他进程释放该资源。<br>不可剥夺条件；当线程已经持有了资源 ，在⾃⼰使⽤完之前不能被其他线程获取，<br>环路等待条件；在死锁发⽣的时候，两个线程获取资源的顺序构成了环形链。</p><h3 id="死锁预防"><a href="#死锁预防" class="headerlink" title="死锁预防"></a>死锁预防</h3><p>确保系统永远不会进入死锁状态<br><mark>a、破坏“占有且等待”条件</mark></p><ol><li>所有的进程在开始运行之前，必须一次性地申请其在整个运行过程中所需要的全部资源。</li><li>允许进程只获得运行初期需要的资源，便开始运行，在运行过程中逐步释放掉分配到的已经使用完毕的资源，然后再去请求新的资源。</li></ol><p><mark>b、破坏“不可抢占”条件</mark><br>当一个已经持有了一些资源的进程在提出新的资源请求没有得到满足时，它必须释放已经保持的所有资源，待以后需要使用的时候再重新申请。</p><p><mark>c、破坏“循环等待”条件</mark><br> 可以通过定义资源类型的线性顺序来预防，可将每个资源编号，当一个进程占有编号为i的资源时，那么它下一次申请资源只能申请编号大于i的资源。</p><h3 id="避免死锁"><a href="#避免死锁" class="headerlink" title="避免死锁"></a>避免死锁</h3><p>在分配资源时判断是否会出现死锁，只在不会出现死锁的情况下才分配资源。<br>两种避免办法：<br>    1、如果一个进程的请求会导致死锁，则不启动该进程<br>    2、如果一个进程的增加资源请求会导致死锁 ，则拒绝该申请。</p><p>避免死锁的具体实现通常利用<strong>银行家算法</strong><br>可利用资源向量Available：用于表示系统里边各种资源剩余的数目。<br>最大需求矩阵Max：用于表示各个进程对各种资源的额最大需求量。<br>分配矩阵Allocation：就是用于表示已经分配给各个进程的各种资源的数目。也是一个nxm的矩阵。<br>需求矩阵Need：用于表示进程仍然需要的资源数目，用一个nxm的矩阵表示。</p><h3 id="哲学家就餐问题"><a href="#哲学家就餐问题" class="headerlink" title="哲学家就餐问题"></a>哲学家就餐问题</h3><p>方案一：<br>让偶数编号的哲学家「先拿左边的叉⼦后拿右边的叉⼦」，奇数编号的哲学家「先拿右边的叉⼦后拿左边的叉⼦」。</p><p><img data-src="/transblog/images/2239037095963.png"></p><p>方案二：<br>用一个数组 state 来记录每一位哲学家的三个状态，分别是在进餐状态、思考状态、饥饿状态（正在试图拿叉子）。那么，一个哲学家只有在两个邻居都没有进餐时，才可以进入进餐状态。第 i 个哲学家的左邻右舍，则由宏 LEFT 和 RIGHT 定义：<br>LEFT : ( i + 5 - 1 ) % 5<br>RIGHT : ( i + 1 ) % 5<br>比如 i 为 2，则 LEFT 为 1，RIGHT 为 3。</p><p><img data-src="/transblog/images/283411416246744.png"></p><h3 id="悲观锁与乐观锁"><a href="#悲观锁与乐观锁" class="headerlink" title="悲观锁与乐观锁"></a>悲观锁与乐观锁</h3><ol><li>悲观锁做事⽐较悲观，它认为多线程同时修改共享资源的概率⽐较⾼，于是很容易出现冲突，所以访问共享资源前，先要上锁。</li><li>乐观锁做事⽐较乐观，它假定冲突的概率很低，先修改完共享资源，再验证这段时间内有没有发⽣冲突，如果没有其他线程在修改资源，那么操作完成，如果发现有其他线程已经修改过这个资源，就放弃本次操作。<br>应用：在线文档多人编辑</li></ol><h1 id="调度算法"><a href="#调度算法" class="headerlink" title="调度算法"></a>调度算法</h1><h2 id="调度的原则"><a href="#调度的原则" class="headerlink" title="调度的原则"></a>调度的原则</h2><ul><li>CPU 利用率：调度程序应确保 CPU 是始终匆忙的状态，这可提高 CPU 的利用率；</li><li>系统吞吐量：吞吐量表示的是单位时间内 CPU 完成进程的数量，长作业的进程会占用较长的 CPU 资源，因此会降低吞吐量，相反，短作业的进程会提升系统吞吐量；</li><li>周转时间：周转时间是进程运行+阻塞时间+等待时间的总和，一个进程的周转时间越小越好；</li><li>等待时间：这个等待时间不是阻塞状态的时间，而是进程处于就绪队列的时间，等待的时间越长，用户越不满意；</li><li>响应时间：用户提交请求到系统第一次产生响应所花费的时间，在交互式系统中，响应时间是衡量调度算法好坏的主要标准。</li></ul><h2 id="进程调度算法"><a href="#进程调度算法" class="headerlink" title="进程调度算法"></a>进程调度算法</h2><h3 id="先来先服务调度算法"><a href="#先来先服务调度算法" class="headerlink" title="先来先服务调度算法"></a>先来先服务调度算法</h3><h3 id="最短作业优先调度算法"><a href="#最短作业优先调度算法" class="headerlink" title="最短作业优先调度算法"></a>最短作业优先调度算法</h3><h3 id="高响应比优先调度算法"><a href="#高响应比优先调度算法" class="headerlink" title="高响应比优先调度算法"></a>高响应比优先调度算法</h3><p><img data-src="/transblog/images/4947119118995.png"></p><p>如果两个进程的「等待时间」相同时，「要求的服务时间」越短，「响应⽐」就越⾼，这样短作业的进程容易被选中运⾏；<br>如果两个进程「要求的服务时间」相同时，「等待时间」越⻓，「响应⽐」就越⾼，这就兼顾到了⻓作业进程，因为进程的响应⽐可以随时间等待的增加⽽提⾼，当其等待时间⾜够⻓时，其响应⽐便可以升到很⾼，从⽽获得运⾏的机会；</p><h3 id="时间⽚轮转调度算法"><a href="#时间⽚轮转调度算法" class="headerlink" title="时间⽚轮转调度算法"></a>时间⽚轮转调度算法</h3><p>如果时间⽚设得太短会导致过多的进程上下⽂切换，降低了 CPU 效率；如果设得太⻓⼜可能引起对短作业进程的响应时间变⻓。</p><h3 id="最⾼优先级调度算法"><a href="#最⾼优先级调度算法" class="headerlink" title="最⾼优先级调度算法"></a>最⾼优先级调度算法</h3><p>静态优先级：创建进程时候，就已经确定了优先级了，然后整个运⾏时间优先级都不会变化；<br>动态优先级：根据进程的动态变化调整优先级，如果进程运⾏时间增加，则降低其优先级，如果进程等待时间（就绪队列的等待时间）增加，则升⾼其优先级，也就是随着时间的推移增加等待进程的优先级。<br>⾮抢占式：当就绪队列中出现优先级⾼的进程，运⾏完当前进程，再选择优先级⾼的进程。<br>抢占式：当就绪队列中出现优先级⾼的进程，当前进程挂起，调度优先级⾼的进程运⾏。<br>但是依然有缺点，可能会导致低优先级的进程永远不会运⾏。</p><h3 id="多级反馈队列调度算法"><a href="#多级反馈队列调度算法" class="headerlink" title="多级反馈队列调度算法"></a>多级反馈队列调度算法</h3><p><img data-src="/transblog/images/5290937106862.png"></p><p>设置了多个队列，赋予每个队列不同的优先级，每个队列优先级从⾼到低，同时优先级越⾼时间⽚越短；新的进程会被放⼊到第⼀级队列的末尾，按先来先服务的原则排队等待被调度，如果在第⼀级队列规定的时间⽚没运⾏完成，则将其转⼊到第⼆级队列的末尾，以此类推，直⾄完成；<br>当较⾼优先级的队列为空，才调度较低优先级的队列中的进程运⾏。如果进程运⾏时，有新进程进⼊较⾼优先级的队列，则停⽌当前运⾏的进程并将其移⼊到原队列末尾，接着让较⾼优先级的进程运⾏；</p><h2 id="内存页面置换算法"><a href="#内存页面置换算法" class="headerlink" title="内存页面置换算法"></a>内存页面置换算法</h2><h3 id="最佳⻚⾯置换算法"><a href="#最佳⻚⾯置换算法" class="headerlink" title="最佳⻚⾯置换算法"></a>最佳⻚⾯置换算法</h3><p>最佳⻚⾯置换算法基本思路是，置换在「未来」最⻓时间不访问的⻚⾯。</p><h3 id="先进先出置换算法"><a href="#先进先出置换算法" class="headerlink" title="先进先出置换算法"></a>先进先出置换算法</h3><p>既然我们⽆法预知⻚⾯在下⼀次访问前所需的等待时间，那我们可以选择在内存驻留时间很⻓的⻚⾯进⾏中置换，这个就是「先进先出置换」算法的思想</p><h3 id="最近最久未使⽤的置换算法"><a href="#最近最久未使⽤的置换算法" class="headerlink" title="最近最久未使⽤的置换算法"></a>最近最久未使⽤的置换算法</h3><p>最近最久未使⽤（LRU）的置换算法的基本思路是，发⽣缺⻚时，选择最⻓时间没有被访问的⻚⾯进⾏置换，</p><h4 id="LRU缓存"><a href="#LRU缓存" class="headerlink" title="LRU缓存"></a>LRU缓存</h4><p>像浏览器的缓存策略、memcached的缓存策略都是使用LRU这个算法，LRU算法会将近期最不会访问的数据淘汰掉。<br><img data-src="/transblog/images/1128100686625.png"></p><ul><li>新数据插入到链表头部</li><li>每当缓存命中（即缓存数据被访问），则将数据移到链表头部</li><li>当链表满的时候，将链表尾部的数据丢弃</li></ul><p>LRU Cache具备的操作：<br>set(key,value)：如果key在hashmap中存在，则先重置对应的value值，然后获取对应的节点cur，将cur节点从链表删除，并移动到链表的头部；若果key在hashmap不存在，则新建一个节点，并将节点放到链表的头部。当Cache存满的时候，将链表最后一个节点删除即可。<br>get(key)：如果key在hashmap中存在，则把对应的节点放到链表头部，并返回对应的value值；如果不存在，则返回-1。</p><p>LRU的c++实现：<br>LRU实现采用双向链表 + Map 来进行实现。这里采用双向链表的原因是：如果采用普通的单链表，则删除节点的时候需要从表头开始遍历查找，效率为O(n)，采用双向链表可以直接改变节点的前驱的指针指向进行删除达到O(1)的效率。使用Map来保存节点的key、value值便于能在O(logN)的时间查找元素,对应get操作。</p><p>双链表节点的定义：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">CacheNode</span> &#123;</span><br><span class="line"> <span class="type">int</span> key;  <span class="comment">// 键</span></span><br><span class="line"> <span class="type">int</span> value; <span class="comment">// 值</span></span><br><span class="line"> CacheNode *pre, *next; <span class="comment">// 节点的前驱、后继指针</span></span><br><span class="line"> <span class="built_in">CacheNode</span>(<span class="type">int</span> k, <span class="type">int</span> v) : <span class="built_in">key</span>(k), <span class="built_in">value</span>(v), <span class="built_in">pre</span>(<span class="literal">NULL</span>), <span class="built_in">next</span>(<span class="literal">NULL</span>) &#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>对于LRUCache这个类而言，构造函数需要指定容量大小</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">LRUCache</span>(<span class="type">int</span> capacity)</span><br><span class="line">&#123;</span><br><span class="line"> size = capacity;  <span class="comment">// 容量</span></span><br><span class="line"> head = <span class="literal">NULL</span>;   <span class="comment">// 链表头指针</span></span><br><span class="line"> tail = <span class="literal">NULL</span>;   <span class="comment">// 链表尾指针</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>双链表的节点删除操作：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">remove</span><span class="params">(CacheNode *node)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> <span class="keyword">if</span> (node -&gt; pre != <span class="literal">NULL</span>)</span><br><span class="line"> &#123;</span><br><span class="line"> node -&gt; pre -&gt; next = node -&gt; next;</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">else</span></span><br><span class="line"> &#123;</span><br><span class="line"> head = node -&gt; next;</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">if</span> (node -&gt; next != <span class="literal">NULL</span>)</span><br><span class="line"> &#123;</span><br><span class="line"> node -&gt; next -&gt; pre = node -&gt; pre;</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">else</span></span><br><span class="line"> &#123;</span><br><span class="line"> tail = node -&gt; pre;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>将节点插入到头部的操作：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">setHead</span><span class="params">(CacheNode *node)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> node -&gt; next = head;</span><br><span class="line"> node -&gt; pre = <span class="literal">NULL</span>;</span><br><span class="line"> </span><br><span class="line"> <span class="keyword">if</span> (head != <span class="literal">NULL</span>)</span><br><span class="line"> &#123;</span><br><span class="line"> head -&gt; pre = node;</span><br><span class="line"> &#125;</span><br><span class="line"> head = node;</span><br><span class="line"> <span class="keyword">if</span> (tail == <span class="literal">NULL</span>)</span><br><span class="line"> &#123;</span><br><span class="line"> tail = head;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>get(key)操作的实现比较简单，直接通过判断Map是否含有key值即可，如果查找到key，则返回对应的value，否则返回-1;</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">get</span><span class="params">(<span class="type">int</span> key)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> map&lt;<span class="type">int</span>, CacheNode *&gt;::iterator it = mp.<span class="built_in">find</span>(key);</span><br><span class="line"> <span class="keyword">if</span> (it != mp.<span class="built_in">end</span>())</span><br><span class="line"> &#123;</span><br><span class="line"> CacheNode *node = it -&gt; second;</span><br><span class="line"> <span class="built_in">remove</span>(node);</span><br><span class="line"> <span class="built_in">setHead</span>(node);</span><br><span class="line"> <span class="keyword">return</span> node -&gt; value;</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">else</span></span><br><span class="line"> &#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>set(key, value)操作需要分情况判断。如果当前的key值对应的节点已经存在，则将这个节点取出来，并且删除节点所处的原有的位置，并在头部插入该节点；如果节点不存在节点中，这个时候需要在链表的头部插入新节点，插入新节点可能导致容量溢出，如果出现溢出的情况，则需要删除链表尾部的节点。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">set</span><span class="params">(<span class="type">int</span> key, <span class="type">int</span> value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> map&lt;<span class="type">int</span>, CacheNode *&gt;::iterator it = mp.<span class="built_in">find</span>(key);</span><br><span class="line"> <span class="keyword">if</span> (it != mp.<span class="built_in">end</span>())</span><br><span class="line"> &#123;</span><br><span class="line"> CacheNode *node = it -&gt; second;</span><br><span class="line"> node -&gt; value = value;</span><br><span class="line"> <span class="built_in">remove</span>(node);</span><br><span class="line"> <span class="built_in">setHead</span>(node);</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">else</span></span><br><span class="line"> &#123;</span><br><span class="line"> CacheNode *newNode = <span class="keyword">new</span> <span class="built_in">CacheNode</span>(key, value);</span><br><span class="line"> <span class="keyword">if</span> (mp.<span class="built_in">size</span>() &gt;= size)</span><br><span class="line"> &#123;</span><br><span class="line">  map&lt;<span class="type">int</span>, CacheNode *&gt;::iterator iter = mp.<span class="built_in">find</span>(tail -&gt; key);</span><br><span class="line">  <span class="built_in">remove</span>(tail);</span><br><span class="line">  mp.<span class="built_in">erase</span>(iter);</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="built_in">setHead</span>(newNode);</span><br><span class="line"> mp[key] = newNode;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="时钟⻚⾯置换算法"><a href="#时钟⻚⾯置换算法" class="headerlink" title="时钟⻚⾯置换算法"></a>时钟⻚⾯置换算法</h3><p><img data-src="/transblog/images/5742525189098.png"></p><p><img data-src="/transblog/images/1333708746621.png"></p><h3 id="最不常⽤算法"><a href="#最不常⽤算法" class="headerlink" title="最不常⽤算法"></a>最不常⽤算法</h3><p>当发⽣缺⻚中断时，选择「访问次数」最少的那个⻚⾯，并将其淘汰。</p><h2 id="磁盘调度算法"><a href="#磁盘调度算法" class="headerlink" title="磁盘调度算法"></a>磁盘调度算法</h2><h3 id="先来先服务算法"><a href="#先来先服务算法" class="headerlink" title="先来先服务算法"></a>先来先服务算法</h3><h3 id="最短寻道时间优先算法"><a href="#最短寻道时间优先算法" class="headerlink" title="最短寻道时间优先算法"></a>最短寻道时间优先算法</h3><p>根据距离磁头最近的请求的算法</p><h3 id="扫描算法（电梯算法）"><a href="#扫描算法（电梯算法）" class="headerlink" title="扫描算法（电梯算法）"></a>扫描算法（电梯算法）</h3><p>磁头在⼀个⽅向上移动，访问所有未完成的请求，直到磁头到达该⽅向上的最后的磁道，才调换⽅向，这就是扫描（Scan）算法。</p><p><img data-src="/transblog/images/5363799515717.png"></p><h3 id="循环扫描算法"><a href="#循环扫描算法" class="headerlink" title="循环扫描算法"></a>循环扫描算法</h3><p>只有磁头朝某个特定⽅向移动时，才处理磁道访问请求，⽽返回时直接快速移动⾄最靠边缘的磁道，也就是复位磁头，这个过程是很快的，并且返回中途不处理任何请求，该算法的特点，就是磁道只响应⼀个⽅向上的请求。</p><p><img data-src="/transblog/images/3568121535713.png"></p><p>循环扫描算法相⽐于扫描算法，对于各个位置磁道响应频率相对⽐较平均。</p><h1 id="文件系统"><a href="#文件系统" class="headerlink" title="文件系统"></a>文件系统</h1><p>一切皆文件</p><h2 id="Linux的文件类型"><a href="#Linux的文件类型" class="headerlink" title="Linux的文件类型"></a>Linux的文件类型</h2><ol><li>普通文件<br>普通文件根据存放的内容的不同，又分为如下两种：</li></ol><ul><li>文本文件<br>存放的都是文字编码，文本编辑器打开后，会将这些文字编码翻译为文字图形，以供人识别。</li><li>纯二进制文件（机器码）<ol><li>比如经过编译后得到的可执行文件，里面放的是cpu执行的纯二进制机器码，由于文编编辑器只认识文字编码，所以用文本编辑器打开后，显示的内容无法是错乱的，无法辨识。命令cat就是一个二进制文件</li><li>其实不管存放的是文字编码，还是机器码，在计算机中存储时，其实都是以二进制形式存放的，只不过我们这里可刻意的把机器码这类非文字编码的数据，特意强调为了二进制数据。</li><li>对linux内核而言，这两种文件并无区别，至于文件中的数据如何解释，则由处理这些数据的应用程序（比如文本编辑器）来决定。</li><li>不管是文字编码数据，还是纯二进制数据，应用程序调用read、write读写文件时，没有任何区别。</li></ol></li></ul><ol start="2"><li>目录文件<br>目录是一种特殊的文件，专门用于管理其它文件。第一个属性为 [d]，例如 [drwxrwxrwx]。</li><li>字符设备文件<br>字符设备文件，就是字符设备驱动程序，在上层的表现形式。<br>当应用程序调用底层字符设备驱动程序，实现对某个字符设备进行读写时，上层就需要对接底层的字符驱动程序，字符设备驱动在上层，会以“字符设备文件”的形式表现出来，我们通过open、read、write去读写字符设备文件，就实现了和底层字符设备驱动程序的交互。</li><li>块设备文件<br>对应块设备（如磁盘等）。<br>块设备文件，是块设备驱动程序在上层的表现形式。</li></ol><ul><li><strong>字符设备与块设备有什么区别？</strong><ol><li>字符设备<br>  以字节为单位来操作数据。比如：键盘、鼠标、显示器都等是字符设备。</li><li>块设备<br>  块设备存储的数据量往往非常大，为了提高读写效率，都是以块（1024字节）为单位来操作数据。比如：电脑硬盘、移动硬盘、u盘等，凡是涉及大量数据存储的，都是以块为单位来操作数据的，都是块设备。</li></ol></li></ul><ol start="5"><li>FIFO（fifo：p）<br>管道文件，用于实现不同进程（程序）之间的通信，管道是OS提供的一种纯代码层面的通信机制。<br>A进程 ————————&gt; 管道文件 ————————&gt;B进程</li><li>套接字文件（socket：s）<br>专门用于网络通信的文件。</li></ol><h2 id="文件的管理"><a href="#文件的管理" class="headerlink" title="文件的管理"></a>文件的管理</h2><p>索引节点，也就是 inode，用来记录文件的元信息，比如 inode 编号、文件大小、访问权限、创建时间、修改时间、数据在磁盘的位置等等。索引节点是文件的唯一标识，它们之间一一对应，也同样都会被存储在硬盘中，所以索引节点同样占用磁盘空间。<br>目录项，也就是 dentry，用来记录文件的名字、索引节点指针以及与其他目录项的层级关联关系。多个目录项关联起来，就会形成目录结构，但它与索引节点不同的是，<strong>目录项是由内核维护的一个数据结构，不存放于磁盘，而是缓存在内存。</strong></p><p>磁盘读写的最小单位是扇区，扇区的大小只有 512B 大小，Linux 读写逻辑块，逻辑块大小为 4KB，也就是一次性读写 8 个扇区。</p><p><img data-src="/transblog/images/503013809240452.png"></p><p>磁盘进行格式化的时候，会被分成三个存储区域，分别是超级块、索引节点区和数据块区。</p><ul><li>超级块：用来存储文件系统的详细信息，比如块个数、块大小、空闲块等等。当文件系统挂载时进入内存。</li><li>索引节点区：用来存储索引节点；当文件被访问时进入内存</li><li>数据块区：用来存储文件或目录数据；</li></ul><h2 id="文件的存放方式"><a href="#文件的存放方式" class="headerlink" title="文件的存放方式"></a>文件的存放方式</h2><h3 id="连续空间存放方式"><a href="#连续空间存放方式" class="headerlink" title="连续空间存放方式"></a>连续空间存放方式</h3><p>缺陷：「磁盘空间碎片」和「文件长度不易扩展」</p><h3 id="非连续空间存放方式："><a href="#非连续空间存放方式：" class="headerlink" title="非连续空间存放方式："></a>非连续空间存放方式：</h3><h4 id="链表方式"><a href="#链表方式" class="headerlink" title="链表方式"></a>链表方式</h4><h5 id="隐式链表"><a href="#隐式链表" class="headerlink" title="隐式链表"></a>隐式链表</h5><p>实现的方式是文件头要包含「第一块」和「最后一块」的位置，并且每个数据块里面留出一个指针空间，用来存放下一个数据块的位置，</p><p><img data-src="/transblog/images/104654809258878.png"></p><p>缺点在于无法直接访问数据块，只能通过指针顺序访问文件，以及数据块指针消耗了一定的存储空间。隐式链接分配的稳定性较差，系统在运行过程中由于软件或者硬件错误导致链表中的指针丢失或损坏，会导致文件数据的丢失</p><h5 id="显式链接"><a href="#显式链接" class="headerlink" title="显式链接"></a>显式链接</h5><p>它指把用于链接文件各数据块的指针，显式地存放在内存的一张链接表中，该表在整个磁盘仅设置一张，每个表项中存放链接指针，指向下一个数据块号。<br>缺点是不适用于大磁盘</p><p><img data-src="/transblog/images/280175009246745.png"></p><h4 id="索引方式"><a href="#索引方式" class="headerlink" title="索引方式"></a>索引方式</h4><h5 id="单索引"><a href="#单索引" class="headerlink" title="单索引"></a>单索引</h5><p><img data-src="/transblog/images/552165409256135.png"></p><h5 id="链表-索引"><a href="#链表-索引" class="headerlink" title="链表+索引"></a>链表+索引</h5><p><img data-src="/transblog/images/437125409259580.png"></p><h5 id="多级索引块"><a href="#多级索引块" class="headerlink" title="多级索引块"></a>多级索引块</h5><p><img data-src="/transblog/images/60975509251889.png"></p><h2 id="Unix和Linux-Ext-2-3"><a href="#Unix和Linux-Ext-2-3" class="headerlink" title="Unix和Linux Ext 2&#x2F;3"></a>Unix和Linux Ext 2&#x2F;3</h2><p><img data-src="/transblog/images/144745809269769.png"></p><h2 id="空闲空间管理"><a href="#空闲空间管理" class="headerlink" title="空闲空间管理"></a>空闲空间管理</h2><h3 id="空闲表法"><a href="#空闲表法" class="headerlink" title="空闲表法"></a>空闲表法</h3><p>适用于建立连续文件</p><p><img data-src="/transblog/images/527295909267373.png"></p><h3 id="空闲链表法"><a href="#空闲链表法" class="headerlink" title="空闲链表法"></a>空闲链表法</h3><p><img data-src="/transblog/images/145370110264875.png"></p><h3 id="位图法"><a href="#位图法" class="headerlink" title="位图法"></a>位图法</h3><p>在 Linux 文件系统就采用了位图的方式来管理空闲空间，不仅用于数据空闲块的管理，还用于 inode 空闲块的管理，</p><p>「位图 + 数据」<br>每位表示一个数据块，共可以表示 4 * 1024 * 8 &#x3D; 2^15 个空闲块，由于 1 个数据块是 4K 大小，那么最大可以表示的空间为 2^15 * 4 * 1024 &#x3D; 2^27 个 byte，也就是 128M。表示的大小过少，Linux使用块组结构</p><p><img data-src="/transblog/images/594342710246116.png"></p><ul><li>超级块，包含的是文件系统的重要信息，比如 inode 总个数、块总个数、每个块组的 inode 个数、每个块组的块个数等等。</li><li>块组描述符，包含文件系统中各个块组的状态，比如块组中空闲块和 inode 的数目等，每个块组都包含了文件系统中「所有块组的组描述符信息」。</li><li>数据位图和 inode 位图， 用于表示对应的数据块或 inode 是空闲的，还是被使用中。</li><li>inode 列表，包含了块组中所有的 inode，inode 用于保存文件系统中与各个文件和目录相关的所有元数据。</li><li>数据块，包含文件的有用数据。</li></ul><p>超级块和块组描述符表，这两个都是全局信息，而且非常的重要，这么做是有两个原因：<br>如果系统崩溃破坏了超级块或块组描述符，有关文件系统结构和内容的所有信息都会丢失。如果有冗余的副本，该信息是可能恢复的。<br>通过使文件和管理数据尽可能接近，减少了磁头寻道和旋转，这可以提高文件系统的性能。</p><h2 id="目录的管理"><a href="#目录的管理" class="headerlink" title="目录的管理"></a>目录的管理</h2><p>目录文件的块里面保存的是目录里面一项一项的文件信息。</p><p><img data-src="/transblog/images/301453410268556.png"></p><h2 id="软链接-ln-s-和硬链接-ln-d）"><a href="#软链接-ln-s-和硬链接-ln-d）" class="headerlink" title="软链接(ln -s)和硬链接(ln -d）"></a>软链接(ln -s)和硬链接(ln -d）</h2><h3 id="硬链接"><a href="#硬链接" class="headerlink" title="硬链接"></a>硬链接</h3><p> 多个目录项中的「索引节点」指向一个文件，也就是指向同一个 inode。<strong>每个文件系统都有各自的 inode 数据结构和列表，所以硬链接是不可用于跨文件系统的</strong>。由于多个目录项都是指向一个 inode，那么只有删除文件的所有硬链接以及源文件时，系统才会彻底删除该文件。</p><p><img data-src="/transblog/images/366183610263692.png"></p><h3 id="软链接"><a href="#软链接" class="headerlink" title="软链接"></a>软链接</h3><p>相当于重新创建一个文件，这个文件有独立的 inode，但是这个文件的内容是另外一个文件的路径，所以访问软链接的时候，实际上相当于访问到了另外一个文件，所以软链接是可以跨文件系统的，甚至目标文件被删除了，链接文件还是在的，只不过指向的文件找不到了而已。</p><p><img data-src="/transblog/images/234184310240902.png" alt="软链接"></p><h2 id="Page-Cache"><a href="#Page-Cache" class="headerlink" title="Page Cache"></a>Page Cache</h2><p>非直接I&#x2F;O中，内核会找个合适的时机，将 page cache 中的数据持久化到磁盘。但是如果 page cache 里的文件数据，在持久化到磁盘化到磁盘之前，系统发生了崩溃，那这部分数据就会丢失了。</p><p>当然， 我们也可以在程序里调用 fsync 函数，在写文文件的时候，立刻将文件数据持久化到磁盘，这样就可以解决系统崩溃导致的文件数据丢失的问题。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">cat /proc/meminfo</span><br><span class="line"></span><br><span class="line">Buffers + Cached + SwapCached = Active(file) + Inactive(file) + Shmem + SwapCached</span><br></pre></td></tr></table></figure><p>Page Cache 用于缓存文件的页数据，buffer cache 用于缓存块设备（如磁盘）的块数据。</p><p><img data-src="/transblog/images/404355917247154.png"></p><h3 id="文件持久化的一致性-可靠性"><a href="#文件持久化的一致性-可靠性" class="headerlink" title="文件持久化的一致性&amp;可靠性"></a>文件持久化的一致性&amp;可靠性</h3><p>当前 Linux 下以两种方式实现文件一致性：</p><p>Write Through（写穿）：向用户层提供特定接口，应用程序可主动调用接口来保证文件一致性；<br>Write back（写回）：系统中存在定期任务（表现形式为内核线程），周期性地同步文件系统中文件脏数据块，这是默认的 Linux 一致性方案；</p><table><thead><tr><th align="center">方法</th><th align="center">含义</th></tr></thead><tbody><tr><td align="center">fsync(intfd)</td><td align="center">fsync(fd)：将 fd 代表的文件的脏数据和脏元数据全部刷新至磁盘中。</td></tr><tr><td align="center">fdatasync(int fd)</td><td align="center">fdatasync(fd)：将 fd 代表的文件的脏数据刷新至磁盘，同时对必要的元数据刷新至磁盘中，这里所说的必要的概念是指：对接下来访问文件有关键作用的信息，如文件大小，而文件修改时间等不属于必要信息</td></tr><tr><td align="center">sync()</td><td align="center">sync()：则是对系统中所有的脏的文件数据元数据刷新至磁盘中</td></tr></tbody></table><ul><li>关于多线程的架构问题，Linux 内核采取了 Lighthttp 的做法，即系统中存在一个管理线程和多个刷新线程（每个持久存储设备对应一个刷新线程）。</li><li>管理线程监控设备上的脏页面情况，若设备一段时间内没有产生脏页面，就销毁设备上的刷新线程；若监测到设备上有脏页面需要回写且尚未为该设备创建刷新线程，那么创建刷新线程处理脏页面回写。</li><li>而刷新线程的任务较为单调，只负责将设备中的脏页面回写至持久存储设备中。</li></ul><p>刷新线程刷新设备上脏页面大致设计如下：<br>每个设备保存脏文件链表，保存的是该设备上存储的脏文件的 inode 节点。所谓的回写文件脏页面即回写该 inode 链表上的某些文件的脏页面；</p><p>系统中存在多个回写时机:</p><ol><li>第一是应用程序主动调用回写接口（fsync，fdatasync 以及 sync 等）</li><li>第二管理线程周期性地唤醒设备上的回写线程进行回写</li><li>第三是某些应用程序&#x2F;内核任务发现内存不足时要回收部分缓存页面而事先进行脏页面回写，设计一个统一的框架来管理这些回写任务非常有必要。</li></ol><h2 id="缺页中断-1"><a href="#缺页中断-1" class="headerlink" title="缺页中断"></a>缺页中断</h2><p>操作系统以 page 为单位管理内存，当进程发现需要访问的数据不在内存时，操作系统可能会将数据以页的方式加载到内存中。上述过程被称为<strong>缺页中断</strong>，当操作系统发生缺页中断时，就会通过系统调用将 page 再次读到内存中。</p><h2 id="文件I-O"><a href="#文件I-O" class="headerlink" title="文件I&#x2F;O"></a>文件I&#x2F;O</h2><h3 id="缓冲与非缓冲-I-O"><a href="#缓冲与非缓冲-I-O" class="headerlink" title="缓冲与非缓冲 I&#x2F;O"></a>缓冲与非缓冲 I&#x2F;O</h3><p>文件操作的标准库是可以实现数据的缓存，那么根据「是否利用标准库缓冲」，可以把文件 I&#x2F;O 分为缓冲 I&#x2F;O 和非缓冲 I&#x2F;O：</p><ul><li>缓冲 I&#x2F;O，利用的是标准库的缓存实现文件的加速访问，而标准库再通过系统调用访问文件。</li><li>非缓冲 I&#x2F;O，直接通过系统调用访问文件，不经过标准库缓存。<br>这里所说的「缓冲」特指标准库内部实现的缓冲。</li></ul><p>例如，很多程序遇到换行时才真正输出，而换行前的内容，其实就是被标准库暂时缓存了起来，这样做的目的是，减少系统调用的次数，毕竟系统调用是有 CPU 上下文切换的开销的。</p><h3 id="直接与非直接-I-O"><a href="#直接与非直接-I-O" class="headerlink" title="直接与非直接 I&#x2F;O"></a>直接与非直接 I&#x2F;O</h3><p>我们都知道磁盘 I&#x2F;O 是非常慢的，所以 Linux 内核为了减少磁盘 I&#x2F;O 次数，在系统调用后，会把用户数据拷贝到内核中缓存起来，<strong>这个内核缓存空间也就是「页缓存」（page cache），只有当缓存满足某些条件的时候，才发起磁盘 I&#x2F;O 的请求。</strong></p><p>那么，根据是「否利用操作系统的缓存」，可以把文件 I&#x2F;O 分为直接 I&#x2F;O 与非直接 I&#x2F;O：</p><p>直接 I&#x2F;O，不会发生内核缓存和用户程序之间数据复制，而是直接经过文件系统访问磁盘。<br>非直接 I&#x2F;O，读操作时，数据从内核缓存中拷贝给用户程序，写操作时，数据从用户程序拷贝给内核缓存，再由内核决定什么时候写入数据到磁盘。</p><p><strong>非直接I&#x2F;O下，什么时候刷盘？</strong></p><ol><li>在调用 write 的最后，当发现内核缓存的数据太多的时候，内核会把数据写到磁盘上；</li><li>用户主动调用 sync，内核缓存会刷到磁盘上；</li><li>当内存十分紧张，无法再分配页面时，也会把内核缓存的数据刷到磁盘上；</li><li>内核缓存的数据的缓存时间超过某个时间时，也会把数据刷到磁盘上；</li></ol><h1 id="设备管理"><a href="#设备管理" class="headerlink" title="设备管理"></a>设备管理</h1><p><img data-src="/transblog/images/106675315259580.png"></p><h2 id="I-O-控制⽅式"><a href="#I-O-控制⽅式" class="headerlink" title="I&#x2F;O 控制⽅式"></a>I&#x2F;O 控制⽅式</h2><h3 id="轮询等待"><a href="#轮询等待" class="headerlink" title="轮询等待"></a>轮询等待</h3><p>让 CPU ⼀直查寄存器的状态，直到状态标记为完成，很明显，这种⽅式⾮常的傻⽠，它会占⽤ CPU 的全部时间。</p><h3 id="中断"><a href="#中断" class="headerlink" title="中断"></a>中断</h3><p>通知操作系统数据已经准备好了。我们⼀般会有⼀个硬件的中断控制器，当设备完成任务后触发中断到中断控制器，中断控制器就通知 CPU，⼀个中断产⽣了，CPU 需要停下当前⼿⾥的事情来处理中断。<br>另外，中断有两种，⼀种软中断，例如代码调⽤ INT 指令触发，⼀种是硬件中断，就是硬件通过中断控制器触发的。<br>但中断的⽅式对于频繁读写数据的磁盘，并不友好，这样 CPU 容易经常被打断，会占⽤CPU ⼤量的时间。对于这⼀类设备的问题的解决⽅法是使⽤ DMA功能，它可以使得设备在 CPU 不参与的情况下，能够⾃⾏完成把设备 I&#x2F;O 数据放⼊到内存。那要实现 DMA 功能要有 「DMA 控制器」硬件的⽀持。</p><h2 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a>零拷贝</h2><h3 id="DMA控制器"><a href="#DMA控制器" class="headerlink" title="DMA控制器"></a>DMA控制器</h3><p>在进⾏ I&#x2F;O 设备和内存的数据传输的时候，数据搬运的⼯作全部交给 DMA 控制器，⽽ CPU 不再参与任何与数据搬运相关的事情，这样 CPU 就可以去处理别的事务。</p><p><img data-src="/transblog/images/3320624841468.png" alt="DMA搬运数据流程"></p><ol><li>⽤户进程调⽤ read ⽅法，向操作系统发出 I&#x2F;O 请求，请求读取数据到⾃⼰的内存缓冲区中，进程进⼊阻塞状态；</li><li>操作系统收到请求后，进⼀步将 I&#x2F;O 请求发送 DMA，然后让 CPU 执⾏其他任务；</li><li>DMA 进⼀步将 I&#x2F;O 请求发送给磁盘；</li><li>磁盘收到 DMA 的 I&#x2F;O 请求，把数据从磁盘读取到磁盘控制器的缓冲区中，当磁盘控制器的缓冲区被读满后，向 DMA 发起中断信号，告知⾃⼰缓冲区已满；</li><li>DMA 收到磁盘的信号，将磁盘控制器缓冲区中的数据拷⻉到内核缓冲区中，此时不占⽤CPU，CPU 可以执⾏其他任务；</li><li>当 DMA 读取了⾜够多的数据，就会发送中断信号给 CPU；</li><li>CPU 收到 DMA 的信号，知道数据已经准备好，于是将数据从内核拷⻉到⽤户空间，系统调⽤返回；</li></ol><h3 id="无零拷贝文件传输"><a href="#无零拷贝文件传输" class="headerlink" title="无零拷贝文件传输"></a>无零拷贝文件传输</h3><p>两个系统调用：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">read</span>(file, tmp_buf, len);</span><br><span class="line"><span class="built_in">write</span>(socket, tmp_buf, len);</span><br></pre></td></tr></table></figure><p>4 次用户态与内核态的上下文切换， 4 次数据拷贝</p><p><em>相当于DMA控制器做了两次搬运数据操作（见图DMA搬运数据流程），左边代表一次，右边代表一次（右边的流程要反过来且搬运对象变为网卡）</em><br><img data-src="/transblog/images/324970016256135.png"></p><ul><li>要想提⾼⽂件传输的性能，就需要<strong>减少⽤户态与内核态的上下⽂切换</strong>和<strong>内存拷贝</strong>的次数。</li></ul><h3 id="零拷贝的实现"><a href="#零拷贝的实现" class="headerlink" title="零拷贝的实现"></a>零拷贝的实现</h3><ol><li>方案一：<br>mmap（见上文共享内存） + write</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">buf = <span class="built_in">mmap</span>(file, len);</span><br><span class="line"><span class="built_in">write</span>(sockfd, buf, len);</span><br></pre></td></tr></table></figure><p>mmap() 系统调用函数会直接把内核缓冲区里的数据「映射」到用户空间，可以减少一次数据拷贝，但是需要通过 CPU 把内核缓冲区的数据拷贝到 socket 缓冲区里，而且仍然需要 4 次上下文切换，因为系统调用还是 2 次，3次数据拷贝</p><p><img data-src="/transblog/images/165871416251889.png"></p><ol start="2"><li>方案二：<br>sendfile();</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">ssize_t</span> <span class="title">sendfile</span><span class="params">(<span class="type">int</span> out_fd, <span class="type">int</span> in_fd, <span class="type">off_t</span> *offset, <span class="type">size_t</span> count)</span></span>;</span><br><span class="line"><span class="comment">//前两个参数分别是目的端和源端的文件描述符，后面两个参数是源端的偏移量和复制数据的长度，返回值是实际复制数据的长度。</span></span><br></pre></td></tr></table></figure><p>只有 2 次上下文切换，和 3 次数据拷贝</p><p><img data-src="/transblog/images/515381916269769.png"></p><h3 id="真正的零拷贝"><a href="#真正的零拷贝" class="headerlink" title="真正的零拷贝"></a>真正的零拷贝</h3><ol start="3"><li>方案三：<br>网卡需支持SG-DMA技术<figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">ethtool -k eth0 | grep scatter-gather</span></span><br><span class="line">scatter-gather: on</span><br></pre></td></tr></table></figure></li></ol><p><img data-src="/transblog/images/3305717821354.png"></p><ol><li>通过 DMA 将磁盘上的数据拷⻉到内核缓冲区⾥；</li><li>缓冲区描述符和数据⻓度传到 socket 缓冲区，这样⽹卡的 SG-DMA 控制器就可以<strong>直接将内核缓存中的数据拷⻉到⽹卡的缓冲区⾥</strong>，此过程不需要将数据从操作系统内核缓冲区拷⻉到 socket 缓冲区中，这样就减少了⼀次数据拷⻉；</li></ol><p><strong>零拷贝技术的文件传输方式相比传统文件传输的方式，只需要 2 次上下⽂切换和数据拷贝次数，其中cpu执行0次拷贝，就可以完成文件的传输，而且 2 次的数据拷贝过程，都不需要通过 CPU，2 次都是由 DMA 来搬运。</strong></p><p><strong>零拷贝技术的应用：Kafka、Nginx、Java NIO 库里的 transferTo方法</strong></p><h2 id="网络I-O"><a href="#网络I-O" class="headerlink" title="网络I&#x2F;O"></a>网络I&#x2F;O</h2><h3 id="阻塞与非阻塞I-O"><a href="#阻塞与非阻塞I-O" class="headerlink" title="阻塞与非阻塞I&#x2F;O"></a>阻塞与非阻塞I&#x2F;O</h3><h4 id="阻塞I-O"><a href="#阻塞I-O" class="headerlink" title="阻塞I&#x2F;O"></a>阻塞I&#x2F;O</h4><p>阻塞等待的是<strong>内核数据准备好</strong> 和 <strong>数据从内核态拷贝到用户态</strong>这两个过程</p><p><img data-src="/transblog/images/422510511240452.png"></p><p><img data-src="/transblog/images/435884609258196.png"></p><p>服务端为了处理客户端的连接和请求的数据，写了如下代码：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">listenfd = <span class="built_in">socket</span>(); <span class="comment">// 打开一个网络通信端口</span></span><br><span class="line"><span class="built_in">bind</span>(listenfd);      <span class="comment">// 绑定</span></span><br><span class="line"><span class="built_in">listen</span>(listenfd);    <span class="comment">// 监听</span></span><br><span class="line"><span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    connfd = <span class="built_in">accept</span>(listenfd); <span class="comment">// 阻塞建立连接</span></span><br><span class="line">    <span class="type">int</span> n = <span class="built_in">read</span>(connfd, buf); <span class="comment">// 阻塞读数据</span></span><br><span class="line">    <span class="built_in">doSomeThing</span>(buf);          <span class="comment">// 利用读到的数据做些什么</span></span><br><span class="line">    <span class="built_in">close</span>(connfd);             <span class="comment">// 关闭连接，循环等待下一个连接</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/465305921250251.gif"></p><p>可以看到，服务端的线程阻塞在了两个地方，一个是 accept 函数，一个是 read 函数。<br>如果再把 read 函数的细节展开，我们会发现其阻塞在了两个阶段：</p><p><img data-src="/transblog/images/508850022268677.gif"></p><p>所以，如果这个连接的客户端一直不发数据，那么服务端线程将会一直阻塞在 read 函数上不返回，也无法接受其他客户端连接。这样显然是不行的，有一种聪明的办法是，每次都创建一个新的进程或线程，去调用 read 函数，并做业务处理。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    connfd = <span class="built_in">accept</span>(listenfd); <span class="comment">// 阻塞建立连接</span></span><br><span class="line">    pthread_create（doWork);  <span class="comment">// 创建一个新的线程</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">doWork</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n = <span class="built_in">read</span>(connfd, buf); <span class="comment">// 阻塞读数据</span></span><br><span class="line">    <span class="built_in">doSomeThing</span>(buf);          <span class="comment">// 利用读到的数据做些什么</span></span><br><span class="line">    <span class="built_in">close</span>(connfd);             <span class="comment">// 关闭连接，循环等待下一个连接</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这样，当给一个客户端建立好连接后，就可以立刻等待新的客户端连接，而不用阻塞在原客户端的 read 请求上：</p><p><img data-src="/transblog/images/398260422256544.gif"></p><p>不过，这不叫非阻塞 IO，只不过用了多线程的手段使得主线程没有卡在 read 函数上不往下走罢了。操作系统为我们提供的 read 函数仍然是阻塞的。所以真正的非阻塞 IO，不能是通过我们用户层的小把戏，而是要使用操作系统为我们提供一个非阻塞的 read 函数：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">fcntl</span>(connfd, F_SETFL, O_NONBLOCK);</span><br><span class="line"><span class="type">int</span> n = <span class="built_in">read</span>(connfd, buffer) != SUCCESS);</span><br></pre></td></tr></table></figure><p>这个 read 函数的效果是，如果没有数据到达时（到达网卡并拷贝到了内核缓冲区），立刻返回一个错误值（-1），而不是阻塞地等待。只需要在调用 read 前，将文件描述符设置为非阻塞即可。</p><h4 id="非阻塞-I-O"><a href="#非阻塞-I-O" class="headerlink" title="非阻塞 I&#x2F;O"></a>非阻塞 I&#x2F;O</h4><p>如果没有连接可接受，accept 会立即返回一个错误。<br>如果有连接可接受，就需要用户线程循环调用 read，直到返回值不为 -1，再开始处理业务，这就是非阻塞I&#x2F;O了。<br><img data-src="/transblog/images/465571022276710.gif"></p><p>非阻塞的 read，指的是在数据到达前，即数据还未到达网卡，或者到达网卡但还没有拷贝到内核缓冲区之前，这个阶段是非阻塞的。<br>当数据已到达内核缓冲区，此时调用 read 函数仍然是阻塞的，需要等待数据从内核缓冲区拷贝到用户缓冲区，才能返回。</p><p><img data-src="/transblog/images/253940811258878.png"></p><ul><li>非阻塞的 read 请求在数据未准备好的情况下<strong>立即返回，可以继续往下执行</strong>，此时应用程序不断<strong>轮询内核</strong>，直到数据准备好，内核将数据拷贝到应用程序缓冲区，read 调用才可以获取到结果。</li><li>最后一次 read 调用，获取数据的过程，<strong>是一个同步的过程，是需要等待的过程</strong>。这里的同步指的是内核态的数据拷贝到用户程序的缓存区这个过程。</li></ul><h3 id="同步与异步-I-O"><a href="#同步与异步-I-O" class="headerlink" title="同步与异步 I&#x2F;O"></a>同步与异步 I&#x2F;O</h3><h4 id="同步I-O"><a href="#同步I-O" class="headerlink" title="同步I&#x2F;O"></a>同步I&#x2F;O</h4><p>使用 select  I&#x2F;O多路复用过程（select、poll、epoll都是同步调用的，都需要操作系统具体的返回值）。注意，read 获取数据的过程（数据从内核态拷贝到用户态的过程），也是<strong>一个同步的过程</strong>，需要等待</p><p><img data-src="/transblog/images/375061511246745.png"></p><p>为每个客户端创建一个线程，服务器端的线程资源很容易被耗光，但是我们可以每 accept 一个客户端连接后，将这个文件描述符（connfd）放到一个数组</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">fdlist.<span class="built_in">add</span>(connfd);</span><br><span class="line"><span class="keyword">while</span> (<span class="number">1</span>)&#123;</span><br><span class="line">    <span class="keyword">for</span> (fd &lt; --fdlist)&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">read</span>(fd) != <span class="number">-1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">doSomeThing</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，我们就成功用一个线程处理了多个客户端连接。你是不是觉得这有些多路复用的意思？但这种遍历方式也只是我们用户自己想出的小把戏，每次遍历遇到 read 返回 -1 时仍然是一次浪费资源的系统调用。</p><p><img data-src="/transblog/images/467042422269379.gif"></p><p>所以，还是得恳请操作系统老大，提供给我们一个有这样效果的函数，我们将一批文件描述符通过一次系统调用传给内核，由内核层去遍历，才能真正解决这个问题。<a href="#target2">点击此处跳转到：I&#x2F;O多路复用</a></p><h4 id="异步I-O"><a href="#异步I-O" class="headerlink" title="异步I&#x2F;O"></a>异步I&#x2F;O</h4><p><img data-src="/transblog/images/421221711266911.png"></p><h3 id="信号驱动式IO"><a href="#信号驱动式IO" class="headerlink" title="信号驱动式IO"></a>信号驱动式IO</h3><p>信号驱动式I&#x2F;O是指进程预先告知内核，使得当某个描述符上发生某事时，内核使用信号通知相关进程。当数据报到达时触发SIGIO信号，该信号通知数据已经到来，并没有将数据都入到应用程序的buffer中。因此，还需要我们在SIGIO信号处理函数中，手动的读取到来的数据，将其存放在buffer中。</p><p><img data-src="/transblog/images/551232711258987.png"></p><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><p><strong>阻塞&#x2F;非阻塞、同步&#x2F;异步的关系：</strong></p><p><strong>是否阻塞一般指的是进程、线程状态，是否异步一般指的是是否依赖其它任务已经完成（一般指客户端和服务端）；同步不一定阻塞，阻塞一定同步。</strong></p><ul><li>如果这个进程、线程依赖当前函数返回时，没有执行其他消息处理，而是处于挂起等待状态，叫做同步阻塞</li><li>如果这个进程、线程依赖当前函数返回时，仍在执行其他消息处理，叫做同步非阻塞<br>例如线程池的线程，依赖客户端返回的数据，向处理线程定时查询处理结果，没有结果时可以回到池子中处理其它的线程连接</li><li>如果这个进程、线程不依赖当前函数返回，直接去执行其他消息处理，通过消息回写返回给进程，叫做异步非阻塞</li><li>如果这个进程、线程不依赖当前函数返回，也没有执行其他消息处理，而是处于挂起等待状态，可以认为是异步阻塞，但是这种情况显得很蠢，一般异步就不会再区分阻塞与非阻塞，只有异步非阻塞</li></ul><p>所以，<strong>阻塞 I&#x2F;O、非阻塞 I&#x2F;O，还是基于非阻塞 I&#x2F;O 的多路复用都是同步调用</strong>。因为它们在 read 调用时，内核将数据从内核空间拷贝到应用程序空间，过程都是需要等待的，也就是说这个过程是同步的，如果内核实现的拷贝效率不高，read 调用就会在这个同步过程中等待比较长的时间。</p><p><img data-src="/transblog/images/206442911246854.png"></p><h2 id="I-O多路复用"><a href="#I-O多路复用" class="headerlink" title="I&#x2F;O多路复用"></a><span id="target2">I&#x2F;O多路复用</span></h2><p>select、poll、epoll都是同步调用的，都需要操作系统具体的返回值</p><h3 id="select"><a href="#select" class="headerlink" title="select"></a>select</h3><p>select 实现多路复⽤的⽅式是，将已连接的 Socket 都放到⼀个⽂件描述符集合，然后调⽤select 函数<mark>将⽂件描述符集合拷⻉到内核⾥</mark>，让内核来检查是否有⽹络事件产⽣，检查的⽅式很粗暴，就是<mark>通过遍历</mark>⽂件描述符集合的⽅式，当检查到有事件产⽣后，将此 Socket 标记为可读或可写， 接着<mark>再把整个⽂件描述符集合拷⻉回⽤户态⾥</mark>，然后⽤户态还需要<mark>再通过遍历</mark>的⽅法找到可读或可写的 Socket，然后再对其处理。</p><p>所以，对于 select 这种⽅式，需要进行2 次「遍历」⽂件描述符集合，⼀次是在内核态⾥，⼀个次是在⽤户态⾥ ，⽽且还会发⽣ 2 次「拷⻉」⽂件描述符集合，先从⽤户空间传⼊内核空间，由内核修改后，再传出到⽤户空间中。<br>select 使⽤固定⻓度的 BitsMap，表示⽂件描述符集合，⽽且所⽀持的⽂件描述符的个数是有限制的。</p><h3 id="poll"><a href="#poll" class="headerlink" title="poll"></a>poll</h3><p>poll 不再⽤ BitsMap 来存储所关注的⽂件描述符，取⽽代之⽤<mark>动态数组，以链表形式来组织</mark>，突破了 select 的⽂件描述符个数限制，当然还会受到系统⽂件描述符限制。<br>但是 poll 和 select 并没有太⼤的本质区别，都是使⽤<strong>线性结构</strong>存储进程关注的 Socket集合，因此都需要遍历⽂件描述符集合来找到可读或可写的 Socket，时间复杂度为 O(n)，⽽且也需要在⽤户态与内核态之间拷⻉⽂件描述符集合，这种⽅式随着并发数上来，性能的损耗会呈指数级增⻓。</p><h3 id="epoll"><a href="#epoll" class="headerlink" title="epoll"></a>epoll</h3><p>首先创建一个epoll对象，然后使用epoll_ctl对这个对象进行操作，把需要监控的描述添加进去，这些描述如将会以epoll_event结构体的形式组成一颗红黑树，接着阻塞在epoll_wait，进入大循环，当某个fd上有事件发生时，内核将会把其对应的结构体放入到一个链表中，返回有事件发生的链表。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> s = <span class="built_in">socket</span>(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line"><span class="built_in">bind</span>(s, ...);</span><br><span class="line"><span class="built_in">listen</span>(s, ...)</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> epfd = <span class="built_in">epoll_create</span>(...);</span><br><span class="line"><span class="built_in">epoll_ctl</span>(epfd, ...); <span class="comment">//将所有需要监听的socket添加到epfd中</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="type">int</span> n = <span class="built_in">epoll_wait</span>(...);</span><br><span class="line">    <span class="keyword">for</span>(接收到数据的socket)&#123;</span><br><span class="line">        <span class="comment">//处理</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>epoll事件集合:</p><blockquote><p>EPOLLIN ：表示对应的文件描述符可以读（包括对端SOCKET正常关闭）；<br>EPOLLOUT：表示对应的文件描述符可以写；<br>EPOLLPRI：表示对应的文件描述符有紧急的数据可读（这里应该表示有带外数据到来）；<br>EPOLLERR：表示对应的文件描述符发生错误；<br>EPOLLHUP：表示对应的文件描述符被挂断；<br>EPOLLET： 将EPOLL设为边缘触发(Edge Triggered)模式，这是相对于水平触发(Level Triggered)来说的。<br>EPOLLONESHOT：只监听一次事件，当监听完这次事件之后，如果还需要继续监听这个socket的话，需要再次把这个socket加入到EPOLL队列里</p></blockquote><p><strong>epoll的特点</strong>：</p><ol><li><strong>epoll 在内核里使用红黑树来跟踪进程所有待检测的文件描述字</strong>，把需要监控的socket 通过 epoll_ctl() 函数加⼊内核中的红⿊树⾥，<mark>红黑树增删查⼀般时间复杂度是 O(logn)</mark> ，通过对这棵⿊红树进⾏操作，这样就不需要像 select&#x2F;poll 每次操作时都传⼊整个 socket 集合，只需要传⼊⼀个待检测的 socket，<mark>减少了内核和⽤户空间⼤量的数据拷贝和内存分配</mark></li><li>epoll 使用事件驱动的机制，内核⾥维护了⼀个链表来记录就绪事件，当某个socket 有事件发⽣时，通过回调函数内核会将其加⼊到这个就绪事件列表中，当⽤户调⽤epoll_wait() 函数时，只会返回有事件发⽣的⽂件描述符的个数，不需要像 select&#x2F;poll 那样轮询扫描整个 socket 集合，⼤⼤提⾼了检测的效率。</li><li>使用__put_user 函数，将数据从内核拷贝到用户空间，而非共享内存</li></ol><p><img data-src="/transblog/images/118080312240551.png"></p><h4 id="两种事件触发模式"><a href="#两种事件触发模式" class="headerlink" title="两种事件触发模式"></a>两种事件触发模式</h4><h5 id="水平触发-LT"><a href="#水平触发-LT" class="headerlink" title="水平触发(LT)"></a>水平触发(LT)</h5><p>当被监控的 Socket 上有可读事件发⽣时，<mark>服务器端不断地从epoll_wait 中苏醒，直到内核缓冲区数据被 read 函数读完才结束</mark>，⽬的是告诉我们有数据需要读取。</p><ul><li><strong>如果文件描述符上有数据可读，它的状态码会一直保持就绪状态，直到所有的数据都被读取完毕才会变为未就绪</strong>。</li><li><strong>即使在读取数据的过程中有新的数据写入，文件描述符的状态码仍然会保持就绪状态，不会因为新数据的写入而改变</strong>。</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> epfd, nfds;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">epoll_event</span> event;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">epoll_event</span> events[<span class="number">10</span>];</span><br><span class="line">    <span class="type">int</span> ret;</span><br><span class="line"> </span><br><span class="line">    epfd = <span class="built_in">epoll_create</span>(<span class="number">10</span>);</span><br><span class="line">    <span class="keyword">if</span> (epfd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;epoll_create&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    event.data.fd = STDIN_FILENO;</span><br><span class="line">    event.events = EPOLLIN ;</span><br><span class="line"> </span><br><span class="line">    ret = <span class="built_in">epoll_ctl</span>(epfd, EPOLL_CTL_ADD, STDIN_FILENO, &amp;event);</span><br><span class="line">    <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">perror</span>(<span class="string">&quot;epoll_ctl&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        nfds = <span class="built_in">epoll_wait</span>(epfd, events, <span class="number">10</span>, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">if</span> (nfds &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">perror</span>(<span class="string">&quot;epoll_wait&quot;</span>);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; nfds; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (events[i].data.fd == STDIN_FILENO) &#123;</span><br><span class="line">                <span class="type">char</span> buffer[<span class="number">1024</span>];</span><br><span class="line">                <span class="type">ssize_t</span> count;</span><br><span class="line"> </span><br><span class="line">                count = <span class="built_in">read</span>(STDIN_FILENO, buffer, <span class="built_in">sizeof</span>(buffer) - <span class="number">1</span>);</span><br><span class="line">                <span class="keyword">if</span> (count &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    buffer[count] = <span class="string">&#x27;\0&#x27;</span>; <span class="comment">// 确保读取的字符串有结束符</span></span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;Read: %s&quot;</span>, buffer);</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (count == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// 文件结束</span></span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;End of file\n&quot;</span>);</span><br><span class="line">                    <span class="built_in">close</span>(STDIN_FILENO);</span><br><span class="line">                    <span class="built_in">exit</span>(EXIT_SUCCESS);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 读取错误</span></span><br><span class="line">                    <span class="built_in">perror</span>(<span class="string">&quot;read&quot;</span>);</span><br><span class="line">                    <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">close</span>(epfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="边缘触发-ET"><a href="#边缘触发-ET" class="headerlink" title="边缘触发(ET)"></a>边缘触发(ET)</h5><p>当被监控的 Socket 描述符上有可读事件发⽣时，<mark>服务器端只会从epoll_wait 中苏醒⼀次</mark>，即使进程没有调⽤ read 函数从内核读取数据，也依然只苏醒⼀次。</p><ul><li><strong>如果使用边缘触发模式，在收到通知后程序要尽可能地读写数据，保证⼀次性将内核缓冲区的数据读取完</strong>。</li><li><strong>如果是阻塞的，没有数据可读写时，进程会阻塞在读写函数那⾥，程序就没办法继续往下执⾏</strong>。</li></ul><p>所以，边缘触发模式一般和非阻塞 I&#x2F;O 搭配使用，程序会⼀直执行 I&#x2F;O 操作，直到系统调用（如 read 和 write ）返回错误，errorno错误类型为EAGAIN 或 EWOULDBLOCK 。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> epfd = <span class="built_in">epoll_create</span>(<span class="number">1024</span>);</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">epoll_event</span> event;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">epoll_event</span> ready_events[EPOLL_SIZE];</span><br><span class="line">    <span class="type">int</span> fd = <span class="comment">/* 你的文件描述符 */</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 设置为非阻塞模式</span></span><br><span class="line">    <span class="type">int</span> flags = <span class="built_in">fcntl</span>(fd, F_GETFL);</span><br><span class="line">    <span class="built_in">fcntl</span>(fd, F_SETFL, flags | O_NONBLOCK);</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 设置epoll事件</span></span><br><span class="line">    event.data.fd = fd;</span><br><span class="line">    event.events = EPOLLIN | EPOLLOUT | EPOLLET;</span><br><span class="line">    <span class="built_in">epoll_ctl</span>(epfd, EPOLL_CTL_ADD, fd, &amp;event);</span><br><span class="line"> </span><br><span class="line">    <span class="type">char</span> buffer[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> bytes_read;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">int</span> n = <span class="built_in">epoll_wait</span>(epfd, ready_events, EPOLL_SIZE, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((ready_events[i].events &amp; EPOLLIN) != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">while</span> ((bytes_read = <span class="built_in">read</span>(ready_events[i].data.fd, buffer, BUF_SIZE)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// 处理读取到的数据</span></span><br><span class="line">                    <span class="comment">// ...</span></span><br><span class="line"> </span><br><span class="line">                    <span class="comment">// 如果read返回EAGAIN，意味着数据已经读完</span></span><br><span class="line">                    <span class="keyword">if</span> (bytes_read == <span class="number">-1</span> &amp;&amp; (errno == EAGAIN || errno == EWOULDBLOCK)) &#123;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 处理其他事件...</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">close</span>(epfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h6 id="多线程下的ET"><a href="#多线程下的ET" class="headerlink" title="多线程下的ET"></a>多线程下的ET</h6><ol><li><p>在ET模式下，一个文件描述符上有数据可读（EPOLLIN触发），线程开始处理数据，而在处理过程中又有新数据加入。当旧数据开始处理时，文件描述符仍然保持在就绪状态。但当有新的数据写入时，文件描述符会从就绪状态变为未就绪状态，然后再次变为就绪状态，触发一次新的 EPOLLIN 事件。这样可以确保即使在旧数据处理过程中有新的数据写入，应用程序也能及时地得到通知，并读取新的数据。<strong>因此，在单线程上，此时只要程序循环读取数据就不会造成新数据的丢失</strong></p></li><li><p>但是对于多线程就需要特殊处理（EPOLLONESHOT）。当一个线程处理一个socket时有新数据写入，此时另外一个线程被唤醒读取这些数据，于是出现了两个线程同时操作一个socket的情况。<strong>EPOLLONESHOT解决了多个线程同时操作一个socket的问题</strong>，对于注册了EPOLLONESHOT的事件，操作系统最多触发其上注册的一个可读可写异常事件，且只触发一次。这样，一个线程在操作这个socket时，其他线程不可能有机会操作该socket。</p></li><li><p>但反过来思考，<strong>注册了EPOLLONESHOT事件的socket，一旦被某个线程处理完毕，要及时修改为EPOLLIN或其他事件</strong>，以确保下次这个socket可读时，其事件能够被触发，进而让其他线程有机会继续处理。</p></li></ol><p>可以简单理解为：<strong>ET就是缓冲区数据从无到有时，调用epoll_wait才会通知；如果数据继续可读，除非有新数据到达，否则不会再次触发EPOLLIN 。而LT 只要缓冲区有数据，调用epoll_wait就会通知</strong></p><h4 id="模式对比"><a href="#模式对比" class="headerlink" title="模式对比"></a>模式对比</h4><h5 id="水平触发-阻塞IO"><a href="#水平触发-阻塞IO" class="headerlink" title="水平触发+阻塞IO"></a>水平触发+阻塞IO</h5><p>在这种模式下，<strong>程序会阻塞在epoll_wait()函数这里</strong>，只有客服端有新的数据发送到服务端，EPOLLOUT事件（伴随着EPOLLIN事件）才会触发：</p><ol><li>读缓冲区能够一次存放客户端发送过来的数据时，只触发一次EPOLLOUT事件。</li><li>读缓冲区不能一次存放客户端发送过来的数据时，会多次触发EPOLLOUT事件，直到读完客服端发送过来的数据。</li></ol><p><img data-src="/transblog/images/377585217269394.png"></p><h5 id="水平触发-非阻塞IO"><a href="#水平触发-非阻塞IO" class="headerlink" title="水平触发+非阻塞IO"></a>水平触发+非阻塞IO</h5><p>在水平触发模式下，采用非阻塞IO情况下，程序不会阻塞在epoll_wait()这里，而是只要客服端与服务端建立的连接可用，即使客服端没有发送数据，EPOLLOUT事件也会一直触发（写缓冲区可写）。<br><img data-src="/transblog/images/138825017276725.png"></p><h5 id="边缘触发-阻塞IO"><a href="#边缘触发-阻塞IO" class="headerlink" title="边缘触发+阻塞IO"></a>边缘触发+阻塞IO</h5><p>采用边缘触发模式，在阻塞IO的情况下，客服端发送过来的数据小于读缓冲区大小时，程序会一直阻塞在recv()函数（具体时间转入sk_wait_data函数，直到客户端有新数据发送过来或者达到超时才会跳出此函数）这里，导致程序无法进行下一步操作。</p><p><img data-src="/transblog/images/151735617265949.png"></p><h5 id="边缘触发-非阻塞IO"><a href="#边缘触发-非阻塞IO" class="headerlink" title="边缘触发+非阻塞IO"></a>边缘触发+非阻塞IO</h5><p>边缘触发模式下采用非阻塞IO，具体地说，采用边缘触发模式必须使用非阻塞IO，不然读缓冲区数据没有读满就会一直阻塞在recv()函数这里；改成了非阻塞IO之后，当读缓冲区数据没有读满或者读到的数据为空时，recv()函数就会返回errno代码EAGAIN 或者 EWOULDBLOCK，进而做相应的后续处理。<br>这里采用了循环读取数据处理，因此在边缘触发模式下，只要客服端发送过来的数据没有一次读完，会循环读取直到读取结束。</p><p><img data-src="/transblog/images/301235617261703.png"></p><h4 id="内核实现方式"><a href="#内核实现方式" class="headerlink" title="内核实现方式"></a>内核实现方式</h4><ul><li>在epoll_wait的时候，阻塞等待事件发生， 事件发生时通过回调挂到ready list链表中</li><li>epoll_wait返回， 处理ready list, 返回事件给调用者</li><li>此时ET模式已经将事件从ready list中删除，LT模式中还存在</li><li>此时假设应用程序处理完了事件， 再次epoll_wait.  ET模式继续阻塞</li><li>LT模式由于ready list中依然存在事件则不会阻塞， 对这些socket调用poll方法获取最新的事件信息，如果确认没事件了才会删除。</li></ul><p><img data-src="/transblog/images/378815116259283.png"></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//该结构存储在文件结构的&quot;private_data&quot;成员中，代表eventpoll接口的主数据结构。</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">eventpoll</span> &#123;</span><br><span class="line">    <span class="comment">//添加到eventpoll接口的每个文件描述符都有一个链接到“rbr”RB树的这种类型的条目。避免增加该结构体的大小，服务器上可能有数千个这样的结构体，我们不希望它占用另一条缓存线。</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">epitem</span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">io_epoll_ctl</span>()-&gt;<span class="built_in">do_epoll_ctl</span>()-&gt;<span class="built_in">ep_insert</span>()</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//将当前项添加到RB树中。所有RB树操作都由&quot;mtx&quot;保护，并且ep_insert()调用时持有&quot;mtx&quot;。</span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">ep_rbtree_insert</span><span class="params">(<span class="keyword">struct</span> eventpoll *ep, <span class="keyword">struct</span> epitem *epi)</span><span class="comment">//红黑树插入</span></span></span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//这个回调函数用于将等待队列添加到目标文件唤醒列表中。</span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">ep_ptable_queue_proc</span><span class="params">(<span class="keyword">struct</span> file *file, <span class="type">wait_queue_head_t</span> *whead,poll_table *pt)</span></span></span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//这是传递给等待队列唤醒机制的回调。 当存储的文件描述符有事件要报告时，就会调用它。</span></span><br><span class="line"><span class="comment">//该回调使用读取锁，以避免与另一个文件描述符的并发事件发生冲突，因此对 -&gt;rdllist 或 -&gt;ovflist 的所有修改都是无锁的。  读锁与来自 ep_start/done_scan() 的写锁配对，可停止所有列表修改，确保正确查看列表状态。</span></span><br><span class="line"><span class="comment">//另一个值得一提的是，如果轮询表中有多个等待队列条目，那么不同 CPU 可以同时调用 ep_poll_callback()来唤醒同一个 @epi。  不同 CPU 对单个等待队列的多次唤醒是通过 wq.lock 序列化的，但如果使用了多个等待队列，则应进行相应检测。  这种情况可通过 cmpxchg() 操作进行检测。</span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">int</span> <span class="title">ep_poll_callback</span><span class="params">(<span class="type">wait_queue_entry_t</span> *wait, <span class="type">unsigned</span> mode, <span class="type">int</span> sync, <span class="type">void</span> *key)</span></span></span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//以无锁的方式向列表尾部添加新条目，即允许多个cpu并发调用此函数。</span></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">bool</span> <span class="title">list_add_tail_lockless</span><span class="params">(<span class="keyword">struct</span> list_head *<span class="keyword">new</span>,<span class="keyword">struct</span> list_head *head)</span></span></span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//ET和LT在ready list的区别</span></span><br><span class="line"><span class="keyword">if</span> (!(epi-&gt;event.events &amp; ~EP_PRIVATE_BITS))</span><br><span class="line">    <span class="keyword">goto</span> out_unlock;</span><br></pre></td></tr></table></figure><h4 id="水平触发的问题"><a href="#水平触发的问题" class="headerlink" title="水平触发的问题"></a>水平触发的问题</h4><p><strong>不必要的唤醒</strong><br>内核：收到一个新建连接的请求<br>内核：由于 “惊群效应” ，唤醒两个正在 epoll_wait() 的线程 A 和线程 B<br>线程A：epoll_wait() 返回<br>线程B：epoll_wait() 返回<br>线程A：执行 accept() 并且成功<br>线程B：执行 accept() 失败，accept() 返回 EAGAIN</p><h4 id="边缘触发的问题"><a href="#边缘触发的问题" class="headerlink" title="边缘触发的问题"></a>边缘触发的问题</h4><p><strong>不必要的唤醒</strong></p><p>1.内核：收到第一个连接请求。线程 A 和 线程 B 两个线程都在 epoll_wait() 上等待。<br>由于采用边缘触发模式，所以只有一个线程会收到通知。这里假定线程 A 收到通知<br>2.线程A：epoll_wait() 返回<br>3.线程A：调用 accpet() 并且成功<br>4.内核：此时 accept queue 为空，所以将边缘触发的 socket 的状态从可读置成不可读<br>5.内核：收到第二个建连请求<br>6.内核：此时，由于线程 A 还在执行 accept() 处理，只剩下线程 B 在等待 epoll_wait()，<br>于是唤醒线程 B。<br>7.线程A：继续执行 accept() 直到返回 EAGAIN<br>8.线程B：执行 accept()，并返回 EAGAIN，此时线程 B 可能有点困惑(“明明通知我有事件，结果却返回 EAGAIN”)<br>9.线程A：再次执行 accept()，这次终于返回 EAGAIN</p><p><strong>饥饿</strong><br>1.内核：接收到两个建连请求。线程 A 和 线程 B 两个线程都在等在 epoll_wait()。<br>由于采用边缘触发模式，只有一个线程会被唤醒，我们这里假定线程 A 先被唤醒<br>2.线程A：epoll_wait() 返回<br>3.线程A：调用 accpet() 并且成功<br>4.内核：收到第三个建连请求。由于线程 A 还没有处理完(没有返回 EAGAIN)，<br>当前 socket 还处于可读的状态，由于是边缘触发模式，所有不会产生新的事件<br>5.线程A：继续执行 accept() 希望返回 EAGAIN 再进入 epoll_wait() 等待，<br>然而它又 accept() 成功并处理了一个新连接<br>6.内核：又收到了第四个建连请求<br>7.线程A：又继续执行 accept()，结果又返回成功</p><h4 id="ET和LT应用场景"><a href="#ET和LT应用场景" class="headerlink" title="ET和LT应用场景"></a>ET和LT应用场景</h4><p>⼀般来说，边缘触发的效率比水平触发的效率要高,因为边缘触发可以减少 epoll_wait 的系统调用次数，系统调用也是有⼀定的开销的的，毕竟也存在上下文的切换。</p><ol><li>LT的编程更符合用户直觉，业务层逻辑更简单，不易出错，但效率较低；</li><li>ET的编程可以做到更加简洁，某些场景下更加高效，但另一方面容易遗漏事件，容易产生bug；</li><li>对于nginx这种高性能服务器，ET模式是很好的，Redis使用的是LT，避免使用的过程中出现bug；</li><li>当并发量比较小时，比较推荐LT，因为LT模式下应用的读写逻辑比较简单，不容易遗漏事件，代码不易出错好维护，而且性能损失不大。当并发量非常大时，推荐使用ET模式，可以有效提升EPOLL效率。</li></ol><h2 id="文件传输方式的选择"><a href="#文件传输方式的选择" class="headerlink" title="文件传输方式的选择"></a>文件传输方式的选择</h2><p>传输⼤⽂件的时候，使⽤「异步 I&#x2F;O + 直接 I&#x2F;O」；<br>传输⼩⽂件的时候，则使⽤「零拷⻉技术」；</p><h1 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h1><h2 id="Shell"><a href="#Shell" class="headerlink" title="Shell"></a>Shell</h2><ol><li>什么是shell？</li></ol><ul><li>shell是linux的一个外壳，它包在linux内核的外面，为用户和内核之间的交互提供了一个接口。</li><li>当用户下达指令给该操作系统的时候，实际上是把指令告诉shell，经过shell解释，处理后让内核做出相应的动作。</li><li>系统的回应和输出的信息也由shell处理，然后显示在用户的屏幕上</li></ul><ol start="2"><li>为什么使用shell脚本？</li></ol><ul><li>适合处理操作系统底层的业务，有众多系统命令为其做支撑（还有文本处理三兄弟：grep，sed，awk）</li><li>适合处理纯文本文件，linux中许多服务配置文件，启动脚本，都是纯文本文件（如：httpd，nfs，mysql，nginx，lvs）</li><li>linux系统脚本用shell开发更简单，简言之，shell脚本操作更加方便，快捷，高效。</li></ul><ol start="3"><li>shell脚本与编程语言的区别？</li></ol><ul><li>shell脚本是能运行的文本，它包含命令和运行逻辑关系 。与C语言、C++、C#等编程语言不同。</li><li>shell脚本不需要编译、连接及生成可执行文件，直接由相应的解释器（最常用的解释器为bash） 解释执行即可。</li><li>它的优点是可批量，多次执行（使用）。简言之，shell脚本是解释性语言，而c语言则是编译性语言。</li></ul><h2 id="Handle"><a href="#Handle" class="headerlink" title="Handle"></a>Handle</h2><p><strong>句柄</strong></p><p><img data-src="/transblog/images/2535111169068.png">       <img data-src="/transblog/images/3580711156935.png"></p><p>Windows是一个以虚拟内存为基础的操作系统，进程的代码和数据并不全部装入内存，进程的某一段装入内存后，还可能被换出到外存，当再次需要时，再装入内存。两次装入的地址绝大多数情况下是不一样的。</p><p>系统为每个进程在内存中分配一定的区域，用来存放各个句柄，即一个个无符号整型。每个无符号整型值相当于一个指针，指向内存中的另一个区域A。而区域A中存放的正是对象在内存中的地址。<mark>当对象在内存中的位置发生变化时，区域A的值被更新，变为当前时刻对象在内存中的地址，而在这个过程中，区域A的位置以及对应句柄的值是不发生变化的。</mark></p><ol><li>所谓“唯一”、“不变”是指在程序的一次运行中。如果本次运行完，关闭程序，再次启动程序运行，那么这次运行中，同一对象的句柄的值和上次运行时比较，一般是不一样的。</li><li>句柄是对象生成时系统指定的，属性是只读的，程序员不能修改句柄。</li><li>不同的系统中，句柄的大小（字节数）是不同的，可以使用sizeof()来计算句柄的大小。</li><li>通过句柄，程序员只能调用系统提供的服务（即API调用），不能像使用指针那样，做其它的事。</li></ol><h2 id="高性能网络模式：Reactor-和-Proactor"><a href="#高性能网络模式：Reactor-和-Proactor" class="headerlink" title="高性能网络模式：Reactor 和 Proactor"></a>高性能网络模式：Reactor 和 Proactor</h2><h3 id="Reactor"><a href="#Reactor" class="headerlink" title="Reactor"></a>Reactor</h3><h4 id="1-单-Reactor-单进程-线程"><a href="#1-单-Reactor-单进程-线程" class="headerlink" title="1. 单 Reactor 单进程 &#x2F; 线程"></a>1. 单 Reactor 单进程 &#x2F; 线程</h4><p><img data-src="/transblog/images/53424515240561.png"></p><p>Reactor 对象的作用是监听和分发事件；Acceptor 对象的作用是获取连接；Handler 对象的作用是处理业务；对象里的 select、accept、read、send 是系统调用函数，dispatch 和 「业务处理」是需要完成的操作，其中 dispatch 是分发事件操作。</p><ul><li>Reactor 对象通过 select （IO 多路复⽤接⼝） 监听事件，收到事件后通过 dispatch 进⾏分发，具体分发给 Acceptor 对象还是 Handler 对象，还要看收到的事件类型；</li><li>如果是连接建⽴的事件，则交由 Acceptor 对象进⾏处理，Acceptor 对象会通过 accept⽅法 获取连接，并创建⼀个 Handler 对象来处理后续的响应事件；</li><li>如果不是连接建⽴事件， 则交由当前连接对应的 Handler 对象来进⾏响应；Handler 对象通过 read -&gt; 业务处理 -&gt; send 的流程来完成完整的业务流程。</li><li>Handler 对象通过 read -&gt; 业务处理 -&gt; send 的流程来完成完整的业务流程</li></ul><p>但是，这种方案存在 2 个缺点：</p><ol><li>因为只有一个进程，无法充分利用 多核 CPU 的性能；</li><li>Handler 对象在业务处理时，整个进程是无法处理其他连接的事件的，如果业务处理耗时比较长，那么就造成响应的延迟；<br><strong>所以，单 Reactor 单进程的方案不适用计算机密集型的场景，只适用于业务处理非常快速的场景。</strong></li></ol><h4 id="2-单-Reactor-多线程-多进程"><a href="#2-单-Reactor-多线程-多进程" class="headerlink" title="2. 单 Reactor 多线程 &#x2F; 多进程"></a>2. 单 Reactor 多线程 &#x2F; 多进程</h4><p><img data-src="/transblog/images/323594515258987.png"></p><ul><li>Reactor 对象通过 select （IO 多路复⽤接⼝） 监听事件，收到事件后通过 dispatch 进⾏分发，具体分发给 Acceptor 对象还是 Handler 对象，还要看收到的事件类型；</li><li>如果是连接建⽴的事件，则交由 Acceptor 对象进⾏处理，Acceptor 对象会通过 accept⽅法 获取连接，并创建⼀个 Handler 对象来处理后续的响应事件；</li><li>如果不是连接建⽴事件， 则交由当前连接对应的 Handler 对象来进⾏响应；</li><li>Handler 对象不再负责业务处理，只负责数据的接收和发送，Handler 对象通过 read 读取到数据后，会将数据发给⼦线程⾥的 Processor 对象进⾏业务处理；</li><li>⼦线程⾥的 Processor 对象就进⾏业务处理，处理完后，将结果发给主线程中的 Handler对象，接着由 Handler 通过 send ⽅法将响应结果发送给 client；</li></ul><p>「单 Reactor」的模式能够充分利用多核 CPU 的能力，但是有个问题，因为⼀个 Reactor 对象承担所有事件的监听和响应，⽽且只在主线程中运⾏，在⾯对瞬间⾼并发的场景时，容易成为性能的瓶颈的地⽅。</p><h4 id="3-多-Reactor-多进程-线程"><a href="#3-多-Reactor-多进程-线程" class="headerlink" title="3. 多 Reactor 多进程 &#x2F; 线程"></a>3. 多 Reactor 多进程 &#x2F; 线程</h4><p><img data-src="/transblog/images/386655415246854.png"></p><ul><li>主线程中的 MainReactor 对象通过 select 监控连接建⽴事件，收到事件后通过 Acceptor对象中的 accept 获取连接，将新的连接分配给某个⼦线程；</li><li>⼦线程中的 SubReactor 对象将 MainReactor 对象分配的连接加⼊ select 继续进⾏监听，并创建⼀个 Handler ⽤于处理连接的响应事件。</li><li>如果有新的事件发⽣时，SubReactor 对象会调⽤当前连接对应的 Handler 对象来进⾏响应。</li><li>Handler 对象通过 read -&gt; 业务处理 -&gt; send 的流程来完成完整的业务流程。</li><li>主线程和⼦线程分⼯明确，主线程只负责接收新连接，⼦线程负责完成后续的业务处理。</li><li>主线程和⼦线程的交互很简单，主线程只需要把新连接传给⼦线程，⼦线程⽆须返回数据，直接就可以在⼦线程将处理结果发送给客户端。</li></ul><p>Netty 和 Memcache 都采用了「多 Reactor 多线程」的方案。采用了「多 Reactor 多进程」方案的开源软件是 Nginx，不过方案与标准的多 Reactor 多进程有些差异</p><h3 id="Proactor"><a href="#Proactor" class="headerlink" title="Proactor"></a>Proactor</h3><ul><li>Reactor 是⾮阻塞同步⽹络模式，感知的是就绪可读写事件。在每次感知到有事件发⽣（⽐如可读就绪事件）后，就需要应⽤进程主动调⽤ read ⽅法来完成数据的读取，也就是要应⽤进程主动将 socket 接收缓存中的数据读到应⽤进程内存中，这个过程是同步的，读取完数据后应⽤进程才能处理数据。</li><li>Proactor 是异步⽹络模式， 感知的是已完成的读写事件。在发起异步读写请求时，需要传⼊数据缓冲区的地址（⽤来存放结果数据）等信息，这样系统内核才可以⾃动帮我们把数据的读写⼯作完成，这⾥的读写⼯作全程由操作系统来做，并不需要像 Reactor 那样还需要应⽤进程主动发起 read&#x2F;write 来读写数据，操作系统完成读写⼯作后，就会通知应⽤进程直接处理数据。</li></ul><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Linux内核源码地址&quot;&gt;&lt;a href=&quot;#Linux内核源码地址&quot; class=&quot;headerlink&quot; title=&quot;Linux内核源码地址&quot;&gt;&lt;/a&gt;Linux内核源码地址&lt;/h1&gt;&lt;p&gt;&lt;a href=&quot;https://elixir.bootlin.co</summary>
      
    
    
    
    <category term="四件套" scheme="https://trans-xu.github.io/transblog/categories/%E5%9B%9B%E4%BB%B6%E5%A5%97/"/>
    
    
  </entry>
  
  <entry>
    <title>排序算法</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/</id>
    <published>2025-08-27T03:50:08.905Z</published>
    <updated>2026-04-27T10:30:17.854Z</updated>
    
    <content type="html"><![CDATA[<h1 id="排序算法"><a href="#排序算法" class="headerlink" title="排序算法"></a>排序算法</h1><p>稳定：如果a原本在b前面，而a&#x3D;b，排序之后a仍然在b的前面。</p><table><thead><tr><th align="center">排序方法</th><th align="center">时间复杂度（平均）</th><th align="center">时间复杂度（最坏）</th><th align="center">时间复杂度（最好）</th><th align="center">空间复杂度</th><th align="center">稳定性</th></tr></thead><tbody><tr><td align="center">插入排序</td><td align="center">O($n^2$)</td><td align="center">O($n^2$)</td><td align="center">O($n$)</td><td align="center">O($1$)</td><td align="center">稳定</td></tr><tr><td align="center">希尔排序</td><td align="center">O($n^{1.3}$)</td><td align="center">O($n^2$)</td><td align="center">O($n$)</td><td align="center">O($1$)</td><td align="center">不稳定</td></tr><tr><td align="center">选择排序</td><td align="center">O($n^2$)</td><td align="center">O($n^2$)</td><td align="center">O($n^2$)</td><td align="center">O($1$)</td><td align="center">不稳定</td></tr><tr><td align="center">堆排序</td><td align="center">O($n log_2 n$)</td><td align="center">O($n log_2 n$)</td><td align="center">O$(n log_2 n$)</td><td align="center">O($1$)</td><td align="center">不稳定</td></tr><tr><td align="center">冒泡排序</td><td align="center">O($n^2$)</td><td align="center">O($n^2$)</td><td align="center">O($n$)</td><td align="center">O($1$)</td><td align="center">稳定</td></tr><tr><td align="center">快速排序</td><td align="center">O($n log_2 n$)</td><td align="center">O($n^2$)</td><td align="center">O($n log_2 n$)</td><td align="center">O($n log_2 n$)</td><td align="center">不稳定</td></tr><tr><td align="center">归并排序</td><td align="center">O($n log_2 n$)</td><td align="center">O($n log_2 n$)</td><td align="center">O($n log_2 n$)</td><td align="center">O($n$)</td><td align="center">稳定</td></tr><tr><td align="center"></td><td align="center"></td><td align="center"></td><td align="center"></td><td align="center"></td><td align="center"></td></tr><tr><td align="center">计数排序</td><td align="center">O($n+k$)</td><td align="center">O($n+k$)</td><td align="center">O($n+k$)</td><td align="center">O($n+k$)</td><td align="center">稳定</td></tr><tr><td align="center">桶排序</td><td align="center">O($n+k$)</td><td align="center">O($n^2$)</td><td align="center">O($n$)</td><td align="center">O($n+k$)</td><td align="center">稳定</td></tr><tr><td align="center">基数排序</td><td align="center">O($n*k$)</td><td align="center">O($n*k$)</td><td align="center">O($n*k$)</td><td align="center">O($n*k$)</td><td align="center">稳定</td></tr></tbody></table><h1 id="冒泡排序"><a href="#冒泡排序" class="headerlink" title="冒泡排序"></a>冒泡排序</h1><p>冒泡排序是一种简单的排序算法。它重复地走访过要排序的数列，一次比较两个元素，如果它们的顺序错误就把它们交换过来。走访数列的工作是重复地进行直到没有再需要交换，也就是说该数列已经排序完成。这个算法的名字由来是因为越小的元素会经由交换慢慢“浮”到数列的顶端。 </p><h2 id="算法描述"><a href="#算法描述" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>比较两个相邻的元素，如果第一个比第二个大，那么就交换他们的位置。</li><li>重复步骤（1）的操作，依次比较两两相邻的两个元素。所有元素对比过后表示一次循环结束。</li><li>至多循环n-1次，重复上面两个步骤。</li><li>直到没有任何一组元素需要交换位置表示排序完成。</li></ol><h2 id="算法图解"><a href="#算法图解" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/305950812267532.gif"></p><h2 id="C-代码"><a href="#C-代码" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">BubbleSort</span><span class="params">(<span class="type">int</span>* slist,<span class="type">int</span> length)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> temp;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; length - <span class="number">1</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; length - <span class="number">1</span> - i; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (slist[j] &gt; slist[j + <span class="number">1</span>])</span><br><span class="line">            &#123;</span><br><span class="line">                temp = slist[j];</span><br><span class="line">                slist[j] = slist[j + <span class="number">1</span>];</span><br><span class="line">                slist[j + <span class="number">1</span>] = temp;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度"><a href="#算法复杂度" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>最好的情况为正序，只需要一遍遍历，所以时间复杂度为O(n)，最坏的情况为逆序，需要遍历$n^2$次，所以时间复杂度为O($n^2$)。由于只有一个交换的变量temp需要内存，所以空间复杂度为O(1)。该算法稳定</p><h1 id="选择排序"><a href="#选择排序" class="headerlink" title="选择排序"></a>选择排序</h1><p>选择排序(Selection-sort)是一种简单直观的排序算法。它的工作原理：首先在未排序序列中找到最小（大）元素，存放到排序序列的起始位置，然后，再从剩余未排序元素中继续寻找最小（大）元素，然后放到已排序序列的末尾。以此类推，直到所有元素均排序完毕。 </p><h2 id="算法描述-1"><a href="#算法描述-1" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>遍历整个列表N个元素，找到最小的元素，与第一个元素交换位置。</li><li>遍历剩余的N-1个元素，找到最小的元素，将它排在剩余N-1个元素的第一个。</li><li>以此类推，重复步骤（2），直到N-1小于1。</li></ol><h2 id="算法图解-1"><a href="#算法图解-1" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/544561118630035.gif"></p><h2 id="C-代码-1"><a href="#C-代码-1" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">SelectSort</span><span class="params">(<span class="type">int</span>* slist, <span class="type">int</span> length)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> min;</span><br><span class="line">    <span class="type">int</span> temp;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; length; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        min = i;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = i + <span class="number">1</span>; j &lt; length; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (slist[j] &lt; slist[min])        <span class="comment">// 寻找最小的数</span></span><br><span class="line">            &#123;</span><br><span class="line">                min = j;                      <span class="comment">// 将最小数的索引保存</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        temp = slist[i];</span><br><span class="line">        slist[i] = slist[min];</span><br><span class="line">        slist[min] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-1"><a href="#算法复杂度-1" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>不管最后还是最坏的情况，选择排序的时间复杂度都是O($n^2$)，空间复杂度为O(1)。该算法不稳定。</p><h1 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h1><p>插入排序（Insertion-Sort）的算法描述是一种简单直观的排序算法。它的工作原理是通过构建有序序列，对于未排序数据，在已排序序列中从后向前扫描，找到相应位置并插入。</p><h2 id="算法描述-2"><a href="#算法描述-2" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>从第一个元素开始，该元素可以认为已经被排序；</li><li>取出下一个元素，在已经排序的元素序列中从后向前扫描；</li><li>如果该元素（已排序）大于新元素，将该元素移到下一位置；</li><li>重复步骤3，直到找到已排序的元素小于或者等于新元素的位置；</li><li>将新元素插入到该位置后；</li><li>重复步骤2~5。</li></ol><h2 id="算法图解-2"><a href="#算法图解-2" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/576393010972560.gif"></p><h2 id="C-代码-2"><a href="#C-代码-2" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">InsertSort</span><span class="params">(<span class="type">int</span>* slist,<span class="type">int</span> length)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt; length; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> preIndex = i - <span class="number">1</span>;</span><br><span class="line">        <span class="type">int</span> number = slist[i];                <span class="comment">//记录要进行插入的值</span></span><br><span class="line">        <span class="keyword">while</span> (preIndex &gt;= <span class="number">0</span> &amp;&amp; slist[preIndex] &gt; number)</span><br><span class="line">        &#123;</span><br><span class="line">            slist[preIndex<span class="number">+1</span>] = slist[preIndex];        <span class="comment">//将插入值大的数进行后移，会有两个一样的值，后续直接覆盖掉</span></span><br><span class="line">            preIndex--;</span><br><span class="line">        &#125;</span><br><span class="line">        slist[preIndex<span class="number">+1</span>] = number;            <span class="comment">//preIndex为插入位置的前一个索引，当到达边界时preIndex为-1，此时会插入头部</span></span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-2"><a href="#算法复杂度-2" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>最好的情况为正序，只需要一遍遍历，所以时间复杂度为O(n)，最坏的情况为逆序，需要遍历$n^2$次，所以时间复杂度为O($n^2$)。空间复杂度为O(1)。该算法稳定</p><h1 id="希尔排序"><a href="#希尔排序" class="headerlink" title="希尔排序"></a>希尔排序</h1><p>希尔排序，也称递减增量排序算法。将整个列表根据增量分为若干个子列表分别进行插入排序。随着增量的减小，列表趋于基本有序，当增量为1时，相当再做一次插入排序，使列表有序。</p><h2 id="算法描述-3"><a href="#算法描述-3" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>选择一个增量gap，一般为列表长度的一半。</li><li>将列表中元素下标每隔gap的元素组成一个新的子列表。</li><li>对每个子列表进行插入排序。</li><li>将gap去原来的一半，重复步骤2、3，直到gap小于1。</li></ol><h2 id="算法图解-3"><a href="#算法图解-3" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/213005627987098.gif"></p><h2 id="C-代码-3"><a href="#C-代码-3" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">ShellSort</span><span class="params">(<span class="type">int</span>* slist, <span class="type">int</span> length)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> temp;</span><br><span class="line">    <span class="comment">//gap为增量，一般取长度的一般</span></span><br><span class="line">    <span class="type">int</span> gap = length / <span class="number">2</span>;</span><br><span class="line">    <span class="comment">//当增量小于1时结束排序</span></span><br><span class="line">    <span class="keyword">while</span> (gap &gt;= <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//最多分为gap个列表</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; gap; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">//下面的代码为一个简单的插入排序，只是插入排序的数组下标每次移动的不是1而是gap</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = i+gap; j &lt; length; j = j + gap)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (slist[j] &lt; slist[j-gap])</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="type">int</span> k = j - gap;</span><br><span class="line">                    <span class="type">int</span> temp = slist[j];</span><br><span class="line">                    <span class="keyword">while</span> (k &gt;= <span class="number">0</span> &amp;&amp; slist[k] &gt; temp)</span><br><span class="line">                    &#123;</span><br><span class="line">                        slist[k + gap] = slist[k];</span><br><span class="line">                        k = k - gap;</span><br><span class="line">                    &#125;</span><br><span class="line">                    slist[k+gap] = temp;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//增量递减</span></span><br><span class="line">        gap = gap/ <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-3"><a href="#算法复杂度-3" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>希尔排序是不稳定的排序，它时间复杂度和步长的选择有关。</p><blockquote><p>n&#x2F;2、n&#x2F;4、n&#x2F;8…1        </p></blockquote><p>该序列称为希尔增量序列，使用希尔增量时，希尔排序在最坏情况下的时间复杂度为O($n^2$)。</p><blockquote><p>1、3、7…2^k-1</p></blockquote><p>该序列序列称为Hibbard增量序列，使用Hibbard增量时，希尔排序在最坏情况下的时间复杂度为O($n^{1.5}$)。</p><h1 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h1><p>归并排序采用分治的思想，将一个列表分为多个子列表，每个子序列是有序的。然后再把有序子序列合并为整体有序序列。</p><h2 id="算法描述-4"><a href="#算法描述-4" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>将列表元素对半分开，分为两个列表。</li><li>重复步骤 1 ，直至每个列表只有一个元素。</li><li>将两个相邻的列表排序，直至整个列表有序。</li></ol><h2 id="算法图解-4"><a href="#算法图解-4" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/442321706589153.gif"></p><h2 id="C-代码-4"><a href="#C-代码-4" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//排序</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">MergeSort</span><span class="params">(<span class="type">int</span> sourceArr[], <span class="type">int</span> tempArr[], <span class="type">int</span> startIndex, <span class="type">int</span> midIndex, <span class="type">int</span> endIndex)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//i:第一个待排序的起始位置，</span></span><br><span class="line">    <span class="type">int</span> i = startIndex;</span><br><span class="line">    <span class="comment">//j:第二个待排序的起始位置，</span></span><br><span class="line">    <span class="type">int</span> j = midIndex + <span class="number">1</span>;</span><br><span class="line">    <span class="type">int</span> k = startIndex;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (i != midIndex + <span class="number">1</span> &amp;&amp; j != endIndex<span class="number">+1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (sourceArr[i] &gt; sourceArr[j])</span><br><span class="line">        &#123;</span><br><span class="line">            tempArr[k] = sourceArr[j];</span><br><span class="line">            k++;</span><br><span class="line">            j++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            tempArr[k] = sourceArr[i];</span><br><span class="line">            k++;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;    </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//将两个中未排序的添加到tempArr中</span></span><br><span class="line">    <span class="keyword">while</span> (i != midIndex + <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        tempArr[k] = sourceArr[i];</span><br><span class="line">        i++;</span><br><span class="line">        k++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//将两个中未排序的添加到tempArr中</span></span><br><span class="line">    <span class="keyword">while</span> (j != endIndex<span class="number">+1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        tempArr[k] = sourceArr[j];</span><br><span class="line">        j++;</span><br><span class="line">        k++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将tempArr中的元素赋值给sourceArr</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = startIndex; i &lt;= endIndex; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        sourceArr[i] = tempArr[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//分解递归</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">BranchSort</span><span class="params">(<span class="type">int</span> sourceArr[], <span class="type">int</span> tempArr[], <span class="type">int</span> startIndex, <span class="type">int</span> endIndex)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (startIndex &lt; endIndex)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> mid = startIndex + (endIndex - startIndex) / <span class="number">2</span>;</span><br><span class="line">        <span class="built_in">BranchSort</span>(sourceArr, tempArr, startIndex, mid);</span><br><span class="line">        <span class="built_in">BranchSort</span>(sourceArr, tempArr, mid<span class="number">+1</span>, endIndex);</span><br><span class="line">        <span class="built_in">MergeSort</span>(sourceArr, tempArr, startIndex, mid, endIndex);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-4"><a href="#算法复杂度-4" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>归并排序是稳定排序算法，假设数组长度为n，那么拆分数组共需logn, 又每步都是一个普通的合并子数组的过程，时间复杂度为O(n)， 故其综合时间复杂度为O(nlogn)。另一方面， 归并排序多次递归过程中拆分的子数组需要保存在内存空间， 其空间复杂度为O(n)。</p><h1 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h1><p>快速排序采用分治的思想，通通过一趟排序将要排序的数据分割成独立的两部分，其中一部分的所有数据都比另外一部分的所有数据都要小，然后再按此方法对这两部分数据分别进行快速排序，整个排序过程可以 递归 进行，以此达到整个数据变成有序序列。</p><h2 id="算法描述-5"><a href="#算法描述-5" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>选定一个基准元素</li><li>从右往左找到比基准元素小的元素</li><li>从左往右找到比基准元素大的元素</li><li>交换左右找到的两个元素的位置</li><li>重复上面2、3、4步，直至左右两个元素相遇。</li></ol><h2 id="算法图解-5"><a href="#算法图解-5" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/463803448673497.jpg"></p><h2 id="C-代码-5"><a href="#C-代码-5" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">quickSort</span><span class="params">(<span class="type">int</span> left, <span class="type">int</span> right, vector&lt;<span class="type">int</span>&gt;&amp; arr)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">if</span>(left &gt;= right)&#123;</span><br><span class="line"><span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="type">int</span> i = left, j = right;</span><br><span class="line"><span class="type">int</span> base = arr[left];  <span class="comment">//取最左边的数为基准数</span></span><br><span class="line"><span class="keyword">while</span> (i &lt; j)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">while</span> (arr[j] &gt;= base &amp;&amp; i &lt; j)&#123;</span><br><span class="line">j--;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (arr[i] &lt;= base &amp;&amp; i &lt; j)&#123;</span><br><span class="line">i++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(i &lt; j)</span><br><span class="line">&#123;</span><br><span class="line">            <span class="built_in">swap</span>(arr[i],arr[j]);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//基准数归位</span></span><br><span class="line">    <span class="built_in">swap</span>(arr[i],arr[left]);</span><br><span class="line"><span class="built_in">quickSort</span>(left, i - <span class="number">1</span>, arr);<span class="comment">//递归左边</span></span><br><span class="line"><span class="built_in">quickSort</span>(i + <span class="number">1</span>, right, arr);<span class="comment">//递归右边</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="算法复杂度-5"><a href="#算法复杂度-5" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><ul><li>快速排序之所比较快，因为相比冒泡排序，每次交换是跳跃式的。<strong>每次排序的时候设置一个基准点，将小于等于基准点的数全部放到基准点的左边，将大于等于基准点的数全部放到基准点的右边</strong>。</li><li>这样在每次交换的时候就不会像冒泡排序一样每次只能在相邻的数之间进行交换，交换的距离就大的多了。因此总的比较和交换次数就少了，速度自然就提高了。</li><li>当然在最坏的情况下，仍可能是相邻的两个数进行了交换。因此快速排序的最差时间复杂度和冒泡排序是一样的都是O($n^2$)，它的平均时间复杂度为O(nlogn)。</li></ul><h1 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h1><p>堆排序（Heapsort）是指利用堆这种数据结构所设计的一种排序算法。<strong>堆是一个近似完全二叉树的结构</strong>，并同时满足堆积的性质：即子结点的键值或索引总是小于（或者大于）它的父节点。</p><p>完全二叉树的特点：</p><ul><li>叶子结点只能出现在最下两层。</li><li>最下层的叶子一定集中在左部连续位置</li><li>倒数二层，若有叶子结点，一定都在右部连续位置。</li><li>如果结点度为1，则该结点只有左孩子，即不存在只有右子树的情况。</li><li>同样结点数的二叉树，完全二叉树的深度最小。</li><li>‌完全二叉树的深度‌：具有$n$个节点的完全二叉树的深度为$log_2n+1$</li><li>‌编号规则‌：具有$n$个节点的完全二叉树，按顺序编号，节点$i$的父节点为$i&#x2F;2$，左子节点为$2i$，右子节点为$2i+1$</li></ul><p><img data-src="/transblog/images/597178083951359.png"></p><p>要想学习堆排序，首先需要了解两种调整算法：</p><h2 id="堆的向上调整算法"><a href="#堆的向上调整算法" class="headerlink" title="堆的向上调整算法"></a>堆的向上调整算法</h2><p>堆的向上调整算法是实现堆的<strong>插入核心算法</strong>。</p><p>对于堆的建立，假设我们将一组数据一个一个的加入到堆中，那么我们需要在每一个元素入堆时都要进行调整，我们将加入的元素存入到顺序表的最后，然后利用双亲结点和孩子结点之间的关系来判断是否需要调整，知道调整到根节点为止。对于此算法思想既可以使用递归算法，也可以使用循环算法，如下：</p><p><img data-src="/transblog/images/469058882560529.png"></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">AdjustUp</span><span class="params">(HPDataType* a, <span class="type">int</span> child)</span> </span>&#123;</span><br><span class="line"><span class="type">int</span> parent = (child - <span class="number">1</span>) / <span class="number">2</span>;                    <span class="comment">//index从0开始，所以减1，从1开始则是 child / 2</span></span><br><span class="line"><span class="keyword">while</span> (child &gt; <span class="number">0</span>) &#123;</span><br><span class="line"><span class="comment">//建立小堆，若建大堆，将 &lt; 改为 &gt;</span></span><br><span class="line"><span class="keyword">if</span> (a[child] &lt; a[parent]) &#123;</span><br><span class="line"><span class="built_in">Swap</span>(&amp;a[child], &amp;a[parent]);</span><br><span class="line">child = parent;</span><br><span class="line">parent = (child - <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="堆的向下调整算法"><a href="#堆的向下调整算法" class="headerlink" title="堆的向下调整算法"></a>堆的向下调整算法</h2><p>向下调整算法是<strong>堆删除堆顶元素</strong>的核心算法。</p><p> 当我们需要将堆顶的元素弹出时，将整个堆进行维护，建立一个新的堆。其主要思路是将当前元素的孩子结点与双亲结点进行比较，但是此时双亲节点的孩子节点有两个，我们需要找出最合适的那一个，比如：建小堆则找出最小的孩子结点，建大堆则找出最大的孩子结点。然后将孩子结点与双亲结点进行交换，以此迭代，直到迭代到叶子结点。</p><p><img data-src="/transblog/images/32739316630263.png"></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//建小堆的向下调整算法</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">AdjustDown</span><span class="params">(HPDataType* a, <span class="type">int</span> size, <span class="type">int</span> parent)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//左孩子结点与双亲结点的关系</span></span><br><span class="line"><span class="type">int</span> child = parent * <span class="number">2</span> + <span class="number">1</span>;                <span class="comment">//index从0开始，所以加1，从1开始则是 parent * 2</span></span><br><span class="line">    <span class="comment">//假若右孩子比左孩子更小，则将child变量加一。child + 1 &lt;size 用于保证不会越界</span></span><br><span class="line"><span class="comment">//若建大堆，改为if ((a[child + 1] &gt; a[child]) &amp;&amp; (child + 1) &lt; size)</span></span><br><span class="line">    <span class="keyword">if</span> ((a[child + <span class="number">1</span>] &lt; a[child]) &amp;&amp; (child + <span class="number">1</span>) &lt; size) &#123;</span><br><span class="line">++child;</span><br><span class="line">&#125;</span><br><span class="line">    <span class="comment">//当child&gt;=size时表示已经到达叶子结点跳出循环</span></span><br><span class="line"><span class="keyword">while</span> (child&lt;size) &#123;</span><br><span class="line">        <span class="comment">//双亲结点大于孩子结点交换</span></span><br><span class="line">        <span class="comment">//若建大堆，改为if (a[parent] &lt; a[child])</span></span><br><span class="line"><span class="keyword">if</span> (a[parent] &gt; a[child]) &#123;</span><br><span class="line"><span class="built_in">Swap</span>(&amp;a[parent], &amp;a[child]);</span><br><span class="line">            <span class="comment">//进行迭代</span></span><br><span class="line">parent = child;</span><br><span class="line">child = parent * <span class="number">2</span> + <span class="number">1</span>;</span><br><span class="line"><span class="keyword">if</span> ((a[child + <span class="number">1</span>] &lt; a[child]) &amp;&amp; (child + <span class="number">1</span>) &lt; size) &#123;</span><br><span class="line">++child;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">        <span class="comment">//若双亲结点不在大于孩子结点，说明已经迭代结束，不在需要继续进行迭代</span></span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法描述-6"><a href="#算法描述-6" class="headerlink" title="算法描述"></a>算法描述</h2><p>堆排序的步骤分为三步:</p><ol><li>建堆（升序建大堆，降序建小堆），每次将堆顶元素与最大或最小的叶子节点交换，并调整为最大或最小堆</li><li>交换数据，建完堆之后堆顶元素为最大或最小值，将其与未排序的末尾元素交换。</li><li>向下调整。交换完成后堆顶进入有序队列（等于删除），使用向下调整，继续调整为最大或最小堆</li></ol><p><strong>可以将整个堆压扁到叶子节点这一层，形成一个数组，便于理解</strong>。</p><h2 id="算法图解-6"><a href="#算法图解-6" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/174425365274469.gif"></p><h2 id="C-代码-6"><a href="#C-代码-6" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 堆排序</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">大顶堆sort之后，数组为从小到大排序</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">//====调整=====</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">AdjustHeap</span><span class="params">(<span class="type">int</span> *h, <span class="type">int</span> node, <span class="type">int</span> len)</span> <span class="comment">//----node为需要调整的结点编号，从0开始编号;len为堆长度</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> index = node;</span><br><span class="line">    <span class="type">int</span> child = <span class="number">2</span> * index + <span class="number">1</span>; <span class="comment">// 左孩子，第一个节点编号为0，index从0开始，所以加1，从1开始则是 2 * index</span></span><br><span class="line">    <span class="keyword">while</span> (child &lt; len)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 右子树</span></span><br><span class="line">        <span class="keyword">if</span> (child + <span class="number">1</span> &lt; len &amp;&amp; h[child] &lt; h[child + <span class="number">1</span>])</span><br><span class="line">        &#123;</span><br><span class="line">            child++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (h[index] &gt;= h[child])</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="built_in">Swap</span>(h[index], h[child]);</span><br><span class="line">        index = child;</span><br><span class="line">        child = <span class="number">2</span> * index + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//====建堆=====</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">MakeHeap</span><span class="params">(<span class="type">int</span> *h, <span class="type">int</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = len / <span class="number">2</span>; i &gt;= <span class="number">0</span>; --i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">AdjustHeap</span>(h, i, len);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//====排序=====</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">HeapSort</span><span class="params">(<span class="type">int</span> *h, <span class="type">int</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">MakeHeap</span>(h, len);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = len - <span class="number">1</span>; i &gt;= <span class="number">0</span>; --i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">Swap</span>(h[i], h[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">AdjustHeap</span>(h, <span class="number">0</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-6"><a href="#算法复杂度-6" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>堆排序使用堆来选数，效率就高了很多。时间复杂度：O(N*logN)；空间复杂度：O(1)；稳定性：不稳定</p><h1 id="计数排序"><a href="#计数排序" class="headerlink" title="计数排序"></a>计数排序</h1><ul><li>计数排序<strong>不是基于比较</strong>的排序算法，其核心在于将输入的数据值转化为键存储在额外开辟的数组空间中。 </li><li>它的基本思想是，对于待排序的数组，遍历数组元素，统计每个元素出现的次数，然后根据次数输出排序后的数组。</li><li>作为一种线性时间复杂度的排序，计数排序要求输入的数据必须是有确定范围的整数。</li></ul><h2 id="算法描述-7"><a href="#算法描述-7" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>找出待排序的数组中最大和最小的元素；</li><li>统计数组中每个值为i的元素出现的次数，存入数组C的第i项；</li><li>对所有的计数累加（从C中的第一个元素开始，每一项和前一项相加）；</li><li>反向填充目标数组：将每个元素i放在新数组的第C(i)项，每放一个元素就将C(i)减去1。</li></ol><h2 id="算法图解-7"><a href="#算法图解-7" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/581405040704017.gif"></p><h2 id="C-代码-7"><a href="#C-代码-7" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">countingSort</span><span class="params">(vector&lt;<span class="type">int</span>&gt; arr, <span class="type">int</span> maxValue)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个大小为maxValue + 1的桶，并初始化为0</span></span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">bucket</span><span class="params">(maxValue + <span class="number">1</span>, <span class="number">0</span>)</span></span>;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; sortedArr; <span class="comment">// 用于存储排序后的数组</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 统计每个值出现的次数</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; arr.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">        bucket[arr[i]]++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 根据桶中的数据重建排序后的数组</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt;= maxValue; ++j) &#123;</span><br><span class="line">        <span class="keyword">while</span> (bucket[j] &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            sortedArr.<span class="built_in">push_back</span>(j);</span><br><span class="line">            bucket[j]--;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sortedArr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-7"><a href="#算法复杂度-7" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>计数排序是一个稳定的排序算法。当输入的元素是 n 个 0到 k 之间的整数时，时间复杂度是O(n+k)，空间复杂度也是O(n+k)，其排序速度快于任何比较排序算法。当k不是很大并且序列比较集中时，计数排序是一个很有效的排序算法。</p><h1 id="桶排序"><a href="#桶排序" class="headerlink" title="桶排序"></a>桶排序</h1><p>桶排序是计数排序的升级版。它利用了函数的映射关系，高效与否的关键就在于这个映射函数的确定。</p><p>桶排序 (Bucket sort)的工作的原理：假设输入数据服从均匀分布，将数据分到有限数量的桶里，每个桶再分别排序（有可能再使用别的排序算法或是以递归方式继续使用桶排序进行排）。</p><h2 id="算法描述-8"><a href="#算法描述-8" class="headerlink" title="算法描述"></a>算法描述</h2><ol><li>设置一个定量的数组当作空桶；</li><li>遍历输入数据，并且把数据一个一个放到对应的桶里去；</li><li>对每个不是空的桶进行排序；</li><li>从不是空的桶里把排好序的数据拼接起来。</li></ol><h2 id="算法图解-8"><a href="#算法图解-8" class="headerlink" title="算法图解"></a>算法图解</h2><p><img data-src="/transblog/images/219875669536949.png"></p><h2 id="C-代码-8"><a href="#C-代码-8" class="headerlink" title="C++代码"></a>C++代码</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 插入排序辅助函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">insertionSort</span><span class="params">(std::vector&lt;<span class="type">int</span>&gt;&amp; arr)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">1</span>; i &lt; arr.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">        <span class="type">int</span> key = arr[i];</span><br><span class="line">        <span class="type">int</span> j = i - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span> (j &gt;= <span class="number">0</span> &amp;&amp; arr[j] &gt; key) &#123;</span><br><span class="line">            arr[j + <span class="number">1</span>] = arr[j];</span><br><span class="line">            --j;</span><br><span class="line">        &#125;</span><br><span class="line">        arr[j + <span class="number">1</span>] = key;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 桶排序主函数</span></span><br><span class="line"><span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">bucketSort</span><span class="params">(std::vector&lt;<span class="type">int</span>&gt; arr, <span class="type">int</span> bucketSize = <span class="number">5</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (arr.<span class="built_in">empty</span>()) <span class="keyword">return</span> arr;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> minValue = *std::<span class="built_in">min_element</span>(arr.<span class="built_in">begin</span>(), arr.<span class="built_in">end</span>());</span><br><span class="line">    <span class="type">int</span> maxValue = *std::<span class="built_in">max_element</span>(arr.<span class="built_in">begin</span>(), arr.<span class="built_in">end</span>());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 计算桶的数量</span></span><br><span class="line">    <span class="type">size_t</span> bucketCount = (maxValue - minValue) / bucketSize + <span class="number">1</span>;</span><br><span class="line">    std::vector&lt;std::vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">buckets</span>(bucketCount);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 分配输入数组到各个桶中</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> num : arr) &#123;</span><br><span class="line">        buckets[(num - minValue) / bucketSize].<span class="built_in">push_back</span>(num);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 清空原始数组并按序合并已排序的桶</span></span><br><span class="line">    arr.<span class="built_in">clear</span>();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; bucket : buckets) &#123;</span><br><span class="line">        <span class="built_in">insertionSort</span>(bucket); <span class="comment">// 对每个桶进行排序</span></span><br><span class="line">        arr.<span class="built_in">insert</span>(arr.<span class="built_in">end</span>(), bucket.<span class="built_in">begin</span>(), bucket.<span class="built_in">end</span>());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="算法复杂度-8"><a href="#算法复杂度-8" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>当桶的数量和需要排序的元素一样多，桶排序此时就变成了计数排序，桶排序最好情况下使用线性时间O(n)，桶排序的时间复杂度，取决与对各个桶之间数据进行排序的时间复杂度，因为其它部分的时间复杂度都为O(n)。很显然，桶划分的越小，各个桶之间的数据越少，排序所用的时间也会越少。但相应的空间消耗就会增大。 </p><h1 id="基数排序"><a href="#基数排序" class="headerlink" title="基数排序"></a>基数排序</h1><p>基数排序（Radix Sort）是按照低位先排序，然后收集；再按照高位排序，然后再收集；依次类推，直到最高位。有时候有些属性是有优先级顺序的，先按低优先级排序，再按高优先级排序。最后的次序就是高优先级高的在前，高优先级相同的低优先级高的在前。</p><p>基数排序可以分为两种：<strong>LSD(低位到高位排序)，MSD(高位到低位排序)，LSD的基数排序适用于位数少的数列，如果位数多的话，使用MSD的效率会比较好</strong>。</p><h2 id="LSD"><a href="#LSD" class="headerlink" title="LSD"></a>LSD</h2><h3 id="算法描述-9"><a href="#算法描述-9" class="headerlink" title="算法描述"></a>算法描述</h3><ol><li>取得数组中的最大数，并取得位数；</li><li>arr为原始数组，从最低位开始取每个位组成radix数组；</li><li>对radix进行计数排序（利用计数排序适用于小范围数的特点）；</li></ol><h3 id="算法图解-9"><a href="#算法图解-9" class="headerlink" title="算法图解"></a>算法图解</h3><p><img data-src="/transblog/images/174116081423331.gif"></p><h3 id="C-代码-9"><a href="#C-代码-9" class="headerlink" title="C++代码"></a>C++代码</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/********************************************************</span></span><br><span class="line"><span class="comment">*函数名称：GetNumInPos</span></span><br><span class="line"><span class="comment">*参数说明：num 一个整形数据</span></span><br><span class="line"><span class="comment">*     pos 表示要获得的整形的第pos位数据</span></span><br><span class="line"><span class="comment">*说明：    找到num的从低到高的第pos位的数据</span></span><br><span class="line"><span class="comment">*********************************************************/</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">GetNumInPos</span><span class="params">(<span class="type">int</span> num, <span class="type">int</span> pos)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="type">int</span> temp = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; pos - <span class="number">1</span>; i++)</span><br><span class="line">temp *= <span class="number">10</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">return</span> (num / temp) % <span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAXPOS 10    <span class="comment">//最高位的位数</span></span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">RadixSort</span><span class="params">(vector&lt;<span class="type">int</span>&gt; &amp;A)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="type">int</span> len = A.<span class="built_in">size</span>();</span><br><span class="line">vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">radixArray</span>(<span class="number">10</span>);  <span class="comment">//分为0~9的序列空间</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> pos = <span class="number">1</span>; pos &lt;= MAXPOS; pos++)    <span class="comment">//从个位开始到最高位数</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len; i++)    <span class="comment">//分配过程</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> num = <span class="built_in">GetNumInPos</span>(A[i], pos);</span><br><span class="line">radixArray[num].<span class="built_in">push_back</span>(A[i]);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>, j = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)    <span class="comment">//收集</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">while</span> (!radixArray[i].<span class="built_in">empty</span>())</span><br><span class="line">&#123;</span><br><span class="line">A[j++] = radixArray[i].<span class="built_in">front</span>();  <span class="comment">//取首部数据依次插入原数组</span></span><br><span class="line">radixArray[i].<span class="built_in">erase</span>(radixArray[i].<span class="built_in">begin</span>());    <span class="comment">//移除首部元素</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="MSD"><a href="#MSD" class="headerlink" title="MSD"></a>MSD</h2><p>MSD基数排序是从最高位开始对序列进行分组，到最低位为止。但是其实现过程是和LSD基数排序不同的，排序过程中需要用到递归函数。</p><h3 id="算法描述-10"><a href="#算法描述-10" class="headerlink" title="算法描述"></a>算法描述</h3><p>MSD的方式由高位数为基底开始进行分配，但在分配之后并不马上合并回一个数组中，而是在每个“桶子”中建立“子桶”，将每个桶子中的数值按照下一数位的值分配到“子桶”中。在进行完最低位数的分配后再合并回单一的数组中。</p><h3 id="算法图解-10"><a href="#算法图解-10" class="headerlink" title="算法图解"></a>算法图解</h3><p><img data-src="/transblog/images/443275659345444.png"></p><h3 id="C-代码-10"><a href="#C-代码-10" class="headerlink" title="C++代码"></a>C++代码</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/********************************************************</span></span><br><span class="line"><span class="comment"> *函数名称：GetNumInPos</span></span><br><span class="line"><span class="comment"> *参数说明：num 一个整形数据</span></span><br><span class="line"><span class="comment"> *     pos 表示要获得的整形的第pos位数据</span></span><br><span class="line"><span class="comment"> *说明：    找到num的从低到高的第pos位的数据</span></span><br><span class="line"><span class="comment"> *********************************************************/</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">GetNumInPos</span><span class="params">(<span class="type">int</span> num, <span class="type">int</span> pos)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> temp = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; pos - <span class="number">1</span>; i++)</span><br><span class="line">        temp *= <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">return</span> (num / temp) % <span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// MSD,调用时指定最高位数d</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">RadixSort</span><span class="params">(vector&lt;<span class="type">int</span>&gt; &amp;A, <span class="type">int</span> d)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> len = A.<span class="built_in">size</span>();</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">radixArray</span>(<span class="number">10</span>); <span class="comment">// 分为0~9的序列空间，用队列保存每个桶分配的数据</span></span><br><span class="line">    <span class="comment">// 位数大于0，且数组长度大于1</span></span><br><span class="line">    <span class="keyword">if</span> (d &gt;= <span class="number">1</span> &amp;&amp; len &gt; <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; len; i++) <span class="comment">// 分配过程</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> num = <span class="built_in">GetNumInPos</span>(A[i], d);</span><br><span class="line">            radixArray[num].<span class="built_in">push_back</span>(A[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        cout &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>, j = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) <span class="comment">// 收集</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">RadixSort</span>(radixArray[i], d - <span class="number">1</span>); <span class="comment">// 递归,对每个子桶从次高位开始分配</span></span><br><span class="line">            <span class="keyword">while</span> (!radixArray[i].<span class="built_in">empty</span>())</span><br><span class="line">            &#123;</span><br><span class="line">                A[j++] = radixArray[i].<span class="built_in">front</span>(); <span class="comment">// 取队列首部数据依次插入原数组</span></span><br><span class="line">                radixArray[i].<span class="built_in">erase</span>(radixArray[i].<span class="built_in">begin</span>());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="算法复杂度-9"><a href="#算法复杂度-9" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>基数排序基于分别排序，分别收集，所以是稳定的。但基数排序的性能比桶排序要略差，每一次关键字的桶分配都需要O(n)的时间复杂度，而且分配之后得到新的关键字序列又需要O(n)的时间复杂度。假如待排数据可以分为d个关键字，则基数排序的时间复杂度将是O(d*2n) ，当然d要远远小于n，因此基本上还是线性级别的。</p><p>基数排序的空间复杂度为O(n+k)，其中k为桶的数量。一般来说n&gt;&gt;k，因此额外空间需要大概n个左右。</p><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;排序算法&quot;&gt;&lt;a href=&quot;#排序算法&quot; class=&quot;headerlink&quot; title=&quot;排序算法&quot;&gt;&lt;/a&gt;排序算法&lt;/h1&gt;&lt;p&gt;稳定：如果a原本在b前面，而a&amp;#x3D;b，排序之后a仍然在b的前面。&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
</summary>
      
    
    
    
    <category term="设计" scheme="https://trans-xu.github.io/transblog/categories/%E8%AE%BE%E8%AE%A1/"/>
    
    
  </entry>
  
  <entry>
    <title>系统监控-Prometheus</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/%E5%8F%AF%E8%A7%86%E5%8C%96%E7%B3%BB%E7%BB%9F%E7%9B%91%E6%8E%A7/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/%E5%8F%AF%E8%A7%86%E5%8C%96%E7%B3%BB%E7%BB%9F%E7%9B%91%E6%8E%A7/</id>
    <published>2025-08-27T03:50:08.903Z</published>
    <updated>2025-08-27T03:37:51.447Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Grafana"><a href="#Grafana" class="headerlink" title="Grafana"></a>Grafana</h1><h2 id="什么是Grafana？"><a href="#什么是Grafana？" class="headerlink" title="什么是Grafana？"></a>什么是Grafana？</h2><p><a href="https://grafana.com/docs/grafana/latest/datasources/prometheus/">Grafana使用Prometheus数据源官方文档</a><br>Grafana是一款功能强大的开源监控数据分析和可视化套件。它最初主要用于显示和监控Graphite指标数据，但随着时间的推移，其功能和应用范围得到了极大的扩展。Grafana的核心功能在于对基础设施和应用数据分析的时间序列数据进行可视化分析，使用户能够更直观地理解和分析数据。<strong>可以理解为一种前端展示</strong></p><p><img data-src="/transblog/images/238945509252002.png"></p><p>特点：</p><ol><li>展示方式：快速灵活的客户端图表，面板插件有许多不同方式的可视化指标和日志，官方库中具有丰富的仪表盘插件，比如热图、折线图、图表等多种展示方式；</li><li>数据源：Graphite，InfluxDB，OpenTSDB，Prometheus，Elasticsearch，CloudWatch和KairosDB等；</li><li>通知提醒：以可视方式定义最重要指标的警报规则，Grafana将不断计算并发送通知，在数据达到阈值时通过Slack、PagerDuty等获得通知；</li><li>混合展示：在同一图表中混合使用不同的数据源，可以基于每个查询指定数据源，甚至自定义数据源；</li><li>注释：使用来自不同数据源的丰富事件注释图表，将鼠标悬停在事件上会显示完整的事件元数据和标记；</li><li>过滤器：Ad-hoc过滤器允许动态创建新的键&#x2F;值过滤器，这些过滤器会自动应用于使用该数据源的所有查询。</li></ol><h2 id="Grafana视图模板"><a href="#Grafana视图模板" class="headerlink" title="Grafana视图模板"></a>Grafana视图模板</h2><p><img data-src="/transblog/images/498635111236447.png"></p><h1 id="Prometheus"><a href="#Prometheus" class="headerlink" title="Prometheus"></a>Prometheus</h1><h2 id="Prometheus实战"><a href="#Prometheus实战" class="headerlink" title="Prometheus实战"></a>Prometheus实战</h2><p><a href="https://prometheus.wang/">Prometheus学习与实战</a></p><ul><li>Retrieval：负责在活跃的target主机上抓取监控指标数据</li><li>Storage：存储主要是把采集到的数据存储到磁盘中</li><li>PromQL：是Prometheus提供的查询语言模块。</li><li>Prometheus Server：用于收集和存储时间序列数据</li><li>pushgateway：各个目标主机可上报数据到pushgateway，然后prometheus server统一从pushgateway拉取数据。</li></ul><p><img data-src="/transblog/images/179791315256613.png"></p><p><strong>！！！Prometheus 的指标值，并非 100% 准确！！！</strong></p><h2 id="可观测三角"><a href="#可观测三角" class="headerlink" title="可观测三角"></a>可观测三角</h2><p>在可观测的世界里，metrics（指标）、log（日志）、trace（调用链） 三足鼎立。有点<strong>类似分布式系统的CAP理论</strong></p><p><img data-src="/transblog/images/375883109258991.png"></p><ul><li>metrics（指标）好似运动手表：24小时采样心率，并在心率超限时告警；</li><li>log（日志）好似详细体检：输出厚厚一沓严密、精确的权威报告；</li><li>trace（调用链）好似血管灌注：哪段血管堵了，只需看灌注造影，一目了然。</li></ul><p>Prometheus 所在的 metrics（指标） 领域，其目标是诊断整体健康状况，其手段通常是对原始数据先采样、再聚合，利用有限的信息，分析变化趋势；而并非像 log（日志）那样，翔实精确、事无巨细地，记录每一桩事件、每一条原始数据。</p><h2 id="Prometheus监控数据要点"><a href="#Prometheus监控数据要点" class="headerlink" title="Prometheus监控数据要点"></a>Prometheus监控数据要点</h2><h3 id="请求类数据"><a href="#请求类数据" class="headerlink" title="请求类数据"></a>请求类数据</h3><ol><li>请求量 - 按接口统计</li><li>错误数 - 按接口统计</li><li>接口耗时 - P50（可选）、P90、P95、P99、MAX</li><li>访问第三方资源数据 - 请求量、错误数、接口耗时</li></ol><h3 id="资源类数据"><a href="#资源类数据" class="headerlink" title="资源类数据"></a>资源类数据</h3><ol><li>线程使用数</li><li>队列长度——run: size()——PushPackage：&gt;10</li><li>协程使用数量</li><li>进程句柄数</li><li>机器句柄数</li><li>机器CPU</li><li>机器内存使用数量</li><li>….</li></ol><h3 id="组件类数据"><a href="#组件类数据" class="headerlink" title="组件类数据"></a>组件类数据</h3><ol><li>redis访问数据 - 访问量、耗时（Pxx）</li><li>Mysql访问数据</li><li>MQ访问数据</li><li>…</li></ol><h3 id="自定义数据"><a href="#自定义数据" class="headerlink" title="自定义数据"></a>自定义数据</h3><ol><li>特殊业务流量上报</li><li>CUPS</li><li>特殊（重点关注）错误码</li><li>…</li></ol><h2 id="Prometheus的数据形式–时间序列"><a href="#Prometheus的数据形式–时间序列" class="headerlink" title="Prometheus的数据形式–时间序列"></a>Prometheus的数据形式–时间序列</h2><p>在Prometheus中，时间序列数据以时间戳为索引，以连续的数据样本构成的系列称为时间序列。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">│   . . . . . . . . . . . . . . . . .   node_cpu&#123;cpu=<span class="string">&quot;cpu0&quot;</span>,mode=<span class="string">&quot;idle&quot;</span>&#125;    时间序列1</span><br><span class="line">│   . . . . . . . . . . . . . . . . .   node_cpu&#123;cpu=<span class="string">&quot;cpu0&quot;</span>,mode=<span class="string">&quot;system&quot;</span>&#125;  时间序列2</span><br><span class="line">│   . . . . . . . . . . . . . . . ..    node_load1&#123;&#125;                        时间序列3</span><br><span class="line">v</span><br><span class="line">  ----------------------------------- 时间 -----------------------------------------&gt;</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">&lt;-------------------- 指标 ------------------&gt;&lt;--- 时间戳 ---&gt;&lt;- 样本值 -&gt;</span><br><span class="line">&lt;------------------- metric -----------------&gt;&lt;- timestamp -&gt;&lt;- value -&gt;</span><br><span class="line">http_request_total&#123;status=<span class="string">&quot;200&quot;</span>, method=<span class="string">&quot;GET&quot;</span>&#125;@1434417560938 =&gt; 94355</span><br><span class="line">http_request_total&#123;status=<span class="string">&quot;200&quot;</span>, method=<span class="string">&quot;GET&quot;</span>&#125;@1434417561287 =&gt; 94334</span><br><span class="line"></span><br><span class="line">http_request_total&#123;status=<span class="string">&quot;404&quot;</span>, method=<span class="string">&quot;GET&quot;</span>&#125;@1434417560938 =&gt; 38473</span><br><span class="line">http_request_total&#123;status=<span class="string">&quot;404&quot;</span>, method=<span class="string">&quot;GET&quot;</span>&#125;@1434417561287 =&gt; 38544</span><br><span class="line"></span><br><span class="line">http_request_total&#123;status=<span class="string">&quot;200&quot;</span>, method=<span class="string">&quot;POST&quot;</span>&#125;@1434417560938 =&gt; 4748</span><br><span class="line">http_request_total&#123;status=<span class="string">&quot;200&quot;</span>, method=<span class="string">&quot;POST&quot;</span>&#125;@1434417561287 =&gt; 4785</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>在形式上，所有的指标(Metric)都通过如下格式标示：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;metric name&gt;&#123;&lt;label name&gt;=&lt;label value&gt;, ...&#125;</span><br></pre></td></tr></table></figure><h2 id="Prometheus做出的妥协"><a href="#Prometheus做出的妥协" class="headerlink" title="Prometheus做出的妥协"></a>Prometheus做出的妥协</h2><p><strong>宁愿“脑补”，也不愿低效。</strong></p><p>除了 metrics 领域自身的特性，Prometheus 毕竟处在一个条件有限的真实世界，它还要随时面临以下困难：</p><ul><li>自身硬件有限：不敢拼命计算，不敢无限存储；</li><li>采样统计的局限性：稀稀拉拉、分布未知的样本；</li><li>分布式的局限性：丢数据的网络、不靠谱的对端；<br>……</li></ul><p>Prometheus 需要在上述限制下，交出 not perfect、但是 good enough 的指标。于是就有了下述设计：</p><ul><li><strong>单次采样不重要，多次采样组成的时间序列才重要。所以，单次采样受阻，是可以无需重试、直接丢弃的。</strong></li><li><strong>单点数值不重要，多点数值汇聚的变化趋势才重要。所以，单点数值是可以“无中生有”、”脑补”估算的。</strong></li></ul><h2 id="P50、P90、P95"><a href="#P50、P90、P95" class="headerlink" title="P50、P90、P95"></a>P50、P90、P95</h2><p>P90 是一个统计术语，代表着第90百分位数（90th percentile）。在性能监控和服务质量评估中，P90 常用来衡量响应时间或延迟的指标。具体来说，P90 的含义是在所有测量值中，有 90% 的数据点小于或等于这个值，而只有 10% 的数据点大于这个值。</p><p>例如，如果一个网络服务的响应时间的 P90 是 200 毫秒，这意味着在所有的请求中，90% 的请求的响应时间都不会超过 200 毫秒，只有 10% 的请求的响应时间会超过这个数值。这是一个衡量系统在高负载下性能的重要指标，因为它可以告诉你绝大多数用户的体验如何。</p><h2 id="Prometheus的range"><a href="#Prometheus的range" class="headerlink" title="Prometheus的range"></a>Prometheus的range</h2><p>选择 rate 的 range 时，到底在选择什么？</p><ul><li>rate[30s] 计算过去 30 秒内的平均速率。</li><li>rate[1m] 计算过去 1 分钟内的平均速率。</li><li>rate[5m] 计算过去 5 分钟内的平均速率。</li></ul><p><img data-src="/transblog/images/430785410252008.png" alt="rate[30s]"></p><p><img data-src="/transblog/images/558635410269888.png" alt="rate[1m]"></p><p><img data-src="/transblog/images/110965510267492.png" alt="rate[5m]"></p><p>为什么上面三幅图选择范围不同图形状况会大变？<br>假设我们系统的错误数长期为0，而在某时刻暴增100，那么上述三种时间范围窗口，意味着将这 100 均分到 30秒，还是 60 秒，还是300秒；那么答案也显而易见：分母越大，按秒平均后的增量则越平滑。<br><strong>总结：</strong></p><ul><li><strong>窗口小则更加敏感，能够捕捉到更短时间内的变化。这意味着如果有突发事件或者短期波动，它会在曲线上表现得更明显。</strong></li><li><strong>窗口大会更加平滑，因为它平均了更长时间内的数据。这样可以减少短期波动的影响，但也可能掩盖掉短时间内的突发事件。</strong></li></ul><h2 id="案例分解"><a href="#案例分解" class="headerlink" title="案例分解"></a>案例分解</h2><h3 id="rate-increase"><a href="#rate-increase" class="headerlink" title="rate&#x2F;increase"></a>rate&#x2F;increase</h3><p>在使用 rate 或者 increase 观测 counter 类型的指标增量时，经常碰到以下问题：</p><ul><li>每分钟新增的请求数，竟然是个小数？</li><li>不仅是个小数，还比真实增量更大？</li></ul><p>Prometheus在没有踩中样本值做法是：naive 地假设所有样本点在该时间范围内是均匀分布的，然后按照这个均匀分布的线性规律，**<mark>脑补</mark>估算出边界上的采样点**。那么，补出来一些不准确的值，也就不足为奇了。</p><p>其中脑补这一操作，使用的就是<strong>线性外推算法</strong>：rate&#x2F;increase[时间范围] 在计算该时间范围内的增量时，第一步要拿到该时间范围边界上（开始时刻和结束时刻）的样本点，相减得到差值。<strong>但是：在当前时间范围的边界，并不一定那么凑巧地有样本点存在。</strong></p><p><strong>分析：</strong></p><p><img data-src="/transblog/images/435884609258196.png"></p><p>计算一分钟之内，errors_total 值的增量，也即 increase(errors_total[1m])。要计算 [1m] 的时间范围&#x2F;取样窗口内的 increase，在最理想的情况下，Prometheus 根本不想关心这个窗口内的其他数据，而只需从窗口左边界取第一个点，右边界取最后一个点，相减即可：</p><p><img data-src="/transblog/images/440064009267024.png"></p><p><strong>但是，能保证每次都能踩中自己上报的值吗？答案很显然不能</strong></p><p><img data-src="/transblog/images/222244109259693.png"></p><p>取窗口覆盖范围内的第一个点和最后一个点，计算斜率，并按照该斜率将直线延伸至窗口边界，无中生有地“脑补”出虚拟的两个“样本点”，即可相减计算 increase 了：</p><p><img data-src="/transblog/images/249594409256248.png"></p><p>如上图：用绿圈圈所代表的“虚拟样本”相减，得到的 increase 1.3 不仅是个小数，还比实际值偏大，也就不足为奇了。</p><h3 id="histogram"><a href="#histogram" class="headerlink" title="histogram"></a>histogram</h3><p>histogram是柱状图，有三种作用：</p><ol><li>对每个采样点进行统计（并不是一段时间的统计），打到各个桶(bucket)中</li><li>对每个采样点值累计和(sum)</li><li>对采样点的次数累计和(count)</li></ol><h4 id="为什么histogram得到的值会不准？"><a href="#为什么histogram得到的值会不准？" class="headerlink" title="为什么histogram得到的值会不准？"></a>为什么histogram得到的值会不准？</h4><p>先从茫茫多的原始数据中采样出样本点，放到各个 bucket（桶）里；然后 naive 地假设所有样本是均匀分布的，据此做线性插值，“无中生有”出所需的“样本点”。</p><p><strong>分析：</strong></p><p><img data-src="/transblog/images/435884609258196.png"></p><p>模拟拟每秒产生一个新的 HTTP 请求耗时的观察值，然后计算其 P99。为了埋点生成 http_response_time_seconds 这一 histogram 指标，每秒钟暴露一个观察值：假设有 50% 的概率，样本值大小在 [0.1, 0.5) 范围内。有 50% 的概率，样本值大小在 [0.5, 1.0) 范围内。<br><strong>这里故意对histogram划分成四个不合理的桶: &lt;&#x3D; 0.1、&lt;&#x3D;0.5、&lt;&#x3D;100、&lt;&#x3D;正无穷</strong>。它们在各个桶内的分布，大致示意如下：</p><p><img data-src="/transblog/images/375914210240571.png"></p><p>如何计算P99：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">histogram_quantile(0.99, rate(http_response_time_seconds_bucket[5m]))</span><br></pre></td></tr></table></figure><p>99% 分位 * 总数 100 &#x3D; 第 99 号。第 99 号样本在 0.5~100 bucket 段的第 51 号 ~ 第 100 号样本当中，排行 49。<br><strong>Prometheus 只知道：</strong></p><ul><li>有 50 个样本落在 0.5~100 bucket 段。</li><li>排行第一和排行最末的的样本值之差最大可达 100-0.5（bucket 段的左右边界之差）。</li><li>目标样本在这个 bucket 段里排行 49。</li></ul><p><strong>但是它并不知道：</strong></p><ul><li>这个 bucket 段左右边界的差值 100 - 0.5 是否完全分布到了这 50 个样本头上。</li><li>bucket 段内的差值是如何分布到这 50 个样本头上的。</li></ul><p>保险起见，Prometheus<strong>又自动脑补</strong>bucket 段内的全额差值，被均匀、线性地，分到了所有样本头上。因为这样就<strong>又可以用线性插值</strong>的方式，来计算分位值了：</p><p>所求分位值 &#x3D; bucket 段左边界值 + (bucket 段右边界值 - bucket 段左边界值) * (目标样本在本 bucket 段的排行 &#x2F; 本 bucket 段的样本总数)即：P99 &#x3D; 0.5 + (100 - 0.5) * (49&#x2F;50) &#x3D; 98.01</p><p><img data-src="/transblog/images/267775010258997.png"></p><h3 id="summary"><a href="#summary" class="headerlink" title="summary"></a>summary</h3><p>用于记录某些东西的平均大小，可能是计算所需的时间或处理的文件大小，summary显示两个相关的信息：count（事件发生的次数）和 sum（所有事件的总大小）。</p><p>例如：</p><ul><li>在客户端对于一段时间内（默认是10分钟）的每个采样点进行统计，并形成分位图。（如：正态分布一样，统计低于60分不及格的同学比例，统计低于80分的同学比例，统计低于95分的同学比例）</li><li>统计班上所有同学的总成绩(sum)</li><li>统计班上同学的考试总人数(count)</li></ul><p>如果返回次数为 3 和总和 15，也就意味着 3 次计算总共需要 15s 来处理，平均每次计算需要花费 5s。下一个样本的次数为 10，总和为 113，那么平均值为 11.3，因为两组指标都记录有时间戳，所以我们可以使用摘要来构建一个图表，显示平均值的变化率</p><h4 id="怎么选择histogram和summary？"><a href="#怎么选择histogram和summary？" class="headerlink" title="怎么选择histogram和summary？"></a>怎么选择histogram和summary？</h4><p><strong>如果需要聚合，或对将要观察的值的范围和分布有所了解，请选择histogram。无论值的范围和分布是什么，如果都需要准确的分位数，请选择summary。</strong></p><h2 id="PromQL"><a href="#PromQL" class="headerlink" title="PromQL"></a>PromQL</h2><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Grafana&quot;&gt;&lt;a href=&quot;#Grafana&quot; class=&quot;headerlink&quot; title=&quot;Grafana&quot;&gt;&lt;/a&gt;Grafana&lt;/h1&gt;&lt;h2 id=&quot;什么是Grafana？&quot;&gt;&lt;a href=&quot;#什么是Grafana？&quot; class=&quot;he</summary>
      
    
    
    
    <category term="奇闻轶事" scheme="https://trans-xu.github.io/transblog/categories/%E5%A5%87%E9%97%BB%E8%BD%B6%E4%BA%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>ZooKeeper</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/ZooKeeper/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/ZooKeeper/</id>
    <published>2025-08-27T03:50:08.900Z</published>
    <updated>2025-08-27T03:36:11.989Z</updated>
    
    <content type="html"><![CDATA[<h1 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h1><p>Zookeeper 提供的主要功能包括：</p><ol><li>配置管理</li><li>分布式锁</li><li>集群管理</li></ol><h1 id="数据模型"><a href="#数据模型" class="headerlink" title="数据模型"></a>数据模型</h1><p>ZooKeeper 是一个树形目录服务，和文件系统目录树很类似，拥有一个层次化结构。<br>这里面的每一个节点都被称为： ZNode，每个节点上都会保存自己的数据和节点信息。<br>节点可以拥有子节点，同时也允许少量（1MB）数据存储在该节点之下。<br>节点可以分为四大类：</p><ol><li>PERSISTENT     持久化节点</li><li>EPHEMERAL     临时节点 ：-e</li><li>PERSISTENT_SEQUENTIAL     持久化顺序节点 ：-s</li><li>EPHEMERAL_SEQUENTIAL     临时顺序节点 ：-es</li></ol><h1 id="ZNode节点属性"><a href="#ZNode节点属性" class="headerlink" title="ZNode节点属性"></a>ZNode节点属性</h1><table><thead><tr><th align="left">节点属性</th><th align="left">注解</th></tr></thead><tbody><tr><td align="left">cZxid</td><td align="left">该数据节点被创建时的事务Id</td></tr><tr><td align="left">mZxid</td><td align="left">该数据节点被修改时最新的事物Id</td></tr><tr><td align="left">pZxid</td><td align="left">当前节点的父级节点事务Id</td></tr><tr><td align="left">ctime</td><td align="left">该数据节点创建时间</td></tr><tr><td align="left">mtime</td><td align="left">数据节点最后修改时间</td></tr><tr><td align="left">dataVersion</td><td align="left">当前节点版本号（每修改一次值+1递增）</td></tr><tr><td align="left">cversion</td><td align="left">子节点版本号（子节点修改次数，每修改一次值+1递增）</td></tr><tr><td align="left">aclVersion</td><td align="left">当前节点acl版本号（节点被修改acl权限，每修改一次值+1递增）</td></tr><tr><td align="left">ephemeralOwner</td><td align="left">临时节点标示，当前节点如果是临时节点，则存储的创建者的会话id（sessionId），如果不是，那么值&#x3D;0</td></tr><tr><td align="left">dataLength</td><td align="left">当前节点所存储的数据长度</td></tr><tr><td align="left">numChildren</td><td align="left">当前节点下子节点的个数</td></tr></tbody></table><h1 id="分布式锁原理"><a href="#分布式锁原理" class="headerlink" title="分布式锁原理"></a>分布式锁原理</h1><p>zookeeper基于watcher机制和znode的有序节点，天生就是一个分布式锁的坯子。</p><p>核心思想：当客户端要获取锁，则创建节点，使用完锁，则删除该节点。</p><ol><li>客户端获取锁时，在lock节点下创建临时顺序节点。</li><li>然后获取lock下面的所有子节点，客户端获取到所有的子节点之后，如果发现自己创建的子节点序号最小，那么就认为该客户端获取到了锁。使用完锁后，将该节点删除。</li><li>如果发现自己创建的节点并非lock所有子节点中最小的，说明自己还没有获取到锁，此时客户端需要找到比自己小的那个节点，同时对其注册事件监听器，监听删除事件。</li><li>如果发现比自己小的那个节点被删除，则客户端的Watcher会收到相应通知，此时再次判断自己创建的节点是否是lock子节点中序号最小的，如果是则获取到了锁，如果不是则重复以上步骤继续获取到比自己小的一个节点并注册监听。</li></ol><p><img data-src="/transblog/images/435884609258196.png"></p><ul><li><p>首先创建一个&#x2F;test&#x2F;lock父节点作为一把锁，尽量是持久节点（PERSISTENT类型），每个尝试获取这把锁的客户端，在&#x2F;test&#x2F;lock父节点下创建临时顺序子节点。</p></li><li><p>由于序号的递增性，我们规定序号最小的节点即获得锁。例如：客户端来获取锁，在&#x2F;test&#x2F;lock节点下创建节点为&#x2F;test&#x2F;lock&#x2F;seq-00000001，它是最小的所以它优先拿到了锁，其它节点等待通知再次获取锁。&#x2F;test&#x2F;lock&#x2F;seq-00000001执行完自己的逻辑后删除节点释放锁。</p></li><li><p>那么节点&#x2F;test&#x2F;lock&#x2F;seq-00000002想要获取锁等谁的通知呢？这里让&#x2F;test&#x2F;lock&#x2F;seq-00000002节点监听&#x2F;test&#x2F;lock&#x2F;seq-00000001节点，一旦&#x2F;test&#x2F;lock&#x2F;seq-00000001节点删除，则通知&#x2F;test&#x2F;lock&#x2F;seq-00000002节点，让它再次判断自己是不是最小的节点，是则拿到锁，不是继续等通知。</p></li><li><p>以此类推&#x2F;test&#x2F;lock&#x2F;seq-00000003节点监听&#x2F;test&#x2F;lock&#x2F;seq-00000002节点，总是让后一个节点监听前一个节点，不用让所有节点都监听最小的节点，避免设置不必要的监听，以免造成大量无效的通知，形成“羊群效应”。</p></li></ul><p><img data-src="/transblog/images/555070515252284.png"></p><p>zookeeper分布式锁和redis分布式锁相比，因为大量的创建、删除节点性能上比较差，并不是很推荐。</p><h1 id="羊群效应"><a href="#羊群效应" class="headerlink" title="羊群效应"></a>羊群效应</h1><p>分布式锁的普通实现：注册临时节点，谁注册成功谁获取锁，其他监听该节点的删除事件，一旦被删除，通知其他客户端，再次重复该流程；此为最简单的实现。</p><p><strong>但这种情况下，大量客户端在短时间内对 ZooKeeper 服务器发起请求，可能超出了服务器的处理能力，导致延迟增加、响应时间变长，甚至导致服务不可用。</strong></p><h1 id="集群"><a href="#集群" class="headerlink" title="集群"></a>集群</h1><p><img data-src="/transblog/images/158591415270164.png"></p><ul><li><p>集群配置： 一个 ZooKeeper 集群通常由多个 ZooKeeper 服务器组成，这些服务器分布在不同的物理节点上。在集群中，每个服务器都知道其他服务器的存在，并且彼此协调工作以提供一致性和可用性。</p></li><li><p>Leader 选举： 在 ZooKeeper 集群中，每个服务器可能处于三种状态之一：Leader、Follower 和 Observer。Leader 负责处理客户端的写请求，并将更新广播给其他服务器；而 Follower 和 Observer 则负责接收更新并复制数据。当一个 ZooKeeper 服务器启动或者 Leader 失效时，集群中的服务器会通过一种选举算法选出新的 Leader，以确保系统的可用性。</p></li><li><p>数据同步： ZooKeeper 使用 ZAB（ZooKeeper Atomic Broadcast）协议来确保数据的一致性和可靠性。当 Leader 收到写请求时，它会将请求转发给所有的 Follower，并等待大多数 Follower 确认写操作，然后再将写操作应用到本地状态。这种方式确保了数据的一致性，并且即使在部分服务器故障的情况下，系统仍然可以继续工作。</p></li><li><p>客户端访问： 客户端可以通过连接到任意一个 ZooKeeper 服务器来访问集群，一旦连接建立成功，客户端就可以向任意一个服务器发送读写请求。如果客户端连接的是 Follower 或 Observer，那么它会被重定向到 Leader，并在 Leader 上执行操作。</p></li><li><p>Watch 机制： ZooKeeper 提供了 Watch 机制，允许客户端在节点状态发生变化时接收通知。客户端可以在节点上设置 Watch，当节点的状态发生变化时，ZooKeeper 会向客户端发送通知，客户端可以据此执行相应的逻辑。</p></li></ul><h2 id="ZAB"><a href="#ZAB" class="headerlink" title="ZAB"></a>ZAB</h2><p>Zookeeper 原子广播协议；ZAB 协议是为分布式协调服务 Zookeeper 专门设计的一种支持崩溃恢复和原子广播协议。</p><p>在以下情况下，ZooKeeper 需要进行选举：</p><ol><li>Leader 故障： 当前的 Leader 出现故障或不可用时。</li><li>新节点加入： 当新的 ZooKeeper 服务器加入集群时，新节点会参与选举，并且如果选举成功，它可能成为新的 Leader。</li><li>集群初始化： 当第一个 ZooKeeper 服务器启动时，它会尝试成为 Leader。如果此时没有其他节点加入集群，那么它就会成为唯一的 Leader。</li></ol><ul><li><p>选举权重：<br>Serverid：服务器ID。比如有三台服务器，编号分别是1,2,3。编号越大在选择算法中的权重越大。<br>Zxid：数据ID。服务器中存放的最大数据ID，值越大说明数据越新，在选举算法中数据越新权重越大。</p></li><li><p>节点角色：<br>Leader：Leader 负责处理客户端请求和状态变更，<br>Follower：Follower 则从 Leader 处同步状态</p></li><li><p>节点状态：<br>LOOKING，竞选状态。<br>FOLLOWING，随从状态，同步leader状态，参与投票。<br>OBSERVING，观察状态，同步leader状态，不参与投票。<br>LEADING，领导者状态。</p></li><li><p>选举过程：</p></li></ul><ol><li>当一个节点启动时，它首先会进入 LOOKING 状态，表示它正在寻找 Leader。</li><li>在 LOOKING 状态下，节点会向其他节点发送消息，请求它们投票支持自己成为 Leader。</li><li>如果收到了超过半数的选票，那么该节点就会成为 Leader，并将状态切换为 LEADING。</li><li>如果一个节点在一定时间内没有收到足够多的投票或者发生了网络分区等情况，它会重新进入 LOOKING 状态，重新发起选举。</li><li>在选举过程中，如果某个节点成为 Leader，那么其他节点会将自己的状态切换为 FOLLOWING，并开始与 Leader 同步数据。</li></ol><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;功能&quot;&gt;&lt;a href=&quot;#功能&quot; class=&quot;headerlink&quot; title=&quot;功能&quot;&gt;&lt;/a&gt;功能&lt;/h1&gt;&lt;p&gt;Zookeeper 提供的主要功能包括：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;配置管理&lt;/li&gt;
&lt;li&gt;分布式锁&lt;/li&gt;
&lt;li&gt;集群管理&lt;/li</summary>
      
    
    
    
    <category term="中间件" scheme="https://trans-xu.github.io/transblog/categories/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
    
  </entry>
  
  <entry>
    <title>Top-K问题</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/Top-K/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/Top-K/</id>
    <published>2025-08-27T03:50:08.896Z</published>
    <updated>2025-08-27T03:35:55.416Z</updated>
    
    <content type="html"><![CDATA[<h1 id="有1亿个浮点数，如果找出其中最大的10000个？"><a href="#有1亿个浮点数，如果找出其中最大的10000个？" class="headerlink" title="有1亿个浮点数，如果找出其中最大的10000个？"></a>有1亿个浮点数，如果找出其中最大的10000个？</h1><ul><li>局部淘汰法<br>用一个容器保存前10000个数，然后将剩余的所有数字——与容器内的最小数字相比，如果所有后续的元素都比容器内的10000个数还小，那么容器内这个10000个数就是最大10000个数。<br>如果某一后续元素比容器内最小数字大，则删掉容器内最小元素，并将该元素插入容器，最后遍历完这1亿个数，得到的结果容器中保存的数即为最终结果了。此时的时间复杂度为O（n+m^2），其中m为容器的大小，即10000。</li><li>分治法<br>将1亿个数据分成100份，每份100万个数据，找到每份数据中最大的10000个，最后在剩下的100 * 10000个数据里面找出最大的10000个。如果100万数据选择足够理想，那么可以过滤掉1亿数据里面99%的数据。<br>100万个数据里面查找最大的10000个数据的方法如下：用快速排序的方法，将数据分为2堆，如果大的那堆个数N大于10000个，继续对大堆快速排序一次分成2堆，如果大堆个数N小于10000个，就在小的那堆里面快速排序一次，找第10000-n大的数字；递归以上过程，就可以找到第1w大的数。参考上面的找出第1w大数字，就可以类似的方法找到前10000大数字了。此种方法需要每次的内存空间为10^6*4&#x3D;4MB，一共需要101次这样的比较。</li><li>Hash法<br>如果这1亿个数里面有很多重复的数，先通过Hash法，把这1亿个数字去重复，这样如果重复率很高的话，会减少很大的内存用量，从而缩小运算空间，然后通过分治法或最小堆法查找最大的10000个数。</li><li>采用最小堆<br>首先读入前10000个数来创建大小为10000的最小堆，建堆的时间复杂度为O（mlogm）（m为数组的大小即为10000），然后遍历后续的数字，并于堆顶（最小）数字进行比较。如果比最小的数小，则继续读取后续数字；如果比堆顶数字大，则替换堆顶元素并重新调整堆为最小堆。整个过程直至1亿个数全部遍历完为止。然后按照中序遍历的方式输出当前堆中的所有10000个数字。该算法的时间复杂度为O（nmlogm），空间复杂度是10000（常数）。</li></ul><h1 id="大文件找次数出现最多的100个词"><a href="#大文件找次数出现最多的100个词" class="headerlink" title="大文件找次数出现最多的100个词"></a>大文件找次数出现最多的100个词</h1><ol><li>先hash映射把大文件分成很多个小文件，具体操作如下：读文件中，对于每个词x，取hash(x)%5000，然后按照该值存到5000个小文件(记为f0,f1,…,f4999)中，这样每个文件大概是200k左右（每个相同的词一定被映射到了同一文件中）</li><li>对于每个文件fi，都用hash_map做词和出现频率的统计，取出频率大的前100个词（怎么取？topK问题，建立一个100个节点的最小堆），把这100个词和出现频率再单独存入一个文件</li><li>根据上述处理，我们又得到了5000个文件，归并文件取出top100（Top K 问题，比较最大的前100个频数）</li></ol><h1 id="———-请稍等————–"><a href="#———-请稍等————–" class="headerlink" title="———-请稍等————–"></a>———-请稍等————–</h1><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;有1亿个浮点数，如果找出其中最大的10000个？&quot;&gt;&lt;a href=&quot;#有1亿个浮点数，如果找出其中最大的10000个？&quot; class=&quot;headerlink&quot; title=&quot;有1亿个浮点数，如果找出其中最大的10000个？&quot;&gt;&lt;/a&gt;有1亿个浮点数，如果找出其中</summary>
      
    
    
    
    <category term="其它" scheme="https://trans-xu.github.io/transblog/categories/%E5%85%B6%E5%AE%83/"/>
    
    
  </entry>
  
  <entry>
    <title>Redis</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/Redis/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/Redis/</id>
    <published>2025-08-27T03:50:08.892Z</published>
    <updated>2025-08-27T03:35:36.559Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Redis数据类型和数据结构"><a href="#Redis数据类型和数据结构" class="headerlink" title="Redis数据类型和数据结构"></a>Redis数据类型和数据结构</h1><h2 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h2><p>常见的有五种：String（字符串），Hash（哈希），List（列表），Set（集合）、Zset（有序集合）。<br>随着 Redis 版本的更新，后面又支持了四种数据类型： BitMap（2.2 版新增）、HyperLogLog（2.8 版新增）、GEO（3.2 版新增）、Stream（5.0 版新增）。</p><h3 id="String类型"><a href="#String类型" class="headerlink" title="String类型"></a><span id="target1">String类型</span></h3><p>是redis中最基本的数据类型，一个key对应一个value。内部由<strong>SDS数据结构</strong>实现</p><p><img data-src="/transblog/images/542052117263693.png"></p><ul><li>如果一个字符串对象保存的是整数值，并且这个整数值可以用long类型来表示，那么字符串对象会将整数值保存在字符串对象结构的ptr属性里面（将void*转换成 long），并将字符串对象的编码设置为int</li><li>如果字符串对象保存的是一个字符串，并且这个字符申的长度小于等于 32 字节（redis 2.+版本），那么字符串对象将使用一个简单动态字符串（SDS）来保存这个字符串，并将对象的编码设置为embstr， embstr编码是专门用于保存短字符串的一种优化编码方式</li><li>如果字符串对象保存的是一个字符串，并且这个字符串的长度大于 32 字节（redis 2.+版本），那么字符串对象将使用一个简单动态字符串（SDS）来保存这个字符串，并将对象的编码设置为raw</li></ul><p><strong>embstr优点：</strong><br>embstr会通过一次内存分配函数来分配一块连续的内存空间来保存redisObject和SDS，raw编码会通过调用两次内存分配函数来分别分配两块空间来保存redisObject和SDS<br>因为embstr编码的字符串对象的所有数据都保存在一块连续的内存里面可以更好的利用 CPU 缓存提升性能。</p><p><strong>embstr缺点：</strong><br>embstr编码的字符串对象实际上是只读的，redis没有为embstr编码的字符串对象编写任何相应的修改程序。当我们对embstr编码的字符串对象执行任何修改命令（例如append）时，程序会先将对象的编码从embstr转换成raw，然后再执行修改命令。<br>如果字符串的长度增加需要重新分配内存时，整个redisObject和sds都需要重新分配空间，</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; <span class="built_in">set</span> hello world</span><br><span class="line">OK</span><br><span class="line">127.0.0.1:6379&gt; get hello</span><br><span class="line"><span class="string">&quot;world&quot;</span></span><br></pre></td></tr></table></figure><p><strong>使用场景：</strong></p><ul><li>缓存对象</li><li>常规计数</li><li>分布式锁</li></ul><h3 id="List类型"><a href="#List类型" class="headerlink" title="List类型"></a><span id="target2">List类型</span></h3><p>List 类型的底层数据结构是由<strong>双向链表或压缩列表</strong>实现的</p><ul><li>如果列表的元素个数小于 512 个（默认值，可由 list-max-ziplist-entries 配置），列表每个元素的值都小于 64 字节（默认值，可由 list-max-ziplist-value 配置），Redis 会使用压缩列表作为 List 类型的底层数据结构；</li><li>如果列表的元素不满足上面的条件，Redis 会使用双向链表作为 List 类型的底层数据结构；</li><li>在 Redis 3.2 版本之后，List 数据类型底层数据结构就只由 quicklist 实现了，替代了双向链表和压缩列表。</li></ul><p>使用列表的技巧:</p><p><img data-src="/transblog/images/87172014255125.png"></p><ul><li>lpush+lpop&#x3D;Stack(栈)</li><li>lpush+rpop&#x3D;Queue（队列）</li><li>lpush+ltrim&#x3D;Capped Collection（有限集合）</li><li>lpush+brpop&#x3D;Message Queue（消息队列）<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; lpush mylist 1 2 ll <span class="built_in">ls</span> mem</span><br><span class="line">(<span class="built_in">integer</span>) 5</span><br><span class="line">127.0.0.1:6379&gt; lrange mylist 0 -1</span><br><span class="line">1) <span class="string">&quot;mem&quot;</span></span><br><span class="line">2) <span class="string">&quot;ls&quot;</span></span><br><span class="line">3) <span class="string">&quot;ll&quot;</span></span><br><span class="line">4) <span class="string">&quot;2&quot;</span></span><br><span class="line">5) <span class="string">&quot;1&quot;</span></span><br><span class="line">127.0.0.1:6379&gt;</span><br></pre></td></tr></table></figure></li></ul><p><strong>应用场景：</strong><br>消息队列</p><ul><li>消息保序：使用 LPUSH + RPOP；</li><li>阻塞读取：使用 BRPOP；</li><li>重复消息处理：生产者自行实现全局唯一 ID；</li><li>消息的可靠性：使用 BRPOPLPUSH</li></ul><h3 id="Hash类型"><a href="#Hash类型" class="headerlink" title="Hash类型"></a><span id="target3">Hash类型</span></h3><p>是一个Mapmap，指值本身又是一种键值对结构，Hash 类型的底层数据结构是由<strong>压缩列表或哈希表</strong>实现的：</p><ul><li>如果哈希类型元素个数小于 512 个（默认值，可由 hash-max-ziplist-entries 配置），所有值小于 64 字节（默认值，可由 hash-max-ziplist-value 配置）的话，Redis 会使用压缩列表作为 Hash 类型的底层数据结构；</li><li>如果哈希类型元素不满足上面条件，Redis 会使用哈希表作为 Hash 类型的 底层数据结构。</li><li>在 Redis 7.0 中，压缩列表数据结构已经废弃了，交由 listpack 数据结构来实现了。</li></ul><p><img data-src="/transblog/images/313723511267020.png"></p><p>使用：所有hash的命令都是  h   开头的     hget  、hset 、  hdel 等</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; hset user name1 hao</span><br><span class="line">(<span class="built_in">integer</span>) 1</span><br><span class="line">127.0.0.1:6379&gt; hset user email1 hao@163.com</span><br><span class="line">(<span class="built_in">integer</span>) 1</span><br><span class="line">127.0.0.1:6379&gt; hgetall user</span><br><span class="line">1) <span class="string">&quot;name1&quot;</span></span><br><span class="line">2) <span class="string">&quot;hao&quot;</span></span><br><span class="line">3) <span class="string">&quot;email1&quot;</span></span><br><span class="line">4) <span class="string">&quot;hao@163.com&quot;</span></span><br></pre></td></tr></table></figure><p><strong>应用场景</strong></p><ul><li>缓存对象</li><li>购物车</li></ul><h3 id="Set类型"><a href="#Set类型" class="headerlink" title="Set类型"></a><span id="target5">Set类型</span></h3><p>Set 类型的底层数据结构是由<strong>哈希表</strong>或<strong>整数集合</strong>实现的：</p><ul><li>如果集合中的元素都是整数且元素个数小于 512 （默认值，set-maxintset-entries配置）个，Redis 会使用整数集合作为 Set 类型的底层数据结构；</li><li>如果集合中的元素不满足上面条件，则 Redis 使用哈希表作为 Set 类型的底层数据结构。</li></ul><p>使用：命令都是以s开头的  sset 、srem、scard、smembers、sismember</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; sadd myset hao hao1 xiaohao hao</span><br><span class="line">(<span class="built_in">integer</span>) 3</span><br><span class="line">127.0.0.1:6379&gt; SMEMBERS myset</span><br><span class="line">1) <span class="string">&quot;xiaohao&quot;</span></span><br><span class="line">2) <span class="string">&quot;hao1&quot;</span></span><br><span class="line">3) <span class="string">&quot;hao&quot;</span></span><br><span class="line">127.0.0.1:6379&gt; SISMEMBER myset hao</span><br><span class="line">(<span class="built_in">integer</span>) 1</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/208841010257242.png"></p><p>Set 类型和 List 类型的区别如下：</p><p>List 可以存储重复元素，Set 只能存储非重复元素；<br>List 是按照元素的先后顺序存储元素的，而 Set 则是无序方式存储元素的。<br>集合中的元素是无序的，不能通过索引下标获取元素</p><p><strong>Set 的差集、并集和交集的计算复杂度较高，在数据量较大的情况下，如果直接执行这些计算，会导致 Redis 实例阻塞</strong></p><p>应用场景：<br>点赞<br>共同关注<br>抽奖活动</p><h3 id="ZSet类型"><a href="#ZSet类型" class="headerlink" title="ZSet类型"></a><span id="target4">ZSet类型</span></h3><p>Zset 类型的底层数据结构是由<strong>压缩列表或跳表</strong>实现的：</p><ul><li>如果有序集合的元素个数小于 128 个，并且每个元素的值小于 64 字节时，Redis 会使用压缩列表作为 Zset 类型的底层数据结构；</li><li>如果有序集合的元素不满足上面的条件，Redis 会使用跳表作为 Zset 类型的底层数据结构；</li><li>在 Redis 7.0 中，压缩列表数据结构已经废弃了，交由 listpack 数据结构来实现了。</li></ul><p>对于有序集合 ZSet 来说，每个存储元素相当于有两个值组成的，一个是有序集合的元素值，一个是排序值。<br>有序集合和集合有着必然的联系，保留了集合不能有重复成员的特性，区别是，有序集合中的元素是可以排序的，它给每个元素设置一个分数，作为排序的依据。</p><p><img data-src="/transblog/images/432203811259689.png"></p><p>使用： 有序集合的命令都是 以  z  开头    zadd 、 zrange、 zscore</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; zadd myscoreset 100 hao 90 xiaohao</span><br><span class="line">(<span class="built_in">integer</span>) 2</span><br><span class="line">127.0.0.1:6379&gt; ZRANGE myscoreset 0 -1</span><br><span class="line">1) <span class="string">&quot;xiaohao&quot;</span></span><br><span class="line">2) <span class="string">&quot;hao&quot;</span></span><br><span class="line">127.0.0.1:6379&gt; ZSCORE myscoreset hao</span><br><span class="line"><span class="string">&quot;100&quot;</span></span><br></pre></td></tr></table></figure><h3 id="BitMap类型"><a href="#BitMap类型" class="headerlink" title="BitMap类型"></a>BitMap类型</h3><p>Bitmap，即位图，是一串连续的二进制数组（0和1），可以通过偏移量（offset）定位元素。BitMap通过最小的单位bit来进行0|1的设置，表示某个元素的值或者状态，时间复杂度为O(1)。</p><p>由于 bit 是计算机中最小的单位，使用它进行储存将非常节省空间，特别<strong>适合一些数据量大且使用二值统计的场景。</strong></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置值，其中value只能是 0 和 1</span></span><br><span class="line">SETBIT key offset value</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">获取值</span></span><br><span class="line">GETBIT key offset</span><br></pre></td></tr></table></figure><p>内部实现:<br>Bitmap 本身是用 String 类型作为底层数据结构实现的一种统计二值状态的数据类型。<br>String 类型是会保存为二进制的字节数组，所以，Redis 就把字节数组的每个 bit 位利用起来，用来表示一个元素的二值状态，你可以把 Bitmap 看作是一个 bit 数组</p><p><strong>应用场景</strong><br>签到统计<br>判断用户登陆态<br>连续签到用户总数</p><h3 id="HyperLogLog类型"><a href="#HyperLogLog类型" class="headerlink" title="HyperLogLog类型"></a>HyperLogLog类型</h3><p>HyperLogLog 提供不精确的去重计数, 是统计规则是基于概率完成的，不是非常准确，标准误算率是 0.81%。<br>HyperLogLog 的优点是，在输入元素的数量或者体积非常非常大时，计算基数所需的内存空间总是固定的、并且是很小的。<br>每个 HyperLogLog 键<strong>只需要花费 12 KB 内存，就可以计算接近 2^64 个不同元素的基数</strong>，和元素越多就越耗费内存的 Set 和 Hash 类型相比，HyperLogLog 就非常节省空间:<br><a href="https://en.wikipedia.org/wiki/HyperLogLog">HyperLogLog数学原理</a></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">添加指定元素到 HyperLogLog 中</span></span><br><span class="line">PFADD key element [element ...]</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">返回给定 HyperLogLog 的基数估算值。</span></span><br><span class="line">PFCOUNT key [key ...]</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将多个 HyperLogLog 合并为一个 HyperLogLog</span></span><br><span class="line">PFMERGE destkey sourcekey [sourcekey ...]</span><br></pre></td></tr></table></figure><p><strong>应用场景</strong><br>百万级网页 UV 计数</p><h3 id="GEO类型"><a href="#GEO类型" class="headerlink" title="GEO类型"></a>GEO类型</h3><p><strong>内部实现</strong><br><strong>GEO 本身并没有设计新的底层数据结构，而是直接使用了 Sorted Set 集合类型。</strong></p><p>GEO 类型使用 GeoHash 编码方法实现了经纬度到 Sorted Set 中元素权重分数的转换，这其中的两个关键机制就是「对二维地图做区间划分」和「对区间进行编码」。一组经纬度落在某个区间后，就用区间的编码值来表示，并把编码值作为 Sorted Set 元素的权重分数。</p><p>这样一来，我们就可以把经纬度保存到 Sorted Set 中，利用 Sorted Set 提供的“按权重进行有序范围查找”的特性，实现 LBS 服务中频繁使用的“搜索附近”的需求。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">存储指定的地理空间位置，可以将一个或多个经度(longitude)、纬度(latitude)、位置名称(member)添加到指定的 key 中。</span></span><br><span class="line">GEOADD key longitude latitude member [longitude latitude member ...]</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">从给定的 key 里返回所有指定名称(member)的位置（经度和纬度），不存在的返回 nil。</span></span><br><span class="line">GEOPOS key member [member ...]</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">返回两个给定位置之间的距离。</span></span><br><span class="line">GEODIST key member1 member2 [m|km|ft|mi]</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">根据用户给定的经纬度坐标来获取指定范围内的地理位置集合。</span></span><br><span class="line">GEORADIUS key longitude latitude radius m|km|ft|mi [WITHCOORD] [WITHDIST] [WITHHASH] [COUNT count] [ASC|DESC] [STORE key] [STOREDIST key]</span><br></pre></td></tr></table></figure><p><strong>应用场景</strong><br>滴滴叫车<br>这里以滴滴叫车的场景为例，介绍下具体如何使用 GEO 命令：GEOADD 和 GEORADIUS 这两个命令。</p><p>假设车辆 ID 是 33，经纬度位置是（116.034579，39.030452），我们可以用一个 GEO 集合保存所有车辆的经纬度，集合 key 是 cars:locations。</p><p>执行下面的这个命令，就可以把 ID 号为 33 的车辆的当前经纬度位置存入 GEO 集合中：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GEOADD cars:locations 116.034579 39.030452 33</span><br></pre></td></tr></table></figure><p>当用户想要寻找自己附近的网约车时，LBS 应用就可以使用 GEORADIUS 命令。</p><p>例如，LBS 应用执行下面的命令时，Redis 会根据输入的用户的经纬度信息（116.054579，39.030452 ），查找以这个经纬度为中心的 5 公里内的车辆信息，并返回给 LBS 应用。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RADIUS cars:locations 116.054579 39.030452 5 km ASC COUNT 10</span><br></pre></td></tr></table></figure><h3 id="Stream类型"><a href="#Stream类型" class="headerlink" title="Stream类型"></a>Stream类型</h3><p>Redis Stream 是 Redis 5.0 版本新增加的数据类型，Redis 专门为消息队列设计的数据类型。它支持消息的持久化、支持自动生成全局唯一 ID、支持 ack 确认消息的模式、支持消费组模式等，让消息队列更加的稳定和可靠。</p><p>在 Redis 5.0 Stream 没出来之前，消息队列的实现方式都有着各自的缺陷，例如：</p><p>发布订阅模式，不能持久化也就无法可靠的保存消息，并且对于离线重连的客户端不能读取历史消息的缺陷；<br>List 实现消息队列的方式不能重复消费，一个消息消费完就会被删除，而且生产者需要自行实现全局唯一 ID。</p><p><strong>常见命令</strong><br>Stream 消息队列操作命令：</p><p>XADD：插入消息，保证有序，可以自动生成全局唯一 ID；<br>XLEN ：查询消息长度；<br>XREAD：用于读取消息，可以按 ID 读取数据；<br>XDEL ： 根据消息 ID 删除消息；<br>DEL ：删除整个 Stream；<br>XRANGE ：读取区间消息<br>XREADGROUP：按消费组形式读取消息；<br>XPENDING 和 XACK：<br>XPENDING 命令可以用来查询每个消费组内所有消费者「已读取、但尚未确认」的消息；<br>XACK 命令用于向消息队列确认消息处理已完成；</p><p><strong>应用场景</strong></p><h4 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">* 表示让 Redis 为插入的数据自动生成一个全局唯一的 ID</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">往名称为 mymq 的消息队列中插入一条消息，消息的键是 name，值是 xiaolin</span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XADD mymq * name xiaolin</span></span><br><span class="line">&quot;1654254953808-0&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">从 ID 号为 1654254953807-0 的消息开始，读取后续的所有消息（示例中一共 1 条）。</span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XREAD STREAMS mymq 1654254953807-0</span></span><br><span class="line">1) 1) &quot;mymq&quot;</span><br><span class="line">   2) 1) 1) &quot;1654254953808-0&quot;</span><br><span class="line">         2) 1) &quot;name&quot;</span><br><span class="line">            2) &quot;xiaolin&quot;</span><br><span class="line">            </span><br></pre></td></tr></table></figure><p>如果想要实现阻塞读（当没有数据时，阻塞住），可以调用 XRAED 时设定 BLOCK 配置项，实现类似于 BRPOP 的阻塞读取操作。</p><p><img data-src="/transblog/images/332081109240466.png"></p><p>Stream 可以以使用 XGROUP 创建消费组，创建消费组之后，Stream 可以使用 XREADGROUP 命令让消费组内的消费者读取消息。</p><p>创建两个消费组，这两个消费组消费的消息队列是 mymq，都指定从第一条消息开始读取：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">创建一个名为 group1 的消费组，0-0 表示从第一条消息开始读取。</span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XGROUP CREATE mymq group1 0-0</span></span><br><span class="line">OK</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">创建一个名为 group2 的消费组，0-0 表示从第一条消息开始读取。</span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XGROUP CREATE mymq group2 0-0</span></span><br><span class="line">OK</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>消费组 group1 内的消费者 consumer1 从 mymq 消息队列中读取所有消息的命令如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">命令最后的参数“&gt;”，表示从第一条尚未被消费的消息开始读取。</span></span><br><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XREADGROUP GROUP group1 consumer1 STREAMS mymq &gt;</span></span><br><span class="line">1) 1) &quot;mymq&quot;</span><br><span class="line">   2) 1) 1) &quot;1654254953808-0&quot;</span><br><span class="line">         2) 1) &quot;name&quot;</span><br><span class="line">            2) &quot;xiaolin&quot;</span><br></pre></td></tr></table></figure><p>消息队列中的消息一旦被消费组里的一个消费者读取了，就不能再被该消费组内的其他消费者读取了，即同一个消费组里的消费者不能消费同一条消息。</p><p>比如说，我们执行完刚才的 XREADGROUP 命令后，再执行一次同样的命令，此时读到的就是空值了：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&gt; XREADGROUP GROUP group1 consumer1 STREAMS mymq &gt;</span><br><span class="line">(nil)</span><br></pre></td></tr></table></figure><p>但是，不同消费组的消费者可以消费同一条消息（但是有前提条件，创建消息组的时候，不同消费组指定了相同位置开始读取消息）。<br>比如说，刚才 group1 消费组里的 consumer1 消费者消费了一条 id 为 1654254953808-0 的消息，现在用 group2 消费组里的 consumer1 消费者消费消息：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XREADGROUP GROUP group2 consumer1 STREAMS mymq &gt;</span></span><br><span class="line">1) 1) &quot;mymq&quot;</span><br><span class="line">   2) 1) 1) &quot;1654254953808-0&quot;</span><br><span class="line">         2) 1) &quot;name&quot;</span><br><span class="line">            2) &quot;xiaolin&quot;</span><br></pre></td></tr></table></figure><p>因为我创建两组的消费组都是从第一条消息开始读取，所以可以看到第二组的消费者依然可以消费 id 为 1654254953808-0 的这一条消息。因此，不同的消费组的消费者可以消费同一条消息。</p><h5 id="基于-Stream-实现的消息队列，如何保证消费者在发生故障或宕机再次重启后，仍然可以读取未处理完的消息？"><a href="#基于-Stream-实现的消息队列，如何保证消费者在发生故障或宕机再次重启后，仍然可以读取未处理完的消息？" class="headerlink" title="基于 Stream 实现的消息队列，如何保证消费者在发生故障或宕机再次重启后，仍然可以读取未处理完的消息？"></a>基于 Stream 实现的消息队列，如何保证消费者在发生故障或宕机再次重启后，仍然可以读取未处理完的消息？</h5><p>Streams 会自动使用内部队列（也称为 PENDING List）留存消费组里每个消费者读取的消息，直到消费者使用 XACK 命令通知 Streams“消息已经处理完成”。</p><p>消费确认增加了消息的可靠性，一般在业务处理完成之后，需要执行 XACK 命令确认消息已经被消费完成，整个流程的执行如下图所示：</p><p><img data-src="/transblog/images/5962009258892.png"></p><p>如果消费者没有成功处理消息，它就不会给 Streams 发送 XACK 命令，消息仍然会留存。此时，消费者可以在重启后，用 XPENDING 命令查看已读取、但尚未确认处理完成的消息。</p><p>一旦消息 1654256265584-0 被 consumer2 处理了，consumer2 就可以使用 XACK 命令通知 Streams，然后这条消息就会被删除。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XACK mymq group2 1654256265584-0</span></span><br><span class="line">(integer) 1</span><br></pre></td></tr></table></figure><p>当我们再使用 XPENDING 命令查看时，就可以看到，consumer2 已经没有已读取、但尚未确认处理的消息了。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">&gt; </span><span class="language-bash">XPENDING mymq group2 - + 10 consumer2</span></span><br><span class="line">(empty array)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>总结：</p><ul><li>消息保序：XADD&#x2F;XREAD</li><li>阻塞读取：XREAD block</li><li>重复消息处理：Stream 在使用 XADD 命令，会自动生成全局唯一 ID；</li><li>消息可靠性：内部使用 PENDING List 自动保存消息，使用 XPENDING 命令查看消费组已经读取但是未被确认的消息，消费者使用 XACK 确认消息；</li><li>支持消费组形式消费数据</li></ul><h5 id="Redis-基于-Stream-消息队列与专业的消息队列有哪些差距？"><a href="#Redis-基于-Stream-消息队列与专业的消息队列有哪些差距？" class="headerlink" title="Redis 基于 Stream 消息队列与专业的消息队列有哪些差距？"></a>Redis 基于 Stream 消息队列与专业的消息队列有哪些差距？</h5><ul><li>消息不丢?</li></ul><p>生产者会不会丢消息，消费者不会丢消息,,Redis 消息中间件会丢消息, Redis 在以下 2 个场景下，都会导致数据丢失：</p><ol><li>AOF 持久化配置为每秒写盘，但这个写盘过程是异步的，Redis 宕机时会存在数据丢失的可能</li><li>主从复制也是异步的，主从切换时，也存在丢失数据的可能 。</li></ol><ul><li>消息可堆积?<br>Redis 的数据都存储在内存中，这就意味着一旦发生消息积压，则会导致 Redis 的内存持续增长，如果超过机器内存上限，就会面临被 OOM 的风险。但 Kafka、RabbitMQ 专业的消息队列它们的数据都是存储在磁盘上</li></ul><p><strong>关键看业务场景：</strong><br><strong>如果你的业务场景足够简单，对于数据丢失不敏感，而且消息积压概率比较小的情况下，把 Redis 当作队列是完全可以的。</strong><br><strong>如果你的业务有海量消息，消息积压的概率比较大，并且不能接受数据丢失，那么还是用专业的消息队列中间件吧。</strong></p><h5 id="Redis-发布-订阅机制为什么不可以作为消息队列？"><a href="#Redis-发布-订阅机制为什么不可以作为消息队列？" class="headerlink" title="Redis 发布&#x2F;订阅机制为什么不可以作为消息队列？"></a>Redis 发布&#x2F;订阅机制为什么不可以作为消息队列？</h5><ol><li>发布&#x2F;订阅机制没有基于任何数据类型实现，所以不具备「数据持久化」的能力，也就是发布&#x2F;订阅机制的相关操作，不会写入到 RDB 和 AOF 中，当 Redis 宕机重启，发布&#x2F;订阅机制的数据也会全部丢失。</li><li>发布订阅模式是“发后既忘”的工作模式，如果有订阅者离线重连之后不能消费之前的历史消息。</li><li>当消费端有一定的消息积压时，也就是生产者发送的消息，消费者消费不过来时，如果超过 32M 或者是 60s 内持续保持在 8M 以上，消费端会被强行断开，这个参数是在配置文件中设置的，默认值是<code>client-output-buffer-limit pubsub 32mb 8mb 60</code></li></ol><p>数据结构实现</p><p><img data-src="/transblog/images/161783909246759.png"></p><ul><li>redisDb 结构，表示 Redis 数据库的结构，结构体里存放了指向了 dict 结构的指针；</li><li>dict 结构，结构体里存放了 2 个哈希表，正常情况下都是用「哈希表1」，「哈希表2」只有在 rehash 的时候才用，具体什么是 rehash，我在本文的哈希表数据结构会讲；</li><li>ditctht 结构，表示哈希表的结构，结构里存放了哈希表数组，数组中的每个元素都是指向一个哈希表节点结构（dictEntry）的指针；</li><li>dictEntry 结构，表示哈希表节点的结构，结构里存放了 **void * key 和 void * value 指针， key 指向的是 String 对象，而 value 则可以指向 String 对象，也可以指向集合类型的对象，比如 List 对象、Hash 对象、Set 对象和 Zset 对象。</li></ul><p><img data-src="/transblog/images/539934514259594.png"></p><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><h3 id="SDS–String类型"><a href="#SDS–String类型" class="headerlink" title="SDS–String类型"></a>SDS–String类型</h3><p><a href="#target1">点击此处跳转到String类型</a></p><h4 id="C-语言字符串的缺陷"><a href="#C-语言字符串的缺陷" class="headerlink" title="C 语言字符串的缺陷"></a>C 语言字符串的缺陷</h4><ol><li>字符数组的结尾位置就用“\0”表示，意思是指字符串的结束。</li><li>C 语言获取字符串长度的时间复杂度是 O（N）</li><li>不能保存像图片、音频、视频文化这样的二进制数据，有个 “\0” 字符，这时在操作这个字符串时就会提早结束</li><li>必须提前分配足够多的内存，可以容纳所有内容，否则就会发生缓冲区溢出将可能会造成程序运行终止。</li></ol><h4 id="SDS设计"><a href="#SDS设计" class="headerlink" title="SDS设计"></a>SDS设计</h4><p><img data-src="/transblog/images/171905210245187.png"></p><ol><li>O（1）复杂度获取字符串长度</li><li>二进制安全， SDS 的所有 API 都会以处理二进制的方式处理存放在 buf[] 数组里的数据。所以 SDS 不光能存放文本数据，而且能保存图片、音频、视频、压缩文件这样的二进制数据。</li><li>不会发生缓冲区溢出，当判断出缓冲区大小不够用时，Redis 会自动将扩大 SDS 的空间大小，如果所需的 sds 长度小于 1 MB，那么最后的扩容是按照翻倍扩容来执行的，即 2 倍的newlen，如果所需的 sds 长度超过 1 MB，那么最后的扩容长度应该是 newlen + 1MB。<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">hisds <span class="title">hi_sdsMakeRoomFor</span><span class="params">(hisds s, <span class="type">size_t</span> addlen)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ... ...</span><br><span class="line">    <span class="comment">// s目前的剩余空间已足够，无需扩展，直接返回</span></span><br><span class="line">    <span class="keyword">if</span> (avail &gt;= addlen)</span><br><span class="line">        <span class="keyword">return</span> s;</span><br><span class="line">    <span class="comment">//获取目前s的长度</span></span><br><span class="line">    len = <span class="built_in">hi_sdslen</span>(s);</span><br><span class="line">    sh = (<span class="type">char</span> *)s - <span class="built_in">hi_sdsHdrSize</span>(oldtype);</span><br><span class="line">    <span class="comment">//扩展之后 s 至少需要的长度</span></span><br><span class="line">    newlen = (len + addlen);</span><br><span class="line">    <span class="comment">//根据新长度，为s分配新空间所需要的大小</span></span><br><span class="line">    <span class="keyword">if</span> (newlen &lt; HI_SDS_MAX_PREALLOC)</span><br><span class="line">        <span class="comment">//新长度&lt;HI_SDS_MAX_PREALLOC 则分配所需空间*2的空间</span></span><br><span class="line">        newlen *= <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="comment">//否则，分配长度为目前长度+1MB</span></span><br><span class="line">        newlen += HI_SDS_MAX_PREALLOC;</span><br><span class="line">       ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>节省内存空间<br>flags 成员变量，表示的是 SDS 类型。设计了 5 种类型，分别是 sdshdr5、sdshdr8、sdshdr16、sdshdr32 和 sdshdr64。<br>比如 sdshdr16 和 sdshdr32 这两个类型，它们的定义分别如下：</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//数据结构中的 len 和 alloc 成员变量的数据类型不同。</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">__attribute__</span> ((__packed__)) sdshdr16 &#123;            <span class="comment">//表示字符数组长度和分配空间大小不能超过 2 的 16 次方。</span></span><br><span class="line">    <span class="type">uint16_t</span> len;                                </span><br><span class="line">    <span class="type">uint16_t</span> alloc; </span><br><span class="line">    <span class="type">unsigned</span> <span class="type">char</span> flags; </span><br><span class="line">    <span class="type">char</span> buf[];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">__attribute__</span> ((__packed__)) sdshdr32 &#123;     <span class="comment">//表示表示字符数组长度和分配空间大小不能超过 2 的 32 次方</span></span><br><span class="line">    <span class="type">uint32_t</span> len;</span><br><span class="line">    <span class="type">uint32_t</span> alloc; </span><br><span class="line">    <span class="type">unsigned</span> <span class="type">char</span> flags;</span><br><span class="line">    <span class="type">char</span> buf[];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>Redis在struct 声明了**<strong>attribute</strong> ((packed)) **，它的作用是：告诉编译器取消结构体在编译过程中的优化对齐，按照实际占用字节数进行对齐。</p><p><img data-src="/transblog/images/82071911255664.png"></p><p>Redis的对齐方式：</p><p><img data-src="/transblog/images/96622011248234.png"></p><h3 id="链表–List类型"><a href="#链表–List类型" class="headerlink" title="链表–List类型"></a>链表–List类型</h3><p><a href="#target2">点击此处跳转到List类型</a><br>C 语言本身没有链表这个数据结构的，所以 Redis 自己设计了一个链表数据结构。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">typedef struct listNode &#123;</span><br><span class="line">    //前置节点</span><br><span class="line">    struct listNode *prev;</span><br><span class="line">    //后置节点</span><br><span class="line">    struct listNode *next;</span><br><span class="line">    //节点的值</span><br><span class="line">    void *value;</span><br><span class="line">&#125; listNode;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">typedef struct list &#123;</span><br><span class="line">    //链表头节点</span><br><span class="line">    listNode *head;</span><br><span class="line">    //链表尾节点</span><br><span class="line">    listNode *tail;</span><br><span class="line">    //节点值复制函数</span><br><span class="line">    void *(*dup)(void *ptr);</span><br><span class="line">    //节点值释放函数</span><br><span class="line">    void (*free)(void *ptr);</span><br><span class="line">    //节点值比较函数</span><br><span class="line">    int (*match)(void *ptr, void *key);</span><br><span class="line">    //链表节点数量</span><br><span class="line">    unsigned long len;</span><br><span class="line">&#125; list;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/227012311268776.png"></p><h3 id="压缩列表–List类型-Hash类型-Zset类型"><a href="#压缩列表–List类型-Hash类型-Zset类型" class="headerlink" title="压缩列表–List类型&amp;Hash类型&amp;Zset类型"></a>压缩列表–List类型&amp;Hash类型&amp;Zset类型</h3><p><a href="#target2">点击此处跳转到List类型</a><br><a href="#target3">点击此处跳转到Hash类型</a><br><a href="#target4">点击此处跳转到Zset类型</a><br>压缩列表的最大特点，就是它被设计成一种内存紧凑型的数据结构，<strong>由连续内存块组成的顺序型数据结构，有点类似于数组</strong>。占用一块连续的内存空间，不仅可以利用 CPU 缓存，而且会针对不同长度的数据，进行相应编码，这种方法可以有效地节省内存开销。</p><p><img data-src="/transblog/images/437893411266070.png"></p><ul><li>zlbytes：记录整个压缩列表占用对内存字节数；</li><li>zltail：记录压缩列表「尾部」节点距离起始地址由多少字节，也就是列表尾的偏移量；</li><li>zllen：记录压缩列表包含的节点数量；</li><li>zlend：标记压缩列表的结束点，固定值 0xFF（十进制255）。</li><li>prevlen：记录了「前一个节点」的长度，目的是为了实现从后向前遍历；<br>如果前一个节点的长度小于 254 字节，那么 prevlen 属性需要用 1 字节的空间来保存这个长度值；<br>如果前一个节点的长度大于等于 254 字节，那么 prevlen 属性需要用 5 字节的空间来保存这个长度值；</li><li>encoding：记录了当前节点实际数据的「类型和长度」，类型主要有两种：字符串和整数。</li><li>data：记录了当前节点的实际数据，类型和长度都由 encoding 决定；</li></ul><p><strong>缺点：</strong></p><ul><li>查找定位第一个元素和最后一个元素较快，其它元素查找复杂度高</li><li>连锁更新：造成访问压缩列表性能的下降。</li></ul><p><strong>什么是连锁更新？</strong></p><p><img data-src="/transblog/images/513264611268167.png"></p><p>如果将一个长度大于等于 254 字节的新节点加入到压缩列表的表头节点，即新节点将成为 e1 的前置节点，因为 e1 节点的 prevlen 属性只有 1 个字节大小，无法保存新节点的长度，<strong>此时就需要对压缩列表的空间重分配操作，并将 e1 节点的 prevlen 属性从原来的 1 字节大小扩展为 5 字节大小。</strong></p><p><img data-src="/transblog/images/370004911273185.png"></p><h3 id="哈希表–Hash类型-Set类型"><a href="#哈希表–Hash类型-Set类型" class="headerlink" title="哈希表–Hash类型&amp;Set类型"></a>哈希表–Hash类型&amp;Set类型</h3><p><a href="#target3">点击此处跳转到Hash类型</a><br><a href="#target5">点击此处跳转到Set类型</a></p><p>哈希表的结构如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">dictht</span> &#123;</span><br><span class="line">    <span class="comment">//哈希表数组</span></span><br><span class="line">    dictEntry **table;</span><br><span class="line">    <span class="comment">//哈希表大小</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> size;  </span><br><span class="line">    <span class="comment">//哈希表大小掩码，用于计算索引值</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> sizemask;</span><br><span class="line">    <span class="comment">//该哈希表已有的节点数量</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> used;</span><br><span class="line">&#125; dictht;</span><br></pre></td></tr></table></figure><p>哈希表是一个数组（dictEntry **table），数组的每个元素是一个指向「哈希表节点（dictEntry）」的指针。</p><p><img data-src="/transblog/images/367405511273280.png"></p><p>哈希表节点的结构如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">dictEntry</span> &#123;</span><br><span class="line">    <span class="comment">//键值对中的键</span></span><br><span class="line">    <span class="type">void</span> *key;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">//键值对中的值</span></span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        <span class="type">void</span> *val;</span><br><span class="line">        <span class="type">uint64_t</span> u64;</span><br><span class="line">        <span class="type">int64_t</span> s64;</span><br><span class="line">        <span class="type">double</span> d;</span><br><span class="line">    &#125; v;</span><br><span class="line">    <span class="comment">//指向下一个哈希表节点，形成链表</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">dictEntry</span> *next;</span><br><span class="line">&#125; dictEntry;</span><br></pre></td></tr></table></figure><h4 id="链式哈希–Redis解决哈希冲突"><a href="#链式哈希–Redis解决哈希冲突" class="headerlink" title="链式哈希–Redis解决哈希冲突"></a>链式哈希–Redis解决哈希冲突</h4><p>可参考其它–如何解决哈希冲突篇</p><p>实现的方式就是每个哈希表节点都有一个 next 指针，用于指向下一个哈希表节点，因此多个哈希表节点可以用 next 指针构成一个单项链表，被分配到同一个哈希桶上的多个节点可以用这个单项链表连接起来，这样就解决了哈希冲突。</p><p><img data-src="/transblog/images/483485911260560.png"></p><h4 id="Rehash"><a href="#Rehash" class="headerlink" title="Rehash"></a>Rehash</h4><p>随着链表长度的增加，在查询这一位置上的数据的耗时就会增加，毕竟链表的查询的时间复杂度是 O(n)。要想解决这一问题，就需要进行 rehash，也就是对哈希表的大小进行扩展。Redis 定义一个 dict 结构体，这个结构体里定义了两个哈希表（ht[2]）。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">dict</span> &#123;</span><br><span class="line">    …</span><br><span class="line">    <span class="comment">//两个Hash表，交替使用，用于rehash操作</span></span><br><span class="line">    dictht ht[<span class="number">2</span>]; </span><br><span class="line">    …</span><br><span class="line">&#125; dict;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/180842211269882.png"></p><p>随着数据逐步增多，触发了 rehash 操作，这个过程分为三步</p><ul><li>给「哈希表 2」 分配空间，一般会比「哈希表 1」 大一倍（两倍的意思）；</li><li>将「哈希表 1 」的数据迁移到「哈希表 2」 中；</li><li>迁移完成后，「哈希表 1 」的空间会被释放，并把「哈希表 2」 设置为「哈希表 1」，然后在「哈希表 2」 新创建一个空白的哈希表，为下次 rehash 做准备。<br>如果「哈希表 1 」的数据量非常大，那么在迁移至「哈希表 2 」的时候，因为<strong>会涉及大量的数据拷贝，此时可能会对 Redis 造成阻塞，无法服务其他请求</strong>。</li></ul><h4 id="渐进式Rehash"><a href="#渐进式Rehash" class="headerlink" title="渐进式Rehash"></a>渐进式Rehash</h4><p>为了避免拷贝数据影响Redis性能，采用了渐进式 rehash，也就是将数据的迁移的工作不再是一次性迁移完成，而是分多次迁移。</p><ul><li>给「哈希表 2」 分配空间；</li><li>在 rehash 进行期间，<strong>每次哈希表元素进行新增、删除、查找或者更新操作时</strong>，Redis 除了会执行对应的操作之外，还会顺序将「哈希表 1 」中索引位置上的所有 key-value 迁移到「哈希表 2」 上；</li><li>随着处理客户端发起的哈希表操作请求数量越多，最终在某个时间点会把「哈希表 1 」的所有 key-value 迁移到「哈希表 2」，从而完成 rehash 操作。</li></ul><p>在渐进式 rehash 进行期间，新增一个 key-value 时，会被保存到「哈希表 2 」里面，而「哈希表 1」 则不再进行任何添加操作，这样保证了「哈希表 1 」的 key-value 数量只会减少，随着 rehash 操作的完成，最终「哈希表 1 」就会变成空表。</p><p><strong>触发条件</strong></p><p><img data-src="/transblog/images/278192115240667.png"></p><ol><li>当负载因子大于等于 1 ，并且 Redis 没有在执行 bgsave 命令或者 bgrewiteaof 命令，也就是没有执行 RDB 快照或没有进行 AOF 重写的时候，就会进行 rehash 操作。</li><li>当负载因子大于等于 5 时，此时说明哈希冲突非常严重了，不管有没有有在执行 RDB 快照或 AOF 重写，都会强制进行 rehash 操作。</li></ol><h3 id="跳表–Zset类型"><a href="#跳表–Zset类型" class="headerlink" title="跳表–Zset类型"></a>跳表–Zset类型</h3><p><a href="#target4">点击此处跳转到Zset类型</a></p><p>跳表是在链表基础上改进过来的，实现了一种<strong>多层的有序链表</strong>，这样的好处是能快读定位数据。当数据量很大时，跳表的查找复杂度就是 O(logN)。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">zskiplist</span> &#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> *header, *tail; <span class="comment">//头尾节点，便于在O(1)时间复杂度内访问跳表的头节点和尾节点；</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> length;                <span class="comment">//长度，便于在O(1)时间复杂度获取跳表节点的数量；</span></span><br><span class="line">    <span class="type">int</span> level;                           <span class="comment">//最大层数，便于在O(1)时间复杂度获取跳表中层高最大的那个节点的层数量；</span></span><br><span class="line">&#125; zskiplist;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> &#123;</span><br><span class="line">    <span class="comment">//Zset 对象的元素值</span></span><br><span class="line">    sds ele;</span><br><span class="line">    <span class="comment">//元素权重值</span></span><br><span class="line">    <span class="type">double</span> score;</span><br><span class="line">    <span class="comment">//后向指针</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> *backward;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">//节点的level数组，保存每层上的前向指针和跨度</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">zskiplistLevel</span> &#123;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">zskiplistNode</span> *forward;</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> span;</span><br><span class="line">    &#125; level[];</span><br><span class="line">&#125; zskiplistNode;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/516273415246960.png"></p><ul><li>每个跳表节点都有一个后向指针（*backward），指向前一个节点，目的是为了方便从跳表的尾节点开始访问节点，这样倒序查找时很方便。</li><li>跳表是一个带有层级关系的链表，而且每一层级可以包含多个节点，每一个节点通过指针连接起来，实现这一特性就是靠跳表节点结构体中的zskiplistLevel 结构体类型的 level 数组。level 数组中的每一个元素代表跳表的一层，也就是由 zskiplistLevel 结构体表示，</li></ul><h4 id="跳表查询"><a href="#跳表查询" class="headerlink" title="跳表查询"></a>跳表查询</h4><ul><li>从头节点的最高层开始，在遍历某一层的跳表节点时，会用跳表节点中的 SDS 类型的元素和元素的权重来进行判断</li><li>如果当前节点的权重「小于」要查找的权重时，跳表就会访问该层上的下一个节点。</li><li>如果当前节点的权重「等于」要查找的权重时，并且当前节点的 SDS 类型数据「小于」要查找的数据时，跳表就会访问该层上的下一个节点。</li><li>如果上面两个条件都不满足，或者下一个节点为空时，跳表就会使用目前遍历到的节点的 level 数组里的下一层指针，然后沿着下一层指针继续查找，这就相当于跳到了下一层接着查找。</li></ul><p><img data-src="/transblog/images/167815115256350.png"></p><p>如果要查找「元素：abcd，权重：4」的节点，查找的过程是这样的：</p><ol><li>先从头节点的最高层开始，L2 指向了「元素：abc，权重：3」节点，这个节点的权重比要查找节点的小，所以要访问该层上的下一个节点；</li><li>但是该层的下一个节点是空节点（ leve[2]指向的是空节点），于是就会跳到「元素：abc，权重：3」节点的下一层去找，也就是 leve[1];</li><li>「元素：abc，权重：3」节点的 leve[1] 的下一个指针指向了「元素：abcde，权重：4」的节点，然后将其和要查找的节点比较。虽然「元素：abcde，权重：4」的节点的权重和要查找的权重相同，但是当前节点的 SDS 类型数据「大于」要查找的数据，所以会继续跳到「元素：abc，权重：3」节点的下一层去找，也就是 leve[0]；</li><li>「元素：abc，权重：3」节点的 leve[0] 的下一个指针指向了「元素：abcd，权重：4」的节点，该节点正是要查找的节点，查询结束</li></ol><h4 id="确定跳表层数"><a href="#确定跳表层数" class="headerlink" title="确定跳表层数"></a>确定跳表层数</h4><p><strong>跳表的相邻两层的节点数量的比例会影响跳表的查询性能。跳表的相邻两层的节点数量最理想的比例是 2:1，查找复杂度可以降低到 O(logN)</strong></p><p>那怎样才能维持相邻两层的节点数量的比例为 2 : 1 呢？</p><ul><li>如果采用新增节点或者删除节点时，来调整跳表节点以维持比例的方法的话，会带来额外的开销。Redis跳表在创建节点的时候，随机生成每个节点的层数，并没有严格维持相邻两层的节点数量比例为 2 : 1 的情况。</li><li>具体的做法是，跳表在创建节点时候，会生成范围为[0-1]的一个随机数，如果这个随机数小于 0.25（相当于概率 25%），那么层数就增加 1 层，然后继续生成下一个随机数，直到随机数的结果大于 0.25 结束，最终确定该节点的层数。</li><li>这样的做法，相当于每增加一层的概率不超过 25%，层数越高，概率越低，层高限制 7.0 定义为 32，R 5.0 定义为 64， 3.0 定义为 32。</li></ul><p>那么为什么是小于0.25呢？</p><p>直接上干货：</p><ul><li>首先，每个节点肯定都有第1层指针（每个节点都在第1层链表里）。</li><li>如果一个节点有第i层(i&gt;&#x3D;1)指针（即节点已经在第1层到第i层链表中），那么它有第(i+1)层指针的概率为p。</li><li>节点最大的层数不允许超过一个最大值，记为MaxLevel（即层高限制）。</li></ul><p>产生越高的节点层数，概率越低,无论如何层数总是满足幂次定律（power law，越大的数出现的概率越小）。定量的分析如下：</p><p>• 节点层数至少为1。而大于1的节点层数，满足一个概率分布。<br>• 节点层数恰好等于1的概率为1-p。<br>• 节点层数大于等于2的概率为p，而节点层数恰好等于2的概率为p(1-p)。<br>……..</p><p>因此，一个节点的平均层数（也即包含的平均指针数目），计算如下：</p><p>$$<br>1 \times (1 - p) + 2p(1 - p) + 3p^2(1 - p) + 4p^3(1 - p) + … &#x3D; (1 - p) \sum_{k&#x3D;1}^{+\infty} kp^{k-1} &#x3D; (1 - p) \cdot \frac{1}{(1 - p)^2} &#x3D; \frac{1}{1 - p}<br>$$</p><p>当p&#x3D;1&#x2F;2时，每个节点所包含的平均指针数目为2；当p&#x3D;1&#x2F;4时，每个节点所包含的平均指针数目为1.33，你，听懂了吗？</p><h4 id="为什么不用平衡树"><a href="#为什么不用平衡树" class="headerlink" title="为什么不用平衡树"></a>为什么不用平衡树</h4><p>为什么 Zset 的实现用跳表而不用平衡树（如 AVL树、红黑树等）？</p><p>@antirez的回答：</p><blockquote><p>There are a few reasons:<br>They are not very memory intensive. It’s up to you basically. Changing parameters about the probability of a node to have a given number of levels will make then less memory intensive than btrees.<br>A sorted set is often target of many ZRANGE or ZREVRANGE operations, that is, traversing the skip list as a linked list. With this operation the cache locality of skip lists is at least as good as with other kind of balanced trees.<br>They are simpler to implement, debug, and so forth. For instance thanks to the skip list simplicity I received a patch (already in Redis master) with augmented skip lists implementing ZRANK in O(log(N)). It required little changes to the code.</p></blockquote><ul><li>平衡树不是非常内存密集型的。改变关于节点具有给定级别数的概率的参数将使其比 btree 占用更少的内存。</li><li>Zset 经常需要执行 ZRANGE 或 ZREVRANGE 的命令，即作为链表遍历跳表。通过此操作，跳表的缓存局部性至少与其他类型的平衡树一样好。(在做范围查找的时候，跳表比平衡树操作要简单)</li><li>它们更易于实现、调试等。例如，由于跳表的简单性，我收到了一个补丁（已经在Redis master中），其中扩展了跳表，在 O(log(N) 中实现了 ZRANK。它只需要对代码进行少量修改。</li></ul><h3 id="整数集合"><a href="#整数集合" class="headerlink" title="整数集合"></a>整数集合</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">intset</span> &#123;</span><br><span class="line">    <span class="comment">//编码方式</span></span><br><span class="line">    <span class="type">uint32_t</span> encoding;</span><br><span class="line">    <span class="comment">//集合包含的元素数量</span></span><br><span class="line">    <span class="type">uint32_t</span> length;</span><br><span class="line">    <span class="comment">//保存元素的数组</span></span><br><span class="line">    <span class="type">int8_t</span> contents[];</span><br><span class="line">&#125; intset;</span><br></pre></td></tr></table></figure><p><strong>整数集合的升级操作</strong></p><p>如果 encoding 属性值为 INTSET_ENC_INT16、INTSET_ENC_INT32、INTSET_ENC_INT64，那么 contents 就是一个 int16_t、int32_t、int64_t类型的数组，数组中每一个元素的类型都是 int16_t、int32_t、int64_t；</p><p>往这个整数集合中加入一个新元素 65535，这个新元素需要用 int32_t 类型来保存，所以整数集合要进行升级操作，首先需要为 contents 数组扩容，在原本空间的大小之上再扩容多 80 位（4x32-3x16&#x3D;80），这样就能保存下 4 个类型为 int32_t 的元素。</p><p><img data-src="/transblog/images/524120417269984.png"></p><p>扩容完 contents 数组空间大小后，需要将之前的三个元素转换为 int32_t 类型，并将转换后的元素放置到正确的位上面，并且需要维持底层数组的有序性不变，整个转换过程如下：</p><p><img data-src="/transblog/images/558860517267588.png"></p><p><strong>整数集合的升级操作</strong></p><h3 id="quicklist"><a href="#quicklist" class="headerlink" title="quicklist"></a>quicklist</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">quicklist</span> &#123;</span><br><span class="line">    <span class="comment">//quicklist的链表头</span></span><br><span class="line">    quicklistNode *head;      <span class="comment">//quicklist的链表头</span></span><br><span class="line">    <span class="comment">//quicklist的链表尾</span></span><br><span class="line">    quicklistNode *tail; </span><br><span class="line">    <span class="comment">//所有压缩列表中的总元素个数</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> count;</span><br><span class="line">    <span class="comment">//quicklistNodes的个数</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> len;       </span><br><span class="line">    ...</span><br><span class="line">&#125; quicklist;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">quicklistNode</span> &#123;</span><br><span class="line">    <span class="comment">//前一个quicklistNode</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">quicklistNode</span> *prev;     <span class="comment">//前一个quicklistNode</span></span><br><span class="line">    <span class="comment">//下一个quicklistNode</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">quicklistNode</span> *next;     <span class="comment">//后一个quicklistNode</span></span><br><span class="line">    <span class="comment">//quicklistNode指向的压缩列表</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">char</span> *zl;              </span><br><span class="line">    <span class="comment">//压缩列表的的字节大小</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> sz;                </span><br><span class="line">    <span class="comment">//压缩列表的元素个数</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> count : <span class="number">16</span>;        <span class="comment">//ziplist中的元素个数 </span></span><br><span class="line">    ....</span><br><span class="line">&#125; quicklistNode;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/54604115267126.png"></p><ul><li>在向 quicklist 添加一个元素的时候，会检查插入位置的压缩列表是否能容纳该元素，如果能容纳就直接保存到 quicklistNode 结构里的压缩列表，如果不能容纳，才会新建一个新的 quicklistNode 结构。</li><li>quicklist 会控制 quicklistNode 结构里的压缩列表的大小或者元素个数，来规避潜在的连锁更新的风险，但是这并没有完全解决连锁更新的问题。</li></ul><h3 id="listpack"><a href="#listpack" class="headerlink" title="listpack"></a>listpack</h3><p>Redis 在 5.0 新设计一个数据结构叫 listpack，目的是替代压缩列表，它最大特点是 listpack 中每个节点不再包含前一个节点的长度了，压缩列表每个节点正因为需要保存前一个节点的长度字段，就会有连锁更新的隐患。</p><p><img data-src="/transblog/images/90844415259795.png"></p><ul><li>encoding，定义该元素的编码类型，会对不同长度的整数和字符串进行编码；</li><li>data，实际存放的数据；</li><li>len，encoding+data的总长度；</li></ul><p>listpack 没有压缩列表中记录前一个节点长度的字段了，listpack 只记录当前节点的长度，当我们向 listpack 加入一个新元素的时候，不会影响其他节点的长度字段的变化，从而避免了压缩列表的连锁更新问题。</p><h1 id="Redis持久化"><a href="#Redis持久化" class="headerlink" title="Redis持久化"></a>Redis持久化</h1><h2 id="RDB快照"><a href="#RDB快照" class="headerlink" title="RDB快照"></a>RDB快照</h2><p>RDB 快照就是记录某一个瞬间的内存数据，记录的是实际数据，并写入.rdb格式的二进制文件保存在磁盘，这样如果服务器崩溃内存内数据丢失，重新连接后redis会自动读取默认名称为dump.rdb（可以在redis.conf内修改为其他名称）的rdb备份文件，恢复其上次备份时redis存在的所有数据。</p><ul><li>save命令：会在主线程生成 RDB 文件，阻塞当前Redis，直到RDB持久化过程完成为止，若内存实例比较大会造成长时间阻塞，线上环境不建议用它</li><li>bgsave命令：redis进程执行fork操作创建子线程，由子线程完成持久化，阻塞时间很短（微秒级），是save的优化, 在执行redis-cli shutdown关闭redis服务时，如果没有开启AOF持久化，自动执行bgsave;</li></ul><p>Redis 的快照是全量快照，执行的频率不能太频繁，否则会影响 Redis 性能，</p><h3 id="执行快照时，数据能被修改吗？"><a href="#执行快照时，数据能被修改吗？" class="headerlink" title="执行快照时，数据能被修改吗？"></a>执行快照时，数据能被修改吗？</h3><p>写时复制技术</p><p>如果主线程（父进程）要修改共享数据里的某一块数据（比如键值对 A）时，就会发生写时复制，于是这块数据的物理内存就会被复制一份（键值对 A’），然后主线程在这个数据副本（键值对 A’）进行修改操作。与此同时，bgsave 子进程可以继续把原来的数据（键值对 A）写入到 RDB 文件。</p><p>注意：<strong>发生了写时复制后，RDB 快照保存的是原本的内存数据，而主线程刚修改的数据，是没办法在这一时间写入 RDB 文件的</strong>，只能交由下一次的 bgsave 快照。</p><p>快照的频率不好把握：</p><ul><li>如果频率太低，两次快照间一旦服务器发生宕机，就可能会比较多的数据丢失；</li><li>如果频率太高，频繁写入磁盘和创建子进程会带来额外的性能开销。</li></ul><h2 id="AOF日志持久化"><a href="#AOF日志持久化" class="headerlink" title="AOF日志持久化"></a>AOF日志持久化</h2><p><img data-src="/transblog/images/217274914256149.png"></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">*3</span><br><span class="line"><span class="meta prompt_">$</span><span class="language-bash">3</span></span><br><span class="line">set</span><br><span class="line"><span class="meta prompt_">$</span><span class="language-bash">4</span></span><br><span class="line">name</span><br><span class="line"><span class="meta prompt_">$</span><span class="language-bash">7</span></span><br><span class="line">xiaolin</span><br></pre></td></tr></table></figure><p>「*3」表示当前命令有三个部分，每部分都是以「$+数字」开头，后面紧跟着具体的命令、键或值。然后，这里的「数字」表示这部分中的命令、键或值一共有多少字节。例如，「$3 set」表示这部分有 3 个字节，也就是「set」命令这个字符串的长度。</p><p>先执行写操作命令后，才记录到 AOF 日志<br>好处：</p><ol><li>避免额外的检查开销。</li><li>不会阻塞当前写操作命令的执行</li></ol><p>坏处：</p><ol><li>服务器发生宕机，这个数据就会有丢失的风险</li><li>可能会给「下一个」命令带来阻塞风险</li></ol><h3 id="AOF何时写回磁盘？"><a href="#AOF何时写回磁盘？" class="headerlink" title="AOF何时写回磁盘？"></a>AOF何时写回磁盘？</h3><p>此操作在主进程中，是同步的</p><p><img data-src="/transblog/images/144465514251903.png"></p><p><img data-src="/transblog/images/523995614269783.png"></p><p>在 redis.conf 配置文件中的 appendfsync 配置项可以有以下 3 种参数可填：</p><p>Always，这个单词的意思是「总是」，所以它的意思是每次写操作命令执行完后，同步将 AOF 日志数据写回硬盘；<br>Everysec，这个单词的意思是「每秒」，所以它的意思是每次写操作命令执行完后，先将命令写入到 AOF 文件的内核缓冲区，然后每隔一秒将缓冲区里的内容写回到硬盘；<br>No，意味着不由 Redis 控制写回硬盘的时机，<strong>转交给操作系统控制写回的时机</strong>，也就是每次写操作命令执行完后，先将命令写入到 AOF 文件的内核缓冲区，再由操作系统决定何时将缓冲区内容写回硬盘。</p><p><img data-src="/transblog/images/281445814267387.png"></p><p>这三种策略控制的是fsync()系统调用的时机，详情参照《操作系统》</p><p><img data-src="/transblog/images/29525914264889.png"></p><h3 id="AOF重写机制"><a href="#AOF重写机制" class="headerlink" title="AOF重写机制"></a>AOF重写机制</h3><p>AOF 日志是一个文件，随着执行的写操作命令越来越多，文件的大小会越来越大。如果当 AOF 日志文件过大就会带来性能问题，比如重启 Redis 后，需要读 AOF 文件的内容以恢复数据，如果文件过大，整个恢复的过程就会很慢。所以，Redis 为了避免 AOF 文件越写越大，提供了 AOF 重写机制，当 AOF 文件的大小超过所设定的阈值后，Redis 就会启用 AOF 重写机制，来压缩 AOF 文件。</p><p>AOF 重写机制是在重写时，读取当前数据库中的所有键值对，然后将每一个键值对用一条命令记录到「新的 AOF 文件」，等到全部记录完后，就将新的 AOF 文件替换掉现有的 AOF 文件。</p><p>使用重写机制后，就会读取 key 最新的 value（键值对） ，然后用最新的命令值记录到新的 AOF 文件，最终也只需要根据这个「键值对」当前的最新状态，然后用一条命令去记录键值对，如果 AOF 重写过程中失败了，放弃新的AOF文件</p><h4 id="AOF后台重写"><a href="#AOF后台重写" class="headerlink" title="AOF后台重写"></a>AOF后台重写</h4><p>重写 AOF 过程是由后台子进程 bgrewriteaof 来完成的。子进程进行 AOF 重写期间，主进程可以继续处理命令请求，从而避免阻塞主进程</p><p>为何使用子进程？不是线程？（考虑写时复制和共享数据）</p><p>修改的数据量比较大的 key-value 的时候，这时写时复制的物理内存数据的过程就会比较耗时，有阻塞主进程的风险。还有个问题，重写 AOF 日志过程中，如果主进程修改了已经存在 key-value，此时这个 key-value 数据在子进程的内存数据就跟主进程的内存数据不一致了</p><p>为了解决这种数据不一致问题，Redis 设置了一个 AOF 重写缓冲区，这个缓冲区在创建 bgrewriteaof 子进程之后开始使用。在重写 AOF 期间，当 Redis 执行完一个写命令之后，它会同时将这个写命令写入到 「AOF 缓冲区」和 「AOF 重写缓冲区」。</p><p><img data-src="/transblog/images/403101515246130.png"></p><p>在 bgrewriteaof 子进程执行 AOF 重写期间，主进程需要执行以下三个工作:</p><ol><li>执行客户端发来的命令；</li><li>将执行后的写命令追加到 「AOF 缓冲区」；</li><li>将执行后的写命令追加到 「AOF 重写缓冲区」；</li></ol><p>AOF重写完成后：</p><ul><li>将 AOF 重写缓冲区中的所有内容追加到新的 AOF 的文件中，使得新旧两个 AOF 文件所保存的数据库状态一致；（两个缓冲区的作用是一样的）</li><li>新的 AOF 的文件进行改名，覆盖现有的 AOF 文件因为如果 AOF 重写过程中失败了，现有的 AOF 文件就会造成污染，可能无法用于恢复使用。所以 AOF 重写过程，先重写到新的 AOF 文件，重写失败的话，就直接删除这个文件就好，不会对现有的 AOF 文件造成影响</li></ul><p><strong>在整个 AOF 后台重写过程中，除了发生写时复制会对主进程造成阻塞，还有信号处理函数执行时也会对主进程造成阻塞，在其他时候，AOF 后台重写都不会阻塞主进程。</strong></p><p>针对RDB不适合实时持久化，redis提供了AOF持久化方式来解决</p><ul><li>AOF持久化采取的是日志式添加记录每次修改操作的方式，每次执行修改后，AOF持久化只需要将新执行的操作添加到appendonly.aof文件的末尾，对文件进行简单的append操作的IO消耗很小，这种文件是可读的，也就意味着可以被手动修改，拥有更强的灵活性。</li><li>比如Redis不小心执行了flushall指令，清空了所有数据，只要是aof没有被rewrite，只需要复制一份aof文件，去掉最后的flushall命令，再重启redis，redis会自动读取aof文件进行恢复（即从头到尾依次执行记录的操作）。</li><li>AOF持久化默认不开启，需要在redis.conf配置文件中将appendonly no改为appendonly yes开启。可以设置为每秒进行一次，或每次修改都进行持久化。</li><li>相同数据产生的AOF文件比RDB文件更大，而且开启AOF持久化对Redis主线程的性能影响也比RDB更大，但是可以更好保证数据完整性。</li></ul><h2 id="区别"><a href="#区别" class="headerlink" title="区别"></a>区别</h2><p><strong>RDB适用于备份数据和快速恢复场景，适合对数据实时性要求不高、恢复速度要求高的场景</strong>。<br><strong>AOF适用于需要更高数据安全性、能够接受较大存储空间的场景</strong>。</p><h2 id="双剑合璧"><a href="#双剑合璧" class="headerlink" title="双剑合璧"></a>双剑合璧</h2><p>如果想要开启混合持久化功能，可以在 Redis 配置文件将下面这个配置项设置成 yes：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">aof-use-rdb-preamble yes</span><br></pre></td></tr></table></figure><p>混合持久化工作在 AOF 日志重写过程。</p><p>当开启了混合持久化时，在 AOF 重写日志时，fork 出来的重写子进程会先将与主线程共享的内存数据以 RDB 方式写入到 AOF 文件，然后主线程处理的操作命令会被记录在重写缓冲区里，重写缓冲区里的增量命令会以 AOF 方式写入到 AOF 文件，写入完成后通知主进程将新的含有 RDB 格式和 AOF 格式的 AOF 文件替换旧的的 AOF 文件。</p><p>简单来说，如下图：其它与AOF重写一致<br>AOF 文件的前半部分是 RDB 格式的全量数据，后半部分是 AOF 格式的增量数据。</p><p><img data-src="/transblog/images/138633315268570.png"></p><h2 id="Redis的大key"><a href="#Redis的大key" class="headerlink" title="Redis的大key"></a>Redis的大key</h2><p>key 对应的 value 很大。String 类型的值大于 10 KB；Hash、List、Set、ZSet 类型的元素的个数超过 5000个；<br>大 key 会带来以下四种影响：</p><ul><li>客户端超时阻塞。<br>由于 Redis 执行命令是单线程处理，然后在操作大 key 时会比较耗时，那么就会阻塞 Redis，从客户端这一视角看，就是很久很久都没有响应。</li><li>引发网络阻塞。<br>每次获取大 key 产生的网络流量较大，如果一个 key 的大小是 1 MB，每秒访问量为 1000，那么每秒会产生 1000MB 的流量，这对于普通千兆网卡的服务器来说是灾难性的。</li><li>阻塞工作线程。<br>如果使用 del 删除大 key 时，会阻塞工作线程，这样就没办法处理后续的命令。</li><li>内存分布不均。<br>集群模型在 slot 分片均匀情况下，会出现数据和查询倾斜情况，部分有大 key 的 Redis 节点占用内存多，QPS 也会比较大。</li></ul><h3 id="大key对持久化的影响"><a href="#大key对持久化的影响" class="headerlink" title="大key对持久化的影响"></a>大key对持久化的影响</h3><p>大 Key 对 AOF 日志的影响：参照 fsync() 系统调用函数<br>大 Key 对 AOF 重写的影响：考虑频繁触发重写，写时复制的性能<br>大 Key 对 RDB 快照的影响：考虑写时复制的性能</p><p><strong>如果 Linux 开启了内存大页，会影响 Redis 的性能的。</strong><br>常规的内存页分配是按 4KB 的粒度来执行的。内存大页机制支持 2MB 大小的内存页分配</p><p>禁用方法如下：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">echo never &gt;  /sys/kernel/mm/transparent_hugepage/enabled</span><br></pre></td></tr></table></figure><h3 id="如何查找大key"><a href="#如何查找大key" class="headerlink" title="如何查找大key"></a>如何查找大key</h3><h4 id="redis-cli-–bigkeys-查找大key"><a href="#redis-cli-–bigkeys-查找大key" class="headerlink" title="redis-cli –bigkeys 查找大key"></a>redis-cli –bigkeys 查找大key</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">redis-cli -h 127.0.0.1 -p6379 -a &quot;password&quot; -- bigkeys</span><br></pre></td></tr></table></figure><p>注意事项：<br><strong>最好选择在从节点上执行该命令。因为主节点上执行时，会阻塞主节点</strong>；<br>如果没有从节点，那么可以选择在 Redis 实例业务压力的低峰阶段进行扫描查询，以免影响到实例的正常运行；或者可以使用 -i 参数控制扫描间隔，避免长时间扫描降低 Redis 实例的性能。</p><p>该方式的不足之处：</p><p>这个方法只能返回每种类型中最大的那个 bigkey，无法得到大小排在前 N 位的 bigkey；<br>对于集合类型来说，这个方法只统计集合元素个数的多少，而不是实际占用的内存量。但是，<strong>一个集合中的元素个数多，并不一定占用的内存就多</strong>。因为，有可能每个元素占用的内存很小，这样的话，即使元素个数有很多，总内存开销也不大；</p><h4 id="使用-SCAN-命令查找大-key"><a href="#使用-SCAN-命令查找大-key" class="headerlink" title="使用 SCAN 命令查找大 key"></a>使用 SCAN 命令查找大 key</h4><p>使用 SCAN 命令对数据库扫描，然后用 TYPE 命令获取返回的每一个 key 的类型。</p><p>对于 String 类型，可以直接使用 STRLEN 命令获取字符串的长度，也就是占用的内存空间字节数。</p><p>对于集合类型来说，有两种方法可以获得它占用的内存大小：</p><p>如果能够预先从业务层知道集合元素的平均大小，那么，可以使用下面的命令获取集合元素的个数，然后乘以集合元素的平均大小，这样就能获得集合占用的内存大小了。List 类型：LLEN 命令；Hash 类型：HLEN 命令；Set 类型：SCARD 命令；Sorted Set 类型：ZCARD 命令；<br>如果不能提前知道写入集合的元素大小，可以使用 MEMORY USAGE 命令（需要 Redis 4.0 及以上版本），查询一个键值对占用的内存空间。</p><h4 id="使用-RdbTools-工具查找大-key"><a href="#使用-RdbTools-工具查找大-key" class="headerlink" title="使用 RdbTools 工具查找大 key"></a>使用 RdbTools 工具查找大 key</h4><p>使用 RdbTools 第三方开源工具，可以用来解析 Redis 快照（RDB）文件，找到其中的大 key。<br>比如，下面这条命令，将大于 10 kb 的  key  输出到一个表格文件。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rdb dump.rdb -c memory --bytes 10240 -f redis.csv</span><br></pre></td></tr></table></figure><h3 id="如何删除大key"><a href="#如何删除大key" class="headerlink" title="如何删除大key"></a>如何删除大key</h3><p>在应用程序释放内存时，操作系统需要把释放掉的内存块插入一个空闲内存块的链表，以便后续进行管理和再分配。这个过程本身需要一定时间，而且会阻塞当前释放内存的应用程序。</p><p>如果一下子释放了大量内存，空闲内存块链表操作时间就会增加，<strong>相应地就会造成 Redis 主线程的阻塞</strong>，如果主线程发生了阻塞，其他所有请求可能都会超时，超时越来越多，会造成 Redis 连接耗尽，产生各种异常。</p><h4 id="分批次删除"><a href="#分批次删除" class="headerlink" title="分批次删除"></a>分批次删除</h4><p>对于删除大 Hash，使用 hscan 命令，每次获取 100 个字段，再用 hdel 命令，每次删除 1 个字段。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">del_large_hash</span>():</span><br><span class="line">  r = redis.StrictRedis(host=<span class="string">&#x27;redis-host1&#x27;</span>, port=<span class="number">6379</span>)</span><br><span class="line">    large_hash_key =<span class="string">&quot;xxx&quot;</span> <span class="comment">#要删除的大hash键名</span></span><br><span class="line">    cursor = <span class="string">&#x27;0&#x27;</span></span><br><span class="line">    <span class="keyword">while</span> cursor != <span class="number">0</span>:</span><br><span class="line">        <span class="comment"># 使用 hscan 命令，每次获取 100 个字段</span></span><br><span class="line">        cursor, data = r.hscan(large_hash_key, cursor=cursor, count=<span class="number">100</span>)</span><br><span class="line">        <span class="keyword">for</span> item <span class="keyword">in</span> data.items():</span><br><span class="line">                <span class="comment"># 再用 hdel 命令，每次删除1个字段</span></span><br><span class="line">                r.hdel(large_hash_key, item[<span class="number">0</span>])</span><br></pre></td></tr></table></figure><p>对于删除大 List，通过 ltrim 命令，每次删除少量元素。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">del_large_list</span>():</span><br><span class="line">  r = redis.StrictRedis(host=<span class="string">&#x27;redis-host1&#x27;</span>, port=<span class="number">6379</span>)</span><br><span class="line">  large_list_key = <span class="string">&#x27;xxx&#x27;</span>  <span class="comment">#要删除的大list的键名</span></span><br><span class="line">  <span class="keyword">while</span> r.llen(large_list_key)&gt;<span class="number">0</span>:</span><br><span class="line">      <span class="comment">#每次只删除最右100个元素</span></span><br><span class="line">      r.ltrim(large_list_key, <span class="number">0</span>, -<span class="number">101</span>) </span><br></pre></td></tr></table></figure><p>对于删除大 Set，使用 sscan 命令，每次扫描集合中 100 个元素，再用 srem 命令每次删除一个键。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">del_large_set</span>():</span><br><span class="line">  r = redis.StrictRedis(host=<span class="string">&#x27;redis-host1&#x27;</span>, port=<span class="number">6379</span>)</span><br><span class="line">  large_set_key = <span class="string">&#x27;xxx&#x27;</span>   <span class="comment"># 要删除的大set的键名</span></span><br><span class="line">  cursor = <span class="string">&#x27;0&#x27;</span></span><br><span class="line">  <span class="keyword">while</span> cursor != <span class="number">0</span>:</span><br><span class="line">    <span class="comment"># 使用 sscan 命令，每次扫描集合中 100 个元素</span></span><br><span class="line">    cursor, data = r.sscan(large_set_key, cursor=cursor, count=<span class="number">100</span>)</span><br><span class="line">    <span class="keyword">for</span> item <span class="keyword">in</span> data:</span><br><span class="line">      <span class="comment"># 再用 srem 命令每次删除一个键</span></span><br><span class="line">      r.srem(large_size_key, item)</span><br></pre></td></tr></table></figure><p>对于删除大 ZSet，使用 zremrangebyrank 命令，每次删除 top 100个元素。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">del_large_sortedset</span>():</span><br><span class="line">  r = redis.StrictRedis(host=<span class="string">&#x27;large_sortedset_key&#x27;</span>, port=<span class="number">6379</span>)</span><br><span class="line">  large_sortedset_key=<span class="string">&#x27;xxx&#x27;</span></span><br><span class="line">  <span class="keyword">while</span> r.zcard(large_sortedset_key)&gt;<span class="number">0</span>:</span><br><span class="line">    <span class="comment"># 使用 zremrangebyrank 命令，每次删除 top 100个元素</span></span><br><span class="line">    r.zremrangebyrank(large_sortedset_key,<span class="number">0</span>,<span class="number">99</span>) </span><br></pre></td></tr></table></figure><h4 id="异步删除"><a href="#异步删除" class="headerlink" title="异步删除"></a>异步删除</h4><p>用 unlink 命令代替 del 来删除。这样 Redis 会将这个 key 放入到一个异步线程中进行删除，这样不会阻塞主线程。</p><ul><li>lazyfree-lazy-eviction：表示当 Redis 运行内存超过 maxmeory 时，是否开启 lazy free 机制删除；</li><li>lazyfree-lazy-expire：表示设置了过期时间的键值，当过期之后是否开启 lazy free 机制删除；</li><li>lazyfree-lazy-server-del：有些指令在处理已存在的键时，会带有一个隐式的 del 键的操作，比如 rename 命令，当目标键已存在，Redis 会先删除目标键，如果这些目标键是一个 big key，就会造成阻塞删除的问题，此配置表示在这种场景中是否开启 lazy free 机制删除；</li><li>slave-lazy-flush：针对 slave (从节点) 进行全量数据同步，slave 在加载 master 的 RDB 文件前，会运行 flushall 来清理自己的数据，它表示此时是否开启 lazy free 机制删除。</li></ul><p>建议开启其中的 lazyfree-lazy-eviction、lazyfree-lazy-expire、lazyfree-lazy-server-del 等配置，这样就可以有效的提高主线程的执行效率。</p><h1 id="Redis的内存策略"><a href="#Redis的内存策略" class="headerlink" title="Redis的内存策略"></a>Redis的内存策略</h1><h2 id="Redis内存满了，会发生什么？"><a href="#Redis内存满了，会发生什么？" class="headerlink" title="Redis内存满了，会发生什么？"></a>Redis内存满了，会发生什么？</h2><p>在 Redis 的运行内存达到了某个阀值，就会触发内存淘汰机制(OOM)，这个阀值就是我们设置的最大运行内存，此值在 Redis 的配置文件中可以找到，配置项为 maxmemory。</p><h2 id="判定key已过期"><a href="#判定key已过期" class="headerlink" title="判定key已过期"></a>判定key已过期</h2><p>每当我们对一个 key 设置了过期时间时，Redis 会把该 key 带上过期时间存储到一个过期字典（expires dict）中，也就是说「过期字典」保存了数据库中所有 key 的过期时间。</p><p>过期字典存储在 redisDb 结构中，如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">redisDb</span> &#123;</span><br><span class="line">    dict *dict;    <span class="comment">/* 数据库键空间，存放着所有的键值对 */</span></span><br><span class="line">    dict *expires; <span class="comment">/* 键的过期时间 */</span></span><br><span class="line">    ....</span><br><span class="line">&#125; redisDb;</span><br></pre></td></tr></table></figure><p>过期字典数据结构结构如下：</p><p><img data-src="/transblog/images/170314415263706.png"></p><p>过期字典的 key 是一个指针，指向某个键对象；<br>过期字典的 value 是一个 long long 类型的整数，这个整数保存了 key 的过期时间；</p><h2 id="过期删除策略"><a href="#过期删除策略" class="headerlink" title="过期删除策略"></a>过期删除策略</h2><p>定时删除：在设置 key 的过期时间时，同时创建一个定时事件，当时间到达时，由事件处理器自动执行 key 的删除操作。<strong>对内存友好，对CPU不友好</strong><br>惰性删除：不主动删除过期键，每次从数据库访问 key 时，都检测 key 是否过期，如果过期则删除该 key。<strong>对CPU友好，对内存不友好</strong><br>定期删除：隔一段时间「随机」从数据库中取出一定数量的 key 进行检查，并删除其中的过期key。<strong>减少对CPU影响，难以确定删除操作执行的时长和频率</strong></p><p><strong>Redis 选择「惰性删除+定期删除」这两种策略配和使用</strong></p><p><strong>惰性删除：</strong><br>Redis 在访问或者修改 key 之前，都会调用 expireIfNeeded 函数对其进行检查，检查 key 是否过期：如果过期，则删除该 key，至于选择异步删除，还是选择同步删除，根据 lazyfree_lazy_expire 参数配置决定，然后返回 null 客户端；如果没有过期，不做任何处理，然后返回正常的键值对给客户端；<br><strong>定期删除：</strong><br>1、这个间隔检查的时间是多长呢？<br>在 Redis 中，默认每秒进行 10 次过期检查一次数据库<br>2、随机抽查的数量是多少呢？<br>定期删除的实现在 expire.c 文件下的 activeExpireCycle 函数中，其中随机抽查的数量由 ACTIVE_EXPIRE_CYCLE_LOOKUPS_PER_LOOP 定义的，它是写死在代码中的，数值是 20。</p><p><strong>Redis 的定期删除的流程：</strong></p><ol><li>从过期字典中随机抽取 20 个 key；</li><li>检查这 20 个 key 是否过期，并删除已过期的 key；</li><li>如果本轮检查的已过期 key 的数量，超过 5 个（20&#x2F;4），也就是「已过期 key 的数量」占比「随机抽取 key 的数量」大于 25%，则继续重复步骤 1；如果已过期的 key 比例小于 25%，则停止继续删除过期 key，然后等待下一轮再检查。</li><li>定期删除是一个循环的流程。Redis 为了保证定期删除不会出现循环过度，导致线程卡死现象，为此增加了定期删除循环流程的时间上限，默认不会超过 25ms。</li></ol><h2 id="内存淘汰策略"><a href="#内存淘汰策略" class="headerlink" title="内存淘汰策略"></a>内存淘汰策略</h2><p>1、不进行数据淘汰的策略<br>noeviction（Redis3.0之后，默认的内存淘汰策略） ：</p><p>2、进行数据淘汰的策略<br>针对「进行数据淘汰」这一类策略，又可以细分为「在设置了过期时间的数据中进行淘汰」和「在所有数据范围内进行淘汰」这两类策略。</p><p>在设置了过期时间的数据中进行淘汰：</p><ol><li>volatile-random：随机淘汰设置了过期时间的任意键值；</li><li>volatile-ttl：优先淘汰更早过期的键值。</li><li>volatile-lru（Redis3.0 之前，默认的内存淘汰策略）：淘汰所有设置了过期时间的键值中，最久未使用的键值；</li><li>volatile-lfu（Redis 4.0 后新增的内存淘汰策略）：淘汰所有设置了过期时间的键值中，最少使用的键值；</li></ol><p>在所有数据范围内进行淘汰：</p><ol><li>allkeys-random：随机淘汰任意键值;</li><li>allkeys-lru：淘汰整个键值中最久未使用的键值；</li><li>allkeys-lfu（Redis 4.0 后新增的内存淘汰策略）：淘汰整个键值中最少使用的键值。</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; config get maxmemory-policy</span><br><span class="line">1) &quot;maxmemory-policy&quot;</span><br><span class="line">2) &quot;noeviction&quot;</span><br></pre></td></tr></table></figure><h2 id="LRU和LFU"><a href="#LRU和LFU" class="headerlink" title="LRU和LFU"></a>LRU和LFU</h2><p><strong>Redis 实现的是一种 LFU 算法</strong>，目的是为了更好的节约内存，它的实现方式是在 Redis 的对象结构体中<strong>添加一个额外的字段，用于记录此数据的最后一次访问时间</strong>。当 Redis 进行内存淘汰时，会使用随机采样的方式来淘汰数据，它是随机取 5 个值（此值可配置），然后淘汰最久没有使用的那个。<strong>但无法解决缓存污染问题</strong></p><p>LFU 为最近最不常用，是根据数据访问次数来淘汰数据的，它的核心思想是<strong>如果数据过去被访问多次，那么将来被访问的频率也更高</strong>。<strong>LFU 算法会记录每个数据的访问次数。当一个数据被再次访问时，就会增加该数据的访问次数</strong>。这样就解决了偶尔被访问一次之后，数据留存在缓存中很长一段时间的问题，相比于 LRU 算法也更合理一些。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">redisObject</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">      </span><br><span class="line">    <span class="comment">// 24 bits，用于记录对象的访问信息</span></span><br><span class="line">    <span class="type">unsigned</span> lru:<span class="number">24</span>;  </span><br><span class="line">    ...</span><br><span class="line">&#125; robj;</span><br></pre></td></tr></table></figure><p>Redis 对象头中的 lru 字段，在 LRU 算法下和 LFU 算法下使用方式并不相同。<br>在 LRU 算法中，Redis 对象头的 24 bits 的 lru 字段是用来记录 key 的访问时间戳，因此在 LRU 模式下，Redis可以根据对象头中的 lru 字段记录的值，来比较最后一次 key 的访问时间长，从而淘汰最久未被使用的 key。<br>在 LFU 算法中，Redis对象头的 24 bits 的 lru 字段被分成两段来存储，高 16bit 存储 ldt(Last Decrement Time)，低 8bit 存储 logc(Logistic Counter)。</p><p><img data-src="/transblog/images/587813417257252.png"></p><p>注意，logc 并不是单纯的访问次数，而是访问频次（访问频率），因为 logc 会随时间推移而衰减的。</p><p>在每次 key 被访问时，<strong>会先对 logc 做一个衰减操作</strong>，衰减的值跟前后访问时间的差距有关系，<strong>如果上一次访问的时间与这一次访问的时间差距很大，那么衰减的值就越大</strong>，这样实现的 LFU 算法是根据访问频率来淘汰数据的，而不只是访问次数。访问频率需要考虑 key 的访问是多长时间段内发生的。key 的先前访问距离当前时间越长，那么这个 key 的访问频率相应地也就会降低，这样被淘汰的概率也会更大。</p><p>对 logc 做完衰减操作后，就开始对 logc 进行增加操作，增加操作并不是单纯的 + 1，而是<strong>根据概率增加</strong>，如果 logc 越大的 key，它的 logc 就越难再增加。</p><p>redis.conf 提供了两个配置项，用于调整 LFU 算法从而控制 logc 的增长和衰减：<br>lfu-decay-time 用于调整 logc 的衰减速度，它是一个以分钟为单位的数值，默认值为1，lfu-decay-time 值越大，衰减越慢；<br>lfu-log-factor 用于调整 logc 的增长速度，lfu-log-factor 值越大，logc 增长越慢。</p><h2 id="从库处理过期key"><a href="#从库处理过期key" class="headerlink" title="从库处理过期key"></a>从库处理过期key</h2><p>当 Redis 运行在主从模式下时，从库不会进行过期扫描，从库对过期的处理是被动的。也就是即使从库中的 key 过期了，如果有客户端访问从库时，依然可以得到 key 对应的值，像未过期的键值对一样返回。</p><p>从库的过期键处理依靠主服务器控制，主库在 key 到期时，会在 AOF 文件里增加一条 del 指令，同步到所有的从库，从库通过执行这条 del 指令来删除过期的 key。</p><h1 id="Redis的高可用"><a href="#Redis的高可用" class="headerlink" title="Redis的高可用"></a>Redis的高可用</h1><h2 id="主从结构"><a href="#主从结构" class="headerlink" title="主从结构"></a>主从结构</h2><p><img data-src="/transblog/images/488184417250386.png"></p><p>在服务器 B 上执行下面这条命令：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">replicaof &lt;服务器 A 的 IP 地址&gt; &lt;服务器 A 的 Redis 端口号&gt;</span><br></pre></td></tr></table></figure><p>接着，服务器 B 就会变成服务器 A 的「从服务器」，然后与主服务器进行第一次同步。</p><p>主从服务器间的第一次同步的过程可分为三个阶段：</p><p><img data-src="/transblog/images/150742911246842.png"></p><p>主从服务器在完成第一次同步后，双方之间就会维护一个 TCP 连接，而且这个连接是长连接的，<strong>目的是避免频繁的 TCP 连接和断开带来的性能开销</strong>。</p><p><strong>主服务器只有一个，如何分摊主服务器的压力</strong>？<br>从服务器可以有自己的从服务器，可以把拥有从服务器的从服务器当作<strong>经理角色</strong>，它不仅可以接收主服务器的同步数据，自己也可以同时作为主服务器的形式将数据同步给从服务器，</p><p>如果从服务器数量非常多，而且都与主服务器进行全量同步的话，就会带来两个问题：</p><ol><li>bgsave生成 RDB文件的，主服务器就会忙于使用 fork() 创建子进程，如果主服务器的内存数据很大，在执行 fork() 函数时会阻塞主线程，从而使得 Redis 无法正常处理请求</li><li>传输 RDB 文件会占用主服务器的网络带宽，会对主服务器响应命令请求产生影响。</li></ol><p><strong>主服务器生成 RDB 和传输 RDB 的压力可以分摊到充当经理角色的从服务器</strong></p><p><img data-src="/transblog/images/49241511240549.png"></p><p>在「从服务器」上执行下面这条命令，使其作为目标服务器的从服务器：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">replicaof &lt;目标服务器的IP&gt; 6379</span><br></pre></td></tr></table></figure><h3 id="全量同步"><a href="#全量同步" class="headerlink" title="全量同步"></a>全量同步</h3><ol><li>第一阶段是建立链接、协商同步</li></ol><p>从服务器会给主服务器发送 psync 命令，表示要进行数据同步。psync 命令包含两个参数，分别是<strong>主服务器的 runID <strong>和</strong>复制进度 offset</strong>。</p><ul><li>runID，每个 Redis 服务器在启动时都会自动生产一个随机的 ID 来唯一标识自己。当从服务器和主服务器第一次同步时，因为不知道主服务器的 run ID，所以将其设置为 “?”。</li><li>offset，表示复制的进度，第一次同步时，其值为 -1。</li></ul><p>主服务器收到 psync 命令后，会用 FULLRESYNC 作为响应命令返回给对方。并且这个响应命令会带上这两个参数。从服务器收到响应后，会记录这两个值。FULLRESYNC 响应命令的意图是<strong>采用全量复制的方式</strong></p><ol start="2"><li>第二阶段是主服务器同步数据给从服务器；</li></ol><p>主服务器会执行 bgsave 命令（异步）来生成 RDB 文件，然后把文件发送给从服务器。从服务器收到 RDB 文件后，会先清空当前的数据，然后载入 RDB 文件。<br><strong>但是，这期间的写操作命令并没有记录到刚刚生成的 RDB 文件中，这时主从服务器间的数据就不一致了。</strong></p><h4 id="replication-buffer"><a href="#replication-buffer" class="headerlink" title="replication buffer"></a>replication buffer</h4><p>为了保证主从服务器的数据一致性，主服务器在下面这三个时间间隙中将收到的写操作命令，写入到<strong>replication buffer 缓冲区</strong>里：</p><ul><li>主服务器生成 RDB 文件期间；</li><li>主服务器发送 RDB 文件给从服务器期间；</li><li>「从服务器」加载 RDB 文件期间；</li></ul><ol start="3"><li>第三阶段是主服务器发送新写操作命令给从服务器。</li></ol><p>从服务器完成 RDB 的载入后，会回复一个确认消息给主服务器。主服务器将 replication buffer 缓冲区里所记录的写操作命令发送给从服务器，从服务器执行来自主服务器 replication buffer 缓冲区里发来的命令，这时主从服务器的数据就一致了。</p><h3 id="增量同步"><a href="#增量同步" class="headerlink" title="增量同步"></a>增量同步</h3><p>如果主从服务器间的网络连接断开了，那么就无法进行命令传播了，这时从服务器的数据就没办法和主服务器保持一致了，客户端就可能从「从服务器」读到旧的数据。<br>从 Redis 2.8 开始，网络断开又恢复后，从主从服务器会采用增量复制的方式继续同步，也就是只会把网络断开期间主服务器接收到的写操作命令，同步给从服务器。</p><p><img data-src="/transblog/images/252172211258975.png"></p><ol><li>从服务器在恢复网络后，会发送 psync 命令给主服务器，此时的 psync 命令里的 offset 参数不是 -1；</li><li>主服务器收到该命令后，然后用 CONTINUE 响应命令告诉从服务器接下来采用增量复制的方式同步数据；</li><li>然后主服务将主从服务器断线期间，所执行的写命令发送给从服务器，然后从服务器执行这些命令。</li></ol><h4 id="repl-backlog-buffer"><a href="#repl-backlog-buffer" class="headerlink" title="repl_backlog_buffer"></a>repl_backlog_buffer</h4><p><strong>主服务器怎么知道要将哪些增量数据发送给从服务器呢？</strong></p><ul><li>repl_backlog_buffer，是一个「环形」缓冲区，用于主从服务器断连后，从中找到差异的数据；</li><li>replication offset，标记缓冲区的同步进度，主从服务器都有各自的偏移量，主服务器使用 master_repl_offset 来记录自己「写」到的位置，从服务器使用 slave_repl_offset 来记录自己「读」到的位置。</li></ul><p>从Redis完成初始化后，每一个对主Redis的操作会被发送到从Redis执行相同操作，这被称为增量同步，以保证主从Redis数据一致性。</p><p><strong>repl_backlog_buffer 缓冲区是什么时候写入的呢？</strong></p><ul><li><p>在主服务器<strong>进行命令传播时</strong>，不仅会将写命令发送给从服务器，还会将写命令写入到 repl_backlog_buffer 缓冲区里，因此 这个缓冲区里会保存着最近传播的写命令。</p></li><li><p>网络断开后，当从服务器重新连上主服务器时，从服务器会通过 psync 命令将自己的复制偏移量 slave_repl_offset 发送给主服务器，主服务器根据自己的 master_repl_offset 和 slave_repl_offset 之间的差距，然后来决定对从服务器执行哪种同步操作：从服务器要读取的数据还在 repl_backlog_buffer 缓冲区里，那么主服务器将采用增量同步的方式；相反，主服务器将采用全量同步的方式。</p></li><li><p>repl_backlog_buffer 缓行缓冲区的默认大小是 1M，为了避免在网络恢复时，主服务器频繁地使用全量同步的方式，应该调整该缓冲区大小，尽可能的大一些，减少出现从服务器要读取的数据被覆盖的概率，从而使得主服务器采用增量同步的方式。<br><img data-src="/transblog/images/554712018267008.png"></p></li></ul><h3 id="两个Buffer区别"><a href="#两个Buffer区别" class="headerlink" title="两个Buffer区别"></a>两个Buffer区别</h3><p>replication buffer 、repl backlog buffer 区别如下：</p><ul><li><p>出现的阶段不一样：<br>repl backlog buffer 是在增量复制阶段出现，<strong>一个主节点只分配一个 repl backlog buffer</strong>；<br>replication buffer 是在全量复制阶段和增量复制阶段都会出现，<strong>主节点会给每个新连接的从节点，分配一个 replication buffer</strong>；</p></li><li><p>这两个 Buffer 都有大小限制的，当缓冲区满了之后，发生的事情不一样：<br>当 repl backlog buffer 满了，因为是环形结构，<strong>会直接覆盖起始位置数据</strong>;<br>当 replication buffer 满了，会导致连接断开，删除缓存，从节点重新连接，<strong>重新开始全量复制</strong>。</p></li></ul><h3 id="主从同步策略"><a href="#主从同步策略" class="headerlink" title="主从同步策略"></a>主从同步策略</h3><p>主从刚刚连接的时候，进行全量同步；全同步结束后，进行增量同步。如果有需要，slave 在任何时候都可以发起全量同步。redis 策略是，无论如何，首先会尝试进行增量同步，如不成功，要求从机进行全量同步。</p><h3 id="主从数据不一致"><a href="#主从数据不一致" class="headerlink" title="主从数据不一致"></a><font color="red">主从数据不一致</font></h3><p>主从节点间的命令复制是异步进行的，所以会主从数据不一致</p><ol><li><p>保证主从节点间的网络连接状况良好，避免主从节点在不同的机房。</p></li><li><p>可以开发一个外部程序来监控主从节点间的复制进度。具体做法：<br>Redis 的 INFO replication 命令可以查看主节点接收写命令的进度信息（master_repl_offset）和从节点复制写命令的进度信息（slave_repl_offset），所以，我们就可以开发一个监控程序，先用 INFO replication 命令查到主、从节点的进度，这样就能得到从节点和主节点间的复制进度差值了。<br>如果某个从节点的进度差值大于我们预设的阈值，我们可以让客户端不再和这个从节点连接进行数据读取，这样就可以减少读到不一致数据的情况。不过，为了避免出现客户端和所有从节点都不能连接的情况，我们需要把复制进度差值的阈值设置得大一些。</p></li></ol><h2 id="哨兵机制"><a href="#哨兵机制" class="headerlink" title="哨兵机制"></a>哨兵机制</h2><h3 id="哨兵的职责"><a href="#哨兵的职责" class="headerlink" title="哨兵的职责"></a>哨兵的职责</h3><p>哨兵节点主要负责三件事情：监控、通知、选主。</p><ol><li>检测主从节点是否运转正常</li><li>收集主从节点信息，出错的时候可以通过API通知系统管理员</li><li>自动failover：当一个master节点出现了问题，多个哨兵需要沟通是否真的确认出现问题，确认出现问题要检查其他slave节点的情况，自动选举新的适合充当master节点的slave，改变其状态成为新的master，继续提供服务并管理其他slave</li></ol><p><mark>第三个职责自动failover是哨兵最重要的职责，配合主从架构很好的保证了系统的高可用性，</mark>前面讲过主从架构中所有master和slave的数据都是一致的，所以当提供主要服务的master宕机，可以立刻把拥有相同数据的slave推举为新的master继续提供服务，而不会造成业务的不可用。</p><p>注：一个哨兵可以设置监测多个redis节点，但是没必要设置监测slave节点，监测master节点时会自动获取到slave节点的信息。</p><h4 id="哨兵集群"><a href="#哨兵集群" class="headerlink" title="哨兵集群"></a>哨兵集群</h4><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sentinel monitor &lt;master-name&gt; &lt;ip&gt; &lt;redis-port&gt; &lt;quorum&gt; </span><br></pre></td></tr></table></figure><p><strong>为什么只需要执行命令设置主节点名字、主节点的 IP 地址和端口号以及 quorum 值，就能搭建哨兵集群？</strong></p><p>在主从集群中，主节点上有一个名为__sentinel__:hello的频道，不同哨兵就是通过它来相互发现，实现互相通信的。</p><p>哨兵 A 把自己的 IP 地址和端口的信息发布到__sentinel__:hello 频道上，哨兵 B 和 C 订阅了该频道。那么此时，哨兵 B 和 C 就可以从这个频道直接获取哨兵 A 的 IP 地址和端口号。然后，哨兵 B、C 可以和哨兵 A 建立网络连接。</p><p><img data-src="/transblog/images/53561010246215.png"></p><p><strong>那哨兵集群如何知道「从节点」的信息？</strong></p><p>主节点知道所有「从节点」的信息，所以哨兵会每 10 秒一次的频率向主节点发送 INFO 命令来获取所有「从节点」的信息。</p><h3 id="主节点故障"><a href="#主节点故障" class="headerlink" title="主节点故障"></a>主节点故障</h3><p>如何判断主节点真的故障了？<br><img data-src="/transblog/images/460293209259679.png"></p><ol><li>主观下线<br>如果主节点或者从节点没有在规定的时间内发送PONG，响应哨兵的 PING 命令，哨兵就会将它们标记为<strong>主观下线</strong>（sdown状态）。这个「规定的时间」是配置项 down-after-milliseconds 参数设定的</li><li>客观下线<br>可能只是因为主节点的系统压力比较大或者网络发送了拥塞，当一个哨兵判断主节点为「主观下线」后，就会向其他哨兵发起命令（is-master-down-by-addr），其他哨兵收到这个命令后，就会根据自身和主节点的网络状况，做出赞成投票或者拒绝投票的响应。<br>如果超过规定数量的哨兵（<strong>超过quorum值</strong>）将该Redis节点标注成了sdown状态，即可确定这个服务确实成为了不可用状态，将该状态改为odown，也就是客观的不可用状态，准备进行新master节点的推举来代替该redis节点继续提供服务。</li></ol><p><img data-src="/transblog/images/425823609256234.png"></p><h3 id="主从故障转移"><a href="#主从故障转移" class="headerlink" title="主从故障转移"></a>主从故障转移</h3><p>主从故障转移操作包含以下四个步骤：</p><ol><li>在已下线主节点（旧主节点）属下的所有「从节点」里面，挑选出一个从节点，并将其转换为主节点。</li><li>让已下线主节点属下的所有「从节点」修改复制目标，修改为复制「新主节点」；</li><li>将新主节点的 IP 地址和信息，通过「发布者&#x2F;订阅者机制」通知给客户端；</li><li>继续监视旧主节点，当这个旧主节点重新上线时，将它设置为新主节点的从节点；</li></ol><h4 id="选出新主节点"><a href="#选出新主节点" class="headerlink" title="选出新主节点"></a>选出新主节点</h4><p>故障转移操作第一步要做的就是在已下线主节点属下的所有「从节点」中，挑选出一个状态良好、数据完整的从节点，然后向这个「从节点」发送 SLAVEOF no one 命令，将这个「从节点」转换为「主节点」。</p><p><strong>那么多「从节点」，到底选择哪个从节点作为新主节点的？</strong></p><p>先进行网络过滤<br>Redis 有个叫 down-after-milliseconds * 10 配置项，其 down-after-milliseconds 是主从节点断连的最大连接超时时间。如果在 down-after-milliseconds 毫秒内，主从节点都没有通过网络联系上，我们就可以认为主从节点断连了。如果发生断连的次数超过了 10 次，就说明这个从节点的网络状况不好，不适合作为新主节点。</p><p>再进行三轮考察：优先级、复制进度、ID 号。</p><ol><li><p>哨兵首先会根据从节点的优先级来进行排序，优先级越小排名越靠前，<br>Redis 有个叫 slave-priority 配置项，可以给从节点设置优先级。如果「A 从节点」的物理内存是所有从节点中最大的，那么我们可以把「A 从节点」的优先级设置成最高。</p></li><li><p>如果优先级相同，则查看复制的下标，哪个从「主节点」接收的复制数据多，哪个就靠前。<br>如果某个从节点的 slave_repl_offset 最接近 master_repl_offset，说明它的复制进度是最靠前的，于是就可以将它选为新主节点。</p></li><li><p>如果优先级和下标都相同，就选择从节点 ID 较小的那个。<br>每个从节点都有一个编号，这个编号就是 ID 号，是用来唯一标识从节点的。</p></li></ol><p>如下图，哨兵 leader 向被选中的从节点 server2 发送 SLAVEOF no one 命令，将该从节点升级为新主节点。</p><p><img data-src="/transblog/images/99025809251988.png"></p><p>在发送 SLAVEOF no one 命令之后，哨兵 leader 会以每秒一次的频率向被升级的从节点发送 INFO 命令（没进行故障转移之前，INFO 命令的频率是每十秒一次），并观察命令回复中的角色信息，当被升级节点的角色信息从原来的 slave 变为 master 时，哨兵 leader 就知道被选中的从节点已经顺利升级为主节点了。</p><h4 id="将从节点指向新主节点"><a href="#将从节点指向新主节点" class="headerlink" title="将从节点指向新主节点"></a>将从节点指向新主节点</h4><p>向「从节点」发送 SLAVEOF 命令来实现。</p><p><img data-src="/transblog/images/493510010269868.png"></p><h4 id="通知客户的主节点已更换"><a href="#通知客户的主节点已更换" class="headerlink" title="通知客户的主节点已更换"></a>通知客户的主节点已更换</h4><p>通过 Redis 的发布者&#x2F;订阅者机制来实现，主从切换完成后，哨兵就会向 +switch-master 频道发布新主节点的 IP 地址和端口的消息，这个时候客户端就可以收到这条信息，然后用这里面的新主节点的 IP 地址和端口进行通信了。</p><p><img data-src="/transblog/images/414930110267472.png"></p><h4 id="将旧主节点变为从节点"><a href="#将旧主节点变为从节点" class="headerlink" title="将旧主节点变为从节点"></a>将旧主节点变为从节点</h4><p>继续监视旧主节点，当旧主节点重新上线时，哨兵集群就会向它发送 SLAVEOF 命令，让它成为新主节点的从节点</p><p><img data-src="/transblog/images/443500610264974.png"></p><h3 id="进行故障转移的哨兵"><a href="#进行故障转移的哨兵" class="headerlink" title="进行故障转移的哨兵"></a>进行故障转移的哨兵</h3><p>由哪个哨兵进行主从故障转移？<br>需要在哨兵集群中选出一个 leader，让 leader 来执行主从切换。哪个哨兵节点判断主节点为「客观下线」，这个哨兵节点就是候选者。</p><p>任何一个「候选者」，要满足两个条件：</p><ul><li>第一，拿到半数以上的赞成票；</li><li>第二，拿到的票数同时还需要大于等于哨兵配置文件中的 quorum 值。</li></ul><p><strong>如果某个时间点，刚好有两个哨兵节点判断到主节点为客观下线？</strong><br>每位候选者都会先给自己投一票，然后向其他哨兵发起投票请求。投票者先收到谁的投票请求，就会先投票给它，如果投票者用完投票机会后，就会拒绝投票。</p><h3 id="三个哨兵原则"><a href="#三个哨兵原则" class="headerlink" title="三个哨兵原则"></a>三个哨兵原则</h3><p>哨兵在部署的时候不会只部署一个节点，而是用多个节点部署成哨兵集群（最少需要三台机器来部署哨兵集群），通过多个哨兵节点一起判断，就可以就可以避免单个哨兵因为自身网络状况不好，而误判主节点下线的情况。同时，多个哨兵的网络同时不稳定的概率较小，由它们一起做决策，误判率也能降低。</p><p>一个高可用的架构应该至少包括三个部署在不同服务器上的哨兵，因为如果只有两个哨兵，有一台服务器挂了，哨兵也就只剩下了一个，即使能确定服务器上redis服务的odown状态，也没有超过半数（超过2&#x2F;2 &#x3D; 1个哨兵）的哨兵来选举出哨兵执行failover，因此一个合理的系统至少有三台服务器，三个哨兵来保证系统的高可用性</p><p><strong>Redis 1 主 4 从，5 个哨兵，quorum 设置为 3，如果 2 个哨兵故障，当主节点宕机时，哨兵能否判断主节点“客观下线”？主从能否自动切换？</strong><br>哨兵集群可以判定主节点“客观下线”。哨兵集群还剩下 3 个哨兵，当一个哨兵判断主节点“主观下线”后，询问另外 2 个哨兵后，<strong>有可能能拿到 3 张赞同票，这时就达到了 quorum 的值</strong>，因此，哨兵集群可以判定主节点为“客观下线”。</p><p>哨兵集群可以完成主从切换。当有个哨兵标记主节点为「客观下线」后，就会进行选举 Leader 的过程，因为此时哨兵集群还剩下 3 个哨兵，<strong>那么还是可以拿到半数以上（5&#x2F;2+1&#x3D;3）的票，而且也达到了 quorum 值</strong>，满足了选举 Leader 的两个条件，所以就能选举成功，因此哨兵集群可以完成主从切换。</p><p>如果 quorum 设置为 2，并且如果有 3 个哨兵故障的话。此时哨兵集群还是可以判定主节点为“客观下线”，但是哨兵不能完成主从切换了，大家可以自己推演下。</p><p>如果 quorum 设置为 3，并且如果有 3 个哨兵故障的话，哨兵集群即不能判定主节点为“客观下线”，也不能完成主从切换了。</p><p><strong>quorum 的值建议设置为哨兵个数的二分之一加 1，例如 3 个哨兵就设置 2，5 个哨兵设置为 3，而且哨兵节点的数量应该是奇数。</strong></p><h3 id="脑裂现象"><a href="#脑裂现象" class="headerlink" title="脑裂现象"></a>脑裂现象</h3><p>如果主节点的网络突然发生了问题，它与所有的从节点都失联了，但是此时的主节点和客户端的网络是正常的。<br>这个客户端并不知道 Redis 内部已经出现了问题，还在照样的向这个失联的主节点写数据，此时这些数据被主节点缓存到了缓冲区里，因为主从节点之间的网络问题，这些数据都是无法同步给从节点的。<br>这时，哨兵也发现主节点失联了，它就认为主节点挂了（但实际上主节点正常运行，只是网络出问题了），于是哨兵就会在从节点中选举出一个 leeder 作为主节点，这时集群就有两个主节点了 —— 脑裂出现了。</p><p><strong>集群产生脑裂数据丢失</strong>：<br>脑裂现象产生后，这时候网络突然好了，哨兵因为之前已经选举出一个新主节点了，它就会把旧主节点降级为从节点（A），然后从节点（A）会向新主节点请求数据同步，因为第一次同步是全量同步的方式，<strong>此时的从节点（A）会清空掉自己本地的数据，然后再做全量同步</strong>。所以，之前客户端在过程 A 写入的数据就会丢失了，也就是集群产生脑裂数据丢失的问题。</p><p>解决方案：<br>当主节点发现「从节点下线的数量太多」，或者「网络延迟太大」的时候，那么主节点会禁止写操作，直接把错误返回给客户端。等到新主节点上线时，就只有新主节点能接收和处理客户端请求，此时，新写的数据会被直接写到新主节点中。而原主节点会被哨兵降为从节点，即使它的数据被清空了，也不会有新数据丢失。</p><h3 id="如何减少数据丢失"><a href="#如何减少数据丢失" class="headerlink" title="如何减少数据丢失"></a><font color="red">如何减少数据丢失</font></h3><p>当客户端发送写请求给主节点的时候，客户端会返回 ok，接着主节点将写请求异步同步给各个从节点，但是<strong>如果此时主节点还没来得及同步给从节点时发生了断电，那么主节点内存中的数据会丢失</strong>。</p><p>解决方案：在 Redis 的配置文件中有两个参数可以设置：</p><ol><li>min-slaves-to-write x，主节点必须要有至少 x 个从节点连接，如果小于这个数，主节点会禁止写数据。<br>当客户端发现 master 不可写后，可以采取降级措施，将数据暂时写入本地缓存、磁盘、kafka 消息队列中，在一段时间（等 master 恢复正常）后重新写入 master 来保证数据不丢失。</li><li>min-slaves-max-lag x，主从数据复制和同步的延迟不能超过 x 秒，如果主从同步的延迟超过 x 秒，主节点会禁止写数据。</li></ol><p>分别给它们设置一定的阈值，假设为 N 和 T。这两个配置项组合后的要求是，<strong>主节点连接的从节点中至少有 N 个从节点，「并且」主节点进行数据复制时的 ACK 消息延迟不能超过 T 秒，否则，主节点就不会再接收客户端的写请求了</strong>。</p><h3 id="如何判断某个节点是否正常工作？"><a href="#如何判断某个节点是否正常工作？" class="headerlink" title="如何判断某个节点是否正常工作？"></a>如何判断某个节点是否正常工作？</h3><p>Redis 判断节点是否正常工作，基本都是通过互相的 ping-pong 心态检测机制，如果有一半以上的节点去 ping 一个节点的时候没有 pong 回应，集群就会认为这个节点挂掉了，会断开与这个节点的连接。</p><p>Redis 主从节点发送的心态间隔是不一样的，而且作用也有一点区别：</p><p>Redis 主节点默认每隔 10 秒对从节点发送 ping 命令，判断从节点的存活性和连接状态，可通过参数repl-ping-slave-period控制发送频率。<br>Redis 从节点每隔 1 秒发送 replconf ack{offset} 命令，给主节点上报自身当前的复制偏移量，目的是为了：<br>实时监测主从节点网络状态；<br>上报自身复制偏移量， 检查复制数据是否丢失， 如果从节点数据丢失， 再从主节点的复制缓冲区中拉取丢失数据。</p><h1 id="Redis的缓存机制"><a href="#Redis的缓存机制" class="headerlink" title="Redis的缓存机制"></a>Redis的缓存机制</h1><h2 id="缓存污染和预读失效"><a href="#缓存污染和预读失效" class="headerlink" title="缓存污染和预读失效"></a>缓存污染和预读失效</h2><p>详情请见操作系统–内存篇</p><h2 id="缓存雪崩"><a href="#缓存雪崩" class="headerlink" title="缓存雪崩"></a>缓存雪崩</h2><ol><li>大量缓存数据在同一时间过期（失效）</li><li>Redis 故障宕机<br>此时如果有大量的用户请求，都无法在 Redis 中处理，于是全部请求都直接访问数据库，<strong>从而导致数据库的压力骤增，严重的会造成数据库宕机</strong>，从而形成一系列连锁反应，造成整个系统崩溃。</li></ol><h3 id="大量数据同时过期"><a href="#大量数据同时过期" class="headerlink" title="大量数据同时过期"></a>大量数据同时过期</h3><h4 id="均匀设置过期时间"><a href="#均匀设置过期时间" class="headerlink" title="均匀设置过期时间"></a>均匀设置过期时间</h4><p>如果要给缓存数据设置过期时间，应该避免将大量的数据设置成同一个过期时间。我们可以在对缓存数据设置过期时间时，<strong>给这些数据的过期时间加上一个随机数</strong>，这样就保证数据不会在同一时间过期。</p><h4 id="互斥锁构建缓存"><a href="#互斥锁构建缓存" class="headerlink" title="互斥锁构建缓存"></a>互斥锁构建缓存</h4><p>当业务线程在处理用户请求时，<strong>如果发现访问的数据不在 Redis 里，就加个互斥锁，保证同一时间内只有一个请求来构建缓存（从数据库读取数据，再将数据更新到 Redis 里）</strong>，当缓存构建完成后，再释放锁。未能获取互斥锁的请求，要么等待锁释放后重新读取缓存，要么就返回空值或者默认值。</p><p><strong>实现互斥锁的时候，最好设置超时时间</strong>，不然第一个请求拿到了锁，然后这个请求发生了某种意外而一直阻塞，一直不释放锁，这时其他请求也一直拿不到锁，整个系统就会出现无响应的现象。</p><h4 id="后台更新缓存"><a href="#后台更新缓存" class="headerlink" title="后台更新缓存"></a>后台更新缓存</h4><p>业务线程不再负责更新缓存，缓存也不设置有效期，而是<strong>让缓存“永久有效”，并将更新缓存的工作交由后台线程定时更新。</strong></p><p>事实上，缓存数据不设置有效期，<strong>并不是意味着数据一直能在内存里，因为当系统内存紧张的时候，有些缓存数据会被淘汰</strong>，而在缓存被“淘汰”到下一次后台定时更新缓存的这段时间内，业务线程读取缓存失败就返回空值，业务的视角就以为是数据丢失了。</p><ol><li><p><strong>后台线程不仅负责定时更新缓存，而且也负责频繁地检测缓存是否有效</strong>，检测到缓存失效了，原因可能是系统紧张而被淘汰的，于是就要马上从数据库读取数据，并更新到缓存。<br>这种方式的检测时间间隔不能太长，太长也导致用户获取的数据是一个空值而不是真正的数据，所以检测的间隔最好是毫秒级的，但是总归是有个间隔时间，用户体验一般。</p></li><li><p>在业务线程发现缓存数据失效后（缓存数据被淘汰），<strong>通过消息队列发送一条消息通知后台线程更新缓存</strong>，后台线程收到消息后，在更新缓存前可以判断缓存是否存在，存在就不执行更新缓存操作；不存在就读取数据库数据，并将数据加载到缓存。这种方式相比第一种方式缓存的更新会更及时，用户体验也比较好。</p></li></ol><h4 id="缓存预热"><a href="#缓存预热" class="headerlink" title="缓存预热"></a>缓存预热</h4><p>在业务刚上线的时候，我提前把数据缓起来，而不是等待用户访问才来触发缓存构建，这就是所谓的缓存预热，后台更新缓存的机制刚好也适合干这个事情。<br>Redis自带缓存预读，注意甄别区别</p><h3 id="Redis-故障宕机"><a href="#Redis-故障宕机" class="headerlink" title="Redis 故障宕机"></a>Redis 故障宕机</h3><h4 id="服务熔断或请求限流机制"><a href="#服务熔断或请求限流机制" class="headerlink" title="服务熔断或请求限流机制"></a>服务熔断或请求限流机制</h4><ul><li><p>可以启动服务熔断机制，<strong>暂停业务应用对缓存服务的访问</strong>，直接返回错误，不用再继续访问数据库，从而降低对数据库的访问压力，保证数据库系统的正常运行，然后等到 Redis 恢复正常后，再允许业务应用访问缓存服务。</p></li><li><p>为了减少对业务的影响，可以<strong>启用请求限流机制</strong>，只将少部分请求发送到数据库进行处理，再多的请求就在入口直接拒绝服务，等到 Redis 恢复正常并把缓存预热完后，再解除请求限流的机制。</p></li></ul><h4 id="构建-Redis-缓存高可靠集群"><a href="#构建-Redis-缓存高可靠集群" class="headerlink" title="构建 Redis 缓存高可靠集群"></a>构建 Redis 缓存高可靠集群</h4><p>主从节点的方式构建 Redis 缓存高可靠集群。</p><h2 id="缓存击穿"><a href="#缓存击穿" class="headerlink" title="缓存击穿"></a>缓存击穿</h2><p>热点数据过期，大量的请求访问了该热点数据，就无法从缓存中读取，直接访问数据库，数据库很容易就被高并发的请求冲垮。<br>可以认为缓存击穿是缓存雪崩的一个子集。应对缓存击穿可以采取前面说到两种方案：</p><ol><li>互斥锁方案，保证同一时间只有一个业务线程更新缓存，未能获取互斥锁的请求，要么等待锁释放后重新读取缓存，要么就返回空值或者默认值。</li><li>不给热点数据设置过期时间，由后台异步更新缓存，或者在热点数据准备要过期前，提前通知后台线程更新缓存以及重新设置过期时间；</li></ol><h2 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a>缓存穿透</h2><p><strong>当用户访问的数据，既不在缓存中，也不在数据库中</strong>，导致请求在访问缓存时，发现缓存缺失，再去访问数据库时，发现数据库中也没有要访问的数据，<strong>没办法构建缓存数据</strong>，来服务后续的请求。那么当有大量这样的请求到来时，数据库的压力骤增，这就是缓存穿透的问题。</p><p>缓存穿透的发生一般有这两种情况：</p><ol><li>业务误操作，缓存中的数据和数据库中的数据都被误删除了，所以导致缓存和数据库中都没有数据；</li><li>黑客恶意攻击，故意大量访问某些读取不存在数据的业务；</li></ol><p>应对缓存穿透的方案，常见的方案有三种。</p><h3 id="非法请求的限制"><a href="#非法请求的限制" class="headerlink" title="非法请求的限制"></a>非法请求的限制</h3><p>在入口处进行参数校验，如果判断出是恶意请求就直接返回错误，避免进一步访问缓存和数据库。</p><h3 id="缓存空值或者默认值"><a href="#缓存空值或者默认值" class="headerlink" title="缓存空值或者默认值"></a>缓存空值或者默认值</h3><p>可以针对查询的数据，在缓存中设置一个空值或者默认值，这样后续请求就可以从缓存中读取到空值或者默认值，返回给应用，而不会继续查询数据库。</p><h3 id="布隆过滤器"><a href="#布隆过滤器" class="headerlink" title="布隆过滤器"></a>布隆过滤器</h3><p><strong>最主要的作用是可以快速判断数据是否存在</strong></p><ul><li>可以在写入数据库数据时，使用布隆过滤器做个标记，然后在用户请求到来时，业务线程确认缓存失效后，可以通过查询布隆过滤器快速判断数据是否存在，如果不存在，就不用通过查询数据库来判断数据是否存在。</li><li>即使发生了缓存穿透，大量请求只会查询 Redis 和布隆过滤器，而不会查询数据库，保证了数据库能正常运行，Redis 自身也是支持布隆过滤器的。</li></ul><p>布隆过滤器由「初始值都为 0 的位图数组」和「 N 个哈希函数」两部分组成。</p><p>布隆过滤器会通过 3 个操作完成标记：</p><ol><li>使用 N 个哈希函数分别对数据做哈希计算，得到 N 个哈希值；</li><li>将第一步得到的 N 个哈希值对位图数组的长度取模，得到每个哈希值在位图数组的对应位置。</li><li>将每个哈希值在位图数组的对应位置的值设置为 1；</li></ol><p>假设有一个位图数组长度为 8，哈希函数 3 个的布隆过滤器。</p><p><img data-src="/transblog/images/41684310268655.png"></p><p>当应用要查询数据 x 是否数据库时，通过布隆过滤器只要查到位图数组的第 1、4、6 位置的值是否全为 1，只要有一个为 0，就认为数据 x 不在数据库中。</p><p>布隆过滤器由于是基于哈希函数实现查找的，高效查找的同时<strong>存在哈希冲突的可能性</strong>，比如数据 x 和数据 y 可能都落在第 1、4、6 位置，而事实上，可能数据库中并不存在数据 y，存在误判的情况。</p><p><strong>查询布隆过滤器查询到数据不存在，数据库中一定就不存在这个数据，数据存在，并不一定证明数据库中存在这个数据。</strong></p><h1 id="Redis的系统设计"><a href="#Redis的系统设计" class="headerlink" title="Redis的系统设计"></a>Redis的系统设计</h1><h2 id="数据库和缓存的数据如何保持一致性"><a href="#数据库和缓存的数据如何保持一致性" class="headerlink" title="数据库和缓存的数据如何保持一致性"></a>数据库和缓存的数据如何保持一致性</h2><h3 id="先更新数据库，再更新缓存"><a href="#先更新数据库，再更新缓存" class="headerlink" title="先更新数据库，再更新缓存"></a>先更新数据库，再更新缓存</h3><p>并发问题导致：</p><p><img data-src="/transblog/images/241604910263791.png"></p><h3 id="先更新缓存，再更新数据库"><a href="#先更新缓存，再更新数据库" class="headerlink" title="先更新缓存，再更新数据库"></a>先更新缓存，再更新数据库</h3><p><img data-src="/transblog/images/376975010257337.png"></p><p>如果业务对缓存命中率有很高的要求，可以采用「更新数据库 + 更新缓存」的方案，因为更新缓存并不会出现缓存未命中的情况。<br>在更新缓存前先加个分布式锁，保证同一时间只运行一个请求更新缓存，就会不会产生并发问题了，当然引入了锁后，对于写入的性能就会带来影响。<br>在更新完缓存时，给缓存加上较短的过期时间，这样即时出现缓存不一致的情况，缓存的数据也会很快过期，对业务还是能接受的。</p><h3 id="所以先更新数据库，还是先删除缓存"><a href="#所以先更新数据库，还是先删除缓存" class="headerlink" title="所以先更新数据库，还是先删除缓存"></a>所以先更新数据库，还是先删除缓存</h3><p>是否可以借用cpu三级缓存失效策略？可以，详情请见操作系统–cpu篇</p><p>Cache Aside 策略(旁路缓存策略):不更新缓存，而是删除缓存中的数据。然后，到读取数据时，发现缓存中没了数据之后，再从数据库中读取数据，更新到缓存中。</p><p><img data-src="/transblog/images/304165210250471.png"></p><p>这样的话在写时又会带来两个问题</p><h4 id="先删除缓存，再更新数据库"><a href="#先删除缓存，再更新数据库" class="headerlink" title="先删除缓存，再更新数据库"></a>先删除缓存，再更新数据库</h4><p><img data-src="/transblog/images/470005410241001.png"></p><p>先删除缓存，再更新数据库，在「读 + 写」并发的时候，还是会出现缓存和数据库的数据不一致的问题。</p><p>解决方法<strong>可以使用延迟双删</strong>，延迟双删实现的伪代码如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">#删除缓存</span><br><span class="line">redis.delKey(X)</span><br><span class="line">#更新数据库</span><br><span class="line">db.update(X)</span><br><span class="line">#睡眠</span><br><span class="line">Thread.sleep(N)</span><br><span class="line">#再删除缓存</span><br><span class="line">redis.delKey(X)</span><br></pre></td></tr></table></figure><p>请求 A 的睡眠时间就需要大于请求 B 「从数据库读取数据 + 写入缓存」的时间。但是具体睡眠多久其实是个玄学，</p><h4 id="先更新数据库，再删除缓存"><a href="#先更新数据库，再删除缓存" class="headerlink" title="先更新数据库，再删除缓存"></a>先更新数据库，再删除缓存</h4><p><img data-src="/transblog/images/318075510243505.png"></p><p>先更新数据库，再删除缓存也是会出现数据不一致性的问题</p><p><strong>但是：</strong><br>redis运行在内存中，数据库实例在磁盘中，内存的速度远大于磁盘，所以上述的情况基本不存在，<strong>「先更新数据库 + 再删除缓存」的方案，是可以保证数据一致性的</strong>。<br>值得注意的是，不管哪种方案，都避免不了Redis存在脏数据的问题，只能减轻这个问题，要想彻底解决，得要用到同步锁和对应的业务逻辑层面解决。</p><ul><li>缓存添加过期时间<br>为了确保万无一失，还可以<strong>给缓存数据加上了「过期时间」</strong>，就算在这期间存在缓存数据不一致，有过期时间来兜底，这样也能达到最终一致。</li></ul><p>问题：<br><strong>明明更新了数据，但是数据要过一段时间才生效，客户接受不了。</strong><br>如果在删除缓存（第二个操作）的时候失败了，会导致缓存中的数据是旧值。加上了过期时间，所以才会出现客户说的过一段时间才更新生效的现象，假设如果没有这个过期时间的兜底，那后续的请求读到的就会一直是缓存中的旧数据，这样问题就更大了。</p><p><img data-src="/transblog/images/525772711245337.png"></p><p><strong>如何保证「先更新数据库 ，再删除缓存」这两个操作能执行成功</strong>？<br>其实不管是先操作数据库，还是先操作缓存，只要第二个操作失败都会出现数据一致的问题。有两种方法：都是采用异步操作缓存。</p><ul><li><p>重试机制。<br>可以引入消息队列，将第二个操作（删除缓存）要操作的数据加入到消息队列，由消费者来操作数据。<br>如果应用删除缓存失败，可以从消息队列中重新读取数据，然后再次删除缓存，这个就是重试机制。当然，如果重试超过的一定次数，还是没有成功，我们就需要向业务层发送报错信息了。<br>如果删除缓存成功，就要把数据从消息队列中移除，避免重复操作，否则就继续重试。</p></li><li><p>订阅 MySQL binlog，再操作缓存。<br>第一步是更新数据库，那么更新数据库成功，就会产生一条变更日志，记录在 binlog 里。通过订阅 binlog 日志，拿到具体要操作的数据，然后再执行缓存删除，阿里巴巴开源的 Canal 中间件就是基于这个实现的。<br>Canal 模拟 MySQL 主从复制的交互协议，把自己伪装成一个 MySQL 的从节点，向 MySQL 主节点发送 dump 请求，MySQL 收到请求后，就会开始推送 Binlog 给 Canal，Canal 解析 Binlog 字节流之后，转换为便于读取的结构化数据，供下游程序订阅使用。</p></li></ul><p><img data-src="/transblog/images/19073311245946.png"></p><h1 id="Redis的分布式锁"><a href="#Redis的分布式锁" class="headerlink" title="Redis的分布式锁"></a>Redis的分布式锁</h1><h2 id="Redis分布式锁"><a href="#Redis分布式锁" class="headerlink" title="Redis分布式锁"></a>Redis分布式锁</h2><p>Redis 的 SET 命令有个 NX 参数可以实现「key不存在才插入」，所以可以用它来实现分布式锁：</p><p>如果 key 不存在，则显示插入成功，可以用来表示加锁成功；<br>如果 key 存在，则会显示插入失败，可以用来表示加锁失败。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SET lock_key unique_value NX PX|EX 10000</span><br></pre></td></tr></table></figure><p>解锁的时候，要先判断锁的 unique_value 是否为加锁客户端，是的话，才将 lock_key 键删除。解锁是有两个操作，这时就需要 Lua 脚本来保证解锁的原子性，因为 Redis 在执行 Lua 脚本时，可以以原子性的方式执行，保证了锁释放操作的原子性</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 释放锁时，先比较 unique_value 是否相等，避免锁的误释放</span><br><span class="line">if redis.call(&quot;get&quot;,KEYS[1]) == ARGV[1] then</span><br><span class="line">    return redis.call(&quot;del&quot;,KEYS[1])</span><br><span class="line">else</span><br><span class="line">    return 0</span><br><span class="line">end</span><br></pre></td></tr></table></figure><p><strong>Redis 主从复制模式中的数据是异步复制的，这样导致分布式锁的不可靠性</strong>，如果在 Redis 主节点获取到锁后，在没有同步到其他节点时，Redis 主节点宕机了，此时新的 Redis 主节点依然可以获取锁，所以多个应用服务就可以同时获取到锁。</p><h2 id="分布式锁的一致性"><a href="#分布式锁的一致性" class="headerlink" title="分布式锁的一致性"></a>分布式锁的一致性</h2><p><a href="https://juejin.cn/post/6844903465181773831">【转载】基于 Redis 的分布式锁到底安全吗？</a></p><p><strong>考虑下面的执行序列：</strong></p><ol><li>客户端1从Master获取了锁。</li><li>Master宕机了，存储锁的key还没有来得及同步到Slave上。</li><li>Slave升级为Master。</li><li>客户端2从新的Master获取到了对应同一个资源的锁。</li></ol><p>此时的锁是不可靠的</p><p><strong>分布式锁算法 Redlock（红锁）–解决单点故障</strong><br>官方推荐是至少部署 5 个 Redis 节点，而且都是主节点，它们之间没有任何关系，都是一个个孤立的节点。</p><p>Redlock 算法的基本思路，是让客户端和多个独立的 Redis 节点依次请求申请加锁，如果客户端能够和半数以上的节点成功地完成加锁操作，那么我们就认为，客户端成功地获得分布式锁，否则加锁失败。<br>条件一：客户端从超过半数（大于等于 N&#x2F;2+1）的 Redis 节点上成功获取到了锁；<br>条件二：客户端从大多数节点获取锁的总耗时（t2-t1）小于锁设置的过期时间。</p><p>Redlock 算法加锁三个过程：</p><ol><li>客户端获取当前时间（t1）。</li><li>客户端按顺序依次向 N 个 Redis 节点执行加锁操作：<br>如果某个 Redis 节点发生故障了，为了保证在这种情况下，Redlock 算法能够继续运行，需要给「加锁操作」设置一个超时时间（<strong>不是对「锁」设置超时时间，而是对「加锁操作」设置超时时间</strong>），加锁操作的超时时间需要远远地小于锁的过期时间，一般也就是设置为几十毫秒。</li><li>一旦客户端从超过半数（大于等于 N&#x2F;2+1）的 Redis 节点上成功获取到了锁，就再次获取当前时间（t2），然后计算计算整个加锁过程的总耗时（t2-t1）。如果 t2-t1 &lt; 锁的过期时间，此时，认为客户端加锁成功，否则认为加锁失败。</li></ol><p>加锁成功后，客户端需要重新计算这把锁的有效时间，计算的结果是「锁最初设置的过期时间」减去「客户端从大多数节点获取锁的总耗时（t2-t1）」。如果计算的结果已经来不及完成共享数据的操作了，我们可以释放锁，以免出现还没完成数据操作，锁就过期了的情况。</p><p>加锁失败后，客户端向所有 Redis 节点发起释放锁的操作，释放锁的操作和在单节点上释放锁的操作一样，只要执行释放锁的 Lua 脚本就可以了。</p><h1 id="Redis常见问题和总结"><a href="#Redis常见问题和总结" class="headerlink" title="Redis常见问题和总结"></a>Redis常见问题和总结</h1><h2 id="Redis-和-Memcached-有什么区别？"><a href="#Redis-和-Memcached-有什么区别？" class="headerlink" title="Redis 和 Memcached 有什么区别？"></a>Redis 和 Memcached 有什么区别？</h2><ul><li>Redis 支持的数据类型更丰富（String、Hash、List、Set、ZSet），而 Memcached 只支持最简单的 key-value 数据类型；</li><li>Redis 支持数据的持久化，可以将内存中的数据保持在磁盘中，重启的时候可以再次加载进行使用，而 Memcached 没有持久化功能，数据全部存在内存之中，Memcached 重启或者挂掉后，数据就没了；</li><li>Redis 原生支持集群模式，Memcached 没有原生的集群模式，需要依靠客户端来实现往集群中分片写入数据；</li><li>Redis 支持发布订阅模型、Lua 脚本、事务等功能，而 Memcached 不支持；</li></ul><h2 id="为什么用-Redis-作为-MySQL-的缓存？"><a href="#为什么用-Redis-作为-MySQL-的缓存？" class="headerlink" title="为什么用 Redis 作为 MySQL 的缓存？"></a>为什么用 Redis 作为 MySQL 的缓存？</h2><ol><li><p>Redis 具备高性能<br>运行在内存中</p></li><li><p>Redis 具备高并发<br>单台设备的 Redis 的 QPS（Query Per Second，每秒钟处理完请求的次数） 是 MySQL 的 10 倍，Redis 单机的 QPS 能轻松破 10w，而 MySQL 单机的 QPS 很难破 1w。</p></li></ol><h2 id="如何设计一个缓存策略，可以动态缓存热点数据呢？"><a href="#如何设计一个缓存策略，可以动态缓存热点数据呢？" class="headerlink" title="如何设计一个缓存策略，可以动态缓存热点数据呢？"></a>如何设计一个缓存策略，可以动态缓存热点数据呢？</h2><p>由于数据存储受限，系统并不是将所有数据都需要存放到缓存中的，<strong>而只是将其中一部分热点数据缓存起来</strong>，所以我们要设计一个热点数据动态缓存的策略。</p><p>热点数据动态缓存的策略总体思路：<strong>通过数据最新访问时间来做排名，并过滤掉不常访问的数据，只留下经常访问的数据</strong>。</p><p>以电商平台场景中的例子，现在要求只缓存用户经常访问的 Top 1000 的商品。具体细节如下：</p><p>先通过缓存系统做一个排序队列（比如存放 1000 个商品），系统会根据商品的访问时间，更新队列信息，越是最近访问的商品排名越靠前；<br>同时系统会定期过滤掉队列中排名最后的 200 个商品，然后再从数据库中随机读取出 200 个商品加入队列中；<br>这样当请求每次到达的时候，会先从队列中获取商品 ID，如果命中，就根据 ID 再从另一个缓存数据结构中读取实际的商品信息，并返回。<br>在 Redis 中可以用 zadd 方法和 zrange 方法来完成排序队列和获取 200 个商品的操作。</p><h2 id="Redis-如何实现延迟队列？"><a href="#Redis-如何实现延迟队列？" class="headerlink" title="Redis 如何实现延迟队列？"></a>Redis 如何实现延迟队列？</h2><p>延迟队列是指把当前要做的事情，往后推迟一段时间再做。延迟队列的常见使用场景有以下几种：</p><ul><li>在淘宝、京东等购物平台上下单，超过一定时间未付款，订单会自动取消；</li><li>打车的时候，在规定时间没有车主接单，平台会取消你的单并提醒你暂时没有车主接单；</li><li>点外卖的时候，如果商家在10分钟还没接单，就会自动取消订单；</li></ul><p>在 Redis 可以使用有序集合（ZSet）的方式来实现延迟消息队列的，<strong>ZSet 有一个 Score 属性可以用来存储延迟执行的时间</strong>。</p><p>使用 zadd score1 value1 命令就可以一直往内存中生产消息。再利用 zrangebysocre 查询符合条件的所有待处理的任务， 通过循环执行队列任务即可。</p><p><img data-src="/transblog/images/199813014258977.png"></p><h2 id="Redis-事务支持回滚吗？"><a href="#Redis-事务支持回滚吗？" class="headerlink" title="Redis 事务支持回滚吗？"></a>Redis 事务支持回滚吗？</h2><p>Redis 中并没有提供回滚机制，Redis 并不一定保证原子性</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">获取name原本的值</span></span><br><span class="line">127.0.0.1:6379&gt; GET name</span><br><span class="line">&quot;xiaolin&quot;</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">开启事务</span></span><br><span class="line">127.0.0.1:6379&gt; MULTI</span><br><span class="line">OK</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">设置新值</span></span><br><span class="line">127.0.0.1:6379(TX)&gt; SET name xialincoding</span><br><span class="line">QUEUED</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">注意，这条命令是错误的</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">expire 过期时间正确来说是数字，并不是‘10s’字符串，但是还是入队成功了</span></span><br><span class="line">127.0.0.1:6379(TX)&gt; EXPIRE name 10s</span><br><span class="line">QUEUED</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">提交事务，执行报错</span></span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">可以看到 <span class="built_in">set</span> 执行成功，而 expire 执行错误。</span></span><br><span class="line">127.0.0.1:6379(TX)&gt; EXEC</span><br><span class="line">1) OK</span><br><span class="line">2) (error) ERR value is not an integer or out of range</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">可以看到，name 还是被设置为新值了</span></span><br><span class="line">127.0.0.1:6379&gt; GET name</span><br><span class="line">&quot;xialincoding&quot;</span><br></pre></td></tr></table></figure><p>作者不支持事务回滚的原因有以下两个：</p><ol><li>他认为 Redis 事务的执行时，错误通常都是编程错误造成的，这种错误通常只会出现在开发环境中，而很少会在实际的生产环境中出现，所以他认为没有必要为 Redis 开发事务回滚功能；</li><li>不支持事务回滚是因为这种复杂的功能和 Redis 追求的简单高效的设计主旨不符合。这里不支持事务回滚，指的是不支持事务运行时错误的事务回滚。</li></ol><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Redis数据类型和数据结构&quot;&gt;&lt;a href=&quot;#Redis数据类型和数据结构&quot; class=&quot;headerlink&quot; title=&quot;Redis数据类型和数据结构&quot;&gt;&lt;/a&gt;Redis数据类型和数据结构&lt;/h1&gt;&lt;h2 id=&quot;数据类型&quot;&gt;&lt;a href=&quot;#数</summary>
      
    
    
    
    <category term="中间件" scheme="https://trans-xu.github.io/transblog/categories/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
    
  </entry>
  
  <entry>
    <title>RabbitMq消息队列</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/RabbitMq/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/RabbitMq/</id>
    <published>2025-08-27T03:50:08.889Z</published>
    <updated>2025-08-27T03:35:16.495Z</updated>
    
    <content type="html"><![CDATA[<h1 id="消息模型"><a href="#消息模型" class="headerlink" title="消息模型"></a>消息模型</h1><ol><li>点对点<br>消息生产者向消息队列中发送了一个消息之后，只能被一个消费者消费一次。</li><li>发布&#x2F;订阅<br>消息生产者向频道发送一个消息之后，多个消费者可以从该频道订阅到这条消息并消费。<br>临时（ephemeral）订阅：这种订阅只有在消费者启动并且运行的时候才存在。一旦消费者退出，相应的订阅以及尚未处理的消息就会丢失。<br>持久（durable）订阅：这种订阅会一直存在，除非主动去删除。消费者退出后，消息系统会继续维护该订阅，并且后续消息可以被继续处理。</li><li>发布与订阅模式和观察者模式有以下不同：<br>观察者模式中，观察者和主题都知道对方的存在；而在发布与订阅模式中，生产者与消费者不知道对方的存在，它们之间通过频道进行通信。<br>观察者模式是同步的，当事件触发时，主题会调用观察者的方法，然后等待方法返回；而发布与订阅模式是异步的，生产者向频道发送一个消息之后，就不需要关心消费者何时去订阅这个消息，可以立即返回。</li></ol><h1 id="AMQP协议"><a href="#AMQP协议" class="headerlink" title="AMQP协议"></a>AMQP协议</h1><p>AMQP协议是具有现代特征的二进制协议。是一个提供统一消息服务的应用层标准高级消息队列协议，是应用层协议的一个开放标准，为面向消息的中间件设计。</p><ul><li><p>Server：接收客户端的连接，实现AMQP实体服务。</p></li><li><p>Connection：连接，应用程序与Server的网络连接，TCP连接。</p><blockquote><p>（1）Connection会执行认证、IP解析、路由等底层网络任务。<br>（2）应用与消息队列RabbitMQ版完成Connection建立大约需要15个TCP报文交互，因而会消耗大量的网络资源和消息队列RabbitMQ版资源。<br>（3）一个进程对应一个Connection，一个进程中的多个线程则分别对应一个Connection中的多个Channel。<br>（4）Producer和Consumer分别使用不同的Connection进行消息发送和消费；</p></blockquote></li><li><p>Channel：信道，消息读写等操作在信道中进行。客户端可以建立多个信道，每个信道代表一个会话任务。</p><blockquote><p>（1）Channel是物理TCP连接中的虚拟连接。<br>（2）当应用通过Connection与消息队列RabbitMQ版建立连接后，所有的AMQP协议操作（例如创建队列、发送消息、接收消息等）都会通过Connection中的Channel完成。<br>（3） Channel可以复用Connection，即一个Connection下可以建立多个Channel。<br>（4） Channel不能脱离Connection独立存在，而必须存活在Connection中。<br>（5） 当某个Connection断开时，该Connection下的所有Channel都会断开。</p></blockquote></li><li><p>Message：消息，应用程序和服务器之间传送的数据，消息可以非常简单，也可以很复杂。由Properties和Body组成。Properties为外包装，可以对消息进行修饰，比如消息的优先级、延迟等高级特性；Body就是消息体内容。</p></li><li><p>Virtual Host：虚拟主机，用于逻辑隔离。一个虚拟主机里面可以有若干个Exchange和Queue，同一个虚拟主机里面不能有相同名称的Exchange或Queue。</p></li><li><p>Exchange：交换器，接收消息，按照路由规则将消息路由到一个或者多个队列。如果路由不到，或者返回给生产者，或者直接丢弃。RabbitMQ常用的交换器常用类型有direct、topic、fanout、headers四种，后面详细介绍。</p></li><li><p>Binding：绑定，交换器和消息队列之间的虚拟连接，绑定中可以包含一个或者多个RoutingKey。</p></li><li><p>RoutingKey：路由键，生产者将消息发送给交换器的时候，会发送一个RoutingKey，用来指定路由规则，这样交换器就知道把消息发送到哪个队列。路由键通常为一个“.”分割的字符串，例如“com.rabbitmq”。</p></li><li><p>Queue：消息队列，用来保存消息，供消费者消费。</p></li></ul><p><img data-src="/transblog/images/475593716247141.png"></p><h1 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h1><h2 id="生产者发送消息流程"><a href="#生产者发送消息流程" class="headerlink" title="生产者发送消息流程"></a>生产者发送消息流程</h2><p>1、生产者和Broker建立TCP连接；<br>2、生产者和Broker建立通道；<br>3、生产者通过通道消息发送给Broker，由Exchange将消息进行转发；<br>4、Exchange将消息转发到指定的Queue（队列）。</p><blockquote><p>【详细】<br>1、消息生产者连接到<code>RabbitMQ Broker</code>,建立链接（Connection）,在链接（Connection）上开启一个信道（Channel）；<br>2、声明一个交换机（Exchange）,并设置相关属性，比如交换机类型、是否持久化等；<br>3、声明一个队列（Queue），并设置相关属性，比如是否排他、是否持久化、是否自动删除等；<br>4、使用路由键（RoutingKey）将队列（Queue）和交换机（Exchange）绑定起来；<br>5、生产者发送消息至 RabbitMQ Broker，其中包含路由键、交换器等信息，根据路由键（RoutingKey）发送消息到交换机（Exchange）；<br>6、相应的交换器（Exchange）根据接收到的路由键（RoutingKey）查找相匹配的队列如果找到 ，则将从生产者发送过来的消息存入相应的队列中；<br>7、如果没有找到 ，则根据生产者配置的属性选择丢弃还是回退给生产者；<br>8、关闭信道（Channel）；<br>9、关闭链接（Connection）；</p></blockquote><h2 id="消费者接收消息流程"><a href="#消费者接收消息流程" class="headerlink" title="消费者接收消息流程"></a>消费者接收消息流程</h2><p>1、消费者和Broker建立TCP连接；<br>2、消费者和Broker建立通道；<br>3、消费者监听指定的Queue（队列）；<br>4、当有消息到达Queue时Broker默认将消息推送给消费者；<br>5、消费者接收到消息；<br>6、ack回复。</p><blockquote><p>【详细】<br>1、建立链接（Connection）；<br>2、在链接（Connection）上开启一个信道（Channel）；<br>3、请求消费指定队列（Queue）的消息，并设置回调函数（onMessage）；<br>4、[MQ]将消息推送给消费者，消费者接收消息；<br>5、消费者发送消息确定（Ack[acknowledge]）；<br>6、[MQ]删除被确认的消息；<br>7、关闭信道（Channel）；<br>8、关闭链接（Connection）；</p></blockquote><h1 id="特性分析"><a href="#特性分析" class="headerlink" title="特性分析"></a>特性分析</h1><p>不仅需要知道Rabbit的特性，还需要知道支持这些特性的原因：</p><ol><li>消息路由（支持）：RabbitMQ可以通过不同的交换器支持不同种类的消息路由；</li><li>消息有序（不支持）：当消费消息时，如果消费失败，消息会被放回队列，然后重新消费，这样会导致消息无序；</li><li>消息时序（非常好）：通过延时队列，可以指定消息的延时时间，过期时间TTL等；</li><li>容错处理（非常好）：通过交付重试和死信交换器（DLX）来处理消息处理故障；</li><li>伸缩（一般）：伸缩其实没有非常智能，因为即使伸缩了，master queue还是只有一个，负载还是只有这一个master queue去抗，所以我理解RabbitMQ的伸缩很弱（个人理解）。</li><li>持久化（不太好）：没有消费的消息，可以支持持久化，这个是为了保证机器宕机时消息可以恢复，但是消费过的消息，就会被马上删除，因为RabbitMQ设计时，就不是为了去存储历史数据的。</li><li>消息回溯（不支持）：因为消息不支持永久保存，所以自然就不支持回溯。</li><li>高吞吐（中等）：因为所有的请求的执行，最后都是在master queue，它的这个设计，导致单机性能达不到十万级的标准。</li></ol><h1 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h1><h2 id="异步处理"><a href="#异步处理" class="headerlink" title="异步处理"></a>异步处理</h2><p>发送者将消息发送给消息队列之后，不需要同步等待消息接收者处理完毕，而是立即返回进行其它操作。消息接收者从消息队列中订阅消息之后异步处理。</p><h2 id="流量削锋"><a href="#流量削锋" class="headerlink" title="流量削锋"></a>流量削锋</h2><p>在高并发的场景下，如果短时间有大量的请求到达会压垮服务器。<br>可以将请求发送到消息队列中，服务器按照其处理能力从消息队列中订阅消息进行处理。</p><h2 id="应用解耦"><a href="#应用解耦" class="headerlink" title="应用解耦"></a>应用解耦</h2><p>如果模块之间不直接进行调用，模块之间耦合度就会很低，那么修改一个模块或者新增一个模块对其它模块的影响会很小，从而实现可扩展性。</p><p>通过使用消息队列，一个模块只需要向消息队列中发送消息，其它模块可以选择性地从消息队列中订阅消息从而完成调用。</p><h2 id="日志处理"><a href="#日志处理" class="headerlink" title="日志处理"></a>日志处理</h2><p>日志处理是指将消息队列用在日志处理中，比如Kafka的应用，解决大量日志传输的问题。<br>日志采集客户端，负责日志数据采集，定时写入Kafka队列；Kafka消息队列，负责日志数据的接收，存储和转发；日志处理应用：订阅并消费kafka队列中的日志数据。</p><h2 id="消息通讯"><a href="#消息通讯" class="headerlink" title="消息通讯"></a>消息通讯</h2><p>消息通讯是指，消息队列一般都内置了高效的通信机制，因此也可以用在纯的消息通讯。比如实现点对点消息队列，或者聊天室等</p><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;消息模型&quot;&gt;&lt;a href=&quot;#消息模型&quot; class=&quot;headerlink&quot; title=&quot;消息模型&quot;&gt;&lt;/a&gt;消息模型&lt;/h1&gt;&lt;ol&gt;
&lt;li&gt;点对点&lt;br&gt;消息生产者向消息队列中发送了一个消息之后，只能被一个消费者消费一次。&lt;/li&gt;
&lt;li&gt;发布&amp;#x</summary>
      
    
    
    
    <category term="中间件" scheme="https://trans-xu.github.io/transblog/categories/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
    
  </entry>
  
  <entry>
    <title>Mysql</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/Mysql/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/Mysql/</id>
    <published>2025-08-27T03:50:08.886Z</published>
    <updated>2025-08-27T03:34:47.614Z</updated>
    
    <content type="html"><![CDATA[<h1 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h1><h2 id="sql分类"><a href="#sql分类" class="headerlink" title="sql分类"></a>sql分类</h2><h3 id="DDL："><a href="#DDL：" class="headerlink" title="DDL："></a>DDL：</h3><p>数据定义语言，用来操作数据库、表、列等；常用的命令有：create，drop，alter，truncate、rename</p><h3 id="DML："><a href="#DML：" class="headerlink" title="DML："></a>DML：</h3><p>数据操作语言，用来操作数据库中表里的数据；常用的命令有：insert，update，delete</p><h3 id="DCL："><a href="#DCL：" class="headerlink" title="DCL："></a>DCL：</h3><p>数据控制语言，用来操作访问权限和安全级别； 常用的命令有：grant、deny</p><h3 id="DQL："><a href="#DQL：" class="headerlink" title="DQL："></a>DQL：</h3><p>数据查询语言，用来查询数据；常用的命令有：select</p><h2 id="数据库三大范式"><a href="#数据库三大范式" class="headerlink" title="数据库三大范式"></a>数据库三大范式</h2><h3 id="第一范式（1NF）"><a href="#第一范式（1NF）" class="headerlink" title="第一范式（1NF）"></a>第一范式（1NF）</h3><p><strong>列不可再分</strong><br>每一列属性都是不可再分的属性值，确保每一列的原子性</p><h3 id="第二范式（2NF）"><a href="#第二范式（2NF）" class="headerlink" title="第二范式（2NF）"></a>第二范式（2NF）</h3><p><strong>属性完全依赖于主键</strong><br>第二范式需要确保数据库表中的每一列都和主键相关，而不能只与主键的某一部分相关（主要针对联合主键而言）。也就是说在一个数据库表中，一个表中只能保存一种数据，不可以把多种数据保存在同一张数据库表中。</p><h3 id="第三范式（3NF）"><a href="#第三范式（3NF）" class="headerlink" title="第三范式（3NF）"></a>第三范式（3NF）</h3><p><strong>属性不依赖于其它非主属性，属性直接依赖于主键</strong><br>数据不能存在传递关系，需要确保数据表中的每一列数据都和主键直接相关，而不能间接相关。像：a–&gt;b–&gt;c  属性之间含有这样的关系，是不符合第三范式的。</p><h2 id="Mysql存储"><a href="#Mysql存储" class="headerlink" title="Mysql存储"></a>Mysql存储</h2><h3 id="Mysql的表"><a href="#Mysql的表" class="headerlink" title="Mysql的表"></a>Mysql的表</h3><p>Mysql的表存储在哪里？<br>使用以下命令可以查看mysql存储的路径</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE <span class="string">&#x27;datadir&#x27;</span>;</span><br><span class="line">+---------------+-----------------+</span><br><span class="line">| Variable_name | Value           |</span><br><span class="line">+---------------+-----------------+</span><br><span class="line">| datadir       | /var/lib/mysql/ |</span><br><span class="line">+---------------+-----------------+</span><br><span class="line">1 row <span class="keyword">in</span> <span class="built_in">set</span> (0.00 sec)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>每创建一个 database（数据库） 都会在 &#x2F;var&#x2F;lib&#x2F;mysql&#x2F; 目录里面创建一个以 database 为名的目录，然后保存表结构和表数据的文件都会存放在这个目录里。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> /var/lib/mysql/my_test</span><br><span class="line">db.opt  </span><br><span class="line">t_order.frm  </span><br><span class="line">t_order.ibd</span><br></pre></td></tr></table></figure><ul><li>db.opt，用来存储当前数据库的默认字符集和字符校验规则。</li><li>t_order.frm ，t_order 的表结构会保存在这个文件。在 MySQL 中建立一张表都会生成一个.frm 文件，该文件是用来保存每个表的元数据信息的，主要包含表结构定义。</li><li>t_order.ibd，t_order 的表数据会保存在这个文件。表数据既可以存在共享表空间文件（文件名：ibdata1）里，也可以存放在独占表空间文件（文件名：表名字.ibd）。这个行为是由参数 innodb_file_per_table 控制的，若设置了参数 innodb_file_per_table 为 1，则会将存储的数据、索引等信息单独存储在一个独占表空间，从 MySQL 5.6.6 版本开始，它的默认值就是 1 了，因此从这个版本之后， MySQL 中每一张表的数据都存放在一个独立的 .ibd 文件。</li></ul><h3 id="表空间文件的结构"><a href="#表空间文件的结构" class="headerlink" title="表空间文件的结构"></a>表空间文件的结构</h3><p>表空间由 <strong>段(segment)、区(extent)、页(page)、行(row)</strong> 组成，InnoDB存储引擎的逻辑存储结构大致如下图：</p><p><img data-src="/transblog/images/70203411258998.png"></p><h4 id="行"><a href="#行" class="headerlink" title="行"></a>行</h4><p>数据库表中的记录都是按行（row）进行存放的，每行记录根据不同的行格式，有不同的存储结构。</p><h4 id="页"><a href="#页" class="headerlink" title="页"></a><span id="target1">页</span></h4><p>表中的记录存储在「数据页」，记录是按照行来存储的，但是数据库的读取并不以「行」为单位，否则一次读取（也就是一次 I&#x2F;O 操作）只能处理一行数据，效率会非常低。因此，InnoDB 的数据是按「页」为单位来读写的，也就是说，当需要读一条记录的时候，并不是将这个行记录从磁盘读出来，而是以页为单位，将其整体读入内存。<br>默认每个页的大小为 16KB，也就是最多能保证 16KB 的连续存储空间。页是 InnoDB 存储引擎磁盘管理的最小单元，意味着数据库每次读写都是以 16KB 为单位的，一次最少从磁盘中读取 16K 的内容到内存中，一次最少把内存中的 16K 内容刷新到磁盘中。</p><p><img data-src="/transblog/images/506664309265069.png"></p><p>存储的记录会按照我们指定的行格式存储到 User Records 部分。但是在一开始生成页的时候，其实并没有 User Records 这个部分，每当我们插入一条记录，都会从 Free Space 部分，也就是尚未使用的存储空间中申请一个记录大小的空间划分到 User Records 部分。</p><p>当 Free Space 部分的空间全部被 User Records 部分替代掉之后，也就意味着这个页使用完了，如果还有新的记录插入的话，就需要去申请新的页了。</p><p><img data-src="/transblog/images/539591111267103.png"></p><p>数据页中的记录按照「主键」顺序组成单向链表，单向链表的特点就是插入、删除非常方便，数据页中有一个页目录，起到记录的索引作用</p><p><img data-src="/transblog/images/213291411256327.png"></p><p>页目录创建的过程如下：</p><ul><li>将所有的记录划分成几个组，这些记录包括最小记录和最大记录，但不包括标记为“已删除”的记录；</li><li>每个记录组的最后一条记录就是组内最大的那条记录，并且最后一条记录的头信息中会存储该组一共有多少条记录，作为 n_owned 字段（上图中粉红色字段）</li><li>页目录用来存储每组最后一条记录的地址偏移量，这些地址偏移量会按照先后顺序存储起来，每组的地址偏移量也被称之为槽（slot），每个槽相当于指针指向了不同组的最后一个记录。</li></ul><p>页目录就是由多个槽组成的，槽相当于分组记录的索引。然后，因为记录是按照「主键值」从小到大排序的，所以我们通过槽查找记录时，<strong>可以使用二分法快速定位要查询的记录在哪个槽</strong>，定位到槽后，再遍历槽内的所有记录，找到对应的记录，无需从最小记录开始遍历整个页中的记录链表。</p><h4 id="区"><a href="#区" class="headerlink" title="区"></a>区</h4><p>InnoDB 存储引擎是用 B+ 树来组织数据的。B+ 树中每一层都是通过双向链表连接起来的，如果是以页为单位来分配存储空间，那么链表中相邻的两个页之间的物理位置并不是连续的，可能离得非常远，那么磁盘查询时就会有大量的随机I&#x2F;O，随机 I&#x2F;O 是非常慢的。<br>解决这个问题也很简单，就是让链表中相邻的页的物理位置也相邻，这样就可以使用顺序 I&#x2F;O 了，那么在范围查询（扫描叶子节点）的时候性能就会很高。在表中数据量大的时候，为某个索引分配空间的时候就不再按照页为单位分配了，而是按照区为单位分配。<strong>每个区的大小为 1MB，对于 16KB 的页来说，连续的 64 个页会被划为一个区，这样就使得链表中相邻的页的物理位置也相邻，就能使用顺序 I&#x2F;O 了</strong></p><h4 id="段"><a href="#段" class="headerlink" title="段"></a>段</h4><p>表空间是由各个段组成的，段是由多个区组成的。段一般分为数据段、索引段和回滚段等。</p><ul><li>索引段：存放 B + 树的非叶子节点的区的集合；</li><li>数据段：存放 B + 树的叶子节点的区的集合；</li><li>回滚段：存放的是回滚数据的区的集合</li></ul><h2 id="InnoDB与MyISAM的区别"><a href="#InnoDB与MyISAM的区别" class="headerlink" title="InnoDB与MyISAM的区别"></a>InnoDB与MyISAM的区别</h2><p>InnoDB支持4个事务隔离级别，回滚，崩溃修复能力和多版本并发的事务安全，包括ACID。如果应用中需要执行大量的INSERT或UPDATE操作，则应该使用InnoDB，这样可以提高多用户并发操作的性能</p><p>MyISAM管理非事务表。它提供高速存储和检索，以及全文搜索能力。如果应用中需要执行大量的SELECT查询，那么MyISAM是更好的选择</p><ol><li><p><strong>InnoDB 支持事务，MyISAM 不支持事务。</strong><br>这是 MySQL 将默认存储引擎从 MyISAM 变成 InnoDB 的重要原因之一；</p></li><li><p><strong>InnoDB 支持外键，而 MyISAM 不支持。</strong><br>对一个包含外键的 InnoDB 表转为 MYISAM 会失败；  </p></li><li><p><strong>InnoDB 是聚集索引，MyISAM 是非聚集索引。</strong><br>InnoDB的B+树主键索引的叶子节点就是数据文件，辅助索引的叶子节点是主键的值；而MyISAM的B+树主键索引和辅助索引的叶子节点都是数据文件的地址指针。</p></li><li><p><strong>InnoDB 不保存表的具体行数，执行 select count(*) from table 时需要全表扫描。而MyISAM 用一个变量保存了整个表的行数，执行上述语句时只需要读出该变量即可，速度很快</strong></p></li><li><p><strong>InnoDB 最小的锁粒度是行锁，MyISAM 最小的锁粒度是表锁。一个更新语句会锁住整张表，导致其他查询和更新都会被阻塞，因此并发访问受限。这也是 MySQL 将默认存储引擎从 MyISAM 变成 InnoDB 的重要原因之一；</strong></p></li><li><p><strong>MyISAM支持全文类型索引，而InnoDB不支持全文索引</strong></p></li><li><p><strong>MyISAM相对简单，效率上要优于InnoDB，小型应用可以考虑使用MyISAM</strong></p></li></ol><h1 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h1><p><strong>形象的说就是索引是数据的目录。</strong></p><h2 id="索引的本质"><a href="#索引的本质" class="headerlink" title="索引的本质"></a>索引的本质</h2><h3 id="索引–B-树"><a href="#索引–B-树" class="headerlink" title="索引–B+ 树"></a>索引–B+ 树</h3><p><strong>InnoDB 里的 B+ 树中的每个节点都是一个数据页</strong></p><p>聚簇索引：</p><p><img data-src="/transblog/images/352001811252081.png"></p><p>非聚簇索引：</p><p><img data-src="/transblog/images/461752211269961.png"></p><p><strong>什么是B+树？</strong></p><ul><li>叶子节点才存放了数据，包括了所有的索引值信息和数据，非叶子节点仅用来存放目录项作为索引。</li><li>非叶子节点分为不同层次，通过分层来降低每一层的搜索量；</li><li>所有节点按照索引键大小排序，构成一个双向链表，便于范围查询；</li></ul><h3 id="索引数据结构设计"><a href="#索引数据结构设计" class="headerlink" title="索引数据结构设计"></a>索引数据结构设计</h3><p>磁盘读写的最小单位是扇区，扇区的大小只有 512B 大小，操作系统一次会读写多个扇区，所以操作系统的最小读写单位是块（Block）。Linux 中的块大小为 4KB，也就是一次磁盘 I&#x2F;O 操作会直接读写 8 个扇区。</p><p>MySQL中的数据一般是放在磁盘中的，读取数据的时候肯定会有访问磁盘的操作，磁盘中有两个机械运动的部分，分别是盘片旋转和磁臂移动。盘片旋转就是我们市面上所提到的多少转每分钟，而磁盘移动则是在盘片旋转到指定位置以后，移动磁臂后开始进行数据的读写。那么这就存在一个定位到磁盘中的块的过程，而定位是磁盘的存取中花费时间比较大的一块，毕竟机械运动花费的时候要远远大于电子运动的时间。当大规模数据存储到磁盘中的时候，显然定位是一个非常花费时间的过程。</p><p>因此当我们通过索引查找某行数据的时候，就需要先从磁盘读取索引到内存，再通过索引从磁盘中找到某行数据，然后读入到内存，也就是说查询过程中会发生多次磁盘 I&#x2F;O，而磁盘 I&#x2F;O 次数越多，所消耗的时间也就越大。所以操作效率取决于访问磁盘的次数，B+ Tree的高度远远小于红黑树的高度，高度越小，磁盘I&#x2F;O所花的时间越少。</p><ol><li>尽量多的在结点上存储相关的信息，保证层数尽量的少，以便可以更快的找到信息，能在尽可能少的磁盘的 I&#x2F;O 操作中完成查询工作；</li><li>要能高效地查询某一个记录，也要能高效地执行范围查找；</li><li>保证了每个查询是稳定的，每个结点到叶子结点的高度都是相同，B+ Tree刚好是平衡树</li></ol><h3 id="B-Tree-vs-二叉树"><a href="#B-Tree-vs-二叉树" class="headerlink" title="B+Tree vs 二叉树"></a>B+Tree vs 二叉树</h3><ol><li>对于有 N 个叶子节点的 B+Tree，其搜索复杂度为O(logdN)，其中 d 表示节点允许的最大子节点个数为 d 个。</li><li>在实际的应用当中， d 值是大于100的，这样就保证了，<strong>即使数据达到千万级别时，B+Tree 的高度依然维持在 3~4 层左右</strong>，也就是说一次数据查询操作只需要做 3~4 次的磁盘 I&#x2F;O 操作就能查询到目标数据。</li><li>而二叉树的每个父节点的儿子节点个数只能是 2 个，意味着其搜索复杂度为 O(logN)，这已经比 B+Tree 高出不少，因此二叉树检索到目标数据所经历的磁盘 I&#x2F;O 次数要更多。</li></ol><h3 id="B-Tree-vs-B-Tree"><a href="#B-Tree-vs-B-Tree" class="headerlink" title="B+Tree vs B Tree"></a>B+Tree vs B Tree</h3><p><strong>为什么说B+树比B树更适合做索引</strong></p><ol><li>B+Tree 只在叶子节点存储数据，<strong>而 B 树 的非叶子节点也要存储数据，所以 B+Tree 的单个节点的数据量更小</strong>，在相同的磁盘 I&#x2F;O 次数下，就能查询更多的节点。</li><li>B+Tree 叶子节点采用的是双链表连接，数据都存储在叶子结点中，方便扫库，不用跨层，只需要扫一遍叶子结点即可，<strong>适合 MySQL 中常见的基于范围的顺序查找</strong>，而 B 树无法做到这一点。因为其分支结点同样存储着数据，要找到具体的数据，需要进行一次中序遍历按序来扫，例如select，很多时候会选多条，比如按照id排序后选10条。<strong>B Tree需要做局部的中序遍历，可能要跨层访问，所以B+树更加适合在区间查询的情况，所以通常B+树用于数据库索引。</strong></li><li>B+Tree 的查询效率更加稳定，由于非终结点并不是最终指向文件内容的结点，而只是叶子结点中关键字的索引。<strong>任何关键字的查找必须走一条从根结点到叶子结点的路，所有关键字查询的路径长度相同，每一个数据的查询效率相当</strong>。</li></ol><h3 id="B-Tree-vs-Hash"><a href="#B-Tree-vs-Hash" class="headerlink" title="B+Tree vs Hash"></a>B+Tree vs Hash</h3><p><strong>为什么数据库索引不采用hash</strong></p><ol><li>Hash不能进行范围查找，如果只选一个数据，搜索复杂度为 O(1)，确实更快。但是数据库中经常会选择多条，这时候由于B+树索引有序，并且又有链表相连，它的查询效率比hash就快很多了。</li><li>数据库中的索引一般是在磁盘上，使用Hash时，数据量大的情况可能无法一次装入内存，B+ Tree的设计可以允许数据分批加载，同时树的高度较低，提高查找效率。</li><li>当数据量很大时，Hash冲突的概率也会非常大</li></ol><h2 id="索引的分类"><a href="#索引的分类" class="headerlink" title="索引的分类"></a>索引的分类</h2><h3 id="【数据结构】分类："><a href="#【数据结构】分类：" class="headerlink" title="【数据结构】分类："></a>【数据结构】分类：</h3><h4 id="B-tree索引"><a href="#B-tree索引" class="headerlink" title="B+tree索引"></a>B+tree索引</h4><p>创建的聚簇索引和非聚簇索引默认使用的是 B+Tree 索引</p><h4 id="Hash索引"><a href="#Hash索引" class="headerlink" title="Hash索引"></a>Hash索引</h4><p>不再赘述，自己查询资料</p><h4 id="Full-text索引"><a href="#Full-text索引" class="headerlink" title="Full-text索引"></a>Full-text索引</h4><p>不再赘述，自己查询资料</p><p><img data-src="/transblog/images/17315911246865.png"></p><h3 id="【物理存储】分类："><a href="#【物理存储】分类：" class="headerlink" title="【物理存储】分类："></a>【物理存储】分类：</h3><h4 id="聚簇索引"><a href="#聚簇索引" class="headerlink" title="聚簇索引"></a>聚簇索引</h4><p><img data-src="/transblog/images/408423214267031.png"></p><h4 id="非聚簇索引（二级索引、辅助索引）"><a href="#非聚簇索引（二级索引、辅助索引）" class="headerlink" title="非聚簇索引（二级索引、辅助索引）"></a>非聚簇索引（二级索引、辅助索引）</h4><p><img data-src="/transblog/images/557233914259700.png"></p><p><strong>聚集索引和非聚集索引区别</strong></p><ul><li>聚集索引物理上连续存在，按照索引排序，索引的键值逻辑顺序决定了表数据行的物理存储顺序。非聚集索引是逻辑上的连续，物理存储并不连续</li><li>聚集索引一个表只能有一个，而非聚集索引一个表可以存在多个</li><li><strong>聚集索引的叶节点就是数据节点。而非聚簇索引的叶节点仍然是索引节点（主键值），而不是实际数据，只不过有一个指针指向对应的数据块</strong>。</li></ul><h4 id="什么是回表"><a href="#什么是回表" class="headerlink" title="什么是回表"></a><span id="target6">什么是回表</span></h4><p>总结字面意思是再查表的意思，二次查询操作。</p><ul><li>如果查询通过主键查询，因为主键索引节点包含当行数据所有记录值，所有一次就能查询出来。</li><li>那么如果不是主键索引，查询索引以外的字段值，索引节点并没有存储它，所有会通过索引节点存储的主键值再去查询一次。这就是回表操作</li></ul><p><img data-src="/transblog/images/403523609240644.png"></p><h5 id="解决回表问题–覆盖索引"><a href="#解决回表问题–覆盖索引" class="headerlink" title="解决回表问题–覆盖索引"></a>解决回表问题–覆盖索引</h5><p>覆盖索引：即指索引包含了查询所需的所有列，那么也不会出现回表操作，因为可以直接通过索引获取所需的列数据。这可以进一步提高查询性能和效率。简单来讲，把所有列变成联合索引。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> employees (</span><br><span class="line">  employee_id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">  first_name <span class="type">VARCHAR</span>(<span class="number">50</span>),</span><br><span class="line">  last_name <span class="type">VARCHAR</span>(<span class="number">50</span>),</span><br><span class="line">  salary <span class="type">INT</span></span><br><span class="line">);</span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_covering_index <span class="keyword">ON</span> employees(first_name, last_name, salary);</span><br></pre></td></tr></table></figure><h3 id="【字段特性】分类："><a href="#【字段特性】分类：" class="headerlink" title="【字段特性】分类："></a>【字段特性】分类：</h3><h4 id="主键索引"><a href="#主键索引" class="headerlink" title="主键索引"></a>主键索引</h4><p>索引列的值不允许有空值。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> table_name  (</span><br><span class="line">  ....</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (index_column_1) <span class="keyword">USING</span> BTREE</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p><strong>主键索引与聚集索引的区别</strong></p><ul><li>innodb引擎里面，主键的确就是聚集索引，但是myisam引擎里面主键也不是聚集索引。</li><li>聚集索引决定了数据库的物理存储结构，而主键只是确定表格逻辑组织方式。这两者不可混淆，<strong>字段特性与物理存储没有必然联系</strong></li></ul><h4 id="唯一索引"><a href="#唯一索引" class="headerlink" title="唯一索引"></a>唯一索引</h4><p>唯一索引建立在 UNIQUE 字段上的索引，一张表可以有多个唯一索引，索引列的值必须唯一，但是允许有空值。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> table_name  (</span><br><span class="line">  ....</span><br><span class="line">  <span class="keyword">UNIQUE</span> KEY(index_column_1,index_column_2,...) </span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> INDEX index_name</span><br><span class="line"><span class="keyword">ON</span> table_name(index_column_1,index_column_2,...); </span><br></pre></td></tr></table></figure><h4 id="普通索引"><a href="#普通索引" class="headerlink" title="普通索引"></a>普通索引</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> table_name  (</span><br><span class="line">  ....</span><br><span class="line">  INDEX(index_column_1,index_column_2,...) </span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> INDEX index_name</span><br><span class="line"><span class="keyword">ON</span> table_name(index_column_1,index_column_2,...); </span><br></pre></td></tr></table></figure><h4 id="前缀索引"><a href="#前缀索引" class="headerlink" title="前缀索引"></a>前缀索引</h4><p>前缀索引是指对字符类型字段的前几个字符建立的索引，而不是在整个字段上建立的索引，前缀索引可以建立在字段类型为 char、 varchar、binary、varbinary 的列上。使用前缀索引的目的是为了减少索引占用的存储空间，提升查询效率。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> table_name(</span><br><span class="line">    column_list,</span><br><span class="line">    INDEX(column_name(length))</span><br><span class="line">); </span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> INDEX index_name</span><br><span class="line"><span class="keyword">ON</span> table_name(column_name(length)); </span><br></pre></td></tr></table></figure><h3 id="【字段个数】分类："><a href="#【字段个数】分类：" class="headerlink" title="【字段个数】分类："></a>【字段个数】分类：</h3><h4 id="单列索引"><a href="#单列索引" class="headerlink" title="单列索引"></a>单列索引</h4><p>单列的索引，不再赘述</p><h4 id="联合索引"><a href="#联合索引" class="headerlink" title="联合索引"></a>联合索引</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX index_product_no_name <span class="keyword">ON</span> product(product_no, name);</span><br></pre></td></tr></table></figure><h5 id="最左匹配原则"><a href="#最左匹配原则" class="headerlink" title="最左匹配原则"></a>最左匹配原则</h5><p>按照最左优先的方式进行索引的匹配。在使用联合索引进行查询的时候，如果不遵循「最左匹配原则」，联合索引会失效，这样就无法利用到索引快速查询的特性了。</p><ul><li><strong>&gt;、&lt;、like 的%放在左边，会中断联合索引</strong></li><li><strong>&gt;&#x3D;、&lt;&#x3D;、between、like的%只出现在右边，不会中断联合索引</strong></li></ul><p><img data-src="/transblog/images/229160317268672.png"></p><p> a，b排序分析：</p><ul><li>a 排序：1，1，2，2，3，3(有序排列)</li><li>b 排序：1，2，1，4，1，2(无序排列)</li></ul><p>直观的可以发现 a 是有序的，b 是无序的，再进一步发现<strong>在 a 确定的情况下，b 也是有序的</strong><br> <strong>select * from t_table where a &gt; 1 and b &#x3D; 2，联合索引（a, b）哪一个字段用到了联合索引的 B+Tree？</strong><br>联合索引先按照 a 字段的值排序的，但是在符合 a &gt; 1 条件的二级索引记录的范围里，b 字段的值是无序的。因此，这条查询语句只有 a 字段用到了联合索引进行索引查询，而 b 字段并没有使用到联合索引。</p><p> <strong>select * from t_table where a &gt;&#x3D; 1 and b &#x3D; 2，联合索引（a, b）哪一个字段用到了联合索引的 B+Tree？</strong><br>由于使用了&gt;&#x3D;，虽然在符合 a&gt;&#x3D; 1 条件的二级索引记录的范围里，b 字段的值是「无序」的，但是对于符合 a &#x3D; 1 的二级索引记录的范围里，b 字段的值是「有序」的。<br>当二级索引记录的 a 字段值为 1 时，可以通过 b &#x3D; 2 条件减少需要扫描的二级索引记录范围。也就是说，从符合 a &#x3D; 1 and b &#x3D; 2 条件的第一条记录开始扫描，而不需要从第一个 a 字段值为 1 的记录开始扫描。</p><hr><p>再比如，如果创建了一个 (a, b, c) 联合索引：</p><ul><li>where b&#x3D;2；</li><li>where c&#x3D;3；</li><li>where b&#x3D;2 and c&#x3D;3；</li></ul><p>上面这些查询条件联合索引之所以会失效，是因为(a, b, c) 联合索引，是先按 a 排序，在 a 相同的情况再按 b 排序，在 b 相同的情况再按 c 排序。所以，<strong>b 和 c 是全局无序，局部相对有序的</strong>，这样在没有遵循最左匹配原则的情况下，是无法利用到索引的。</p><p>更多补充：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">1</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> b<span class="operator">=</span><span class="number">5</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">4</span>;</span><br><span class="line"># abc 三列都使用索引，而且都有效</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">2</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span>  c<span class="operator">=</span><span class="number">4</span> <span class="keyword">and</span> b<span class="operator">=</span><span class="number">6</span> <span class="keyword">and</span> a<span class="operator">=</span><span class="number">3</span>;</span><br><span class="line"># mysql没有那么笨，不会因为书写顺序而无法识辨索引。</span><br><span class="line"># <span class="keyword">where</span>里面的条件顺序在查询之前会被mysql自动优化，效果跟上一句一样。</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">3</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">7</span>;</span><br><span class="line"># a用到索引，<span class="keyword">sql</span>中没有使用 b列，b列中断，c没有用到索引</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">4</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> b<span class="operator">&gt;</span><span class="number">7</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">3</span>;</span><br><span class="line"># a用到索引，b也用到索引，c没有用到。</span><br><span class="line"># 因为b是范围索引，所以b列中断，复合索引中后序的列即使出现，索引也是无效的。</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">5</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> b<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">4</span>;</span><br><span class="line"># <span class="keyword">sql</span>中没有使用a列，所以b，c就无法使用到索引</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">6</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">&gt;</span><span class="number">4</span> <span class="keyword">and</span> b<span class="operator">=</span><span class="number">7</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">9</span>;</span><br><span class="line"># a用到索引，a是范围索引，索引在a处中断，b、c没有使用索引</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">7</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">order</span> <span class="keyword">by</span> b;</span><br><span class="line"># a用到了索引，b在结果排序中也用到了索引的效果。前面说过，a下面任意一段的b是排好序的</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">8</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">order</span> <span class="keyword">by</span> c;</span><br><span class="line"># a 用到了索引，<span class="keyword">sql</span>中没有使用 b列，索引中断，c处没有使用索引，在 Extra列 可以看到 filesort</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">9</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> b<span class="operator">=</span><span class="number">3</span> <span class="keyword">order</span> <span class="keyword">by</span> a;</span><br><span class="line"># 此<span class="keyword">sql</span>中，先b，后a，导致 b<span class="operator">=</span><span class="number">3</span> 索引无效，排序a也索引无效。</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">10</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> c<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> a<span class="operator">=</span><span class="number">7</span>;</span><br><span class="line"># (<span class="number">3</span>)的衍生例子，a 用到索引。原因同(<span class="number">2</span>)</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">11</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> b<span class="operator">&gt;=</span><span class="number">7</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">3</span>;</span><br><span class="line"># (<span class="number">4</span>)的衍生例子，但 a、b、c 都用到索引，其中b<span class="operator">=</span><span class="number">7</span>用了索引</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">12</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> b <span class="keyword">between</span> <span class="number">2</span> <span class="keyword">and</span> <span class="number">5</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">3</span>;</span><br><span class="line"># (<span class="number">4</span>)的衍生例子，但 a、b、c 都用到索引</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">13</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> b <span class="keyword">like</span> <span class="string">&#x27;bbb%&#x27;</span> <span class="keyword">and</span> c<span class="operator">=</span><span class="number">3</span>; —— 假设b字段临时变为<span class="type">varchar</span></span><br><span class="line"># (<span class="number">4</span>)的衍生例子，但 a、b、c 都用到索引</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">14</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> mytable <span class="keyword">where</span> a<span class="operator">=</span><span class="number">3</span> <span class="keyword">and</span> b <span class="keyword">in</span> (<span class="number">7</span>,<span class="number">8</span>) <span class="keyword">and</span> c<span class="operator">=</span><span class="number">3</span>;</span><br><span class="line"># abc都用到索引，<span class="keyword">in</span>不会中断索引匹配</span><br></pre></td></tr></table></figure><h5 id="联合索引树"><a href="#联合索引树" class="headerlink" title="联合索引树"></a>联合索引树</h5><p><img data-src="/transblog/images/169701210240646.png"><br><mark>第一个问题：where c 这个条件并不符合联合索引的最左匹配原则，怎么就查询的时候走了索引呢？</mark></p><ul><li><p>首先，这张表的字段没有「非索引」字段，所以 select * 相当于 select id,a,b,c，然后<strong>这个查询的数据都在二级索引的 B+ 树，因为二级索引的 B+ 树的叶子节点包含「索引值+主键值」，所以查二级索引的 B+ 树就能查到全部结果了，这个就是覆盖索引</strong>。<a href="#target6">点击此处跳转到回表</a></p></li><li><p><strong>执行计划里的 type 是 index，这代表着是通过全扫描联合索引树的方式查询到数据的</strong>，这是因为 where c 并不符合联合索引最左匹配原则。那么，<strong>如果写了个符合最左原则的 select 语句，那么 type 就是 ref</strong>，这个效率就比 index 全扫描要高一些。<a href="#target7">点击此处跳转到执行计划–type</a></p></li><li><ul><li>为什么选择全扫描二级索引树，而不扫描聚簇索引树呢？</li></ul></li><li><ul><li>因为二级索引树的记录东西很少，就只有「索引列+主键值」，而聚簇索引记录的东西会更多，比如聚簇索引中的叶子节点则记录了主键值、事务 id、用于事务和 MVCC 的回滚指针以及所有的剩余列。再加上，这个 select * 不用执行回表操作。所以， MySQL 优化器认为直接遍历二级索引树要比遍历聚簇索引树的成本要小的多，因此 MySQL 选择了「全扫描二级索引树」的方式查询数据。</li></ul></li></ul><p><mark>第二个问题：在这个数据表加了非索引字段，执行同样的查询语句后，怎么变成走的是全表扫描呢？</mark></p><ul><li>因为加了其他字段（列）后，select * from t where c &#x3D; 0; 查询的内容就不能在联合索引树里找到了，而且条件也不符合最左匹配原则，这样<strong>既不能覆盖索引也不能执行回表操作，所以这时只能通过扫描全表来查询到所有的数据</strong>。</li></ul><h2 id="如何优化索引"><a href="#如何优化索引" class="headerlink" title="如何优化索引?"></a>如何优化索引?</h2><h3 id="使用前缀索引"><a href="#使用前缀索引" class="headerlink" title="使用前缀索引"></a>使用前缀索引</h3><p>使用前缀索引是为了减小索引字段大小，可以增加一个索引页中存储的索引值，有效提高索引的查询速度。在一些大字符串的字段作为索引时，使用前缀索引可以帮助我们减小索引项的大小。<br>不过，前缀索引有一定的局限性，例如：</p><ul><li>order by 就无法使用前缀索引；</li><li>无法把前缀索引用作覆盖索引；</li></ul><h3 id="使用覆盖索引"><a href="#使用覆盖索引" class="headerlink" title="使用覆盖索引"></a>使用覆盖索引</h3><p>覆盖索引是指 SQL 中 query 的所有字段，在索引 B+Tree 的叶子节点上都能找得到的那些索引，从二级索引中查询得到记录，而不需要通过聚簇索引查询获得，可以避免回表的操作。<a href="#target6">点击此处跳转到回表</a><br>假设我们只需要查询商品的名称、价格，有什么方式可以避免回表呢？我们可以建立一个联合索引，即「商品ID、名称、价格」作为一个联合索引。如果索引中存在这些数据，查询将不会再次检索主键索引，从而避免回表。<br>所以，使用覆盖索引的好处就是，不需要查询出包含整行记录的所有信息，也就减少了大量的 I&#x2F;O 操作。</p><h3 id="主键索引最好是自增的"><a href="#主键索引最好是自增的" class="headerlink" title="主键索引最好是自增的"></a>主键索引最好是自增的</h3><p>如果我们使用自增主键，那么每次插入的新数据就会按顺序添加到当前索引节点的位置，不需要移动已有的数据，当页面写满，就会自动开辟一个新页面。因为每次插入一条新记录，都是追加操作，不需要重新移动数据，因此这种插入数据的方法效率非常高。<br>如果我们使用非自增主键，由于每次插入主键的索引值都是随机的，因此每次插入新的数据时，就可能会插入到现有数据页中间的某个位置，这将不得不移动其它数据来满足新数据的插入，甚至需要从一个页面复制数据到另外一个页面，我们通常将这种情况称为页分裂。页分裂还有可能会造成大量的内存碎片，导致索引结构不紧凑，从而影响查询效率。</p><h3 id="索引最好设置为-NOT-NULL"><a href="#索引最好设置为-NOT-NULL" class="headerlink" title="索引最好设置为 NOT NULL"></a>索引最好设置为 NOT NULL</h3><p>第一原因：索引列存在 NULL 就会导致优化器在做索引选择的时候更加复杂，更加难以优化，因为可为 NULL 的列会使索引、索引统计和值比较都更复杂，比如进行索引统计时，count 会省略值为NULL 的行。</p><p>第二个原因：NULL 值是一个没意义的值，但是它会占用物理空间，所以会带来的存储空间的问题，因为 InnoDB 存储记录的时候，如果表中存在允许为 NULL 的字段，那么行格式中至少会用 1 字节空间存储 NULL 值列表，如下图的紫色部分：</p><p><img data-src="/transblog/images/595990111246937.png"></p><h3 id="避免索引失效"><a href="#避免索引失效" class="headerlink" title="避免索引失效"></a>避免索引失效</h3><h4 id="索引失效常见场景"><a href="#索引失效常见场景" class="headerlink" title="索引失效常见场景"></a>索引失效常见场景</h4><ol><li><strong>对索引使用左或者左右模糊匹配</strong><br>like %xx 或者 like %xx%这两种方式都会造成索引失效；</li><li><strong>在查询条件中对索引列做了计算、函数、隐式类型转换、表达式计算操作</strong><br>这些情况下都会造成索引失效；</li><li><strong>联合索引违背最左匹配原则，就会导致索引失效</strong><br>联合索引要能正确使用需要遵循最左匹配原则，也就是按照最左优先的方式进行索引的匹配。</li><li><strong>使用 OR 连接多个条件</strong><br>在 WHERE 子句中，如果在 OR 前的条件列是索引列，而在 OR 后的条件列不是索引列，那么索引会失效。</li></ol><h2 id="InnoDB创建索引的策略"><a href="#InnoDB创建索引的策略" class="headerlink" title="InnoDB创建索引的策略"></a>InnoDB创建索引的策略</h2><p>在创建表时，InnoDB 存储引擎会根据不同的场景选择不同的列作为索引：</p><ul><li>如果有主键，默认会使用主键作为聚簇索引的索引键；</li><li>如果没有主键，就选择第一个不包含 NULL 值的唯一列作为聚簇索引的索引键；</li><li>在上面两个都没有的情况下，InnoDB 将自动生成一个隐式自增 id 列作为聚簇索引的索引键；这个隐藏的主键是每行数据内置的6字节ROWID，该列的值会随着数据的插入自增</li></ul><h2 id="什么时候不需要创建索引"><a href="#什么时候不需要创建索引" class="headerlink" title="什么时候不需要创建索引"></a>什么时候不需要创建索引</h2><ol><li>WHERE 条件，GROUP BY，ORDER BY 里用不到的字段，索引的价值是快速定位，如果起不到定位的字段通常是不需要创建索引的，因为索引是会占用物理空间的。</li><li>字段中存在大量重复数据，不需要创建索引，比如性别字段，只有男女，如果数据库表中，男女的记录分布均匀，那么无论搜索哪个值都可能得到一半的数据。在这些情况下，还不如不要索引，因为 MySQL 还有一个查询优化器，查询优化器发现某个值出现在表的数据行中的百分比很高的时候，它一般会忽略索引，进行全表扫描。</li><li>表数据太少的时候，不需要创建索引；</li><li>经常更新的字段不用创建索引，比如不要对电商项目的用户余额建立索引，因为索引字段频繁修改，由于要维护 B+Tree的有序性，那么就需要频繁的重建索引，这个过程是会影响数据库性能的。</li></ol><h1 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h1><h2 id="缓冲池-Buffer-Pool"><a href="#缓冲池-Buffer-Pool" class="headerlink" title="缓冲池(Buffer Pool)"></a>缓冲池(Buffer Pool)</h2><p><img data-src="/transblog/images/530493514256338.png"></p><p>虽然说 MySQL 的数据是存储在磁盘里的，但是也不能每次都从磁盘里面读取数据，这样性能是极差的。要想提升查询性能，加个缓存就行了。<em>（可参考操作系统–内存篇–内存的预读失效和缓存污染）</em></p><h3 id="Buffer-Pool存什么"><a href="#Buffer-Pool存什么" class="headerlink" title="Buffer Pool存什么"></a>Buffer Pool存什么</h3><p>Buffer Pool 同样需要按「页」来划分。Buffer Pool 除了缓存「索引页」和「数据页」，还包括了 undo 页，插入缓存、自适应哈希索引、锁信息等等。</p><p>为了更好的管理这些在 Buffer Pool 中的缓存页，InnoDB 为每一个缓存页都创建了一个控制块，控制块信息包括「缓存页的表空间、页号、缓存页地址、链表节点」等等。控制块也是占有内存空间的，它是放在 Buffer Pool 的最前面，接着才是缓存页</p><p><img data-src="/transblog/images/436033415269972.png"></p><p>控制块和缓存页之间灰色部分称为碎片空间。当查询一条记录时，InnoDB 是会把整个页的数据加载到 Buffer Pool 中，因为，<strong>通过索引只能定位到磁盘中的页，而不能定位到页中的一条记录</strong>。将页加载到 Buffer Pool 后，再通过页里的页目录去定位到某条具体的记录。</p><h3 id="Buffer-Pool的大小"><a href="#Buffer-Pool的大小" class="headerlink" title="Buffer Pool的大小"></a>Buffer Pool的大小</h3><p>Buffer Pool 在 MySQL 启动的时候，向操作系统申请一片连续的内存空间，默认配置下 Buffer Pool 只有 128MB 。</p><p>可以通过调整<code>innodb_buffer_pool_size</code>参数来设置 Buffer Pool 的大小，一般建议设置成可用物理内存的 60%~80%。</p><h2 id="Buffer-Pool管理"><a href="#Buffer-Pool管理" class="headerlink" title="Buffer Pool管理"></a>Buffer Pool管理</h2><h3 id="LRU-list"><a href="#LRU-list" class="headerlink" title="LRU list"></a>LRU list</h3><ul><li>LRU算法是基于最近使用时间，其核心思想是<strong>淘汰最长时间未被使用的数据</strong>，这适用于访问模式具有时间局部性的场景</li><li>LFU算法是基于访问频率，其核心思想是<strong>淘汰访问频率最低的数据</strong>，这适用于访问模式具有频率局部性的场景。</li></ul><p><strong>以上问题十分常见，包括操作系统，redis，mysql等</strong>，<mark>Redis实现 LFU 算法，MySql 和 Linux 操作系统是改进 LRU 算法</mark></p><blockquote><p><em>有关Redis如何实现LFU，请参照Redis–Redis的内存策略–LRU和LFU</em><br><em>有关操作系统如何改进LRU，请参照操作系统–内存篇</em></p></blockquote><p>最频繁使用的页在LRU列表的前端，最少使用的在LRU列表的尾端，首先释放LRU尾端的页<br><img data-src="/transblog/images/328330515252092.png"></p><h4 id="解决预读失效"><a href="#解决预读失效" class="headerlink" title="解决预读失效"></a><font color="red">解决预读失效</font></h4><p><strong>预读失效：读磁盘多读了一些，但是没有用到</strong></p><p>MySQL 的 Innodb 存储引擎是在一个 LRU 链表上划分来 2 个区域：young 区域 和 old 区域。预读的页就只需要加入到 old 区域的头部，当页被真正访问的时候，才将页插入 young 区域的头部。</p><p>两个区域的分界点位置，一般叫做midpoint。在默认配置下，该位置在LRU列表长度的5／8处。由参数<code>innodb_old_blocks_pct</code>控制</p><p><img data-src="/transblog/images/26395211252351.png"></p><h4 id="解决缓存污染"><a href="#解决缓存污染" class="headerlink" title="解决缓存污染"></a><font color="red">解决缓存污染</font></h4><p><strong>缓存污染：批量读，把热点数据挤出去</strong></p><p>MySQL Innodb：在内存页被访问第二次的时候，并不会马上将该页从 old 区域升级到 young 区域，因为还要<strong>进行停留在 old 区域的时间判断</strong><br>如果第二次的访问时间与第一次访问的时间在 1 秒内（默认值），那么该页就不会被从 old 区域升级到 young 区域；<br>如果第二次的访问时间与第一次访问的时间超过 1 秒，那么该页就会从 old 区域升级到 young 区域；</p><h3 id="空闲页管理-Free-list"><a href="#空闲页管理-Free-list" class="headerlink" title="空闲页管理(Free list)"></a>空闲页管理(Free list)</h3><p>为了能够快速找到空闲的缓存页，可以使用链表结构，将空闲缓存页的「控制块」作为链表的节点，这个链表称为 Free 链表（空闲链表）。</p><p><img data-src="/transblog/images/55073915267576.png"></p><ul><li>当数据库刚启动时，LRU列表是空的，即没有任何的页。这时页都存放在Free列表中。</li><li>Free 链表上除了有控制块，还有一个头节点，该头节点包含链表的头节点地址，尾节点地址，以及当前链表中节点的数量等信息。</li><li>Free 链表节点是一个一个的控制块，而每个控制块包含着对应缓存页的地址，所以相当于 Free 链表节点都对应一个空闲的缓存页。</li><li>每当需要从磁盘中加载一个页到 Buffer Pool 中时，就从 Free链表中取一个空闲的缓存页，放入到LRU列表中。并且把该缓存页对应的控制块的信息填上，然后把该缓存页对应的控制块从 Free 链表中移除。</li></ul><h3 id="脏页管理-Flush-List"><a href="#脏页管理-Flush-List" class="headerlink" title="脏页管理(Flush List)"></a>脏页管理(Flush List)</h3><ul><li>更新数据的时候，不需要每次都要写入磁盘，而是将 Buffer Pool 对应的缓存页标记为脏页，然后再由后台线程将脏页写入到磁盘。</li><li><strong>Flush 链表的元素都是脏页，脏页既存在于LRU列表中，也存在于Flush列表中</strong></li><li><strong>页从缓冲池刷新回磁盘的操作并不是在每次页发生更新时触发，而是通过Checkpoint机制将脏页刷新回磁盘</strong></li></ul><p><img data-src="/transblog/images/182285015265078.png"></p><h1 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h1><ul><li>在执行一条“增删改”语句的时候，虽然没有输入 begin 开启事务和 commit 提交事务，但是 MySQL 会隐式开启事务来执行的，执行完就自动提交事务的，所以我们可以及时在数据库表看到结果。</li><li>执行一条语句是否自动提交事务，是由 autocommit 参数决定的，默认是开启。所以，执行一条 update 语句也是会使用事务的。</li><li>事务是数据库操作的最小工作单元</li></ul><p>注意：执行「开始事务」命令，并不意味着启动了事务。在 MySQL 有两种开启事务的命令，这两种开启事务的命令，事务的启动时机是不同的：</p><ol><li>begin&#x2F;start transaction 命令<br>执行了 begin&#x2F;start transaction 命令后，并不代表事务启动了。只有在执行这个命令后，执行了第一条 select 语句，才是事务真正启动的时机；</li><li>start transaction with consistent snapshot 命令<br>执行了 start transaction with consistent snapshot 命令，就会马上启动事务。</li></ol><h2 id="事务的四种特性"><a href="#事务的四种特性" class="headerlink" title="事务的四种特性"></a>事务的四种特性</h2><p><strong>ACID</strong></p><ol><li><strong>原子性</strong> <em>(Atomicity)</em><br>原子性是指事务包含的所有操作要么全部成功，要么全部失败回滚，因此事务的操作如果成功就必须要完全应用到数据库，如果操作失败则不能对数据库有任何影响。</li><li><strong>一致性</strong> <em>(Consistency)</em><br>一致性是指事务必须使数据库从一个一致性状态变换到另一个一致性状态，也就是说一个事务执行之前和执行之后都必须处于一致性状态。<br>拿转账来说，假设用户A和用户B两者的钱加起来一共是5000，那么不管A和B之间如何转账，转几次账，事务结束后两个用户的钱相加起来应该还得是5000，这就是事务的一致性。</li><li><strong>隔离性</strong> <em>(Isolation)</em><br>隔离性是当多个用户并发访问数据库时，比如操作同一张表时，数据库为每一个用户开启的事务，不能被其他事务的操作所干扰，多个并发事务之间要相互隔离。</li><li><strong>持久性</strong> <em>(Durability)</em><br>持久性是指一个事务一旦被提交了，那么对数据库中的数据的改变就是永久性的，即便是在数据库系统遇到故障的情况下也不会丢失提交事务的操作。</li></ol><h3 id="InnoDB引擎如何保证四个特性"><a href="#InnoDB引擎如何保证四个特性" class="headerlink" title="InnoDB引擎如何保证四个特性"></a>InnoDB引擎如何保证四个特性</h3><ul><li>原子性是通过 undo log（回滚日志） 来保证的；</li><li>持久性是通过 redo log （重做日志）来保证的；</li><li>隔离性是通过 MVCC（多版本并发控制） 或锁机制来保证的；</li><li>一致性则是通过持久性+原子性+隔离性来保证；</li></ul><h2 id="并行事务导致的问题"><a href="#并行事务导致的问题" class="headerlink" title="并行事务导致的问题"></a>并行事务导致的问题</h2><h3 id="脏读"><a href="#脏读" class="headerlink" title="脏读"></a>脏读</h3><p>脏读就是指当一个事务正在访问数据，并且对数据进行了修改，而这种修改还没有提交到数据库中，这时，另外一个事务也访问这个数据，然后使用了这个数据。</p><h3 id="不可重复读"><a href="#不可重复读" class="headerlink" title="不可重复读"></a>不可重复读</h3><p>不可重复读是指在对于数据库中的某个数据，一个事务范围内多次查询却返回了不同的数据值，这是由于在查询间隔，被另一个事务修改并提交了。</p><p>例如事务T1在读取某一数据，而事务T2立马修改了这个数据并且提交事务给数据库，事务T1再次读取该数据就得到了不同的结果，发送了不可重复读。</p><p><mark>不可重复读和脏读的区别是，脏读是某一事务读取了另一个事务未提交的脏数据，而不可重复读则是读取了前一事务提交的数据。</mark></p><h3 id="幻读"><a href="#幻读" class="headerlink" title="幻读"></a>幻读</h3><p>幻读是事务非独立执行时发生的一种现象。</p><p>例如事务T1对一个表中所有的行的某个数据项做了从“1”修改为“2”的操作，这时事务T2又对这个表中插入了一行数据项，而这个数据项的数值还是为“1”并且提交给数据库。而操作事务T1的用户如果再查看刚刚修改的数据，会发现还有一行没有修改，其实这行是从事务T2中添加的，就好像产生幻觉一样，这就是发生了幻读。</p><p><mark>幻读和不可重复读都是读取了另一条已经提交的事务（这点就脏读不同），所不同的是不可重复读查询的都是同一个数据项，而幻读针对的是一批数据整体。</mark></p><h2 id="事务隔离级别"><a href="#事务隔离级别" class="headerlink" title="事务隔离级别"></a>事务隔离级别</h2><p>MySQL数据库为我们提供的四种隔离级别：<span id="target2"></span></p><h3 id="读未提交"><a href="#读未提交" class="headerlink" title="读未提交"></a>读未提交</h3><p>*(Read uncommitted)*：最低级别，任何情况都无法保证。</p><h3 id="读已提交"><a href="#读已提交" class="headerlink" title="读已提交"></a>读已提交</h3><p>*(Read committed)*：可避免脏读的发生。</p><h3 id="可重复读"><a href="#可重复读" class="headerlink" title="可重复读"></a>可重复读</h3><p>*(Repeatable read)*：可避免脏读、不可重复读的发生。</p><h3 id="串行化"><a href="#串行化" class="headerlink" title="串行化"></a>串行化</h3><p>*(Serializable)*：可避免脏读、不可重复读、幻读的发生。</p><p>设置数据库的隔离级别一定要是在开启事务之前！<strong>Mysql 默认采用的可重复读隔离级别，但是它很大程度上避免幻读现象（并不是完全解决了）</strong></p><h2 id="MVCC（隔离性）"><a href="#MVCC（隔离性）" class="headerlink" title="MVCC（隔离性）"></a>MVCC（隔离性）</h2><p>即多版本并发控制，主要是为了解决的是读–写冲突问题，提高数据库的并发性能。</p><h3 id="快照读"><a href="#快照读" class="headerlink" title="快照读"></a>快照读</h3><p>又名一致性读, 读取到的是快照数据，不加锁的简单SELECT都是快照读。<br>快照读可能读到的并不一定是数据的最新版本，而有可能是之前的历史版本。快照读可以形象的理解为我们生活中的照片，拍摄到的画面都是过去式。</p><h3 id="当前读"><a href="#当前读" class="headerlink" title="当前读"></a>当前读</h3><p>当前读读取到的是最新版本数据，读取时需要保证其他并发事务不能修改当前记录, 因此需要加锁。<br>MySQL 里除了普通查询是快照读，其他都是当前读(update、insert、delete)，这些语句执行前都会查询最新版本的数据，然后再做进一步的操作。</p><p>当发生读写冲突时:</p><ul><li>读采用快照读，不加锁，非阻塞并发读</li><li>写采用当前读，加锁</li></ul><h3 id="MVCC原理"><a href="#MVCC原理" class="headerlink" title="MVCC原理"></a>MVCC原理</h3><p>每个不同的事务访问查询同一行数据时，每个事务修改的都是这行数据的不同版本，<strong>InnoDB只需要去记录这个数据的访问链，就可以实现一个SELECT操作的并发执行</strong>。</p><p>MVCC的实现主要依赖于<strong>Read View</strong>和<strong>记录中的隐藏字段trx_id和roll_pointer</strong>来实现的。</p><h4 id="聚簇索引记录中的两个隐藏列"><a href="#聚簇索引记录中的两个隐藏列" class="headerlink" title="聚簇索引记录中的两个隐藏列"></a>聚簇索引记录中的两个隐藏列</h4><ol><li>trx_id，当一个事务对某条聚簇索引记录进行改动时，就会把该事务的事务 id 记录在 trx_id 隐藏列里；</li><li>roll_pointer，每次对某条聚簇索引记录进行改动时，都会把旧版本的记录写入到 undo 日志中，然后这个隐藏列是个指针，指向每一个旧版本记录，于是就可以通过它找到修改前的记录。</li></ol><p><img data-src="/transblog/images/209261419276705.png"></p><h4 id="Read-View"><a href="#Read-View" class="headerlink" title="Read View"></a>Read View</h4><p>Read View 类似一个数据快照。就是事务在使用MVCC机制进行快照读操作时产生的读视图.</p><p><img data-src="/transblog/images/115620511259072.png"></p><p>Read View 有四个重要的字段：</p><ol><li>m_ids ：指的是在创建 Read View 时，当前数据库中「活跃事务」的事务 id 列表，注意是一个列表，<strong>“活跃事务”指的就是，启动了但还没提交的事务</strong>。</li><li>min_trx_id ：指的是在创建 Read View 时，当前数据库中「活跃事务」中<strong>事务 id 最小的事务，也就是 m_ids 的最小值</strong>。</li><li>max_trx_id ：这个并不是 m_ids 的最大值，而是<strong>创建 Read View 时当前数据库中应该给下一个事务的 id 值，也就是全局事务中最大的事务 id 值 + 1</strong>；</li><li>creator_trx_id ：指的是<strong>创建该 Read View 的事务的事务 id</strong>。</li></ol><p><img data-src="/transblog/images/74731011246939.png"></p><p>一个事务去访问记录的时候，除了自己的更新记录总是可见之外，还有这几种情况：</p><ol><li>如果记录的 trx_id 值小于 Read View 中的 min_trx_id 值，表示这个版本的记录是在创建 Read View 前已经提交的事务生成的，所以该版本的记录对当前事务可见。</li><li>如果记录的 trx_id 值大于等于 Read View 中的 max_trx_id 值，表示这个版本的记录是在创建 Read View 后才启动的事务生成的，所以该版本的记录对当前事务不可见。</li><li>如果记录的 trx_id 值在 Read View 的 min_trx_id 和 max_trx_id 之间，需要判断 trx_id 是否在 m_ids 列表中：如果记录的 trx_id 在 m_ids 列表中，表示生成该版本记录的活跃事务依然活跃着（还没提交事务），所以该版本的记录对当前事务不可见。如果记录的 trx_id 不在 m_ids列表中，表示生成该版本记录的活跃事务已经被提交，所以该版本的记录对当前事务可见。</li></ol><h3 id="实现的隔离级别"><a href="#实现的隔离级别" class="headerlink" title="实现的隔离级别"></a>实现的隔离级别</h3><ol><li>「读未提交」：因为可以读到未提交事务修改的数据，所以直接读取最新的数据就好了；</li><li>「读提交」和「可重复读」：它们是通过 Read View 来实现的，它们的区别在于创建 Read View 的时机不同，<ul><li>「读提交」隔离级别是在「每个语句执行前」都会重新生成一个 Read View，</li><li>「可重复读」隔离级别是「启动事务时」生成一个 Read View，然后整个事务期间都在用这个 Read View。</li></ul></li><li>「串行化」：隔离级别的事务来说，通过加读写锁的方式来避免并行访问；</li></ol><h4 id="读已提交-1"><a href="#读已提交-1" class="headerlink" title="读已提交"></a>读已提交</h4><p>读提交隔离级别是在每次读取数据时，都会生成一个新的 Read View。</p><ol><li>事务 B 读取数据（创建 Read View），账户余额为 100 万；</li><li>事务 A 修改数据（还没提交事务），将账户余额从 100 万修改成了 200 万；</li><li>事务 B 读取数据（创建 Read View），账户余额为 100 万；</li><li>事务 A 提交事务；</li><li>事务 B 读取数据（创建 Read View），账户余额为 200 万；</li></ol><p>（三次事务B读取为三个事务）</p><p><img data-src="/transblog/images/455811109269969.png"></p><p>为什么事务 B 第二次读数据时，读不到事务 A （还未提交事务）修改的数据？</p><ul><li>事务 B 在找到这条记录时，会看这条记录的 trx_id 是 51，在事务 B 的 Read View 的 min_trx_id 和 max_trx_id 之间，</li><li>接下来需要判断 trx_id 值是否在 m_ids 范围内，判断的结果是在的，那么说明这条记录是被还未提交的事务修改的，这时事务 B 并不会读取这个版本的记录。</li><li>而是，沿着 undo log 链条往下找旧版本的记录，直到找到 trx_id 「小于」事务 B 的 Read View 中的 min_trx_id 值的第一条记录，所以事务 B 能读取到的是 trx_id 为 50 的记录</li></ul><p>为什么事务 A 提交后，事务 B 就可以读到事务 A 修改的数据？</p><p>在事务 A 提交后，由于隔离级别是「读提交」，所以事务 B 在每次读数据的时候，会重新创建 Read View，此时事务 B 第三次读取数据时创建的 Read View 如下</p><p><img data-src="/transblog/images/136934509267573.png"></p><p>事务 B 在找到这条记录时，会发现这条记录的 trx_id 是 51，比事务 B 的 Read View 中的 min_trx_id 值（52）还小，这意味着修改这条记录的事务早就在创建 Read View 前提交过了，所以该版本的记录对事务 B 是可见的。</p><p>正是因为在读提交隔离级别下，事务每次读数据时都重新创建 Read View，那么在事务期间的多次读取同一条数据，前后两次读的数据可能会出现不一致，因为可能这期间另外一个事务修改了该记录，并提交了事务</p><h4 id="可重复读-1"><a href="#可重复读-1" class="headerlink" title="可重复读"></a>可重复读</h4><p><img data-src="/transblog/images/262013011259774.png"></p><p><img data-src="/transblog/images/435884609258196.png"></p><ol><li>事务 B 读取账户余额记录，读到余额是 100 万；</li><li>事务 A 将账户余额记录修改成 200 万，并没有提交事务；</li><li>事务 B 读取账户余额记录，读到余额还是 100 万；</li><li>事务 A 提交事务；</li><li>事务 B 读取账户余额记录，读到余额依然还是 100 万；</li></ol><p>（三次事务B读取为同一个事务）<br>事务 B 第一次（1）读账户余额记录，在找到记录后，它会先看这条记录的 trx_id，此时发现 trx_id 为 50，比事务 B 的 Read View 中的 min_trx_id 值（51）还小，这意味着修改这条记录的事务早就在事务 B 启动前提交过了，所以该版本的记录对事务 B 可见的，也就是事务 B 可以获取到这条记录。</p><p><span id="target3"></span><br>事务 A 通过 update 语句将这条记录修改了（还未提交事务），将小林的余额改成 200 万，<strong>这时 MySQL 会记录相应的 undo log，并以链表的方式串联起来，形成版本链：</strong></p><p><img data-src="/transblog/images/507233611252083.png"></p><p>事务 B 第二次（3）去读取该记录，发现这条记录的 trx_id 值为 51，在事务 B 的 Read View 的 min_trx_id 和 max_trx_id 之间，则需要判断 trx_id 值是否在 m_ids 范围内，判断的结果是在的，那么<strong>说明这条记录是被还未提交的事务修改的</strong>，这时事务 B 并不会读取这个版本的记录。而是<strong>沿着 undo log 链条往下找旧版本的记录，直到找到 trx_id 「小于」事务 B 的 Read View 中的 min_trx_id 值的第一条记录</strong>，所以事务 B 能读取到的是 trx_id 为 50 的记录，也就是小林余额是 100 万的这条记录。</p><p>当事物 A 提交事务后，由于隔离级别时「可重复读」，所以事务 B 再次读取记录时，还是基于启动事务时创建的 Read View 来判断当前版本的记录是否可见。所以，即使事务 A 将小林余额修改为 200 万并提交了事务，事务 B 第三次读取记录时，读到的记录也是小林余额是 100 万的这条记录。</p><h4 id="串行化-尽力避免幻读"><a href="#串行化-尽力避免幻读" class="headerlink" title="串行化(尽力避免幻读)"></a>串行化(尽力避免幻读)</h4><p>前面提到MySQL InnoDB 引擎的默认隔离级别虽然是「可重复读」，但是它很大程度上避免幻读现象，<a href="#target2">点击此处跳转</a></p><h5 id="快照读是如何避免幻读的"><a href="#快照读是如何避免幻读的" class="headerlink" title="快照读是如何避免幻读的"></a>快照读是如何避免幻读的</h5><p>针对快照读（普通 select 语句），是通过上述的 MVCC 方式解决了幻读，因为可重复读隔离级别下，事务执行过程中看到的数据，一直跟这个事务启动时看到的数据是一致的，即使中途有其他事务插入了一条数据，是查询不出来这条数据的，所以就很好了避免幻读问题。</p><h5 id="当前读是如何避免幻读的"><a href="#当前读是如何避免幻读的" class="headerlink" title="当前读是如何避免幻读的"></a>当前读是如何避免幻读的</h5><p>Innodb 引擎为了解决「可重复读」隔离级别使用「当前读」而造成的幻读问题，<strong>就引出了间隙锁</strong>。这里和之前的两种隔离级别有所不同。<a href="#target8">点击此处跳转到锁–间隙锁</a></p><p><img data-src="/transblog/images/427761314267114.png"></p><p>事务 A 执行了这面这条锁定读语句后，就在对表中的记录加上 id 范围为 (2, +∞] 的 next-key lock（next-key lock 是间隙锁+记录锁的组合）。</p><p>然后，事务 B 在执行插入语句的时候，判断到插入的位置被事务 A 加了 next-key lock，于是事物 B 会生成一个插入意向锁，同时进入等待状态，直到事务 A 提交了事务。这就避免了由于事务 B 插入新记录而导致事务 A 发生幻读的现象。</p><ul><li>针对当前读（select … for update 等语句），是通过 next-key lock（记录锁+间隙锁）方式解决了幻读，因为当执行 select … for update 语句的时候，会加上 next-key lock，如果有其他事务在 next-key lock 锁范围内插入了一条记录，那么这个插入语句就会被阻塞，无法成功插入，所以就很好了避免幻读问题。</li></ul><h5 id="没有避免的场景"><a href="#没有避免的场景" class="headerlink" title="没有避免的场景"></a>没有避免的场景</h5><p>前面说了是尽力避免幻读，看起来好像也解决了幻读（其实NO），那么到底哪些场景会有幻读</p><p><strong>场景一：</strong><br><img data-src="/transblog/images/568472214259783.png"></p><p>在可重复读隔离级别下，事务 A 第一次执行普通的 select 语句时生成了一个 ReadView，之后事务 B 向表中新插入了一条 id &#x3D; 5 的记录并提交。此时，事务 A 更新 id &#x3D; 5 这条记录，对没错，事务 A 看不到 id &#x3D; 5 这条记录，但是他去更新了这条记录，<strong>在这个时刻，这条新记录的 trx_id 隐藏列的值就变成了事务 A 的事务 id</strong>，这场景确实很违和，然后再次查询 id &#x3D; 5 的记录，事务 A 就能看到事务 B 插入的纪录了，幻读就是发生在这种违和的场景。因为这种特殊现象的存在，所以我们认为 MySQL Innodb 中的 MVCC 并不能完全避免幻读现象。</p><p><strong>场景二：</strong></p><ul><li>T1 时刻：事务 A 先执行「快照读语句」：select * from t_test where id &gt; 100 得到了 3 条记录。</li><li>T2 时刻：事务 B 往插入一个 id&#x3D; 200 的记录并提交；</li><li>T3 时刻：事务 A 再执行「当前读语句」 select * from t_test where id &gt; 100 for update 就会得到 4 条记录，此时也发生了幻读现象。</li></ul><p>要避免这类特殊场景下发生幻读的现象的话，就是尽量在开启事务之后，马上执行 select … for update 这类当前读的语句，因为它会对记录加 next-key lock，从而避免其他事务插入一条新记录。</p><h1 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h1><h2 id="日志的种类"><a href="#日志的种类" class="headerlink" title="日志的种类"></a>日志的种类</h2><ol><li><p><strong>undo log（回滚日志）</strong><br>实现了事务中的原子性，主要用于事务回滚和 MVCC。</p></li><li><p><strong>redo log（重做日志）</strong><br>实现了事务中的持久性，主要用于掉电等故障恢复；</p></li><li><p><strong>binlog （归档日志）</strong><br>是 Server 层生成的日志，主要用于数据备份和主从复制；</p></li></ol><h2 id="undo-log（原子性）"><a href="#undo-log（原子性）" class="headerlink" title="undo log（原子性）"></a>undo log（原子性）</h2><p>一个事务在执行过程中，在还没有提交事务之前，如果 MySQL 发生了崩溃，要怎么回滚到事务之前的数据呢？</p><p>undo log 是一种逻辑日志，用于撤销回退，主要记录数据的逻辑变化。在事务没提交之前，MySQL 会先记录更新前的数据到 undo log 日志文件里面，当事务回滚时，可以利用 undo log 来进行回滚。</p><p><img data-src="/transblog/images/565170616268759.png"></p><p><strong>DML操作修改聚簇索引前，记录undo log，二级索引记录的修改，不记录undo log</strong></p><h3 id="undo-log的作用"><a href="#undo-log的作用" class="headerlink" title="undo log的作用"></a>undo log的作用</h3><ol><li><p>事务回滚<br>undo log只将数据库逻辑地恢复到原来的样子，在回滚的时候，它实际上是做的相反的工作，比如一条INSERT ，对应一条 DELETE，对于每个UPDATE,对应一条相反的 UPDATE，将修改前的行放回去。</p></li><li><p>MVCC(配合ReadView，<a href="#target3">点击此处跳转</a>)<br>一条记录的每一次更新操作产生的 undo log 格式都有一个 roll_pointer 指针和一个 trx_id 事务id：通过 trx_id 可以知道该记录是被哪个事务修改的；通过 roll_pointer 指针可以将这些 undo log 串成一个链表，这个链表就被称为版本链；</p></li></ol><h3 id="undo-log的写入时机"><a href="#undo-log的写入时机" class="headerlink" title="undo log的写入时机"></a>undo log的写入时机</h3><p>注意：undo log页面的修改，同样需要记录redo日志。和数据页的刷盘策略是一样的，redo log 会每秒刷盘，提交事务时也会刷盘，数据页和 undo 页都是靠这个机制保证持久化的。<a href="#target4">点击此处跳转到redo log的写入时机</a></p><h3 id="undo-log的存储位置"><a href="#undo-log的存储位置" class="headerlink" title="undo log的存储位置"></a>undo log的存储位置</h3><p>在InnoDB存储引擎中，undo log存储在回滚段(Rollback Segment)中,每个回滚段记录了1024个undo log segment，而在每个undo log segment段中进行undo 页的申请，在5.6以前，Rollback Segment是在共享表空间里的，5.6.3之后，可通过 innodb_undo_tablespace设置undo存储的位置。</p><h3 id="undo-log的类型"><a href="#undo-log的类型" class="headerlink" title="undo log的类型"></a>undo log的类型</h3><ol><li>insert undo log<br>是指在insert 操作中产生的undo log，因为insert操作的记录，只对事务本身可见，对其他事务不可见。故该undo log可以在事务提交后直接删除，不需要进行purge操作。</li><li>update undo log<br>记录的是对delete 和update操作产生的undo log，该undo log可能需要提供MVCC机制，因此不能再事务提交时就进行删除。提交时放入undo log链表，等待purge线程进行最后的删除。</li></ol><p><strong>补充：purge线程两个主要作用是：清理undo页和清除page里面带有Delete_Bit标识的数据行。在InnoDB中，事务中的Delete操作实际上并不是真正的删除掉数据行，而是一种Delete Mark操作，在记录上标识Delete_Bit，而不删除记录。是一种”假删除”,只是做了个标记，真正的删除工作需要后台purge线程去完成。</strong></p><h2 id="redo-log（持久性）"><a href="#redo-log（持久性）" class="headerlink" title="redo log（持久性）"></a>redo log（持久性）</h2><p>内存里的Buffer Pool万一断电重启，还没来得及落盘的脏页数据丢失了怎么办？<br>redo log 是物理日志，记录了某个数据页做了什么修改，<strong>比如对 XXX 表空间中的 YYY 数据页 ZZZ 偏移量的地方做了AAA 更新，每当执行一个事务就会产生这样的一条或者多条物理日志。</strong></p><p>当有一条记录需要更新的时候，InnoDB 引擎就会先更新内存（同时标记为脏页），然后将本次对这个页的修改以 redo log 的形式记录下来，这个时候更新就算完成了。后续，InnoDB 引擎会在适当的时候，由后台线程将缓存在 Buffer Pool 的脏页刷新到磁盘里，这就是 WAL（Write-Ahead Logging）技术：MySQL 的写操作并不是立刻写到磁盘上，而是先写日志，然后在合适的时间再写到磁盘上。</p><p><img data-src="/transblog/images/311201510257444.png"></p><p>在事务提交时，只要先将 redo log 持久化到磁盘即可，可以不需要等到将缓存在 Buffer Pool 里的脏页数据持久化到磁盘。<br>事务提交之前发生了崩溃，重启后会通过 undo log 回滚事务，事务提交之后发生了崩溃，重启后会通过 redo log 恢复事务：</p><p><img data-src="/transblog/images/84330511241108.png"></p><h3 id="redo-log的作用"><a href="#redo-log的作用" class="headerlink" title="redo log的作用"></a>redo log的作用</h3><ol><li>数据库恢复：实现事务的持久性，让 MySQL 有 crash-safe 的能力，能够保证 MySQL 在任何时间段突然崩溃，重启后之前已提交的记录都不会丢失；</li><li>提高性能：将写操作从「随机写」变成了「顺序写」，提升 MySQL 写入磁盘的性能。</li></ol><h3 id="redo-log写到磁盘效率很低吗"><a href="#redo-log写到磁盘效率很低吗" class="headerlink" title="redo log写到磁盘效率很低吗"></a>redo log写到磁盘效率很低吗</h3><p>答案并不会，磁盘的「顺序写 」比「随机写」 高效的多，因此 redo log 写入磁盘的开销更小。写入 redo log 的方式使用了追加操作， 对磁盘是顺序写，而写入数据需要先找到写入位置，然后才写到磁盘，所以是随机写。</p><h3 id="redo-log直接写入磁盘吗"><a href="#redo-log直接写入磁盘吗" class="headerlink" title="redo log直接写入磁盘吗"></a>redo log直接写入磁盘吗</h3><p>不是的。执行一个事务的过程中，产生的 redo log 也不是直接写入磁盘的，因为这样会产生大量的 I&#x2F;O 操作，而且磁盘的运行速度远慢于内存。所以，redo log 也有自己的缓存—— redo log buffer，每当产生一条 redo log 时，会先写入到 redo log buffer：</p><p><img data-src="/transblog/images/229902114243612.png"></p><p>redo log buffer 默认大小 16 MB，可以通过 innodb_log_Buffer_size 参数动态的调整大小，增大它的大小可以让 MySQL 处理「大事务」是不必写入磁盘，进而提升写 IO 性能。</p><h3 id="redo-log的写入时机"><a href="#redo-log的写入时机" class="headerlink" title="redo log的写入时机"></a><span id="target4">redo log的写入时机</span></h3><ol><li>MySQL 正常关闭时；</li><li>当 redo log buffer 中记录的写入量大于 redo log buffer 内存空间的一半时，会触发落盘；</li><li>InnoDB 的后台线程（MasterThread）每隔 1 秒，将 redo log buffer 持久化到磁盘。</li><li>每次事务提交时都将缓存在 redo log buffer 里的 redo log 直接持久化到磁盘（由 innodb_flush_log_at_trx_commit 参数控制）。</li></ol><ul><li><p>为 0 时，表示每次事务提交时 ，还是将 redo log 留在 redo log buffer 中 ，该模式下在事务提交时不会主动触发写入磁盘的操作。通过调用 write() 将 redo log写到操作系统的 Page Cache，然后调用 fsync() 持久化到磁盘。所以参数为 0 的策略，MySQL 进程的崩溃会导致上一秒钟所有事务数据的丢失;</p></li><li><p>为 1 时，表示每次事务提交时，都将缓存在 redo log buffer 里的 redo log 直接持久化到磁盘，这样可以保证 MySQL 异常重启之后数据不会丢失。</p></li><li><p>为 2 时，表示每次事务提交时，都只是缓存在 redo log buffer 里的 redo log 写到 redo log 文件，意味着写入到了操作系统的文件缓存，而不是磁盘。<em>操作系统的文件系统中有个 Page Cache，是专门用来缓存文件数据的（详情请参考操作系统–文件系统–Page Cache ）</em>，只有在操作系统崩溃或者系统断电的情况下，上一秒钟所有事务数据才可能丢失。</p></li></ul><p><img data-src="/transblog/images/56801615245444.png"></p><ul><li>数据安全性：参数 1 &gt; 参数 2 &gt; 参数 0</li><li>写入性能：参数 0 &gt; 参数 2&gt; 参数 1</li></ul><p><span id="target5"></span></p><h3 id="redo-log-文件满了发生什么"><a href="#redo-log-文件满了发生什么" class="headerlink" title="redo log 文件满了发生什么"></a>redo log 文件满了发生什么</h3><p>默认情况下， InnoDB 存储引擎有 1 个重做日志文件组( redo log Group），「重做日志文件组」由有 2 个 redo log 文件组成，这两个 redo 日志的文件名叫 ：ib_logfile0 和 ib_logfile1 。</p><p><img data-src="/transblog/images/169245310273009.png"></p><p>在重做日志组中，每个 redo log File 的大小是固定且一致的，假设每个 redo log File 设置的上限是 1 GB，那么总共就可以记录 2GB 的操作。重做日志文件组是以循环写的方式工作的，从头开始写，写到末尾就又回到开头，相当于一个环形。所以 InnoDB 存储引擎会先写 ib_logfile0 文件，当 ib_logfile0 文件被写满的时候，会切换至 ib_logfile1 文件，当 ib_logfile1 文件也被写满时，会切换回 ib_logfile0 文件。</p><h3 id="undo-log和redo-log的区别"><a href="#undo-log和redo-log的区别" class="headerlink" title="undo log和redo log的区别"></a>undo log和redo log的区别</h3><ul><li>undo log 记录了此次事务「开始前」的数据状态，记录的是更新之前的值；</li><li>redo log 记录了此次事务「完成后」的数据状态，记录的是更新之后的值；</li></ul><h2 id="binlog"><a href="#binlog" class="headerlink" title="binlog"></a>binlog</h2><p>binlog 文件是记录了所有数据库表结构变更和表数据修改的日志，不会记录查询类的操作，比如 SELECT 和 SHOW 操作。</p><p>undo log 和 redo log 都是 Innodb 存储引擎生成的，binlog 是由Server 层生成的。</p><p>MySQL 在完成一条更新操作后，产生对应的binlog，等之后事务提交的时候，会将该事务执行过程中产生的所有 binlog 统一写 入 binlog 文件。</p><h3 id="为什么要有binlog"><a href="#为什么要有binlog" class="headerlink" title="为什么要有binlog"></a>为什么要有binlog</h3><ol><li><p><strong>复制</strong><br>binlog用于实现MySQL的主从复制，即从服务器可以读取主服务器的binlog来保持与主服务器的同步。主服务器上的所有数据改变（比如增，删，改）都写入二进制日志，然后从服务器获取这些日志，然后在它的本地数据据进行重放这些日志，从而达到数据的一致性。从而实现负载均衡和故障转移。</p></li><li><p><strong>数据恢复</strong><br>当数据库出现故障时，可以使用binlog进行点时间恢复。通过回放binlog日志记录，可以将数据库状态恢复到某一特定时间点的状态，从而可以恢复数据并最小化数据丢失。</p></li><li><p><strong>审计</strong><br>binlog也被用于审计目的。通过审查日志，您可以看到何时对数据进行了更改，以及更改是如何进行的。</p></li></ol><h3 id="binlog-和-redo-log-的区别"><a href="#binlog-和-redo-log-的区别" class="headerlink" title="binlog 和 redo log 的区别"></a>binlog 和 redo log 的区别</h3><ol><li><p>适用对象不同：<br>binlog 是 MySQL 的 Server 层实现的日志，所有存储引擎都可以使用；<br>redo log 是 Innodb 存储引擎实现的日志；</p></li><li><p>文件格式不同：<br>binlog 有 3 种格式类型，分别是 STATEMENT（默认格式）、ROW、 MIXED，区别如下：</p><ul><li>STATEMENT：每一条修改数据的 SQL 都会被记录到 binlog 中（相当于记录了逻辑操作，所以针对这种格式， binlog 可以称为逻辑日志），主从复制中 slave 端再根据 SQL 语句重现。但 STATEMENT 有动态函数的问题，比如你用了 uuid 或者 now 这些函数，你在主库上执行的结果并不是你在从库执行的结果，这种随时在变的函数会导致复制的数据不一致；</li><li>ROW：记录行数据最终被修改成什么样了（这种格式的日志，就不能称为逻辑日志了），不会出现 STATEMENT 下动态函数的问题。但 ROW 的缺点是每行数据的变化结果都会被记录，比如执行批量 update 语句，更新多少行数据就会产生多少条记录，使 binlog 文件过大，而在 STATEMENT 格式下只会记录一个 update 语句而已；</li><li>MIXED：包含了 STATEMENT 和 ROW 模式，它会根据不同的情况自动使用 ROW 模式和 STATEMENT 模式；</li></ul><p> redo log 是物理日志，记录的是在某个数据页做了什么修改，比如对 XXX 表空间中的 YYY 数据页 ZZZ 偏移量的地方做了AAA 更新；</p></li><li><p>写入方式不同：<br>binlog 是追加写，写满一个文件，就创建一个新的文件继续写，不会覆盖以前的日志，保存的是全量的日志。<br>redo log 是循环写，日志空间大小是固定，全部写满就从头开始，保存未被刷入磁盘的脏页日志。</p></li><li><p>用途不同：<br>binlog 用于备份恢复、主从复制；<br>redo log 用于掉电等故障恢复。</p></li></ol><h3 id="binlog的写入时机"><a href="#binlog的写入时机" class="headerlink" title="binlog的写入时机"></a>binlog的写入时机</h3><p>MySQL 给每个线程分配了一片内存binlog cache，参数 binlog_cache_size 用于控制单个线程内 binlog cache 所占内存的大小。如果超过了这个参数规定的大小，就要暂存到磁盘。</p><p>事务执行过程中，先把日志写到 binlog cache（Server 层的 cache），事务提交的时候，再把 binlog cache 写到 binlog 文件中，并清空 binlog cache。</p><p><img data-src="/transblog/images/138975409240772.png"></p><p>write：把日志写入到 binlog 文件，数据还缓存在文件系统的 page cache 里，不涉及磁盘 I&#x2F;O。<br>fsync：将数据持久化到磁盘的操作，这里就会涉及磁盘 I&#x2F;O。</p><p>sync_binlog 参数来控制数据库的 binlog 刷到磁盘上的频率：</p><ul><li>为 0 (默认)：，表示每次提交事务都只 write，不 fsync，后续交由操作系统决定何时将数据持久化到磁盘；</li><li>为 1：，表示每次提交事务都会 write，然后马上执行 fsync；</li><li>为 N(N&gt;1) ：，表示每次提交事务都 write，但累积 N 个事务后才 fsync。</li></ul><p><strong>一个事务的 binlog 是不能被拆开的，因此无论这个事务有多大（比如有很多条语句），也要保证一次性写入。如果一个事务的 binlog 被拆开的时候，在备库执行就会被当做多个事务分段自行，这样破坏了原子性，是有问题的。</strong></p><h3 id="能使用-redo-log-恢复被删除的数据库数据吗？"><a href="#能使用-redo-log-恢复被删除的数据库数据吗？" class="headerlink" title="能使用 redo log 恢复被删除的数据库数据吗？"></a>能使用 redo log 恢复被删除的数据库数据吗？</h3><ul><li><strong>只能使用 binlog 文件恢复</strong>。因为 redo log 文件是循环写，是会边写边擦除日志的，只记录未被刷入磁盘的数据的物理日志，已经刷入磁盘的数据都会从 redo log 文件里擦除。</li><li>binlog 文件保存的是全量的日志，也就是保存了所有数据变更的情况，理论上只要记录在 binlog 上的数据，都可以恢复，所以如果不小心整个数据库的数据被删除了，得用 binlog 文件恢复数据。</li></ul><h2 id="Checkpoint–持久化机制"><a href="#Checkpoint–持久化机制" class="headerlink" title="Checkpoint–持久化机制"></a>Checkpoint–持久化机制</h2><p>页从缓冲池刷新回磁盘的操作并不是在每次页发生更新时触发，而是通过Checkpoint机制将脏页刷新回磁盘</p><p>Checkpoint是解决以下几个问题：</p><ol><li>缩短数据库的恢复时间；<br>当数据库宕机时，数据库不需要重做所有日志，因为CheckPoint之前的页都已经刷新回磁盘。只需对CheckPoint后的重做日志进行恢复，从而缩短恢复时间</li><li>缓冲池不够用时，将脏页刷新到磁盘；<br>当缓存池不够用时，LRU算法会溢出最近最少使用的页，若此页为脏页，会强制执行CheckPoint，将该脏页刷回磁盘</li><li>redo log不可用时，刷新脏页。<br>redo log 是循环写的方式，相当于一个环形，InnoDB 用 write pos 表示 redo log 当前记录写到的位置，用 checkpoint 表示当前要擦除的位置<br><a href="#target5">点击此处跳转到redo log满了会发生什么</a></li></ol><p><img data-src="/transblog/images/452215410255222.png"></p><ul><li>write pos 和 checkpoint 的移动都是顺时针方向；</li><li>write pos ～ checkpoint 之间的部分（图中的红色部分），用来记录新的更新操作；</li><li>check point ～ write pos 之间的部分（图中蓝色部分）：待落盘的脏数据页记录；</li></ul><p>如果 write pos 追上了 checkpoint，就意味着 redo log 文件满了，这时 MySQL 不能再执行新的更新操作，也就是说 MySQL 会被阻塞（因此所以针对并发量大的系统，适当设置 redo log 的文件大小非常重要），此时会停下来将 Buffer Pool 中的脏页刷新到磁盘中，然后标记 redo log 哪些记录可以被擦除，接着对旧的 redo log 记录进行擦除，等擦除完旧记录腾出了空间，checkpoint 就会往后移动（图中顺时针），然后 MySQL 恢复正常运行，继续执行新的更新操作。</p><p>所以，一次 checkpoint 的过程就是脏页刷新到磁盘中变成干净页，然后标记 redo log 哪些记录可以被覆盖的过程。</p><h1 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h1><h2 id="锁的类型"><a href="#锁的类型" class="headerlink" title="锁的类型"></a>锁的类型</h2><h3 id="全局锁"><a href="#全局锁" class="headerlink" title="全局锁"></a>全局锁</h3><p>执行后，整个数据库就处于只读状态了，这时其他线程执行以下操作，都会被阻塞：<br>对数据的增删改操作，比如 insert、delete、update等语句；对表结构的更改操作，比如 alter table、drop table 等语句。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">flush tables <span class="keyword">with</span> read lock</span><br></pre></td></tr></table></figure><p>如果要释放全局锁，则要执行这条命令：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">unlock tables</span><br></pre></td></tr></table></figure><p>应用场景：<br>全局锁主要应用于做全库逻辑备份，这样在备份数据库期间，不会因为数据或表结构的更新，而出现备份文件的数据与预期的不一样。</p><p>缺点：<br>加上全局锁，意味着整个数据库都是只读状态。那么如果数据库里有很多数据，备份就会花费很多的时间，关键是备份期间，业务只能读数据，而不能更新数据，这样会造成业务停滞。</p><h3 id="表锁"><a href="#表锁" class="headerlink" title="表锁"></a>表锁</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="operator">/</span><span class="operator">/</span>表级别的共享锁，也就是读锁；</span><br><span class="line">lock tables t_student read;</span><br><span class="line"></span><br><span class="line"><span class="operator">/</span><span class="operator">/</span>表级别的独占锁，也就是写锁；</span><br><span class="line">lock tables t_stuent write;</span><br></pre></td></tr></table></figure><p>表锁除了会限制别的线程的读写外，也会限制本线程接下来的读写操作。如果本线程对表加了「共享表锁」，那么本线程接下来如果要对表执行写操作的语句，也是会被阻塞的。</p><p>要释放表锁，可以使用下面这条命令，会释放当前会话的所有表锁：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">unlock tables</span><br></pre></td></tr></table></figure><p>当会话退出后，也会释放所有表锁。</p><h4 id="元数据锁（MDL）"><a href="#元数据锁（MDL）" class="headerlink" title="元数据锁（MDL）"></a>元数据锁（MDL）</h4><p>不需要显示的使用 MDL，因为当我们对数据库表进行操作时，会自动给这个表加上 MDL：</p><ul><li>对一张表进行 CRUD 操作时，加的是 MDL 读锁；</li><li>对一张表做结构变更操作的时候，加的是 MDL 写锁；</li></ul><p>MDL 是为了保证当用户对表执行 CRUD 操作时，防止其他线程对这个表结构做了变更。</p><ul><li>当有线程在执行 select 语句（ 加 MDL 读锁）的期间，如果有其他线程要更改该表的结构（ 申请 MDL 写锁），那么将会被阻塞，直到执行完 select 语句（ 释放 MDL 读锁）。</li><li>反之，当有线程对表结构进行变更（ 加 MDL 写锁）的期间，如果有其他线程执行了 CRUD 操作（ 申请 MDL 读锁），那么就会被阻塞，直到表结构变更完成（ 释放 MDL 写锁）。</li></ul><p>MDL 是在事务提交后才会释放，这意味着事务执行期间，MDL 是一直持有的。</p><p>那如果数据库有一个事务，但是一直还没提交，那在对表结构做变更操作的时候，可能会发生意想不到的事情，比如下面这个顺序的场景：</p><ol><li>线程 A 先启用了事务（但是一直不提交），然后执行一条 select 语句，此时就先对该表加上 MDL 读锁；</li><li>线程 B 修改了表字段，此时由于线程 A 的事务并没有提交，也就是 MDL 读锁还在占用着，这时线程 B 就无法申请到 MDL 写锁，就会被阻塞，</li><li>在线程 B 阻塞后，后续有对该表的 select 语句，就都会被阻塞，如果此时有大量该表的 select 语句的请求到来，就会有大量的线程被阻塞住，这时数据库的线程很快就会爆满了。</li></ol><p>这是因为申请 MDL 锁的操作会形成一个队列，队列中写锁获取优先级高于读锁，一旦出现 MDL 写锁等待，会阻塞后续该表的所有 CRUD 操作。<strong>所以在对表结构变更前，先要看看数据库中的长事务</strong></p><h3 id="意向锁"><a href="#意向锁" class="headerlink" title="意向锁"></a>意向锁</h3><p>在使用 InnoDB 引擎的表里对某些记录加上「共享锁」之前，需要先在表级别加上一个「意向共享锁」；<br>在使用 InnoDB 引擎的表里对某些纪录加上「独占锁」之前，需要先在表级别加上一个「意向独占锁」；</p><p>当执行插入、更新、删除操作，需要先对表加上「意向独占锁」，然后对该记录加独占锁。而普通的 select 是不会加行级锁的，普通的 select 语句是利用 MVCC 实现一致性读，是无锁的。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="operator">/</span><span class="operator">/</span>先在表上加上意向共享锁，然后对读取的记录加共享锁</span><br><span class="line"><span class="keyword">select</span> ... lock <span class="keyword">in</span> share mode;</span><br><span class="line"></span><br><span class="line"><span class="operator">/</span><span class="operator">/</span>先表上加上意向独占锁，然后对读取的记录加独占锁</span><br><span class="line"><span class="keyword">select</span> ... <span class="keyword">for</span> <span class="keyword">update</span>;</span><br></pre></td></tr></table></figure><p>意向共享锁和意向独占锁是表级锁，不会和行级的共享锁和独占锁发生冲突，而且意向锁之间也不会发生冲突，只会和共享表锁和独占表锁发生冲突。</p><p>表锁和行锁是满足读读共享、读写互斥、写写互斥的。</p><p>如果没有「意向锁」，那么加「独占表锁」时，就需要遍历表里所有记录，查看是否有记录存在独占锁，这样效率会很慢。那么有了「意向锁」，由于在对记录加独占锁前，先会加上表级别的意向独占锁，那么在加「独占表锁」时，直接查该表是否有意向独占锁，如果有就意味着表里已经有记录被加了独占锁，这样就不用去遍历表里的记录。<strong>所以，意向锁的目的是为了快速判断表里是否有记录被加锁</strong>。</p><h3 id="AUTO-INC-锁"><a href="#AUTO-INC-锁" class="headerlink" title="AUTO-INC 锁"></a>AUTO-INC 锁</h3><p>表里的主键通常都会设置成自增的，这是通过对主键字段声明 AUTO_INCREMENT 属性实现的。之后可以在插入数据时，可以不指定主键的值，数据库会自动给主键赋值递增的值，这主要是通过 AUTO-INC 锁实现的。</p><p>AUTO-INC 锁是特殊的表锁机制，锁不是再一个事务提交后才释放，而是再执行完插入语句后就会立即释放。在插入数据时，会加一个表级别的 AUTO-INC 锁，然后为被 AUTO_INCREMENT 修饰的字段赋值递增的值，等插入语句执行完成后，才会把 AUTO-INC 锁释放掉。</p><p><strong>那么，一个事务在持有 AUTO-INC 锁的过程中，其他事务的如果要向该表插入语句都会被阻塞，从而保证插入数据时，被 AUTO_INCREMENT 修饰的字段的值是连续递增的</strong></p><p>但是， AUTO-INC 锁再对大量数据进行插入的时候，会影响插入性能，因为另一个事务中的插入会被阻塞。</p><p>因此， 在 MySQL 5.1.22 版本开始，InnoDB 存储引擎提供了一种轻量级的锁来实现自增。</p><p>一样也是在插入数据的时候，会为被 AUTO_INCREMENT 修饰的字段加上轻量级锁，然后给该字段赋值一个自增的值，就把这个轻量级锁释放了，而不需要等待整个插入语句执行完后才释放锁。</p><p>InnoDB 存储引擎提供了个 innodb_autoinc_lock_mode 的系统变量，是用来控制选择用 AUTO-INC 锁，还是轻量级的锁。</p><ul><li><p>当 innodb_autoinc_lock_mode &#x3D; 0，就采用 AUTO-INC 锁，语句执行结束后才释放锁；</p></li><li><p>当 innodb_autoinc_lock_mode &#x3D; 2，就采用轻量级锁，申请自增主键后就释放锁，并不需要等语句执行后才释放。</p></li><li><p>当 innodb_autoinc_lock_mode &#x3D; 1：</p><ul><li>普通 insert 语句，自增锁在申请之后就马上释放；</li><li>类似 insert … select 这样的批量插入数据的语句，自增锁还是要等语句结束后才被释放；</li></ul></li><li><p>当 innodb_autoinc_lock_mode &#x3D; 2 是性能最高的方式，意味着「申请自增主键后就释放锁，不必等插入语句执行完」，但是当搭配 binlog 的日志格式是 statement 一起使用的时候，在「主从复制的场景」中会发生数据不一致的问题。binlog 日志格式要设置为 row。</p></li></ul><h3 id="行级锁"><a href="#行级锁" class="headerlink" title="行级锁"></a>行级锁</h3><p>InnoDB 引擎是支持行级锁的，普通的 select 语句是不会对记录加锁的，因为它属于快照读。如果要在查询时对记录加行锁，可以使用下面这两个方式，这种查询会加锁的语句称为锁定读。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="operator">/</span><span class="operator">/</span>对读取的记录加共享锁</span><br><span class="line"><span class="keyword">select</span> ... lock <span class="keyword">in</span> share mode;</span><br><span class="line"></span><br><span class="line"><span class="operator">/</span><span class="operator">/</span>对读取的记录加独占锁</span><br><span class="line"><span class="keyword">select</span> ... <span class="keyword">for</span> <span class="keyword">update</span>;</span><br></pre></td></tr></table></figure><h4 id="记录锁–共享锁和独占锁"><a href="#记录锁–共享锁和独占锁" class="headerlink" title="记录锁–共享锁和独占锁"></a>记录锁–共享锁和独占锁</h4><p>共享锁（S锁）满足读读共享，读写互斥。独占锁（X锁）满足写写互斥、读写互斥。</p><p><img data-src="/transblog/images/231052315240742.png"></p><p>记录锁是有 S 锁和 X 锁之分的：</p><ul><li>当一个事务对一条记录加了 S 型记录锁后，其他事务也可以继续对该记录加 S 型记录锁（S 型与 S 锁兼容），但是不可以对该记录加 X 型记录锁（S 型与 X 锁不兼容）;</li><li>当一个事务对一条记录加了 X 型记录锁后，其他事务既不可以对该记录加 S 型记录锁（S 型与 X 锁不兼容），也不可以对该记录加 X 型记录锁（X 型与 X 锁不兼容）。</li></ul><h4 id="间隙锁"><a href="#间隙锁" class="headerlink" title="间隙锁"></a><span id="target8">间隙锁</span></h4><p>只存在于可重复读隔离级别，目的是为了解决可重复读隔离级别下幻读的现象。</p><p>假设，表中有一个范围 id 为（3，5）间隙锁，那么其他事务就无法插入 id &#x3D; 4 这条记录了，这样就有效的防止幻读现象的发生。</p><p>间隙锁虽然存在 X 型间隙锁和 S 型间隙锁，但是并没有什么区别，间隙锁之间是兼容的，即两个事务可以同时持有包含共同间隙范围的间隙锁，并不存在互斥关系，因为间隙锁的目的是防止插入幻影记录而提出的。</p><p><strong>间隙锁的意义只在于阻止区间被插入，因此是可以共存的</strong>。一个事务获取的间隙锁不会阻止另一个事务获取同一个间隙范围的间隙锁，共享和排他的间隙锁是没有区别的，他们相互不冲突，且功能相同，即两个事务可以同时持有包含共同间隙的间隙锁。</p><h4 id="插入意向锁"><a href="#插入意向锁" class="headerlink" title="插入意向锁"></a>插入意向锁</h4><p>一个事务在插入一条记录的时候，需要判断插入位置是否已被其他事务加了间隙锁（next-key lock 也包含间隙锁）。如果有的话，<strong>插入操作就会发生阻塞，直到拥有间隙锁的那个事务提交为止（释放间隙锁的时刻）</strong>，在此期间会生成一个插入意向锁，表明有事务想在某个区间插入新记录，但是现在处于等待状态。</p><p>举个例子，假设事务 A 已经对表加了一个范围 id 为（3，5）间隙锁。当事务 A 还没提交的时候，事务 B 向该表插入一条 id &#x3D; 4 的新记录，这时会判断到插入的位置已经被事务 A 加了间隙锁，于是事物 B 会生成一个插入意向锁，然后将锁的状态设置为等待状态, <em>（PS：MySQL 加锁时，是先生成锁结构，然后设置锁的状态，如果锁状态是等待状态，并不是意味着事务成功获取到了锁，只有当锁状态为正常状态时，才代表事务成功获取到了锁）</em>，此时事务 B 就会发生阻塞，直到事务 A 提交了事务。</p><p><strong>插入意向锁名字虽然有意向锁，但是它并不是意向锁，它是一种特殊的间隙锁，属于行级别锁</strong>。如果说间隙锁锁住的是一个区间，那么「插入意向锁」锁住的就是一个点。因而从这个角度来说，插入意向锁确实是一种特殊的间隙锁。</p><p>插入意向锁与间隙锁的另一个非常重要的差别是：尽管「插入意向锁」也属于间隙锁，但两个事务却不能在同一时间内，一个拥有间隙锁，另一个拥有该间隙区间内的插入意向锁（当然，插入意向锁如果不在间隙锁区间内则是可以的）。</p><h2 id="如何加锁"><a href="#如何加锁" class="headerlink" title="如何加锁"></a>如何加锁</h2><p>加锁的对象是索引，加锁的基本单位是 next-key lock，它是由记录锁和间隙锁组合而成的，next-key lock 是前开后闭区间，而间隙锁是前开后开区间。但是，在能使用记录锁或者间隙锁就能避免幻读现象的场景下， next-key lock 就会退化成记录锁或间隙锁。</p><h3 id="唯一索引等值查询"><a href="#唯一索引等值查询" class="headerlink" title="唯一索引等值查询"></a>唯一索引等值查询</h3><p>当用唯一索引进行等值查询的时候，查询的记录存不存在，加锁的规则也会不同：<br>当查询的记录是「存在」的，在索引树上定位到这一条记录后，将该记录的索引中的 next-key lock 会退化成「记录锁」。<br>原因就是在唯一索引等值查询并且查询记录存在的场景下，仅靠记录锁也能避免幻读的问题。</p><p>当查询的记录是「不存在」的，在索引树找到第一条大于该查询记录的记录后，将该记录的索引中的 next-key lock 会退化成「间隙锁」。<br>原因就是在唯一索引等值查询并且查询记录不存在的场景下，仅靠间隙锁就能避免幻读的问题。锁是加在索引上的，而这个场景下查询的记录是不存在的，自然就没办法加记录锁。</p><h3 id="唯一索引范围查询"><a href="#唯一索引范围查询" class="headerlink" title="唯一索引范围查询"></a>唯一索引范围查询</h3><p>当唯一索引进行范围查询时，会对每一个扫描到的索引加 next-key 锁，然后如果遇到下面这些情况，会退化成记录锁或者间隙锁：</p><p>情况一：针对「大于等于」的范围查询，因为存在等值查询的条件，那么如果等值查询的记录是存在于表中，那么该记录的索引中的 next-key 锁会退化成记录锁。<br>情况二：针对「小于或者小于等于」的范围查询，要看条件值的记录是否存在于表中：</p><ul><li>当条件值的记录不在表中，那么不管是「小于」还是「小于等于」条件的范围查询，扫描到终止范围查询的记录时，该记录的索引的 next-key 锁会退化成间隙锁，其他扫描到的记录，都是在这些记录的索引上加 next-key 锁。</li><li>当条件值的记录在表中，如果是「小于」条件的范围查询，扫描到终止范围查询的记录时，该记录的索引的 next-key 锁会退化成间隙锁，其他扫描到的记录，都是在这些记录的索引上加 next-key 锁；如果「小于等于」条件的范围查询，扫描到终止范围查询的记录时，该记录的索引 next-key 锁不会退化成间隙锁。其他扫描到的记录，都是在这些记录的索引上加 next-key 锁。</li></ul><h3 id="非唯一索引等值查询"><a href="#非唯一索引等值查询" class="headerlink" title="非唯一索引等值查询"></a>非唯一索引等值查询</h3><p>当我们用非唯一索引进行等值查询的时候，因为存在两个索引，一个是主键索引，一个是非唯一索引（二级索引），所以在加锁时，同时会对这两个索引都加锁，但是对主键索引加锁的时候，只有满足查询条件的记录才会对它们的主键索引加锁。</p><p>针对非唯一索引等值查询时，查询的记录存不存在，加锁的规则也会不同：</p><ul><li>当查询的记录「存在」时，由于不是唯一索引，所以肯定存在索引值相同的记录，于是非唯一索引等值查询的过程是一个扫描的过程，直到扫描到第一个不符合条件的二级索引记录就停止扫描，然后在扫描的过程中，对扫描到的二级索引记录加的是 next-key 锁，而对于第一个不符合条件的二级索引记录，该二级索引的 next-key 锁会退化成间隙锁。同时，在符合查询条件的记录的主键索引上加记录锁。</li><li>当查询的记录「不存在」时，扫描到第一条不符合条件的二级索引记录，该二级索引的 next-key 锁会退化成间隙锁。因为不存在满足查询条件的记录，所以不会对主键索引加锁。</li></ul><h3 id="非唯一索引范围查询"><a href="#非唯一索引范围查询" class="headerlink" title="非唯一索引范围查询"></a>非唯一索引范围查询</h3><p>非唯一索引和主键索引的范围查询的加锁也有所不同，不同之处在于非唯一索引范围查询，索引的 next-key lock 不会有退化为间隙锁和记录锁的情况，也就是非唯一索引进行范围查询时，对二级索引记录加锁都是加 next-key 锁。</p><h3 id="未加索引的查询"><a href="#未加索引的查询" class="headerlink" title="未加索引的查询"></a>未加索引的查询</h3><p>如果锁定读查询语句，没有使用索引列作为查询条件，或者查询语句没有走索引查询，导致扫描是全表扫描。那么，每一条记录的索引上都会加 next-key 锁，这样就相当于锁住的全表，这时如果其他事务对该表进行增、删、改操作的时候，都会被阻塞。</p><p>不只是锁定读查询语句不加索引才会导致这种情况，update 和 delete 语句如果查询条件不加索引，那么由于扫描的方式是全表扫描，于是就会对每一条记录的索引上都会加 next-key 锁，这样就相当于锁住的全表。</p><p>因此，在线上在执行 update、delete、select … for update 等具有加锁性质的语句，一定要检查语句是否走了索引，如果是全表扫描的话，会对每一个索引加 next-key 锁，相当于把整个表锁住了，这是挺严重的问题。</p><h2 id="update没有where引起事故"><a href="#update没有where引起事故" class="headerlink" title="update没有where引起事故"></a>update没有where引起事故</h2><p>在 update 语句的 where 条件没有使用索引，就会全表扫描，于是就会对所有记录加上 next-key 锁（记录锁 + 间隙锁），相当于把整个表锁住了。</p><p><strong>如何解决：</strong><br>MySQL 里的 sql_safe_updates 参数设置为 1，开启安全更新模式。update 语句必须满足如下条件之一才能执行成功：</p><ul><li>使用 where，并且 where 条件中必须有索引列；</li><li>使用 limit；</li><li>同时使用 where 和 limit，此时 where 条件中可以没有索引列；</li></ul><p>delete 语句必须满足以下条件能执行成功：</p><ul><li>同时使用 where 和 limit，此时 where 条件中可以没有索引列；</li></ul><p>如果 where 条件带上了索引列，但是优化器最终扫描选择的是全表，而不是索引的话，我们可以使用 force index([index_name]) 可以告诉优化器使用哪个索引，以此避免有几率锁全表带来的隐患。</p><h2 id="死锁案例"><a href="#死锁案例" class="headerlink" title="死锁案例"></a>死锁案例</h2><p><img data-src="/transblog/images/474670717259168.png"></p><p><img data-src="/transblog/images/523630817247035.png"></p><ul><li><p>因为当执行插入语句时，会在插入间隙上获取插入意向锁，而插入意向锁与间隙锁是冲突的，所以当其它事务持有该间隙的间隙锁时，需要等待其它事务释放间隙锁之后，才能获取到插入意向锁。而间隙锁与间隙锁之间是兼容的，所以所以两个事务中 select … for update 语句并不会相互影响。</p></li><li><p>案例中的事务 A 和事务 B 在执行完后 select … for update 语句后都持有范围为(1006,+∞]的next-key 锁，而接下来的插入操作为了获取到插入意向锁，都在等待对方事务的间隙锁释放，于是就造成了循环等待，导致死锁。</p></li></ul><p>事务 A 和事务 B 在执行完后 update 语句后都持有范围为(20, 30）的间隙锁，而接下来的插入操作为了获取到插入意向锁，都在等待对方事务的间隙锁释放，于是就造成了循环等待，满足了死锁的四个条件：互斥、占有且等待、不可强占用、循环等待，因此发生了死锁。</p><h3 id="如何打破死锁"><a href="#如何打破死锁" class="headerlink" title="如何打破死锁"></a>如何打破死锁</h3><p>在数据库层面，有两种策略通过「打破循环等待条件」来解除死锁状态：</p><ol><li>设置事务等待锁的超时时间。当一个事务的等待时间超过该值后，就对这个事务进行回滚，于是锁就释放了，另一个事务就可以继续执行了。在 InnoDB 中，参数 innodb_lock_wait_timeout 是用来设置超时时间的，默认值时 50 秒。</li></ol><p>当发生超时后，就出现下面这个提示：</p><p><img data-src="/transblog/images/488692017267201.png"></p><ol start="2"><li>开启主动死锁检测。主动死锁检测在发现死锁后，主动回滚死锁链条中的某一个事务，让其他事务得以继续执行。将参数 innodb_deadlock_detect 设置为 on，表示开启这个逻辑，默认就开启。</li></ol><p>当检测到死锁后，就会出现下面这个提示：</p><p><img data-src="/transblog/images/218452117259870.png"></p><p>我们可以回归业务的角度来预防死锁，对订单做幂等性校验的目的是为了保证不会出现重复的订单，那我们可以直接将 order_no 字段设置为唯一索引列，利用它的唯一性来保证订单表不会出现重复的订单，不过有一点不好的地方就是在我们插入一个已经存在的订单记录时就会抛出异常。</p><h1 id="集群"><a href="#集群" class="headerlink" title="集群"></a>集群</h1><h2 id="主从复制"><a href="#主从复制" class="headerlink" title="主从复制"></a>主从复制</h2><p>MySQL 的主从复制依赖于 binlog ，也就是记录 MySQL 上的所有变化并以二进制形式保存在磁盘上。复制的过程就是将 binlog 中的数据从主库传输到从库上。<strong>这个过程一般是异步的，也就是主库上执行事务操作的线程不会等待复制 binlog 的线程同步完成</strong></p><p><img data-src="/transblog/images/285182617244520.png" alt="主从复制过程"></p><ol><li>MySQL 主库在收到客户端提交事务的请求之后，会先写入 binlog，再提交事务，更新存储引擎中的数据，事务提交完成后，返回给客户端“操作成功”的响应。</li><li>从库会创建一个专门的 I&#x2F;O 线程，连接主库的 log dump 线程，来接收主库的 binlog 日志，再把 binlog 信息写入 relay log 的中继日志里，再返回给主库“复制成功”的响应。</li><li>从库会创建一个用于回放 binlog 的线程，去读 relay log 中继日志，然后回放 binlog 更新存储引擎中的数据，最终实现主从的数据一致性。</li></ol><h3 id="主从复制的模型"><a href="#主从复制的模型" class="headerlink" title="主从复制的模型"></a>主从复制的模型</h3><ol><li>同步复制：<br>MySQL 主库提交事务的线程要等待所有从库的复制成功响应，才返回客户端结果。这种方式在实际项目中，基本上没法用，原因有两个：一是性能很差，因为要复制到所有节点才返回响应；二是可用性也很差，主库和所有从库任何一个数据库出问题，都会影响业务。</li><li>异步复制（默认模型）：<br>MySQL 主库提交事务的线程并不会等待 binlog 同步到各从库，就返回客户端结果。这种模式一旦主库宕机，数据就会发生丢失。</li><li>半同步复制：<br>MySQL 5.7 版本之后增加的一种复制方式，介于两者之间，事务线程不用等待所有的从库复制成功响应，只要一部分复制成功响应回来就行，比如一主二从的集群，只要数据成功复制到任意一个从库上，主库的事务线程就可以返回给客户端。这种半同步复制的方式，兼顾了异步复制和同步复制的优点，即使出现主库宕机，至少还有一个从库有最新的数据，不存在数据丢失的风险。</li></ol><p><strong>从库是不是越多越好？</strong><br>不是的。因为从库数量增加，从库连接上来的 I&#x2F;O 线程也比较多，<strong>主库也要创建同样多的 log dump 线程来处理复制的请求，对主库资源消耗比较高，同时还受限于主库的网络带宽。</strong></p><p>所以在实际使用中，<strong>一个主库一般跟 2～3 个从库（1 套数据库，1 主 2 从 1 备主）</strong>，这就是一主多从的 MySQL 集群结构。</p><h2 id="二阶段提交"><a href="#二阶段提交" class="headerlink" title="二阶段提交"></a>二阶段提交</h2><p>什么是二阶段提交？<br><em>可以参考高可用系统设计–分布式系统篇–二阶段提交协议（2PC）</em><br>两阶段提交把单个事务的提交拆分成了 2 个阶段，分别是<strong>准备阶段</strong>和<strong>提交阶段</strong>，每个阶段都由<strong>协调者</strong>和<strong>参与者</strong>共同完成。</p><h3 id="mysql为什么需要二阶段提交"><a href="#mysql为什么需要二阶段提交" class="headerlink" title="mysql为什么需要二阶段提交"></a>mysql为什么需要二阶段提交</h3><p>事务提交后，redo log 和 binlog 都要持久化到磁盘，<strong>但是这两个是独立的逻辑，可能出现半成功的状态</strong>，这样就造成两份日志之间的逻辑不一致。</p><p><img data-src="/transblog/images/435884609258196.png"></p><p>假设 id &#x3D; 1 这行数据的字段 name 的值原本是 ‘周杰伦’，然后执行 UPDATE t_user SET name &#x3D; ‘林俊杰’ WHERE id &#x3D; 1; 如果在持久化 redo log 和 binlog 两个日志的过程中，出现了半成功状态，那么就有两种情况：</p><ul><li><p><strong>如果在将 redo log 刷入到磁盘之后， MySQL 突然宕机了，而 binlog 还没有来得及写入</strong>。MySQL 重启后，通过 redo log 能将 Buffer Pool 中 id &#x3D; 1 这行数据的 name 字段恢复到新值 ‘林俊杰’，但是 binlog 里面没有记录这条更新语句，<strong>在主从架构中，binlog 会被复制到从库</strong>，由于 binlog 丢失了这条更新语句，从库的这一行 name 字段是旧值 ‘周杰伦’，与主库的值不一致性；</p></li><li><p><strong>如果在将 binlog 刷入到磁盘之后， MySQL 突然宕机了，而 redo log 还没有来得及写入</strong>。由于 redo log 还没写，崩溃恢复以后这个事务无效，所以 id &#x3D; 1 这行数据的 name 字段还是旧值 ‘周杰伦’，而 binlog 里面记录了这条更新语句，<strong>在主从架构中，binlog 会被复制到从库</strong>，从库执行了这条更新语句，那么这一行 name 字段是新值 ‘林俊杰’，与主库的值不一致性；</p></li></ul><p>总结：redo log 影响主库的数据，binlog 影响从库的数据，所以 redo log 和 binlog 必须保持一致才能保证主从数据一致。</p><p>MySQL 为了避免出现两份日志之间的逻辑不一致的问题，使用了「两阶段提交」来解决，两阶段提交其实是分布式事务一致性协议，它可以保证多个逻辑操作要不全部成功，要不全部失败，不会出现半成功的状态。</p><h3 id="mysql二阶段提交流程"><a href="#mysql二阶段提交流程" class="headerlink" title="mysql二阶段提交流程"></a>mysql二阶段提交流程</h3><p>为了保证这两个日志的一致性，MySQL 使用了内部 XA 事务，内部 XA 事务由 binlog 作为协调者，存储引擎是参与者。</p><p>当客户端执行 commit 语句或者在自动提交的情况下，MySQL 内部开启一个 XA 事务，分两阶段来完成 XA 事务的提交，如下图：</p><p><img data-src="/transblog/images/151382310240911.png" alt="Mysql两阶段提交"></p><p>事务的提交过程有两个阶段，就是将 redo log 的写入拆成了两个步骤：prepare 和 commit，中间再穿插写入binlog，具体如下：</p><ul><li>prepare 阶段：将 XID（内部 XA 事务的 ID） 写入到 redo log，同时将 redo log 对应的事务状态设置为 prepare，然后将 redo log 持久化到磁盘（innodb_flush_log_at_trx_commit &#x3D; 1 的作用）；</li><li>commit 阶段：把 XID 写入到 binlog，然后将 binlog 持久化到磁盘（sync_binlog &#x3D; 1 的作用），接着调用引擎的提交事务接口，将 redo log 状态设置为 commit，此时该状态并不需要持久化到磁盘，只需要 write 到文件系统的 page cache 中就够了，因为只要 binlog 写磁盘成功，就算 redo log 的状态还是 prepare 也没有关系，一样会被认为事务已经执行成功；</li></ul><h3 id="mysql二阶段提交时重启会发生什么"><a href="#mysql二阶段提交时重启会发生什么" class="headerlink" title="mysql二阶段提交时重启会发生什么"></a>mysql二阶段提交时重启会发生什么</h3><p><img data-src="/transblog/images/3721011258040.png" alt="两阶段提交崩溃点"></p><p>不管是时刻 A，还是时刻 B 崩溃，此时的 redo log 都处于 prepare 状态。在 MySQL 重启后会按顺序扫描 redo log 文件，碰到处于 prepare 状态的 redo log，就拿着 redo log 中的 XID 去 binlog 查看是否存在此 XID：</p><ul><li>如果 binlog 中没有当前内部 XA 事务的 XID，说明 redolog 完成刷盘，但是 binlog 还没有刷盘，则回滚事务。对应时刻 A 崩溃恢复的情况。</li><li>如果 binlog 中有当前内部 XA 事务的 XID，说明 redolog 和 binlog 都已经完成了刷盘，则提交事务。对应时刻 B 崩溃恢复的情况。</li></ul><p>即可以提交事务，也可以回滚事务，取决于是否能在 binlog 中查找到与 redo log 相同的 XID，这样就可以保证 redo log 和 binlog 这两份日志的一致性了。</p><h3 id="mysql两阶段提交引入的问题"><a href="#mysql两阶段提交引入的问题" class="headerlink" title="mysql两阶段提交引入的问题"></a>mysql两阶段提交引入的问题</h3><h4 id="磁盘-I-O-次数高"><a href="#磁盘-I-O-次数高" class="headerlink" title="磁盘 I&#x2F;O 次数高"></a>磁盘 I&#x2F;O 次数高</h4><p>binlog 会缓存在 binlog cache，redo log 会缓存在 redo log buffer。对于“双1”配置，每个事务提交都会进行两次 fsync（刷盘），一次是 redo log 刷盘，另一次是 binlog 刷盘，所以这会成为性能瓶颈。</p><ul><li>当 sync_binlog &#x3D; 1 的时候，表示每次提交事务都会将 binlog cache 里的 binlog 直接持久到磁盘；</li><li>当 innodb_flush_log_at_trx_commit &#x3D; 1 时，表示每次事务提交时，都将缓存在 redo log buffer 里的 redo log 直接持久化到磁盘；</li></ul><h4 id="锁竞争激烈"><a href="#锁竞争激烈" class="headerlink" title="锁竞争激烈"></a>锁竞争激烈</h4><p>两阶段提交虽然能够保证「单事务」两个日志的内容一致，但在「多事务」的情况下，却不能保证两者的提交顺序一致，因此，在两阶段提交的流程基础上，<strong>还需要加一个锁(prepare_commit_mutex)来保证提交的原子性，从而保证多事务的情况下，两个日志的提交顺序一致</strong>。</p><p>通过加锁虽然完美地解决了顺序一致性的问题，但在并发量较大的时候，就会导致对锁的争用，性能不佳。所以MySQL引入了组提交</p><h5 id="组提交"><a href="#组提交" class="headerlink" title="组提交"></a>组提交</h5><p>组提交（group commit）机制，当有多个事务提交的时候，会将多个刷盘操作合并成一个，从而减少磁盘 I&#x2F;O 的次数，如果说 10 个事务依次排队刷盘的时间成本是 10，那么将这 10 个事务一次性一起刷盘的时间成本则近似于 1。</p><p><strong>引入了组提交机制后，prepare 阶段不变，只针对 commit 阶段</strong>，将 commit 阶段拆分为三个过程：</p><ol><li><p>flush 阶段：多个事务按进入的顺序将 binlog 从 cache 写入文件（不刷盘）（5.7后融合了prepare，即redo log的write + fsync）<br>在 5.7 版本中，做了个改进，融合了prepare阶段，即redo log的write + fsync。在 prepare 阶段不再让事务各自执行 redo log 刷盘操作，而是<strong>推迟到组提交的 flush 阶段，也就是说 prepare 阶段融合在了 flush 阶段、sync 阶段之前</strong>。通过延迟写 redo log 的方式，为 redolog 做了一次组写入，这样 binlog 和 redo log 都进行了优化。<br><mark>如果在这一步完成后数据库崩溃，由于 binlog 中没有该组事务的记录，所以 MySQL 会在重启后回滚该组事务</mark></p></li><li><p>sync 阶段：对 binlog 文件做 fsync 操作（多个事务的 binlog 合并一次刷盘）；</p><ul><li>binlog_group_commit_sync_delay&#x3D; N，表示在等待 N 微妙后，直接调用 fsync，将处于文件系统中 page cache 中的 binlog 刷盘，也就是将「 binlog 文件」持久化到磁盘。</li><li>binlog_group_commit_sync_no_delay_count &#x3D; N，表示如果队列中的事务数达到 N 个，就忽视binlog_group_commit_sync_delay 的设置，直接调用 fsync，将处于文件系统中 page cache 中的 binlog 刷盘。</li></ul><p> <mark>如果在这一步完成后数据库崩溃，由于 binlog 中已经有了事务记录，MySQL会在重启后通过 redo log 刷盘的数据继续进行事务的提交。</mark></p></li><li><p>commit 阶段：各个事务按顺序做 InnoDB commit 操作；<br>最后进入 commit 阶段，调用引擎的提交事务接口，将 redo log 状态设置为 commit。</p></li></ol><p>上面的每个阶段都有一个队列，<strong>每个阶段有锁进行保护</strong>，因此保证了事务写入的顺序，第一个进入队列的事务会成为 leader，leader领导所在队列的所有事务，全权负责整队的操作，完成后通知队内其他事务操作结束。</p><p>对每个阶段引入了队列后，锁就只针对每个队列进行保护，不再锁住提交事务的整个过程，可以看的出来，锁粒度减小了，这样就使得多个阶段可以并发执行，从而提升效率。</p><p><img data-src="/transblog/images/353123017259336.png"></p><h1 id="一条sql语句的执行流程"><a href="#一条sql语句的执行流程" class="headerlink" title="一条sql语句的执行流程"></a>一条sql语句的执行流程</h1><p><img data-src="/transblog/images/5001518199187.png"></p><p>MySQL 主要分为 Server 层和存储引擎层：</p><ul><li><strong>Server 层：大多数的核心功能模块</strong>；主要包括连接器、查询缓存、分析器、优化器、执行器等，所有跨存储引擎的功能都在这一层实现，比如存储过程、触发器、视图，函数等，还有一个通用的日志模块 binglog 日志模块。</li><li><strong>存储引擎： 主要负责数据的存储和读取</strong>，采用可以替换的插件式架构，同的存储引擎共用一个 Server 层。支持 InnoDB、MyISAM、Memory 等多个存储引擎，其中 InnoDB 引擎有自有的日志模块 redolog 模块。</li></ul><h2 id="①连接器"><a href="#①连接器" class="headerlink" title="①连接器"></a>①连接器</h2><p>连接的过程需要先经过 TCP 三次握手，因为 MySQL 是基于 TCP 协议进行传输的</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u root -p</span><br></pre></td></tr></table></figure><p>如果用户密码都没有问题，连接器就会获取该用户的权限，然后保存起来，后续该用户在此连接里的任何操作，都会基于连接开始时读到的权限进行权限逻辑的判断。所以，如果一个用户已经建立了连接，即使管理员中途修改了该用户的权限，也不会影响已经存在连接的权限。修改完成后，只有再新建的连接才会使用新的权限设置。</p><h3 id="如何查看-MySQL-服务被多少个客户端连接了？"><a href="#如何查看-MySQL-服务被多少个客户端连接了？" class="headerlink" title="如何查看 MySQL 服务被多少个客户端连接了？"></a>如何查看 MySQL 服务被多少个客户端连接了？</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">show processlist</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/411774514257357.png"></p><p>共有两个用户名为 root 的用户连接了 MySQL 服务，其中 id 为 6 的用户的 Command 列的状态为 Sleep ，这意味着该用户连接完 MySQL 服务就没有再执行过任何命令，也就是说这是一个空闲的连接，并且空闲的时长是 736 秒（ Time 列）。</p><h3 id="空闲连接是否会一直占用？"><a href="#空闲连接是否会一直占用？" class="headerlink" title="空闲连接是否会一直占用？"></a>空闲连接是否会一直占用？</h3><p>由 wait_timeout 参数控制的，默认值是 8 小时（28880秒），如果空闲连接超过了这个时间，连接器就会自动将它断开。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; show variables like <span class="string">&#x27;wait_timeout&#x27;</span>;</span><br><span class="line">+---------------+-------+</span><br><span class="line">| Variable_name | Value |</span><br><span class="line">+---------------+-------+</span><br><span class="line">| wait_timeout  | 28800 |</span><br><span class="line">+---------------+-------+</span><br><span class="line">1 row <span class="keyword">in</span> <span class="built_in">set</span> (0.00 sec)</span><br></pre></td></tr></table></figure><p>可以手动断开空闲的连接，使用的是 kill connection + id 的命令。一个处于空闲状态的连接被服务端主动断开后，这个客户端并不会马上知道，等到客户端在发起下一个请求的时候，才会收到这样的报错“ERROR 2013 (HY000): Lost connection to MySQL server during query”。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; <span class="built_in">kill</span> connection +6;</span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br></pre></td></tr></table></figure><h3 id="MySQL-的连接数有限制吗？"><a href="#MySQL-的连接数有限制吗？" class="headerlink" title="MySQL 的连接数有限制吗？"></a>MySQL 的连接数有限制吗？</h3><p>由 max_connections 参数控制，超过这个值，系统就会拒绝接下来的连接请求，并报错提示“Too many connections”。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; show variables like <span class="string">&#x27;max_connections&#x27;</span>;</span><br><span class="line">+-----------------+-------+</span><br><span class="line">| Variable_name   | Value |</span><br><span class="line">+-----------------+-------+</span><br><span class="line">| max_connections | <span class="number">151</span>   |</span><br><span class="line">+-----------------+-------+</span><br><span class="line"><span class="number">1</span> <span class="function">row in <span class="title">set</span> <span class="params">(<span class="number">0.00</span> sec)</span></span></span><br></pre></td></tr></table></figure><h2 id="②查询缓存"><a href="#②查询缓存" class="headerlink" title="②查询缓存"></a>②查询缓存</h2><p>连接器工作完成后，客户端就可以向 MySQL 服务发送 SQL 语句了，MySQL 服务收到 SQL 语句后，就会解析出 SQL 语句的第一个字段，看看是什么类型的语句。</p><p>如果 SQL 是查询语句（select 语句），MySQL 就会先去查询缓存（ Query Cache ）里查找缓存数据，看看之前有没有执行过这一条命令，这个查询缓存是以 key-value 形式保存在内存中的，key 为 SQL 查询语句，value 为 SQL 语句查询的结果。</p><p>如果查询的语句命中查询缓存，那么就会直接返回 value 给客户端。如果查询的语句没有命中查询缓存中，那么就要往下继续执行，等执行完后，查询的结果就会被存入查询缓存中。<strong>这里说的查询缓存是 server 层的，也就是 MySQL 8.0 版本移除的是 server 层的查询缓存，并不是 Innodb 存储引擎中的 buffer pool。</strong></p><p>但其实查询缓存挺鸡肋的一批：对于更新比较频繁的表，查询缓存的命中率很低的，因为只要一个表有更新操作，那么这个表的查询缓存就会被清空。如果刚缓存了一个查询结果很大的数据，还没被使用的时候，刚好这个表有更新操作，查询缓冲就被清空了，<strong>相当于缓存了个寂寞。</strong></p><p>所以，MySQL 8.0 版本直接将查询缓存删掉了，也就是说 MySQL 8.0 开始，执行一条 SQL 查询语句，不会再走到查询缓存这个阶段了。对于 MySQL 8.0 之前的版本，如果想关闭查询缓存，可以通过将参数 query_cache_type 设置成 DEMAND。</p><h2 id="③解析器"><a href="#③解析器" class="headerlink" title="③解析器"></a>③解析器</h2><p>在正式执行 SQL 查询语句之前， MySQL 会先对 SQL 语句做解析，这个工作交由「解析器」来完成。</p><ol><li>词法分析。MySQL 会根据你输入的字符串识别出关键字出来，例如，SQL语句 select username from userinfo，在分析之后，会得到4个Token，其中有2个Keyword，分别为select和from：</li></ol><table><thead><tr><th align="center">关键字</th><th align="center">非关键字</th><th align="center">关键字</th><th align="center">非关键字</th></tr></thead><tbody><tr><td align="center">select</td><td align="center">username</td><td align="center">from</td><td align="center">userinfo</td></tr></tbody></table><ol start="2"><li>语法分析。根据词法分析的结果，语法解析器会根据语法规则，判断你输入的这个 SQL 语句是否满足 MySQL 语法，如果没问题就会构建出 SQL 语法树，这样方便后面模块获取 SQL 类型、表名、字段名、 where 条件等等。</li></ol><p><img data-src="/transblog/images/478183015240571.png"></p><p><strong>解析器只负责检查语法和构建语法树，但是不会去查表或者字段存不存在。</strong></p><h2 id="④执行SQL"><a href="#④执行SQL" class="headerlink" title="④执行SQL"></a>④执行SQL</h2><h3 id="预处理阶段"><a href="#预处理阶段" class="headerlink" title="预处理阶段"></a>预处理阶段</h3><ol><li>检查 SQL 查询语句中的表或者字段是否存在；</li><li>将 select * 中的 * 符号，扩展为表上的所有列；</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; <span class="keyword">select</span> * from <span class="built_in">test</span>;</span><br><span class="line">ERROR 1146 (42S02): Table <span class="string">&#x27;mysql.test&#x27;</span> doesn<span class="string">&#x27;t exist</span></span><br></pre></td></tr></table></figure><p>如果表是不存在的，这时 MySQL 就会在执行 SQL 查询语句的 prepare 阶段中（get_table_share() 函数）报错。</p><h3 id="优化阶段"><a href="#优化阶段" class="headerlink" title="优化阶段"></a>优化阶段</h3><p>经过预处理阶段后，还需要为 SQL 查询语句先制定一个执行计划，这个工作交由「优化器」来完成的。</p><p>优化器主要负责将 SQL 查询语句的执行方案确定下来，比如在表里面有多个索引的时候，优化器会基于查询成本的考虑，来决定选择使用哪个索引。要想知道优化器选择了哪个索引，可以在查询语句最前面加个 explain 命令，这样就会输出这条 SQL 语句的执行计划，然后执行计划中的 key 就表示执行过程中使用了哪个索引。</p><h3 id="执行阶段"><a href="#执行阶段" class="headerlink" title="执行阶段"></a>执行阶段</h3><p>经历完优化器后，就确定了执行方案，接下来 MySQL 就真正开始执行语句了，这个工作是由「执行器」完成的。在执行的过程中，执行器就会和存储引擎交互了，交互是以记录为单位的。</p><p>执行器和存储引擎的交互过程</p><h4 id="执行计划"><a href="#执行计划" class="headerlink" title="执行计划"></a>执行计划</h4><p>即在通用的sql前添加explian。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">explain sql</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/360462117256539.png"></p><h5 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h5><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>id</td><td>查询序列号，标识执行的顺序</td></tr><tr><td>select_type</td><td>查询类型，普通查询、联合查询和子查询等</td></tr><tr><td>table</td><td>查询的表名</td></tr><tr><td>partitions</td><td>查询涉及的分区</td></tr><tr><td>type</td><td>执行计划类型</td></tr><tr><td>possible_keys</td><td>查询过程可能用到的索引</td></tr><tr><td>key</td><td>实际使用的索引，如果为NULL，则没有使用索引</td></tr><tr><td>key_len</td><td>索引长度</td></tr><tr><td>ref</td><td>索引字段关联对应表的字段</td></tr><tr><td>rows</td><td>查询扫描的行数</td></tr><tr><td>filtered</td><td>返回结果占扫描行数的比例，值越大，说明查询效率越高，查询即扫描</td></tr><tr><td>extra</td><td>额外信息</td></tr></tbody></table><p><strong>1. id</strong><br>id相同，执行顺序自上而下。<br>id不同，如果是子查询，id需要递增，id值越大，优先级越高，执行顺序越靠前。</p><p><strong>2. select_type</strong><br>查询类型，分为普通查询、联合查询和子查询等。</p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>SIMPLE</td><td>简单select查询，查询中不包括子查询或联合查询</td></tr><tr><td>PRIMARY</td><td>查询中包含子查询，最外层查询标记为PRIMARY</td></tr><tr><td>SUBQUERY</td><td><code>SELECT</code>或<code>WHERE</code>中包含了子查询</td></tr><tr><td>MATERIALIZED</td><td><code>WHERE</code>后面<code>IN</code>条件的子查询</td></tr><tr><td>UNION</td><td><code>UNION</code>中的第二个<code>UNION</code>或<code>SELECT</code></td></tr><tr><td>UNION RESULT</td><td><code>UNION</code>结果</td></tr></tbody></table><p><strong>3. table</strong><br>查询的表。</p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>table_name</td><td>表名</td></tr><tr><td><code>&lt;unionM,N&gt;</code></td><td>联合查询，其中M和N为id（查询序列号），表示联合查询产生的结果</td></tr><tr><td><code>&lt;·subqueryN·&gt;</code></td><td>子查询结果，N为id（查询序列号）</td></tr></tbody></table><p><span id="target7"><strong>4. type</strong></span></p><p>执行计划类型，执行效率指标，共有7个维度，效率为：system&gt;const&gt;eq_ref&gt;ref&gt;range&gt;index&gt;ALL</p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>system</td><td>系统表，少数数据，往往无需进行磁盘IO</td></tr><tr><td>const</td><td>主键或者唯一索引的常量查询，表格最多只有一行记录符合查询</td></tr><tr><td>eq_ref</td><td>主键索引或非空唯一索引等值扫描</td></tr><tr><td>ref</td><td>非主键非唯一索引等值扫描</td></tr><tr><td>range</td><td>范围扫描</td></tr><tr><td>index</td><td>索引全扫描（遍历索引树）</td></tr><tr><td>ALL</td><td>全表扫描（遍历整个表的数据）</td></tr></tbody></table><p><strong>5. extra</strong></p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>Using filesort</td><td>对数据使用一个外部的文件进行排序，而不是按照表内的索引进行排序读取</td></tr><tr><td>Using temporary</td><td>使用临时表保存中间结果，常见于<code>ORDER BY</code>或<code>GROUP BY</code>操作</td></tr><tr><td>Using index</td><td>使用覆盖索引，避免访问表的数据行，效率高</td></tr><tr><td>Using index condition</td><td>命中索引，但不是所有的列都在索引树上，还需要访问实际的行记录</td></tr><tr><td>Using where</td><td>操作使用<code>WHERE</code>过滤条件</td></tr><tr><td>Select tables optimized away</td><td>基于索引优化<code>MIN/MAX</code>操作或者<code>MyISAM</code>存储引擎优化<code>COUNT(*)</code>操作，不必等到执行阶段再进行计算，查询执行计划生成的阶段即可完成优化</td></tr><tr><td>Using join buffer (Block Nested Loop)</td><td>SQL操作使用了关联查询或子查询且使用嵌套循环</td></tr></tbody></table><h4 id="主键索引查询"><a href="#主键索引查询" class="headerlink" title="主键索引查询"></a>主键索引查询</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> product <span class="keyword">where</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>这条查询语句的查询条件用到了主键索引，而且是等值查询，同时主键 id 是唯一，不会有 id 相同的记录，所以优化器决定选用访问类型为 const 进行查询，也就是使用主键索引查询一条记录，那么执行器与存储引擎的执行流程是这样的：</p><ul><li>执行器第一次查询，会调用 read_first_record 函数指针指向的函数，因为优化器选择的访问类型为 const，这个函数指针被指向为 InnoDB 引擎索引查询的接口，把条件 id &#x3D; 1 交给存储引擎，让存储引擎定位符合条件的第一条记录。</li><li>存储引擎通过主键索引的 B+ 树结构定位到 id &#x3D; 1的第一条记录，如果记录是不存在的，就会向执行器上报记录找不到的错误，然后查询结束。如果记录是存在的，就会将记录返回给执行器；</li><li>执行器从存储引擎读到记录后，接着判断记录是否符合查询条件，如果符合则发送给客户端，如果不符合则跳过该记录。</li><li>执行器查询的过程是一个 while 循环，所以还会再查一次，但是这次因为不是第一次查询了，所以会调用 read_record 函数指针指向的函数，因为优化器选择的访问类型为 const，这个函数指针被指向为一个永远返回 - 1 的函数，所以当调用该函数的时候，执行器就退出循环，也就是结束查询了。</li></ul><h4 id="全表扫描"><a href="#全表扫描" class="headerlink" title="全表扫描"></a>全表扫描</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> product <span class="keyword">where</span> name <span class="operator">=</span> <span class="string">&#x27;iphone&#x27;</span>;</span><br></pre></td></tr></table></figure><p>这条查询语句的查询条件没有用到索引，所以优化器决定选用访问类型为 ALL 进行查询，也就是全表扫描的方式查询，那么这时执行器与存储引擎的执行流程是这样的：</p><ul><li>执行器第一次查询，会调用 read_first_record 函数指针指向的函数，因为优化器选择的访问类型为 all，这个函数指针被指向为 InnoDB 引擎全扫描的接口，让存储引擎读取表中的第一条记录；</li><li>执行器会判断读到的这条记录的 name 是不是 iphone，如果不是则跳过；如果是则将记录发给客户的（是的没错，<strong>Server 层每从存储引擎读到一条记录就会发送给客户端</strong>，之所以客户端显示的时候是直接显示所有记录的，是因为客户端是等查询语句查询完成后，才会显示出所有的记录）。</li><li>执行器查询的过程是一个 while 循环，所以还会再查一次，会调用 read_record 函数指针指向的函数，因为优化器选择的访问类型为 all，read_record 函数指针指向的还是 InnoDB 引擎全扫描的接口，所以接着向存储引擎层要求继续读刚才那条记录的下一条记录，存储引擎把下一条记录取出后就将其返回给执行器（Server层），执行器继续判断条件，不符合查询条件即跳过该记录，否则发送到客户端；</li><li>一直重复上述过程，直到存储引擎把表中的所有记录读完，然后向执行器（Server层） 返回了读取完毕的信息；</li><li>执行器收到存储引擎报告的查询完毕的信息，退出循环，停止查询。</li></ul><h4 id="索引下推"><a href="#索引下推" class="headerlink" title="索引下推"></a>索引下推</h4><p>索引下推能够<strong>减少二级索引在查询时的回表操作</strong>，提高查询的效率，因为它将 Server 层部分负责的事情，交给存储引擎层去处理了。<br><a href="#target6">点击此处跳转到回表</a><br>举一个具体的例子，对 age 和 reward 字段建立了联合索，有如下sql:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> t_user  <span class="keyword">where</span> age <span class="operator">&gt;</span> <span class="number">20</span> <span class="keyword">and</span> reward <span class="operator">=</span> <span class="number">100000</span>;</span><br></pre></td></tr></table></figure><p>联合索引当遇到范围查询 (&gt;、&lt;) 就会停止匹配，也就是 age 字段能用到联合索引，但是 reward 字段则无法利用到索引</p><p>那么，不使用索引下推（MySQL 5.6 之前的版本）时，执行器与存储引擎的执行流程是这样的：</p><ul><li>Server 层首先调用存储引擎的接口定位到满足查询条件的第一条二级索引记录，也就是定位到 age &gt; 20 的第一条记录；</li><li>存储引擎根据二级索引的 B+ 树快速定位到这条记录后，获取主键值，然后进行回表操作，将完整的记录返回给 Server 层；</li><li>Server 层在判断该记录的 reward 是否等于 100000，如果成立则将其发送给客户端；否则跳过该记录；</li><li>接着，继续向存储引擎索要下一条记录，存储引擎在二级索引定位到记录后，获取主键值，然后回表操作，将完整的记录返回给 Server 层；</li><li>如此往复，直到存储引擎把表中的所有记录读完。</li></ul><p>可以看到，没有索引下推的时候，每查询到一条二级索引记录，都要进行回表操作，然后将记录返回给 Server，接着 Server 再判断该记录的 reward 是否等于 100000。而使用索引下推后，判断记录的 reward 是否等于 100000 的工作交给了存储引擎层，过程如下 ：</p><ul><li>Server 层首先调用存储引擎的接口定位到满足查询条件的第一条二级索引记录，也就是定位到 age &gt; 20 的第一条记录；</li><li>存储引擎定位到二级索引后，先不执行回表操作，而是先判断一下该索引中包含的列（reward列）的条件（reward 是否等于 100000）是否成立。如果条件不成立，则直接跳过该二级索引。如果成立，则执行回表操作，将完成记录返回给 Server 层。</li><li>Server 层在判断其他的查询条件（本次查询没有其他条件）是否成立，如果成立则将其发送给客户端；否则跳过该记录，然后向存储引擎索要下一条记录。</li><li>如此往复，直到存储引擎把表中的所有记录读完。</li></ul><p>可以看到，使用了索引下推后，虽然 reward 列无法使用到联合索引，但是因为它包含在联合索引（age，reward）里，所以直接在存储引擎过滤出满足 reward &#x3D; 100000 的记录后，才去执行回表操作获取整个记录。相比于没有使用索引下推，节省了很多回表操作。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol><li>客户端向MySQL服务器发送一条查询请求</li><li>服务器首先先检查查询缓存，如果命中缓存，则立刻返回存储在缓存中的结果。否则进入下一阶段</li><li>服务器进行SQL解析、预处理、再由优化器生成对应的执行计划</li><li>MySQL根据执行计划，调用存储引擎的API来执行查询</li><li>将结果返回给客户端，同时缓存查询结果</li></ol><h1 id="mysql常见问题"><a href="#mysql常见问题" class="headerlink" title="mysql常见问题"></a>mysql常见问题</h1><h2 id="mysql单表数据为什么不要超过两千万"><a href="#mysql单表数据为什么不要超过两千万" class="headerlink" title="mysql单表数据为什么不要超过两千万"></a>mysql单表数据为什么不要超过两千万</h2><p>同样一个 16K 的页，非叶子节点里的每条数据都指向新的页，而新的页有两种可能：如果是叶子节点，那么里面就是一行行的数据，如果是非叶子节点的话，那么就会继续指向新的页</p><p>假设：</p><ol><li>非叶子节点内指向其他页的数量为 x</li><li>叶子节点内能容纳的数据行数为 y</li><li>B+ 数的层数为 z</li></ol><p>如下图中所示，<strong>Total &#x3D;x^(z-1) * y 也就是说总数会等于 x 的 z-1 次方 与 Y 的乘积</strong>。</p><p><img data-src="/transblog/images/400924209267567.png"></p><p><a href="#target1">点击此处跳转到mysql的页</a><br>File Header (38 byte)、Page Header (56 Byte)、Infimum + Supermum（26 byte）、File Trailer（8byte）, 再加上页目录，大概 1k 左右。剩下 15k 用于存数据，在索引页中主要记录的是主键与页号，主键我们假设是 Bigint (8 byte), 而页号也是固定的（4Byte）, 那么索引页中的一条数据也就是 12byte。所以<mark>X &#x3D;15 * 1024&#x2F;12≈1280 行 </mark>，对于y来说，同理，能放数据的空间也是 15k。暂时按一条行数据 1k 来算，那一页就能存下 15 条，<mark> Y &#x3D; 15*1024&#x2F;1000 ≈15 </mark></p><p>已知 x&#x3D;1280，y&#x3D;15：</p><ul><li>假设 B+ 树是两层，那就是 z &#x3D; 2， Total &#x3D; （1280 ^1 ）*15 &#x3D; 19200</li><li>假设 B+ 树是三层，那就是 z &#x3D; 3， Total &#x3D; （1280 ^2） *15 &#x3D; 24576000 <strong>（约 2.45kw）</strong></li></ul><p>如果是 4 层，除了查询的时候磁盘 IO 次数会增加，Total 值大概是 3 百多亿，也不太合理，所以，3 层应该是比较合理的一个值。</p><p>那假如实际当行的数据占用空间不是 1K , 而是 5K, 那么单个数据页最多只能放下 3 条数据。同样，还是按照 z &#x3D; 3 的值来计算，</p><p>那 Total &#x3D; （1280 ^2） *3 &#x3D; 4915200 （近 500w）</p><p>所以，在保持相同的层级（相似查询性能）的情况下，在行数据大小不同的情况下，其实这个最大建议值也是不同的，而且影响查询性能的还有很多其他因素，比如，数据库版本，服务器配置，sql 的编写等等。</p><p>MySQL 为了提高性能，会将表的索引装载到内存中，在 InnoDB buffer size 足够的情况下，其能完成全加载进内存，查询不会有问题。</p><p>但是，当单表数据库到达某个量级的上限时，导致内存无法存储其索引，使得之后的 SQL 查询会产生磁盘 IO，从而导致性能下降，所以增加硬件配置（比如把内存当磁盘使），可能会带来立竿见影的性能提升哈。</p><h2 id="mysql磁盘-I-O-很高优化的方法"><a href="#mysql磁盘-I-O-很高优化的方法" class="headerlink" title="mysql磁盘 I&#x2F;O 很高优化的方法"></a>mysql磁盘 I&#x2F;O 很高优化的方法</h2><ol><li><p>设置组提交的两个参数：<br> binlog_group_commit_sync_delay 和 binlog_group_commit_sync_no_delay_count 参数，延迟 binlog 刷盘的时机，从而减少 binlog 的刷盘次数。这个方法是基于“额外的故意等待”来实现的，因此可能会增加语句的响应时间，但即使 MySQL 进程中途挂了，也没有丢失数据的风险，因为 binlog 早被写入到 page cache 了，只要系统没有宕机，缓存在 page cache 里的 binlog 就会被持久化到磁盘。</p></li><li><p>将 sync_binlog 设置为大于 1 的值（比较常见是 100~1000）:<br>表示每次提交事务都 write，但累积 N 个事务后才 fsync，相当于延迟了 binlog 刷盘的时机。但是这样做的风险是，主机掉电时会丢 N 个事务的 binlog 日志。</p></li><li><p>将 innodb_flush_log_at_trx_commit 设置为 2:<br>表示每次事务提交时，都只是缓存在 redo log buffer 里的 redo log 写到 redo log 文件，注意写入到「 redo log 文件」并不意味着写入到了磁盘，因为操作系统的文件系统中有个 Page Cache，专门用来缓存文件数据的，所以写入「 redo log文件」意味着写入到了操作系统的文件缓存，然后交由操作系统控制持久化到磁盘的时机。但是这样做的风险是，主机掉电的时候会丢数据。</p></li></ol><h2 id="update语句的执行过程"><a href="#update语句的执行过程" class="headerlink" title="update语句的执行过程"></a>update语句的执行过程</h2><p>具体更新一条记录 UPDATE t_user SET name &#x3D; ‘刘德华’ WHERE id &#x3D; 1; 的流程如下:</p><ol><li><p>执行器负责具体执行，会调用存储引擎的接口，通过主键索引树搜索获取 id &#x3D; 1 这一行记录：</p><ul><li>如果 id&#x3D;1 这一行所在的数据页本来就在 buffer pool 中，就直接返回给执行器更新；</li><li>如果记录不在 buffer pool，将数据页从磁盘读入到 buffer pool，返回记录给执行器。</li></ul></li><li><p>执行器得到聚簇索引记录后，会看一下更新前的记录和更新后的记录是否一样：<br>如果一样的话就不进行后续更新流程；如果不一样的话就把更新前的记录和更新后的记录都当作参数传给 InnoDB 层，让 InnoDB 真正的执行更新记录的操作；</p></li><li><p>开启事务， InnoDB 层更新记录前，首先要记录相应的 undo log，因为这是更新操作，需要把被更新的列的旧值记下来，也就是要生成一条 undo log，undo log 会写入 Buffer Pool 中的 Undo 页面，不过在内存修改该 Undo 页面后，需要记录对应的 redo log。</p></li><li><p>InnoDB 层开始更新记录，会先更新内存（同时标记为脏页），然后将记录写到 redo log 里面，这个时候更新就算完成了。为了减少磁盘I&#x2F;O，不会立即将脏页写入磁盘，后续由后台线程选择一个合适的时机将脏页写入到磁盘。这就是 WAL 技术，MySQL 的写操作并不是立刻写到磁盘上，而是先写 redo 日志，然后在合适的时间再将修改的行数据写到磁盘上。<strong>至此，一条记录更新完了</strong>。</p></li><li><p>在一条更新语句执行完成后，然后开始记录该语句对应的 binlog，此时记录的 binlog 会被保存到 binlog cache，并没有刷新到硬盘上的 binlog 文件，在事务提交时才会统一将该事务运行过程中的所有 binlog 刷新到硬盘。</p></li><li><p>事务提交，两阶段提交</p></li></ol><h2 id="mysql分页查询的性能"><a href="#mysql分页查询的性能" class="headerlink" title="mysql分页查询的性能"></a>mysql分页查询的性能</h2><p>Server 层维护了一个称作 limit_count 的变量用于统计已经跳过了多少条记录。limit m , n 工作原理就是先读取前面 m+n 条记录，然后抛弃前 m条，读后面 n条想要的，所以 m越大，偏移量越大，性能就越差。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">1</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">order</span> <span class="keyword">by</span> id limit m, n;</span><br></pre></td></tr></table></figure><p>该语句的意思就是查询m+n条记录，去掉前m条，返回后n条。无疑该查询能够实现分页，但m越大，查询性能就越低，因为MySQL需要扫描全部m+n条记录。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">2</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">where</span> id <span class="operator">&gt;</span> <span class="variable">@max_id</span> <span class="keyword">order</span> <span class="keyword">by</span> id limit n;</span><br></pre></td></tr></table></figure><p>该查询同样会返回后n条记录，却无需像方式1扫描前m条记录，但必须在每次查询时拿到上一次查询（上一页）的最大id（或最小id）。该查询的问题也在于我们不一定能拿到这个id，比如当前在第3页，需要查询第5页的数据，就不行了。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">3</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">where</span> id <span class="operator">&gt;</span> <span class="variable">@max_id</span> <span class="keyword">order</span> <span class="keyword">by</span> id limit <span class="number">10</span>, <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p>为了避免方式2不能实现的跨页查询，就需要结合方式1。性能需要，m得尽量小，比如当前在第3页，需要查询第5页，每页10条数据，而当前第3页的最大id为max_id，该查询方式就部分解决了方式2的问题，但如果当前在第2页，要查第1000页，性能仍然较差。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">4</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">as</span> a <span class="keyword">inner</span> <span class="keyword">join</span> (<span class="keyword">select</span> id <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">order</span> <span class="keyword">by</span> id limit m, n) <span class="keyword">as</span> b <span class="keyword">on</span> a.id <span class="operator">=</span> b.id <span class="keyword">order</span> <span class="keyword">by</span> a.id;</span><br></pre></td></tr></table></figure><p>该查询同方式1一样，m的值可能很大，但由于内部的子查询只扫描了id字段，而非全表，所以性能要强于方式1，并且能够解决方式2和方式3不能解决的问题。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">5</span>) <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">where</span> id <span class="operator">&gt;</span> (<span class="keyword">select</span> id <span class="keyword">from</span> <span class="keyword">table</span> <span class="keyword">order</span> <span class="keyword">by</span> id limit m, <span class="number">1</span>) limit n;</span><br></pre></td></tr></table></figure><p>该查询同样是通过子查询扫描字段id，效果同方式4。但方式5的性能会略好于方式4，因为方式5不需要进行表的关联，而是一个简单的比较，在不知道上一页最大id的情况下，是比较推荐的用法。</p><center><font size="32">------赞助耶耶，加快更新！------</font></center><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;基础&quot;&gt;&lt;a href=&quot;#基础&quot; class=&quot;headerlink&quot; title=&quot;基础&quot;&gt;&lt;/a&gt;基础&lt;/h1&gt;&lt;h2 id=&quot;sql分类&quot;&gt;&lt;a href=&quot;#sql分类&quot; class=&quot;headerlink&quot; title=&quot;sql分类&quot;&gt;&lt;/a&gt;sql分</summary>
      
    
    
    
    <category term="四件套" scheme="https://trans-xu.github.io/transblog/categories/%E5%9B%9B%E4%BB%B6%E5%A5%97/"/>
    
    
  </entry>
  
  <entry>
    <title>Golang</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/Go/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/Go/</id>
    <published>2025-08-27T03:50:08.884Z</published>
    <updated>2025-08-27T03:34:24.500Z</updated>
    
    <content type="html"><![CDATA[<h1 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h1><h2 id="整型"><a href="#整型" class="headerlink" title="整型"></a>整型</h2><table><thead><tr><th align="center">类型</th><th align="center">范围</th></tr></thead><tbody><tr><td align="center">uint8</td><td align="center">0 到 255</td></tr><tr><td align="center">uint16</td><td align="center">0 到 65535</td></tr><tr><td align="center">uint32</td><td align="center">0 到 4294967295</td></tr><tr><td align="center">unit64</td><td align="center">(0 到 18446744073709551615)</td></tr><tr><td align="center">int8</td><td align="center">-128 到 127</td></tr><tr><td align="center">int16</td><td align="center">-32768 到 32767</td></tr><tr><td align="center">int32</td><td align="center">-2147483648 到 2147483647</td></tr><tr><td align="center">int64</td><td align="center">(-9223372036854775808 到 9223372036854775807)</td></tr></tbody></table><h2 id="浮点型–float"><a href="#浮点型–float" class="headerlink" title="浮点型–float"></a>浮点型–float</h2><p>float32、float64；分别表示单精度浮点数和双精度浮点数。</p><h2 id="复数型–complex"><a href="#复数型–complex" class="headerlink" title="复数型–complex"></a>复数型–complex</h2><p>complex64、complex128；表示由实部和虚部组成的复数。</p><h2 id="字符和字符串型–byte"><a href="#字符和字符串型–byte" class="headerlink" title="字符和字符串型–byte"></a>字符和字符串型–byte</h2><p>byte表示 0 到 255、string</p><h2 id="错误类型–error"><a href="#错误类型–error" class="headerlink" title="错误类型–error"></a>错误类型–error</h2><p>error，表示错误信息的类型。</p><h2 id="布尔型–bool"><a href="#布尔型–bool" class="headerlink" title="布尔型–bool"></a>布尔型–bool</h2><p>true或false</p><h2 id="数组类型"><a href="#数组类型" class="headerlink" title="数组类型"></a>数组类型</h2><p>[n]T，表示固定长度的数组，其中 n 是数组的长度，T 是数组元素的类型。</p><h2 id="切片类型"><a href="#切片类型" class="headerlink" title="切片类型"></a>切片类型</h2><p>[]T，表示可变长度的序列，其中 T 是切片元素的类型。</p><h3 id="数组和切片区别"><a href="#数组和切片区别" class="headerlink" title="数组和切片区别"></a>数组和切片区别</h3><p><strong>数组</strong></p><ul><li><p>固定大小：数组的大小是在编译时确定的，且在声明时必须指定。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> arr [<span class="number">5</span>]<span class="type">int</span>  <span class="comment">// 长度为5的整型数组</span></span><br></pre></td></tr></table></figure></li><li><p>连续内存：数组的元素在内存中是连续存储的。</p></li><li><p>整体值传递：数组作为函数参数时，传递的是整个数组的副本。</p></li><li><p>索引访问：通过索引访问数组元素，索引从0开始。</p></li></ul><p><strong>切片</strong></p><ul><li>动态大小：切片的长度可以在运行时动态改变，不需要在声明时指定大小。<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> slice []<span class="type">int</span>  <span class="comment">// 空切片，长度为0</span></span><br><span class="line">slice = <span class="built_in">append</span>(slice, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)  <span class="comment">// 添加元素，长度自动增加</span></span><br></pre></td></tr></table></figure></li><li>内部结构：切片由三个部分组成：指向底层数组的指针、切片的长度（当前包含的元素数量）和切片的容量（从指针位置开始，底层数组还能容纳的元素数量）。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> slice <span class="keyword">struct</span> &#123;</span><br><span class="line">    array unsafe.Pointer <span class="comment">// 指向底层数组的指针</span></span><br><span class="line">    <span class="built_in">len</span>   <span class="type">int</span>            <span class="comment">// 当前切片的长度</span></span><br><span class="line">    <span class="built_in">cap</span>   <span class="type">int</span>            <span class="comment">// 切片的容量（从指针位置到数组末尾的长度）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>引用传递：切片作为函数参数时，传递的是切片的头部（指针、长度和容量），而不是整个底层数组的副本。这使得操作切片时更加高效。</li><li>可变长度：可以通过内置的append函数动态地添加元素到切片中，如果底层数组空间不足，会自动分配新的内存空间并复制原数据。</li></ul><h2 id="映射类型–map"><a href="#映射类型–map" class="headerlink" title="映射类型–map"></a>映射类型–map</h2><p>map[K]V，表示键值对的集合，其中 K 是键的类型，V 是值的类型。</p><h3 id="map使用"><a href="#map使用" class="headerlink" title="map使用"></a>map使用</h3><p><strong>1. 某些场景下，将value定义为struct，会有更优的性能</strong><br>当 map 的 value 是 struct 类型时，数据会直接存储在 map 中，而不是通过指针引用。这可以减少内存分配的开销和 GC（垃圾回收）的负担。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> User <span class="keyword">struct</span> &#123;</span><br><span class="line">    ID   <span class="type">int</span></span><br><span class="line">    Name <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">m := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">string</span>]User)</span><br><span class="line">m[<span class="string">&quot;user1&quot;</span>] = User&#123;ID: <span class="number">1</span>, Name: <span class="string">&quot;John&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Example with pointer to struct</span></span><br><span class="line">m2 := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">string</span>]*User)</span><br><span class="line">m2[<span class="string">&quot;user1&quot;</span>] = &amp;User&#123;ID: <span class="number">1</span>, Name: <span class="string">&quot;John&quot;</span>&#125;</span><br></pre></td></tr></table></figure><p>在示例中，map 中存储的是指向 User 结构体的指针，这意味着除了存储指针本身外，还需要额外的内存来存储 User 结构体。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">set1 := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">int</span>]<span class="type">bool</span>)</span><br><span class="line">set1[<span class="number">1</span>] = <span class="literal">true</span></span><br><span class="line">set1[<span class="number">2</span>] = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">set2 := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">int</span>]<span class="keyword">struct</span>&#123;&#125;)</span><br><span class="line">set2[<span class="number">1</span>] = <span class="keyword">struct</span>&#123;&#125;&#123;&#125;</span><br><span class="line">set2[<span class="number">2</span>] = <span class="keyword">struct</span>&#123;&#125;&#123;&#125;</span><br></pre></td></tr></table></figure><p>在示例中，map[int]bool{} 会比 map[int]struct{}{} 使用更多的内存，因为 bool 类型需要存储一个字节（在实际应用中可能会有额外的内存对齐和管理开销），而 struct{} 是空的，不会增加任何内存开销。</p><p>优点：</p><ul><li>节省内存， 并且会减少 GC 的负担。</li><li>避免内存碎片化，存储指针时，由于指针可能指向堆中的不同位置，这会导致内存碎片化，增加了内存使用的不确定性。而存储 struct 使得数据更紧凑，减少了碎片化。</li><li>更高的缓存命中率，由于 struct 的数据是紧凑存储的，相对于存储指针，struct 的数据更可能在相邻的内存位置。这增加了 CPU 缓存的命中率，从而提高了性能。</li></ul><p><strong>2. Go语言的map的键类型为什么不能是函数类型、字典类型、切片类型？</strong></p><ul><li>对于所有键值对元素的数据结构来说，存储过程先计算key的哈希值，随后<strong>使用哈希值定位到对应的bucket</strong>，将key和value作为元素存储到对应的bucket中。</li><li>在这个过程中可能会发生哈希冲突，由于哈希冲突的存在对于哈希表的键的要求，<strong>不仅仅能够计算哈希值，同时能够对键进行判等操作</strong>（解决哈希冲突时key重复的问题）。</li><li>如果go的map的键为上述三种类型，在运行时就会发生panic。</li></ul><p><strong>3. map使用时的并发安全问题：</strong></p><p>对于map的读写是非原子性操作，存在资源竞争，不是现成安全的。为了将非并发安全的读取操作更改为并发安全的，可以引入sync.Mutex，在读、写操作前进行加锁操作；操作后进行解锁，保证并发安全。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">m := <span class="keyword">map</span>[<span class="type">int</span>]<span class="type">string</span>&#123;</span><br><span class="line"><span class="number">1</span>: <span class="string">&quot;haha&quot;</span>,</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> mutex sync.Mutex <span class="comment">// 创建一个互斥锁</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动读协程</span></span><br><span class="line"><span class="keyword">go</span> read(m, &amp;mutex)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待一秒钟，确保读协程已经开始运行</span></span><br><span class="line">time.Sleep(time.Second)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动写协程</span></span><br><span class="line"><span class="keyword">go</span> write(m, &amp;mutex)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待足够长的时间，以便读写协程可以运行</span></span><br><span class="line">time.Sleep(<span class="number">30</span> * time.Second)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 打印最终的map</span></span><br><span class="line">fmt.Println(m)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">read</span><span class="params">(m <span class="keyword">map</span>[<span class="type">int</span>]<span class="type">string</span>, mutex *sync.Mutex)</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">mutex.Lock() <span class="comment">// 在读取之前加锁</span></span><br><span class="line">value := m[<span class="number">1</span>]</span><br><span class="line">mutex.Unlock() <span class="comment">// 读取完毕后解锁</span></span><br><span class="line">fmt.Println(<span class="string">&quot;Read:&quot;</span>, value)</span><br><span class="line">time.Sleep(<span class="number">1</span> * time.Second)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">write</span><span class="params">(m <span class="keyword">map</span>[<span class="type">int</span>]<span class="type">string</span>, mutex *sync.Mutex)</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">mutex.Lock() <span class="comment">// 在写入之前加锁</span></span><br><span class="line">m[<span class="number">1</span>] = <span class="string">&quot;write&quot;</span></span><br><span class="line">mutex.Unlock() <span class="comment">// 写入完毕后解锁</span></span><br><span class="line">fmt.Println(<span class="string">&quot;Write:&quot;</span>, m[<span class="number">1</span>])</span><br><span class="line">time.Sleep(<span class="number">1</span> * time.Second)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="map原理"><a href="#map原理" class="headerlink" title="map原理"></a>map原理</h3><p>map实现的两个关键数据结构</p><ul><li>hmap 定义了map的结构<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> hmap <span class="keyword">struct</span> &#123;</span><br><span class="line">    count     <span class="type">int</span>           <span class="comment">// 当前 map 中的元素个数</span></span><br><span class="line">    flags     <span class="type">uint8</span>         <span class="comment">// 状态标志位</span></span><br><span class="line">    B         <span class="type">uint8</span>         <span class="comment">// buckets 数组的长度为 2^B</span></span><br><span class="line">    noverflow <span class="type">uint16</span>        <span class="comment">// 溢出桶的数量</span></span><br><span class="line">    </span><br><span class="line">    buckets    unsafe.Pointer <span class="comment">// 指向 buckets 数组的指针</span></span><br><span class="line">    oldbuckets unsafe.Pointer <span class="comment">// 扩容时保存旧的 buckets 数组</span></span><br><span class="line">    extra      *mapextra    <span class="comment">// 用于保存溢出桶的地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>bmap 定义了hmap.buckets中每个bucket的结构，每个 bmap 可以存储 8 个键值对（默认）。以下是 bmap 的关键字段：<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> bmap <span class="keyword">struct</span> &#123;</span><br><span class="line">    tophash   [bucketCnt]<span class="type">uint8</span>              <span class="comment">// 存储每个键的哈希值的高 8 位</span></span><br><span class="line">    keys      [bucketCnt]keytype    <span class="comment">// 存储 bucketCnt 个键</span></span><br><span class="line">    values    [bucketCnt]valuetype  <span class="comment">// 存储 bucketCnt 个值</span></span><br><span class="line">    overflow  <span class="type">uintptr</span>               <span class="comment">// 指向下一个溢出桶</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><p>key hash值的后B位作为桶index查找桶<br>key hash值的前8位作为桶内结构体的三个数组（tophash，key，value）的index<br>桶结构体的tophash复用，既作为tophash使用，也作为标志位使用</p><h4 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h4><ol><li><p>哈希计算<br>当我们向map中插入一个键值对，首先对键进行哈希计算。Go内置了哈希函数来计算键的哈希值。哈希值是一个64位的整数。</p></li><li><p>分桶依据<br>Go 中的 map 是分成多个桶 (bucket) 来存储的。桶的数量通常是 2 的幂次，这样可以方便地通过位运算来定位到具体的桶。哈希值的高八位和低八位分别用于分桶和桶内定位：<br>高八位 (top 8 bits)：用于决定哈希表中的桶位置。<br>低八位 (low 8 bits)：用于桶内查找。</p></li><li><p>溢出桶使用<br>每个桶中可以存储 8 个键值对。当某个桶中的元素超过 8 个时，Go 会使用溢出桶来存储额外的键值对，并通过 overflow 字段将其链接到当前 bmap 后面。这样可以动态扩展 map 的存储空间，而不需要立即进行扩容。</p></li></ol><h4 id="插入过程"><a href="#插入过程" class="headerlink" title="插入过程"></a>插入过程</h4><p>当插入一个键值对时，过程如下：</p><ol><li>计算哈希值<br>对键进行哈希计算得到哈希值 hash。</li><li>定位桶<br>通过 hash &gt;&gt; (64 - B)（B 是桶的数量的对数）得到桶的索引 index。</li><li>桶内查找<br>通过 hash &amp; (bucketCnt - 1) 得到桶内索引。然后通过对比 tophash 数组中的值来定位到具体的键值对存储位置。</li><li>存储键值对<br>将键值对存储到相应的位置，如果当前桶已满，则分配新的溢出桶来存储额外的键值对。</li></ol><h4 id="查找过程"><a href="#查找过程" class="headerlink" title="查找过程"></a>查找过程</h4><p>查找的过程与插入类似：</p><ol><li>计算哈希值：<br>对键进行哈希计算得到哈希值 hash。</li><li>定位桶：<br>通过 hash &gt;&gt; (64 - B) 得到桶的索引 index。</li><li>桶内查找：<br>通过 hash &amp; (bucketCnt - 1) 得到桶内索引，然后在相应的 bmap 中查找 tophash 和 keys 数组中匹配的键。如果在当前桶中没有找到，则继续查找溢出桶。</li></ol><h4 id="扩容机制"><a href="#扩容机制" class="headerlink" title="扩容机制"></a>扩容机制</h4><p><strong>扩容触发条件</strong></p><ul><li>装载因子过高：装载因子（load factor）是 <strong>map 中元素数量与桶数量的比值</strong>。装载因子阈值通常为 6.5，当装载因子超过这个值时会触发<strong>双倍扩容</strong>。</li><li>溢出桶过多：当溢出桶的数量过多时，会触发<strong>等量扩容</strong>。</li></ul><h5 id="双倍扩容"><a href="#双倍扩容" class="headerlink" title="双倍扩容"></a>双倍扩容</h5><p>如下图，原来 B &#x3D; 0，只有一个桶，装满后触发翻倍扩容，B &#x3D; 1，buckets 指向两个新桶，oldbuckets 指向旧桶，nevacuate 表示接下来要迁移编号为 0 的旧桶。旧桶的键值对会<strong>渐进式分流</strong>到两个新桶中。直到旧桶中的键值对全部搬迁完毕后，删除oldbuckets。</p><p>这种扩容会导致 <strong>桶迁移</strong>，需要将原有的 key&#x2F;value 重新搬迁到新的内存地址，如果map存储了数以亿计的key-value，一次性搬迁将会造成比较大的延时，因此 Go map 的扩容采取了<strong>渐进式扩容的方式，原有的 key 并不会一次性搬迁完毕，每次最多只会搬迁 2 个 bucket。只有在插入或修改、删除 key 的时候，都会尝试进行搬迁 buckets 的工作</strong>。具体来说就是检查 oldbuckets 是否为 nil。</p><p><img data-src="/transblog/images/158593311250264.jpg"></p><p>迁移过程中使用与运算法hash &amp; (bucketCnt-1)，用旧桶的hash值跟扩容后的桶的个数 bucketCnt-1 的值相与(&amp;)，得几就在哪个位置上。</p><p>示例：</p><ul><li>如果旧桶数量为4，那么新桶的数量就为 8。</li><li>如果该参数选择 0 号旧桶说明哈希值为 xxxxxx00，旧桶 m-1 &#x3D; 3 &#x3D; 00000011  –&gt; xxxxxx00 &amp; 00000011 &#x3D; 0</li><li>因此选择新桶取决于哈希值的第三位是 0 还是 1，新桶 m-1 &#x3D; 7 &#x3D; 00000111，与原哈希值 xxxxxx00 进行与运算，若第三位是 0 则为 00000000 &#x3D; 0，第三位为 1 则为 00000100 &#x3D; 4。</li></ul><h5 id="等量扩容"><a href="#等量扩容" class="headerlink" title="等量扩容"></a>等量扩容</h5><ul><li>当 B &lt; 15 时，如果溢出桶的数量超过 2^B，则触发等量扩容。</li><li>当 B &gt;&#x3D; 15 时，如果溢出桶的数量超过 2^15，则触发等量扩容。</li></ul><p>虽然没有超过负载因子限制，但是使用溢出桶过多，就会触发等量扩容，创建和旧桶数目一样多的新桶，然后把原来的键值对迁移到新桶中。</p><p>一般发生在很多键值对被删除的情况下，这样会造成overflow的bucket数量增多，但负载因子又不高。同样数目的键值对，迁移到新桶中会把松散的键值对重新排列一次，使其排列的更加紧凑，进而保证更快的存取，这就是等量扩容的意义所在。</p><h3 id="线程不安全"><a href="#线程不安全" class="headerlink" title="线程不安全"></a>线程不安全</h3><p>golang 中的 map 是并发不安全的，多个 go 协程同时对同一个 map 进行读写操作时，会导致数据竞争（data race）问题，程序会 panic。读取到的值可能是过期的、不正确的或 nil；写入的值可能被覆盖、丢失或者多次计数。</p><p>举个例子：某 map 桶数量为 4，即 B&#x3D;2。此时 goroutine1 来插入 key1，goroutine2 来读取 key2。可能会发生如下过程：</p><blockquote><ol><li>goroutine2 计算 key2 的 hash 值，B&#x3D;2，并确定桶号为 1。</li><li>goroutine1 添加 key1，触发扩容条件。</li><li>B&#x3D;B+1&#x3D;3，buckets 数据迁移到 oldbuckets。</li><li>goroutine2 从桶 1 中遍历，获取数据失败。</li></ol></blockquote><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    m := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">int</span>]<span class="type">int</span>)</span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            m[i] = i</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wg.Wait()</span><br><span class="line">    fmt.Println(m)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="使用-sync-Mutex-进行同步"><a href="#使用-sync-Mutex-进行同步" class="headerlink" title="使用 sync.Mutex 进行同步"></a>使用 sync.Mutex 进行同步</h4><p>为了避免数据竞争，可以使用 sync.Mutex 进行同步。sync.Mutex 提供了锁机制，在这个示例中，使用 mu.Lock() 和 mu.Unlock() 确保每次只有一个 goroutine 可以访问 map，从而避免数据竞争。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    m := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">int</span>]<span class="type">int</span>)</span><br><span class="line">    <span class="keyword">var</span> mu sync.Mutex</span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            mu.Lock()</span><br><span class="line">            m[i] = i</span><br><span class="line">            mu.Unlock()</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wg.Wait()</span><br><span class="line">    fmt.Println(m)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="使用-sync-RWMutex-进行读写锁"><a href="#使用-sync-RWMutex-进行读写锁" class="headerlink" title="使用 sync.RWMutex 进行读写锁"></a>使用 sync.RWMutex 进行读写锁</h4><p>如果读操作远多于写操作，可以使用 sync.RWMutex 进行读写锁。sync.RWMutex 提供了读锁和写锁，允许多个 goroutine 同时进行读操作，但写操作仍然是互斥的。在这个示例中，使用 mu.RLock() 和 mu.RUnlock() 进行读操作，使用 mu.Lock() 和 mu.Unlock() 进行写操作，从而提高并发读的效率。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    m := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">int</span>]<span class="type">int</span>)</span><br><span class="line">    <span class="keyword">var</span> mu sync.RWMutex</span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            mu.Lock()</span><br><span class="line">            m[i] = i</span><br><span class="line">            mu.Unlock()</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            mu.RLock()</span><br><span class="line">            fmt.Println(m[i])</span><br><span class="line">            mu.RUnlock()</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wg.Wait()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="使用-sync-Map"><a href="#使用-sync-Map" class="headerlink" title="使用 sync.Map"></a>使用 sync.Map</h4><p>Golang 标准库提供了 sync.Map，它是一个并发安全的 map 实现，适用于需要高并发访问的场景。sync.Map 提供了原子操作，避免了手动加锁的复杂性。在这个示例中，使用 m.Store() 进行写操作，使用 m.Load() 进行读操作，sync.Map 内部已经实现了并发安全。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> m sync.Map</span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            m.Store(i, i)</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            value, _ := m.Load(i)</span><br><span class="line">            fmt.Println(value)</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wg.Wait()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="结构体类–struct"><a href="#结构体类–struct" class="headerlink" title="结构体类–struct"></a>结构体类–struct</h2><p>struct，表示由多个字段组成的复合类型。</p><h2 id="接口类型–interface"><a href="#接口类型–interface" class="headerlink" title="接口类型–interface"></a>接口类型–interface</h2><p>interface，表示一组方法的集合。</p><h2 id="函数类型–func"><a href="#函数类型–func" class="headerlink" title="函数类型–func"></a>函数类型–func</h2><p>func，表示函数类型。</p><h1 id="Go实现继承"><a href="#Go实现继承" class="headerlink" title="Go实现继承"></a>Go实现继承</h1><h2 id="1-通过结构体嵌套进行组合"><a href="#1-通过结构体嵌套进行组合" class="headerlink" title="1. 通过结构体嵌套进行组合"></a>1. 通过结构体嵌套进行组合</h2><p>在Go语言中，结构体嵌套是一种常见的实现继承的方式。通过将一个结构体嵌套在另一个结构体中，可以使得外层结构体拥有内层结构体的所有字段和方法。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Animal <span class="keyword">struct</span> &#123;</span><br><span class="line">Name <span class="type">string</span></span><br><span class="line">Age  <span class="type">int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a Animal)</span></span> Speak() &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;I am an animal.&quot;</span>)</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Dog <span class="keyword">struct</span> &#123;</span><br><span class="line">Animal</span><br><span class="line">Breed <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d Dog)</span></span> Bark() &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;Woof! Woof!&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">dog := Dog&#123;</span><br><span class="line">Animal: Animal&#123;Name: <span class="string">&quot;Buddy&quot;</span>, Age: <span class="number">3</span>&#125;,</span><br><span class="line">Breed:  <span class="string">&quot;Golden Retriever&quot;</span>,</span><br><span class="line">&#125;</span><br><span class="line">fmt.Printf(<span class="string">&quot;Name: %s, Age: %d, Breed: %s\n&quot;</span>, dog.Name, dog.Age, dog.Breed)</span><br><span class="line">dog.Speak() <span class="comment">// 通过组合实现了继承</span></span><br><span class="line">dog.Bark()  <span class="comment">// Dog结构体自己的方法</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="2-通过接口实现多态性"><a href="#2-通过接口实现多态性" class="headerlink" title="2. 通过接口实现多态性"></a>2. 通过接口实现多态性</h2><p>接口是Go语言实现多态性的重要机制。通过定义接口，可以抽象出不同类型对象的共同行为，从而实现类似于继承和多态的效果。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Speaker <span class="keyword">interface</span> &#123;</span><br><span class="line">Speak()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Animal <span class="keyword">struct</span> &#123;</span><br><span class="line">Name <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a Animal)</span></span> Speak() &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;I am an animal.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Dog <span class="keyword">struct</span> &#123;</span><br><span class="line">Animal</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d Dog)</span></span> Speak() &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;Woof! Woof!&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> speaker Speaker</span><br><span class="line">animal := Animal&#123;Name: <span class="string">&quot;Buddy&quot;</span>&#125;</span><br><span class="line">dog := Dog&#123;Animal: Animal&#123;Name: <span class="string">&quot;Max&quot;</span>&#125;&#125;</span><br><span class="line">speaker = animal</span><br><span class="line">speaker.Speak() <span class="comment">// 输出：I am an animal.</span></span><br><span class="line"></span><br><span class="line">speaker = dog</span><br><span class="line">speaker.Speak() <span class="comment">// 输出：Woof! Woof!</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="3-通过方法实现功能共享"><a href="#3-通过方法实现功能共享" class="headerlink" title="3. 通过方法实现功能共享"></a>3. 通过方法实现功能共享</h2><p>在Go语言中，方法可以绑定到特定类型的结构体上，从而实现功能的共享和复用。这种方式在某些情况下也可以看作是继承的一种变体。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Animal <span class="keyword">struct</span> &#123;</span><br><span class="line">Name <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a Animal)</span></span> Speak() &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;I am an animal.&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Dog <span class="keyword">struct</span> &#123;</span><br><span class="line">Animal</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d Dog)</span></span> Speak() &#123;</span><br><span class="line">d.Animal.Speak() <span class="comment">// 调用Animal的Speak方法</span></span><br><span class="line">fmt.Println(<span class="string">&quot;Woof! Woof!&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">dog := Dog&#123;Animal: Animal&#123;Name: <span class="string">&quot;Buddy&quot;</span>&#125;&#125;</span><br><span class="line">dog.Speak() <span class="comment">// 输出：I am an animal. Woof! Woof!</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="函数参数传递"><a href="#函数参数传递" class="headerlink" title="函数参数传递"></a>函数参数传递</h1><p><strong>值传递的数据类型有：基本数据类型（int、float、bool、string等）、数组和结构体</strong>。<br><strong>引用传递的数据类型有：指针、slice、map、管道（channel）、interface等</strong>。</p><h1 id="Go-Runtime"><a href="#Go-Runtime" class="headerlink" title="Go Runtime"></a>Go Runtime</h1><p>Go runtime 可以形象的理解为 Go 程序运行时的环境，类似于 JVM。不同于 JVM 的是，Go 的 runtime 与业务程序直接打包在一块，是一个可执行文件，直接运行在操作系统上，效率很高。</p><p>runtime 包含了一些 Go 的一些非常核心的功能：<strong>协程调度、垃圾回收、内存分配</strong>等。</p><h2 id="协程调度"><a href="#协程调度" class="headerlink" title="协程调度"></a>协程调度</h2><p><span id="target1"></span></p><p>协程调度是指 Go 如何管理和执行协程，Go 的协程调度基于 GMP 模型。即：</p><ol><li>G (Goroutine)：即 Go 的协程，包含了栈信息，代码指针，状态等；</li><li>M (Machine)：代表一个工作线程，由操作系统直接分配；</li><li>P (Processor)：处理器（Go 定义的一个概念，不是指 CPU），包含了协程运行的所需资源，如本地队列、全局队列、计数器等。</li></ol><p>GMP 三者的关系：</p><p>P 的个数取决于设置的 runtime.GOMAXPROCS，默认是物理 CPU 的逻辑核心数量，比如四核八线程的 CPU，P 的数量就是 8；<br>M 的数量一般是多于 P 的，M 要想被 CPU 执行，必须先获取 P。没有获取 P 的 M，则处于休眠状态；<br>G 可以理解为代码本体，G 必须要被 P 调度进入 M，才可以被 CPU 执行；<br>P 包含了一个 LRQ （Local Run Queue）本地运行队列，保存着等待执行的协程（G）队列。没有被分配到 P 的 G，会被保存到 GRQ (Global Run Queue) 全局队列中，处于休眠状态。<br>假如主机是单逻辑 CPU 的，那么 GMP 是这样的：</p><p><img data-src="/transblog/images/421481918247064.png"></p><p>红色部分表示休眠或者挂起状态，黄色代表等待执行，绿色表示正在运行。系统初始化了两个线程，但我们只有一个处理器（P）， M1 没有获取到 P，所以只能休眠。M0 当前获取到 P ，正在处理 G0, LRQ 里面目前有三个 G 在排队等待被 M 运行，GRQ 里面保存着 G4、G5、G6，表示它们还没有分配到队列中。</p><p>P 这个时候会分别对 LRQ 进行周期队列轮转 和 GRO 周期性检查：</p><p>队列轮转：LRQ 中的 G 被 P 调度到 M 中执行，每个 G 执行一段时间后，就会保存其上下文并放入队列尾部，然后取出队列头部的 G 进入 M 执行。<br>周期性检查：P 会检查 GRQ 中是否有 G 正在等待运行，并将其放入 M 中执行，防止协程被饿死。<br>在队列轮转中，如果当前正在运行的 G 遇到了系统调用，那么系统就会挂起当前 M0，释放 P，M1 就会绑定释放的 P，来继续执行其他协程。<br>假设 GO 遇到了系统调用：</p><p><img data-src="/transblog/images/555091918267230.png"></p><p>等到 M1 中所有的协程执行完或者 M1 处理某个协程也遇到了了系统调用，就会重新释放 P 给其他空闲的 M。而另外一边 G0 的系统调用结束后，就会将 M0 线程从挂起状态变成休眠状态，并将 G0 放入 GRQ，等待被 P 重新调入 LRQ 中轮转执行。</p><p>如果我们的主机具备多个逻辑 CPU，创建了多个 P，那么就会变成多个线程并行执行：</p><p><img data-src="/transblog/images/108562018259899.png"></p><p>多线程同时处理时，很有可能多个 LRQ 是不均衡的。假如上图的 M0 已经执行完了，其他线程还处于繁忙状态，M0 所绑定的 P 就会去检查 GQR，GQR 中也没有 G，那么它就会去偷取其他 LRQ 一部分的 G 来执行，一般每次会偷取一半。</p><h2 id="垃圾回收（GC）"><a href="#垃圾回收（GC）" class="headerlink" title="垃圾回收（GC）"></a>垃圾回收（GC）</h2><p>垃圾回收（Garbage Collection，GC） 是Go语言的核心特性之一，是实现内存自动管理的一种形式。golang的自动垃圾回收屏蔽了复杂且容易出错的内存操作，让开发变得更加简单、高效。在Go语言中，从实现机制上来说，垃圾回收可能是最复杂的模块了。了解垃圾回收的机制，有助于更好地理解Go语言的内存管理机制，从而更好的使用Go语言进行开发。</p><p>垃圾回收涉及到一下三个组件：</p><p><img data-src="/transblog/images/441184310256443.png"></p><ul><li>Allocator-分配器：在堆上申请内存</li><li>Mutator-赋值器：将Allocator申请到的内存对象赋值给栈上的变量（或全局变量）。</li><li>Collector-回收器：回收不再活跃的内存对象。</li></ul><h3 id="标记-清除-算法"><a href="#标记-清除-算法" class="headerlink" title="标记-清除 算法"></a>标记-清除 算法</h3><p>Go语言使用标记-清除（Mark-Sweep）算法来进行内存垃圾回收。Mark-Sweep算法执行过程可以分成标记（Mark）和清除（Sweep）两个阶段：</p><ul><li>Mark阶段： 从根对象开始，对内存对象进行图遍历，对所有可达的对象进行标记。</li><li>Sweep阶段： 对Mark阶段未被标记的内存对象进行回收，回收完毕后重置所有内存对象的标记以便下一轮‘标记-清除’。</li></ul><p>什么是根对象？<br><strong>根对象主要是指应用程序中通过变量可以直接访问的对象</strong>。假设应用程序在堆上申请了A-J共10个内存对象。其中可以被程序中的变量直接访问的只有对象A和B（分别被变量a、b直接访问），因此根对象只有A和B。其他内存对象都是间接访问，不是根对象。</p><h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><p>原始的Mark-Sweep算法流程上相对简单，但是在实际应用中有一定的限制条件。在垃圾回收的整个过程中，如果应用程序并发进行内存相关操作，<strong>可能导致活跃对象被错误回收</strong></p><p><img data-src="/transblog/images/570304810276609.png"></p><p>如上图所示，在标记阶段根对象A做DFS遍历完成后，Mutator并发修改了对内存对象D的引用关系（增加了A对象对D对象的引用，删除了B对象对D对象的引用），然后Collector在对根对象B做DFS遍历。最终导致的结果是，从根对象A可以到达对象D（即D是活跃的），但是标记结果显示对象D是可以被回收的内存对象，从而错误释放了对象D的内存。</p><p>在原始&lt;标记-清除&gt;算法中，要保证活跃对象不被被错误回收，需要满足以下必要条件：<strong>活跃白色对象必须从白色的根对象可达</strong>。这个条件在并发和增量执行的过程中其实是很难保证的，**因而：原始的Mark-Sweep算法会在垃圾回收阶段Stop-The-World（STW），不支持并发和增量执行，就是需要将程序暂停，程序出现卡顿 (重要问题)**。还有一些小问题就是这个：标记需要扫描整个heap，清除数据会产生heap碎片。</p><h3 id="三色标记法"><a href="#三色标记法" class="headerlink" title="三色标记法"></a>三色标记法</h3><p>Go语言使用三色标记法和屏障技术来支持并发和增量执行。</p><p>三色标记算法将所有的内存对象抽象为黑、白、灰三类：</p><ul><li>白色： 初始色，如果标记阶段结束还是白色，则该内存对象将被回收。</li><li>灰色： 活跃的对象，存在于标记阶段中间状态，自身及其下游需要被进一步扫描、标记。所有的根对象在标记开始时被全部置为灰色。</li><li>黑色： 活跃的对象，已被标记完成，不会再通过该对象对其下游对象扫描标记。</li></ul><p>三色标记法的基本过程：</p><ol><li>垃圾回收开始前，所有的内存对象都是白色；</li><li>垃圾回收开始时，所有根对象被标记会灰色；</li><li>从灰色对象集合，选取一个灰色对象，将其下游白色对象置灰，将自己置为黑色；</li><li>重复第3步骤，直至灰色对象集合为空。</li></ol><p><strong>三色标记法只能由白-&gt;灰-&gt;黑单调变色</strong></p><p><img data-src="/transblog/images/354290508469088.png"></p><p>三色标记法本身仍然不能保证并发和增量执行的正确性。在并发和增量执行的场景下，活跃对象（白色）被错误回收的触发条件：</p><ol><li>不存在从灰色对象到达该白色对象的路径。（该白色对象在标记阶段不会再被扫描到）</li><li>存在从黑色对象到达该白色对象的路径。（否则结合1的话，该白色对象本来就是一个需要被回收的垃圾对象）</li></ol><p>等于说，单纯的三色标记法，还是无法避免要进行STW，如果不进行STW，则活跃对象可能被错误回收，如下图：</p><p>此时在进行GC但是程序也是在运行的，此时黑色对象4创建了一个指针指向了白色对象3</p><p><img data-src="/transblog/images/197016623769751.png"></p><p>并且这个黑色对象2删除了和对象3的引用关系，那么活跃对象3会被回收<br><img data-src="/transblog/images/347984041280436.png"></p><h4 id="两种三色不变性"><a href="#两种三色不变性" class="headerlink" title="两种三色不变性"></a>两种三色不变性</h4><p>由此，就衍生了两种三色不变性：</p><ol><li>强三色不变性：不存在黑色对象对白色对象的直接引用。（隐含了一层意思：如果该对象是活跃对象，那么必然存在从灰色对象到该对象的路径）。强三色不变性破坏了两个条件。</li><li>弱三色不变性：被黑色对象直接引用的白色对象，必须要能够通过一些灰色对象可达。破坏了”不存在从灰色对象到达该白色对象的路径”这个条件。</li></ol><p><img data-src="/transblog/images/443236016183411.png"><br><img data-src="/transblog/images/106235435019228.png"></p><h4 id="屏障技术"><a href="#屏障技术" class="headerlink" title="屏障技术"></a>屏障技术</h4><p>内存屏障技术是一种CPU屏障指令，用于让CPU和编译器对屏障指令前后的内存操作的执行顺序进行强制约束。现在处理器为了优化性能，经常会改变程序指令的执行顺序，而内存屏障技术则是用来对一些内存操作的执行顺序进行强制约束。<strong>内存屏障指令前的内存操作一定会在内存屏障指令后的操作之前执行。</strong><br>内存屏障技术本身并不能用来保证三色不变性。但是我们可以利用内存屏障技术在内存操作之前执行特定的（用于保护三色不变性）的指令（执行特定代码）（就像Hook一样）。可以把内存操作分为以下几种：</p><ol><li>堆对象的读，写</li><li>栈对象的读，写</li></ol><p>通过两种三色不变性，谷歌团队引入了这个插入屏障和删除屏障</p><h5 id="插入写屏障"><a href="#插入写屏障" class="headerlink" title="插入写屏障"></a>插入写屏障</h5><p>当Mutator在增加一个内存对象A到另一个内存对象B的指向时（在有向图中增加A-&gt;B这条边），引入内存屏障，将内存对象B置灰。可以用来保证内存对象的强三色不变性。<br>还有一种情况就是A之前引用了这个对象C，现在A想引用B也就是不引用C了此时B也会被置为灰色。</p><p>下图的示例：没有暂停程序，黑色对象4引用了这个白色对象8<br><img data-src="/transblog/images/45536040005929.png"></p><p>那么此时开启这个插入屏障，白色对象会被置为灰色。如下图所示：<br>栈是不开启这个插入写屏障的，所以对象9并不会被标记为灰色，<strong>这是因为栈要保证效率</strong>，因此运行时对栈上对象一般都不会开启写屏障，而只对堆上的对象做此处理</p><p><img data-src="/transblog/images/384867418122791.png"></p><p>然后继续三次标记法的流程，遍历完成后的样子大致是这样：</p><p><img data-src="/transblog/images/158188185122180.png"></p><p>但是此时对象9不会被当作是垃圾而被清除掉吗？此时在处理回收这个白色对象时，会开启STW，<strong>注意：只是对栈开启这个SWT，将栈里面的对象全部置为白色重新上述过程（此处STW占用大概10ms-100ms）</strong>。这样就能保证这个对象9不会被误删</p><p><img data-src="/transblog/images/446725432389805.png"></p><p>最终会变成如下图：<br><img data-src="/transblog/images/154056556979502.png"></p><h5 id="删除写屏障"><a href="#删除写屏障" class="headerlink" title="删除写屏障"></a>删除写屏障</h5><p>当Mutator在删除一个内存对象A到另一个内存对象B的指向时（在有向图中删除A-&gt;B这条边），引入内存屏障，将内存对象B置灰。这一点其实就是为了满足弱三色不变式。 (保护灰色对象到白色对象的路径不会断)<br>还有一种情况就是A之前引用了B，此时A更换了引用关系引用C，也就是A和B之间的引用关系被删除掉了。此时B对象会被置为灰色。</p><p>下图的示例：此时对象1想要删除和对象5之间的引用关系，此时就会触发删除写屏障。<br><img data-src="/transblog/images/277420432971214.png"></p><p>删除完成后如下图：</p><p><img data-src="/transblog/images/493744345367524.png"></p><p>最后的情况：</p><p><img data-src="/transblog/images/417872654956706.png"></p><p>此时又有疑问了：对象5、2、3不是垃圾吗？为什么保留下来了？在这一次GC过程当中他们确实是被保留下来了，但是下一轮GC的时候就会被干掉了。<br>由此可知：<strong>这种方式的回收精度低</strong>，一个对象即使被删除了最后一个指向它的指针也依旧可以活过这一轮，在下一轮GC中被清理掉。</p><h5 id="混合写屏障"><a href="#混合写屏障" class="headerlink" title="混合写屏障"></a>混合写屏障</h5><p><span id="target2"></span></p><p>那么我们可以得知这两个写屏障的缺点：</p><ul><li>插入写屏障：需要两次STW。</li><li>删除写屏障：一次STW， 回收精度较低。</li></ul><p>那么有没有一种方式，即不需要STW，也能有较高的回收精度呢？Go V1.8版本引入了混合写屏障机制（hybrid write barrier），避免了对栈re-scan的过程，极大的减少了STW的时间。结合了两者的优点：</p><ol><li>混合写屏障一开始将栈上的对象全部标记为黑色</li><li>在GC期间任何在栈上创建的对象均为黑色。</li><li>被删除引用的对象均标记为灰色</li><li>被添加引用的对象均标记为灰色</li><li>栈不开启屏障机制</li></ol><h6 id="场景一"><a href="#场景一" class="headerlink" title="场景一"></a>场景一</h6><p><strong>对象被一个堆对象删除引用，成为栈对象的下游</strong></p><p>下图示例：请注意GC刚开始所有对象都是白色的，然后第一步扫描栈区将栈上的对象全部标记为黑色（后续不再赘述）</p><p><img data-src="/transblog/images/350634034397969.png"></p><p>对象1添加引用关系到对象7，此时因为栈是不开启任何屏障机制所以了直接添加即可，如下图：<br><img data-src="/transblog/images/452795838973598.png"></p><p>对象4删除和对象7的引用关系，那么按照规则需要将对象7置为灰色。此时我们发现不会有任何错误，如下图：</p><p><img data-src="/transblog/images/263045830266873.png"></p><h6 id="场景二"><a href="#场景二" class="headerlink" title="场景二"></a>场景二</h6><p><strong>对象被一个栈对象删除引用，成为另一个栈对象的下游</strong></p><p>首先在栈上新键一个对象9，按照规则在栈上创建的对象均为黑色对象。</p><p><img data-src="/transblog/images/39155480225688.png"></p><p>添加对象9到对象3的引用关系，由于这个栈不启动任何屏障直接添加即可。</p><p><img data-src="/transblog/images/537903220496054.png"></p><p>接着对象2删除和对象3之间的引用关系，由于是在栈上所以了之间删除即可。此时也是没有任何问题，不存在误删的情况</p><p><img data-src="/transblog/images/139063330680373.png"></p><h6 id="场景三"><a href="#场景三" class="headerlink" title="场景三"></a>场景三</h6><p><strong>对象被一个堆对象删除引用，成为另一个堆对象的下游</strong></p><p>对象10想要添加这个引用关系和对象7<br><img data-src="/transblog/images/135235495380802.png"></p><p>由于是在堆上所以了会触发这个屏障机制会将对象7变为灰色。对象7变为灰色之后，就不会被当中垃圾被回收了。</p><p><img data-src="/transblog/images/502093294335607.png"></p><h6 id="场景四"><a href="#场景四" class="headerlink" title="场景四"></a>场景四</h6><p><strong>对象从一个栈对象删除引用，成为另一个堆对象的下游</strong></p><p>对象1想要删除和对象2之间的引用关系，由于栈不开启这个屏障机制所以直接删除即可。</p><p><img data-src="/transblog/images/71224738016901.png"></p><p>对象4尝试引用对象2，对象2本来就是黑色了所以不用管。</p><p><img data-src="/transblog/images/299987682086952.png"></p><p>对象4删除和对象7之间的引用关系，此时由于是在堆上所以了触发了这个混合写屏障机制，将对象7置为灰色。</p><p><img data-src="/transblog/images/257604698885192.png"></p><p>对象7和对象6现在属于垃圾对象，应该被回收。但是对象7是灰色，这会让对象7和对象6不被回收掉。<strong>在这一轮GC的情况下确实不会被回收，但是下一轮GC就会将对象7和对象6回收掉</strong>。这一点设计者是考虑到了的</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>以上就是Go语言垃圾回收机制，Go中的混合写屏障满足<strong>弱三色不变式</strong></p><p>Go 1.3版本：普通标记清除法，整体过程需要启动STW，效率极低。<br>Go 1.5版本：三色标记法， 堆空间启动写屏障，栈空间不启动，全部扫描之后，需要重新扫描一次栈(需要STW)，效率普通<br>Go 1.8版本：三色标记法，混合写屏障机制， 栈空间不启动，堆空间启动。整个过程几乎不需要STW，效率较高。</p><h2 id="内存分配"><a href="#内存分配" class="headerlink" title="内存分配"></a>内存分配</h2><p>Go是内置运行时的编程语言(runtime)，像这种内置运行时的编程语言通常会抛弃传统的内存分配方式，改为自己管理。这样可以完成类似预分配、内存池等操作，以避开系统调用带来的性能问题，防止每次分配内存都需要系统调用。</p><p>Go的内存分配的核心思想可以分为以下几点：</p><ol><li>每次从操作系统申请一大块儿的内存，由Go来对这块儿内存做分配，减少系统调用</li><li>内存分配算法采用Google的TCMalloc算法。算法比较复杂，究其原理可自行查阅。其核心思想就是把内存切分的非常的细小，分为多级管理，以降低锁的粒度。</li><li>回收对象内存时，并没有将其真正释放掉，只是放回预先分配的大块内存中，以便复用。只有内存闲置过多的时候，才会尝试归还部分内存给操作系统，降低整体开销</li></ol><h3 id="Go的内存结构"><a href="#Go的内存结构" class="headerlink" title="Go的内存结构"></a>Go的内存结构</h3><p>Go在程序启动的时候，会分配一块连续的内存(虚拟内存)。整体如下：图中span和bitmap的大小会随着heap的改变而改变</p><p><img data-src="/transblog/images/269295915250151.png"></p><h4 id="arena"><a href="#arena" class="headerlink" title="arena"></a>arena</h4><p>arena区域就是我们通常所说的heap。</p><h4 id="spans"><a href="#spans" class="headerlink" title="spans"></a>spans</h4><p>spans区域，可以认为是用于上面所说的管理分配arena(即heap)的区域。<br>此区域存放了mspan的指针，spans区域用于表示arena区中的某一页(page)属于哪个mspan。</p><p><img data-src="/transblog/images/61650216268577.png"></p><p>思考下面的问题，假如再分配“p4”的时候，是不是内存不足没法分配了？是不是有很多碎片？<br><img data-src="/transblog/images/351170516256444.png"></p><p>go的解决方式：按需分配<br>go将内存块分为大小不同的67种，然后再把这67种大内存块，逐个分为小块(可以理解为大小不同的page)，称之为span(连续的page)，在go语言中就是上文提及的mspan</p><p><img data-src="/transblog/images/33920716276610.png"></p><p>对象分配的时候，根据对象的大小选择大小相近的span，这样，碎片问题就解决了。</p><p>在源码 src&#x2F;runtime&#x2F;sizeclasses.go,可以查到所有的 span 类型，67中不同大小的span代码注释如下(go版本1.11)：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br></pre></td><td class="code"><pre><span class="line">// class  bytes/obj  bytes/span  objects  tail waste  max waste</span><br><span class="line">//     1          8        8192     1024           0     87.50%</span><br><span class="line">//     2         16        8192      512           0     43.75%</span><br><span class="line">//     3         32        8192      256           0     46.88%</span><br><span class="line">//     4         48        8192      170          32     31.52%</span><br><span class="line">//     5         64        8192      128           0     23.44%</span><br><span class="line">//     6         80        8192      102          32     19.07%</span><br><span class="line">//     7         96        8192       85          32     15.95%</span><br><span class="line">//     8        112        8192       73          16     13.56%</span><br><span class="line">//     9        128        8192       64           0     11.72%</span><br><span class="line">//    10        144        8192       56         128     11.82%</span><br><span class="line">//    11        160        8192       51          32      9.73%</span><br><span class="line">//    12        176        8192       46          96      9.59%</span><br><span class="line">//    13        192        8192       42         128      9.25%</span><br><span class="line">//    14        208        8192       39          80      8.12%</span><br><span class="line">//    15        224        8192       36         128      8.15%</span><br><span class="line">//    16        240        8192       34          32      6.62%</span><br><span class="line">//    17        256        8192       32           0      5.86%</span><br><span class="line">//    18        288        8192       28         128     12.16%</span><br><span class="line">//    19        320        8192       25         192     11.80%</span><br><span class="line">//    20        352        8192       23          96      9.88%</span><br><span class="line">//    21        384        8192       21         128      9.51%</span><br><span class="line">//    22        416        8192       19         288     10.71%</span><br><span class="line">//    23        448        8192       18         128      8.37%</span><br><span class="line">//    24        480        8192       17          32      6.82%</span><br><span class="line">//    25        512        8192       16           0      6.05%</span><br><span class="line">//    26        576        8192       14         128     12.33%</span><br><span class="line">//    27        640        8192       12         512     15.48%</span><br><span class="line">//    28        704        8192       11         448     13.93%</span><br><span class="line">//    29        768        8192       10         512     13.94%</span><br><span class="line">//    30        896        8192        9         128     15.52%</span><br><span class="line">//    31       1024        8192        8           0     12.40%</span><br><span class="line">//    32       1152        8192        7         128     12.41%</span><br><span class="line">//    33       1280        8192        6         512     15.55%</span><br><span class="line">//    34       1408       16384       11         896     14.00%</span><br><span class="line">//    35       1536        8192        5         512     14.00%</span><br><span class="line">//    36       1792       16384        9         256     15.57%</span><br><span class="line">//    37       2048        8192        4           0     12.45%</span><br><span class="line">//    38       2304       16384        7         256     12.46%</span><br><span class="line">//    39       2688        8192        3         128     15.59%</span><br><span class="line">//    40       3072       24576        8           0     12.47%</span><br><span class="line">//    41       3200       16384        5         384      6.22%</span><br><span class="line">//    42       3456       24576        7         384      8.83%</span><br><span class="line">//    43       4096        8192        2           0     15.60%</span><br><span class="line">//    44       4864       24576        5         256     16.65%</span><br><span class="line">//    45       5376       16384        3         256     10.92%</span><br><span class="line">//    46       6144       24576        4           0     12.48%</span><br><span class="line">//    47       6528       32768        5         128      6.23%</span><br><span class="line">//    48       6784       40960        6         256      4.36%</span><br><span class="line">//    49       6912       49152        7         768      3.37%</span><br><span class="line">//    50       8192        8192        1           0     15.61%</span><br><span class="line">//    51       9472       57344        6         512     14.28%</span><br><span class="line">//    52       9728       49152        5         512      3.64%</span><br><span class="line">//    53      10240       40960        4           0      4.99%</span><br><span class="line">//    54      10880       32768        3         128      6.24%</span><br><span class="line">//    55      12288       24576        2           0     11.45%</span><br><span class="line">//    56      13568       40960        3         256      9.99%</span><br><span class="line">//    57      14336       57344        4           0      5.35%</span><br><span class="line">//    58      16384       16384        1           0     12.49%</span><br><span class="line">//    59      18432       73728        4           0     11.11%</span><br><span class="line">//    60      19072       57344        3         128      3.57%</span><br><span class="line">//    61      20480       40960        2           0      6.87%</span><br><span class="line">//    62      21760       65536        3         256      6.25%</span><br><span class="line">//    63      24576       24576        1           0     11.45%</span><br><span class="line">//    64      27264       81920        3         128     10.00%</span><br><span class="line">//    65      28672       57344        2           0      4.91%</span><br><span class="line">//    66      32768       32768        1           0     12.50%</span><br></pre></td></tr></table></figure><ul><li>class： class ID，每个span结构中都有一个class ID, 表示该span可处理的对象类型</li><li>bytes&#x2F;obj：该class代表对象的字节数</li><li>bytes&#x2F;span：每个span占用堆的字节数，也即页数*页大小</li><li>objects: 每个span可分配的对象个数，也即（bytes&#x2F;spans）&#x2F;（bytes&#x2F;obj）</li><li>waste bytes: 每个span产生的内存碎片，也即（bytes&#x2F;spans）%（bytes&#x2F;obj）</li></ul><p>以类型(class)为1的span为例,span中的元素大小是8 byte, span本身占1页也就是8K, 一共可以保存1024个对象。</p><p>细心的同学可能会发现代码中一共有66种，还有一种特殊的span：即对于大于32k的对象出现时，会直接从heap分配一个特殊的span，这个特殊的span的类型(class)是0, 只包含了一个大对象, span的大小由对象的大小决定。</p><h4 id="bitmap"><a href="#bitmap" class="headerlink" title="bitmap"></a>bitmap</h4><p>bitmap 有好几种:Stack, data, and bss bitmaps和heap bitmaps。</p><p>在此bitmap的作用是标记arena(即heap)中的对象。<strong>一是标记对应地址中是否存在对象，另外是标记此对象是否被gc标记过</strong>。一个功能一个bit位，所以，heap bitmaps用两个bit位。</p><p>bitmap区域中的一个byte对应arena区域的四个指针大小的内存的结构如下：</p><p><img data-src="/transblog/images/552064516279468.png"></p><p>bitmap的地址是由高地址向低地址增长的。</p><p><img data-src="/transblog/images/227274616277072.png"></p><p>arena中包含基本的管理单元和程序运行时候生成的对象或实体，这两部分分别被spans和bitmap这两块非heap区域的内存所对应着。</p><p><img data-src="/transblog/images/105724716274574.png"></p><h3 id="Go的内存管理组件"><a href="#Go的内存管理组件" class="headerlink" title="Go的内存管理组件"></a>Go的内存管理组件</h3><ol><li>mspan：为内存管理的基础单元，直接存储数据的地方。</li><li>mcache：每个运行期的goroutine都会绑定的一个mcache(具体来讲是绑定的GMP并发模型中的P，所以可以无锁分配mspan)，mcache会分配goroutine运行中所需要的内存空间(即mspan)。</li><li>mcentral：为所有mcache切分好后备的mspan</li><li>mheap：代表Go程序持有的所有堆空间。还会管理闲置的span，需要时向操作系统申请新内存。</li></ol><h4 id="mspan"><a href="#mspan" class="headerlink" title="mspan"></a>mspan</h4><p><strong>mspan可以说是go内存管理的最基本单元，page是内存存储的基本单元</strong>。</p><p>mspan(源码：src&#x2F;runtime&#x2F;mheap.go)是包含了 n 个页的集合,内部有两个字段startAddr和npages,分别代表 page 的开始地址,和一共几个 page. 还有spanclass字段表示该 mspan 是哪种对象的集合(上述定义的 67 种的一种)</p><p>spans 里面存放了 mspan 的集合,每个 mspan 里面存放 n 个page ,span 内部的 page 一定是连续的</p><p><img data-src="/transblog/images/149224216261588.png"></p><p><strong>mspan结构本身的内存是从系统分配的，且是双向链表</strong></p><h4 id="mcache"><a href="#mcache" class="headerlink" title="mcache"></a>mcache</h4><p>为了避免多线程申请内存时不断的加锁，goroutine为每个线程分配了span内存块的缓存，这个缓存即是mcache。</p><p>mcache (源码：src&#x2F;runtime&#x2F;mcache.go)是绑定在GMP模型中 P 上的缓存对象，每个goroutine都会绑定的一个mcache，各个goroutine申请内存时不存在锁竞争的情况。<br><a href="#target1">点击此处跳转到：Go协程调度</a><br><img data-src="/transblog/images/228075316255815.png"><br>如何做到的？因为运行期间一个goroutine只能和一个P关联，而mcache就在P上，所以，不可能有锁的竞争。</p><p><img data-src="/transblog/images/444010417273391.png"></p><p>mcache中的span链表分为两组，一组是包含指针类型的对象，另一组是不包含指针类型的对象。主要是方便GC，在进行垃圾回收的时候，对于不包含指针的对象列表无需进一步扫描是否引用其他活跃的对象。<a href="#target2">点击此处跳转到：Go垃圾回收</a><br><img data-src="/transblog/images/60135616278255.png"></p><h4 id="mcentral"><a href="#mcentral" class="headerlink" title="mcentral"></a>mcentral</h4><p>mcentral(源码：src&#x2F;runtime&#x2F;mcentral.go)为所有mcache提供切分好的mspan,但是保存了从 mheap 申请的内存块,包括带指针和不带指针的, 67 种类型.每一种类型对应一个 mcentral，每个mcentral保存一种特定类型的全局mspan列表。 </p><p><img data-src="/transblog/images/562090517266937.png"></p><p>有多少种类型的mspan就有多少个mcentral。每个mcentral都会包含两个mspan的列表：</p><ol><li>没有空闲对象或mspan已经被mcache缓存的mspan列表(empty mspanList)</li><li>有空闲对象的mspan列表(empty mspanList)</li></ol><p><img data-src="/transblog/images/594141217250601.png"></p><p>由于mspan是全局的，会被所有的mcache访问，所以会出现并发性问题，因而mcentral会存在一个锁。</p><p>单个的mcentral结构如下：</p><p><img data-src="/transblog/images/388160717260071.png"></p><p>假如需要分配内存时，mcentral没有空闲的mspan列表了，此时需要向mheap去获取。</p><h4 id="mheap"><a href="#mheap" class="headerlink" title="mheap"></a>mheap</h4><p>mheap(源码：src&#x2F;runtime&#x2F;mheap.go)保存了全局的对象,从内存分配的对象先由 mheap 托管,同时,大文件(&gt;32KB)文件也是直接从 mheap 申请,拥有全局锁</p><p>mheap可以认为是Go程序持有的整个堆空间，mheap全局唯一，可以认为是个全局变量。</p><p><img data-src="/transblog/images/59261617253105.png"></p><p>这个lock是作用是什么呢？</p><p>大对象直接通过mheap 分配，这些大对象的申请是由mcache发出的，而mcache在P上，程序运行的时候往往会存在多个P，因此，这个内存申请是并发的；所以为了保证线程安全，必须有一个全局锁。</p><p>假如需要分配的内存时，mheap中也没有了，则向操作系统申请一系列新的页（最小 1MB）。</p><h3 id="Go的内存分配"><a href="#Go的内存分配" class="headerlink" title="Go的内存分配"></a>Go的内存分配</h3><ol><li>微对象 (&lt;16B)<br>有一个微型分配器tiny allocator来分配，分配的对象都是不包含指针的，例如一些小的字符串和不包含指针的独立的逃逸变量等</li><li>小对象(16B~32KB)<br>就是mcache根据对象的大小来找自身存在的大小相匹配mspan来分配。又分为 67 种</li><li>大对象(&gt;32KB)<br>大对象直接从 mheap 上申请,同时由于可能并发,所以一定是加锁获取的</li></ol><p>微对象和小对象都是直接通过mcache来分配的，<strong>小对象分配顺序：</strong></p><ol><li>首先去 mcache 中申请，由于 mcache 是绑定在 P (GMP) 上的，所以不会发生竞争，这块不用加锁，分配器根据申请的对象大小，向上取整，找到对应的 67 种类型的一种。</li><li>当 mcache 中的该类型内存不够分配时，分配器去先去 mcentral 对应的类型下的 mspan 列表申请，然后放到P 的mcache 中，这个过程由于可能多个 goroutine 竞争，所以会有锁 </li><li>当该类型的 mcentral 中的空间也不够时，分配器会去 mheap中申请，mheap 最后给 goroutine 分配 </li><li>最后如果 mheap 中的内存也不够了，那么这时候，就需要重新去操作系统申请了(mmap)。</li></ol><p><img data-src="/transblog/images/167183317254937.png"></p><p>整体内存分配示意图：</p><p><img data-src="/transblog/images/595612317262052.png"></p><h2 id="runtime包常用"><a href="#runtime包常用" class="headerlink" title="runtime包常用"></a>runtime包常用</h2><h3 id="runtime-GOMAXPROCS"><a href="#runtime-GOMAXPROCS" class="headerlink" title="runtime.GOMAXPROCS"></a>runtime.GOMAXPROCS</h3><p> runtime.GOMAXPROCS() 可以用来设置 P 的数量，一般设置为和逻辑 CPU  数量相等的值：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">fmt.Println(runtime.NumCPU())</span><br><span class="line">runtime.GOMAXPROCS(runtime.NumCPU()) <span class="comment">// 使用所有的逻辑 CPU</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line">我的主机 CPU 是<span class="number">16</span>核<span class="number">24</span>线程，所以会使用<span class="number">24</span>个 P</span><br></pre></td></tr></table></figure><h3 id="runtime-Gosched"><a href="#runtime-Gosched" class="headerlink" title="runtime.Gosched"></a>runtime.Gosched</h3><p>runtime.Gosched() 用于让出当前协程的运行时间片，也就是当 P 遇到它时，会先安排其他协程先执行：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">runtime.GOMAXPROCS(<span class="number">1</span>)</span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">5</span>; i++ &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;go&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line">runtime.Gosched()</span><br><span class="line">fmt.Println(<span class="string">&quot;hello&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line">输入结果不是固定的，有可能是</span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line">hello</span><br><span class="line">也有可能是</span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line"><span class="keyword">go</span></span><br><span class="line">hello</span><br><span class="line"><span class="keyword">go</span></span><br><span class="line">也有可能是</span><br><span class="line">hello</span><br></pre></td></tr></table></figure><p>输出第一种情况容易理解，主协程让出了时间片，理所应当先打印 Go，但是如果子协程还没有来得及被调度或者打印，就会出现其他情况。</p><h3 id="runtime-Goexit"><a href="#runtime-Goexit" class="headerlink" title="runtime.Goexit"></a>runtime.Goexit</h3><p>runtime.Goexit() 会结束当前的协程，但是 defer 语句会正常执行。此语法不能在主函数中使用，会引发 panic:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">runtime.GOMAXPROCS(<span class="number">1</span>)</span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">defer</span> fmt.Println(<span class="string">&quot;defer不受影响&quot;</span>)</span><br><span class="line">fmt.Println(<span class="string">&quot;我被执行了&quot;</span>)</span><br><span class="line">runtime.Goexit()</span><br><span class="line">fmt.Println(<span class="string">&quot;我被跳过了&quot;</span>)</span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line">time.Sleep(<span class="number">1</span> * time.Second)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line">我被执行了</span><br><span class="line"><span class="keyword">defer</span>不受影响</span><br></pre></td></tr></table></figure><h1 id="Go的变量逃逸"><a href="#Go的变量逃逸" class="headerlink" title="Go的变量逃逸"></a>Go的变量逃逸</h1><p>以一个问题进行举例：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//test.c程序</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> * <span class="title function_">get_res</span><span class="params">(<span class="type">int</span> num)</span>&#123;</span><br><span class="line">    <span class="type">int</span> tmp = num + <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">return</span> &amp;tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">int</span> * res = get_res(<span class="number">80</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d  -- %p\n&quot;</span> , *res, res);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面写了一个简单的 C 代码，获取传入数据并 + 10 得到的结果</p><p><img data-src="/transblog/images/98652016269279.png"></p><p>这里可以看出编译程序，报了 warning 了，不过不影响程序的编译 ， 这个 warning 报错信息是 因为我们返回了临时变量的地址，C 编译器检测到了，给我们抛出了一个 warning。<br>执行编译的程序后，崩溃了 ，出现“段错误”的原因很明显，上面有说到，是因为外部访问了局部变量的地址，外部访问的时候，此时这个局部变量已经被销毁了，此时外部访问的这个指针，属于野指针，因此出现程序崩溃</p><p> 熟悉 C 的小伙伴一点都不惊慌，他们不会写出这种代码，那么 go 程序的逻辑和上面 C 程序的逻辑一模一样，那么我们看看是否会出现程序崩溃呢</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//test.go程序</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">getRes</span><span class="params">(num <span class="type">int</span>)</span></span> *<span class="type">int</span> &#123;</span><br><span class="line"> tmp := num + <span class="number">10</span></span><br><span class="line"> <span class="keyword">return</span> &amp;tmp</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> res := getRes(<span class="number">80</span>)</span><br><span class="line"> fmt.Printf(<span class="string">&quot;%d  -- %p\n&quot;</span>, *res, res)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行上述代码，查看效果</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">go run main.go</span></span><br><span class="line">90  -- 0xc420018078</span><br></pre></td></tr></table></figure><p>熟悉 go 语言的小伙伴看到这里心中也毫无波澜，程序正常执行，没有崩溃，因为他们知道原因，这个现象属于<strong>变量逃逸</strong></p><p>go自己决定了内存分配，一个变量是分配在栈上面还是堆上面，不是简单的看一个变量是局部变量就分配到栈上，这个是根据具体的使用的，有时候它也会被分配到堆上面，<strong>当我们发现本应该分配在栈上面的变量，却分配在堆上面了，说明发生了逃逸</strong></p><p>尝试写一个简单的 demo ，还是将局部变量的地址返回到外部去，外部来访问这个局部变量的地址</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">getRes</span><span class="params">(tmp <span class="type">int</span>)</span></span> *<span class="type">int</span> &#123;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">var</span> t1 <span class="type">int</span> = <span class="number">1</span></span><br><span class="line"> <span class="keyword">var</span> t2 <span class="type">int</span> = <span class="number">2</span></span><br><span class="line"> <span class="keyword">var</span> t3 <span class="type">int</span> = <span class="number">3</span></span><br><span class="line"></span><br><span class="line"> <span class="built_in">println</span>(&amp;tmp, &amp;t1, &amp;t2, &amp;t3)</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> &amp;t2</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> res := getRes(<span class="number">80</span>)</span><br><span class="line"> <span class="built_in">println</span>(*res, res)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行上述代码查看效果，通过将变量地址打印出来，貌似没有看出端倪，地址是也是连续的。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">go run main.go</span></span><br><span class="line">0xc420045f50 0xc420045f68 0xc420045f60 0xc420045f58</span><br><span class="line">2 0xc420045f60</span><br></pre></td></tr></table></figure><p>那么我们使用 go 提供的工具来看看这个程序是不是存在逃逸，执行 <code>go tool compile -m main.go</code>查看效果如下</p><p><img data-src="/transblog/images/545232616265834.jpg"></p><p>go tool compile 工具很明显的调试出来说明 t2 这个变量已经逃逸到堆上面去了，参数 -m 是直接查看是否逃逸，可以加 -S 会打印出具体的会变代码，查看该变量是否是 new 出来的</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># go tool compile -S main.go | grep new</span><br><span class="line">0x0035 00053 (main.go:6)        CALL    runtime.newobject(SB)</span><br><span class="line">rel 54+4 t=8 runtime.newobject+0</span><br></pre></td></tr></table></figure><h1 id="Go的使用"><a href="#Go的使用" class="headerlink" title="Go的使用"></a>Go的使用</h1><h2 id="Sort包"><a href="#Sort包" class="headerlink" title="Sort包"></a>Sort包</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Sort</span><span class="params">(data Interface)</span></span></span><br></pre></td></tr></table></figure><p>排序思想：</p><ol><li>当元素数目小于等于 12 时进行插入排序</li><li>当元素数目大于 12 同时阈值为 0 时进行堆排序</li><li>当元素数目大于 12 同时阈值不为 0 时进行快速排序，返回初次排序的分界值后重新进行元素数目判断排序</li></ol><p><a href="https://learnku.com/articles/46607">Sort排序原理详解</a></p><p><a href="https://blog.csdn.net/a772304419/article/details/134355591">Sort包的用法</a></p><h2 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h2><p>闭包的核心特性在于其能够捕获并持有外部变量的引用。这意味着，即使外部函数已经返回，闭包仍然可以访问这些变量。<br><strong>Go语言中的闭包通过捕获外部变量的引用来实现这一功能。当闭包捕获一个外部变量时，该变量不会随函数的结束而销毁，而是继续存在于堆内存中，直到闭包本身不再被引用</strong>。<br>以下是一个简单的示例：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">adder</span><span class="params">()</span></span> <span class="function"><span class="keyword">func</span><span class="params">(<span class="type">int</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    sum := <span class="number">0</span></span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">func</span><span class="params">(x <span class="type">int</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">        sum += x</span><br><span class="line">        <span class="keyword">return</span> sum</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">     valueFunc:= adder()</span><br><span class="line">     fmt.Println(valueFunc(<span class="number">2</span>))     <span class="comment">// output: 2</span></span><br><span class="line">     fmt.Println(valueFunc(<span class="number">2</span>))   <span class="comment">// output: 4</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/151565411250265.jpg"></p><ol><li>函数调用栈 (Stack)</li></ol><ul><li><ul><li>在程序执行时，main 函数被调用，其栈帧（stack frame）会被压入调用栈中。</li></ul></li><li><ul><li>main 函数调用了 adder 函数，adder 函数的栈帧同样会被压入调用栈。</li></ul></li><li><ul><li>在 adder 函数中，定义了局部变量 sum，初始值为 0。虽然 sum 是在 adder 的栈帧中声明的，但由于闭包的* 存在，它被捕获并存储在堆（heap）中。</li></ul></li></ul><ol start="2"><li>堆 (Heap)</li></ol><ul><li><ul><li>堆中存储了变量 sum，当 adder 函数返回匿名函数时，sum 变量被绑定在这个返回的函数对象中，匿名函数对象和捕获的 sum 在堆中分配内存。</li></ul></li><li><ul><li>由于 sum 是被闭包捕获的变量，它的值在 adder 函数返回后不会被销毁，而是被保留以供后续的函数调用。</li></ul></li></ul><ol start="3"><li>闭包捕获</li></ol><ul><li><ul><li>当 main 函数调用 valueFunc(2) 时，匿名函数被调用，sum 的值被更新并存储在堆中。每次调用 valueFunc 时，sum 的值会根据传入的参数累加并更新其在堆中的值。</li></ul></li></ul><ol start="4"><li>输出结果</li></ol><ul><li><ul><li>第一次调用 valueFunc(2) 时，sum 变为 2。</li></ul></li><li><ul><li>第二次调用 valueFunc(2) 时，sum 变为 4。这说明闭包捕获的变量 sum 在多次调用中保留了其状态。</li></ul></li></ul><p><img data-src="/transblog/images/221024615256558.jpg"></p><h1 id="Go的channel"><a href="#Go的channel" class="headerlink" title="Go的channel"></a>Go的channel</h1><p>channel(通道)用于goroutine(协程)之间的通信。它提供了一种在不同协程之间传递数据的机制。channel是一种类型安全的、阻塞的、先进先出（FIFO）的数据结构，确保发送的数据按照发送的顺序接收。Go语言提供通过通信来共享内存，而不是通过共享内存来通信</p><p>默认情况下，读写未就绪的channel（读没有数据的channel，或写缓冲区已经满了的channel）时，协程会阻塞。但是当读写channel操作和select搭配操作时，即使channel没有准备就绪，也可以执行其他分支，当前协程并不会阻塞</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">int</span>)</span><br><span class="line"><span class="keyword">select</span> &#123;</span><br><span class="line"><span class="keyword">case</span> &lt;-ch:</span><br><span class="line"><span class="keyword">default</span>:</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>channel的底层源码和相关实现在src&#x2F;runtime&#x2F;chan.go中，channel设计的核心数据结构包含以下三个：</p><h2 id="hchan"><a href="#hchan" class="headerlink" title="hchan"></a>hchan</h2><p>hchan是channel底层的数据结构，其核心是由数组实现的一个环形缓冲区</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//channel</span></span><br><span class="line"><span class="keyword">type</span> hchan <span class="keyword">struct</span> &#123;</span><br><span class="line">qcount   <span class="type">uint</span>           <span class="comment">// total data in the queue 通道中数据个数</span></span><br><span class="line">dataqsiz <span class="type">uint</span>           <span class="comment">// size of the circular queue 数据缓冲buf长度</span></span><br><span class="line">buf      unsafe.Pointer <span class="comment">// points to an array of dataqsiz elements 数组指针，存储对应的channel数据</span></span><br><span class="line">elemsize <span class="type">uint16</span> <span class="comment">//元素大小 channel 元素大小</span></span><br><span class="line">closed   <span class="type">uint32</span> <span class="comment">//通道关闭标志 </span></span><br><span class="line">elemtype *_type <span class="comment">// element type channel 元素类型</span></span><br><span class="line">sendx    <span class="type">uint</span>   <span class="comment">// send index  写入数据的index</span></span><br><span class="line">recvx    <span class="type">uint</span>   <span class="comment">// receive index 读取数据的index</span></span><br><span class="line">recvq    waitq  <span class="comment">// list of recv waiters 由双向链表实现的recv waiters 队列 阻塞读操作的gorotinue队列</span></span><br><span class="line">sendq    waitq  <span class="comment">// list of send waiters 由双向链表实现的send waiters队列 阻塞写操作的gorotinue队列</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// lock protects all fields in hchan, as well as several</span></span><br><span class="line"><span class="comment">// fields in sudogs blocked on this channel.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Do not change another G&#x27;s status while holding this lock</span></span><br><span class="line"><span class="comment">// (in particular, do not ready a G), as this can deadlock</span></span><br><span class="line"><span class="comment">// with stack shrinking.</span></span><br><span class="line">lock mutex <span class="comment">//锁</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/45341710255547.png"></p><h2 id="waitq"><a href="#waitq" class="headerlink" title="waitq"></a>waitq</h2><p>waitq 是因读写channel而陷入阻塞的协程等待队列，hchan的recvq和sendq使用这种数据结构，本质是双向链表</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//等待队列（双向链表）</span></span><br><span class="line"><span class="keyword">type</span> waitq <span class="keyword">struct</span>&#123;</span><br><span class="line">first *sudog    <span class="comment">//队列头部</span></span><br><span class="line">last *sudog     <span class="comment">//队列尾部</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="sudog"><a href="#sudog" class="headerlink" title="sudog"></a>sudog</h2><p>sudog 是协程等待队列的节点</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> sudog <span class="keyword">struct</span>&#123;</span><br><span class="line">g *g <span class="comment">//等待读取或写入的协程g，即因读写channel而陷入阻塞的协程</span></span><br><span class="line">next *sudog<span class="comment">//等待队列下一个节点</span></span><br><span class="line">prev *sudog<span class="comment">//等待队列上一个节点</span></span><br><span class="line">elem unsafe,Pointer <span class="comment">//对于写channel 是需要发送的数据指针，对于读channel是目标需要复制的数据指针</span></span><br><span class="line">success <span class="type">bool</span> <span class="comment">//标记协程g被唤醒是因为数据传递（true）还是channle被关闭（false）</span></span><br><span class="line">c *hchan<span class="comment">//对应的channel指针</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="分配内存"><a href="#分配内存" class="headerlink" title="分配内存"></a>分配内存</h2><ol><li>如果是无缓冲channel，直接给hchan结构体分配内存并返回指针。</li><li>如果是有缓冲channel，但元素不包含指针类型，则一次性为hchan结构体和底层循环数组分配连续内存并返回指针。（需要连续内存空间）</li><li>如果是有缓冲channel，且元素包含指针类型，则分别分配hchan结构体内存和底层循环数组的内存并返回指针。（可以利用内存碎片）</li></ol><p>创建channel的主要实现是在makechan()函数中：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">makechan</span><span class="params">(t *chantype, size <span class="type">int</span>)</span></span> *hchan</span><br></pre></td></tr></table></figure><h2 id="发送数据"><a href="#发送数据" class="headerlink" title="发送数据"></a>发送数据</h2><p>如果channel的读等待队列中存在接收者goroutine，则为<strong>同步发送</strong>：</p><ul><li>无缓冲channel，不用经过channel直接将数据发送给第一个等待接收的goroutine，并将其唤醒等待调度。</li><li>有缓冲channel，但是元素个数为0，不用经过channel（假装经过channel）直接将数据发送给第一个等待接收的goroutine，并将其唤醒等待调度。</li></ul><p>如果channel的读等待队列中不存在接收者goroutine：</p><ul><li>如果底层循环数组未满，那么把发送者携带的数据入队队尾，此为<strong>异步发送</strong></li><li>如果底层循环数组已满或者是无缓冲channel，那么将当前goroutine加入写等待队列，并将其挂起，等待被唤醒，此为<strong>阻塞发送</strong></li></ul><p><img data-src="/transblog/images/6754210264715.jpg"></p><h2 id="接收数据"><a href="#接收数据" class="headerlink" title="接收数据"></a>接收数据</h2><p>如果 channel 的写等待队列存在发送者 goroutine，此为<strong>同步接收</strong>：</p><ul><li>如果是无缓冲 channel，直接从第一个发送者 goroutine 那里把数据拷贝给接收变量，唤醒发送的 goroutine；</li><li>如果是有缓冲 channel（已满），将循环数组 buf 的队首元素拷贝给接收变量，将第一个发送者 goroutine 的数据拷贝到 buf 循环数组队尾，唤醒发送的 goroutine；</li></ul><p>如果 channel 的写等待队列不存在发送者 goroutine：</p><ul><li>如果循环数组 buf 非空，将循环数组 buf 的队首元素拷贝给接收变量，此为<strong>异步接收</strong></li><li>如果循环数组 buf 为空，将当前 goroutine 加入读等待队列，并挂起等待唤醒，此为<strong>阻塞接收</strong></li></ul><p><img data-src="/transblog/images/80344310254013.jpg"></p><h2 id="关闭channel"><a href="#关闭channel" class="headerlink" title="关闭channel"></a>关闭channel</h2><p>关闭操作在编译时转换为closechan()函数，总体流程如下：</p><ol><li>边界检查</li><li>从recvq释放所有的readers</li><li>从sendq释放所有的writers（会产生panic）</li><li>唤醒所有的readers和writers</li></ol><h1 id="———-请稍等————–"><a href="#———-请稍等————–" class="headerlink" title="———-请稍等————–"></a>———-请稍等————–</h1><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;数据类型&quot;&gt;&lt;a href=&quot;#数据类型&quot; class=&quot;headerlink&quot; title=&quot;数据类型&quot;&gt;&lt;/a&gt;数据类型&lt;/h1&gt;&lt;h2 id=&quot;整型&quot;&gt;&lt;a href=&quot;#整型&quot; class=&quot;headerlink&quot; title=&quot;整型&quot;&gt;&lt;/a&gt;整型&lt;/h</summary>
      
    
    
    
    <category term="语言" scheme="https://trans-xu.github.io/transblog/categories/%E8%AF%AD%E8%A8%80/"/>
    
    
  </entry>
  
  <entry>
    <title>C++</title>
    <link href="https://trans-xu.github.io/transblog/2025/08/27/C++/"/>
    <id>https://trans-xu.github.io/transblog/2025/08/27/C++/</id>
    <published>2025-08-27T03:50:08.879Z</published>
    <updated>2025-08-27T03:34:02.881Z</updated>
    
    <content type="html"><![CDATA[<h1 id="面向对象特性"><a href="#面向对象特性" class="headerlink" title="面向对象特性"></a>面向对象特性</h1><h2 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h2><p>把客观事物封装成抽象的类，类可以把自己的数据和方法只让可信的类或者对象操作，对不可信的进行信息隐藏。</p><h2 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h2><p>它可以使用现有类的所有功能，并在无需重新编写原来的类的情况下对这些功能进行扩展。其继承的过程，就是从一般到特殊的过程。</p><h3 id="实现方式"><a href="#实现方式" class="headerlink" title="实现方式"></a>实现方式</h3><ol><li>实现继承：是指使用基类的属性和方法而无需额外编码的能力；</li><li>接口继承：是指仅使用属性和方法的名称、但是子类必须提供实现的能力；</li><li>可视继承：是指子窗体（类）使用基窗体（类）的外观和实现代码的能力。</li></ol><h2 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h2><h3 id="编译时多态"><a href="#编译时多态" class="headerlink" title="编译时多态"></a>编译时多态</h3><p>编译时多态（静态多态）：通常<strong>由函数重载和函数模板实现</strong>。它的特征是函数调用在编译阶段就被解析，以不同的模板参数特例化导致调用不同的函数。</p><h4 id="函数重载"><a href="#函数重载" class="headerlink" title="函数重载"></a>函数重载</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">add</span><span class="params">(<span class="type">double</span> a, <span class="type">double</span> b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>程序从编译到运行出结果几个阶段。其中一个阶段提到生成<strong>符号表</strong>，函数重载就依赖于符号表：</p><ul><li>C语言函数重载：<br>在编译.c文件时，只会给函数进行简单的重命名；具体的方法是给函数名之前加上”_”;所以加入两个函数名相同的函数在编译之后的函数名也照样相同，调用者会因为不知道到底调用那个而出错；</li></ul><p><img data-src="/transblog/images/5868715178993.png"></p><ul><li>C++函数重载：<br>在.cpp文件中，两个函数生成的符号表中的名称不一样，所以是可以编译通过的。后边的3个字符分别表示返回值类型，两个参数类型。“@Z”表示名称结束。<br><img data-src="/transblog/images/1285516166860.png"></li></ul><h4 id="函数模板"><a href="#函数模板" class="headerlink" title="函数模板"></a>函数模板</h4><p>其实编译器对函数模板进行了两次编译：</p><ul><li>第一次编译时，首先去检查函数模板本身有没有语法错误，</li><li>第二次编译时，若编译器发现有调用，又会在调用的地方编译一次，生成相应类型的调用代码，然后通过代码的真正参数,来生成真正的函数。</li></ul><p><strong>所以函数模板,其实只是一个模具,当我们调用它时,编译器就会给我们生成真正的函数.</strong></p><p><strong>什么是函数重载、函数覆盖、函数遮蔽</strong>：</p><ol><li>函数重载发生在同一个类或顶层函数中，同名的函数具有不同的参数列表</li><li>函数覆盖发生在继承层次中，该函数在父类中必须是virtual，而子类的该函数必须与父类具有相同的参数列表</li><li>函数遮蔽（隐藏）发生在继承层次中，父类和子类同名的函数中，不属于函数覆盖的都属于函数遮蔽</li></ol><h3 id="运行时多态"><a href="#运行时多态" class="headerlink" title="运行时多态"></a>运行时多态</h3><p>运行时多态（动态多态）：通过虚函数实现，函数的具体调用是在程序运行时根据对象的类型来决定的，发生于运行期。</p><p>当某个类声明了虚函数时，编译器将为该类对象安插一个虚函数表指针，并为该类设置一张唯一的虚函数表，虚函数表中存放的是该类虚函数地址。运行期间通过虚函数表指针与虚函数表去确定该类虚函数的真正实现。<a href="#target1">点击此处跳转到虚函数</a></p><h1 id="编译与链接"><a href="#编译与链接" class="headerlink" title="编译与链接"></a>编译与链接</h1><h2 id="编译过程"><a href="#编译过程" class="headerlink" title="编译过程"></a>编译过程</h2><ol><li><strong>预处理</strong>：处理以 # 开头的指令；将.c 文件转化成 .i文件</li><li><strong>编译和优化</strong>：将源码 .i 文件翻译成 .s 汇编代码；</li><li><strong>汇编</strong>：将汇编代码 .s 翻译成机器指令 .o 文件；</li><li><strong>链接</strong>：链接程序的主要工作就是将有关的目标文件连接起来<br>.o文件无法立即执行，原因：1.某个源文件调用了另一个源文件中的函数或常量2.在程序中调用了某个库文件中的函数</li></ol><p><img data-src="/transblog/images/2944424248989.png"></p><h2 id="库和链接"><a href="#库和链接" class="headerlink" title="库和链接"></a>库和链接</h2><h3 id="静态库-a、-lib"><a href="#静态库-a、-lib" class="headerlink" title="静态库(.a、.lib)"></a>静态库(.a、.lib)</h3><p>静态库，在链接阶段，会将汇编生成的目标文件.o与引用到的库一起链接打包到可执行文件中。因此对应的链接方式称为静态链接。在该程序被执行时，这些静态库代码会被装入到该进程的虚拟地址空间中。</p><p>特点总结：</p><ul><li>静态库对函数库的链接是放在编译时期完成的。</li><li>程序在运行时与函数库再无瓜葛，移植方便。</li><li>浪费空间和资源，因为所有相关的目标文件与牵涉到的函数库被链接合成一个可执行文件。</li></ul><p>缺点：<br>更新困难，如果目标文件进行了更新操作，就需要重新进行编译链接生成可执行程序。</p><p>优点：<br>执行的时候运行速度快，因为可执行程序具备了程序运行的所有内容。</p><p><strong>Linux创建静态库过程如下</strong>：</p><ol><li>首先，将代码文件编译成目标文件.o（StaticMath.o）<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">g++ -c StaticMath.cpp <span class="comment">//注意带参数-c，否则直接编译为可执行文件</span></span><br></pre></td></tr></table></figure></li><li>然后，通过ar工具将目标文件打包成.a静态库文件<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ar -crv libstaticmath.a StaticMath.o</span><br></pre></td></tr></table></figure></li><li>生成静态库libstaticmath.a。</li></ol><h3 id="动态库-so、-dll"><a href="#动态库-so、-dll" class="headerlink" title="动态库(.so、.dll)"></a>动态库(.so、.dll)</h3><p>代码被放到动态链接库或共享对象的某个目标文件中，链接程序只是在最终的可执行程序中记录了共享对象的名字等一些信息。在程序执行时，动态链接库的全部内容会被映射到运行时相应进行的虚拟地址的空间。</p><p>特点总结：</p><ul><li>动态库在程序编译时并不会被连接到目标代码中，而是在程序运行是才被载入，把对一些库函数的链接载入推迟到程序运行的时期。</li><li>不同的应用程序如果调用相同的库，那么在内存里只需要有一份该共享库的实例，规避了空间浪费问题。</li><li>可以实现进程之间的资源共享（因此动态库也称为共享库）。</li><li>甚至可以真正做到链接载入完全由程序员在程序代码中控制（显式调用）。</li></ul><p>优点：<br>节省内存、更新方便、增量更新，将一些程序升级变得简单，只需要更新动态库即可</p><p>缺点：<br>动态链接在程序运行时，每次执行都需要链接，相比静态链接会有一定的性能损失。</p><p><strong>Linux创建动态库过程如下</strong>：</p><ol><li>首先，生成目标文件，此时要加编译器选项-fpic<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">g++ -fPIC -c DynamicMath.cpp <span class="comment">//-fPIC 创建与地址无关的编译程序（pic，position independent code），是为了能够在多个应用程序间共享。</span></span><br></pre></td></tr></table></figure></li><li>然后，生成动态库，此时要加链接器选项-shared<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">g++ -shared -o libdynmath.so DynamicMath.o <span class="comment">//-shared指定生成动态链接库。</span></span><br></pre></td></tr></table></figure></li></ol><h2 id="gcc和g"><a href="#gcc和g" class="headerlink" title="gcc和g++"></a>gcc和g++</h2><p>GCC:GNU Compiler Collection(GUN 编译器集合)，它可以编译C、C++、JAV、Fortran、Pascal、Object-C、Ada等语言。</p><ul><li>gcc是GCC中的GUN C Compiler（C 编译器）</li><li>g++是GCC中的GUN C++ Compiler（C++编译器）</li></ul><h3 id="区别"><a href="#区别" class="headerlink" title="区别"></a>区别</h3><ol><li>对于* .c和* .cpp文件，gcc分别当做c和cpp文件编译（c和cpp的语法强度是不一样的）</li><li>对于* .c和* .cpp文件，g++则统一当做cpp文件编译</li><li>使用g++编译文件时，g++会自动链接标准库STL，而gcc不会自动链接STL</li><li>gcc在编译C文件时，可使用的预定义宏是比较少的</li><li>gcc在编译cpp文件时&#x2F;g++在编译c文件和cpp文件时（这时候gcc和g++调用的都是cpp文件的编译器），会加入一些额外的宏，这些宏如下：<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> __GXX_WEAK__ 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __cplusplus 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __DEPRECATED 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __GNUG__ 4</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __EXCEPTIONS 1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __private_extern__ extern</span></span><br></pre></td></tr></table></figure></li><li>在用gcc编译c++文件时，为了能够使用STL，需要加参数 –lstdc++ ，但这并不代表 gcc –lstdc++ 和 g++等价。<br>误区:编译只能用gcc，链接只能用g++<br>编译可以用gcc&#x2F;g++，而链接可以用g++或者gcc -lstdc++。因为gcc命令不能自动和C＋＋程序使用的库联接，所以通常使用g++来完成联接。但在编译阶段，g++会自动调用gcc，二者等价。</li></ol><h2 id="gdb工作原理"><a href="#gdb工作原理" class="headerlink" title="gdb工作原理"></a>gdb工作原理</h2><p>通过一个系统调用：ptrace。ptrace系统调用的原型如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/ptrace.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">long</span> <span class="title">ptrace</span><span class="params">(<span class="keyword">enum</span> __ptrace_request request, <span class="type">pid_t</span> pid,</span></span></span><br><span class="line"><span class="params"><span class="function">                   <span class="type">void</span> *addr, <span class="type">void</span> *data)</span></span>;</span><br></pre></td></tr></table></figure><p>ptrace系统调用提供了一种方法来让父进程可以观察和控制其它进程的执行，检查和改变其核心映像以及寄存器。 主要用来实现断点调试和系统调用跟踪。（可查阅man手册）</p><ol><li>request：指定调试的指令，指令的类型很多，如：PTRACE_TRACEME、PTRACE_PEEKUSER、PTRACE_CONT、PTRACE_GETREGS等等，下面会介绍不同指令的作用。</li><li>pid：进程的ID（这个不用解释了）。</li><li>addr：进程的某个地址空间，可以通过这个参数对进程的某个地址进行读或写操作。</li><li>data：根据不同的指令，有不同的用途，下面会介绍。</li></ol><p>下面我们来看ptrace函数中request参数的一些主要选项：</p><ul><li>PTRACE_TRACEME： 表示本进程将被其父进程跟踪，交付给这个进程的所有信号，即使信号是忽略处理的（除SIGKILL之外），都将使其停止，父进程将通过wait()获知这一情况。<br>如果在gdb中run一个程序，首先gdb会fork一个子进程，然后该子进程调用ptrace系统调用，参数是PTRACE_TRACEME，然后调用一个exec执行程序。基本过程是这样，细节上可能会有出入。<strong>PTRACE_TRACEME是由子进程调用</strong>而不是父进程！</li></ul><p>以下选项都是由父进程调用：</p><ul><li><p>PTRACE_ATTACH：attach到一个指定的进程，使其成为当前进程跟踪的子进程，而<strong>子进程的行为等同于它进行了一次PTRACE_TRACEME操作</strong>。但是，需要注意的是，虽然当前进程成为被跟踪进程的父进程，但是子进程使用getppid()的到的仍将是其原始父进程的pid。<br>当你在gdb中使用attach命令来跟踪一个指定进程&#x2F;线程的时候，gdb就自动成为改进程的父进程，而被跟踪的进程则使用了一次PTRACE_TRACEME，gdb也就顺理成章的接管了这个进程。</p></li><li><p>PTRACE_CONT：继续运行之前停止的子进程。可同时向子进程交付指定的信号。<br>相当于gdb中的continue命令。当你使用continue命令之后，一个被gdb停止的进程就能继续执行下去，如果有信号，信号也会被交付给子进程。</p></li></ul><h1 id="浅入C"><a href="#浅入C" class="headerlink" title="浅入C++"></a>浅入C++</h1><h2 id="指针和引用"><a href="#指针和引用" class="headerlink" title="指针和引用"></a>指针和引用</h2><h3 id="指针传递的实质"><a href="#指针传递的实质" class="headerlink" title="指针传递的实质"></a>指针传递的实质</h3><p>形参为指向实参地址的指针，当对形参的指向操作时，就相当于对实参本身进行的操作</p><h3 id="引用传递的实质"><a href="#引用传递的实质" class="headerlink" title="引用传递的实质"></a>引用传递的实质</h3><p>被调函数的形参也作为局部变量在栈中开辟了内存空间，但是这时存放的是由主调函数放进来的<strong>实参的地址</strong>。被调函数对形参的任何操作都被处理成<strong>间接寻址</strong>，即通过栈中存放的地址访问主调函数中的实参变量。正因为如此，被调函数对形参做的任何操作都影响了主调函数中的实参变量。</p><h3 id="区别-1"><a href="#区别-1" class="headerlink" title="区别"></a>区别</h3><ol><li>指针是一个实体，而引用仅是个别名；</li><li>引用只能在定义时被初始化一次，之后不可变；指针可变；</li><li>引用不能为空，指针可以为空；</li><li>“sizeof (引用)”得到的是所指向的变量(对象)的大小，而“sizeof (指针)”得到的是指针本身的大小；32位操作系统返回值4，64位操作系统返回值8</li><li>引用是类型安全的，而指针不是 （引用比指针多了类型检查）</li></ol><h2 id="回调函数"><a href="#回调函数" class="headerlink" title="回调函数"></a>回调函数</h2><p>回调函数机制：</p><ol><li>定义一个函数（普通函数即可）；</li><li>将此函数的地址注册给调用者；</li><li>特定的事件或条件发生时，调用者使用函数指针调用回调函数。</li></ol><h3 id="函数指针"><a href="#函数指针" class="headerlink" title="函数指针"></a>函数指针</h3><p>函数指针是指向函数的指针变量，其定义语法如下：</p><blockquote><p>*返回类型 (指针变量名)(参数列表);</p></blockquote><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="comment">// 定义回调函数类型，它接受一个整数参数，返回一个整数</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">int</span> <span class="params">(*CallbackFunction)</span><span class="params">(<span class="type">int</span>)</span></span>; </span><br><span class="line"><span class="comment">// 函数，它接收一个回调函数作为参数，并对给定数值进行处理</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">processValue</span><span class="params">(<span class="type">int</span> num, CallbackFunction callback)</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">callback</span>(num);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 具体的回调函数，实现将输入数值加10的功能</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">addTen</span><span class="params">(<span class="type">int</span> num)</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> num + <span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 另一个具体的回调函数，实现将输入数值乘以2的功能</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">multiplyByTwo</span><span class="params">(<span class="type">int</span> num)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> num * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> originalValue = <span class="number">5</span>;</span><br><span class="line">    <span class="comment">// 使用addTen作为回调函数进行处理</span></span><br><span class="line">    <span class="type">int</span> result1 = <span class="built_in">processValue</span>(originalValue, addTen);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;使用addTen回调函数的结果: &quot;</span> &lt;&lt; result1 &lt;&lt; std::endl;</span><br><span class="line">    <span class="comment">// 使用multiplyByTwo作为回调函数进行处理</span></span><br><span class="line">    <span class="type">int</span> result2 = <span class="built_in">processValue</span>(originalValue, multiplyByTwo);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;使用multiplyByTwo回调函数的结果: &quot;</span> &lt;&lt; result2 &lt;&lt; std::endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="函数对象-仿函数"><a href="#函数对象-仿函数" class="headerlink" title="函数对象(仿函数)"></a><span id="target4">函数对象(仿函数)</span></h3><p>函数对象是一个类，它重载了函数调用运算符 operator()，使得类的对象可以像函数一样被调用。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="comment">// 函数对象（仿函数）类，用于将容器中的元素翻倍</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DoubleElement</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>&amp; element)</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        element *= <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 函数用于遍历容器，并对每个元素调用传入的回调函数进行处理</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Container, <span class="keyword">typename</span> Functor&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">processContainer</span><span class="params">(Container&amp; container, Functor functor)</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; element : container)</span><br><span class="line">     &#123;</span><br><span class="line">        <span class="built_in">functor</span>(element);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; numbers = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;处理前的容器元素: &quot;</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> num : numbers)</span><br><span class="line">     &#123;</span><br><span class="line">        std::cout &lt;&lt; num &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    std::cout &lt;&lt; std::endl;</span><br><span class="line">    <span class="comment">// 使用 DoubleElement 函数对象作为回调函数处理容器元素</span></span><br><span class="line">    <span class="built_in">processContainer</span>(numbers, <span class="built_in">DoubleElement</span>());</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;处理后的容器元素: &quot;</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> num : numbers) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; num &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    std::cout &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="事件处理"><a href="#事件处理" class="headerlink" title="事件处理"></a>事件处理</h3><p>std::function是一个泛型函数封装器，它可以存储、调用任何可调用的目标——函数、lambda表达式、绑定表达式或其他函数对象，以及它们的重载了()操作符的类的实例。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="comment">// 定义一个简单的事件处理器类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EventHandler</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 使用 std::function 来存储不同类型的回调函数，这里的回调函数无参数，无返回值</span></span><br><span class="line">    std::function&lt;<span class="type">void</span>()&gt; onButtonClick;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    EventHandler handler;</span><br><span class="line">    handler.onButtonClick = []() </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;按钮被点击了！&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">// 模拟按钮点击事件，调用存储的回调函数</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;模拟触发按钮点击事件&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    handler.<span class="built_in">onButtonClick</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h2><p><span id="target5"></span><br>在 C&#x2F;C++ 中，volatile 关键字用于告诉编译器一个变量的值可能会在程序的任何时刻被外部因素改变，因此编译器不应该优化对这个变量的访问。具体来说，volatile 的主要作用有以下几方面：</p><ol><li>防止优化：<br>编译器在优化代码时，通常会假设变量的值在其生命周期内不会改变。但对于 volatile 声明的变量，编译器会强制每次访问该变量时都进行实际的内存读写，而不是使用寄存器中的缓存值。</li><li><strong>多任务环境下各任务间共享的标志应该加volatile</strong>：<br>在多线程程序中，多个线程可能会访问同一个变量，使用 volatile 可以确保一个线程对该变量的修改能被其他线程立即看到，尽管它并不能替代适当的线程同步机制。<strong>该关键字的作用是防止优化编译器把变量从内存装入CPU寄存器中</strong>。如果变量被装入寄存器，那么两个线程有可能一个使用内存中的变量，一个使用寄存器中的变量，这会造成程序的错误执行。volatile的意思是让编译器每次操作该变量时一定要从内存中真正取出，而不是使用已经存在寄存器中的值，</li><li>硬件寄存器：<br>在嵌入式编程中，通常会用 volatile 来描述对硬件寄存器的访问，因为这些寄存器的值可能会被硬件事件改变。</li><li>信号处理：<br>在处理信号时，使用 volatile 可以保证信号处理程序对变量的修改能够被主程序看到。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="type">int</span> counter = <span class="number">0</span>;  </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">increment_counter</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    counter++;  <span class="comment">// 明确告知编译器 counter 的值可能被改变  </span></span><br><span class="line">&#125;  </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">read_counter</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">    <span class="type">int</span> temp = counter;  <span class="comment">// 每次读取都应从内存中获取值  </span></span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure><p>在这个例子中，counter 被声明为 volatile，这样编译器不会对其读写进行优化，确保每次对 counter 的访问都是直接从内存中获取值。</p><h2 id="extern-“C”"><a href="#extern-“C”" class="headerlink" title="extern “C”"></a>extern “C”</h2><p>“提示”编译器，下面的文件和函数，要用C语言的命名规则进行。<br>所以加入要在A.cpp中使用C语言的B.lib库文件中的在xx.h中定义的导出函数CFun()，需要这么做。</p><ol><li>首先将这个库文件链接到我们的工程中。</li><li>声明<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;abc.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="type">void</span> <span class="title">CFun</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>但是我发现，有时候不使用extern “C”的方式(而是直接#include “”….)也可以使用这类函数，原因分析如下：</li><li>那个库文件并不是纯正的C语言下环境下编译而成的(具体是环境是在.c文件中使用了C++中的关键字)。</li><li>extern “C”只是一种“提示”，而不是强制性命令编译器，具体是要不要用C语言规则，让编译器自己去判断。</li></ol><p>在C语言的库文件中，”abc.h”包含如下定义：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">﻿<span class="meta">#<span class="keyword">ifdef</span> __cplusplus  <span class="comment">//在C++编译环境下</span></span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也就是说，在”abc.c”中 #include “abc.h”进行编译的时候，#ifdef__cplusplus并不会生效，因为当前是C语言的编译环境。<br>而在外部”abc.cpp”中  #include “abc.h” #ifdef __cplusplus就会生效了，所以可以用C语言的方式进行编译。</p><h2 id="define和inline"><a href="#define和inline" class="headerlink" title="define和inline"></a>define和inline</h2><h3 id="define"><a href="#define" class="headerlink" title="define"></a>define</h3><p>宏定义在形式及使用上像一个函数，但它使用预处理器实现，没有了参数压栈，代码生成等一系列的操作,因此，效率很高<br>缺点：</p><ol><li>宏定义在形式上类似于一个函数，但在使用它时，仅仅只是做预处理器符号表中的简单替换，因此它不能进行参数有效性的检测，也就不能享受C++编译器严格类型检查的好处，另外它的返回值也不能被强制转换为可转换的合适的类型，和局限性。</li><li>宏不能访问对象私有成员。</li><li>define用参数时，是严格的替换策略。无论你得参数时何种形式，在展开代码中都是用形参替换实参。这样，宏的定义很容易产生二义性，它的使用就存在着一系列的隐患</li></ol><p>　</p><h3 id="inline"><a href="#inline" class="headerlink" title="inline"></a>inline</h3><p>内联函数和普通函数的区别在于：当编译器处理调用内联函数的语句时，不会将该语句编译成函数调用的指令，而是直接将整个函数体的代码插人调用语句处，就像整个函数体在调用处被重写了一遍一样。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">int</span> <span class="title">max</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> <span class="keyword">return</span> a &gt; b ? a : b;</span><br><span class="line">&#125;</span><br><span class="line">调用：cout&lt;&lt;<span class="built_in">max</span>(a, b)&lt;&lt;endl;</span><br><span class="line">在编译时展开为： cout&lt;&lt;(a &gt; b ? a : b)&lt;&lt;endl;</span><br></pre></td></tr></table></figure><h3 id="区别-2"><a href="#区别-2" class="headerlink" title="区别"></a>区别</h3><p>1.内联函数在运行时可调试，而宏定义不可以;<br>2.编译器会对内联函数的参数类型做安全检查或自动类型转换（同普通函数），而宏定义则不会；<br>3.内联函数可以访问类的成员变量，宏定义则不能；</p><h2 id="include重复引入"><a href="#include重复引入" class="headerlink" title="include重复引入"></a>include重复引入</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//student.h</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//school.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;student.h&quot;</span></span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">School</span> &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"> </span><br><span class="line">    Student stu[<span class="number">50</span>];</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//main.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;student.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;school.h&quot;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>编译器报“Student 类型重定义”错误</strong></p><h3 id="使用宏定义避免重复引入"><a href="#使用宏定义避免重复引入" class="headerlink" title="使用宏定义避免重复引入"></a>使用宏定义避免重复引入</h3><p>在实际多文件开发中，我们往往使用如下的宏定义来避免发生重复引入：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> _NAME_H</span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _NAME_H</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//头文件内容</span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>其中，_NAME_H 是宏的名称。这里设置的宏名必须是独一无二的<br>当程序中第一次 #include 该文件时，由于_NAME_H 尚未定义，所以会定义_NAME_H并执行“头文件内容”部分的代码；当发生多次 #include 时，因为前面已经定义了 _NAME_H，所以不会再重复执行“头文件内容”部分的代码。<br>student.h 文件做如下修改：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> _STUDENT_H</span></span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _STUDENT_H</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>虽然该项目 main.cpp 文件中仍 #include 了 2 次 “student.h”，但鉴于 _STUDENT_H 宏只能定义一次，所以 Student 类也仅会定义一次。再次执行该项目会发现，其可以正常执行。</p><h3 id="使用-pragma-once避免重复引入"><a href="#使用-pragma-once避免重复引入" class="headerlink" title="使用#pragma once避免重复引入"></a>使用#pragma once避免重复引入</h3><p>使用 #pragma once指令，将其附加到指定文件的最开头位置，则该文件就只会被 #include 一次。<br>我们知道，#ifndef 是通过定义独一无二的宏来避免重复引入的，这意味着每次引入头文件都要进行识别，所以效率不高。但考虑到 C 和 C++ 都支持宏定义，所以项目中使用 #ifndef 规避可能出现的“头文件重复引入”问题，不会影响项目的可移植性。<br>和 ifndef 相比，#pragma once 不涉及宏定义，当编译器遇到它时就会立刻知道当前文件只引入一次，所以效率很高。<br><strong>除此之外，#pragma once 只能作用于某个具体的文件，而无法向 #ifndef 那样仅作用于指定的一段代码。</strong><br>将其内容修改为：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"> </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="使用-Pragma操作符"><a href="#使用-Pragma操作符" class="headerlink" title="使用_Pragma操作符"></a>使用_Pragma操作符</h3><p>C99 标准中新增加了一个和 #pragma 指令类似的 _Pragma 操作符，其可以看做是 #pragma 的增强版，不仅可以实现 #pragma 所有的功能，更重要的是，_Pragma 还能和宏搭配使用。<br>当处理头文件重复引入问题时，可以将如下语句添加到相应文件的开头：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">_Pragma</span>(<span class="string">&quot;once&quot;</span>)</span><br></pre></td></tr></table></figure><p>比如，将该语句添加到前面项目中 student.h 文件中的开头位置，再次执行项目，其可以正常执行。<br>事实上，无论是 C 语言还是 C++，<strong>为防止用户重复引入系统库文件，几乎所有库文件中都采用了以上 3 种结构中的一种，这也是为什么重复引入系统库文件编译器也不会报错的原因。</strong></p><h2 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h2><h3 id="隐式类型转换"><a href="#隐式类型转换" class="headerlink" title="隐式类型转换"></a>隐式类型转换</h3><ol><li>算术转换(Arithmetic conversion) : 在混合类型的算术表达式中, 最宽的数据类型成为目标转换类型。<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> ival = <span class="number">3</span>;</span><br><span class="line"><span class="type">double</span> dval = <span class="number">3.14159</span>;</span><br><span class="line">ival + dval;<span class="comment">//ival被提升为double类型</span></span><br></pre></td></tr></table></figure></li><li>一种类型表达式赋值给另一种类型的对象：目标类型是被赋值对象的类型<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> *pi = <span class="number">0</span>; <span class="comment">// 0被转化为int *类型</span></span><br><span class="line">ival = dval; <span class="comment">// double-&gt;int</span></span><br></pre></td></tr></table></figure>例外：void指针赋值给其他指定类型指针时，不存在标准转换，编译出错</li><li>将一个表达式作为实参传递给函数调用，此时形参和实参类型不一致：目标转换类型为形参的类型<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="type">double</span> <span class="title">sqrt</span><span class="params">(<span class="type">double</span>)</span></span>;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot;The square root of 2 is &quot;</span> &lt;&lt; <span class="built_in">sqrt</span>(<span class="number">2</span>) &lt;&lt; endl;</span><br><span class="line"><span class="comment">//2被提升为double类型：2.0</span></span><br></pre></td></tr></table></figure></li><li>从一个函数返回一个表达式，表达式类型与返回类型不一致：目标转换类型为函数的返回类型<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">double</span> <span class="title">difference</span><span class="params">(<span class="type">int</span> ival1, <span class="type">int</span> ival2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ival1 - ival2;</span><br><span class="line">    <span class="comment">//返回值被提升为double类型</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="显式类型转换"><a href="#显式类型转换" class="headerlink" title="显式类型转换"></a>显式类型转换</h3><ol><li>向上转换：子类向基类的转换</li><li>向下转换：基类向子类的转换</li></ol><h4 id="static-cast"><a href="#static-cast" class="headerlink" title="static_cast"></a>static_cast</h4><p>进行无条件转换，静态类型转换：</p><ol><li>基类和子类之间的转换：<strong>其中子类指针转换为父类指针是安全的</strong>，但父类指针转换为子类指针是不安全的（基类和子类之间的动态类型转换建议用dynamic_cast）。</li><li>基本数据类型转换，enum，struct，int，char，float等。static_cast不能进行无关类型（如非基类和子类）指针之间的转换。</li><li>把任何类型的表达式转换成void类型。<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> c=<span class="built_in">static_cast</span>&lt;<span class="type">int</span>&gt;(<span class="number">7.987</span>);</span><br></pre></td></tr></table></figure></li></ol><h4 id="const-cast"><a href="#const-cast" class="headerlink" title="const_cast"></a>const_cast</h4><p>去掉类型的const或volatile属性<br><a href="#target5">点击此处跳转到volatile关键字</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">T</span> &#123;</span><br><span class="line">        <span class="type">int</span> i;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> T a;</span><br><span class="line">    <span class="comment">//a.i = 10;  //直接修改const类型，编译错误</span></span><br><span class="line">    T &amp;b = <span class="built_in">const_cast</span>&lt;T&amp;&gt;(a);</span><br><span class="line">    b.i = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="dynamic-cast"><a href="#dynamic-cast" class="headerlink" title="dynamic_cast"></a>dynamic_cast</h4><p>有条件转换，动态类型转换，运行时检查类型安全（转换失败返回NULL）：</p><ol><li>安全的基类和子类之间的转换。</li><li>必须有虚函数。原因：类中存在虚函数，就说明它有想要让基类指针或引用指向派生类对象的情况，此时转换才有意义。</li><li>相同基类不同子类之间的交叉转换，但结果返回NULL。</li></ol><ul><li>对指针进行dynamic_cast，失败返回null，成功返回正常cast后的对象指针；</li><li>对引用进行dynamic_cast，失败抛出一个异常bad_cast，成功返回正常cast后的对象引用。</li></ul><ol><li>对于“向上转换”（即派生类指针或引用转换为其基类类型）都是安全的。</li><li>对于“向下转型”有两种情况：<br>第一，基类指针所指对象是派生类类型，这种转换是安全的；<br>第二，基类指针所指对象为基类类型，dynamic_cast在运行时做检查，转换失败，返回结果为0。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line">   <span class="keyword">public</span>:</span><br><span class="line">   <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">f</span><span class="params">()</span></span></span><br><span class="line"><span class="function">   </span>&#123;</span><br><span class="line">       cout&lt;&lt;<span class="string">&quot;helloA&quot;</span>&lt;&lt;endl;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>:<span class="keyword">public</span> A</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout&lt;&lt;<span class="string">&quot;helloB&quot;</span>&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">pp</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    A* a1=<span class="keyword">new</span> B;<span class="comment">//a1是A类型的指针指向一个B类型的对象</span></span><br><span class="line">    A* a2=<span class="keyword">new</span> A;<span class="comment">//a2是A类型的指针指向一个A类型的对象</span></span><br><span class="line">    B* b;</span><br><span class="line">    C* c;</span><br><span class="line">    b=<span class="built_in">dynamic_cast</span>&lt;B*&gt;(a1);<span class="comment">//向下转换成功，a1（A类型的指针）之前指向的就是B类型的对象，所以可以转换成B类型的指针。</span></span><br><span class="line">    b=<span class="built_in">dynamic_cast</span>&lt;B*&gt;(a2);<span class="comment">//结果为null，向下转换失败</span></span><br><span class="line">    c=<span class="built_in">dynamic_cast</span>&lt;C*&gt;(a);<span class="comment">//结果为null，向下转换失败</span></span><br><span class="line">    <span class="built_in">delete</span>(a);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="reinterpret-cast"><a href="#reinterpret-cast" class="headerlink" title="reinterpret_cast"></a>reinterpret_cast</h4><p>它可以转化任何内置的数据类型为其他任何的数据类型，也可以转化任何指针类型为其他的类型。它甚至可以转化内置的数据类型为指针，无须考虑类型安全或者常量的情形。不到万不得已绝对不用。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//不同类型的指针转换</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Class A&quot;</span> &lt;&lt; std::endl; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">bar</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Class B&quot;</span> &lt;&lt; std::endl; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    A a;</span><br><span class="line">    B* b = <span class="built_in">reinterpret_cast</span>&lt;B*&gt;(&amp;a);  <span class="comment">// 强制将 A* 转换为 B*</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 由于这是不安全的，直接调用会导致未定义行为</span></span><br><span class="line">    b-&gt;<span class="built_in">bar</span>();  <span class="comment">// 可能导致程序崩溃</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个例子中，reinterpret_cast 将 A* 类型的指针转换为 B* 类型。虽然它是允许的，但由于 A 和 B 是不同的类型，直接访问会导致未定义行为。在这种情况下，它不会执行有效的操作，可能会导致程序崩溃。</p><h1 id="深入C"><a href="#深入C" class="headerlink" title="深入C++"></a>深入C++</h1><h2 id="C-内存"><a href="#C-内存" class="headerlink" title="C++内存"></a>C++内存</h2><p>可以结合操作系统–内存篇学习</p><h3 id="内存分布"><a href="#内存分布" class="headerlink" title="内存分布"></a>内存分布</h3><ol><li>BSS段（bss）：<br>包括已初始化的静态常量和全局变量，BSS段属于静态内存分配。</li><li>数据段（data）：<br>包括未初始化的静态变量和全局变量。数据段属于静态内存分配。<br><strong>1和2合起来又称全局数据区(或静态存储区)</strong></li><li>代码段（code &#x2F;text）：<br>用来存放程序执行代码的一块内存区域。这部分区域的大小在程序运行前就已经确定，并且内存区域通常属于只读,，某些架构也允许代码段为可写，即允许修改程序。在代码段中，也有可能包含一些只读的常数变量，例如字符串常量等。</li><li>堆（heap）：<br>堆是用于存放进程运行中被动态分配的内存段，它的大小并不固定，可动态扩张或缩减。当进程调用malloc等函数分配内存时，新分配的内存就被动态添加到堆上（堆被扩张）；当利用free等函数释放内存时，被释放的内存从堆中被剔除（堆被缩减）</li><li>文件映射段：<br>包括动态库、共享内存等，<strong>从低地址开始向上增长</strong>（跟硬件和内核版本有关 ）。</li><li>栈(stack)：<br>存<strong>放函数的局部变量、函数参数、返回地址等</strong>，由编译器自动分配和释放，是用户存放程序临时创建的局部变量，也就是说我们函数括弧“{}”中定义的变量。除此以外，在函数被调用时，其参数也会被压入发起调用的进程栈中，并且待到调用结束后，函数的返回值也会被存放回栈中。由于栈的先进先出特点，所以栈特别方便用来保存&#x2F;恢复调用现场。从这个意义上讲，我们可以把堆栈看成一个寄存、交换临时数据的内存区。</li></ol><p><img data-src="/transblog/images/416540910246114.png"></p><h3 id="栈和堆区别"><a href="#栈和堆区别" class="headerlink" title="栈和堆区别"></a>栈和堆区别</h3><ol><li><p>申请方式：栈是系统自动分配，堆是程序员主动申请。</p></li><li><p>申请后系统响应：</p></li></ol><ul><li>分配栈空间，如果剩余空间大于申请空间则分配成功，否则分配失败栈溢出；</li><li>申请堆空间，堆在内存中呈现的方式类似于链表（记录空闲地址空间的链表），在链表上寻找第一个大于申请空间的节点分配给程序，将该节点从链表中删除，大多数系统中该块空间的首地址存放的是本次分配空间的大小，便于释放，将该块空间上的剩余空间再次连接在空闲链表上。</li></ul><ol start="3"><li><p><strong>栈在内存中是连续的一块空间[向低地址扩展]，最大容量是系统预定好的，堆在内存中的空间[向高地址扩展]是不连续的</strong>。</p></li><li><p>申请效率：栈是有系统自动分配，申请效率高，但程序员无法控制；堆是由程序员主动申请，效率低，使用起来方便但是容易产生碎片。</p></li><li><p>存放内容：栈中存放的是局部变量，函数的参数；堆中存放的内容由程序员控制。</p></li></ol><h3 id="new与malloc区别"><a href="#new与malloc区别" class="headerlink" title="new与malloc区别"></a>new与malloc区别</h3><ol><li><p>malloc与free是c++&#x2F;c语言的标准函数，new&#x2F;delete是C++的运算符，  C++允许重载new&#x2F;delete操作符</p></li><li><p>new&#x2F;delete比malloc&#x2F;free更加智能，其实底层也是执行的malloc&#x2F;free。<strong>new和delete在对象创建的时候自动执行构造函数，对象消亡之前会自动执行析构函数</strong>。</p></li><li><p>new内存分配失败时，会抛出bac_alloc异常。malloc分配内存失败时返回NULL。</p></li><li><p>new返回指定类型的指针，并且可以自动计算出所需要的大小。malloc必须用户指定大小，并且默认返回类型为void*,必须强行转换为实际类型的指针。</p></li><li><p>申请的内存所在位置<br>new操作符从自由存储区(free store)上为对象动态分配内存空间，而malloc函数从堆上动态分配内存。自由存储区是否能够是堆，这取决于operator new 的实现细节，在哪里为对象分配内存。不仅可以是堆，还可以是静态存储区。</p></li><li><p>对数组的处理不同<br>C++提供了new[]与delete[]来专门处理数组类型:</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A * ptr = <span class="keyword">new</span> A[<span class="number">10</span>];<span class="comment">//分配10个A对象</span></span><br></pre></td></tr></table></figure><p>使用new[]分配的内存必须使用delete[]进行释放：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">delete</span> [] ptr;</span><br></pre></td></tr></table></figure><p>new对数组的支持体现在它会分别调用构造函数函数初始化每一个数组元素，释放对象时为每个对象调用析构函数。注意delete[]要与new[]配套使用，不然会找出数组对象部分释放的现象，造成内存泄漏。<br>至于malloc，它并不知道你在这块内存上要放的数组还是啥别的东西，反正它就给你一块原始的内存，在给你个内存的地址就完事。所以如果要动态分配一个数组的内存，还需要我们手动自定数组的大小：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> * ptr = (<span class="type">int</span> *) <span class="built_in">malloc</span>( <span class="built_in">sizeof</span>(<span class="type">int</span>)* <span class="number">10</span> );<span class="comment">//分配一个10个int元素的数组</span></span><br></pre></td></tr></table></figure></li></ol><h3 id="内存泄漏"><a href="#内存泄漏" class="headerlink" title="内存泄漏"></a>内存泄漏</h3><p>内存泄漏的几种情况：</p><ol><li><p>分配和释放不匹配<br>使用 new 操作符分配的内存，没有主动使用 delete 操作符释放。<br>使用 malloc 申请的内存，没有主动调用 free 释放。</p></li><li><p>在类的构造函数和析构函数分配和释放不匹配<br>二是在类的构造函数中动态的分配了内存(new)，但是在析构函数中没有释放内存或者没有正确的释放内存(delete)</p></li><li><p>没有正确地清除嵌套的对象指针（share_ptr）</p></li><li><p>释放对象数组时在delete中没有使用方括号</p></li><li><p>释放指向对象的指针数组没有循环释放每个对象<br><strong>指向对象的指针数组≠对象数组</strong></p></li></ol><ul><li><p>对象数组：数组中存放的是对象，需要delete []p，方括号是告诉编译器这个指针指向的是一个对象数组，调用对象数组中的每个对象的析构函数释放空间。</p></li><li><ul><li>如果没有方括号，那么这个指针就被默认为只指向一个对象，对象数组中的其他对象的析构函数就不会被调用，结果造成了内存泄露。</li></ul></li><li><ul><li>如果方括号大小&gt;对象数组大小，就会调用无效对象（内存溢出）的析构函数，会造成堆的奔溃。</li></ul></li><li><ul><li>如果方括号大小&lt;对象数组大小，就不能调用足够多的析构函数，结果会造成内存泄露。</li></ul></li><li><p>指向对象的指针数组：数组中存放的是指向对象的指针，不仅要释放每个对象的空间，还要释放每个指针的空间。delete []p只是释放了每个指针，但是并没有释放对象的空间，正确的做法，是通过一个循环，将每个对象释放了，然后再把指针释放了。</p></li></ul><ol start="5"><li>缺少拷贝构造函数<br>会导致两次析构函数释放相同的内存，同时可能会造成堆的奔溃。</li></ol><ul><li><ul><li>如果没有定义拷贝构造函数，那么编译器就会调用默认的拷贝构造函数，会逐个成员拷贝的方式来复制数据成员。</li></ul></li><li><ul><li>如果是以逐个成员拷贝的方式来复制指针（将一个变量的地址赋给另一个变量），这种隐式的指针复制结果就是两个对象拥有指向同一个内存空间的指针。</li></ul></li><li><ul><li>当释放第一个对象的时候，它的析构函数就会释放与该对象有关的内存空间。</li></ul></li><li><ul><li>而释放第二个对象的时候，它的析构函数会释放相同的内存，这样是错误的。</li></ul></li><li><ul><li>所以，如果一个类里面有指针成员变量，要么必须显示的写拷贝构造函数和重载赋值运算符，要么禁用拷贝构造函数和重载赋值运算符。注意：<strong>按值传递会调用（拷贝）构造函数，引用传递不会调用</strong>。</li></ul></li></ul><ol start="6"><li>缺少重载赋值运算符<br>这种问题跟上述问题类似，也是逐个成员拷贝的方式复制对象，如果这个类的大小是可变的，那么结果就是造成内存泄露，如下图:</li></ol><p><img data-src="/transblog/images/3373632189087.png"></p><ol start="7"><li><p>运算符重载返回值错误<br>a. 返回栈上对象的引用或者指针（也即返回局部对象的引用或者指针）。导致最后返回的是一个空引用或者空指针，因此变成野指针<br>b. 返回内部静态对象的引用。<br>c. 返回一个泄露内存的动态分配的对象。导致内存泄露，并且无法回收<br>解决这一类问题的办法是重载运算符函数的返回值不是类型的引用，二应该是类型的返回值，即不是 int&amp;而是int</p></li><li><p>没有将基类的析构函数定义为虚函数<br>当基类指针指向子类对象时，如果基类的析构函数不是virtual，那么子类的析构函数将不会被调用，子类的资源没有正确是释放，因此造成内存泄露</p></li><li><p>野指针：指向被释放的或者访问受限内存的指针。<br>造成野指针的原因：</p></li></ol><ul><li><ul><li>指针变量没有被初始化（如果值不定，可以初始化为NULL）</li></ul></li><li><ul><li>指针被free或者delete后，没有置为NULL, free和delete只是把指针所指向的内存给释放掉，并没有把指针本身干掉，此时指针指向的是“垃圾”内存。释放后的指针应该被置为NULL.</li></ul></li><li><ul><li>指针操作超越了变量的作用范围，比如返回指向栈内存的指针就是野指针。</li></ul></li></ul><h2 id="C-的类"><a href="#C-的类" class="headerlink" title="C++的类"></a>C++的类</h2><h3 id="this指针"><a href="#this指针" class="headerlink" title="this指针"></a>this指针</h3><p>成员函数默认第一个参数为T* const this。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">func</span><span class="params">(<span class="type">int</span> p)</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>func的原型在编译器看来应该是：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">func</span><span class="params">(A* <span class="type">const</span> <span class="keyword">this</span>,<span class="type">int</span>  p)</span></span>;</span><br></pre></td></tr></table></figure><ul><li><p>this作用域是在类内部，当在类的非静态成员函数中访问类的非静态成员的时候，编译器会自动将对象本身的地址作为一个隐含参数传递给函数。</p></li><li><p>this指针不会影响sizeof(对象)的结果。任何对类成员的直接访问都被看成this的隐式使用。</p></li><li><p>每个对象都拥有一个this指针，通过this指针来访问自己的地址，所以this是一个常量指针，我们不允许改变this中保存的地址</p></li><li><p>this只能在成员函数中使用。全局函数，静态函数不能使用this。（原因：静态函数不属于具体的对象）在成员函数里，你是可以知道this指针的位置的（可以通过&amp;this获得），也可以直接使用它。</p></li></ul><ol><li><p>this指针是什么时候创建的？<br><strong>this在成员函数的开始执行前构造，在成员的执行结束后清除。</strong></p></li><li><p>this指针存放在何处？堆、栈、全局变量，还是其他？<br>this指针会因编译器不同而又不同的放置位置。可能是栈，也可能是寄存器，甚至全局变量。</p></li></ol><h3 id="struct与class"><a href="#struct与class" class="headerlink" title="struct与class"></a>struct与class</h3><h4 id="C语言区别"><a href="#C语言区别" class="headerlink" title="C语言区别"></a>C语言区别</h4><p>C中的struct与C++中struct的区别</p><ol><li><p>成员<br>C中的struct将一系列数据放在一个结构体中统一管理，只有数据，没有函数。<br>C++中的struct允许同时又函数和数据出现，允许通过构造函数进行初始化等。</p></li><li><p>访问权限<br>C中的struct没有访问权限的设定，也就是说对外并没有隐藏数据的做法。<br>C++中的struct有访问权限的设定，private、protected、public三种。默认的访问权限是public。</p></li><li><p>是否可以继承<br>C中struct不存在继承的关系。<br>C++中struct可以继承class和struct，也可以被class和struct继承。<br><img data-src="/transblog/images/1304608150642.png"></p></li></ol><h4 id="C-区别"><a href="#C-区别" class="headerlink" title="C++区别"></a>C++区别</h4><p>C++中struct与class的区别</p><ol><li><p>在C++中，为了保持对于C的兼容，保留了struct，并在此基础上对struct进行了扩充。<br>C++中的struct可以包含成员函数，可以继承，可以实现多态，但是在C++中的struct还是与class有一些区别的：<br>首先明确，struct是一个数据结构的实现体；而class更像是一个对象的实现体。</p></li><li><p>默认的访问权限不同<br>struct默认的访问权限是public的，class默认的访问权限是private的。<br>但是我们在平时应该习惯在程序里明确指出某个数据&#x2F;成员的访问权限，而不是靠默认，这是个比较好的编程习惯。</p></li><li><p>默认的继承访问权限不同<br>对于默认的继承访问权限：<br>struct是public的，而class是private的。<br>并且要注意的是，C++中，struct可以继承struct也可以继承class，class也是一样，可以继承struct也可以继承class。因此默认的继承访问权限是取决于子类，而不是取决于基类。<br>也就是说：struct做子类的时候，默认是public的；class做子类的时候，默认是private的。与基类无关。</p></li><li><p>定义模板参数<br>class关键字可以用于定义模板参数，而struct不能。</p></li></ol><h3 id="static关键字"><a href="#static关键字" class="headerlink" title="static关键字"></a>static关键字</h3><h4 id="静态局部变量"><a href="#静态局部变量" class="headerlink" title="静态局部变量"></a>静态局部变量</h4><p><strong>用于函数体内部修饰变量，这种变量的生存期长于该函数</strong>。<br>对于一个完整的程序，在内存中的分布情况如下图：　 </p><ul><li>栈区： 由编译器自动分配释放，像局部变量，函数参数，都是在栈区。会随着作用于退出而释放空间。</li><li>全局数据区(静态区，一般指BSS段–bss和数据段–data)：全局变量和静态便令的存储是放在一块的，初始化的全局变量和静态变量在一块区域，未初始化的全局变量和未初始化的静态变量在相邻的另一块区域。程序结束释放。</li><li>堆区：程序员分配并释放的区域，像malloc(c),new(c++) </li><li>代码区<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">foo</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> i = <span class="number">1</span>; <span class="comment">// note:1</span></span><br><span class="line">    <span class="comment">//int i = 1;  // note:2</span></span><br><span class="line">    i += <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><ol><li>它存在的意义就是随着第一次函数的调用而初始化，却不随着函数的调用结束而销毁(如果把以上的note:1换成note:2,那么i就是在栈区分配了，会随着foo的调用结束而释放)。</li><li>它是在第一次调用进入note:1的时候初始化（当初面试被坑过，我居然说是一开始就初始化了）。且只初始化一次，也就是你第二次调用foo(),不会继续初始化，而会直接跳过。</li></ol><p>那么它跟定义一个全局变量有什么区别呢，同样是初始化一次，连续调用foo()的结果是一样的，<strong>但是，使用全局变量的话，变量就不属于函数本身了，不再仅受函数的控制，给程序的维护带来不便。静态局部变量正好可以解决这个问题。静态局部变量保存在全局数据区，而不是保存在栈中，每次的值保持到下一次调用，直到下次赋新值。</strong></p><p>那么我们总结一下，静态局部变量的特点（括号内为note:2,也就是局部变量的对比）：</p><ol><li>该变量在全局数据区分配内存(局部变量在栈区分配内存);</li><li>静态局部变量在程序执行到该对象的声明处时被首次初始化，即以后的函数调用不再进行初始化(局部变量每次函数调用都会被初始化);</li><li>静态局部变量一般在声明处初始化，如果没有显式初始化，会被程序自动初始化为0(局部变量不会被初始化);</li><li>它始终驻留在全局数据区，直到程序运行结束。但<strong>其作用域为局部作用域，也就是不能在函数体外面使用它</strong>(局部变量在栈区，在函数结束后立即释放内存);</li></ol><h4 id="静态全局变量"><a href="#静态全局变量" class="headerlink" title="静态全局变量"></a>静态全局变量</h4><p><strong>定义在函数体外，用于修饰全局变量，表示该变量只在本文件可见</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> i = <span class="number">1</span>;  <span class="comment">//note:3</span></span><br><span class="line"><span class="comment">//int i = 1;  //note:4</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">i += <span class="number">1</span>;</span><br><span class="line"><span class="keyword">return</span> i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假设我有一个文件a.cpp,我们再新建一个b.cpp,内容如下。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//file a.cpp</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//static int n = 15;  //note:5</span></span><br><span class="line"><span class="type">int</span> n = <span class="number">15</span>;  <span class="comment">//note:6</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//file b.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">extern</span> <span class="type">int</span> n;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fn</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">n++;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;after: %d\n&quot;</span>,n);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;before: %d\n&quot;</span>,n);</span><br><span class="line"><span class="built_in">fn</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>我们先使用note:6,也就是非静态全局变量，发现输出为:<br>before: 15<br>after: 16</p><p>也就是我们的b.c通过extern使用了a.c定义的全局变量。<br>那么我们改成使用note:5,也就是使用静态全局变量呢？会出现类似undeference to “n”的报错，它是找不到n的，因为<strong>static进行了文件隔离</strong>，你是没办法访问a.c定义的静态全局变量的，当然你用 #include “a.c”,那就不一样了。</p><p>以上我们就可以得出静态全局变量的特点：</p><ol><li>静态全局变量不能被其它文件所用(全局变量可以);</li><li>其它文件中可以定义相同名字的变量，不会发生冲突(自然了，因为static隔离了文件，其它文件使用相同的名字的变量，也跟它没关系了);</li></ol><h4 id="静态函数"><a href="#静态函数" class="headerlink" title="静态函数"></a>静态函数</h4><p><strong>准确的说，静态函数跟静态全局变量的作用类似</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//file a.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fn</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;this is non-static func in a&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="comment">//file b.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="type">void</span> <span class="title">fn</span><span class="params">()</span></span>;  <span class="comment">//我们用extern声明其他文件的fn(),供本文件使用。</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">fn</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>可以正常输出：this is non-static func in a。当给void fn()加上static的关键字之后呢？ undefined reference to “fn”.</p><p>所以，静态函数的好处跟静态全局变量的好处就类似了：</p><ol><li>静态函数不能被其它文件所用;</li><li>其它文件中可以定义相同名字的函数，不会发生冲突;</li></ol><p>上面一共说了三种用法，为什么说准确来说是两种呢？</p><ol><li>一种是修饰变量，一种是修饰函数，所以说是两种（这种解释不多）。</li><li>静态全局变量和修饰静态函数的作用是一样的，一般合并为一种。（这是比较多的分法）。</li></ol><h4 id="静态数据成员"><a href="#静态数据成员" class="headerlink" title="静态数据成员"></a>静态数据成员</h4><p>用于修饰 class 的数据成员，即所谓“静态成员”。这种数据成员的生存期大于 class 的对象。对于非静态数据成员，每个类对象(实例)都有自己的拷贝。而静态数据成员被当作是类的成员，由该类型的所有对象共享访问，对该类的多个对象来说，静态数据成员只分配一次内存。因此<strong>静态数据成员也叫做类变量，而普通数据成员也叫做实例变量</strong>。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"><span class="type">int</span> m_w,m_h;</span><br><span class="line"><span class="type">static</span> <span class="type">int</span> s_sum;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="built_in">Rectangle</span>(<span class="type">int</span> w,<span class="type">int</span> h)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">this</span>-&gt;m_w = w;</span><br><span class="line"><span class="keyword">this</span>-&gt;m_h = h;</span><br><span class="line">s_sum += (<span class="keyword">this</span>-&gt;m_w * <span class="keyword">this</span>-&gt;m_h);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">GetSum</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">cout&lt;&lt;<span class="string">&quot;sum = &quot;</span>&lt;&lt;s_sum&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> Rectangle::s_sum = <span class="number">0</span>;  <span class="comment">//初始化</span></span><br></pre></td></tr></table></figure><p>static 并不占用Rectangle的内存空间，static在全局数据区(静态区)分配内存的。静态数据成员定义时要分配空间，所以不能在类声明中初始化。而且static只会被初始化一次，于实例无关。也就是说，你每new一个Rectangle，并不会为static int s_sum的构建一份内存拷贝，它是不管你new了多少Rectangle的实例，因为它只与类Rectangle挂钩，而跟你每一个Rectangle的对象没关系。</p><p><strong>为什么？因为静态成员属于整个类，而不属于某个对象，如果在类内初始化，会导致每个对象都包含该静态成员，这是矛盾的</strong>。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;  </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// 静态成员不能在类内初始化</span></span><br><span class="line">&#125;;</span><br><span class="line">错误</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;  </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// 常量成员也不能在类内初始化</span></span><br><span class="line">&#125;;</span><br><span class="line">错误</span><br></pre></td></tr></table></figure><p>能在类中初始化的成员只有一种，那就是静态常量成员。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;  </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">const</span> <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// 静态常量成员可以在类内初始化</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="静态成员函数"><a href="#静态成员函数" class="headerlink" title="静态成员函数"></a>静态成员函数</h4><p><strong>用于修饰 class 的成员函数</strong>。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"><span class="type">int</span> m_w,m_h;</span><br><span class="line"><span class="type">static</span> <span class="type">int</span> s_sum;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="built_in">Rectangle</span>(<span class="type">int</span> w,<span class="type">int</span> h)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">this</span>-&gt;m_w = w;</span><br><span class="line"><span class="keyword">this</span>-&gt;m_h = h;</span><br><span class="line">s_sum += (<span class="keyword">this</span>-&gt;m_w * <span class="keyword">this</span>-&gt;m_h);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">GetSum</span><span class="params">()</span>  <span class="comment">//这里加上static</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">cout&lt;&lt;<span class="string">&quot;sum = &quot;</span>&lt;&lt;s_sum&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> Rectangle::s_sum = <span class="number">0</span>;  <span class="comment">//初始化</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>上面注释可见:对GetSum()加上static，使它变成一个静态成员函数，可以用类名::函数名进行访问。</p><ol><li>静态成员之间可以相互访问，包括静态成员函数访问静态数据成员和访问静态成员函数;</li><li>非静态成员函数可以任意地访问静态成员函数和静态数据成员;</li><li>静态成员函数不能访问非静态成员函数和非静态数据成员;</li><li>调用静态成员函数，可以用成员访问操作符(.)和(-&gt;)为一个类的对象或指向类对象的指针调用静态成员函数,也可以用类名::函数名调用(因为他本来就是属于类的，用类名调用很正常)</li></ol><h4 id="数据成员初始化位置"><a href="#数据成员初始化位置" class="headerlink" title="数据成员初始化位置"></a>数据成员初始化位置</h4><ol><li>静态常量数据成员可以在类内初始化(即类内声明的同时初始化)，也可以在类外，即类的实现文件中初始化，不能在构造函数中初始化，也不能在构造函数的初始化列表中初始化；</li><li>静态非常量数据成员只能在类外，即类的实现文件中初始化，也不能在构造函数中初始化，不能在构造函数的初始化列表中初始化；</li><li>非静态的常量数据成员不能在类内初始化，也不能在构造函数中初始化，而只能且必须在构造函数的初始化列表中初始化；</li><li>非静态的非常量数据成员不能在类内初始化，可以在构造函数中初始化，也可以在构造函数的初始化列表中初始化；</li></ol><p><img data-src="/transblog/images/2939959150567.png"></p><p><strong>为什么静态成员函数只能访问静态成员变量</strong></p><ol><li>静态成员函数只属于类本身，随着类的加载而存在，不属于任何对象，是独立存在的。</li><li>静态成员函数产生在前，非静态成员函数产生在后。类的静态成员在类加载的时候就已经分配内存，而此时类的非静态成员尚未分配内存，当且仅当实例化对象之后才存在，访问内存中不存在的东西自然会出错。</li><li>内部访问静态成员用self：：，而访问非静态成员要用this指针，静态成员函数没有this指针，故不能访问。</li></ol><h3 id="虚函数"><a href="#虚函数" class="headerlink" title="虚函数"></a><span id="target1">虚函数</span></h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">display</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Display from Base&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123; <span class="comment">// 重写虚函数</span></span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Display from Derived&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><img data-src="/transblog/images/435355510268592.png"></p><ol><li>编译器检测到虚函数(virtual)时，会将虚函数的地址放到虚表(vftable)中，并且在类中增加一个指针：vfpter。</li><li>在<strong>对象初始化调用构造函数</strong>时，编译器会在编写的每一个构造函数中，增加vfpter初始化的代码，<strong>使它指向本对象的虚函数表</strong>。</li><li>子类继承基类的vfpter指针，这个指针指向基类虚函数表，<strong>当子类调用构造函数时，子类的vfpter指针便会指向子类的虚函数表</strong>。</li><li>当实例化子类时，检测到有虚函数的重写，编译器会用子类重写的虚函数地址覆盖掉之前父类的虚函数地址</li><li>当调用虚函数时，检测到是虚函数就会从虚表中找对应的位置调用，若子类没有重写，虚表中的虚函数地址就还是父类的，若子类中有重写，虚表记录的就是子类重写的虚函数地址，即实现了父类的指针调用子类的函数。（<strong>在多态调用的时候，根据vfpter指针，找到虚函数表来实现动态绑定</strong>）</li><li>虚表中先记录父类中的虚函数地址，接着记录子类中虚函数地址(若子类重写父类的虚函数则是覆盖)</li><li>最后虚表还有一个尾值是 0</li></ol><p><a href="#target2">点击此处跳转到类的内存布局</a>需要注意的是：</p><ul><li><strong>父类指针可以指向子类对象，是安全的</strong>，开发中经常用到（继承方式必须是public）。</li><li><strong>子类指针不可以指向父类对象，是不安全的</strong>。</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> m_age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="keyword">public</span> Person</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> m_score;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="comment">//Student* p = new Person(); // 报错，子类指针不可以指向父类对象</span></span><br><span class="line"></span><br><span class="line">Student* p = (Student*) <span class="keyword">new</span> <span class="built_in">Person</span>(); <span class="comment">// 强制类型转换</span></span><br><span class="line">p-&gt;m_age = <span class="number">10</span>;</span><br><span class="line">p-&gt;m_score = <span class="number">100</span>; <span class="comment">// 不报错，但不安全</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>普通函数与虚函数的区别</strong></p><ol><li>虚函数通过虚函数指针间接引用，普通函数可以使用函数名直接调用</li><li>虚函数调用流程复杂，效率低，普通函数效率高</li><li>虚函数的目的是为了实现多态</li><li>使用了虚函数，会增加访问内存开销，降低效率。</li></ol><h4 id="override关键字"><a href="#override关键字" class="headerlink" title="override关键字"></a>override关键字</h4><p>C++11 增加了 override 关键字，确保在派生类中声明的重载函数跟基类的虚函数有相同的声明。保证子类重写的虚函数与父类的虚函数有相同的签名（函数的签名包括：函数名、参数列表、const属性）。</p><p>也就是说，加了 override，明确表示子类的这个虚函数是重写父类的，如果子类与父类虚函数的签名不一致，编译器就会报错。</p><p>因此，为了减少程序运行时的错误，重写的虚函数都建议加上 override。</p><h4 id="final关键字"><a href="#final关键字" class="headerlink" title="final关键字"></a>final关键字</h4><p>C++11 增加了 final 关键字，阻止类的进一步派生和虚函数的进一步重写。</p><p>如果不希望某个类被继承或者不希望某个虚函数被重写，则可以在类名和虚函数后加上 final 关键字，加上 final 关键字后，若再被继承或重写，编译器就会报错。</p><h4 id="重载和覆盖"><a href="#重载和覆盖" class="headerlink" title="重载和覆盖"></a>重载和覆盖</h4><ul><li><p>重载要求函数名相同，但是参数列表必须不同，返回值可以相同也可以不同。覆盖要求函数名、参数列表、返回值必须相同。</p></li><li><p>在类中重载是同一个类中不同成员函数之间的关系。在类中覆盖则是子类和基类之间不同成员函数之间的关系</p></li><li><p>重载函数的调用是根据参数列表来决定调用哪一个函数。覆盖函数的调用是根据对象类型的不同决定调用哪一个</p></li><li><p>在类中对成员函数重载是不能够实现多态。在子类中对基类虚函数的覆盖可以实现多态实例</p></li></ul><h4 id="纯虚函数"><a href="#纯虚函数" class="headerlink" title="纯虚函数"></a>纯虚函数</h4><p>纯虚函数是在基类中声明但不实现的虚函数，其声明方式是在函数声明的结尾处添加 &#x3D; 0。类中如果包含至少一个纯虚函数，则该类成为抽象类，不能实例化对象。</p><ol><li>虚函数和纯虚函数可以定义在同一个类中，含有纯虚函数的类被称为抽象类，而只含有虚函数的类不能被称为抽象类。</li><li>虚函数可以被直接使用，也可以被子类重载以后以多态的形式调用，<strong>而纯虚函数必须在子类中实现该函数才可以使用，因为纯虚函数在基类只有声明而没有定义。</strong></li><li><strong>在虚函数和纯虚函数的定义中不能有static标识符，原因很简单，被static修饰的函数在编译时候要求前期bind，然而虚函数却是动态绑定，而且被两者修饰的函数生命周期也不一样</strong>。</li><li>虚函数必须实现，如果不实现，编译器将报错</li><li>对于虚函数来说，父类和子类都有各自的版本。由多态方式调用的时候动态绑定。</li></ol><table><thead><tr><th align="center">特性</th><th align="center">虚函数</th><th align="center">纯虚函数</th></tr></thead><tbody><tr><td align="center">定义</td><td align="center">virtual void func();</td><td align="center">virtual void func() &#x3D; 0;</td></tr><tr><td align="center">实现</td><td align="center">可以在基类中实现</td><td align="center">不在基类中实现，派生类必须实现</td></tr><tr><td align="center">类类型</td><td align="center">基类可以实例化</td><td align="center">基类是抽象类，不能实例化</td></tr><tr><td align="center">强制性</td><td align="center">派生类可以选择实现或使用基类实现</td><td align="center">派生类必须实现，或者继续保持抽象</td></tr><tr><td align="center">典型用途</td><td align="center">提供可选的默认行为</td><td align="center">定义接口，强制派生类实现特定行为</td></tr></tbody></table><p><strong>抽象类为什么不可以实例化？</strong><br>如果你想要定义一个类对象，那么这个类必须是一个具体的类，也就是说，它必须实现了所有的成员函数，包括虚函数。只有这样，才能在程序中创建该类的对象。纯虚函数没有函数体，不是完整的函数，无法调用，也无法为其分配内存空间。</p><h4 id="虚析构"><a href="#虚析构" class="headerlink" title="虚析构"></a>虚析构</h4><p><strong>如果释放父类指针(指向子类的父类指针)，只会调用父类的析构函数，所以要将父类的析构函数声明为虚函数，就会先调用子类的析构函数再调用父类的析构函数</strong>（子类中存放的是自己的析构函数）<br>父类的析构函数加了 virtual 修饰，delete 会调用子类和父类的析构函数，子类可以显式的加 virtual ，也可以不加， 默认是有的 virtual，</p><p><strong>虚析构的原理</strong><br>用对象指针来调用一个函数，有以下两种情况：</p><ol><li>如果是虚函数，会调用子类中的版本。（在有子类的情况下）</li><li>如果是非虚函数，会调用指针所指类型的实现版本。</li></ol><p>如果我们删除一个指向派生类对象的基类指针，而基类析构函数又是非虚的话， 就会只调用基类的析构函数(上面第2种情况)，子类的析构函数得不到调用。</p><p><strong>虚析构为什么不是默认的？</strong><br>当类里面有定义虚函数的时候,编译器会给类添加一个虚函数表,里面来存放虚函数指针,这样就会增加类的存储空间，还有可能降低其可移植性。</p><p><strong>为什么构造函数不可以被声明为虚函数?</strong></p><ol><li>因为创建一个对象时要确定对象的类型，而虚函数是在运行时确定其类型的，而在构造一个对象时，由于对象还未创建成功，编译器无法知道对象的实际类型。</li><li>如果构造函数是虚的，就需要通过虚表来调用，可是对象还没有构造，也就是内存空间还没有，无法找到虚表。所以构造函数不能是虚函数。</li></ol><h3 id="类的继承"><a href="#类的继承" class="headerlink" title="类的继承"></a>类的继承</h3><h4 id="无继承"><a href="#无继承" class="headerlink" title="无继承"></a>无继承</h4><p>类A定义如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"><span class="type">int</span> A_1;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">static</span> <span class="type">int</span> A_2;</span><br><span class="line"><span class="type">int</span> A_3;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> A::A_2 = <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p>这里给A定义了一个私有变量，一个静态变量、一个公有变量以及3个虚函数，现在来看看A的内存布局：<br><span id="target2"></span></p><p><img data-src="/transblog/images/391070916250468.png"></p><p>从A的内存布局中可以知道：在无继承的情况下，对象内存布局中会先放置虚表指针vfptr，然后再按声明顺序存放数据成员。并且可以知道：虚表vftable中按声明先后顺序存放了A中定义的3个虚函数的地址，此外，类中定义的静态变量A_2并不存在于类中，并且类成员变量的访问权限也不会影响到成员的内存布局（比如这里的私有变量A_1和公有变量A_3是放在一起的）。</p><h4 id="一般继承"><a href="#一般继承" class="headerlink" title="一般继承"></a>一般继承</h4><h5 id="单继承"><a href="#单继承" class="headerlink" title="单继承"></a>单继承</h5><p>现在定义一个A的继承类B，类B定义如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> :<span class="keyword">public</span> A</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> B_1;</span><br><span class="line">    <span class="type">int</span> B_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//重写了A中定义的一个虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//定义了一个新的虚函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在类B中，重写了父类A的虚函数Vfun1，并且定义了一个新的虚函数Vfun4，现在来看看B的内存布局：<br>B的内存布局如下：</p><p><img data-src="/transblog/images/18351216248072.png"></p><p>从这里可以看到，在类B中的前面，存放了类A的整个布局，即类A中的所有非静态数据成员都原封不动地放在了类B的前面，然后才是类B自己的数据成员B_1和B_2。不过值得注意的是，在类B的最前面，存放的依然是一个虚表指针vfptr，再来看vfptr所指的虚表中，由于类B重写了虚函数Vfun1，因此B中定义的虚函数的地址&amp;B::Vfun1覆盖了之前的&amp;A::Vfun1，并且在虚表最后还加上了类B中新定义的虚函数地址&amp;B::Vfun4。</p><p>因此我们可以得出以下结论：<strong>在子类的内存布局的最前面存放的是父类的内存布局</strong>，一开始依然是存放一个虚表指针，只不过这个虚表指针与父类的虚表指针是不一样的，这个虚表指针所指向的虚函数表中，会包含所有父类中的虚函数（不一定是父类所定义的，也可能是父类从爷爷类继承下来的虚函数）的地址以及子类中新定义的虚函数的地址，如果子类对虚函数进行了重写，那么重写后的虚函数地址将会覆盖相应的父类虚函数地址。这也说明了<strong>如果父类和子类中有不同的虚函数，最终在子类中也只有一个虚表指针，对应一个虚函数表</strong>。</p><p>那么要是多层继承也是这样吗？</p><h5 id="多层继承"><a href="#多层继承" class="headerlink" title="多层继承"></a>多层继承</h5><p>现在来定义一个类C，用于继承类B，类C的定义如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">C</span> :<span class="keyword">public</span> B</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> C_1;</span><br><span class="line"><span class="type">int</span> C_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写了B中定义的虚函数（这个函数在B中也是重写的A定义的虚函数）</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写了A中定义的虚函数（这个函数在B中未被重写）</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写了B中新定义的虚函数（这个函数未在A中定义）</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun5</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//定义了一个新的虚函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>现在先不看C的内存布局结果，试着来推测一下：</p><p>首先，如前所述：子类的内存布局最前面存放的是父类的内存布局，因此在类C的内存布局中，最开始应当存放一个类B的布局，相当于直接把上面B的内存布局搬到C中，最前面依然是一个虚表指针vfptr，只不过这个虚表指针与原先B中的虚表指针完全不一样。接着在B的布局之后，则应当是按照声明顺序存放的C中的成员变量，如下所示：</p><p><img data-src="/transblog/images/487141316245574.png"></p><p>再来看虚表中的内容：根据前面所分析的，如果C中没有进行任何的虚函数的声明定义，那么C中虚表指针所指虚表中应当与B中虚表指针所指虚表的内容一样了。而现在类C重写了Vfun1，Vfun2和Vfun4，并且新定义了一个虚函数Vfun5，那么虚表中的内容应当如下所示：（黑色表示未发生重写的父类虚函数，蓝色表示重写的虚函数，紫色表示新定义的虚函数）</p><p><img data-src="/transblog/images/54181416226815.png"></p><p>有了上述的分析，再来看下类C实际的内存布局：</p><p><img data-src="/transblog/images/198691416249255.png"></p><p>由此可见，完全符合前面的分析结果。</p><p>为了考虑更多的情况，下面再来分析一下多重继承的情况。</p><h5 id="多重继承"><a href="#多重继承" class="headerlink" title="多重继承"></a>多重继承</h5><p>多重继承的是指一个子类含多个不同的父类。为了验证这种情况，将B与A的继承关系取消，让C同时继承自A和B，如下所示：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"><span class="type">int</span> A_1;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">static</span> <span class="type">int</span> A_2;</span><br><span class="line"><span class="type">int</span> A_3;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> A::A_2 = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> B_1;</span><br><span class="line"><span class="type">int</span> B_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span> :<span class="keyword">public</span> A,<span class="keyword">public</span> B</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> C_1;</span><br><span class="line"><span class="type">int</span> C_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//A中和B中均含有Vfun1虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//重写A中的虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//重写B中的虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun5</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//新定义虚函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这时候的C同时继承A和B，并且重写了一个A和B中均定义了的虚函数，为了考虑所有因素，现在来分别看看类C先继承A再继承B和先继承B再继承A的各自内存布局是怎样的？</p><p><img data-src="/transblog/images/205501616244391.png">   <img data-src="/transblog/images/276331616237937.png"></p><p>第一幅图为类C先继承A再继承B，第二幅图为先继承B再继承A，分析两种情况可以发现，C的内存布局中，先按照继承顺序包含父类的布局，然后才是C中定义的数据成员。不过尤其需要注意的是，C的内存布局中所包含的每一个父类的布局最开始都各自含有一个虚表指针vfptr，也就是说，<strong>有几个父类就会有几个虚表指针，自然也就有几个虚表了</strong>。这里的C类继承自两个类，因此就有两个虚表指针，也就对应两个虚表，这里设定为vftA和vftB，现在来看看两个虚表中的内容。</p><p>当先继承A再继承B的时候，虚函数的覆盖情况如下：</p><p><img data-src="/transblog/images/463331616231071.png"></p><p>这里有个需要注意的地方，C中重写的Vfun1在A和B中都有定义，此时在vftA中有Vfun1的地址，而在vftB中显示的是this-&#x3D;12,goto C::Vfun1，这是什么意思呢？注意到vftB的偏移量本身就是12，而vftA的偏移量为0，因此在C的内存布局中vftB虚表指针的地址 &#x3D; vftA虚表指针的地址+12，而这里有一个this-&#x3D;12，相当于访问vftB的时候又回到了vftA的位置，此时访问的函数还是C::Vfun1。可以写一段程序测试一下：</p><p><img data-src="/transblog/images/120981716221601.png"></p><p>这也就表示vftA所指虚表中第一个虚函数的地址与vftB所指虚表中第一个虚函数的地址是完全相同的，都是指向Vfun1函数。</p><p>另外一个需要注意的地方是：C中新定义了一个Vfun5，而该虚函数的地址，是存放在vftA中的。</p><p>当先继承B然后继承A时，情况和上述类似，这里就不多说了。</p><p>由此可以得出结论：当一个子类C继承于多个相互独立的父类的时候，会先按照继承的先后顺序存放每个父类的内存布局，然后才是子类定义的数据成员。而在子类下的每个父类对应的布局中都有一个虚表指针各自对应一个虚函数表，各个虚函数表的构成只需关注该父类中的虚函数是否在子类中重写。<strong>如果子类中新定义了一个虚函数，那么这个虚函数的地址会放在子类布局下先声明继承的那个父类对应的虚表中</strong>；如果子类重写了多个父类定义的同名虚函数（如这里的Vfun1），那么在子类布局下每个父类对应的虚表中都会有被重写的虚函数的地址。</p><p>那么，要是子类所继承的多个父类之间也有关系呢？下面就拿菱形继承为例进行分析。</p><h5 id="菱形继承"><a href="#菱形继承" class="headerlink" title="菱形继承"></a>菱形继承</h5><p>  菱形继承的含义是在设定的多层继承基础上，类A和类B又同时继承于同一个父类。因此这里先定义一个类D，然后让类A和类B都继承它，这里给类D添加了如下所示：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">D</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> D_1;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun5</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun6</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>:<span class="keyword">public</span> D</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"><span class="type">int</span> A_1;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">static</span> <span class="type">int</span> A_2;</span><br><span class="line"><span class="type">int</span> A_3;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写D中的Vfun1</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写D中的Vfun2</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//新定义的Vfun3</span></span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> A::A_2 = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> :<span class="keyword">public</span> D</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> B_1;</span><br><span class="line"><span class="type">int</span> B_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写D中的Vfun1</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写D中的Vfun4</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span> :<span class="keyword">public</span> A,<span class="keyword">public</span> B</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> C_1;</span><br><span class="line"><span class="type">int</span> C_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写Vfun1，该虚函数在父类A和父类B以及类D中都有定义</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写类D及类A中均有定义的虚函数Vfun2</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写类D及类B中均有定义的虚函数Vfun2</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun5</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//重写类D中定义，但类B和类C中未定义的Vfun5</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun7</span><span class="params">()</span> </span>&#123;&#125;;   <span class="comment">//定义的新虚函数Vfun7</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>查看C的内存布局如下所示：<br><span id="target3"></span></p><p><img data-src="/transblog/images/210981816224105.png"><img data-src="/transblog/images/268671816233052.png"></p><p>整个过程的虚函数地址覆盖如图所示：（蓝色线表示有重写，黑色线表示无重写，红色字体表示为新定义虚函数）</p><p><img data-src="/transblog/images/405231816225937.png"></p><p>虽然举的例子复杂了点，但是从整个过程中也清楚了虚表内容的覆盖过程的：当子类继承自多个类时，每一个父类在子类中都有一个虚表指针，举个例子，类C继承自类A和类B，那么在类C中，会有一个虚表指针vfptrA和虚表指针vfptrB，vfptrA所指向的虚表内容，实际上就只用关注类C和类A之间的虚函数重写关系，这与类B没有任何关系，可以参考2.1；同样的，vfptrB所指向的虚表内容，就只用关注类C和类B之间的虚函数重写关系，和类A也没有任何关系。</p><p>简单来说，<strong>多重继承下的子类，每个父类在该子类中都对应一个虚函数表，而每个虚函数表的内容只用关注该子类与对应父类之间虚函数关系，与其他父类无关</strong>，如果子类重写的虚函数在多个父类中存在定义，那么每个父类与子类对应的虚表中都会存在这个虚函数的地址(实际上只有第一个声明继承的父类与子类对应的虚表中的才是直接存储的该虚函数的地址，<strong>其他虚表都是间接寻址第一个虚表相应位置来得到的该虚函数地址</strong>)。即使是菱形继承，子类中的虚表也只关心父类中的虚函数与自身虚函数的关系，而并不关心父类的父类。</p><p>除此之外，虚表中虚函数地址的顺序，是按照每个虚函数第一次定义和声明的先后顺序。如果子类中含有新定义的虚函数，那么该虚函数的地址只会存在于第一个虚表中的末尾。</p><h4 id="虚继承"><a href="#虚继承" class="headerlink" title="虚继承"></a>虚继承</h4><p>虚继承‌是C++中一种特殊的继承方式，主要用于解决多重继承中的二义性问题。虚继承通过在继承声明中添加virtual关键字来实现，确保派生类中只有一个间接父类的实例</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> 派生类名 : <span class="keyword">virtual</span> 权限控制符 基类名 &#123;</span><br><span class="line">    派生类成员;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>有了前面对一般继承的分析，实际上虚继承也很容易了。不过最需要注意的，还是虚继承与一般继承相比，最大的不同点。我们知道，虚继承最常用的场景，就是在菱形继承下防止子类访问父类成员出现二义性的情况，比如按照上述菱形继承所用例子：<a href="#target3">点击此处跳转到一般继承–菱形继承</a>，现在要在C的实例中访问D_1成员，此时就会出现二义性。如下所示：</p><p><img data-src="/transblog/images/184131916226546.png"></p><p>C的内存布局中由于A和B都继承D，因此存在两个D_1，此时C的实例去访问D_1，到底访问哪一个呢？这样就出现了二义性。虚继承就可以解决这个问题。</p><p>那么是如何解决的呢？我们先从虚继承下子类的内存布局说起。</p><p>用前面单继承的例子，将B继承A改为虚继承，如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line"><span class="type">int</span> A_1;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">static</span> <span class="type">int</span> A_2;</span><br><span class="line"><span class="type">int</span> A_3;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> A::A_2 = <span class="number">5</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> :<span class="keyword">virtual</span> <span class="keyword">public</span> A</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="type">int</span> B_1;</span><br><span class="line"><span class="type">int</span> B_2;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//重写了A中定义的一个虚函数</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;  <span class="comment">//定义了一个新的虚函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>B的内存布局如下所示：<br><img data-src="/transblog/images/429231916253501.png"><img data-src="/transblog/images/484831916235714.png"></p><p>可以看到，和一般继承不同的是，虚继承并不是将父类的布局放在子类布局的开头了，而是将其放在了子类数据成员的后面。在一般继承中，只有一个虚表指针放在布局最前面，而<strong>虚继承下出现了两个虚表指针vfptr，分别位于子类布局最前面以及子类中父类布局的最前面</strong>。这里还涉及到一个vbptr，现在先不管它。</p><p>因此虚继承中子类和父类的布局关系与一般继承中的区别如下所示：(先无视vbptr)<br><img data-src="/transblog/images/55402016225012.png"><br>再来看各自虚函数表的内容。类A中定义了3个虚函数Vfun1、Vfun2和Vfun3，类B中重写了Vfun1并且定义了一个新的虚函数Vfun4。而在类B所对应的虚函数表中，只有一个&amp;B::Vfun4，在类A对应的虚表中，含有被类B重写的&amp;B::Vfun1，以及类A定义的&amp;A::Vfun2和&amp;A::Vfun3。这里尤其需要注意的是，<strong>B重写的虚函数Vfun1的地址是放在父类A对应的虚表中的</strong>。</p><p>也就是说，<strong>在虚继承的情况下，每一个虚函数表中只会包含在对应类下第一次定义的虚函数的地址，即使该虚函数在子类中被重写，它的地址仍然是放在第一次定义该虚函数的父类中，并且被重写后的虚函数地址所覆盖</strong>。如这里的Vfun1，在类A中被第一次定义，在类B中被重写，因此&amp;A::Vfun1被覆盖为&amp;B::Vfun1，并且存在于类A布局中的虚表指针所对应的虚函数表中，而不是类B对应的虚函数表中。而相应的，在子类对应的虚函数表中，只包含在子类中第一次定义的虚函数的地址，如果子类中没有定义新的虚函数，那么子类中的虚表就为空，自然也就不存在虚表指针了，如把上面B类下定义的Vfun4删去，得到的B内存布局如下所示：</p><p><img data-src="/transblog/images/118462416221263.png"></p><p>此时由于子类中没有新定义的虚函数，因此子类所属的内存布局中没有虚表指针。</p><p>实际上，虚继承的类内存布局与一般继承的类内存布局还是大体类似的，每一个父类在子类中依然都各自对应一个虚函数表，不同的是，<strong>虚继承相当于在子类的内存布局中把虚基类的布局独立了出去，在一般继承中，子类新定义的虚函数是放在第一个声明继承的父类所对应的虚函数表的末尾，而在虚继承中，则是单独用一个属于子类的虚函数表去存放所有子类新定义的虚函数的地址</strong>。</p><p>现在再来回头看，这里还多了一个“虚基类表指针”vbptr，它指向一个虚基类表，并且可以看到虚基类表vbtable中有两个数据，一个是-4，一个是12，-4描述的是虚基类表指针vbptr自身相对于整个类首地址的偏移地址，12描述的是虚基类A的首地址相对于vbptr的偏移地址。</p><p>而这两个数据刚好就是两个虚表指针vfptr相对于虚基类表指针vbptr的偏移，也可以说，虚基类表中存放的数据是子类中子类的首地址和各个虚基类的首地址相对于虚基类表指针的地址偏移量。下面举个例子来看一看：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> a;</span><br><span class="line"><span class="type">int</span> b;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> c;</span><br><span class="line"><span class="type">int</span> d;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> e;</span><br><span class="line"><span class="type">int</span> f;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">D</span>:<span class="keyword">virtual</span> <span class="keyword">public</span> B,<span class="keyword">virtual</span> <span class="keyword">public</span> A,<span class="keyword">public</span> C</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> g;</span><br><span class="line"><span class="type">int</span> h;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这里先不去管虚函数了，只关注虚基类在子类中的布局，D的内存布局如下：</p><p><img data-src="/transblog/images/594032416221402.png"></p><p>可以看到， 子类D先后虚继承类B和类A，再一般继承类C，在子类D的内存布局中，由于B和A属于虚继承，因此存在虚基类表指针，D中虚基类表指针地址偏移量为12，虚基类B和虚基类A各自布局下的虚函数表指针偏移量分别为24和36，由于D是一般继承继承类C，因此类C布局下的虚函数表指针放在最开头，偏移量为0。</p><p>再来看虚基类表vbtable中，有三个元素，第一个元素为-12，刚好是C布局下的虚函数表指针(图中红色指向的vfptr)相对于虚基类表指针vbptr的偏移量(0-12 &#x3D; -12)；第二个元素为12，刚好也是B布局下的虚函数表指针(图中黄色指向的vfptr)相对于虚基类表指针vbptr的偏移量(24-12 &#x3D; 12)；第三个元素为24，刚好也是A布局下的虚函数表指针(图中蓝色指向的vfptr)相对于虚基类表指针vbptr的偏移量(36-12 &#x3D; 24)；</p><p>由此可以看出，不管是否为虚继承还是一般继承，如果发生多层继承，子类继承自多少个父类，子类内存布局中就会存在多少个虚表指针，每个虚表指针对应一个虚表；<strong>如果多层继承中某一个继承为虚继承，那么在子类的内存布局就还会存在一个虚基类表指针，对应一个虚基类表，在虚基类表中，第一项放的是整个类首地址相对于虚基类表指针的偏移地址，从第二项开始存放的是所有虚基类首地址相对于虚基类表指针的偏移地址，虚基类表中不会存放非虚基类的偏移地址</strong>。而虚基类指针的位置，则是放在属于子类的那一段布局的最前面（比如说这里由于一般继承C，因为会先放C的布局，C的布局之后，才是属于子类D的布局，当然如果没有一般继承全部为虚继承，那么虚基类指针就直接放在第一个虚表指针的后面）。</p><h5 id="多层继承-1"><a href="#多层继承-1" class="headerlink" title="多层继承"></a>多层继承</h5><p>通过分析虚继承的单继承以及虚继承的多重继承知道了虚继承的内存布局，现在再来看看虚继承的多层继承。</p><p>现有如下定义：类B虚继承类A，类C继承类B，如下所示：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> a;</span><br><span class="line"><span class="type">int</span> b;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>:<span class="keyword">virtual</span> <span class="keyword">public</span> A</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> c;</span><br><span class="line"><span class="type">int</span> d;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span>:<span class="keyword">public</span> B</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> e;</span><br><span class="line"><span class="type">int</span> f;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;    <span class="comment">//重写B中重写的Vfun1</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;    <span class="comment">//重写B中定义的Vfun3</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;    <span class="comment">//重写A中定义的但是B中未重写的Vfun2</span></span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;    <span class="comment">//定义一个新的虚函数Vfun4</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>再看类C的布局：</p><p><img data-src="/transblog/images/225264319250168.png"><img data-src="/transblog/images/312254319268594.png"><br>从该布局中可以看到，类A一旦被类B虚继承，那么即使类B被类C一般继承，在类C中类A的布局也是独立的，类A的布局中也拥有自己的虚表指针对应一个虚函数表。与前面的分析相同，在类A的虚函数表中，即使类C中重写了Vfun1，重写后的Vfun1函数地址依然存在与类A对应的虚函数表中。</p><p>那么如果这里类C也是虚继承类B呢？来看看结果：<br><img data-src="/transblog/images/537002516233492.png"><img data-src="/transblog/images/4332616238531.png"></p><p>可以看到，在类C的内存布局中，类A和类B都成虚基类，并且各自都有一个虚基类表指针，其中类B对应的虚基类表描述的是类B自己的的虚函数表指针和类A的虚函数表指针偏移地址（B也是虚继承自A），而类C对应的虚基类表描述的则是类C自己的虚函数表指针和类B的虚函数表指针偏移地址（C虚继承自B）。因此可以知道，每发生一次虚继承，那么虚继承的子类中就会存在一个虚基类表指针，所对应的虚基类表中存放的则是该子类和其所继承的虚基类的虚函数表指针的偏移量。</p><p>那么如果两个类同样虚继承自同一个父类呢？在子类中会如何存放这一个父类呢？这就来分析一下虚继承下的菱形继承：</p><h5 id="菱形继承-1"><a href="#菱形继承-1" class="headerlink" title="菱形继承"></a>菱形继承</h5><p>在最开始就说了，虚继承最常用于解决菱形继承下的“二义性”，那么现在定义如下：B虚继承自A，C也虚继承自A，D同时继承于B和C。定义如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> a;</span><br><span class="line"><span class="type">int</span> b;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun2</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>:<span class="keyword">virtual</span> <span class="keyword">public</span> A</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> c;</span><br><span class="line"><span class="type">int</span> d;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun3</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span>:<span class="keyword">virtual</span> <span class="keyword">public</span> A</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> e;</span><br><span class="line"><span class="type">int</span> f;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun4</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">D</span>:<span class="keyword">public</span> B,<span class="keyword">public</span> C</span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> g;</span><br><span class="line"><span class="type">int</span> h;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun1</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">Vfun5</span><span class="params">()</span> </span>&#123;&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p> 再来看类D的内存布局：<br><img data-src="/transblog/images/213150716247610.png"><img data-src="/transblog/images/291720716240279.png"></p><p> 可以看到，虽然D的父类B和C都虚继承自类A，但是在类B中，只存在一个类A的布局，这样，当类D的对象访问成员a时，由于在D的内存布局中只存在一个a，这样也就不会出现二义性了。</p><p> 再来看虚函数表，由于这里继承了两个父类，并且还有一个虚基类，因此在类D中会有3个虚函数表，分别对应于类B，类C和类A，由于类B虚继承自类A，因此类B的虚函数表中只含有类B中新定义的虚函数Vfun3的地址，又因为类B是第一个声明继承的，因此类B的虚函数表中还有类D新定义的虚函数Vfun5的地址；同样的，对于类C来说，由于类C虚继承自类A，因此类C对应的虚函数表中只包含新定义的虚函数Vfun4的地址；再看类A，由于类A是虚基类，因此它对应的虚函数表中就包含所有在类A中定义的虚函数的地址，也就是虚函数Vfun1和Vfun2的地址。</p><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>对于单继承来说：</p><ul><li>如果是一般继承，那么子类内存布局的最前面会先放父类的内存布局，然后才是子类自身的数据成员。并且在该父类的内存布局的最前面会有一个虚函数表指针，其所对应的虚函数表中，会先继承父类自身虚函数表的所有内容，如果子类对父类的虚函数进行了重写，那么就会对相应的虚函数地址进行覆盖，如果子类有新定义的虚函数，那么还会将新定义的虚函数地址追加在虚函数表的后面；</li><li>如果是虚继承，那么子类内存布局中则会把父类的内存布局单独放在最后，在该父类的内存布局中，最前面会有一个虚函数表指针，其所对应的虚函数表会继承父类的虚函数表所有内容，如果子类对父类中的虚函数进行了重写，那么也会将继承下来的虚函数表中的对应函数进行覆盖。如果子类中新定义了虚函数，那么在子类布局的最前面，就会有一个虚函数表指针，其对应一个新的虚函数表，表中存放的是所有子类中新定义的虚函数地址，如果子类中没有定义虚函数，那么就不存在子类的虚函数表指针了。然后会放一个虚基类表指针，该指针所指向的虚基类表，反映的是该子类以及父类(虚基类)的虚函数表指针相对于虚基类表指针的偏移位置，由此可以知道，通过虚基类表指针，可以找到虚继承中子类和父类各自的虚函数表。</li></ul><p>对于多层继承来说：</p><ul><li>实际上也可以理解为子类和父类的单继承，如果是一般继承，那么实际上就是先看父类到父类的父类之间的单继承，然后再看子类到父类之间的单继承</li><li>但是一旦在多层继承中出现了虚继承，那么被虚继承的父类，都会在其子类、子类的子类、子类的子类的子类…的内存布局最后存在一份该父类的布局，并且该父类的布局中也会有一个虚函数表指针（如果该父类有继承或定义虚函数）和一个虚基类表指针，虚基类表的第一项是虚基类表指针相对于整个类的首地址的偏移量，从第二项开始是该类所有虚基类首地址相对于该虚基类表指针的偏移地址。</li></ul><p>对于多重继承来说：</p><ul><li>不管是虚继承还是一般继承，每继承一个类就都会有一个虚函数表，该虚函数表会先继承对应父类的虚函数表的所有内容，如果子类中对虚函数有重写，那么就会覆盖相应的虚函数表中的虚函数地址。</li><li>如果子类中有重新定义虚函数，那么该虚函数地址就会放在最先声明的一般继承的父类对应的虚函数表的最后，如果每一个继承都是虚继承，那么在子类的最前面就会有一个虚函数表指针，对应一个属于子类自己的虚函数表，其中是所有子类新定义的虚函数的地址。</li><li>而每个父类在子类内存中的布局，则是虚继承的父类布局放在最后，一般继承的父类布局放在前面，均按照继承声明的先后顺序，并且在子类的开头还会存放一个虚基类指针（在子类的虚函数表指针后面），对应的虚基类表中存放所有虚继承的父类的虚函数表指针的相对于该虚基类指针的偏移地址。</li></ul><h1 id="STL"><a href="#STL" class="headerlink" title="STL"></a>STL</h1><p>容器、迭代器、仿函数、算法、分配器、配接器<br>他们之间的关系：分配器给容器分配存储空间，算法通过迭代器获取容器中的内容，仿函数可以协助算法完成各种操作，配接器用来套接适配仿函数</p><table><thead><tr><th align="center">STL的组成</th><th align="center">含义</th></tr></thead><tbody><tr><td align="center">算法</td><td align="center">STL 提供了非常多的算法，它们都被设计成一个个的模板函数，这些算法在 std 命名空间中定义，其中大部分算法都包含在头文件algorithm中，少部分位于头文件numeric中。</td></tr><tr><td align="center">容器</td><td align="center">一些封装数据结构的模板类，例如 vector 向量容器、list 列表容器等。</td></tr><tr><td align="center">迭代器</td><td align="center">在 C++ STL 中，对容器中数据的读和写，是通过迭代器完成的，扮演着容器和算法之间的胶合剂。</td></tr><tr><td align="center">仿函数</td><td align="center">如果一个类将 () 运算符重载为成员函数，这个类就称为函数对象类，这个类的对象就是函数对象（又称仿函数）。</td></tr><tr><td align="center">适配器</td><td align="center">可以使一个类的接口（模板的参数）适配成用户指定的形式，从而让原本不能在一起工作的两个类工作在一起。值得一提的是，容器、迭代器和函数都有适配器</td></tr><tr><td align="center">内存分配器</td><td align="center">为容器类模板提供自定义的内存申请和释放功能，由于往往只有高级用户才有改变内存分配策略的需求，因此内存分配器对于一般用户来说，并不常用。</td></tr></tbody></table><h2 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h2><p><strong>排序算法</strong><br>sort: 对范围内的元素进行排序。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">std::<span class="built_in">sort</span>(vec.<span class="built_in">begin</span>(), vec.<span class="built_in">end</span>());</span><br></pre></td></tr></table></figure><p><strong>搜索算法</strong><br>find: 查找等于给定值的元素。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> it = std::<span class="built_in">find</span>(vec.<span class="built_in">begin</span>(), vec.<span class="built_in">end</span>(), value);</span><br></pre></td></tr></table></figure><p><strong>拷贝算法</strong><br>copy：容器内指定范围的元素拷贝到另一容器中</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">copy</span>(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>(),v<span class="number">2.</span><span class="built_in">begin</span>()<span class="number">+2</span>);</span><br></pre></td></tr></table></figure><p>更多内容自行查找</p><h2 id="容器"><a href="#容器" class="headerlink" title="容器"></a>容器</h2><table border="1">  <tr>    <th colspan="2">标准容器类</th>    <th>特点</th>  </tr>  <tr>    <th rowspan="3">序列式容器</th>    <td>vector（向量）</td>    <td>动态数组，从后面快速的插入与删除，快速直接访问任何元素</td>  </tr>  <tr>    <td>list（列表）</td>    <td>双链表，从任何地方快速插入与删除</td>  </tr>  <tr>    <td>deque（双端队列）</td>    <td>从前面或后面快速的插入与删除，快速直接访问任何元素</td>  </tr>  <tr>    <th rowspan="8">关联式容器</th>    <td>set（集合）</td>    <td>有序，快速查找，不允许重复值</td>  </tr>  <tr>    <td>multiset（多重集合）</td>    <td>有序，快速查找，允许重复值</td>  </tr>  <tr>    <td>map（映射）</td>    <td>有序，（key,value）基于关键字快速查找，不允许重复值</td>  </tr>  <tr>    <td>multimap（多重映射）</td>    <td>有序，（key,value）基于关键字快速查找，允许重复值</td>  </tr>    <tr>    <td>unordered_set（无序集合）</td>    <td>无序，快速查找，不允许重复值</td>  </tr>    <tr>    <td>unordered_multiset（无序多重集合）</td>    <td>无序，快速查找，允许重复值</td>  </tr>    <tr>    <td>unordered_map（无序映射）</td>    <td>无序，（key,value）基于关键字快速查找，不允许重复值</td>  </tr>    <tr>    <td>unordered_multimap（无序多重映射）</td>    <td>无序，（key,value）基于关键字快速查找，允许重复值</td>  </tr>  <tr>    <th rowspan="3">容器适配器</th>    <td>stack（栈）</td>    <td>后进先出</td>  </tr>  <tr>    <td>queue（队列）</td>    <td>先进先出</td>  </tr>  <tr>    <td>priority_queue（优先队列）</td>    <td>最高优先级元素总是第一个出列</td>  </tr></table><h3 id="序列式容器"><a href="#序列式容器" class="headerlink" title="序列式容器"></a>序列式容器</h3><h4 id="vector"><a href="#vector" class="headerlink" title="vector"></a>vector</h4><p>vector可以动态扩展。动态扩展并不是在原空间之后续接新空间，而是寻找更大的内存空间，将原数据拷贝到新空间，再释放原空间。</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>size()</code></td><td>返回 vector 中元素的数量。</td></tr><tr><td><code>capacity()</code></td><td>返回 vector 分配的存储容量。</td></tr><tr><td><code>empty()</code></td><td>检查 vector 是否为空，返回布尔值。</td></tr><tr><td><code>push_back(value)</code></td><td>在 vector 末尾添加一个元素 value。</td></tr><tr><td><code>pop_back()</code></td><td>删除 vector 末尾的元素。</td></tr><tr><td><code>insert(pos, value)</code></td><td>在指定位置 pos 插入元素 value。</td></tr><tr><td><code>erase(pos)</code></td><td>删除指定位置 pos 的元素。</td></tr><tr><td><code>clear()</code></td><td>删除 vector 中的所有元素。</td></tr><tr><td><code>operator[]</code></td><td>访问指定位置的元素，不支持越界检查。</td></tr><tr><td><code>at(pos)</code></td><td>返回指定位置的元素，支持越界检查。</td></tr><tr><td><code>front()</code></td><td>返回 vector 的第一个元素。</td></tr><tr><td><code>back()</code></td><td>返回 vector 的最后一个元素。</td></tr><tr><td><code>resize(new_size)</code></td><td>更改 vector 的大小，如果新大小小于当前大小则截断。</td></tr><tr><td><code>swap(other_vector)</code></td><td>交换当前 vector 与 other_vector 的内容。</td></tr><tr><td><code>begin()</code></td><td>返回指向 vector 第一个元素的迭代器。</td></tr><tr><td><code>end()</code></td><td>返回指向 vector 末尾后一个位置的迭代器。</td></tr><tr><td><code>rbegin()</code></td><td>返回指向 vector 最后一个元素的逆迭代器。</td></tr><tr><td><code>rend()</code></td><td>返回指向 vector 开始前一个位置的逆迭代器。</td></tr></tbody></table><h5 id="vector内存"><a href="#vector内存" class="headerlink" title="vector内存"></a>vector内存</h5><ul><li>vector的工作原理是系统预先分配一块capacity大小的空间，当插入的数据超过这个空间的时候，这块空间会让某种方式扩展，但是你删除数据的时候，它却不会缩小。</li><li>vector为了防止大量分配连续内存的开销，保持一块默认的尺寸的内存，clear只是清数据了未清内存，因为vector的 capacity容量未变化，系统维护一个的默认值。clear()可以清空所有元素。但是即使clear()，所占用的内存空间依然如故。如果你需要空间动态缩小，可以考虑使用deque。</li><li>vector与deque不同，其内存占用空间只会增长，不会减小。比如你首先分配了10,000个字节，然后erase掉后面9,999个，则虽然有效元素只有一个，但是内存占用仍为10,000个，所有空间在vector析构时回收。</li></ul><p>在《effective STL》和其实很多C++文章中都有指明，用clear()无法保证内存回收。如果非要用vector，这里有一个办法：使用swap()可以：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; nums;</span><br><span class="line">nums.<span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">nums.<span class="built_in">push_back</span>(<span class="number">1</span>);</span><br><span class="line">nums.<span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line">nums.<span class="built_in">push_back</span>(<span class="number">2</span>);</span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;().<span class="built_in">swap</span>(nums); <span class="comment">// 或者nums.swap(vector&lt;int&gt;())；</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="type">int</span>&gt;().<span class="built_in">swap</span>(nums); <span class="comment">// 或者如下所示加一对大括号都可以，意思一样的：</span></span><br><span class="line">&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; tmp = nums;</span><br><span class="line">    nums.<span class="built_in">swap</span>(tmp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="emplace-back-和push-back"><a href="#emplace-back-和push-back" class="headerlink" title="emplace_back()和push_back()"></a>emplace_back()和push_back()</h5><p><strong>主要区别</strong></p><ul><li>push_back() 是较老的方法，从C++98开始就存在。首先会创建这个元素，然后再将这个元素拷贝或者移动到容器中（如果是拷贝的话，事后会自行销毁先前创建的这个元素）。</li><li>emplace_back() 是在C++11中引入的。它接受构造函数的参数。直接在容器尾部创建这个元素，省去了拷贝或移动元素的过程。</li></ul><p><strong>构造方式</strong></p><ul><li>push_back() 需要一个已经构造好的对象。</li><li>emplace_back() 直接在容器的内存空间中构造对象，避免了不必要的临时对象创建。</li></ul><p><strong>性能</strong></p><ul><li>在大多数情况下，emplace_back() 比 push_back() 更高效，尤其是对于复杂对象。</li><li>emplace_back() 避免了额外的复制或移动操作。</li></ul><p><strong>使用方式</strong></p><ul><li>push_back() 通常传递一个完整的对象。</li><li>emplace_back() 可以直接传递构造函数的参数。</li></ul><h4 id="array"><a href="#array" class="headerlink" title="array"></a>array</h4><p>在c++11中，STL中提拱了一个新的容器std::array，该容器在某些程度上替代了之前版本的std::vector的使用，更可以替代之前的自建数组的使用。先简单的做个比较：</p><table><thead><tr><th>特性</th><th>array</th><th>vector</th></tr></thead><tbody><tr><td>大小</td><td>固定大小，编译时确定</td><td>动态大小，可在运行时调整</td></tr><tr><td>内存管理</td><td>栈上分配，固定内存</td><td>堆上分配，可动态分配和调整</td></tr><tr><td>灵活性</td><td>不可调整大小，较为固定</td><td>可调整大小，支持插入和删除元素</td></tr><tr><td>初始化</td><td>直接初始化</td><td>多种方式（默认、指定大小、列表等）</td></tr><tr><td>性能</td><td>性能较高（无动态开销）</td><td>较小的动态调整开销</td></tr><tr><td>标准库支持</td><td>C++11 引入 std::array</td><td>C++ 标准模板库（STL）的一部分</td></tr><tr><td>迭代器支持</td><td>原生数组不支持，std::array 支持</td><td>完全支持 STL 迭代器</td></tr></tbody></table><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//原生 C++ 数组:</span></span><br><span class="line">  <span class="type">int</span> arr[<span class="number">5</span>] = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//std::array (C++11 引入):</span></span><br><span class="line">  std::array&lt;<span class="type">int</span>, 5&gt; arr = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;;</span><br></pre></td></tr></table></figure><h4 id="list"><a href="#list" class="headerlink" title="list"></a>list</h4><p>由于链表的存储方式并不是连续的内存空间，因此链表list中的迭代器只支持前移和后移，属于双向迭代器。</p><p>list有一个重要的性质，插入操作和删除操作都不会造成原有list迭代器的失效。但是vector原来的空间迭代器则会失效，因为vector容器放满后，则、会动态扩展，开辟新的空间来拷贝原有数据。</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>size()</code></td><td>返回 list 中元素的数量。</td></tr><tr><td><code>empty()</code></td><td>检查 list 是否为空，返回布尔值。</td></tr><tr><td><code>push_back(value)</code></td><td>在 list 末尾添加一个元素 value。</td></tr><tr><td><code>push_front(value)</code></td><td>在 list 前面添加一个元素 value。</td></tr><tr><td><code>pop_back()</code></td><td>删除 list 末尾的元素。</td></tr><tr><td><code>pop_front()</code></td><td>删除 list 前面的元素。</td></tr><tr><td><code>insert(pos, value)</code></td><td>在指定位置 pos 插入元素 value。</td></tr><tr><td><code>erase(pos)</code></td><td>删除指定位置 pos 的元素。</td></tr><tr><td><code>clear()</code></td><td>删除 list 中的所有元素。</td></tr><tr><td><code>operator[]</code></td><td>访问指定位置的元素（不直接支持，但可用迭代器实现）。</td></tr><tr><td><code>front()</code></td><td>返回 list 的第一个元素。</td></tr><tr><td><code>back()</code></td><td>返回 list 的最后一个元素。</td></tr><tr><td><code>resize(new_size)</code></td><td>更改 list 的大小（不适用于 list，常用于 vector）。</td></tr><tr><td><code>begin()</code></td><td>返回指向 list 第一个元素的迭代器。</td></tr><tr><td><code>end()</code></td><td>返回指向 list 末尾后一个位置的迭代器。</td></tr><tr><td><code>rbegin()</code></td><td>返回指向 list 最后一个元素的逆迭代器。</td></tr><tr><td><code>rend()</code></td><td>返回指向 list 开始前一个位置的逆迭代器。</td></tr><tr><td><code>sort()</code></td><td>对 list 中的元素进行排序。</td></tr><tr><td><code>unique()</code></td><td>删除相邻重复的元素。</td></tr><tr><td><code>reverse()</code></td><td>反转 list 中元素的顺序。</td></tr></tbody></table><h4 id="deque"><a href="#deque" class="headerlink" title="deque"></a>deque</h4><p>deque内部有个中控器，维护每段缓冲区中的内容，缓冲区中存放真实数据，中控器维护的是每个缓冲区的地址，使得使用deque时像一片连续的内存空间。</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>size()</code></td><td>返回 deque 中元素的数量。</td></tr><tr><td><code>empty()</code></td><td>检查 deque 是否为空，返回布尔值。</td></tr><tr><td><code>push_back(value)</code></td><td>在 deque 末尾添加一个元素 value。</td></tr><tr><td><code>push_front(value)</code></td><td>在 deque 前面添加一个元素 value。</td></tr><tr><td><code>pop_back()</code></td><td>删除 deque 末尾的元素。</td></tr><tr><td><code>pop_front()</code></td><td>删除 deque 前面的元素。</td></tr><tr><td><code>insert(pos, value)</code></td><td>在指定位置 pos 插入元素 value。</td></tr><tr><td><code>erase(pos)</code></td><td>删除指定位置 pos 的元素。</td></tr><tr><td><code>clear()</code></td><td>删除 deque 中的所有元素。</td></tr><tr><td><code>operator[]</code></td><td>访问指定位置的元素，不支持越界检查。</td></tr><tr><td><code>at(pos)</code></td><td>返回指定位置的元素，支持越界检查。</td></tr><tr><td><code>front()</code></td><td>返回 deque 的第一个元素。</td></tr><tr><td><code>back()</code></td><td>返回 deque 的最后一个元素。</td></tr><tr><td><code>resize(new_size)</code></td><td>更改 deque 的大小，如果新大小小于当前大小则截断。</td></tr><tr><td><code>begin()</code></td><td>返回指向 deque 第一个元素的迭代器。</td></tr><tr><td><code>end()</code></td><td>返回指向 deque 末尾后一个位置的迭代器。</td></tr><tr><td><code>rbegin()</code></td><td>返回指向 deque 最后一个元素的逆迭代器。</td></tr><tr><td><code>rend()</code></td><td>返回指向 deque 开始前一个位置的逆迭代器。</td></tr></tbody></table><h3 id="关联式容器"><a href="#关联式容器" class="headerlink" title="关联式容器"></a>关联式容器</h3><h4 id="set-multiset"><a href="#set-multiset" class="headerlink" title="set&#x2F;multiset"></a>set&#x2F;multiset</h4><p>set&#x2F;multiset属于关联式容器，<strong>底层结构是用红黑树实现</strong>。存入的元素具有自动排序的特性，排序的依据是 key ，而 set&#x2F;miltiset 元素的 key 与 value是合二为一的，其value 就是 key。</p><p><strong>不能通过迭代器改变 set&#x2F;multiset 元素的值</strong>，因为set元素值就是其键值，关系到set元素的排序规则。如果任意改变set元素值，会严重破坏set组织。 set容器的迭代器为可读迭代器。</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>insert(value)</code></td><td>向 set 或 multiset 插入一个元素 value。</td></tr><tr><td><code>erase(value)</code></td><td>删除 set 或 multiset 中的元素 value。</td></tr><tr><td><code>find(value)</code></td><td>查找 value 是否在 set 或 multiset 中，返回迭代器。</td></tr><tr><td><code>count(value)</code></td><td>返回 set 或 multiset 中 value 的出现次数（set 返回 0 或 1）。</td></tr><tr><td><code>clear()</code></td><td>删除 set 或 multiset 中的所有元素。</td></tr><tr><td><code>size()</code></td><td>返回 set 或 multiset 中元素的数量。</td></tr><tr><td><code>empty()</code></td><td>检查 set 或 multiset 是否为空，返回布尔值。</td></tr><tr><td><code>begin()</code></td><td>返回指向 set 或 multiset 第一个元素的迭代器。</td></tr><tr><td><code>end()</code></td><td>返回指向 set 或 multiset 末尾后一个位置的迭代器。</td></tr><tr><td><code>rbegin()</code></td><td>返回指向 set 或 multiset 最后一个元素的逆迭代器。</td></tr><tr><td><code>rend()</code></td><td>返回指向 set 或 multiset 开始前一个位置的逆迭代器。</td></tr><tr><td><code>equal_range(value)</code></td><td>返回 value 的范围（对于 multiset，返回一对迭代器）。</td></tr></tbody></table><h4 id="map-multimap"><a href="#map-multimap" class="headerlink" title="map&#x2F;multimap"></a>map&#x2F;multimap</h4><p><strong>map&#x2F;multimap 是以红黑树为底层结构的</strong>，因此存入的元素具有自动排序的特性，排序的依据是 key；</p><p>map&#x2F;multimap 提供遍历操作与迭代器 iterator，通过不断的 iterator++ 遍历可以获取到已经按照 key 排好序的元素；</p><p><strong>不能通过迭代器改变 map&#x2F;multimap 元素的 key 值</strong>，因为key值关系到元素的排序规则。如果任意改变key值，会严重破坏map组织。但可以修改 key 对应的 value 值。因而 map&#x2F;multimap 内部将 key type 设为 const，如此可以避免对 key 的随意修改。</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>insert(pair)</code></td><td>向 map 或 multimap 插入一个键值对 pair。</td></tr><tr><td><code>erase(key)</code></td><td>删除 map 或 multimap 中键为 key 的元素。</td></tr><tr><td><code>find(key)</code></td><td>查找 key 是否在 map 或 multimap 中，返回迭代器。</td></tr><tr><td><code>count(key)</code></td><td>返回 map 中 key 的出现次数（multimap 返回出现次数）。</td></tr><tr><td><code>clear()</code></td><td>删除 map 或 multimap 中的所有元素。</td></tr><tr><td><code>size()</code></td><td>返回 map 或 multimap 中元素的数量。</td></tr><tr><td><code>empty()</code></td><td>检查 map 或 multimap 是否为空，返回布尔值。</td></tr><tr><td><code>begin()</code></td><td>返回指向 map 或 multimap 第一个元素的迭代器。</td></tr><tr><td><code>end()</code></td><td>返回指向 map 或 multimap 末尾后一个位置的迭代器。</td></tr><tr><td><code>rbegin()</code></td><td>返回指向 map 或 multimap 最后一个元素的逆迭代器。</td></tr><tr><td><code>rend()</code></td><td>返回指向 map 或 multimap 开始前一个位置的逆迭代器。</td></tr><tr><td><code>operator[]</code></td><td>访问或插入键为 key 的元素（对于 map）。</td></tr><tr><td><code>equal_range(key)</code></td><td>返回 key 的范围（对于 multimap，返回一对迭代器）。</td></tr></tbody></table><h4 id="unordered-map-unordered-set-unordered-multimap-unordered-multiset"><a href="#unordered-map-unordered-set-unordered-multimap-unordered-multiset" class="headerlink" title="unordered_map&#x2F;unordered_set&#x2F;unordered_multimap&#x2F;unordered_multiset"></a>unordered_map&#x2F;unordered_set&#x2F;unordered_multimap&#x2F;unordered_multiset</h4><p><strong>这四种容器采用哈希表实现</strong>，不同操作的时间复杂度为：<br>插入: O(1)，最坏情况O(N)<br>查看: O(1)，最坏情况O(N)<br>删除: O(1)，最坏情况O(N)</p><ul><li>HashMap是数组加链表组成的复合结构，HashMap的主干是数组，其中数组被分为一个个桶（bucket）</li><li>每个桶存储有一个或多个键值对，每个键值对也称为 Entry ，通过哈希值决定了Entry对象在这个数组的下标</li><li>哈希值相同的Entry对象（键值对），当数据量在8以内使用链表来实现桶，当数据量大于8则自动转换为红黑树结构–也就是有序map的实现结构<br><img data-src="/transblog/images/1709815129102.png"></li></ul><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>insert</code></td><td>插入键值对</td></tr><tr><td><code>erase</code></td><td>删除指定key值的键值对</td></tr><tr><td><code>find</code></td><td>查找指定key值的键值对</td></tr><tr><td><code>size</code></td><td>获取容器中元素的个数</td></tr><tr><td><code>empty</code></td><td>判断容器是否为空</td></tr><tr><td><code>clear</code></td><td>清空容器</td></tr><tr><td><code>swap</code></td><td>交换两个容器中的数据</td></tr><tr><td><code>count</code></td><td>获取容器中指定key值的元素个数（对于 <code>map</code> 返回0或1，对于 <code>multimap</code> 返回实际数量）</td></tr></tbody></table><h3 id="容器适配器"><a href="#容器适配器" class="headerlink" title="容器适配器"></a>容器适配器</h3><h4 id="stack"><a href="#stack" class="headerlink" title="stack"></a>stack</h4><p>stack容器适配器默认使用std::deque作为其底层容器，但你也可以通过模板参数来指定其他容器类型，如std::vector或std::list。不同的底层容器可能会影响栈的性能，例如，std::deque通常提供更好的性能，因为它不需要在插入和删除操作时移动大量元素。</p><table><thead><tr><th>成员函数</th><th>功能</th></tr></thead><tbody><tr><td><code>push(const T&amp; value)</code></td><td>在栈顶插入一个元素。</td></tr><tr><td><code>emplace(Args&amp;&amp;... args)</code></td><td>在栈顶原地构造一个元素。</td></tr><tr><td><code>pop()</code></td><td>移除栈顶元素。</td></tr><tr><td><code>top()</code></td><td>返回栈顶元素的引用（不移除）。</td></tr><tr><td><code>empty()</code></td><td>检查栈是否为空。</td></tr><tr><td><code>size()</code></td><td>返回栈中元素的数量。</td></tr></tbody></table><h4 id="queue"><a href="#queue" class="headerlink" title="queue"></a>queue</h4><p>与stack类似，queue容器适配器也默认使用std::deque作为其底层容器，但你也可以通过模板参数来指定其他容器类型，如std::list。需要注意的是，std::vector不能作为queue的底层容器，因为std::vector不支持在头部进行高效的插入和删除操作。</p><table><thead><tr><th>成员函数</th><th>功能</th></tr></thead><tbody><tr><td><code>push(const T&amp; value)</code></td><td>在队尾插入一个元素。</td></tr><tr><td><code>emplace(Args&amp;&amp;... args)</code></td><td>在队尾原地构造一个元素。</td></tr><tr><td><code>pop()</code></td><td>移除队头元素。</td></tr><tr><td><code>front()</code></td><td>返回队头元素的引用（不移除）。</td></tr><tr><td><code>back()</code></td><td>返回队尾元素的引用（不移除）。</td></tr><tr><td><code>empty()</code></td><td>检查队列是否为空。</td></tr><tr><td><code>size()</code></td><td>返回队列中元素的数量。</td></tr></tbody></table><h4 id="priority-queue"><a href="#priority-queue" class="headerlink" title="priority_queue"></a>priority_queue</h4><p>priority_queue容器适配器默认使用std::vector作为其底层容器，因为std::vector在随机访问和尾部插入方面表现良好。此外，priority_queue还使用一个比较函数来确定元素的优先级顺序。默认情况下，比较函数是std::less<T>，它创建一个最大堆，其中具有最高优先级的元素总是位于堆顶。如果你想要创建一个最小堆，可以使用std::greater<T>作为比较函数。</T></T></p><table><thead><tr><th>成员函数</th><th>功能</th></tr></thead><tbody><tr><td><code>push(const T&amp; value)</code></td><td>在队列中插入一个元素。</td></tr><tr><td><code>emplace(Args&amp;&amp;... args)</code></td><td>在队列中原地构造一个元素。</td></tr><tr><td><code>pop()</code></td><td>移除并返回具有最高优先级的元素（对于最大堆）。</td></tr><tr><td><code>top()</code></td><td>返回具有最高优先级的元素的引用（不移除）。</td></tr><tr><td><code>empty()</code></td><td>检查队列是否为空。</td></tr><tr><td><code>size()</code></td><td>返回队列中元素的数量。</td></tr></tbody></table><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自定义比较仿函数，按字符串长度比较</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">CompareByLength</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> std::string&amp; lhs, <span class="type">const</span> std::string&amp; rhs)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> lhs.<span class="built_in">length</span>() &gt; rhs.<span class="built_in">length</span>(); <span class="comment">// 创建一个最小长度堆</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个最小堆优先队列</span></span><br><span class="line">    std::priority_queue&lt;<span class="type">int</span>, std::vector&lt;<span class="type">int</span>&gt;, std::greater&lt;<span class="type">int</span>&gt;&gt; minHeap;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 插入元素</span></span><br><span class="line">    minHeap.<span class="built_in">push</span>(<span class="number">10</span>);</span><br><span class="line">    minHeap.<span class="built_in">push</span>(<span class="number">20</span>);</span><br><span class="line">    minHeap.<span class="built_in">push</span>(<span class="number">30</span>);</span><br><span class="line">    minHeap.<span class="built_in">push</span>(<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 访问并移除最低优先级的元素</span></span><br><span class="line">    <span class="keyword">while</span> (!minHeap.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Top element (min): &quot;</span> &lt;&lt; minHeap.<span class="built_in">top</span>() &lt;&lt; std::endl;</span><br><span class="line">        minHeap.<span class="built_in">pop</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用自定义比较仿函数的优先队列</span></span><br><span class="line">    std::priority_queue&lt;std::string, std::vector&lt;std::string&gt;, CompareByLength&gt; minLengthHeap;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 插入元素</span></span><br><span class="line">    minLengthHeap.<span class="built_in">push</span>(<span class="string">&quot;short&quot;</span>);</span><br><span class="line">    minLengthHeap.<span class="built_in">push</span>(<span class="string">&quot;this is a longer string&quot;</span>);</span><br><span class="line">    minLengthHeap.<span class="built_in">push</span>(<span class="string">&quot;medium&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 访问并移除最短字符串的元素</span></span><br><span class="line">    <span class="keyword">while</span> (!minLengthHeap.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Shortest string: &quot;</span> &lt;&lt; minLengthHeap.<span class="built_in">top</span>() &lt;&lt; std::endl;</span><br><span class="line">        minLengthHeap.<span class="built_in">pop</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持-运算符的容器"><a href="#支持-运算符的容器" class="headerlink" title="支持[]运算符的容器"></a>支持[]运算符的容器</h3><p>支持随机访问就支持[]：vector，deque，map，unordered_map，string</p><h2 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h2><p>迭代器（Iterator）是 C++ 标准模板库中的一种对象，专为遍历容器中的元素而设计。它的工作方式类似于指针，但具备更多功能。<br>迭代器和指针有许多相似之处，例如它们都可以通过递增（++）、递减（–）、解引用（*）来访问和操作数据。但是，迭代器是针对容器设计的，它们具有更强的抽象能力，可以屏蔽容器的底层实现，从而使代码更通用和易维护。</p><table><thead><tr><th>迭代器类型</th><th>开始位置标志</th><th>末尾位置标志</th><th>说明</th></tr></thead><tbody><tr><td><code>iterator</code></td><td><code>begin()</code></td><td><code>end()</code></td><td>正向迭代</td></tr><tr><td><code>const_iterator</code></td><td><code>cbegin()</code></td><td><code>cend()</code></td><td>正向常量迭代</td></tr><tr><td><code>reverse_iterator</code></td><td><code>rbegin()</code></td><td><code>rend()</code></td><td>反向迭代</td></tr><tr><td><code>const_reverse_iterator</code></td><td><code>crbegin()</code></td><td><code>crend()</code></td><td>反向常量迭代</td></tr></tbody></table><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    string s = <span class="string">&quot;abcdef&quot;</span>;</span><br><span class="line"></span><br><span class="line">    string::iterator it1 = s.<span class="built_in">begin</span>();</span><br><span class="line">    string::iterator it2 = s.<span class="built_in">end</span>();</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; (it1 &lt; it2) &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; it1 - it2 &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><table><thead><tr><th>下标</th><th>0</th><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>-</th></tr></thead><tbody><tr><td>字符</td><td>a</td><td>b</td><td>c</td><td>d</td><td>e</td><td>f</td><td></td></tr><tr><td>迭代器</td><td>s.begin() →</td><td></td><td></td><td></td><td></td><td></td><td>s.end() →</td></tr></tbody></table><h3 id="容器迭代器失效"><a href="#容器迭代器失效" class="headerlink" title="容器迭代器失效"></a>容器迭代器失效</h3><p>本质上是两种情况：</p><ol><li>pos的意义变了（指向的位置不是想要指向位置），</li><li>pos变成了野指针（使用了一块已经被释放了的空间）。</li></ol><h4 id="序列式容器迭代器失效"><a href="#序列式容器迭代器失效" class="headerlink" title="序列式容器迭代器失效"></a>序列式容器迭代器失效</h4><p>容器底层内存连续，允许随机访问。常见下面成员函数的使用可能会导致迭代器失效，本质上还是<strong>内存重新分配导致的全失效</strong>和<strong>元素被删除移动导致的意义改变了</strong>。</p><ol><li><p>push_back() 使迭代器失效。<br>在容器末尾添加一个元素。如果容器有剩余空间(capacity() &gt; size())，则直接添加新元素到容器尾部。此时，原迭代器中end()会失效，其他的都不会失效。<br>如果容器没有剩余空间(capacity() &#x3D;&#x3D; size())，会导致容器重新分配内存，然后将数据从原内存复制到新内存，再在尾部添加新元素。此时，由于内存重新分配，原迭代器(所有)都失效。(这种情况是最容易变成野指针的情形)</p></li><li><p>pop_back() 使迭代器失效。<br>直接将容器中的最后一个元素删除，原迭代器中最后一个元素的迭代器和end()会失效，其余的都不会失效。</p></li><li><p>insert(iterator, n) 使迭代器失效。<br>如果没有内存的重新分配，原迭代器中插入点及插入点之后的迭代器(包括end())都失效。如果有内存的重新分配，原迭代器(所有)都失效。</p></li><li><p>erase(iterator) 使迭代器失效。<br>将删除点及之后的元素都向前移动一位，然后删除最后一个元素。因此，原迭代器中删除点之前的迭代器都有效，删除点之后的元素迭代器都失效。</p></li></ol><h4 id="关联式容器迭代器失效"><a href="#关联式容器迭代器失效" class="headerlink" title="关联式容器迭代器失效"></a>关联式容器迭代器失效</h4><ol><li><p>insert(iterator) 插入不会使得任何迭代器失效；</p></li><li><p>删除运算使指向删除位置的迭代器失效，但是不会失效其他迭代器。<br>erase迭代器只是被删元素的迭代器失效，返回值为被删除元素的下一个迭代器，所以要采用 iter&#x3D;contan.erase(iter)和erase(iter++)的方式删除迭代器。erase(iter++)的删除方法对于内存连续的容器，例如vector不可用，因为被删除元素的下一个迭代器也失效了；iter&#x3D;contan.erase(iter)则都可行。</p></li></ol><p>对于关联容器map、multimap、set、multiset，底层是使用红黑树实现的，所以删除某个元素，仅仅会删除元素的迭代器失效，同时会进行二叉树的调整，但每个节点在内存中的地址是没有改变的，改变的只是他们之间的指针指向。</p><h2 id="仿函数"><a href="#仿函数" class="headerlink" title="仿函数"></a>仿函数</h2><p>又称函数对象，函数对象是一个类，它重载了函数调用运算符 operator()，使得类的对象可以像函数一样被调用。<a href="#target4">点击此处跳转到回调函数–函数对象</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//仿函数1,比较大小</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; <span class="keyword">struct</span> <span class="title class_">comp</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(T in1, T in2)</span> <span class="type">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (in1&gt;in2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line">comp&lt;<span class="type">int</span>&gt; m_comp_objext;</span><br><span class="line">cout &lt;&lt; <span class="built_in">m_comp_objext</span>(<span class="number">6</span>, <span class="number">3</span>) &lt;&lt; endl;     <span class="comment">//使用对象调用</span></span><br><span class="line">cout &lt;&lt; <span class="built_in">comp</span>&lt;<span class="type">int</span>&gt;()(<span class="number">1</span>, <span class="number">2</span>) &lt;&lt; endl;       <span class="comment">//使用仿函数实现</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="适配器"><a href="#适配器" class="headerlink" title="适配器"></a>适配器</h2><p>C++中定义了3种容器适配器，它们让容器提供的接口变成了我们常用的的3种数据结构：栈stack，队列queue和优先队列priority_queue。<br>默认情况下，栈和队列都是基于deque实现的，而优先级队列则是基于vector实现的。</p><h2 id="内存分配器"><a href="#内存分配器" class="headerlink" title="内存分配器"></a>内存分配器</h2><p>分配器 (allocator) 是C++ STL库的基石之一，它是一种策略模式，允许用户将内存管理从容器中解耦出来，进行更具体化的操作。<br>在传统的内存申请方式中，我们在申请内存的同时就会调用对象的构造函数，但有时候，我们可能只是想申请内存，而不想立即构造对象，这时候，就可以使用allocator。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::allocator&lt;<span class="type">int</span>&gt; alloc; <span class="comment">// 创建一个分配int的allocator</span></span><br><span class="line">    <span class="type">int</span>* p = alloc.<span class="built_in">allocate</span>(<span class="number">10</span>); <span class="comment">// 分配10个int的空间</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用未构造的内存</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        alloc.<span class="built_in">construct</span>(p + i, i); <span class="comment">// 在分配的内存上构造对象</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 销毁对象并释放内存</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        alloc.<span class="built_in">destroy</span>(p + i); <span class="comment">// 销毁对象</span></span><br><span class="line">    &#125;</span><br><span class="line">    alloc.<span class="built_in">deallocate</span>(p, <span class="number">10</span>); <span class="comment">// 释放内存</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>new运算分两个阶段：  </p><ol><li>调用::operator new配置内存;</li><li>调用对象构造函数构造对象内容</li></ol><p>delete运算分两个阶段：</p><ol><li>调用对象析构函数；</li><li>调用::operator delete释放内存</li></ol><p>为了精密分工，STL allocator将两个阶段操作区分开来：</p><ul><li>内存配置有alloc::allocate()负责</li><li>内存释放由alloc::deallocate()负责</li><li>对象构造由::construct()负责</li><li>对象析构由::destroy()负责</li></ul><p><strong>上述工作分别由stl_construct.h, stl_alloc.h, stl_uninitialized.h</strong></p><h3 id="对象的构造和析构"><a href="#对象的构造和析构" class="headerlink" title="对象的构造和析构"></a>对象的构造和析构</h3><p>对象的构造和析构工具（stl_construct.h）<br>提供了两种对象的构造方法，默认构造和赋值构造：<br>提供了两种析构方法：</p><ol><li>析构函数接受一个指针，将该指针所指的对象析构掉；</li><li>析构函数接受first和last两个迭代器，将这两个迭代器范围内的对象析构掉。</li></ol><h3 id="内存空间管理"><a href="#内存空间管理" class="headerlink" title="内存空间管理"></a>内存空间管理</h3><p>内存空间管理工具（stl_alloc.h）</p><ol><li>__malloc_alloc_template内存分配器（malloc）</li><li>__default_alloc_template分配器（内存池）</li></ol><ul><li>allocator是一个由两级分配器构成的内存管理器</li><li>当申请的内存大小大于128byte时，就启动第一级分配器通过malloc直接向系统的堆空间分配，</li><li>如果申请的内存大小小于128byte时，就启动第二级分配器，从一个预先分配好的内存池中取一块内存交付给用户，这个内存池由16个不同大小（8的倍数，8~128byte）的空闲列表组成，allocator会根据申请内存的大小（将这个大小round up成8的倍数）从对应的空闲块列表取表头块给用户。</li></ul><p>这种做法有两个优点：</p><ol><li>小对象的快速分配。<br>小对象是从内存池分配的，这个内存池是系统调用一次malloc分配一块足够大的区域给程序备用，当内存池耗尽时再向系统申请一块新的区域，整个过程类似于批发和零售，起先是由allocator向总经商批发一定量的货物，然后零售给用户，与每次都总经商要一个货物再零售给用户的过程相比，显然是快捷了。当然，这里的一个问题时，内存池会带来一些内存的浪费，比如当只需分配一个小对象时，为了这个小对象可能要申请一大块的内存池，但这个浪费还是值得的，况且这种情况在实际应用中也并不多见。</li><li>避免了内存碎片的生成。<br>程序中的小对象的分配极易造成内存碎片，给操作系统的内存管理带来了很大压力，系统中碎片的增多不但会影响内存分配的速度，而且会极大地降低内存的利用率。以内存池组织小对象的内存，从系统的角度看，只是一大块内存池，看不到小对象内存的分配和释放。</li></ol><h3 id="基本内存处理"><a href="#基本内存处理" class="headerlink" title="基本内存处理"></a>基本内存处理</h3><p>基本内存处理工具（stl_uninitialized.h）</p><p>STL提供了三类内存处理工具：uninitialized_copy(), uninitialized_fill()和uninitialized_fill_n()</p><ol><li>uninitialized_copy()会将迭代器_first和_last之间的对象拷贝到迭代器_result开始的地方</li><li>uninitialized_fill()会将迭代器_first和_last范围内的所有元素初始化为x</li><li>uninitialized_fill_n()会将迭代器_first开始处的n个元素初始化为x</li></ol><h2 id="string是如何存储数据的"><a href="#string是如何存储数据的" class="headerlink" title="string是如何存储数据的"></a>string是如何存储数据的</h2><ul><li>以前的编译器是用写时复制（COW）技术，每当字符串发生复制构造或赋值时进行浅拷贝，只复制指针并增加一个引用计数，只有对其中一个字符串进行修改时才会执行真正的复制。既然用到了引用计数，那么就要考虑在多线程环境下的线程安全问题，而且string会把operator[]和at()都认定为修改“语义”，即使我们只是访问字符串也会触发COW，这就导致string的COW实现存在诸多弊端；</li><li>现在编译器大多采用SSO短字符串优化，<strong>当字符串长度小于15字节时直接存放在栈中，大于15字节时，栈中存放指针，指针指向堆中的完整字符串</strong>。这样的好处是，当字符串较短时，直接将其数据存在栈中，而不用去堆中动态申请空间，避免了申请堆空间的开销；</li></ul><p>string为什么会扩容2倍或1.5倍？</p><ol><li>在g++环境下，string的扩容机制跟vector一样是两倍扩容，最初分配的capacity是15字节；</li><li>如果扩容倍率太低，继续插入字符的话会出现频繁扩容的现象，效率降低；如果倍率太高，又会造成空间浪费，所以2倍或1.5倍是一个折中的考虑，兼顾了效率与空间利用率；</li></ol><h1 id="C-11特性"><a href="#C-11特性" class="headerlink" title="C++11特性"></a>C++11特性</h1><h2 id="nullptr"><a href="#nullptr" class="headerlink" title="nullptr"></a>nullptr</h2><p>在某种意义上来说，传统 C++ 会把 NULL、0 视为同一种东西，这取决于编译器如何定义 NULL，有些编译器会将 NULL 定义为 (void* 0)，有些则会直接将其定义为 0。<br>C++ 不允许直接将 void * 隐式转换到其他类型，但如果 NULL 被定义为 (void* 0)，那么当编译char *ch &#x3D; NULL;时，NULL 只好被定义为 0。<br>而这依然会产生问题，将导致了 C++ 中重载特性会发生混乱，考虑：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">char</span> *)</span></span>;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>)</span></span>;</span><br></pre></td></tr></table></figure><p>对于这两个函数来说，如果 NULL 又被定义为了 0 那么 foo(NULL); 这个语句将会去调用 foo(int)，从而导致代码违反直观。<br>为了解决这个问题，C++11 引入了 nullptr 关键字，专门用来区分空指针、0。<br>nullptr 的类型为 nullptr_t，能够隐式的转换为任何指针或成员指针的类型，也能和他们进行相等或者不等的比较。</p><h2 id="类型推导"><a href="#类型推导" class="headerlink" title="类型推导"></a>类型推导</h2><h3 id="auto"><a href="#auto" class="headerlink" title="auto"></a>auto</h3><p>C++11 引入了 auto 和 decltype 这两个关键字实现了类型推导，让编译器来操心变量的类型。<br>注意：auto 不能用于函数传参，因此下面的做法是无法通过编译的（考虑重载的问题，我们应该使用模板）：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="keyword">auto</span> x, <span class="keyword">auto</span> y)</span></span>;<span class="comment">//错误</span></span><br></pre></td></tr></table></figure><p>此外，auto 还不能用于推导数组类型：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"> <span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"> <span class="keyword">auto</span> i = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line"> <span class="type">int</span> arr[<span class="number">10</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"> <span class="keyword">auto</span> auto_arr = arr;</span><br><span class="line"> <span class="keyword">auto</span> auto_arr2[<span class="number">10</span>] = arr;<span class="comment">//该行错误</span></span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="decltype"><a href="#decltype" class="headerlink" title="decltype"></a>decltype</h3><p>decltype 关键字是为了解决 auto 关键字只能对变量进行类型推导的缺陷而出现的。它的用法和 sizeof 很相似：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">auto</span> y = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">decltype</span>(x+y) z;</span><br></pre></td></tr></table></figure><p>拖尾返回类型、auto 与 decltype 配合<br>C++11 还引入了一个叫做拖尾返回类型（trailing return type），利用 auto 关键字将返回类型后置：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">add</span><span class="params">(T x, U y)</span> -&gt; <span class="title">decltype</span><span class="params">(x+y)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x+y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从 C++14 开始是可以直接让普通函数具备返回值推导，因此下面的写法变得合法：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">add</span><span class="params">(T x, U y)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x+y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="区间迭代"><a href="#区间迭代" class="headerlink" title="区间迭代"></a>区间迭代</h2><p>基于范围的 for 循环<br>C++11 引入了基于范围的迭代写法，我们拥有了能够写出像 Python 一样简洁的循环语句。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// &amp; 启用了引用</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">auto</span> &amp;i : arr) &#123;    </span><br><span class="line">    std::cout &lt;&lt; i &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="初始化列表"><a href="#初始化列表" class="headerlink" title="初始化列表"></a>初始化列表</h2><p>C++11 提供了统一的语法来初始化任意的对象，例如：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">float</span> b;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">B</span>(<span class="type">int</span> _a, <span class="type">float</span> _b): <span class="built_in">a</span>(_a), <span class="built_in">b</span>(_b) &#123;&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">float</span> b;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">A a &#123;<span class="number">1</span>, <span class="number">1.1</span>&#125;;    <span class="comment">// 统一的初始化语法</span></span><br><span class="line">B b &#123;<span class="number">2</span>, <span class="number">2.2</span>&#125;;</span><br></pre></td></tr></table></figure><h2 id="lambda表达式"><a href="#lambda表达式" class="headerlink" title="lambda表达式"></a>lambda表达式</h2><p><img data-src="/transblog/images/5796141696695.png"><br>例子：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> func1 = []()&#123;cout &lt;&lt; <span class="string">&quot;hello world!&quot;</span> &lt;&lt; endl; &#125;;</span><br><span class="line"><span class="built_in">func1</span>();</span><br></pre></td></tr></table></figure><ul><li><p>捕获列表：用于指定Lambda表达式可以访问的外部变量。编译器根据[]来判断接下来的代码是否为lambda函数，捕捉列表能够捕捉上下文中的变量供lambda函数使用。</p><blockquote><p>[]:表示不捕获任何外部变量<br>[&#x3D;]:以传值的方式捕获外部的所有变量<br>[&amp;]:以传引用的方式捕获外部的所有变量<br>[this]:捕获外部的this指针<br>[&#x3D;，&amp;a]：以传值的方式捕获外部的所有变量，但是a变量以传引用的方式捕获<br>[a，b]：以传值的方式捕获外部变量a和b<br>[a,&amp;b]：a以传值方式捕获，b以传引用的方式捕获</p></blockquote></li><li><p>参数列表：普通函数的参数列表一致，如果不需要参数传递，则可以连同()一起省略</p></li><li><p>mutable：默认情况下，lambda函数总是一个const函数，mutable可以取消其常量性。使用该修饰符时，参数列表不可省略(即使参数为空)。</p></li><li><p>返回值：用追踪返回类型形式声明函数的返回值类型，没有返回值时此部分可省略。返回值类型明确情况下，也可省略，由编译器对返回类型进行推导。</p></li><li><p>函数体：在该函数体内，除了可以使用其参数外，还可以使用所有捕获到的变量。</p></li></ul><p><strong>仿函数（函数对象）与 lambda表达式的关系</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个简单的函数对象</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Add</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Add</span>(<span class="type">int</span> z)</span><br><span class="line">        :_z(z)</span><br><span class="line">        &#123;&#125;    </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line"><span class="keyword">return</span> x + y + _z;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> z = <span class="number">1</span>;</span><br><span class="line"><span class="comment">// 函数对象</span></span><br><span class="line"><span class="function">Add <span class="title">add</span><span class="params">(<span class="number">1</span>)</span></span>;</span><br><span class="line">cout &lt;&lt; <span class="built_in">add</span>(<span class="number">2</span>, <span class="number">3</span>) &lt;&lt; endl;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// lambda表达式</span></span><br><span class="line"><span class="keyword">auto</span> lam = [z](<span class="type">int</span> a, <span class="type">int</span> b)-&gt;<span class="type">int</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">return</span> a + b + z;</span><br><span class="line">&#125;;</span><br><span class="line">cout &lt;&lt; <span class="built_in">lam</span>(<span class="number">2</span> ,<span class="number">3</span>) &lt;&lt; endl;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从使用方式上来看，函数对象与lambda表达式完全一样。实际在底层编译器对于lambda表达式的处理方式，完全就是按照函数对象的方式处理的，即：如 果定义了一个lambda表达式，编译器会自动生成一个类，在该类中重载了operator()。</p><p><img data-src="/transblog/images/410591618250244.png"><img data-src="/transblog/images/196151718268670.png"></p><h2 id="右值引用"><a href="#右值引用" class="headerlink" title="右值引用"></a>右值引用</h2><p>区分左值和右值的最主要的依据就是能不能取地址，即是否有实际的内存空间。左值持久，右值短暂：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//左值：</span></span><br><span class="line"><span class="type">int</span>* p = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">0</span>);</span><br><span class="line"><span class="type">int</span> b = <span class="number">1</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> c = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//右值：</span></span><br><span class="line"><span class="number">10</span>;</span><br><span class="line">x + y;</span><br><span class="line">x % y;</span><br></pre></td></tr></table></figure><p>左值引用是在C++的第一个标准C++98中设计的语法，如：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span>*&amp; rp = p;</span><br><span class="line"><span class="type">int</span>&amp; rb = b;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span>&amp; rc = c;</span><br><span class="line"><span class="type">int</span>&amp; pvalue = *p;</span><br></pre></td></tr></table></figure><p>右值引用是在C++11中设计的新语法，它的写法是下面这样的：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span>&amp;&amp; rr1 = <span class="number">10</span>;</span><br><span class="line"><span class="type">double</span>&amp;&amp; rr2 = x + y;</span><br><span class="line"><span class="type">double</span>&amp;&amp; rr3 = x % y;</span><br></pre></td></tr></table></figure><p>右值只能绑定到临时对象，所引用的对象将要销毁或该对象没有其他用户</p><p>我们在C++98中就知道，不能引用常量，现在来看，这句话应该是说，左值引用不能引用右值，比如下面这行代码是编译不通过的</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span>&amp; r1 = <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p>但是也有一些特殊之处，const左值引用可以给右值取别名，比如下面这行代码又是可以编译通过的</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span>&amp; r = <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p>move()函数一般用来将左值转换为右值，它的返回值是右值。因此，又有了一个特殊之处，右值引用可以给 move(左值）取别名，下面这行代码就可以编译通过</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="type">int</span>&amp;&amp; r = <span class="built_in">move</span>(x);</span><br></pre></td></tr></table></figure><h3 id="左值引用用来干什么"><a href="#左值引用用来干什么" class="headerlink" title="左值引用用来干什么"></a>左值引用用来干什么</h3><p>在函数的参数传递中，C语言中，函数如果要修改变量，就应该传参为指针，如果要修改指针，就应该传参二级指针，以此类推，这是一项繁琐低效的任务。通过取别名的方式而代替C语言的指针行为，本质的目的是为了减少不必要的拷贝负载，提高程序运行的效率。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">Func</span><span class="params">(<span class="type">const</span> string&amp; s)</span></span>;</span><br></pre></td></tr></table></figure><h3 id="右值引用用来干什么"><a href="#右值引用用来干什么" class="headerlink" title="右值引用用来干什么"></a>右值引用用来干什么</h3><p>解决函数返回时所产生的部分问题</p><p>在一个函数栈帧销毁后，函数的返回值如果是当前作用域之外的变量，并没有被销毁，我们就可以用引用作返回值。过去，返回值是普通类型时，会产生拷贝复制。而如果用引用作返回类型，可以直接返回，消耗很小。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">String <span class="title">Fun</span><span class="params">(String&amp; s)</span></span>&#123;</span><br><span class="line"><span class="function">String <span class="title">ret</span><span class="params">(s)</span></span>;</span><br><span class="line"><span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function">String <span class="title">s1</span><span class="params">(<span class="string">&quot;左值&quot;</span>)</span></span>;</span><br><span class="line">String s2 = <span class="built_in">Fun</span>(s1);    <span class="comment">//会调用String类的拷贝构造函数，进行深拷贝。</span></span><br></pre></td></tr></table></figure><p>但是当我们想返回的值是当前函数的局部变量时，函数栈帧销毁后，局部变量也会被销毁，左值引用是无法给这个值取别名的，只能传值返回，这个过程存在拷贝复制的消耗。</p><p>因此，C++11设计新的语法规则右值引用来减少这部分的拷贝复制。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">String&amp;&amp; <span class="title">Fun</span><span class="params">(String&amp; s)</span></span>&#123;</span><br><span class="line"><span class="function">String <span class="title">ret</span><span class="params">(s)</span></span>;</span><br><span class="line"><span class="keyword">return</span> std::<span class="built_in">move</span>(ret);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>虽然可以编译通过，但是右值引用这样使用未免有点“使劲没使到位的感觉”。正确的写法是在类中新增一个构造函数，称为移动构造函数， 这个构造函数不同于拷贝构造函数实现的深拷贝，它只是转移资源，将右值的资源粘贴到我们需要的地方。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span> <span class="comment">// for std::move</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    std::string data;</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">const</span> std::string&amp; d) : <span class="built_in">data</span>(d) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Copy Constructor called--1\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">const</span> MyClass&amp; other) : <span class="built_in">data</span>(other.data) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Copy Constructor called--2\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">MyClass</span>(MyClass&amp;&amp; other) <span class="keyword">noexcept</span> : <span class="built_in">data</span>(std::<span class="built_in">move</span>(other.data)) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Move Constructor called\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">MyClass <span class="title">obj1</span><span class="params">(<span class="string">&quot;Hello&quot;</span>)</span></span>; <span class="comment">// Copy Constructor called--1</span></span><br><span class="line">    MyClass obj2 = std::<span class="built_in">move</span>(obj1); <span class="comment">// Move Constructor called</span></span><br><span class="line">    MyClass obj3 = obj1; <span class="comment">// Copy Constructor called--2</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>右值优先进入移动构造函数而不是拷贝构造函数。而移动构造函数与拷贝构造不同，它并不是重新分配一块新的空间，将要拷贝的对象复制过来，而是”偷”了过来，将自己的指针指向别人的资源，然后将别人的指针修改为nullptr，这一步很重要，如果不将别人的指针修改为空，那么临时对象析构的时候就会释放掉这个资源，”偷”也白偷了。</p><p>如果我们没有提供移动构造函数，只提供了拷贝构造函数，std::move()会失效但是不会发生错误，因为编译器找不到移动构造函数就去寻找拷贝构造函数，也这是拷贝构造函数的参数是const T&amp;常量左值引用的原因！</p><h2 id="智能指针"><a href="#智能指针" class="headerlink" title="智能指针"></a>智能指针</h2><p><strong>为什么要使用智能指针</strong>？</p><ul><li>申请的空间在函数结束时忘记释放，造成内存泄漏。</li><li>使用智能指针可以很大程度上的避免这个问题，因为智能指针就是一个类，当超出了类的作用域是，类会自动调用析构函数，析构函数会自动释放资源，不需要手动释放内存空间。</li><li>智能指针是一个类对象，这样在被调函数执行完，程序过期时，对象将会被删除（对象的名字保存在栈变量中），这样不仅对象会被删除，它指向的内存也会被删除的。</li></ul><h3 id="auto-ptr"><a href="#auto-ptr" class="headerlink" title="auto_ptr"></a>auto_ptr</h3><p>（c98 的方案，c11已经抛弃）采用所有权模式</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">auto_ptr&lt;string&gt; <span class="title">p1</span> <span class="params">(<span class="keyword">new</span> string (<span class="string">&quot;I reigned lonely as a cloud.”));</span></span></span></span><br><span class="line"><span class="string"><span class="params"><span class="function">auto_ptr&lt;string&gt; p2;</span></span></span></span><br><span class="line"><span class="string"><span class="params"><span class="function">p2 = p1; //auto_ptr 不会报错. </span></span></span></span><br></pre></td></tr></table></figure><p>此时不会报错，p2 剥夺了 p1 的所有权，但是当程序运行时访问 p1 将会报错。所以 auto_ptr的缺点是：存在潜在的内存崩溃问题！</p><h3 id="unique-ptr"><a href="#unique-ptr" class="headerlink" title="unique_ptr"></a>unique_ptr</h3><p>用来替换 auto_ptr，采用所有权模式<br>unique_ptr 实现独占式拥有或严格拥有概念，保证同一时间内只有一个智能指针可以指向该对象。它对于避免资源泄露(例如“以 new 创建对象后因为发生异常而忘记调用 delete”)特别有用。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">unique_ptr&lt;string&gt; <span class="title">p3</span> <span class="params">(<span class="keyword">new</span> string (<span class="string">&quot;auto&quot;</span>))</span></span>; <span class="comment">//#4</span></span><br><span class="line">unique_ptr&lt;string&gt; p4； <span class="comment">//#5</span></span><br><span class="line">p4 = p3;<span class="comment">//此时会报错！！</span></span><br></pre></td></tr></table></figure><p>编译器认为 p4&#x3D;p3 非法，避免了 p3 不再指向有效数据的问题。因此，unique_ptr 比 auto_ptr更安全。<br>另外 unique_ptr 还有更聪明的地方：当程序试图将一个 unique_ptr 赋值给另一个时，如果源 unique_ptr 是个临时右值，编译器允许这么做；如果源 unique_ptr 将存在一段时间，编译器将禁止这么做，比如：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">unique_ptr&lt;string&gt; <span class="title">pu1</span><span class="params">(<span class="keyword">new</span> string (<span class="string">&quot;hello world&quot;</span>))</span></span>;</span><br><span class="line">unique_ptr&lt;string&gt; pu2;</span><br><span class="line">pu2 = pu1; <span class="comment">// #1 not allowed</span></span><br><span class="line">unique_ptr&lt;string&gt; pu3;</span><br><span class="line">pu3 = <span class="built_in">unique_ptr</span>&lt;string&gt;(<span class="keyword">new</span> <span class="built_in">string</span> (<span class="string">&quot;You&quot;</span>));  <span class="comment">// #2 allowed</span></span><br></pre></td></tr></table></figure><p>其中#1 留下悬挂的 unique_ptr(pu1)，这可能导致危害。而#2 不会留下悬挂的 unique_ptr，因为它调用 unique_ptr 的构造函数，该构造函数创建的临时对象在其所有权让给 pu3 后就会被销毁。这种随情况而已的行为表明，unique_ptr 优于允许两种赋值的 auto_ptr 。<br>注：如果确实想执行类似与#1 的操作，要安全的重用这种指针，可给它赋新值。C++有一个标准库函数 std::move()，让你能够将一个 unique_ptr 赋给另一个。例如：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">unique_ptr&lt;string&gt; ps1, ps2;</span><br><span class="line">ps1 = <span class="built_in">demo</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line">ps2 = <span class="built_in">move</span>(ps1);</span><br><span class="line">ps1 = <span class="built_in">demo</span>(<span class="string">&quot;alexia&quot;</span>);</span><br><span class="line">cout &lt;&lt; *ps2 &lt;&lt; *ps1 &lt;&lt; endl;</span><br></pre></td></tr></table></figure><h3 id="shared-ptr"><a href="#shared-ptr" class="headerlink" title="shared_ptr"></a>shared_ptr</h3><p>实现共享式拥有概念</p><ul><li>多个智能指针可以指向相同对象，该对象和其相关资源会在“最后一个引用被销毁”时候释放。从名字 share 就可以看出了资源可以被多个指针共享，它使用计数机制来表明资源被几个指针共享。</li><li>除了可以通过 new 来构造，还可以通过传入 auto _ptr, unique_ptr,weak_ptr 来构造。当我们调用 release()时，当前指针会释放资源所有权，计数减一。当计数等于 0 时，资源会被释放。</li><li>shared_ptr 是为了解决 auto_ptr 在对象所有权上的局限性(auto_ptr 是独占的), 在使用引用计数的机制上提供了可以共享所有权的智能指针。</li></ul><p>成员函数：</p><ul><li>use_count():返回引用计数的个数，即资源的所有者个数。</li><li>unique()：返回是否是独占所有权( use_count 为 1)</li><li>swap()：交换两个 shared_ptr 对象(即交换所拥有的对象)</li><li>reset()：放弃内部对象的所有权或拥有对象的变更, 会引起原有对象的引用计数的减少</li><li>get()：返回内部对象(指针), 由于已经重载了()方法，因此和直接使用对象是一样的。<code>shared_ptr&lt;int&gt; sp(new int(1)); </code>如<code>sp</code>与<code>sp.get()</code>是等价的</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span>* rawPtr = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">10</span>);</span><br><span class="line">    <span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(rawPtr)</span></span>;</span><br><span class="line">    std::shared_ptr&lt;<span class="type">int</span>&gt; ptr1 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">20</span>);</span><br><span class="line">    std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2 = ptr1; <span class="comment">// ptr2 现在也指向同一个对象，引用计数变为2</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>shared_ptr存在循环引用的问题，会造成死锁，资源无法释放，需要用到weak_ptr:</strong></p><h3 id="weak-ptr"><a href="#weak-ptr" class="headerlink" title="weak_ptr"></a>weak_ptr</h3><ul><li>weak_ptr 是一种不控制对象生命周期的智能指针, 它指向一个 shared_ptr 管理的对象，进行该对象的内存管理的是那个强引用的 shared_ptr，weak_ptr 只是提供了对管理对象的一个访问手段。</li><li>weak_ptr 设计的目的是为配合 shared_ptr 而引入的一种智能指针，用来协助shared_ptr 工作，<strong>它只可以从一个 shared_ptr 或另一个 weak_ptr 对象构造,，它的构造和析构不会引起引用记数的增加或减少</strong>。</li><li>weak_ptr 是用来解决 shared_ptr 相互引用时的死锁问题，如果说两个 shared _ptr 相互引用，那么这两个指针的引用计数永远不可能下降为 0，资源永远不会释放。<br>它是对对象的一种弱引用，不会增加对象的引用计数，和 shared_ptr 之间可以相互转化，shared_ptr 可以直接赋值给它，它可以通过调用 lock 函数来获得 shared_ptr。</li></ul><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    shared_ptr&lt;B&gt; pb_;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">A</span>() &#123; cout &lt;&lt; <span class="string">&quot;A delete\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    shared_ptr&lt;A&gt; pa_;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">B</span>() &#123; cout &lt;&lt; <span class="string">&quot;B delete\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">shared_ptr&lt;B&gt; <span class="title">pb</span><span class="params">(<span class="keyword">new</span> B())</span></span>;</span><br><span class="line">    <span class="function">shared_ptr&lt;A&gt; <span class="title">pa</span><span class="params">(<span class="keyword">new</span> A())</span></span>;</span><br><span class="line">    pb-&gt;pa_ = pa;</span><br><span class="line">    pa-&gt;pb_ = pb;</span><br><span class="line">    cout &lt;&lt; pb.<span class="built_in">use_count</span>() &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; pa.<span class="built_in">use_count</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">fun</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以看到 fun 函数中 pa ，pb 之间互相引用，两个资源的引用计数为 2，当要跳出函数时，智能指针 pa，pb 析构时两个资源引用计数会减一，但是两者引用计数还是为 1，导致跳出函数时资源没有被释放（A B 的析构函数没有被调用）</p><p>把其中一个改为 weak_ptr，我们把类 A 里面的<code>shared_ptr pb</code>改为<code>weak_ptr pb</code>; 这样的话，资源 B 的引用开始就只有 1，当 pb 析构时，B 的计数变为 0，B 得到释放，B 释放的同时也会使 A 的计数减 1，同时 pa 析构时使 A 的计数减 1，那么 A 的计数为 0，A 得到释放。</p><p>注意我们不能通过 weak_ptr 直接访问对象的方法，比如 B 对象中有一个方法 print(), 我们不能这样访问：<code>pa-&gt;pb-&gt;print()</code>;  pb是一个 weak_ptr，应该先把它转化为shared_ptr,如：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">shared_ptr p = pa-&gt;pb.<span class="built_in">lock</span>(); </span><br><span class="line">p-&gt;<span class="built_in">print</span>();</span><br></pre></td></tr></table></figure><link rel="stylesheet" href="/transblog/css/spoiler.css" type="text/css"><script src="/transblog/js/spoiler.js" type="text/javascript" async></script>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;面向对象特性&quot;&gt;&lt;a href=&quot;#面向对象特性&quot; class=&quot;headerlink&quot; title=&quot;面向对象特性&quot;&gt;&lt;/a&gt;面向对象特性&lt;/h1&gt;&lt;h2 id=&quot;封装&quot;&gt;&lt;a href=&quot;#封装&quot; class=&quot;headerlink&quot; title=&quot;封装&quot;&gt;&lt;</summary>
      
    
    
    
    <category term="语言" scheme="https://trans-xu.github.io/transblog/categories/%E8%AF%AD%E8%A8%80/"/>
    
    
  </entry>
  
</feed>
